# 2026-05-24

- Codex 套餐徽章：`AttributionCard` 的 badge 区现在由上层传入 `PLUS` / `PRO` / `TEAM` 等套餐标签。账号池通过 `buildAccountAttributionBadges(account, quotaDisplay)` 统一生成套餐 + 格式 badges，优先使用 quota `planType`，再回退账号记录 `planType`；Codex 请求顺序卡片复用 `buildCodexQuotaSummaryAccount` 后解析同一套餐标签。badge 文案进一步收敛为短名显示 + `title` 全名，格式 badge 使用 `ANTH` / `OAI CHAT` / `OAI RESP` / `GEM`。
- 账号筛选菜单重设计：`AccountsToolbar` 现按来源 / 资源 / 状态 / 套餐四组对象状态筛选，默认全选；套餐选项基于账号数据中的 `availablePlanTypes` 动态显示，保持 `全部` 回退。相关过滤模型、选择器、文案和测试已同步更新，`npm --prefix frontend run typecheck` 与 `npm --prefix frontend run test:unit` 已通过。
- 验证：`node --test frontend/src/features/accounts/tests/accountPresentation.test.mjs frontend/src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过。
- 2026-05-24 字号偏好：Codex live sessions workbench 和 Sidebar 的字体偏小，后续同类密集工作台优先从字号 token 和行级标签入手做增大，保持 Swiss/brutalist 视觉但提高可读性。
- 2026-05-24 quota 展示切换：`QuotaBars` 的 percent/token 切换入口从右侧数值按钮改为整块 quota 区域可点击/可键盘切换；仅在窗口存在 `usedTokens`/`limitTokens` 时启用 `role="button"`、`aria-pressed` 与 `Enter`/`Space` 切换。
- CLIProxyAPI 上游同步：`docs-linhay/references/CLIProxyAPI#gettokens/sidecar` 已合并 `upstream/main@50d19e20`（`v7.1.20-1`），生成并推送 merge commit `1c5db246` 到 `AxApp/CLIProxyAPI#gettokens/sidecar`。冲突保留 `internal/registry/model_definitions_test.go` 的 GetTokens fork 回归测试，并把 Antigravity credits 测试的 loadCodeAssist host 断言更新为上游 prod host。
- CLIProxyAPI 验证：子仓库 `go test ./internal/registry`、`go test ./internal/runtime/executor`、`go test ./...`、`git diff --check` 均通过；已执行 `./scripts/ensure-sidecar.sh darwin arm64` 重建本地 sidecar，meta 为 `commit=1c5db246`、`dirty=clean`。
- Codex TUI `Failed to read thread goal` 本机修复：根因是 GetTokens 托管 PATH 当前启用 `codex-cli 0.130.0`，该二进制仍按旧 schema 查询 `~/.codex/state_5.sqlite.thread_goals`；而参考源码中的 state migration `0034_drop_thread_goals.sql` 已删除旧表，并把 goals 迁到 `~/.codex/goals_1.sqlite`。已用 SQLite `.backup` 备份 `state_5.sqlite` / `goals_1.sqlite`，再在 `state_5.sqlite` 补回旧版 TUI 兼容表 `thread_goals`；用户确认 TUI 报错消失。备份文件：`~/.codex/state_5.sqlite.before-thread-goals-compat-20260524.bak`、`~/.codex/goals_1.sqlite.before-thread-goals-compat-20260524.bak`。
- 账号池分组与排序模式规划：`#frame=accounts` 后续需要从固定 provider 分组升级为可切换分组模式，并新增排序模式。默认按套餐分组、按业务优先级排序；P0 分组维度为套餐、来源、状态，P0 排序维度为业务优先级、名称、状态、剩余额度；P1 再规划供应商/资源状态分组与重置时间/最近使用排序。分组和排序只改变列表组织方式，不改变筛选、选择、路由、轮动、禁用或导出语义；详细规划写入 `docs-linhay/spaces/account-pool/plans/20260524-account-grouping-mode-plan-v01.md`，入口 README 已同步。
- 账号池分组与排序实现：`#frame=accounts` 主列表已新增分组/排序工具栏入口，默认 `group=plan`、`sort=priority`；支持套餐/来源/状态/供应商/资源分组，以及业务优先级/名称/状态/剩余额度/重置时间排序。顶层 hash canonicalizer 已保留 `group` / `sort`，避免切换后被还原成 `#frame=accounts`。验证：`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit` 通过；浏览器验证 `#frame=accounts&group=source&sort=quota` 可恢复“分组 · 来源 / 排序 · 剩余额度”，截图归档到 `docs-linhay/spaces/account-pool/screenshots/20260524/accounts/20260524-accounts-group-sort-toolbar-after-v01.png`。
- Codex MCP 配置字段补齐：按 `docs-linhay/references/codex/codex-rs/config/src/mcp_types.rs` 与 `mcp_edit.rs` 校准后，`MCP Servers` 编辑器补齐 `environment_id`、`oauth.client_id` 与可直接新增的 `startup_timeout_sec` / `tool_timeout_sec` 等字段；后端解析和保存 `[mcp_servers.<id>.oauth] client_id`，并保持 nested tools、未知字段和非目标配置不被重写。验证：`go test .`、`go test ./internal/wailsapp -run 'Codex|Mcp|Skill'`、`go test ./internal/wailsapp -run 'TestGetCodexMcpServersParsesSectionServers|TestSaveCodexMcpServerPatchesTargetSectionOnly'`、`npm run test:unit -- src/features/codex-extensions/model.test.mjs`、`npm run typecheck` 通过；Chrome 本地页面确认 `startup_timeout_sec = 20` 可产生变更预览，截图归档到 `docs-linhay/spaces/20260511-cc-switch-codex-skills-mcp/screenshots/20260524/codex-extensions/20260524-codex-extensions-mcp-config-fields-after-v01.png`。
- Account Routing Engine 规划：新建 `docs-linhay/spaces/20260524-account-routing-engine/`，确定自定义端点路由不做普通 HTTP middleware，而是收敛为 `AccountRoutingEngine`：`RouteContext -> CandidateProvider -> PolicyPipeline -> SelectorAdapter -> DecisionTrace -> Executor`。实施拆为 P0 seam/hook 安装、P1 compiled route snapshot + endpoint policy、P2 Wails/management API、P3 前端路由工作台、P4 shadow mode/event ledger、P5 上游合并治理；技术边界写入 `docs-linhay/dev/20260524-account-routing-engine.md`，调用图归档到该 space screenshots。
- Account Routing Engine 范围补充：本次 rollout 必须同时一次性清理既有账号路由逻辑，不能让新 endpoint policy 和旧 `RoutePolicy` / `rateLimitPolicy` / `SessionAffinitySelector` 长期并行。清理清单写入 `docs-linhay/spaces/20260524-account-routing-engine/plans/legacy-routing-cleanup-v01.md`，要求补齐 hook 安装点、收敛 rate-limit 双路径、将 RoutePolicy 作为 RequestPolicy 兼容层、将 session affinity 迁移为 StickyPolicy，并保留 WebSocket request-boundary 特例但统一重选入口。
- Account Routing Engine 产品语义简化：核心改为“可路由账号池 + 三种路由模式”。可路由池由账号激活/禁用、可请求/异常、账号排序值、账号归属组、组级启停和组级排序决定；有效排序为 `group.routeOrder -> account.routeOrder -> stable account id`。主路由模式只保留 `sequential` 顺序、`balanced` 按当前会话数/请求数均衡、`project` 按项目名绑定账号组或账号；`dedicated/prefer/ordered/weighted/canary` 直接剔除出新逻辑，只作为合并上游功能时的兼容边界保留。`exclude` 不作为路由模式，只允许作为请求级 deny 或目标池过滤条件。
- Account Routing Engine ownership 补充：总账号池不负责编排账号轮动，只负责账号/账号组增删改查、启停、弃用、组级启停和组级排序等资产操作；`codex - 账号列表` 和 `claude - 账号列表` 分别负责各自渠道的账号请求顺序、`sequential/balanced/project` 路由模式、项目绑定、路由说明、dry-run/explain 和路由探测。渠道内排序和模式不反写成总账号池属性，Codex 与 Claude 配置互不影响。
