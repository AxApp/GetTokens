# 2026-05-12

## Codex 二进制管理确认决策

- 用户确认按推荐方案推进 Codex 二进制管理首期实现。
- 首期平台固定为 macOS arm64 / amd64；Windows/Linux、企业镜像、自定义 repo / URL、断点续传全部后置。
- 远端源固定为 `openai/codex` GitHub Releases，tag 前缀固定 `rust-v`。
- 下载校验策略：下载后必须计算本地 sha256；上游 checksum asset 首期可记录但不强依赖。
- 下载并发策略：UI 只允许一个活跃下载任务，后端按 `sourceId + tag` 去重复用任务。
- 版本说明策略：前端新增 `react-markdown` + `rehype-sanitize` 安全渲染 release notes，后端继续返回 `bodyPlainText` 作为降级字段；禁止直接注入 HTML。
- 产品入口：放在 Codex 二级菜单下独立入口 `二进制管理`，不挂到账号池、local apply、用量或会话页面。

## Codex 二进制管理实现启动

- 已完成首个实现切片：`internal/codexbinary` 领域服务支持 snapshot、本地导入、sha256 去重、`codex --version` 检测、激活 symlink、版本说明缓存。
- Wails root App 已暴露 Codex Binary 的 snapshot/refresh/import/use/notes/doctor 方法，并重新生成 `frontend/wailsjs`。
- 前端新增 `frontend/src/features/codex-binary/`，Codex 二级菜单新增 `二进制管理`；普通浏览器支持 `?preview=codex-binary#frame=codex&workspace=binary-management` 预览，不依赖 Wails runtime。
- 验证结果：`go test ./...`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 均通过；`agent-browser` 桌面与 390px 移动 preview 冒烟通过，页面错误为空。
- GitHub 发布列表：已接入 REST API，并在匿名 rate limit 403 时回退 `releases.atom`；`http://localhost:34115/#frame=codex&workspace=binary-management` 已验证可显示 `rust-v0.131.0-alpha.*` 列表。
- 继续完成真实下载激活切片：`DownloadCodexBinary` 支持解析 release、优先选择 `codex-<platform>.tar.gz`、下载/解包/导入/激活；Atom 回退缺少 asset 时会读取 GitHub `expanded_assets/<tag>` 补下载链接。
- 前端版本 cell 的“下载并激活”已接入真实 Wails 方法；真实 Wails 页面已验证 release 列表和按钮可执行状态，并归档截图 `20260512-codex-binary-wails-download-enabled-after-v01.png`。验收时未点击真实下载，以避免改变本机当前 Codex 托管版本。
- 用户指出缺少“启用托管”入口；已补齐 `managedConfig` 和顶部摘要：显示托管 PATH 是否启用、托管目录、当前 `codex` 解析路径和实际写入配置路径，并提供“一键托管”按钮。
- 用户进一步指出不能假设用户一定使用 `~/.zshrc`；已调整为后端按 `$SHELL`、`ZDOTDIR`、`XDG_CONFIG_HOME` 和现有 profile 文件识别目标：zsh 优先现有 `.zshrc` / `.zprofile`，bash 优先现有 `.bashrc` / `.bash_profile` / `.profile`，fish 写入 `config.fish`，未知 shell 按平台回退。写入只维护 GetTokens 标记块，已有 profile 先备份，重复点击幂等；POSIX block 使用 `export PATH='<managed-bin>':"$PATH"` 保证 `$PATH` 在新终端运行时展开。
- 用户指出 Codex 二进制管理顶部状态面板太乱；按 `$debate` 与 Gemini 讨论后裁定问题根因是“当前版本、状态 pill、动作按钮、路径元信息、消息提示混排”。已重构为主状态、独立动作区、低权重元信息脚注三段；下载/激活仍保留在版本 cell 上，顶部只负责 orient 和一键托管入口。
- 待实现：取消下载、Reveal in Finder、下载 event 推送。

## Codex Skills GitHub / GitLab 安装与更新

- 用户新增要求：Skills 支持从 GitHub 直接安装，schema 使用 `tk://github.com/<owner>/<repo>?ref=<ref>&path=<skill-dir>`，示例 `tk://github.com/ln/xxx?ref=main&path=skills/foo`。
- 用户进一步确认可顺带支持 GitLab 源；首期扩展为 GitHub + GitLab 显式 schema，GitLab 支持 nested group，例如 `tk://gitlab.com/f2e/axure-helper/axure-skill-group?ref=main&path=skills/foo`，自建 GitLab 需 host allowlist。
- 安装策略：使用 `git clone` 拉到 GetTokens 自有 managed skill cache，校验指定 path 下存在合法 `SKILL.md` 后再链接或复制到 Codex 可扫描 root；禁止直接 clone 到用户 skill root。
- 更新策略：记录 source schema、remote、ref、path、resolved commit；用户触发 `Check Updates` 时 `git fetch` 并比对 commit，有更新则预览后再确认更新，失败保留当前安装版本。
- 设计约束：Skills 顶部使用扁平 source 输入区和 Install / Check Updates，不做多层卡片嵌套，不做 marketplace 搜索或后台自动更新。
- Nolon 经验吸收：安装链路拆成 managed repository/cache 与 Codex 可扫描 materialized root 两层；clone path 使用 `<host>/<namespace>@<repo>` 稳定身份；manifest/lock 记录 source、provider、ref、skillPath、commit/hash、version、displayName、materializedPath 与 materializeMethod，作为更新、回滚和安全删除依据。
- 已重做该 space 的业务文档、技术方案、实施计划和单文件设计稿：业务 README 聚焦范围/非目标/BDD，技术方案落到 `docs-linhay/dev/codex-skills-mcp-architecture-2026-05-12.md`，设计稿改为本地配置控制台风格，侧边栏拆 `Skills` / `MCP Servers`，右侧按会话页面组织，避免多层卡片嵌套。
- 用户补充约束：实现时优先使用已有通用组件样式。已在业务 README、技术方案和实施计划中明确映射 `WorkspacePageHeader`、`SegmentedControl`、`ActionSelect`、`btn-swiss`、`input-swiss`、`select-swiss`、通用 Toggle/Switch 与 `StatusCodexFeaturesSection` 面板模式；设计稿增加 Production mapping 提示并改用对应 class 名称。

## Codex 账号列表行交互收口

- `AccountOrderRow` 的拖拽职责收窄到左侧顺位轨，`article` 本身不再 draggable，行主体只负责打开详情。
- 启停继续使用 switch，点击 switch 不触发详情；模型映射详情维持真实模型 `models[].name` -> Codex 模型 `models[].alias || name`。
- 浏览器验收：`agent-browser` 验证顺位轨可拖拽、拖拽柄点击不打开详情、行主体点击打开详情、拖拽排序后出现未保存提示，控制台无 error。

## Codex 账号列表行视觉密度调整

- 用户确认截图中的账号行视觉过重，需要继续由 Codex 或 `$debate` + Gemini 协作修改。
- 采用合作型 debate：Gemini 建议压缩行高、弱化左侧拖拽柄、把状态色改成整行边缘、将请求状态与 switch 靠近展示；Codex 裁定不合并“请求可用状态”和“账号启停状态”语义，只调整视觉位置。
- `AccountOrderRow` 更新为高密度工作台行：整行左边缘显示请求状态色，左侧只保留顺位数字和拖拽图标，中间展示账号与出口，右侧组合请求状态 badge 与启停 switch；窄屏保持两列布局以避免 switch 区隐式列错位。

## Codex 账号详情模型映射编辑

- 用户指出 `CodexAccountDetailModal` 模型映射区无法添加映射。
- 决策：只对 openai-compatible 账号开放详情 modal 内的模型映射新增、删除、保存；provider 基础信息仍在账号池入口维护。
- 映射保存语义保持真实模型 `models[].name` -> Codex 模型 `models[].alias || name`：详情输入的真实模型写入 `name`，Codex 模型写入 `alias`；Codex 模型与真实模型相同时落空 alias，继续同名透传。
- 浏览器 preview 保存只更新本地行数据；桌面环境通过 `UpdateOpenAICompatibleProvider` 写回 provider 后 reload。

## Codex 账号详情 URL Detail 约定修正

- 用户指出 Codex 账号详情 modal 打开后 URL 没有按约定保留 frame/detail 标记；根因是 Codex 账号详情新 modal 漏接 `detail=<account-id>` hash，同步时全局导航也可能规范化 hash 并清掉 modal detail。
- 已补齐 `buildCodexDetailFrameHash` / `clearCodexDetailFrameHash` 与 Codex 账号列表接线：打开详情写入 `#frame=codex&workspace=account-list&detail=<account-id>`，关闭详情只移除 `detail`。
- 全局 `buildFrameHash` 增加可选 detail 参数，`useAppNavigation` 仅在当前 page/workspace 未变时保留 detail，避免直接打开带 detail 的 URL 后 modal 被导航同步误关。
- 验证通过：前端单测 237 项、`npm run typecheck`、浏览器点击打开/直接打开/关闭 modal 三条 URL 流程，浏览器 errors 为空。

## Codex 账号列表行设计评审

- 用户给出账号列表截图并要求与 Gemini 商量设计。
- Gemini 与 Codex 共识：当前行被虚线分隔和下沉 switch 拆成上下两段，像表单卡片而不是请求顺序列表；应回到单行三列 `rank / account+route / switch`。
- 保留项：整行左侧状态色、source badge、mono endpoint、左侧拖拽柄、switch 独立交互。
- 调整方向：移除行内虚线分隔，压低行高，减轻 rank 方块重量，switch 始终固定右侧同一水平扫描线。

## Codex Skills / MCP Web 预览实现

- 已完成首个 Web 端实现切片：Codex 侧边栏新增 `Skills` / `MCP Servers`，`frontend/src/features/codex-extensions/` 提供无 Wails runtime 也可运行的浏览器预览页面。
- 通用 Toggle 已抽到 `frontend/src/components/ui/ToggleSwitch.tsx`，并复用到 Codex 账号列表和功能开关面板；后续新页面不得再复制手写 switch。
- Skills 页面支持本地预览数据、root/filter/search、启停预览、去 front matter 的 `SKILL.md` 预览、`tk://github.com` / `tk://gitlab.com` schema 校验和安装/更新预览。
- MCP 页面按列表模式实现，不包含 `Config Groups`；支持单 server 参数编辑、`bearer_token_env_var` 字段、env/args 解析、change preview 和本地保存预览。
- 验证通过：`npm run test:unit`、`npm run typecheck`、`npm run build`；浏览器冒烟归档在 `docs-linhay/spaces/20260511-cc-switch-codex-skills-mcp/screenshots/20260512/codex-extensions/`。
- 风险/后续：当前是前端 Web 预览切片，真实 Codex roots 扫描、TOML 保守 patch、Git clone/fetch/install/update 仍需后端 Wails 接口落地。

## Codex Skills / MCP 后端基础切片

- 已补 `internal/wailsapp/codex_extensions.go`：真实扫描 Codex skill roots、解析 `SKILL.md` front matter、返回去 front matter preview、读取/写回 path 型 `[[skills.config]] enabled=false`。
- 已补 Git source schema parser：支持 `tk://github.com/<owner>/<repo>?ref=<ref>&path=<skill-dir>` 与 `tk://gitlab.com/<namespace>/<repo>?ref=<ref>&path=<skill-dir>` nested group；仍未执行真实 clone/fetch。
- MCP 基础实现支持读取 section 型 `[mcp_servers.<id>]`，按 `command` / `url` 推断 `stdio` / `streamable_http`；保存时 patch 单个 section，移除无效 `bearer_token`，保留未知字段和非目标配置。
- Wails root `App`、`app_types.go`、`app_mappers.go`、`frontend/wailsjs` 已同步；前端 Codex Extensions 页面在桌面环境优先调用真实接口，浏览器环境继续使用 preview data。
- 验证通过：`go test ./...`、`npm run test:unit`、`npm run typecheck`、`npm run build`；浏览器 fallback 冒烟通过，仅有既有 `favicon.ico 404`。
- 后续重点：Git managed cache/manifest/materialize/update/rollback、MCP inline table 兼容、自建 GitLab allowlist 和真实 Wails 桌面验收截图。

## Codex MCP tools 子表解析修正

- 用户提供 `chrome-devtools` MCP 示例，包含 `[mcp_servers.chrome-devtools.tools.<tool>] approval_mode = "approve"` 多个子表；该配置按 Codex 语义是一个 MCP server，而不是多个 server。
- Codex 参考确认：`McpServerConfig` 有 `tools: HashMap<String, McpServerToolConfig>`，官方测试 `replace_mcp_servers_serializes_per_tool_approval_overrides` 会生成 `[mcp_servers.docs.tools.read]` 并读回为一个 `docs` server。
- 根因：`readCodexMcpDocument` 之前把所有 `[mcp_servers.<...>]` header 都加入 server 列表，未区分一级 server table 与 nested tool policy table。
- 修复：新增 `parseCodexMcpServerSectionID` / `splitTomlDottedPath`，只接受 `[mcp_servers.<id>]` 一级表，跳过 `[mcp_servers.<id>.tools.<tool>]`；新增回归测试覆盖 `chrome-devtools` 示例。
- 验证通过：新增测试先红灯复现 4 个 server，修复后 `go test ./internal/wailsapp -run 'TestGetCodexMcpServers(TreatsToolApprovalSectionsAsNestedConfig|ParsesSectionServers)|TestSaveCodexMcpServerPatchesTargetSectionOnly' -count=1` 与 `go test ./...` 均通过。

## Codex MCP config.toml 直接编辑入口

- 用户要求在 `CodexMcpServersWorkspace` header 按钮区增加编辑按钮，直接编辑 Codex `config.toml`。
- 第一版先做成系统编辑器打开，用户纠正浏览器也可以直接内置编辑器；已调整为点击 `编辑 config.toml` 打开内置文本编辑 modal。
- 桌面环境新增 `GetCodexConfigToml` / `SaveCodexConfigToml` Wails 方法，读取和保存真实 `~/.codex/config.toml`；浏览器预览环境使用内置 preview 文本，不访问本机文件系统。
- 保存真实 config 后会重新加载 MCP Servers 列表，保证 raw editor 与结构化 MCP 列表重新同步。
- 验证通过：`go test ./...`、`go test ./internal/wailsapp`、`npm run typecheck`、`npm run test:unit`、`npm run build`；浏览器预览确认按钮可见、modal 打开、textarea 可编辑、保存后显示 preview 已保存，浏览器 errors 为空。

## Codex Skills 详情交互修正

- 用户指出 Skills 详情应是 modal/detail layer，而不是页面右侧常驻详情。
- 已清理 `CodexSkillsWorkspace` 中残留的双栏详情 JSX，主视图改为全宽列表；点击 `详细参数` 后弹出 `SkillPreviewModal` 展示元信息、文件列表和去 front matter 的 `SKILL.md` 预览。
- 浏览器验收确认 `role=dialog` 存在，overlay 为 fixed 全屏遮罩，无水平溢出；截图归档为 `20260512-codex-extensions-skills-modal-web-after-v02.png`。
- 用户进一步确认 Skills 列表不需要单独的“详细参数”按钮；已改为点击整条 skill 行打开详情，ToggleSwitch 保持独立点击且不触发详情。
- 用户进一步确认 Skills 列表行不需要额外状态/来源小标签；列表只保留名称、描述、路径和右侧 Toggle，来源等元信息放在详情 modal。
- 用户补充要求：Skills 列表的路径行需要明确标注来源；本地 skill 显示 `来源: <rootPath>`，Git skill 显示 `Git: <tk://...>`，且 Git 地址不能被 uppercase 转换。
- Skills 行交互继续优化：取消 `article role=button` 内嵌 switch 的结构，改为行内透明覆盖按钮负责打开详情，右侧 ToggleSwitch 保持独立层级，避免交互语义嵌套。

## Codex Extensions modal 与 Markdown 预览收口

- 用户指出 MCP server 编辑区也应是独立 modal 页面，不应常驻在 MCP 列表右侧；已将 `CodexMcpServersWorkspace` 改为列表主视图，点击 server 行后打开 `McpServerEditorModal`，modal 内保留启停、字段编辑、change preview、保存和取消。
- 用户指出 Skill preview 需要支持 Markdown 或其他格式渲染；项目已有 `react-markdown@10.1.0` 和 `rehype-sanitize@6.0.0`，已复用这两个依赖对 `SKILL.md` 去 front matter 后的正文做安全 Markdown 渲染。
- 浏览器验收：Skills modal 中出现真实 `h1` / `ol` 节点且不再是纯 `<pre>`；MCP 页面点击 server 后出现 `MODAL_CODEX_MCP_SERVER_EDITOR`，主列表不再有常驻 `变更预览` 面板；控制台仅有既有 `favicon.ico 404`。
