# 2026-06-20

## Proxy pool UI polish closure

- 用户在 `#frame=proxy-pool` 连续指出代理池页面细节：页头、摘要、工具栏、测速网址输入、执行按钮、表格行、菜单按钮和列表操作需要按新的工作台风格收敛。
- 已将代理池页头摘要移入 `WorkspacePageHeader.meta`，页头 actions 改为纯图标按钮并放大图标；工具栏改为搜索 + AntD `AutoComplete` 测速网址 + 纯播放图标执行按钮，执行按钮接入 `handleBatchRetest`。
- 已移除旧的 datalist/历史标签按钮和 Swiss-style 厚描边表格外观，表格、分页、批量选择、行操作和状态 pill 统一到 `--gt-*` surface/border/status token。
- 新增 `frontend/src/features/proxy-pool/proxyPoolLayout.test.mjs`，用源码/DOM 契约锁住关键结构：summary meta、toolbar `contents`、AntD AutoComplete、播放按钮、图标-only header actions、表格新外壳和旧 wrapper 缺失。
- 验证：`node --test frontend/src/features/proxy-pool/model.test.mjs frontend/src/features/proxy-pool/proxyPoolLayout.test.mjs`、`npm run typecheck`、`antd lint frontend/src/features/proxy-pool/ProxyPoolFeature.tsx --format json`、`git diff --check` 均通过；浏览器只读复核确认页头按钮 `40x40` / 图标 `20x20`、测速执行按钮 `44x44`、输入框与按钮间距 `8px`、相关输入文字 `14px`。

## Pinpoint browser polish loop distilled

- 沉淀来源：本轮多次根据 in-app browser 的具体文件/组件/元素定位做局部 UI 微调；仅靠视觉查看容易丢失已修过的重叠、字号、图标-only 按钮和 AntD 内部显示字号。
- 已沉淀到 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Pinpoint browser polish loop`：细粒度浏览器定位微调要补轻量源码/DOM 断言，AntD 组件先查 API/lint，浏览器验收读 bounding rect 和 computed style，图标按钮必须保留 `aria-label/title`。
- 不升级 `AGENTS.md`：这是 GetTokens 前端设计/验收单领域的可复用模式，放在前端设计质量 skill 足够；repo-wide 长期硬约束已有浏览器验收、测试门禁和文档沉淀规则覆盖。

## Session management quiet workspace pass

- 目标来源：持续按账号池/代理池形成的新工作台风格推进剩余界面；旧样式扫描显示 `frontend/src/features/session-management/SessionManagementView.tsx` 是真实运行页面中旧样式信号最集中的入口之一。
- 本轮完成：会话管理主壳层切到 `--gt-surface-*` / `--gt-border-*` quiet token，页头摘要进入 `WorkspacePageHeader.meta`，分析/刷新改为 40px icon-only 按钮；搜索 rail、项目列、会话列和会话行收敛为轻描边列表。
- 追加完成：分析范围选择、分析结果详情、Provider 归并、会话详情 4 个 modal 统一为 `data-session-management-modal` + `sessionManagementModal*` 新 shell，移除旧 `btn-swiss` 和 `border-4` modal 外壳。
- 追加完成：`SessionPluginConsolePanel` 设计系统业务预览统一到 `data-session-plugin-console-*` + `--gt-*` quiet shell，移除旧 thick border、Swiss button 和 heavy uppercase，并新增 `sessionPluginConsolePanel.test.mjs` 锁定结构。
- 浏览器验收发现第一次实现后主 workbench 被列表内容撑到 19677px；已通过外层 `min-h-0` + `overflow-hidden` 修正为内部滚动，并将该全高工作台 bounding rect 检查补入前端设计质量 skill。
- 验证：`node --test frontend/src/features/session-management/model.test.mjs frontend/src/features/session-management/cache.test.mjs`、`npm run typecheck`、`git diff --check` 通过；in-app browser 复核 workbench `903x837`、两列 `763px` 高、页头按钮 `40x40` / 图标 `20x20`、旧 feature `btn-swiss` 计数 0、可见旧粗框壳层计数 0。

## Extension registry quiet workspace pass

- 目标来源：继续按“扫描旧样式信号 -> 选真实页面 -> 加门禁 -> 收敛 -> 提交”的节奏推进剩余界面；旧样式扫描显示 `frontend/src/features/gettokens-extension-registry/GetTokensExtensionRegistryFeature.tsx` 是 Codex workspace 中相对独立且信号集中的页面。
- 本轮完成：Extension Registry 主列表、toolbar chips、notice、右侧 Codex config dry-run、staged temp apply、roots、registry diagnostics、selected extension 区统一到 `--gt-*` quiet shell，移除旧粗边框、`bg-main/bg-surface` 壳层和 heavy uppercase。
- 新增门禁：`featureSource.test.mjs` 增加 `extension registry feature uses the quiet workspace shell`，锁定 `extensionRegistryButtonClass`、`extensionRegistryPanelClass`、`data-gettokens-extension-registry-list-header`、`data-gettokens-extension-registry-aside`、`data-gettokens-extension-registry-selected` 标记和旧样式禁止项。

## Doctor workbench quiet workspace pass

- 目标来源：继续推进剩余真实页面；旧样式扫描显示 `frontend/src/features/doctor-workbench/DoctorWorkbenchFeature.tsx` 是 Codex workspace 中旧粗框、hard shadow、heavy uppercase 信号最集中的独立页面之一，且已有 focused tests 与 preview gate。
- 本轮完成：Doctor Workbench 主 summary、safe action surface、evidence ledger、source boundary、filter、check list、structured route evidence、navigation links 与 core acceptance rail 统一到 `--gt-*` quiet shell；只读诊断、route recheck 受控动作、extension real apply 禁止边界未改变。
- 新增门禁：`doctorWorkbenchEntry.test.mjs` 增加 `doctor workbench uses the quiet workspace shell`，锁定 `doctorPanelClass`、`doctorMutedPanelClass`、`data-doctor-workbench-shell/check-list/core-acceptance`，并禁止旧粗边框、hard shadow、`bg-main/bg-surface` 和 heavy uppercase。
- 验证：`node --test frontend/src/features/doctor-workbench/tests/doctorWorkbench.test.mjs frontend/src/features/doctor-workbench/tests/doctorWorkbenchEntry.test.mjs` 通过；常规 Chrome `--dump-dom` 路径本轮卡在 headless 进程，已中断后用 `CHROME_EXECUTABLE_PATH=/nonexistent/chrome node docs-linhay/scripts/check-doctor-workbench-preview.mjs` 走归档 snapshot/screenshot fallback，所有 read-only / structured route / no mutation / OmniRoute source 检查为 true。

## Quiet workspace migration loop distilled

- 沉淀来源：本轮连续完成代理池、会话管理、会话插件控制台、扩展注册表和 Doctor workbench 的 quiet workspace 小步迁移；相同节奏重复出现，包括旧样式扫描、聚焦源码/DOM 门禁、`--gt-*` shell 常量、无头/浏览器验收、space + memory 写回和每轮单独提交。
- 已沉淀到 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Quiet workspace migration loop`，作为后续状态页、账号详情等剩余页面迁移的执行标准。
- 同步更新 `docs-linhay/spaces/20260519-theme-skinning/README.md` 当前状态；不升级 `AGENTS.md`，因为该规则仍是 GetTokens 前端/Wails UI 领域流程，现有 repo-wide 测试、文档和沉淀硬约束已覆盖触发与收口。

## Status panels quiet workspace pass

- 目标来源：继续推进剩余界面；旧样式扫描显示 `frontend/src/features/status/components/StatusPanels.tsx` 是状态页中 `card-swiss`、`btn-swiss`、粗边框和 `bg-main/bg-surface` 旧壳层最集中的主面板。
- 本轮完成：`StatusQuotaEvidenceSection`、`StatusApplyLocalSection`、recovery notice/buttons、Claude toggle rows、Endpoint picker 和 Codex model catalog preview modal 统一到 `statusPanelClass/statusMutedPanelClass/statusPrimaryButtonClass` 等 `--gt-*` quiet shell；同时收敛 `ActionSelect` 小图标按钮和 `StatusSnippetPanel` header/copy 按钮，避免旧 `btn-swiss` 从嵌套组件重新渲入状态主面板；业务 handler、local apply draft、diff 生成和 model catalog preview 数据流未改变。
- 新增门禁：`statusTypography.test.mjs` 增加 `status panels use the quiet workspace shell`，锁定新 shared class、`data-status-local-cli-panel`、`data-status-local-cli-target` 和旧 `card-swiss` / `btn-swiss` / 粗边框壳层禁止项；同步更新 model catalog 表格分隔测试到 `--gt-border-subtle`。
- 验证：`node --test frontend/src/features/status/tests/statusTypography.test.mjs frontend/src/features/status/tests/relayLocalState.test.mjs` 通过；浏览器只读复核 `data-status-local-cli-panel=true`、`data-status-local-cli-target=codex`，嵌套旧 `btn-swiss/card-swiss` 计数为 0；本轮不新增 skill，直接复用已沉淀的 quiet workspace migration loop。

## Status Codex features quiet workspace pass

- 目标来源：状态页主面板收敛后，旧样式扫描显示 `frontend/src/features/status/components/StatusCodexFeaturesSection.tsx` 仍有集中粗边框、`btn-swiss`、heavy uppercase、`bg-main/bg-surface` 壳层，是同一状态页内下一块高信号真实 section。
- 本轮完成：Codex features panel、multi_agent_v2 complex panel、feature object cards、stage filters、message、preview 和 footer actions 统一到 `codexFeaturePanelClass/codexFeatureMutedPanelClass/codexFeaturePrimaryButtonClass` 等 `--gt-*` quiet shell；保留分组、路径标签、multi_agent_v2 分区、preview/save/reset handler 和 value editor 数据流。
- 门禁更新：`statusTypography.test.mjs` 的 `codex feature rows render a settings table and a grouped multi_agent_v2 complex panel` 同步锁定新 shared class、`--gt-surface-canvas`、`--gt-border-subtle` 和旧 `btn-swiss` / `border-2 border-color` / `bg-main/bg-surface` 禁止项。
- 验证：`node --test frontend/src/features/status/tests/*.test.mjs`、`npm run typecheck`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 通过；浏览器 DOM 只读检查曾尝试定位 `data-codex-feature-config-panel`，但当前 dev runtime 未渲染该子面板，浏览器证据不作为本轮主门禁；本轮不新增 skill，继续复用 quiet workspace migration loop。

## Status Codex config sibling sections quiet workspace pass

- 目标来源：继续收敛状态页剩余 Codex 配置区；旧样式扫描显示 `StatusCodexRootSettingsSection.tsx`、`StatusCodexModelProvidersSection.tsx`、`StatusCodexNoticeSection.tsx` 和共享 `StatusCodexConfigRows.tsx` 仍有 `btn-swiss`、粗描边、`bg-main/bg-surface`、heavy uppercase，是同一状态页内强相关 sibling sections。
- 本轮完成：root settings、model providers、notices 三个 section 的 panel/header/message/group header/preview/footer/actions 统一到 `codexConfigSectionPanelClass/codexConfigSectionPrimaryButtonClass` 等 `--gt-*` quiet shell；共享 rows 改为轻分隔和 token chip；保留 group、path display、value editor、preview/save/reset handler 和 remove draft 数据流。
- 门禁更新：`statusTypography.test.mjs` 增加 `codex config sibling sections use the quiet workspace shell`，并把 `codex config rows render as a settings table` 更新为锁定 `codexConfigRowsChipClass`、`--gt-border-subtle`、`--gt-surface-muted` 与旧粗描边/旧背景禁止项。
- 验证：红灯为 `node --test frontend/src/features/status/tests/statusTypography.test.mjs` 两个新增/更新断言失败；实现后 `node --test frontend/src/features/status/tests/*.test.mjs`、`npm run typecheck`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 通过，目标四个文件旧样式扫描无命中；本轮不新增 skill，继续复用 quiet workspace migration loop。

## Status Relay editor modals quiet workspace pass

- 目标来源：状态页 runtime 子区继续收敛；旧样式扫描显示 `frontend/src/features/status/components/RelayEditors.tsx` 的 service key、provider、model 三个编辑弹窗仍有 `btn-swiss`、粗描边、hard shadow、`bg-main/bg-surface` 和 heavy uppercase。
- 本轮完成：三个 Relay editor modal 统一到 `relayEditorModalPanelClass/relayEditorPrimaryButtonClass` 等 `--gt-*` quiet shell，补 `data-status-relay-editor-modal` 标记；输入、生成按钮、错误提示、footer actions 改为轻描边 token 样式；保留 `onClose/onChange/onSubmit`、随机 key 生成、rename disabled 和 error 清理逻辑。
- 门禁更新：`statusTypography.test.mjs` 增加 `status relay editor modals use the quiet workspace shell`，锁定新 modal 常量、`--gt-surface-canvas`、`--gt-border-subtle`、`--gt-status-danger` 和旧 Swiss/粗边框/旧背景/heavy uppercase/hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/status/tests/statusTypography.test.mjs` 新断言失败；实现后 `node --test frontend/src/features/status/tests/*.test.mjs`、`npm run typecheck`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用 quiet workspace migration loop。

## Vendor Status quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/vendor-status/VendorStatusFeature.tsx` 仍有 `card-swiss`、`btn-swiss`、粗描边、`bg-main/bg-surface` 和 heavy uppercase，集中在 hero、页头 actions、loading/error、metadata 和 uptime matrix。
- 本轮完成：Vendor Status 主壳层、active/operational hero、官方/订阅/历史 actions、loading/error、metadata row 和 uptime matrix 统一到 `vendorStatusPanelClass/vendorStatusPrimaryButtonClass` 等 `--gt-*` quiet shell；保留 RSS/summary fetch、cache、preview fallback、view model 和外链行为。
- 新增门禁：`featureSource.test.mjs` 增加 `vendor status feature uses the quiet workspace shell`，锁定 `data-vendor-status-shell/hero/matrix`、panel/button constants、`--gt-*` status token，并禁止旧 Swiss/粗边框/旧背景/heavy uppercase/hard shadow 信号。
- 验证：红灯为 `node --test frontend/src/features/vendor-status/*.test.mjs` 新断言失败；实现后同命令通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Codex live session detail quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/codex-live-sessions/components/CodexLiveSessionDetail.tsx` 仍有 75 个旧样式信号，集中在 overview cards、趋势图、metrics、timeline、transport lane、详情 modal 和 status badge。
- 本轮完成：Codex live session 详情和 overview shell 统一到 `codexLivePanelClass/codexLiveMutedPanelClass/codexLiveSecondaryButtonClass` 等 `--gt-*` quiet shell；保留 detail history loading、request timing trend、timeline detail modal、AttributionCard 账号卡复用和 polling/model 数据流。
- 新增门禁：`featureSource.test.mjs` 增加 `codex live session detail uses the quiet workspace shell`，锁定 `data-codex-live-detail-shell/overview-shell/timeline-shell`、共享常量、`--gt-*` status token，并禁止旧 Swiss/粗边框/旧背景/heavy uppercase/hard shadow 信号；同步更新既有源码断言到 `--gt-surface-canvas` 与 `codexLiveMutedPanelClass`。
- 验证：红灯为 `node --test frontend/src/features/codex-live-sessions/*.test.mjs` 新断言失败；实现后同命令 70 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Channel Routing quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/channel-routing/components/ChannelRoutingWorkbench.tsx` 仍有 70 个旧样式信号，集中在请求模式 header、高级诊断、当前/对比模式、真实决策、route resilience action、帮助 modal 和策略按钮。
- 本轮完成：Channel Routing 工作台主 shell、header、诊断区、route decision rows、route resilience target/action/response/history、帮助 modal 和 strategy buttons 统一到 `channelRoutingPanelClass/channelRoutingFieldClass/channelRoutingSecondaryButtonClass` 等 `--gt-*` quiet shell；保留 explain、shadow route mode、project/model select、`RunRouteResilienceAction` 和 history state 数据流。
- 门禁更新：`channelRouting.test.mjs` 增加 `ChannelRoutingWorkbench uses the quiet workspace shell`，锁定 `data-channel-routing-shell/diagnostics/route-resilience`、共享常量和 `--gt-*` status token，并禁止旧 Swiss/粗边框/旧背景/heavy uppercase/hard shadow 信号；同步更新既有源码布局断言。
- 验证：红灯为 `node --test frontend/src/features/channel-routing/tests/channelRouting.test.mjs` 新断言失败；实现后同命令 39 条通过，`npm run typecheck`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 通过，目标文件旧样式扫描无命中；Playwright 只读复核 `#frame=codex&workspace=account-list` 中 `data-channel-routing-shell/diagnostics/route-resilience` 均存在且 shell 内旧 Swiss 类计数为 0；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Codex extensions modals quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/codex-extensions/McpModals.tsx` 与 `frontend/src/features/codex-extensions/SkillsModals.tsx` 仍集中残留旧 Swiss 控件、粗描边、旧背景、heavy uppercase 和 hard shadow，是 Codex Extensions 中强相关的一组弹窗 surface。
- 本轮完成：MCP server editor、config.toml editor、MCP preflight result、Skill git install、Skill preview/remove alert 和 success HUD 统一到 `codexExtensionModal*` / `codexSkillModal*` shared class，使用 `--gt-surface-*`、`--gt-border-*`、`--gt-status-*` token；保留 config dirty blocking、MCP preflight、skill file preview、toggle/remove/open finder 等业务 handler。
- 门禁更新：`featureSource.test.mjs` 增加 `Codex extension modals use the quiet workspace shell`，锁定 `data-codex-extension-mcp-modal/config-modal/skill-install-modal/skill-preview-modal/skill-remove-alert`、共享 class 常量和旧 Swiss/粗边框/旧背景/heavy uppercase/hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/codex-extensions/featureSource.test.mjs` 新断言失败；实现后 `node --test frontend/src/features/codex-extensions/*.test.mjs` 18 条通过，`npm run typecheck` 通过，目标两文件旧样式扫描无命中；Playwright 只读复核 `#frame=codex&workspace=skills` 的 Skill install modal、`#frame=codex&workspace=mcp-servers` 的 config.toml modal 和 MCP server modal 均存在新 data 标记且弹窗内旧 Swiss 类计数为 0；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Claude Code asset workbench quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/claude-code/components/ClaudeCodeAssetWorkbench.tsx` 仍有 66 个旧样式信号，集中在 metrics、status strip、skills/mcp rows、inline MCP edit form、diff/plan side panels 和 empty state。
- 本轮完成：Claude Code Skills / MCP Servers 工作台统一到 `claudeAssetPanelClass/claudeAssetMutedPanelClass/claudeAssetButtonClass/claudeAssetInputClass` 等 `--gt-*` quiet shell；保留 Wails snapshot fallback、workspace routing、MCP edit draft/save、diff preview 和 planned assets 数据流。
- 门禁更新：`claudeCodeAssetWorkbench.test.mjs` 增加 `Claude Code asset workbench uses the quiet workspace shell`，锁定 `data-claude-asset-workbench-shell/skill-matrix/mcp-matrix/diff-panel/plan-panel`、共享常量和 `--gt-*` status token，并禁止旧 Swiss/粗边框/旧背景/heavy uppercase/hard shadow 信号。
- 验证：红灯为 `node --test frontend/src/features/claude-code/claudeCodeAssetWorkbench.test.mjs` 新断言失败；实现后同命令通过，`npm run typecheck`、目标文件旧样式扫描通过；in-app browser 只读复核 `#frame=claude&workspace=skills` 和 `#frame=claude&workspace=mcp-servers` 中新 data 标记存在且 shell 内旧 Swiss 类计数为 0；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Codex route probe modal quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/codex/components/CodexRouteProbeCard.tsx` 仍有 33 个旧样式信号，集中在全屏 modal shell、header status bar、控制列、候选队列、terminal 和空态。
- 本轮完成：Codex route probe modal 统一到 `codexRouteProbePanelClass/codexRouteProbeButtonClass/codexRouteProbeBadgeClass` 等 `--gt-*` quiet shell；保留 `modal=route-probe` hash、Escape/overlay close、model combobox、单次/连续探测、reset、候选顺序和 stream line 数据流。
- 门禁更新：`codexAccountList.test.mjs` 增加 `Codex route probe modal uses the quiet workspace shell`，锁定 `data-codex-route-probe-shell/control-panel/candidate-queue/terminal`、共享常量和 `--gt-*` token，并禁止旧 Swiss/粗边框/旧背景/heavy uppercase/hard shadow 信号；同轮修正该测试文件中 `AccountCardFrame` 已迁移 AntD Card 后的陈旧断言。
- 验证：红灯为 `node --test frontend/src/features/codex/codexAccountList.test.mjs` 新 route probe 断言失败；实现后同命令 56 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；in-app browser 只读复核 `#frame=codex&workspace=account-list&modal=route-probe` 中新 data 标记存在、shell 内旧 Swiss 类计数为 0、modal rect 为 `1280x720`；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Quota threshold rule panel quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/accounts/components/QuotaThresholdRulePanel.tsx` 仍有 51 个旧样式信号，集中在停止规则壳层、Budget windows、definition form、规则表单、Route Guard simulation preview 和 reason trace。
- 本轮完成：Quota threshold rule panel 统一到 `quotaThresholdPanelClass/quotaThresholdMutedPanelClass/quotaThresholdButtonClass/quotaThresholdInputClass/quotaThresholdMetaClass` 等 `--gt-*` quiet shell；保留 quota threshold CRUD、budget window definition CRUD/preview、DSL JSON、route guard simulation 和 reason trace 数据流。
- 门禁更新：`quotaCalibrationBindings.test.mjs` 增加 `quota threshold rule panel uses the quiet workspace shell`，锁定 `data-account-quota-threshold-rule-panel/data-budget-window-definition-panel/data-route-guard-simulation-state`、共享常量和 `--gt-*` token，并禁止旧 Swiss input/button、粗边框、旧背景、heavy uppercase 信号。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/quotaCalibrationBindings.test.mjs` 新断言失败；实现后同命令 6 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Account local CLI apply confirm quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/accounts/components/AccountLocalCliApplyConfirm.tsx` 仍有 32 个旧样式信号，集中在 confirm header/footer、左侧 Codex/Claude 配置 rail、字段控件、文件列表和 preview 容器。
- 本轮完成：Account local CLI apply confirm 统一到 `accountLocalCliPanelClass/accountLocalCliMutedPanelClass/accountLocalCliButtonClass/accountLocalCliPrimaryButtonClass/accountLocalCliInputClass/accountLocalCliMetaClass` 等 `--gt-*` quiet shell；保留 Codex/Claude draft 编辑、auth strategy、model catalog、preview diff、apply blocking 和 result message 数据流。
- 门禁更新：`accountLocalCliMapping.test.mjs` 增加 `AccountLocalCliApplyConfirm uses the quiet workspace shell`，锁定 `data-account-local-cli-apply-confirm/data-account-local-cli-apply-rail/data-account-local-cli-file-list`、共享常量和 `--gt-*` token，并禁止旧 Swiss、粗边框、旧背景、heavy uppercase 信号。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountLocalCliMapping.test.mjs` 新断言失败；实现后同命令 28 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Account rotation config section quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/accounts/components/account-rotation/RotationConfigSection.tsx` 仍有 30 个旧样式信号，集中在 strategy select、dropdown options、retry inputs、unit suffix 和 routing toggle tiles。
- 本轮完成：Account rotation config section 统一到 `rotationConfigPanelClass/rotationConfigInputShellClass/rotationConfigInputClass/rotationConfigMetaClass` 等 `--gt-*` quiet shell；保留 routing strategy、session affinity TTL、retry numeric fields、max retry credentials/interval 和 toggle 写回 `routingDraft` 的状态流。
- 门禁更新：`accountRotation.test.mjs` 增加 `RotationConfigSection uses the quiet workspace shell`，锁定 `data-account-rotation-config-section/data-account-rotation-strategy-menu/data-account-rotation-toggle-grid`、共享常量和 `--gt-*` token，并禁止旧 select、粗边框、旧背景、heavy uppercase 信号。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountRotation.test.mjs` 新断言失败；实现后同命令 10 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Usage Desk panels quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/accounts/components/usage-desk/UsageDeskPanels.tsx` 仍有 28 个旧样式信号，集中在 StatePanel、InfoCard、quota evidence chips、session/project drilldown header、table header 和 table cell。
- 本轮完成：Usage Desk panels 统一到 `usageDeskPanelClass/usageDeskMutedPanelClass/usageDeskMetaClass/usageDeskTableHeaderClass` 等 `--gt-*` quiet shell；保留 explicit quota fact / missing quota fact 分支、session/project drilldown 表格列、空态和 usage value formatter 数据流。
- 门禁更新：`usageDesk.test.mjs` 增加 `UsageDeskPanels uses the quiet workspace shell`，锁定 `data-usage-desk-state-panel/data-usage-desk-info-card/data-usage-desk-session-drilldown/data-usage-desk-project-drilldown`、共享常量和 `--gt-*` token，并禁止旧粗边框、旧背景、heavy uppercase 和 hard shadow 信号。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/usageDesk.test.mjs` 新断言失败；实现后同命令 45 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Usage Desk chart quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/accounts/components/usage-desk/UsageDeskChart.tsx` 仍有 10 个旧样式信号，集中在 chart card shell、status/controls header、footer summary、chart surface、point ring 和 empty placeholder。
- 本轮完成：Usage Desk chart 统一到 `usageDeskChartShellClass/usageDeskChartSurfaceClass/usageDeskChartFooterClass/usageDeskChartPointRingClass` 等 `--gt-*` quiet shell；保留 chart grid background、curve animation、selected point auto-scroll、primary/secondary line、point labels 和 empty placeholder 数据流。
- 门禁更新：`usageDesk.test.mjs` 增加 `UsageDeskChart uses the quiet workspace shell`，锁定 `data-usage-desk-chart-card/data-usage-desk-chart-surface/data-usage-desk-empty-chart`、共享常量和 `--gt-*` token，并禁止旧粗边框、旧背景、heavy uppercase 和 hard shadow 信号。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/usageDesk.test.mjs` 新断言失败；实现后同命令 46 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Account detail header/runtime route quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `frontend/src/features/accounts/components/AccountDetailSections.tsx` 仍有 80 个旧样式信号，本轮先切出账号详情顶部 header、运行态路由诊断和 route resilience evidence marker，避免把 quota reset glass modal 与凭据/额度编辑区混进同一提交。
- 本轮完成：`AccountDetailHeader`、`AccountRuntimeRouteSection` 和 `RuntimeRouteResilienceEvidenceMarker` 统一到 `accountDetailHeaderShellClass/accountDetailHeaderPillClass/accountDetailRuntimeDecisionClass/accountDetailRuntimeEvidenceClass` 等 `--gt-*` quiet shell；保留账号类型/凭据/验证/路由/余额摘要、运行态 evidence grid、repair rows、真实路由 decision 和 route resilience digest 数据流。
- 门禁更新：新增 `accountDetailSections.test.mjs`，只截取本轮三个函数块，锁定新 shared class、`data-account-detail-header="quiet"`、`data-account-runtime-route-layout="summary"`、`data-account-runtime-route-resilience-marker` 与 `--gt-*` token，并禁止这些块回退到旧粗边框、旧背景、heavy uppercase 和 hard shadow。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDetailSections.test.mjs` 新断言失败；实现后同命令通过，相关 `openAIQuotaResetDetail`、`quotaCalibrationBindings`、`codexAccountList` 源码测试 68 条通过，`npm run typecheck` 通过；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Theme workspace closure distillation audit

- 触发来源：用户明确要求“整理，沉淀，提交”，且 2026-06-20 已连续完成多轮工作台 quiet workspace 迁移。
- 沉淀结论：可复用流程已落在 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Quiet workspace migration loop` 和 `Pinpoint browser polish loop`；本轮没有新的触发词、执行步骤或验证路径需要新增 skill。
- 层级判断：当前规则仍属于 GetTokens 前端/Wails 视觉收敛领域流程，不升级到 `AGENTS.md`；space 只更新当前状态和下一轮高信号入口，memory 记录本次收尾审计。
- 剩余入口：Account detail primitives、账号详情凭据/额度编辑区、Codex account order 仍可继续按“单页面/强相关组件组 + 红灯门禁 + 最小迁移 + 验证 + 小提交”推进。

## Account detail credential editor quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；space 已标出账号详情凭据/连接编辑区为下一轮高信号入口，旧样式扫描显示 `AccountDetailSections.tsx` 的 `AccountCredentialVerifySection`、协议 endpoint、代理路由、凭据输入、短消息验证和模型菜单仍有旧 Swiss 控件、粗边框、旧背景、heavy uppercase 和 hard shadow。
- 本轮完成：该组统一到 `accountDetailCredentialPaneDividerClass/accountDetailCredentialSectionTitleClass/accountDetailCredentialInputClass/accountDetailCredentialButtonClass/accountDetailCredentialMenuClass` 等 `--gt-*` quiet shell；保留 draft 写回、协议 endpoint 覆盖、proxy route draft、clipboard copy、verify model menu 和 `onVerify` 数据流。
- 门禁更新：`accountDetailSections.test.mjs` 增加 `account credential detail editor uses the quiet workspace shell`，只截取本轮函数块，锁定 `data-account-credential-verify-layout="quiet-split"`、capability/connection markers、共享 class 常量和旧 Swiss/粗边框/旧背景/heavy uppercase/hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDetailSections.test.mjs` 新断言失败；实现后同命令通过，相关 `openAIQuotaResetDetail`、`quotaCalibrationBindings`、`codexAccountList` 源码测试 69 条通过，`npm run typecheck` 通过；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Theme workspace closure after credential editor

- 触发来源：用户再次要求“整理，沉淀，提交”，当前工作区在 `style: align account detail credential editor` 后无未提交代码改动，需要把最新完成面和剩余入口写回。
- 审计候选：账号详情凭据编辑区继续复用了“扫描旧样式信号 -> 聚焦源码门禁红灯 -> `--gt-*` shared class -> focused tests/typecheck -> space + memory”的既有循环，没有新增跨领域流程、失败模式或 repo-wide 约束。
- 沉淀结论：不新增 `.agents/skills/`，不更新 `AGENTS.md`；复用边界仍归 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Quiet workspace migration loop` 管理。
- 后续入口：Account detail primitives、账号详情额度/余额编辑区、Codex account order 仍保留为下一批旧样式高信号候选；下一轮继续保持单页面/强相关组件组、小门禁、小提交。

## Account detail shell primitives quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；space 已标出 Account detail primitives 为下一轮高信号入口，旧样式扫描显示 `AccountDetailPrimitives.tsx` 仍有粗描边、旧背景和 heavy uppercase。本轮只切 `AccountDetailSectionHeader`、`AccountDetailSection`、`AccountDetailBody`、`AccountDetailModuleGrid`、`AccountDetailModuleStack` 壳层，不混入 stat/evidence/notice 后续切片。
- 本轮完成：账号详情共享 section/header/body/module stack 统一到 `accountDetailSectionHeaderDividerClass/accountDetailBandShellClass/accountDetailSectionShellBaseClass/accountDetailBodyClass/accountDetailModuleBandsClass` 等 `--gt-*` quiet shell；保留 band layout、rail controls、card/band/flow layout context、topBorder/headerDivider/bandActionDivider 数据语义。
- 门禁更新：`accountDetailLayout.test.mjs` 将旧 `v09 compact` 和 primitives readable 断言更新为 quiet shell 契约，并新增函数块级旧样式禁止项；同步修正凭据区 `quiet-split` 和 `CardSections` divider token 的历史断言。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 新 primitives 断言失败；实现后账号详情 layout/sections 以及 `openAIQuotaResetDetail`、`quotaCalibrationBindings`、`codexAccountList` 源码测试 128 条通过，`npm run typecheck` 通过。目标文件旧样式扫描仍显示 stat/evidence/notice 区块残留，已保留为下一轮候选；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Account detail stat/evidence primitives quiet workspace pass

- 目标来源：继续收敛上一轮留下的 `AccountDetailPrimitives.tsx` 下半段；旧样式扫描集中在 `AccountDetailStatGrid/Cell`、`AccountDetailFieldGrid/Field`、`AccountDetailPill`、`AccountDetailNotice`、`AccountDetailEmptyState` 和 `AccountDetailEvidenceGrid`。
- 本轮完成：stat、field、pill、notice、empty 和 evidence primitives 统一到 `accountDetailStatGridClass/accountDetailStatCellClass/accountDetailFieldGridClass/accountDetailPillClass/accountDetailNoticeClass/accountDetailEvidenceGridClass` 等 `--gt-*` quiet shell；tone class 从旧 `--color-status-*` 切到 `--gt-status-*`，保留 neutral/success/warning/danger 语义和 compact evidence rows。
- 门禁更新：`accountDetailLayout.test.mjs` 增加 `account detail stat and evidence primitives use the quiet workspace shell`，只截取 primitives 下半段和 tone map，锁定共享常量、`--gt-*` token，并禁止旧粗描边、旧背景、dashed divider、heavy uppercase 和旧 status token。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 新 stat/evidence primitives 断言失败；实现后账号详情 layout/sections 以及 `openAIQuotaResetDetail`、`quotaCalibrationBindings`、`codexAccountList` 源码测试 129 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill，继续复用已沉淀的 quiet workspace migration loop。

## Account detail quota/billing editors quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进账号详情剩余编辑区；旧样式扫描显示 `AccountDetailSections.tsx` 的 quota/billing curl controls、script panes、reset credit panel、runtime balance rows 和 `RuntimeKV` 仍有 `btn-swiss`、粗描边、旧背景、dashed divider、heavy uppercase 和旧 status token。本轮明确排除 `OpenAIQuotaResetConfirmationModal`，该弹窗已有动态渐变和 glass 视觉契约。
- 本轮完成：`AccountQuotaSection`、`AccountBillingSection` 和 `RuntimeKV` 统一到 `accountDetailResourcePaneDividerClass/accountDetailResourceScriptCardClass/accountDetailResourceButtonClass/accountDetailResourceHeadingClass/accountDetailResourceMessageClass/accountDetailResourceKvLabelClass` 等 `--gt-*` quiet shell；保留 quota/billing curl editor hash route、脚本测试、OpenAI reset credit 查询入口和 runtime/test 数据展示语义。
- 门禁更新：`accountDetailSections.test.mjs` 增加 `account quota and billing editors use the quiet workspace shell`，只截取 quota/billing/resource kv 目标函数块，锁定 `data-account-quota-layout`、script pane、reset credit panel、`AccountCurlEditorModal` 和旧 Swiss/粗描边/旧背景/heavy uppercase/hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDetailSections.test.mjs` 新断言失败；实现后账号详情 sections/layout、`openAIQuotaResetDetail`、`quotaCalibrationBindings`、`codexAccountList` 相关测试 130 条通过，`npm run typecheck` 通过；本轮整理结论是不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Codex account order toolbar/filter quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实工作台；旧样式扫描显示 `CodexAccountOrderSection.tsx` 的 toolbar、filter menu、active condition chips、filter options、reset button 和 density switch 仍有 `btn-swiss`、粗描边、旧背景、dashed divider、heavy uppercase 和 hard shadow。本轮只切 order toolbar/filter 控件壳层，不混入 row card、detail modal 或 stories。
- 本轮完成：Codex account order toolbar/filter 统一到 `CODEX_ACCOUNT_ORDER_FILTER_BUTTON_CLASS/CODEX_ACCOUNT_ORDER_FILTER_MENU_CLASS/CODEX_ACCOUNT_ORDER_FILTER_CHIP_CLASS/CODEX_ACCOUNT_ORDER_FILTER_OPTION_CLASS/CODEX_ACCOUNT_ORDER_DISPLAY_SWITCH_CLASS` 等 `--gt-*` quiet shell；保留统一筛选菜单、condition chip 删除、source/status/resource 筛选、刷新按钮和 density 写入 hash/localStorage 的行为。
- 门禁更新：`codexAccountList.test.mjs` 将 `Codex account order toolbar uses the unified filter menu instead of separate scope clusters` 升级为 quiet shell 契约，截取 `InlineActionControls` 到 `buildToolbarFilterLabel`，锁定共享常量、`--gt-*` token，并禁止旧 Swiss、粗描边、旧背景、dashed divider、heavy uppercase 和 hard shadow 信号。
- 验证：红灯为 `node --test frontend/src/features/codex/codexAccountList.test.mjs` 新断言失败；实现后 `codexAccountList` 56 条通过，账号详情 sections/layout + codex 相关测试 119 条通过，`npm run typecheck` 通过；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Account import page quiet workspace pass

- 目标来源：继续按 quiet workspace loop 推进剩余真实页面；旧样式扫描显示 `AccountImportPage.tsx` 仍有 28 个旧样式信号，集中在全页 header、error bar、input panel、dropzone、queue panel、footer 和导入按钮。本轮只切全页导入界面，不混入 `AccountImportModal` 或 deep-link import confirm。
- 本轮完成：`AccountImportPage` 统一到 `accountImportPageShellClass/accountImportHeaderClass/accountImportPanelClass/accountImportDropzoneClass/accountImportButtonClass/accountImportMetaChipClass` 等 `--gt-*` quiet shell；保留文件上传、拖拽导入、剪贴板读取、粘贴解析、queue summary、候选列表移除和 `submitAccountImport` 数据流。
- 门禁更新：`accountCardInteractions.test.mjs` 增加 `account import page uses the quiet workspace shell`，截取 `AccountImportPage` 主函数块，锁定 `data-account-import-page/header/input-panel/dropzone/queue-panel`、共享常量和 `--gt-*` token，并禁止旧 Swiss、粗描边、旧背景、dashed divider、heavy uppercase 和 hard shadow 信号。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountCardInteractions.test.mjs` 新断言失败；实现后 `accountCardInteractions` 18 条通过，账号导入/本地 CLI 相关测试 46 条通过，`npm run typecheck` 通过；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Account import modal quiet workspace pass

- 目标来源：继续收敛账号导入工作流；旧样式扫描显示 `AccountImportModal.tsx` 仍有 24 个旧样式信号，集中在 modal header、error、footer、input panel、dropzone、queue panel 和动作按钮。本轮只切 modal 入口，不混入 deep-link import confirm 或 curl editor。
- 本轮完成：`AccountImportModal` 统一到 `accountImportModalHeaderClass/accountImportModalPanelClass/accountImportModalDropzoneClass/accountImportModalButtonClass/accountImportModalMetaChipClass` 等 `--gt-*` quiet shell；保留 `ModalFrame`、文件上传、拖拽导入、剪贴板读取、粘贴解析、queue summary、候选列表移除和 `onSubmit` 数据流。
- 门禁更新：`accountCardInteractions.test.mjs` 增加 `account import modal uses the quiet workspace shell`，截取 `AccountImportModal` 主函数块，锁定 `data-account-import-modal-header/body/input-panel/dropzone/queue-panel`、共享常量和 `--gt-*` token，并禁止旧 Swiss、粗描边、旧背景、dashed divider、heavy uppercase 和 hard shadow 信号。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountCardInteractions.test.mjs` 新断言失败；实现后 `accountCardInteractions` 19 条通过，账号导入/本地 CLI 相关测试 47 条通过，`npm run typecheck` 通过；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Account curl editor modal quiet workspace pass

- 目标来源：继续收敛账号详情脚本编辑入口；旧样式扫描显示 `AccountCurlEditorModal.tsx` 仍有 19 个旧样式信号，集中在 modal header、footer、脚本编辑列、变量按钮、Cookie 指引和模板按钮。本轮只切 cURL editor modal，不混入 deep-link import confirm 或 reset 动态渐变弹窗。
- 本轮完成：`AccountCurlEditorModal` 统一到 `accountCurlEditorHeaderClass/accountCurlEditorPanelClass/accountCurlEditorButtonClass/accountCurlEditorTextareaClass/accountCurlEditorVariableButtonClass` 等 `--gt-*` quiet shell；保留启用开关、textarea 草稿写回、变量插入/复制、模板应用、Cookie 指引和 `ModalFrame` 关闭流。
- 门禁更新：`accountDetailLayout.test.mjs` 增加 `account curl editor modal uses the quiet workspace shell`，截取 `AccountCurlEditorModal` 主函数块，锁定 `data-account-curl-editor-header/body/script-panel/variable-grid/template-panel`、共享常量和旧 Swiss、粗描边、旧背景、heavy uppercase、hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 新断言失败；实现后 `accountDetailLayout` 61 条通过，账号详情 layout/sections/reset 相关测试 69 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Deep link import confirm quiet workspace pass

- 目标来源：继续收敛账号导入工作流；space 已标出 Deep link import confirm 为下一轮高信号入口，旧样式扫描显示 `DeepLinkAccountImportConfirm.tsx` 仍有 18 个旧样式信号，集中在 header、footer、summary tile、redacted URL、账号候选卡和 notice/result 状态。
- 本轮完成：`DeepLinkAccountImportConfirm` 统一到 `deepLinkImportHeaderClass/deepLinkImportButtonClass/deepLinkImportPanelClass/deepLinkImportSummaryTileClass/deepLinkImportNoticeToneClass` 等 `--gt-*` quiet shell；保留 preview/apply gating、preview mode 阻塞、warnings/blocking/result message、账号候选结果和 `ApplyDeepLinkImport` 调用边界。
- 门禁更新：`accountLocalCliMapping.test.mjs` 增加 `deep link account import confirm uses the quiet workspace shell`，截取 `DeepLinkAccountImportConfirm` 主函数块，锁定 `data-deep-link-import-confirm-header/body/summary/url/account-list`、共享常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountLocalCliMapping.test.mjs` 新断言失败；实现后本地 CLI/账号导入相关测试 48 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Account detail footer quiet workspace pass

- 目标来源：继续收敛账号详情剩余入口；旧样式扫描显示 `AccountDetailSections.tsx` 仍有 `AccountDetailFooter` 的旧 Swiss 按钮和 heavy uppercase 状态行。`OpenAIQuotaResetConfirmationModal` 已有 `data-openai-quota-reset-gradient="dynamic"` 与 glass treatment 测试，本轮不迁移该特殊弹窗。
- 本轮完成：`AccountDetailFooter` 统一到 `accountDetailFooterStatusClass/accountDetailFooterActionsClass/accountDetailFooterButtonClass/accountDetailFooterPrimaryButtonClass` 等 `--gt-*` quiet shell；保留关闭、保存禁用条件、missing fields、config/rate-limit dirty copy 和保存中状态。
- 门禁更新：`accountDetailLayout.test.mjs` 增加 `account detail footer uses the quiet workspace action shell`，只截取 `AccountDetailFooter` 到 `normalizeBillingDisplay` 的函数块，锁定 footer data marker、共享常量和旧 Swiss、旧背景、heavy uppercase 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 新断言失败；实现后 `accountDetailLayout` 62 条通过，账号详情 layout/sections/reset 相关测试 70 条通过，`npm run typecheck` 通过，footer 函数块旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## OpenAI-compatible provider card quiet workspace pass

- 目标来源：继续收敛账号工作台剩余真实运行组件；旧样式扫描显示 `OpenAICompatibleProviderCard.tsx` 仍有 22 个旧样式信号，集中在 provider card 自定义 body、base/runtime 顶区、metrics、models、verify summary 和 footer actions。本轮只切 provider card，不混入 compose modal 或 workspace header/empty state。
- 本轮完成：`OpenAICompatibleProviderCard` 统一到 `openAICompatibleProviderCardBodyClass/openAICompatibleProviderCardPanelClass/openAICompatibleProviderCardButtonClass/openAICompatibleProviderCardDangerButtonClass/openAICompatibleProviderCardStatusClass` 等 `--gt-*` quiet shell；保留 `AttributionCard` 外壳、rate-limit badge、usage summary、provider disable/delete/manage handlers、verify status copy、models preview 和 card ignore click 行为。
- 门禁更新：`openAICompatible.test.mjs` 增加 `openai compatible provider card uses the quiet workspace shell`，截取 provider card 主函数和 `RegionHead/MetricPanel`，锁定 `data-openai-compatible-provider-card-body/models/actions`、共享 class 常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/openAICompatible.test.mjs` 新断言失败；实现后 `openAICompatible` 通过，账号 presentation 相关测试通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## API key compose modal quiet workspace pass

- 目标来源：继续按 theme space 的 compose modal 高信号入口推进；旧样式扫描显示 `ApiKeyComposeModal.tsx` 仍有 31 个旧样式信号，集中在独立 modal shell、header、输入区、quota toggle、probe panel、model chips、status/error 和 footer actions。本轮只切 API key compose modal，不混入 `UnifiedComposeModal` 或 OpenAI-compatible workspace。
- 本轮完成：`ApiKeyComposeModal` 统一到 `apiKeyComposeOverlayClass/apiKeyComposePanelClass/apiKeyComposeInputClass/apiKeyComposeButtonClass/apiKeyComposePrimaryButtonClass/apiKeyComposeStatusClass` 等 `--gt-*` quiet shell；保留 label/base URL/API key/quota curl 草稿写回、model fetch、verify model、loading spinner、error copy、cancel/submit 行为。
- 门禁更新：`accountCardInteractions.test.mjs` 增加 `api key compose modal uses the quiet workspace shell`，锁定 `data-api-key-compose-modal/header/body/probe/footer`、共享 class 常量和旧 Swiss input/button、粗描边、旧背景、旧 status token、heavy uppercase、hard shadow 禁止项；同步修正该测试文件 `sourceBlock` 支持无结束标记，和其他账号源码测试一致。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountCardInteractions.test.mjs` 新断言失败；实现后 `accountCardInteractions`、账号 presentation/config 相关测试 84 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Unified compose modal quiet workspace pass

- 目标来源：继续收敛 theme space 标出的 compose modal 链路；旧样式扫描显示 `UnifiedComposeModal.tsx` 仍有 26 个旧样式信号，集中在 provider picker card、endpoint row、辅助凭据 helper、quota/billing curl card、modal header 和 footer actions。本轮切完整 `UnifiedComposeModal`，不混入 OpenAI-compatible workspace 或 Claude Code workbench。
- 本轮完成：`UnifiedComposeModal` 统一到 `unifiedComposeProviderCardClass/unifiedComposeProviderChipClass/unifiedComposeEndpointRowClass/unifiedComposeCurlButtonClass/unifiedComposeFooterPrimaryButtonClass` 等 `--gt-*` quiet shell；保留厂商搜索/选择、preset apply、endpoint overrides、辅助凭据、quota/billing curl editor、header back-to-presets 和 footer custom/submit 行为。
- 门禁更新：`accountDetailLayout.test.mjs` 增加 `unified compose modal uses the quiet workspace shell`，截取主函数、credential helper、curl section、header/footer，锁定 `data-unified-compose-provider-picker/grid/card`、endpoint row、curl card、header/footer markers，并禁止旧 Swiss、粗描边、旧背景、heavy uppercase、hard shadow 回退。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 新断言失败；实现后账号详情 layout、账号 config/presentation/card interaction 相关测试 147 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Claude Code account list workbench quiet workspace pass

- 目标来源：继续收敛 theme space 标出的 Claude Code account list workbench；旧样式扫描显示 `ClaudeCodeAccountListWorkbench.tsx` 仍有 32 个旧样式信号，集中在 workbench shell/header、metric cards、request queue rows、mapping/probe panels、official profile cards 和 status tones。本轮只切 Claude Code account list workbench，不混入 Claude Code asset workbench 或 OpenAI-compatible workspace。
- 本轮完成：`ClaudeCodeAccountListWorkbench` 统一到 `claudeAccountWorkbenchShellClass/claudeAccountPanelClass/claudeAccountButtonClass/claudeAccountStatusToneClass` 等 `--gt-*` quiet shell；保留 Anthropic-only filtering、requestable/blocked counts、route probe icon button aria/title、mapping rows、probe lines 和 official profile cards。
- 门禁更新：`claudeCodeAssetWorkbench.test.mjs` 增加 `Claude Code account list workbench uses the quiet workspace shell`，锁定 `data-claude-account-workbench-shell/header/queue/queue-row/mapping-panel/probe-panel/profile-list/profile-card`、共享 class 常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase、hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/claude-code/claudeCodeAssetWorkbench.test.mjs` 新断言失败；实现后 Claude Code 相关测试 16 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Project candidate pool rules quiet workspace pass

- 目标来源：继续收敛 theme space 标出的 Project candidate pool rules panel；旧样式扫描显示 `ProjectCandidatePoolRulesPanel.tsx` 仍有 21 个旧样式信号，集中在主动作、项目 select、允许账号列表、规则列表、行操作按钮、缺失账号状态和底部 message。同一入口 `ProjectCandidatePoolRulesModal.tsx` header 仍有旧 Swiss 关闭按钮，因此本轮作为同一 modal/panel 入口一起迁移。
- 本轮完成：`ProjectCandidatePoolRulesPanel` 和 `ProjectCandidatePoolRulesModal` 统一到 `projectCandidateRulesShellClass/projectCandidateRulesPanelClass/projectCandidateRulesButtonClass/projectCandidateRulesSelectClass/projectCandidateRulesModalButtonClass` 等 `--gt-*` quiet shell；保留 `primaryActionSlot` portal、项目历史选择、允许账号启用/排序、预演、启停、删除和 preview fallback message 行为。
- 门禁更新：`channelRouting.test.mjs` 将 `ProjectCandidatePoolRulesPanel stays flat inside the project config modal` 升级为 quiet workspace 契约，锁定 `data-project-candidate-pool-rules-panel/rule-draft/account-list/rule-list/rule-row/rules-message/modal-header`、共享 class 常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase、hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/channel-routing/tests/channelRouting.test.mjs` 新断言失败；实现后 channel-routing 39 条通过，`npm run typecheck` 通过，目标 panel/modal 旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Account delete overlay quiet workspace pass

- 目标来源：继续收敛 theme space 标出的 Account delete overlay；旧样式扫描显示 `accountDeleteOverlay.ts` 仍有 19 个旧样式信号，集中在 full/list 两种密度的危险态渐变、hard shadow、backdrop blur、Swiss 按钮、粗字重和旧 `--color-status-danger` token。本轮只切账号卡删除遮罩，不混入账号卡 frame、AttributionCard overlay frame 或 Unified account detail modal。
- 本轮完成：`buildAccountDeleteOverlay` 统一到 `accountDeleteOverlayShellClass/accountDeleteOverlayListShellClass/accountDeleteOverlayButtonClass/accountDeleteOverlayDangerButtonClass` 等 `--gt-*` quiet shell；保留 full/list 密度、`data-account-card-ignore-click`、点击/键盘阻断、账号来源摘要、取消/删除动作和删除确认文案。
- 门禁更新：`accountDeleteOverlay.test.mjs` 将 full/list overlay 断言升级为 quiet workspace 契约，锁定 `data-account-card-delete-overlay-section="header/details/actions"`、共享 class 常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase、hard shadow、backdrop blur 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/accountDeleteOverlay.test.mjs` 新断言失败；实现后账号删除遮罩、账号卡交互和账号 presentation 相关测试 59 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Account migration gate quiet workspace pass

- 目标来源：继续收敛 theme space 标出的 Account migration gate；旧样式扫描显示 `AccountMigrationGate.tsx` 仍有 24 个旧样式信号，集中在全屏 gate shell、loading overlay、header、刷新按钮、迁移列表 panel、kind chips、stats、notice line 和 footer 主动作。本轮只切账号迁移 gate，不混入 App 壳层或账号工作台页面。
- 本轮完成：`AccountMigrationGate` 统一到 `accountMigrationGateShellClass/accountMigrationGateHeaderClass/accountMigrationGatePanelClass/accountMigrationGateButtonClass/accountMigrationGatePrimaryButtonClass/accountMigrationGateNoticeToneClass` 等 `--gt-*` quiet shell；保留 preview mode、ready/block 判定、重新检查、迁移提交、旧源清理、warnings/error/message 和备份提示语义。
- 门禁更新：`model.test.mjs` 增加 `AccountMigrationGate uses the quiet workspace shell`，锁定 `data-account-migration-loading/gate/header/summary-panel/kind-list/stats/notices/footer`、共享 class 常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase、hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/account-migration/model.test.mjs` 新断言失败；实现后 account-migration 6 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Rate limit rules section quiet workspace pass

- 目标来源：继续收敛 theme space 标出的 Rate limit rules section；旧样式扫描显示 `RateLimitRulesSection.tsx` 仍有旧 Swiss 按钮、粗描边、旧 `--bg-*` 背景、旧 status token、heavy uppercase 和 hard shadow 信号，集中在规则列表、draft 表单、菜单、提示和新增/保存动作。本轮只切 rate-limit rules section，不混入账号详情 modal shell 或 quota calibration panel。
- 本轮完成：`RateLimitRulesSection` 统一到 `rateLimitRulesShellClass/rateLimitRulesPanelClass/rateLimitRulesButtonClass/rateLimitRulesInputClass/rateLimitRulesNoticeToneClass` 等 `--gt-*` quiet shell；保留规则加载、draft 编辑、规则启停/编辑/删除、新增保存、dirty callback、imperative save handle、preview fallback 和 API save 行为。
- 门禁更新：`rateLimit.test.mjs` 将 `rate limit rules section edits account-card rules without matchKey fallback` 升级为 quiet workspace 契约，锁定 `data-rate-limit-rules-section/list/rule-card/draft/message`、共享 class 常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase、hard shadow 禁止项；同步把同文件里交通统计行的历史 token 断言校准到当前 `--gt-*` 事实。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/rateLimit.test.mjs` 新断言失败；实现后 rate-limit 聚焦测试通过，账号详情 layout/sections/rate-limit 相关测试 78 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮整理结论是不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## Quota calibration panel quiet workspace pass

- 目标来源：继续收敛 theme space 标出的 Quota calibration panel；旧样式扫描显示 `QuotaCalibrationPanel.tsx` 仍有 18 个旧样式信号，集中在 panel 外壳、active calibration row、history divider、form labels、select/input、error 和 add/revoke/submit/cancel 按钮。本轮只切 quota calibration panel，不混入 quota threshold rule panel 或账号详情整体 modal shell。
- 本轮完成：`QuotaCalibrationPanel` 统一到 `quotaCalibrationPanelClass/quotaCalibrationItemClass/quotaCalibrationButtonClass/quotaCalibrationInputClass/quotaCalibrationMetaClass/quotaCalibrationErrorClass` 等 `--gt-*` quiet shell；保留 calibration 加载、active/history 分组、delta/set-effective 表单、window 选择、add/revoke 调用和错误提示行为。
- 门禁更新：`quotaCalibrationBindings.test.mjs` 增加 `quota calibration panel uses the quiet workspace shell`，锁定 `data-account-quota-calibration-panel`、`data-quota-calibration-item/history/form`、共享 class 常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase、hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/quotaCalibrationBindings.test.mjs` 新断言失败；实现后 quota calibration 聚焦测试通过，账号详情 sections/layout/calibration 相关测试 73 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮整理结论是不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。

## OpenAI-compatible compose modal quiet workspace pass

- 目标来源：继续收敛 theme space 标出的 OpenAI-compatible compose modal；旧样式扫描显示 `OpenAICompatibleComposeModal.tsx` 仍有 17 个旧样式信号，集中在 modal overlay/panel、header、preset/name/base URL/API key 表单、error 和 footer actions。本轮只切 OpenAI-compatible compose modal，不混入 OpenAI-compatible workspace 或统一账号详情 modal。
- 本轮完成：`OpenAICompatibleComposeModal` 统一到 `openAICompatibleComposeOverlayClass/openAICompatibleComposePanelClass/openAICompatibleComposeInputClass/openAICompatibleComposeButtonClass/openAICompatibleComposeErrorClass` 等 `--gt-*` quiet shell；保留 preset 切换、provider name/base URL/API key 草稿回调、error 展示、overlay click close、stop propagation 和 submit/cancel 行为。
- 门禁更新：`openAICompatible.test.mjs` 增加 `openai compatible compose modal uses the quiet workspace shell`，锁定 `data-openai-compatible-compose-modal/header/body/error/footer`、共享 class 常量和旧 Swiss、粗描边、旧背景、旧 status token、heavy uppercase、hard shadow 禁止项。
- 验证：红灯为 `node --test frontend/src/features/accounts/tests/openAICompatible.test.mjs` 新断言失败；实现后 OpenAI-compatible 聚焦测试通过，账号卡交互/账号详情/OpenAI-compatible 相关测试 107 条通过，`npm run typecheck` 通过，目标文件旧样式扫描无命中；本轮整理结论是不新增 skill、不更新 `AGENTS.md`，继续复用已沉淀的 quiet workspace migration loop。
