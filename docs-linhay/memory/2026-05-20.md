# 2026-05-20

## Claude Code 账号列表设计系统收编

- 决策：Claude Code 账号列表先以纯展示业务组件进入设计系统，入口为 `Design System/业务组件/Claude Code 账号列表`。
- 实现：新增 `frontend/src/features/claude-code/components/ClaudeCodeAccountListWorkbench.tsx` 与对应 Story，覆盖 `ready`、`source-conflict`、`disabled-blocked`、`profile-draft`。
- 边界：Story 只用 mock 数据展示 Anthropic 格式账号筛选、请求顺序、官方默认 profile、模型映射草稿和路由探测，不接 Wails、sidecar 或真实网络请求。
- 语义保持：官网默认值是权威默认；官方文档列出的其他模型只称为“官方可切换模型”，不再称为默认候选。
- 验证：`node --test frontend/src/features/design-system/storyCatalog.test.mjs` 与 `npm --prefix frontend run typecheck` 均已通过。

## Claude Code 账号列表实现切片

- 决策：第一轮落地先做可冒烟垂直切片，不一次性实现完整 Codex 等价交互；范围为模型层、preview 数据、导航入口和页面 smoke。
- 实现：第一轮曾以 `#frame=codex&workspace=claude-account-list` 垂直切片进入页面，页面读取 `ListAccounts` 并按 `supportedFormats.includes("anthropic")` 过滤；浏览器 preview 使用稳定 DeepSeek、百炼、MiMo、MiniMax 账号数据。后续已迁移为 Claude 顶级入口。
- 模型：新增 `claudeCodeAccountList` 模型函数，覆盖 `formatBaseUrls.anthropic || baseUrl`、禁用保留、显式模型映射、同一真实模型多 Claude alias、官方 profile mapping draft。
- 验证：`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`node --test frontend/src/features/design-system/storyCatalog.test.mjs`、`npm --prefix frontend run build` 通过；agent-browser smoke 页面错误为空。
- 风险：真实拖拽保存、启停写回、详情模型映射保存和 Anthropic Messages 路由探测尚未接后端，后续切片继续补。

## Claude Code 账号列表完整闭环

- 决策：Claude Code 账号列表按 Codex 账号列表等价业务闭环推进，但筛选条件固定为 `supportedFormats.includes("anthropic")`，不按 provider 名称判断。
- 实现：页面复用 Codex 请求顺序、账号详情和路由探测组件；拖拽排序写回 `UpdateAccountPriority`，启停写回 `SetAccountDisabled`。
- 模型映射：OAuth/auth-file 走 `UpdateOAuthModelAliases(channel="claude")`；openai-compatible 走 `UpdateOpenAICompatibleProvider(models)`；codex-api-key 新增 `UpdateCodexAPIKeyConfig(models)` 持久化并同步 sidecar。
- 后端：新增 `ProbeClaudeCodeAccountRouting`，用 Anthropic Messages `POST /v1/messages` 最小请求探测，复用 `X-GetTokens-Route-*` header 和 recent request delta 识别命中账号。
- DTO：`AccountRecord` 补 `apiKeys`、`headers`、`models`，用于 Claude Code 账号详情模型映射读取、保存和远端模型查询。
- 验证：`go test ./...`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build` 通过；agent-browser 预览页面和路由探测弹层错误为空，截图已归档到 space。

## Claude 侧边栏顶级分类

- 决策：Claude 在侧边栏是和 Codex 同级的顶级大类，不挂在 Codex 子菜单下；本期子项为 `账号列表`。
- 实现：新增 `AppPage=claude` 与 `ClaudeWorkspace=account-list`，入口改为 `#frame=claude&workspace=account-list`，旧 `#frame=codex&workspace=claude-account-list` 自动迁移。
- 验证：`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build` 通过；agent-browser 已验证新入口渲染和旧 Codex hash 迁移，截图归档到 space。

## Accounts app 壳层快照

- 决策：账号列表快照属于 app 壳层生命周期，不应由 `AccountsFeature` 页面挂载生命周期独占；切出再切回 `#frame=accounts` 应复用已加载的 app 数据。
- 实现：新增 `AccountsPageStateProvider` 常驻挂在 `AppShell`，`AccountsPage/AccountsFeature` 只消费 provider 状态；新增 `accountSnapshot` 判定模型，避免已有快照时重复触发 `ListAuthFiles/ListAccounts`。
- 验证：新增 `frontend/src/features/accounts/tests/accountSnapshot.test.mjs` 并纳入 `npm run test:unit`；`npm run typecheck` 与 `npm run test:unit` 均已通过。

## v1.0.12 发布执行

- 决策：`v1.0.11` 已存在，按 release governance 不复用已消费 tag，本轮发布目标提升为 `v1.0.12`。
- 版本同步：`frontend/package.json`、`frontend/package-lock.json`、`frontend/package.json.md5` 已同步到 `1.0.12`。
- 本地预检：`go test ./...`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh` 均通过。
- 风险：`check` skill 的通用 `run-tests.sh` 未识别本仓库测试命令，发布验证以项目发布手册中的显式命令为准。

## v1.0.13 发布重试

- 结论：`v1.0.12` Release run `26114950432` 在 `Build macOS amd64 / Install Wails` 失败，失败原因为 GitHub runner 下载 `proxy.golang.org` / `sum.golang.org` module 超时；arm64 job 已通过但 Intel 资产未生成。
- 决策：`v1.0.12` tag 已被 CI 消费，不复用；按 release governance bump 到 `v1.0.13`。
- 修复：Release workflow 的 Wails 安装从 `@latest` 改为 pin `v2.12.0` 并增加 3 次重试，降低 runner 网络瞬断导致的失败率。
- 版本同步：`frontend/package.json`、`frontend/package-lock.json`、`frontend/package.json.md5` 已同步到 `1.0.13`。

## v1.0.13 发布验收完成

- CI：Release run `26115510534` 已成功完成，head sha 为 `42e0150772e9e7779881fb743e7288ac1c70ea03`，GitHub Release 已发布到 `https://github.com/AxApp/GetTokens/releases/tag/v1.0.13`。
- 资产：正式 Release 包含 `GetTokens_macOS_AppleSilicon.dmg`、`GetTokens_macOS_AppleSilicon.tar.gz`、`GetTokens_macOS_Intel.dmg`、`GetTokens_macOS_Intel.tar.gz`、`checksums.txt`。
- 校验：从 GitHub Release 下载正式资产后执行 `shasum -a 256 -c checksums.txt`，四个资产均为 `OK`。
- DMG 验收：Apple Silicon 与 Intel DMG 均通过 `spctl -a -t open --context context:primary-signature -v`，结果为 `accepted` / `source=Notarized Developer ID`；`xcrun stapler validate` 均通过。
- App 验收：两个 DMG 内的 `GetTokens.app` 均由 `Developer ID Application: HAN LIN (3L8RM3MDLS)` 签名，包含 timestamp 与 stapled notarization ticket。
- 架构与版本：Apple Silicon app executable 为 `arm64`，Intel app executable 为 `x86_64`；两者 `CFBundleShortVersionString` 与 `CFBundleVersion` 均为 `1.0.13`。
- Sparkle：Apple Silicon `SUFeedURL` 指向 `appcast-arm64.xml`，Intel `SUFeedURL` 指向 `appcast-amd64.xml`，符合分架构 appcast 规则。

## macOS 开机启动闪退修复

- 根因：LaunchAgent 写入了自定义参数 `--gettokens-login-item`，但 `main.go` 没有在 `wails.Run` 前消费该参数；本地构建 `.app` 带参数启动时复现 Wails 报 `flag provided but not defined: -gettokens-login-item`。
- 修复：新增启动参数剥离逻辑，避免 Wails dev/prod flag parser 看到自定义登录项参数；同时将该参数用于登录项启动语义，只有“关闭窗口后继续运行服务”配置下才 `StartHidden` 后台启动。
- 验证：`go test ./... -count=1`、`npm run test:unit`、`npm run typecheck`、`./scripts/wails-cli.sh build` 均通过；构建后的 `.app` 带 `--gettokens-login-item` 启动不再输出未定义 flag。

## 正式版隐藏开发入口

- 根因：`DebugPage` 和 `DesignSystemPage` 只关闭了开发态检查器/高亮，但侧边栏和 hash 路由仍把 `debug`、`design-system` 当普通页面处理，导致正式版仍可见或可通过旧 hash/localStorage 进入。
- 修复：侧边栏导航新增 developer-only 过滤；生产态路由读取、hash 解析和本地存储恢复均拒绝 `debug`、`design-system`，并回落到账号页。
- 验证：`npm run typecheck`、`npm run test:unit`、`npm run build` 均通过；生产预览访问 `#frame=debug` 会重写到 `#frame=accounts`，侧边栏不再出现“设计系统/调试面板”。

## 账号卡额度内部刷新态

- 根因：单账号刷新额度时 `refreshCodexQuota` 会把已有成功 quota 覆盖为纯 loading，`buildQuotaDisplay` 因而返回空窗口，导致额度条消失并切换到整段 loading 占位，卡片高度抖动。
- 修复：quota state 增加 `refreshing` 语义；已有 quota 刷新时保留旧窗口，仅在 `QuotaBars` 条形槽内部叠加 16px 高的刷新骨架；首次无 quota 时仍使用原 loading 占位。
- 设计系统：`Design System/业务组件/账号卡片` 增加 `QuotaBarsRefreshing` story，`CardSections` manifest 增加 `quota-refreshing` 状态。
- 验证：`npm run typecheck`、`npm run test:unit`、`npm run build-storybook`、`npm run build` 均通过；静态 Storybook 浏览器验收确认刷新态只有 2 个额度行和 2 个内部 skeleton，未切换整卡骨架。

## 账号卡片组件拆分与缩略额度

- 决策：账号卡片的 traffic、usage、quota、billing、rate-limit 需要拆为可独立拼装的设计系统组件；请求、Token、额度 mini metrics 也作为列表密度内部组件收编。Evidence 曾按卡片 section 拆出，后续已按下节迁移到详情页。
- 实现：`AttributionCard` 改为复用 `TrafficSection`、`UsageMetrics`、`QuotaBars`、`BillingBalance`、`RateLimitGuard` 和 `AccountMiniMetrics`；缩略模式在 header 后保留 `QuotaBars`，不渲染 traffic/usage/billing/rate-limit。
- 设计系统：`Design System/业务组件/账号卡片` 新增 `CompactAccountCard`、`AttributionCompactQuota`、`Traffic Section`、`Mini Metrics` stories；manifest 增加 `compact-quota`、`traffic`、`mini-metrics` 状态。
- 验证：`npm run build`、`npm run test:unit`、`npm run typecheck`、`npm run build-storybook` 均通过；静态 Storybook 验收确认缩略归因卡显示 2 行额度（5H WINDOW、WEEKLY），没有整卡骨架。
- 视觉修正：账号分组 overview 的窄卡片流量图进入单列布局时，`TrafficChart` 的左 padding 必须由 CSS 管理，避免 Tailwind `pl-3` 覆盖容器查询后让 `ATTRIBUTION` 与图表头部错位；流量图头部的 `窗口/峰值/当前` 与数值保持同一行，且三个指标横向三列展示。

## 账号卡 Evidence 迁移到详情页

- 决策：`EVIDENCE` 属于低优先级诊断信息，不在账号卡片、OpenAI-compatible provider 卡片或 Codex 请求顺序卡片上展示；卡片只保留 traffic、usage、quota、billing、rate-limit 和 mini metrics。
- 实现：移除 `AttributionCard` 的 `evidenceRows` API；新增 `accountEvidence` 纯模型和 `AccountEvidenceSection`，统一账号详情页展示账号 ID、数据源、归因键、最后命中和 format endpoint；OpenAI-compatible 详情弹窗追加 provider evidence 区。
- 设计系统：`Design System/业务组件/账号卡片` overview 已确认不含 `EVIDENCE`；`Design System/业务组件/账号弹窗` 的 AccountDetailSections story 和 OpenAI-compatible DetailModal story 保留详情页 evidence。
- 验证：`npm run typecheck`、`npm run test:unit`、`npm run build-storybook`、`npm run build` 均通过；Playwright 验收确认账号卡片 `EVIDENCE=0`，详情页 `EVIDENCE=1`。

## 账号卡菜单位置

- 决策：账号卡片菜单按钮属于 header 右上角操作，不应在窄卡片容器查询下被挤成单独一行。
- 修复：`AttributionCard` header 改为相对定位，`topActions` 绝对定位到右上角，并给标题内容保留右侧空间；移除 `.account-card-top-actions { width: 100% }` 的窄容器规则。
- 验证：`npm run typecheck`、`npm run test:unit` 通过；Storybook 账号卡片 overview 在 375px 视口下 DOM 验收显示菜单按钮位于 header 右上角。

## 页面加载态轨道动画

- 决策：设计系统 `PageLoadingFallback` 不能只是静态 `Loading` 标签，需要有明确的加载动效；动效沿用 Swiss-industrial 视觉，用行星轨道线旋转、扫描条和阶梯脉冲表达页面装配中。
- 实现：`PageLoadingFallback` 保持 API 不变，新增左侧黑色 rail 内的 planet/orbit 结构；CSS 增加 `page-loading-orbit-spin`、扫描和脉冲动画，并为 `prefers-reduced-motion` 提供静态 fallback。
- 验证：`npm run typecheck`、`npm run test:unit`、`npm run build-storybook`、`npm run build` 均通过；Storybook 页面 DOM 验收确认 orbit animation 为 `page-loading-orbit-spin` 且 transform 随时间变化。

## 页面加载态巨量信息涌入

- 决策：页面加载态需要从“单个轨道加载”增强为“巨量信息涌入”的感受，但仍保持 GetTokens 的 Swiss-industrial 视觉，不使用霓虹渐变或柔和装饰。
- 实现：`PageLoadingFallback` 增加 24 条不同节奏的数据包、3 条二进制位流、左侧数据总线和面板分身残影动画；分身用硬边框错位复制表达高速同步下的赛博残影。
- 验证：`npm run typecheck`、`npm run test:unit`、`npm run build-storybook`、`npm run build` 均通过；Storybook DOM 验收确认每个 loading panel 有 24 条 influx、3 条 bitstream、`page-loading-clone` 分身动画，且 375px 视口无横向溢出。截图归档到 `docs-linhay/screenshots/20260520/design-system/`。

## SegmentedControl 字号与总览验收

- 决策：SegmentedControl 默认字号需要从超小字号提升到 `font-size-ui-md-compact`，避免通用控件在设计系统和设置文本缩放下显得过小；控件视觉也应从重黑底切换为更克制的边框与底部指示器。
- 实现：更新全局 `--gt-control-segmented-font-size` 与三档 text scale token；SegmentedControl 增加 `aria-pressed` 并调整为轻边框、muted 未选中态和 2px 底部选中线；Storybook Overview 收敛为同一基础样式下的短标签、长标签、窄容器三种状态，不再保留额外单状态 story。
- 验证：`npm run typecheck`、`npm run test:unit`、`npm run build-storybook`、`npm run build` 均通过；Storybook DOM 验收确认 Overview 渲染 3 个 SegmentedControl，按钮字号为 11px，选中线为 2px，375px 视口无横向溢出。截图归档到 `docs-linhay/screenshots/20260520/design-system/`。

## SearchInput 收编设计系统

- 决策：Codex Extensions 的 Skills / MCP Servers 搜索框不再手写 `Search + input-swiss` 组合，统一收编为通用 `SearchInput`，便于后续搜索输入样式一致化。
- 实现：新增 `frontend/src/components/ui/SearchInput.tsx` 与 `SearchInput.stories.tsx`，支持搜索图标、清除按钮、禁用态、窄容器状态和 `data-design-system-component-name="SearchInput"`；Codex Extensions 的 Skills 与 MCP Servers 搜索框改为复用该组件。
- 验证：`npm run typecheck`、`npm run test:unit`、`node --test src/features/codex-extensions/model.test.mjs`、`npm run build-storybook`、`npm run build` 均通过；Storybook DOM 验收确认 SearchInput overview 有 4 个状态且 375px 无横向溢出；浏览器验收确认 `#frame=codex&workspace=mcp-servers` 与 `#frame=codex&workspace=skills` 各有 1 个 SearchInput。

## 账号页卡片宽度与缩略资产模块

- 决策：账号页同一分组网格内的卡片宽度必须一致，单卡分组不能因空列折叠而拉满；账号卡缩略模式的资产模块按 `quota > billing > placeholder` 互斥展示，若额度与 balance 同时存在优先显示额度。
- 实现：账号卡 full/compact 网格从 `auto-fit` 改为 `auto-fill`；新增 `resolveAccountCardValueSection` / `hasDisplayableBilling` 纯规则，`AttributionCard` compact 分支按规则渲染 `QuotaBars`、`BillingBalance` 或 unsupported 占位。
- 验证：先确认 `accountCardLayout` 红灯，再修复通过；`node --test src/features/accounts/tests/accountSelectors.test.mjs src/features/accounts/tests/accountCardLayout.test.mjs`、`npm run typecheck`、`npm run test:unit`、`npm run build` 均通过。Playwright / Chrome DevTools 浏览器实例当轮均被占用，未完成浏览器 DOM 截图验收。

## 账号应用模板映射本地 CLI 配置

- 决策：新建 `docs-linhay/spaces/20260520-account-template-cli-mapping/` 讨论账号卡动作菜单中“存在应用模板时映射 Codex / Claude Code 配置”的需求。
- 边界：P0 建议只生成现有 local apply 草稿并经 GetTokens relay 写入本机 CLI 配置，不在账号卡内直接 patch `config.toml` / `auth.json` / `settings.json`，也不默认绕过 relay 写上游 API Key。
- 映射规则：Codex 优先 `openai_responses` / `openai_chat`，Claude Code 要求 `anthropic`；模板来源优先复用 `vendorPresets + AccountRecord`，由纯模型 resolver 输出可用动作、禁用原因和草稿。
- 决策补充：应用前必须弹出新的确认页面或确认面板，让用户查看目标文件 diff、受控字段、保留字段、relay key/base URL/model/provider/auth strategy 和 warning；用户未确认前不得写入本机 CLI 配置。
- 实现参考：确认页面只复用 `frontend/src/features/status/StatusFeature.tsx` / `StatusApplyLocalSection` 背后的 diff builder、preflight 和 apply handler；布局不复刻状态页配置表单，固定为左侧文件列表、右侧选中文件 diff。
- 详细设计：新增 `docs-linhay/spaces/20260520-account-template-cli-mapping/plans/20260520-template-cli-mapping-detail.md`，结合本仓库 local apply 测试、Cherry Studio code tools、cc-switch provider/config editor、Claude Code 官方 settings/model config 和 Codex config reference，补齐数据契约、状态机、组件拆分、字段映射、preview 与测试计划。
- Codex OAuth 校准：保留 ChatGPT 登录态模式下 `CODEX_HOME/auth.json` 只读校验且原样保留，真实写入只 patch `config.toml` 的 custom provider；`experimental_bearer_token` 作为 provider-scoped bearer 优先参与请求，不能与同 provider 的 `env_key` / command-backed `auth` 混用，且不能使用内置 `openai` provider。
- DeepSeek 模板校准：DeepSeek 当前按官方应用模板只开放 Claude Code 入口；P0 不因底层 API 支持 OpenAI-compatible 就自动生成或展示 Codex 按钮，设计系统样例只保留 Claude Code 动作，并用说明文字解释未展示 Codex 的原因。
- 按钮白名单规则：账号卡菜单中的本地 CLI 应用按钮只能由官方/已验证 `localCliTemplateTargets` 或等价白名单生成；未验证目标不展示按钮，也不展示可点击禁用态。
- 厂商文档归档：新增 `docs-linhay/references/20260520-account-template-vendor-doc-links.md`，按现有 `vendorPresets` 逐项保存官方文档/官网链接，并把“应用模板支持”和“API 格式支持”分开；OpenAI 是当前 Codex 官方模板基线，DeepSeek / Zhipu / StepFun / Bailian / SiliconFlow / AiHubMix / Shengsuanyun / Novita 等作为 Claude Code 模板候选，其余 `needs-verification` 不自动启用本地 CLI 应用动作。
- 设计系统体验：`Design System/业务组件/账号卡片` 新增 `AccountTemplateApplyMenu` story；真实复用 `AccountCard` 右上角菜单，通过 `localCliActions` 只展示 DeepSeek 的 `应用到 Claude Code` 已验证动作，不展示 Codex 按钮。点击后打开确认页 overlay，展示来源、目标、relay key、endpoint、Claude Code diff、受控字段、保留字段和“未展示 Codex 动作”说明；确认按钮仅返回 `PREVIEW ONLY`，不调用 Wails。
- 设计系统验收：Storybook 页面 DOM 验收通过，桌面截图归档到 `docs-linhay/spaces/20260520-account-template-cli-mapping/screenshots/20260520/account-card/20260520-account-card-template-menu-after-v01.png` 与 `20260520-account-card-template-confirm-after-v01.png`；390px 窄屏无页面级横向溢出，截图为 `20260520-account-card-template-mobile-after-v01.png`。
- 体验调整：用户反馈确认页信息过重后，`AccountTemplateApplyMenu` story 的弹页改为文件预览器布局：左侧为文件列表，右侧为选中文件 diff，底部保留取消 / 确认并应用；旧的“来源与目标 / Claude Code 配置 / 受控与保留”三块详情面板已移除。新截图为 `20260520-account-card-template-confirm-after-v02.png` 与 `20260520-account-card-template-mobile-after-v02.png`。
- Codex 源码校准：本机 Codex 为 `codex-cli 0.130.0`，仓库镜像位于 `docs-linhay/references/codex/codex-rs`。源码确认 `auth.json` 是 `$CODEX_HOME/auth.json` 的结构化 auth 存储；provider auth 解析优先 `env_key`，其次 `experimental_bearer_token`，否则回退到 CodexAuth；`requires_openai_auth` 要求 auth manager 有有效登录。GetTokens local apply 也已有两种模式：`replace_auth_with_apikey` 写 `auth.json + config.toml`，`preserve_chatgpt_auth` 校验并保留 ChatGPT 登录态，只写 `config.toml` 的 custom provider + `experimental_bearer_token`。
- Codex 设计稿修正：`AccountTemplateApplyMenu` 的 Codex 确认页默认改为“写入 API Key”模式，左侧文件列表包含 `CODEX_HOME/auth.json` 与 `CODEX_HOME/config.toml`；另提供“保留 ChatGPT 登录”切换，切换后 `auth.json` 只读校验且不显示写入 diff。浏览器验收确认 API Key 模式出现 `---/+++ CODEX_HOME/auth.json` 和 `OPENAI_API_KEY`，config 模式出现 `wire_api = "responses"` 且不含 `experimental_bearer_token`；preserve 模式出现 `experimental_bearer_token` 且不重写 `auth.json`。截图为 `20260520-account-card-template-codex-apikey-after-v01.png`、`20260520-account-card-template-codex-config-after-v01.png`、`20260520-account-card-template-codex-preserve-after-v01.png`。
- Codex 官方源码复核：再次对齐 `openai/codex` main 分支的 `login/src/auth/storage.rs`、`model-provider/src/auth.rs`、`model-provider-info/src/lib.rs`，结论保持不变；已把源码链接补进 `docs-linhay/references/20260520-account-template-vendor-doc-links.md`，并在 space README 的 Codex 映射规则中明确 API Key 模式必须写 `auth.json`、preserve 模式才只读校验 `auth.json`。补充截图为 `20260520-account-card-template-codex-source-check-after-v01.png` 与 `20260520-account-card-template-codex-source-check-mobile-after-v01.png`。
- Codex 固定模式修正：用户明确“一个账号的应用模式是固定的，不可能既有 API Key 又有 OAuth”。`AccountTemplateApplyMenu` 已改为两个不同 Codex 账号样例：`OpenAI API Key Relay` 固定写 `auth.json + config.toml`，`OpenAI Codex OAuth` 固定只读校验 `auth.json` 并写 `config.toml` 的 preserve 方案；确认页移除 API Key / OAuth 模式切换，只展示当前账号固定应用模式。浏览器验收确认两个账号各自固定、无横向溢出。
- Codex provider 稳定性修正：用户进一步明确不是固定 `gettokens`，而是先看用户当前在用什么 `model_provider`。设计稿和 space 规则已改为读取并沿用当前 root `model_provider`，默认 patch 对应 `[model_providers.<current>]` 的受控字段；只有当前 provider 不可用或用户明确选择时才变更 root `model_provider` / 新建 provider。
- Codex provider 设计稿验收：`AccountTemplateApplyMenu` 模拟当前用户 provider 为 `team-codex-relay`；API Key / OAuth 两个 Codex 确认页均展示“当前 provider”，diff 中 root `model_provider` 作为上下文保留，不再出现 `+model_provider` 新增行，实际 patch 目标为 `[model_providers.team-codex-relay]`。
- 实施前整理：新增 `docs-linhay/spaces/20260520-account-template-cli-mapping/plans/20260520-implementation-readiness.md`，把 P0 目标、固定产品规则、后端/Wails provider state、前端 resolver、确认页、账号卡集成、apply 复用、自动化/浏览器/Wails 验收和实施顺序整理为施工清单；space 状态更新为 `ready-for-implementation`。
- 施工前口径收敛：确认页形态已定为账号池内 modal / overlay，不跳转 Status 页；布局固定为文件预览器，左侧文件列表、右侧选中文件 diff；状态页只复用 diff builder、preflight 和 apply handler，不复用配置表单布局。
- 施工完成：账号页已接入账号模板到本地 CLI 配置应用。新增 `accountLocalCliMapping` 纯模型与测试、`AccountLocalCliApplyConfirm` 文件预览器确认页、账号卡菜单禁用态、账号页 relay/config/auth/provider state 加载与 Codex/Claude apply 调用；Wails 侧补 `GetLocalCodexModelProviderStateView` 穿透。验证通过 `npm --prefix frontend run test:unit`、`typecheck`、`build-storybook`、`build`、Go local apply/provider state 定向测试和浏览器 preview；截图归档到对应 space。施工疑问已整理到 implementation report：未直接写用户真实 CLI 文件，P0 不做 direct upstream / 单账号候选 pin，后续可把 resolver 白名单迁移到模板字段。

## Status Local Apply 字段标签收编

- 决策：`StatusApplyLocalSection` 内的手写字段 label 需要纳入设计系统追踪，不再散落为业务 JSX 中的裸 `label/span`。
- 实现：新增通用 `FormField` / `FieldLabel` / `SelectField` / `TextInputField`，补 `Design System/通用组件/表单字段` Storybook overview，并将本地 Codex / Claude Code apply 区的 reasoning effort、auth strategy、auth state、wire API、Claude 数值字段和 endpoint 标题迁移到该组件体系；`ActionSelect` 标题也复用 `FieldLabel`。
- 验证：`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit` 均通过。

## Status Snippet pre 收编

- 决策：`StatusSnippetPanel` 的代码预览 `pre` 不再保留为业务组件内的裸节点，统一纳入通用设计系统组件，便于后续配置片段、diff 和长行滚动区域复用同一视觉材料。
- 实现：新增 `SnippetPre` 与 `Design System/通用组件/代码片段区域` Storybook overview；`StatusSnippetPanel` 改为复用 `SnippetPre`，自身只保留 header、copy/action 和 diff line tone 逻辑；manifest 记录 `SnippetPre` 匹配关系。
- 验证：`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build-storybook`、`npm --prefix frontend run build` 均通过；静态 Storybook 浏览器验收确认 `SnippetPre` overview 渲染 2 个 `data-design-system-component-name="SnippetPre"`，窄视口无页面级横向溢出，长行只在片段区域内横向滚动。

## ModalFrame 纳入设计系统

- 决策：modal / dialog 窗口 shell 纳入通用设计系统标准，新增 `ModalFrame` 统一遮罩、窗口材质、尺寸档位、滚动边界、header/body/footer/error 插槽和运行时设计系统标记。
- 实现：新增 `frontend/src/components/ui/ModalFrame.tsx` 与 `ModalFrame.stories.tsx`，`Design System/通用组件/弹窗窗口` 的 Overview 覆盖默认、错误、长内容、确认、详情宽屏状态；`AccountDetailModalFrame` 已改为复用 `ModalFrame`。
- 文档：更新 `docs-linhay/spaces/20260519-design-system-workbench/README.md`，新增 `docs-linhay/dev/20260520-modal-frame-design-system-standard.md`。
- 验证：先让 `storyCatalog.test.mjs` 对缺失的 `ModalFrame` catalog / marker 红灯，再实现转绿；`node --test frontend/src/features/design-system/storyCatalog.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build-storybook` 均通过。

## CLIProxyAPI 上游同步到 v7.1.18

- 里程碑：`docs-linhay/references/CLIProxyAPI#gettokens/sidecar` 已从 canonical upstream `router-for-me/CLIProxyAPI@a726e373`（tag `v7.1.18`）合并，生成并推送维护 fork merge commit `1fdeefb9` 到 `AxApp/CLIProxyAPI#gettokens/sidecar`。
- 上游变化：本次带入 Redis protocol 处理增强、Claude 请求转换跳过空 text parts、Gemini 3.5 Flash 模型注册、Codex reasoning levels 扩展、usage events reasoning effort 等 upstream 更新。
- 验证：fork 内 `go test ./...` 全量通过；GetTokens 根仓库执行 `./scripts/ensure-sidecar.sh darwin arm64` 重建本地 sidecar，meta 指纹为 `1fdeefb9:clean:34ecd6f33a6c0f689853b15a6861c87c5307afbb6c4650096921ec453d4ca819:darwin:arm64`。
- 状态：父仓库 gitlink 已更新到 `1fdeefb9`；`build/bin/cli-proxy-api` 与 meta 为本地构建产物，当前未被 Git 跟踪。
