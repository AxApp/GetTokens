# 2026-07-10

## 正式环境 Codex 账号卡片异常诊断

- **背景**：用户反馈正式环境中 `rf5gqn9grh@privaterelay.appleid.com` 的 Codex Plus 账号卡片显示异常，并提供 `/backend-api/wham/usage` 响应作为额度证据。
- **只读诊断结论**：
  1. `/wham/usage` 响应本身正常：`plan_type=plus`，`rate_limit.allowed=true`，5H 窗口约 99% 剩余，7D 窗口约 100% 剩余；`credits.balance=0` 不是 Plus 账号卡片异常的直接原因。
  2. 正式 sidecar 的 `quota-status` 对当前账号 `acct_5d327073-ed41-434e-9932-83d7189cfd69` 返回 `status=success`、`quotaFact.state=available`，但同时 `blocked=true`，来源为 `auth-error`。
  3. `block_reason` 为 OpenAI OAuth `refresh_token_reused`：refresh token 已被用来生成新 access token，请重新登录。前端 `accountPresentation` 会把 `quotaDisplay.blocked && sources contains auth-error` 归为 operational failure，因此卡片显示异常是按当前规则触发的。
  4. 日志显示 2026-07-10 09:54:54 重新登录成功写入 account store，09:57:44 sidecar 重启/重载后出现一批并发 `gettoken` / `api-call`，09:57:45 连续 5 次 token refresh 以 `refresh_token_reused` 失败。后续排查应优先怀疑 Codex OAuth refresh 的并发复用、单飞缺失或刷新后新 refresh token 未及时持久化/共享，而不是额度耗尽。
- **边界**：本轮只读检查正式环境，未修改 `/Applications/GetTokens.app`、正式 sidecar、正式配置或正式 SQLite 数据。
- **后续建议**：在 dev 环境为 Codex OAuth refresh 补服务级回归测试，固定“同一 refresh token 并发刷新只发起一次上游请求，并将旋转后的 refresh token 持久化到 account store 后再释放等待方”的行为。

## Codex refresh token 并发复用修复

- **修复范围**：在 `docs-linhay/references/CLIProxyAPI` 的 `sdk/cliproxy/auth.Manager.refreshAuth` 增加 per-auth singleflight。并发刷新同一 auth 时，只有第一个 goroutine 进入 executor；等待者拿锁后重新读取 auth 状态，若前一个 refresh 已更新 token 且当前 `shouldRefresh=false`，则跳过重复刷新。
- **fork commit**：`2443e76f19bd0f8b9e1f221a3f75dce6698f8a89` (`Deduplicate concurrent auth refreshes`)。
- **验证**：
  - 红灯复现：新增 `TestManager_RefreshAuthDeduplicatesConcurrentRefreshForAuth` 后，旧实现失败为 `Refresh calls = 5, want 1`。
  - focused refresh 测试、`go test ./sdk/cliproxy/auth -count=1`、fork `go test ./... -count=1` 均通过。
  - `./scripts/ensure-sidecar.sh darwin arm64` 已从 clean fork commit 重建 sidecar，meta 指纹为 `2443e76f19bd0f8b9e1f221a3f75dce6698f8a89:clean:329aff8470e7a1951682c630ade8ccb7ef9958d36a9af75c25745c8cdfdf8bd2:darwin:arm64`。
- **正式环境边界**：未修改、替换、重启或 kill 正式版 GetTokens；修复只落在源码与本仓构建产物。
