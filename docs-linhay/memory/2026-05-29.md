# 2026-05-29

## 账号卡身份模型迁移

- 决策：GetTokens 本地产品模型不引入用户实体，只有账号卡；`account_key/account_id` 是账号卡唯一业务身份。
- 决策：账号登录、新增、导入、复制创建新账号卡；重新登录和编辑当前凭证更新原账号卡并保留原 ID。
- 决策：`auth-id/auth-index/provider/attribution_key/email/API key hash` 都是 runtime evidence，不允许作为 rate-limit 策略匹配键。
- 进展：sidecar fork 已让 Codex API key、auth-file、OpenAI-compatible provider 的 runtime auth 携带 `AccountKey`。
- 进展：sidecar standalone Codex API key 缺失 `local-id` 时会生成 `codex-api-key:legacy-*` 并写回配置。
- 进展：sidecar rate-limit schema/API/evaluator 破坏性删除 `match_key`，usage 查询只按 `account_key`。
- 进展：GetTokens Wails / frontend rate-limit DTO 删除 `matchKey`，`RateLimitRulesSection` 改为单行摘要 + 配置态，无横向滚动表格。
- 进展：账号详情 rate-limit CRUD 改为由页面 shell 注入，browser preview 不再直接触发真实 Wails binding。
- 验证：Sidecar 聚焦 Go 测试、GetTokens Go 测试、frontend `npm run typecheck`、`npm run build`、`npm run test:unit` 均通过。
- 验证：已用 `playwright-cli` 验收账号详情限流区 summary / config 态，确认无 Wails binding 崩溃并归档截图。
- 验证：已用当前 GetTokens worktree 构建产物加载当前 sidecar worktree，dev sidecar `/healthz` 200；rate-limit management API 的 strategies/create/status/events/delete/list 链路均 200，临时规则已清理。

## Sidecar 自治状态治理

- 决策：sidecar 是 GetTokens 运行态自治层；账号选择、rate-limit、route guard、live sessions、usage attribution、system proxy、Codex WebSocket 等热路径状态必须在 `CLIProxyAPI#gettokens/sidecar` 内闭环。
- 决策：合并 CLIProxyAPI 上游提交时，一旦上游实现对 sidecar 自治状态存在风险，不整包照搬；在 `gettokens/sidecar` 内重新实现合理逻辑、补窄测试、重建 sidecar，再合回 GetTokens 主分支（当前为 `master`）。
- 进展：已将该规则写入 `AGENTS.md`、`gettokens-domain-engineering` skill、CLIProxyAPI 上游同步文档和账号卡身份模型文档。

## 账号与凭证 SQLite 统一存储方案

- 决策：账号卡、Codex OAuth/auth-file、Codex API key、OpenAI-compatible provider、本地账号卡 store 等账号/凭证事实源统一收敛到 `~/.config/gettokens-data/accounts-v1.sqlite`。
- 决策：迁移后的 `account_key` 由 SQLite 账号卡 store 分配并持久化，建议格式 `acct_<ulid>`；旧 `auth-file:*`、`codex-api-key:*`、`openai-compatible:*` 只作为迁移证据和 runtime identity，不再作为业务主 ID。
- 决策：迁移完成后删除旧账号凭证事实源文件和 `config.yaml` 中的 `codex-api-key` / `openai-compatibility` 账号配置，不保留双写兼容。
- 产出：新增 `docs-linhay/dev/account-credential-sqlite-store-design.md`，覆盖 schema、迁移流程、sidecar 集成、修改范围和测试清单。

## Codex 二进制管理页面接管

- 决策：Codex 负责后续维护 Codex 二进制管理页面，继续按 `gettokens-codex-binary-management` skill 的边界推进。
- 边界：该页面保持独立业务，只处理 Codex CLI 版本源、下载、激活、回退、托管 PATH、release cache、版本说明和紧凑列表 UI，不并入账号池、local apply、用量、会话或路由策略。
- 基线：当前工作区干净，`frontend/src/features/codex-binary` 已按 controller / components / model / presentation 拆分。
- 验证：已运行 `npm --prefix frontend run test:unit -- src/features/codex-binary/model.test.mjs`，实际 frontend unit suite 613 项全通过；`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`go test ./internal/codexbinary ./internal/wailsapp` 均通过。
- 治理：补齐该 space 的 `screenshots/.gitkeep`，并调整 `.gitignore` 为忽略截图内容但允许跟踪截图目录骨架。

## Codex 二进制管理页面回归修复

- 根因：托管 PATH 状态只看当前 App 进程 `PATH`，macOS GUI App 重启不会 source shell profile，导致 profile 已写入 GetTokens block 时仍显示“一键托管”。
- 修复：`managedConfig()` 同时检查进程 `PATH` 和目标 profile 中的 GetTokens managed block；新增重启场景回归测试。
- 根因：`VersionNotes()` 只读本地 `cache/release-notes`，点击版本行不会请求 GitHub release body，所以已缓存 notes 永远显示“来自缓存”。
- 修复：`GetCodexBinaryVersionNotes` 后端路径改为优先拉取对应 GitHub release 内容并刷新缓存，远端失败时才回退缓存；新增远端优先和缓存兜底测试。
- 验证：`go test ./internal/codexbinary ./internal/wailsapp`、`go test ./...`、`npm --prefix frontend run test:unit -- src/features/codex-binary/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 均通过；build 仅有既有 chunk size warning。
