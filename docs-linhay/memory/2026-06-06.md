# 2026-06-06

- 账号卡片 quota empty badge 的时间差异根因：sidecar `blockReason` 里的 `reset at 2026-...Z` 是 UTC/RFC3339 原始原因；quota row 下方“重置时间”来自 window `resetAtUnix/resetLabel` 并按本地时区格式化。前端展示层不得直接把原始 `quota empty: ...; reset at ...Z` 放入可见 badge，需先归一为本地化短文案，例如 `5H 已空 · 06/06 04:10`，原始 reason 继续保留在数据层供诊断。
- 账号卡片无上限流量统计（`value / ∞`）不应伪造 quota 上限，也不应使用突兀的图表主色进度条。当前采用“活动强度条”：保留 `/ ∞` 文案，宽度按当前累计量相对近期 `trafficBuckets` 基线计算，颜色使用 `text-primary` 低透明混合色，表达近期活跃度而非剩余额度。
- 账号卡片的“刷新额度”是用户理解中的卡片运行状态刷新入口；当卡片存在 Route Guard 规则时，应同时刷新 `GetAllRateLimitStatuses` 并在 Route Guard 区域显示同款扫光反馈。否则 quota 有动画而 Route Guard 不动，会被误解为该区域没有刷新。
- 账号卡片显式刷新时，无上限 usage 活动条也必须显示刷新反馈：顶部刷新、分组刷新、单卡刷新都应触发 usage 刷新 busy 状态并复用同款扫光；后台 usage 轮询保持静默，避免页面周期性自发动画干扰扫读。
- v1.1.1 发布完成：版本三件套为 `frontend/package.json`、`frontend/package-lock.json`、`frontend/package.json.md5`，`package.json` md5 为 `a3160eccd61afd948585908e5243cb33`；release commit/tag 为 `a0c7c401ca7d0cbb21e34982f40387d8c478e4f9` / `v1.1.1`。本地门禁通过：`go test ./...`、`npm --prefix frontend run test:unit`（728 项）、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`；Vite chunk-size warning 按 release 规则不阻塞。GitHub Release workflow `27030754594` 成功，URL `https://github.com/AxApp/GetTokens/actions/runs/27030754594`；`Build H5`、`Build macOS arm64`、`Build macOS amd64`、`Publish Sparkle appcast`、`Publish Release` 均 success。Release URL `https://github.com/AxApp/GetTokens/releases/tag/v1.1.1`，资产齐全：`GetTokens_macOS_AppleSilicon.dmg`、`GetTokens_macOS_AppleSilicon.tar.gz`、`GetTokens_macOS_Intel.dmg`、`GetTokens_macOS_Intel.tar.gz`、`checksums.txt`。Sparkle appcast 分支 `648f9a5f19a3ad18184c2a1c72af2c9bc2d2e2ce`，`appcast-arm64.xml` 与 `appcast-amd64.xml` 顶部均包含 `1.1.1` 且指向对应 DMG。官方 Release 资产下载到 `/tmp/gettokens-v1.1.1-verify.5jvN22` 后，`shasum -a 256 -c checksums.txt` 全部 OK；两个 DMG 均 `spctl ... accepted` 且 `source=Notarized Developer ID`，`xcrun stapler validate` 成功。挂载验收显示 `.app` 签名链为 `Developer ID Application: HAN LIN (3L8RM3MDLS)`，包含 timestamp 与 `Notarization Ticket=stapled`；Apple Silicon 可执行文件为 `arm64`，Intel 可执行文件为 `x86_64`；两个包的 `CFBundleShortVersionString` 与 `CFBundleVersion` 均为 `1.1.1`，`SUFeedURL` 分别为 `appcast-arm64.xml` / `appcast-amd64.xml`。最终状态：CI 发布完成，可分发 DMG 验收完成。
- Codex OAuth/auth-file 账号详情页也应展示额度与余额模块；OAuth 的差异只在管理侧内置模板/数据来源，不应让详情页缺少资源模块。实现上已让 OAuth 详情模块计划包含 `quota` / `billing`，并通过共享区块的 `readOnlyScripts` 只读展示实时资源信息，避免暴露无法保存的脚本编辑入口。
