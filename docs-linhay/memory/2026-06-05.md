# 2026-06-05

## 账号详情页 v09 收敛与重构启动

- Space：`docs-linhay/spaces/20260529-account-detail-ui/`
- 新设计基线：`account-detail-account-types-v09.html`
- 执行计划：`plans/20260605-account-detail-v09-implementation-plan.md`

### 决策

1. 账号详情页后续按 v09 收敛方向实现，不再继续在 v08 设计稿上堆补丁。
2. Auth-file/OAuth 账号已经进入数据库管理语义，详情头部不再用文件名作为主要身份；头部显示账号类型/数据库账号名称。
3. 详情页凭据默认明文展示；隐藏/掩码不是本轮详情页默认交互。
4. 验证区只表达“发送一条短消息”的连通性验证，不再默认暗示模型目录拉取、额度探测或 credential/proxy/model route 深度探测。
5. Footer 状态说明需要保持单行，避免底部命令栏高度和视觉节奏不稳定。

### 今日执行

- 更新 `docs-linhay/spaces/20260529-account-detail-ui/README.md`，将当前设计稿入口调整为 `account-detail-account-types-v09.html`，状态调整为 `v09-consolidated-ready-for-implementation`。
- 新增 `docs-linhay/spaces/20260529-account-detail-ui/plans/20260605-account-detail-v09-implementation-plan.md`，记录已确认约束、待确认事项和 Slice 1-4 实施计划。
- 前端 Slice 1 已启动并落地：
  - `frontend/src/features/accounts/components/AccountDetailSections.tsx`
    - `AccountDetailHeader` 移除 header 内 operational status pill。
    - Codex Auth-file 头部主标签改为 `Codex OAuth`，不再从文件名派生。
    - `VerifyConnectionPanel` 文案收窄为短消息验证/发送验证，并说明 `send one short chat message only`。
    - `AccountDetailFooter` 状态文案增加单行显示标记与 `whitespace-nowrap / overflow-hidden / text-ellipsis`。
  - `frontend/src/features/accounts/tests/accountDetailLayout.test.mjs`
    - 新增 header 状态 pill 移除与 Codex Auth-file 类型显示的 source-level 回归测试。
    - 新增短消息验证文案回归测试。
    - 新增 footer 单行状态文案回归测试。

### 验证

- `cd frontend && node --test src/features/accounts/tests/accountDetailLayout.test.mjs`：通过，24/24。
- `cd frontend && npm run typecheck`：通过。
- `docs-linhay/scripts/check-docs.sh`：通过。

### 待明天确认

1. Auth-file/OAuth 的“配置预览/下载配置/应用配置”真实 management API 边界。
2. 短消息验证是否需要扩展 `onVerify` 入参，加入 message/maxTokens/timeout/persist=false。
3. 模型映射 Source/Alias 编辑的保存 DTO 与 sidecar ownership。
4. 当前 space 下大量历史 HTML 是否需要归档/删除，只保留 v09 作为本期唯一入口。

## 账号详情页 v09 Slice 2 启动

- 前端继续推进身份凭据区重构：
  - `AccountCredentialVerifySection` 从 `stacked` 凭据字段改为 `balanced-grid`，为 v09 的 Display/API Key/Base URL/Prefix 规整间距做准备。
  - `CredentialInputField` 不再根据 `secret` 使用 password 输入，账号详情页凭据固定明文展示。
  - `AuthFileSummarySection` 文案从文件摘要/脱敏/复制原文，改为数据库账号配置管理语义：账号名称、配置预览、下载配置、应用配置、SQLite account store。
- 新增回归测试覆盖上述语义，`accountDetailLayout.test.mjs` 当前 26/26 通过。
- 待确认保持不变：Auth-file/OAuth 的应用配置真实接口仍需明天对齐，当前只是 UI 结构与语义先行。

## 账号详情页 v09 Slice 3A 启动

- 请求配置/代理路线继续向 v09 收敛：账号详情页代理配置不再提供 inherit/direct/custom 模式切换，只保留选择已保存代理池节点；刷新、测速、增删节点留在代理池页面。
- Auth-file 模型目录从 pill 列表改为 `Source Model → Alias / Route` 只读映射卡，便于后续与 API Key / OpenAI-compatible 的可编辑模型映射保持同一视觉语言。
- 新增回归测试覆盖 saved-node-only 代理编辑器与 source-route 模型映射卡，`accountDetailLayout.test.mjs` 当前 28/28 通过。
- 待确认：非只读模型映射编辑保存 DTO 仍未接入，后续需要按 sidecar/account store 边界确定。

## Auth-file 配置管理 API 边界固化

- 为 Auth-file/OAuth 配置管理 UI 增加显式边界：`data-auth-file-config-management="ui-placeholder"`。
- 预览/下载/应用三个动作均增加 data hooks，并在 UI 文案中标注“待接入 account-store management API”。
- 新增回归测试禁止在接口未确认前引入 `ApplyAuthFileConfig` / `SaveAuthFileConfig` 伪 API，防止前端绕过 sidecar/account-store 边界。
- `accountDetailLayout.test.mjs` 当前 29/29 通过。

## 全量前端测试门禁记录

- 运行 `cd frontend && npm run test:unit`，当前失败 1 项：`codex live sessions workbench keeps the right pane as overview until a session is selected`。
- 失败来源是 `frontend/src/features/codex-live-sessions/model.test.mjs:913`，断言 `data-codex-overview-timeline-shell="session-style"`，属于 Codex live sessions unrelated dirty work，不属于账号详情页重构路径。
- 账号详情 focused 测试 `accountDetailLayout.test.mjs` 29/29 通过，`npm run typecheck` 通过。
- 后续交付前若需要全量绿灯，必须先处理 live sessions 这条 unrelated 失败或确认对应测试预期更新。

## Codex app 模型目录模板投影

- Space：`docs-linhay/spaces/20260605-codex-app-model-catalog-template/`。
- 对照 `docs-linhay/references/cc-switch` 后确认：cc-switch 默认同样写 `~/.codex`，关键差异是用 Codex `gpt-5.5` 官方模型模板生成 `cc-switch-model-catalog.json`，并在其设计中切默认 model；用户确认 GetTokens 不自动切默认 model 是既定设计。
- GetTokens 已将 `buildGetTokensCodexModelCatalog` 改为模板级 projection：优先读 `CODEX_HOME/models_cache.json`，其次尝试 `codex debug models --bundled`，最后使用内置 fallback；relay 模型 entry 继承 app-facing 字段，并替换 slug/display/description/priority/reasoning。
- 新增回归测试锁住两点：模板字段继承，以及启用 DeepSeek catalog 不会把顶层 `model = "gpt-5.5"` 改成 `deepseek-v4-flash`。
- 验证：focused catalog 回归与 `go test ./internal/wailsapp` 通过；Codex app 实机可见性仍待后续 dev 环境冒烟。

## 账号详情页真实布局纠偏

- 用户指出真实账号详情页没有按设计稿重做。复核确认此前主要是局部切片，真实 modal 仍是卡片网格。
- 已纠偏：新增 `bands` 模块布局，真实 `UnifiedAccountDetailModal` 改用 v09 的 band row 骨架，`AccountDetailSection` 渲染为左侧 index/title rail + 右侧内容。
- 新增回归测试 `real account detail modal uses v09 band row layout instead of card grid`，当前 `accountDetailLayout.test.mjs` 30/30 通过。
- 后续仍需浏览器/Wails 截图验收，检查是否真正达到 v09 设计稿密度和视觉层级。

## 账号详情页真实 Header 重做

- 在真实前端中把 `AccountDetailHeader` 从旧的上下堆叠信息改为 v09 三栏紧凑摘要：左账号、中间 chips + 描述、右侧 last/latency。
- `UnifiedAccountDetailModal` header padding 改为 `p-0`，避免默认 padding 破坏设计稿贴边结构。
- 新增回归测试 `real account detail header uses v09 compact three-column summary`，当前 `accountDetailLayout.test.mjs` 31/31 通过。

## 账号详情页真实模块低嵌套化

- 继续直接修改真实前端：`AccountCredentialVerifySection` 从旧 vertical/卡中卡感结构改为 `v09-low-nesting`。
- credential / 短消息验证 / proxy-route 仍在同一真实保存模块内，但视觉结构向 v09 的低嵌套 band 内容靠拢。
- 未凭空添加 draft 中不存在的 `headersText` 保存字段；Custom Headers 等真实字段需后续确认 DTO 后再接。

## 账号详情页 Quota/Billing 左右等分

- 真实前端已将 Quota 与 Billing 合并到同一个 v09-style balance band：左右 1:1 等分，中线 full-height。
- 新增回归测试 `account detail quota and billing render as v09 equal split panes`，当前 `accountDetailLayout.test.mjs` 32/32 通过。
- 当前为结构合并第一步，内部仍复用原 Quota/Billing section 逻辑；后续可继续降低内部嵌套。

## Codex.app DeepSeek 模型弹窗根因

- 只读检查 `/Applications/Codex.app/` 后确认：GetTokens `model_catalog_json` 已生效到内置 `codex app-server`，直接发送 `initialize` + `model/list` 可返回 `deepseek-v4-flash`、`deepseek-v4-pro` 等 8 个模型。
- Codex.app 前端模型选择器还有第二层过滤：`webview/assets/model-queries-CpstxAte.js` 读取 Statsig dynamic config `107580212`，当 `use_hidden_models=true` 时只展示 `available_models` 白名单。本机 Codex Local Storage 缓存中该白名单为 `gpt-5.5/gpt-5.4/gpt-5.4-mini/gpt-5.3-codex/gpt-5.2`，不含 DeepSeek。
- 根因结论：当前 Codex.app 不显示 DeepSeek 不是 GetTokens catalog 或 app-server 没生效，而是 Codex.app 桌面前端的官方模型白名单过滤。稳定产品方案不应伪装 model slug；若要让桌面弹窗显示，需要单独评估私有 Statsig cache overlay，风险是会被 Codex.app 刷新覆盖。
- 用户确认的边界保持不变：GetTokens 不自动把默认 `model` 切到 DeepSeek。

## Wails / Relay 局域网支持

- Space：`docs-linhay/spaces/20260605-wails-lan-access/`。
- 决策：支持局域网访问限定在 Wails/Vite dev 预览和 sidecar relay 业务入口；不把 Wails 桌面应用改造成公网 Web 服务，不开放远程 management API。
- 实现：`frontend/package.json` 的 dev script 改为 `vite --host 0.0.0.0`；状态页新增 endpoint 选择模型，LAN 开启且存在 `kind=lan` 时优先选择 LAN 地址。
- 补充：运行状态页头部新增“打开 Web”按钮；Wails runtime 使用 `BrowserOpenURL` 打开当前 sidecar 端口的真实管理面板 `http://127.0.0.1:<sidecar-port>/management.html`，浏览器预览 fallback 到 `window.open`。明确不打开 `5173/#frame=status`，因为 Vite 浏览器页无 Wails bindings 时会走预览/模拟数据，不符合正式环境真实数据要求。
- 安全边界：sidecar `writeConfig` 继续保留 `host: ""` 业务监听，并强制 `remote-management.allow-remote=false`。
- 验证：`go test . -run 'TestWailsDevFrontendBindsToLAN|TestWailsConfigRegistersProdAndDevDeepLinkSchemes' -count=1`、`go test ./internal/sidecar -run 'TestWriteConfigCreatesMinimalConfig|TestWriteConfigPreservesCodexAPIKeys' -count=1`、`go test ./internal/wailsapp -run 'TestNormalizeRelayAPIKeys|TestBuildRelayServiceEndpoints' -count=1`、`node --test frontend/src/features/status/tests/relayLocalState.test.mjs frontend/src/features/status/tests/statusTypography.test.mjs`、`npm --prefix frontend run typecheck` 均通过。

## Codex.app DeepSeek 模型弹窗动态覆盖入口结论
- 继续只读拆 `/Applications/Codex.app/` 后确认：`/wham/statsig/bootstrap` 是 post-login Statsig bootstrap 入口，默认走 `https://chatgpt.com/backend-api`，也可被 `CODEX_API_BASE_URL` 整体覆盖，但会影响所有 ChatGPT backend API。
- Codex.app 的 Statsig 普通 initialize 使用 `networkOverrideFunc` 经 Electron `net.fetch` 访问 `https://ab.chatgpt.com/v1`；可被系统代理/MITM 理论拦截，但风险和影响面大。
- 更轻的实验入口是 CDP/DevTools 注入 `globalThis.__STATSIG__` 的 `overrideAdapter.getDynamicConfigOverride`，只覆盖 dynamic config `107580212`；需要调试端口启动 Codex.app，不能作为默认稳定产品能力。
- shared object / persisted atom / `statsig_default_enable_features` 不控制模型白名单；继续改 app-server/CLI wrapper 也不够，因为 `model/list` 已返回 DeepSeek，过滤在前端 Statsig 层。

## 账号详情页 Balance 内部 rail 修正

- 用户在真实账号详情页指出 `QUOTA / 额度追踪` 不应作为 Balance 内部独立左列，应与测试按钮处在同一内容 header。
- 根因：外层 v09 band layout 的 React context 继续传给 Balance 里的 `AccountQuotaSection` / `AccountBillingSection`，导致嵌套 section 也渲染为 band rail。
- 修正：`AccountDetailPrimitives` 在 band 右侧内容区用 `AccountDetailModuleLayoutContext.Provider value="flow"` 重置嵌套布局；Balance 只保留外层 rail，内部 Quota/Billing 回到普通 header + actions。
- 新增回归测试 `band detail sections reset nested modules so quota titles share the action row`；focused account detail 测试 33/33 通过，`npm run typecheck` 通过。
- Playwright 验收截图已归档：`docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-balance-after-v01.png`。

## 2026-06-05 - cc-switch 最新版 Codex.app 模型显示对照
- 已将参考项目 `docs-linhay/references/cc-switch` 更新到 `03a9296c`。最新版仍未发现 Statsig/CDP/network rewrite 入口，Codex 模型显示仍主要依赖 `model_catalog_json` projection。
- 结论保持：GetTokens 已解决 CLI/app-server `model/list` 层；Codex.app 桌面前端弹窗不显示 DeepSeek 的根因仍是前端 Statsig dynamic config `107580212.available_models` 白名单。
- 可吸收改进：cc-switch 现在写 `model_catalog_json = "cc-switch-model-catalog.json"` 相对文件名，并按 filename 判断 owned pointer；这只提升路径兼容性，不解决前端白名单。
- 产品决策保持：不自动切默认 model 到 DeepSeek；不伪装 DeepSeek 为官方 GPT slug。实验性弹窗解锁优先 CDP runtime injection，其次 cache overlay，最后才考虑 backend/system proxy rewrite。

## OpenAI-compatible 详情旧路径未跟随 v09 的根因与修正

- 用户打开 `detail=acct_doubao` 看到旧大表单，原因不是浏览器刷新问题，而是账号详情页真实前端有两条路径：`UnifiedAccountDetailModal` 已按 v09 改造，`OpenAICompatibleDetailModal -> OpenAICompatibleDetailPanel` 仍保留旧单列 cards 布局。
- 修正：把 `OpenAICompatibleDetailPanel` 同步改为 v09 compact header + `AccountDetailModuleStack layout="bands"`，并将旧测试 `single-column form stack` 改为 v09 band 布局测试。
- 当前 `acct_doubao` 无头预览 DOM 显示：`data-account-detail-header="v09-compact"` 为 1，`data-account-detail-section-layout="band"` 为 3，说明当前路径也已回到统一 v09 骨架。
- 验证：账号详情 focused + openAICompatible 测试 56/56 通过，`npm run typecheck` 通过；截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-openai-compatible-after-v01.png`。

## 账号类型详情入口审计与互斥 modal 修正

- 用户要求自查其他账号类型是否还有漏掉。审计 `AccountsFeature` 后确认当前生产详情入口只有两条：`UnifiedAccountDetailModal` 与 `OpenAICompatibleDetailModal -> OpenAICompatibleDetailPanel`；`ApiKeyDetailModal` 是 story/design-system 旧组件，不在生产 `AccountsFeature` import 路径。
- 发现并修复一个叠层问题：从 unified detail 切到 `acct_*` provider detail 时没有清空 `selectedAccount`，可能出现两个 detail modal 同时挂载，造成视觉误判；现在 provider detail 打开前清 unified，普通 detail 打开前关闭 provider detail。
- 无头 DOM 巡检覆盖 `codex-api-key:stable-001`、`codex-api-key:billing-usd`、`acct_deepseek`、`acct_doubao`、`acct_openrouter`，均为单 dialog 且带 v09 compact header；API key 路径走 unified bands，provider 路径走 openAI-compatible bands。
- Auth-file hash 在当前本地 preview 未稳定 hydrate 成 dialog，但其生产入口共用 `UnifiedAccountDetailModal`，已有 focused tests 锁住 auth-file summary/model mapping/v09 band 结构。
- 新增回归测试 `account detail switches keep unified and provider modals mutually exclusive`，账号详情 + OpenAI-compatible focused 测试 57/57 通过，`npm run typecheck` 通过。

## 账号详情页唯一页面收敛

- 用户明确要求账号详情页只能保留一个页面入口：旧的 `ApiKeyDetailModal`、`OpenAICompatibleDetailModal`、`OpenAICompatibleDetailPanel` 必须移除；不同账号类型只允许在 `UnifiedAccountDetailModal` 内拼装不同模块。
- 已将 `AccountsFeature` 的生产详情入口统一到 `UnifiedAccountDetailModal`：`acct_*` OpenAI-compatible provider detail 会解析为对应 `AccountRecord` 后打开 unified detail，不再挂载 provider 专属旧 modal。
- 已删除三个旧独立详情组件文件，并从 Storybook、design-system manifest 与生产 imports 中清理；`useOpenAICompatibleState` 也移除旧 detail draft/save/verify 状态，测试只保留“禁止旧组件回归”的断言。
- 为真实详情 dialog 增加 `data-account-detail-modal="unified"` 标记；浏览器 DOM 巡检覆盖 `codex-api-key:stable-001`、`codex-api-key:billing-usd`、`acct_doubao`、`acct_deepseek`、`acct_openrouter`，均为单 dialog、单 unified marker、单 v09 header、旧 openai marker 为 0。
- 验证：focused accounts/design-system tests 86/86 通过，`npm run typecheck` 通过；截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-unified-only-after-v01.png`。

## 2026-06-05 - GetTokens Codex model_catalog_json pointer 对齐 cc-switch
- 按用户要求先吸收 cc-switch 最新版路径优化：GetTokens 写入 Codex `config.toml` 时改为 `model_catalog_json = "gettokens-model-catalog.json"`，不再写绝对路径。
- `ModelCatalogPath` 结果字段仍返回绝对 catalog 文件路径；关闭/清理同步时按 basename 识别 `gettokens-model-catalog.json`，兼容历史绝对路径与新的相对 filename。
- 本次只优化 catalog pointer 路径兼容；不自动切默认 model，不绕过 Codex.app Statsig 白名单。
- 已通过 `go test ./internal/wailsapp -count=1` 与 model catalog 聚焦回归。

## 账号详情页 Balance 子模块线条收敛

- 用户在 `acct_doubao` 详情页指出 Balance 内 `Quota / 额度追踪` 子模块不需要 header 上方实线，也不需要 header 下方虚线。
- 修正：`AccountDetailSection` 增加 `topBorder` 与 `headerDivider` 开关；`UnifiedAccountDetailModal` 中 Balance split 内的 `AccountQuotaSection` / `AccountBillingSection` 均传入 `false`，避免子模块再次画分隔线。
- 同时将 Quota/Billing 空态改为无边框轻量提示，避免空态 dashed box 顶边紧贴 header 形成“下方虚线”的误读。
- 验证：`accountDetailLayout.test.mjs` 新增/通过 `balance split child headers do not add extra top or dashed divider lines`；浏览器 DOM 验收显示 Quota/Billing section top、header bottom、empty top 均为 `0px`。

## CLIProxyAPI 原生 management.html 退役

- 覆盖早前 Wails LAN 方案中的“状态页打开 Web 管理面板”决策：GetTokens 不再保留 CLIProxyAPI 原生 `/management.html` 控制台入口。
- 修正：`internal/sidecar/writeConfig` 生成或修复配置时强制 `remote-management.disable-control-panel=true` 与 `disable-auto-update-panel=true`，状态页删除 `BrowserOpenURL` / `resolveSidecarManagementWebOpenURL` / “打开 Web”按钮。
- 本机 dev/prod 运行配置已同步禁用控制台，并删除 `/Users/linhey/.config/gettokens-dev/static/management.html` 与 `/Users/linhey/.config/gettokens/static/management.html`；`18317` 与 `8317` 的 `/management.html` 均返回 404。
- 验证：`go test ./internal/sidecar`、`node --test frontend/src/features/status/tests/relayLocalState.test.mjs frontend/src/features/status/tests/statusTypography.test.mjs`、`npm run typecheck` 通过。

## 账号详情页 Header 标签区分割线移除

- 用户在 `claude-relay.json` auth-file/OAuth 详情页指出：header 中部类型/凭据/验证/路由/余额标签行下方的分割线不应显示，需要重新调整间距。
- 修正：`AccountDetailHeader` 中部从 `grid-rows + description border-t` 改为整块 `content-center gap-1 px-2.5 py-2`，description 行移除 `border-t` 并使用紧凑 `leading-tight`。
- 浏览器 DOM 验收：`data-account-detail-header-description` 的 `borderTopWidth=0px`，中部 `gap=4px`。
- 验证：`node --test src/features/accounts/tests/accountDetailLayout.test.mjs` 与 `npm run typecheck` 通过；截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-header-spacing-after-v01.png`。

## 账号详情页 Header 账号名换行与 rail 对齐

- 用户在 `claude-relay.json` 详情页指出 header 左侧账号名需要支持换行，同时宽度要和下方左侧模块 rail 对齐。
- 修正：`AccountDetailHeader` 主 grid 左列从 `11rem` 改为与 band rail 一致的 `10.5rem`；账号名按钮改为 `w-full`、`whitespace-normal`、`break-word`，移除 `truncate`，并将字号降为 `text-base` 以减少无谓三行撑高。
- 浏览器 DOM 验收：header account width 与首个 band rail width 均为 `168px`，账号名 `white-space=normal`、`overflow-wrap=break-word`、`text-overflow=clip`。
- 验证：`accountDetailLayout.test.mjs` 与 `npm run typecheck` 通过；截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-header-name-wrap-after-v02.png`。

## 账号详情页 Auth File 配置区 action bar 收敛

- 用户在 `claude-relay.json` 详情页指出：Auth File 配置管理区右上角按钮需要放同一行，内容区上方的 dashed 分割线取消。
- 修正：`AuthFileSummarySection` 将 `预览配置 / 下载配置 / 应用配置` 三个按钮统一放入 section actions；移除内容 grid 右侧单独的应用按钮。
- `AccountDetailSection` band 布局新增 `bandActionDivider` 开关；Auth File 配置区传 `false`，action bar 下边框宽度为 `0px`。
- 浏览器 DOM 验收：三个按钮 `top=130` 同行，action bar `borderBottomWidth=0px`。
- 验证：`accountDetailLayout.test.mjs` 与 `npm run typecheck` 通过；截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-auth-config-actions-after-v02.png`。

## 账号详情页 Quota/Billing 脚本控件提升到模块头部

- 用户在真实账号详情页指出 Quota 内部脚本卡片里的 `启用额度 / 编辑脚本` 也应放到模块头部。
- 修正：`AccountQuotaSection` 有额度脚本时，将 `启用额度 / 编辑脚本 / 测试` 全部放进 section header actions；脚本卡片只保留 curl 预览文本。
- 同步统一 `AccountBillingSection`：有余额脚本时，`启用余额 / 编辑脚本 / 测试余额` 也放在模块头部；没有脚本时仍显示 `添加`。
- 浏览器 DOM 验收：Quota header actions 三个控件 top 均为 `664`，body 中不再包含 `启用额度` 或 `编辑脚本`。
- 验证：`accountDetailLayout.test.mjs` 与 `npm run typecheck` 通过；截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-quota-actions-header-after-v01.png`。

## 账号详情页 Route Guard 规则行左右对齐

- 用户在账号详情页 Route Guard 规则行指出：`TOKEN 窗口限流 / 1H / 1M / 阻断` 规则行的左右间距要和上方 dashed 线对齐。
- 修正：`RateLimitRulesSection` 的 `RATE_LIMIT_RULE_SURFACE_CLASS` 移除水平 `px-3`，保留 `py-2`；规则行外边界与上方 action divider 共用同一内容宽度。
- 浏览器 DOM 验收：actionBar 与 listitem 均为 `left=210/right=1222/width=1012`，listitem `paddingLeft=0px`、`paddingRight=0px`。
- 验证：`rateLimit.test.mjs`、`accountDetailLayout.test.mjs` 与 `npm run typecheck` 通过；截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-route-guard-row-align-after-v01.png`。

## 账号详情页 Credential/Connection 模块恢复左右布局

- 用户在账号详情页指出 Credential/Connection 区域应该是左右布局，而当前真实前端仍把 Credential、Connection、Route 纵向堆叠。
- 修正：`AccountCredentialVerifySection` 从 `v09-low-nesting` 改为 `v09-split`，外层使用 `lg:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]`；左栏只放 Credential 字段，右栏放 Connection + Route，并用右栏左分割线区分。
- 浏览器 DOM 验收：左右栏 grid template 为 `498px 498px`，left/right top 均为 `100`，右栏 `borderLeftWidth=2px`，确认是并排结构。
- 验证：`accountDetailLayout.test.mjs` 与 `npm run typecheck` 通过；截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-credential-split-after-v01.png`。

## 账号详情页模型映射模块补齐

- 用户追问账号详情页缺失模型映射模块；根因是 `accountDetailLayout` 只给 auth-file/OAuth 返回 `models`，API key / OpenAI-compatible 详情模块计划未包含模型映射。
- 已将 API key / OpenAI-compatible 详情顺序改为 `credentials -> models -> rate-limit -> quota -> billing`，模型映射模块统一在 `UnifiedAccountDetailModal` 内渲染。
- `CompatibleModelsSection` 改为“模型映射”，API-like 账号支持 Source Model / Alias Route 行内编辑、添加和删除，并把 `models` 纳入 `ApiKeyConfigDraft` 与保存脏检查。
- 保存链路：Codex API key 继续走 `UpdateCodexAPIKeyConfig` 并带 `models`；OpenAI-compatible 账号在 Accounts 详情内走 `UpdateOpenAICompatibleProvider` 并带 `models`，避免统一详情页里模型映射只是展示。
- 验证：账号详情/openAI-compatible/rate-limit/design-system focused tests 89/89 通过，`npm run typecheck`、`npm run build`、`check-docs.sh` 通过。

## 账号详情页 Quota 脚本预览两行高度

- 用户在 Balance / Quota 区指出 curl 脚本预览只显示一行太窄；已将 Quota 脚本预览改为固定两行高度，长 curl 使用两行 line clamp + break-all，不再单行 truncate。
- 新增回归测试 `quota script preview uses a fixed two-line readable height`，锁住 `data-account-quota-script-preview="two-line"`、`min-h-[2.75rem]` 与 `line-clamp-2`。
- 验证：`accountDetailLayout.test.mjs` 39/39 通过，`npm run typecheck` 通过。

## 账号详情页 Connection 归入左侧凭据模块

- 用户指出短消息验证 `CONNECTION` 不应位于右侧路由模块。修正后 `AccountCredentialVerifySection` 的左侧为 `credential-connection`（Credential + Connection），右侧为 `route`（只放代理/出口）。
- 回归测试更新：`api key credential module uses left-right credential and connection layout` 锁住 `data-account-credential-left-pane="credential-connection"` 与 `data-account-credential-right-pane="route"`，防止 Connection 再被移回右侧 Route pane。
- 验证：`accountDetailLayout.test.mjs` 39/39 通过，`npm run typecheck` 通过。

## 账号详情页 Header 左侧显示账号类型

- 用户指出 header 左侧不应显示账号名（如“公司 1”），应显示账号类型。修正后 `AccountDetailHeader` 左侧使用 `resolveAccountHeaderTypeLabel` 输出 `CODEX API KEY / CODEX OAUTH / OPENAI COMPATIBLE`，并以 `data-account-detail-header-account-type="true"` 标记。
- 回归测试锁住：不再调用 `resolveAccountPrimaryLabel(account)`，左侧不再作为 `onRename` 点击编辑入口。
- 验证：`accountDetailLayout.test.mjs` 39/39 通过，`npm run typecheck` 通过。

## 账号详情页 Credential 内编辑账号名称

- 用户指出 Credential 模块内应在 `API 密钥 / 基础 URL / 前缀` 前增加账号名称编辑输入框，账号名不再放在 header 左侧编辑。
- 修正：`ApiKeyConfigDraft` 增加 `label`，`AccountCredentialVerifySection` 首位渲染 `账号名称`；Codex API key 保存链路调用 `UpdateCodexAPIKeyLabel`，OpenAI-compatible 保存链路将 label 作为 provider `name` 提交，统一由底部保存按钮提交。
- 回归测试锁住：账号名称字段必须出现在 API 密钥之前，并纳入 `hasApiKeyConfigChanges` / 预览保存路径。
- 验证：账号详情/accountConfig/openAI-compatible/rate-limit/design-system focused tests 114/114 通过，`npm run typecheck` 与 `npm run build` 通过。

## 账号详情页模型映射额外虚线移除

- 用户指出模型映射模块 `添加映射` 下方多一条 dashed 分割线。修正：`CompatibleModelsSection` 关闭 `bandActionDivider`，只保留空态/内容自身边界。
- 验证：`accountDetailLayout.test.mjs` 40/40 与 `npm run typecheck` 通过。

## 账号详情页 Header 运行态摘要移除

- 用户要求移除 header 右侧 `LAST / RUNTIME / LATENCY` 小模块。修正后 header 为两栏结构：账号类型 rail + 信息标签区，不再展示运行态摘要，也不保留右侧分割线。
- 验证：`accountDetailLayout.test.mjs` 40/40 与 `npm run typecheck` 通过。

## 账号详情页模型映射补齐拉取与默认列表

- 用户指出模型映射模块缺少 `拉取模型` 按钮和默认模型列表。修正后模型映射 header 提供 `拉取模型 / 默认模型 / 添加映射`，空态展示默认模型列表。
- 拉取模型通过 `FetchOpenAICompatibleProviderModels` 使用当前 draft 凭据和 base URL，成功后写入 `ApiKeyConfigDraft.models`；默认模型从当前模型名、账号已有模型、vendor preset 建议和 Codex fallback 归一化生成。所有变更仍走底部保存统一提交。
- 验证：账号详情/openAI-compatible/accountConfig focused tests 84/84、`npm run typecheck`、`npm run build` 通过。

## 账号详情页默认模型列表来源收窄

- 用户指出模型映射的默认模型列表应显示“映射本身模型”，不能混入全局 relay/Codex 模型池。修正后默认模型只来自当前账号已有 `models` 与 baseUrl 命中的 vendor preset suggestions，不再使用全局 `modelNames` 或 GPT fallback。
- 验证：`accountDetailLayout.test.mjs` 40/40 与 `npm run typecheck` 通过。

## 账号详情页模型映射行交互收敛

- 用户要求模型映射列表不内部滚动、由内容撑高；去掉行内 `SOURCE MODEL / ALIAS ROUTE` 显示标签；source 侧只给当前账号支持模型，alias 侧给本地全部模型列表且支持搜索/自定义输入。
- 修正后模型映射 grid 无 `max-h` / `overflow-auto`；source 和 alias 分别使用 `sourceModelOptionNames` 与 `aliasModelOptionNames`，Accounts 页面额外传入 `localModelNames={relayModelNames}` 作为 alias 候选池。
- 验证：账号详情/openAI-compatible/accountConfig focused tests 84/84、`npm run typecheck`、`npm run build` 通过。

## 账号详情页模型映射下拉改用 Combobox

- 用户反馈模型映射行下拉无法触发。根因是原生 `input + datalist` 在当前 in-app/Wails 浏览器下没有稳定显式展开行为。
- 修正：source / alias 输入均改用项目 `Combobox`，下拉层通过 portal 渲染，保留搜索过滤与自定义输入；source 使用当前账号支持模型候选，alias 使用本地全部模型候选。
- 验证：账号详情/openAI-compatible/accountConfig focused tests 84/84、`npm run typecheck`、`npm run build` 通过。

## 账号卡片无规则时显示独立流量统计

- 用户在账号池指出：当前只有配置了 route guard 的账号卡片显示底部 guard 模块；本期要求未配置规则的账号也显示同一模块，但标题改为“流量统计”。
- 修正：无规则分支改为独立的当日流量统计，不再显示 `未配置` 或 `暂无路由守卫规则`；模块以 `今日请求`、`今日 Token` 两行表达当前累计值到 `∞` 的范围。有规则时继续显示 `ROUTE GUARD / PASS|BLOCKED` 与规则进度。
- 验证：`node --test frontend/src/features/accounts/tests/rateLimit.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs`、`npm --prefix frontend run typecheck` 通过；Playwright DOM 验收账号池无规则卡不含 `未配置`、`暂无路由守卫规则`，并保留 `今日请求 / 今日 Token / ∞` 统计行，1 张有规则卡保留 `ROUTE GUARD`，截图归档到 `docs-linhay/spaces/20260515-rate-limit-middleware/screenshots/20260605/account-cards/20260605-account-card-traffic-statistics-after-v02.png`。

## 账号卡片旧 TrafficMetricsModule 下线

- 用户继续指出账号卡上方 `近期请求 / 24H / 峰值 / 当前 / CACHED / 平均耗时` 模块需要移除下线。
- 修正：从 `AttributionCard` 生产渲染移除 `TrafficMetricsModule`，并删除 `TrafficMetricsModule`、`TrafficSection`、`UsageMetrics`、旧曲线计算与对应 Storybook 入口；保留底部 `流量统计` 作为账号卡流量入口。
- 验证：accounts/design-system tests 357/357、`npm --prefix frontend run typecheck` 通过；Playwright DOM 验收 `.account-card-traffic-module`、`.account-card-traffic`、`.account-card-usage-metrics` 均为 0，旧 `24H / 峰值 / 平均耗时` 组合不再存在，截图归档到 `docs-linhay/spaces/20260515-rate-limit-middleware/screenshots/20260605/account-cards/20260605-account-card-traffic-statistics-after-v03.png`。

## 账号卡片 Footer 操作区等分

- 用户指出账号卡底部 `详细参数 / 刷新额度` 上方虚线需要移除，两个按钮同一行等分宽度。
- 修正：`AccountCard` footer action grid 移除 `border-t border-dashed pt-3`，删除动态 `account-card-action-grid-1/2/3` 列 class；`style.css` 固定 `.account-card-action-grid` 为双列等分，第三个 OAuth 重新授权动作使用 `.account-card-action-grid-span` 跨整行。
- 验证：`accountCardLayout.test.mjs`、`rateLimit.test.mjs` 与 `npm --prefix frontend run typecheck` 通过；Playwright DOM 验收 action grid `borderTopWidth=0px`、`paddingTop=0px`、两列 `136px 136px`，`详细参数` 与 `刷新额度` 同 y 坐标同宽，截图归档到 `docs-linhay/spaces/20260515-rate-limit-middleware/screenshots/20260605/account-cards/20260605-account-card-footer-actions-after-v04.png`。

## 账号卡片 Footer 顶线改为实线

- 用户补充指出按钮组上方仍应保留分隔，但虚线要换成实线，且按钮上方间距需要调整。
- 修正：`RateLimitGuard` / `流量统计` section 不再输出底部 dashed border，改由 `AttributionCard` footer wrapper 统一输出 solid `border-t` 与 `pt-3`；按钮 grid 继续只负责两列等分，不承担分隔线。
- 验证：`accountCardLayout.test.mjs`、`rateLimit.test.mjs` 与 `npm --prefix frontend run typecheck` 通过；Playwright DOM 验收 footer wrapper `borderTopStyle=solid`、`paddingTop=12px`，前置统计 section `borderBottomWidth=0px`，两按钮同宽同一行，截图归档到 `docs-linhay/spaces/20260515-rate-limit-middleware/screenshots/20260605/account-cards/20260605-account-card-footer-actions-solid-after-v05.png`。

## 账号卡片无规则统计头部移除

- 用户指出无规则统计模块头部 `流量统计 / 无限上限` 需要移除。
- 修正：`RateLimitGuard` 只在有规则行时渲染 `ROUTE GUARD / PASS|BLOCKED` 头部；无规则统计分支只保留 `今日请求` 与 `今日 Token` 两行，并删除不再使用的 `traffic_statistics` / `traffic_statistics_unbounded` locale key。
- 验证：`rateLimit.test.mjs`、`accountCardLayout.test.mjs` 与 `npm --prefix frontend run typecheck` 通过；Playwright DOM 验收无规则 section 不含 `流量统计` / `无限上限`，仍含 `今日请求` / `今日 Token`，截图归档到 `docs-linhay/spaces/20260515-rate-limit-middleware/screenshots/20260605/account-cards/20260605-account-card-traffic-rows-no-heading-after-v06.png`。

## 账号卡片无规则统计刻度移除

- 用户指出无规则统计行下方的底部 `0 / ∞` 刻度文字没有实际信息，且与右上角 `0 / ∞` 重复；随后确认进度条本身仍应保留。
- 修正：`TrafficStatisticsRow` 保留每行空进度条，只移除进度条下方的 `0` / `∞` 刻度文字；配置了 route guard 的规则进度条不受影响。
- 验证：`rateLimit.test.mjs`、`accountCardLayout.test.mjs` 与 `npm --prefix frontend run typecheck` 通过；Playwright DOM 验收无规则统计行每个模块有 2 条进度条，底部 `0/∞` 刻度为 0，截图归档到 `docs-linhay/spaces/20260515-rate-limit-middleware/screenshots/20260605/account-cards/20260605-account-card-traffic-bars-no-labels-after-v08.png`。

## 账号卡片无额度遥测占位隐藏

- 用户指出 PLUS 卡片中的 `当前凭证无额度遥测` 没有数据时不应显示该模块。
- 修正：生产 `AttributionCard` 不再渲染 `UnsupportedQuotaPlaceholder`；有真实 quota windows 时仍显示 `QuotaBars`，有 billing 数据时仍显示 `BillingBalance`，无数据时直接进入今日统计模块。
- 验证：`accountCardLayout.test.mjs`、`rateLimit.test.mjs` 与 `npm --prefix frontend run typecheck` 通过；Playwright DOM 验收账号池页面 `当前凭证无额度遥测` section 为 0，12 个无规则统计模块与 24 行统计行仍存在，截图归档到 `docs-linhay/spaces/20260515-rate-limit-middleware/screenshots/20260605/account-cards/20260605-account-card-hide-unsupported-quota-after-v09.png`。

## 账号详情页模型映射按钮语义与反馈修正

- 用户指出模型映射下拉文字需左对齐，且 `默认模型 / 添加映射` 点击无反应、概念不清。修正后 alias Combobox 不再右对齐；`默认模型` 改为 `填入支持模型`，空候选时给出提示；`添加映射` 会优先填入一个未使用的当前账号支持模型并显示反馈。
- 验证：账号详情/openAI-compatible/accountConfig focused tests 84/84、`npm run typecheck`、`npm run build` 通过。

## 账号详情页 Quota 测试结果窗口回显与单模块 Balance

- 用户指出额度脚本测试成功后只显示 `OK - ... windows` 文本，没有显示窗口；且无有效 Billing 时不应保留右侧空模块。修正后 `AccountQuotaSection` 将测试返回 `windows` 归一成 `QuotaDisplay`，测试成功直接复用 `QuotaBars` 显示 `QUOTA (TEST)`；Balance 在无实时 Billing 且无 `billingCurl` 时切到 `quota-only / quota-full`，不再显示右侧空栏和 full-height 分割线。
- 验证：账号详情/openAI-compatible/accountConfig focused tests 86/86、`npm run typecheck`、`npm run build` 通过；无头浏览器验证当前账号 detail 为 `quota-only`，点击测试后展示 5H/7D quota rows，截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-quota-test-after-v02.png`。

## 账号池缩略模式下线

- 用户指出账号池视图切换中的 `缩略` 模式需要移除。修正后 `AccountListDisplayMode` 只保留 `full | list`，toolbar 只显示 `完整 / 列表`；旧 `density=compact` hash 回退到完整卡片，compact grid CSS token、compact delete overlay、compact-only quota/billing 选择器、Storybook/manifest/locale 入口同步清理。
- 验证：账号列表/账号卡/route guard/delete overlay/selectors/design-system focused tests 79/79 与 `npm --prefix frontend run typecheck` 通过；Playwright DOM 验收 `#frame=accounts` 与 `#frame=accounts&density=compact` 均只显示 `完整 / 列表`，`缩略` 文案为 0，`.account-card-grid-compact` 为 0。截图归档到 `docs-linhay/spaces/20260515-rate-limit-middleware/screenshots/20260605/account-cards/20260605-account-card-remove-compact-mode-after-v10.png`。

## 账号详情页 Quota 脚本右侧布局与 Billing 添加入口

- 用户纠正：隐藏 Billing 不等于整块铺满，Balance 行仍应分左右区域；Quota 窗口在左，curl 脚本预览在右；同时必须有添加另一个 Balance 模块的按钮。
- 修正：`AccountQuotaSection` 增加内部 `windows/script` split，脚本预览右置；无 Billing 模块时 Balance header 展示 `添加余额模块`，点击后通过 `billing` script route 挂载 Billing pane 并打开 `余额脚本` curl editor。
- 验证：账号详情/openAI-compatible/accountConfig focused tests 87/87、`npm run typecheck`、`npm run build` 通过；无头浏览器确认 `quotaLayout=split`、`scriptRightOfWindows=true`、点击添加后 `panel=quota-billing` 且 `余额脚本` 编辑器出现。截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-add-billing-module-after-v04.png`。

## 账号详情页 Balance 左 rail 模块勾选

- 用户指出 Balance 模块添加入口应放在左侧 rail，而不是右上角按钮。修正后 `AccountDetailSection` band rail 支持 `railControls`，Balance 左侧显示 `额度模块 / 余额模块` 两个 checkbox。
- `余额模块` 勾选后设置 `billingEnabled=true`，通过 `billing` script route 显示右侧 Billing pane 并打开 `余额脚本` 编辑；右上角 `添加余额模块` 已移除。
- 验证：账号详情/openAI-compatible/accountConfig focused tests 87/87、`npm run typecheck`、`npm run build` 通过；无头浏览器确认 `railControls=true`、`topAddButton=false`、勾选余额后 `panel=quota-billing` 且 `curlEditorTitle=true`。截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-balance-checkbox-add-billing-after-v06.png`。

## 账号详情页 Balance checkbox 只控制模块显隐

- 用户指出 Balance 左 rail 的 `额度模块 / 余额模块` checkbox 不应触发脚本弹窗。修正后 checkbox toggle 只更新 draft enabled 状态和模块显隐，不再调用 `onOpenScriptEditor`；脚本弹窗只能由模块内 `添加 / 编辑脚本` 按钮触发。
- 验证：账号详情/openAI-compatible/accountConfig focused tests 87/87、`npm run typecheck`、`npm run build` 通过；无头浏览器确认勾选余额后只显示 Billing pane，`curlEditor=false`。截图归档到 `docs-linhay/spaces/20260529-account-detail-ui/screenshots/20260605/account-detail/20260605-account-detail-balance-checkbox-no-modal-after-v07.png`。

## 账号详情页 Balance / Quota split 细节收口

- 决策：Balance 左 rail 的 `额度模块 / 余额模块` 是模块显隐唯一开关；Quota / Billing 模块 header 不再重复放 `启用额度 / 启用余额`。
- 修正：账号详情内复用 `QuotaBars` 时关闭账号卡底部分隔线；split 布局下右侧脚本卡片拉伸到与左侧额度窗口区同高感。
- 验证：账号详情、账号卡、openAI-compatible、accountConfig focused tests 共 104/104 通过；`npm run typecheck`、`npm run build` 通过。

## 账号详情页 Balance 三态与 header 整理

- 决策：Balance 内容由左 rail checkbox 决定，支持 `quota-billing / quota-only / billing-only` 三态；取消额度模块后 Quota section 必须完全不渲染，只剩余额时 Billing 使用单模块形态。
- UI 收口：账号详情嵌套模块 header 使用紧凑 title/action row，meta 不再单独成松散一行。
- 验证：账号详情与 design-system focused tests、typecheck、build、docs check 通过。
