# 2026-05-23

## Claude Usage Desk attribution filter fix

- 问题：`#frame=claude&workspace=usage` 在真实 sidecar attribution 有数据时仍可能显示空，根因是 `UsageDesk` 的 Claude workspace 只按 `provider/model/accountKey/attributionKey` 文本是否包含 `anthropic/claude/sonnet/opus/haiku` 判断，漏掉了 `openai-compatible:deepseek`、`bailian`、`mimo` 等 Anthropic-compatible 供应商。
- 修复：`frontend/src/features/accounts/model/usageDesk.ts` 新增 Claude provider/accountKey 判定，保留显式 Claude/Anthropic 关键词规则，同时让已知 Anthropic-compatible provider 和 `openai-compatible:<provider>` / `auth-file:<provider>` accountKey 进入 Claude observed usage；`github/openai_chat` 类非 Claude provider 仍被过滤。
- 回归：`frontend/src/features/accounts/tests/usageDesk.test.mjs` 新增 DeepSeek 正例，并把非 Claude 负例改为 GitHub，覆盖 “account key 映射到 Anthropic-compatible 格式时 Claude Usage Desk 不应为空”。
- 验证：`node --test frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/utils/pagePersistence.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 均通过。`npm --prefix frontend run test:unit` 暴露两个无关既有问题：`codexAccountList.test.mjs` 引用不存在的 `moveCodexAccountRowToEdge`，以及 `settingsBuildMetadata.ts` 在 Node test 环境读取缺失的 `import.meta.env.VITE_GIT_HASH`。

## Settings release panel design-system admission

- 决策：将 `SettingsFeature` 更新区块抽为 `frontend/src/features/settings/components/SettingsReleasePanel.tsx`，纳入设计系统业务组件，运行时根节点暴露 `data-design-system-component-name="SettingsReleasePanel"`。
- 实现：设置页更新面板新增当前项目构建 `Git Hash` 字段；前端通过 `VITE_GIT_HASH` 注入，开发态由 Vite 自动读取 `git rev-parse --short=12 HEAD`，release workflow 使用 `${{ github.sha }}` 注入并由前端截断为 12 位。
- 回归：新增 `SettingsReleasePanel.stories.tsx`、`settingsBuildMetadata.test.mjs`，并更新 `componentManifest.ts` / `storyCatalog.ts` / `storyCatalog.test.mjs`，覆盖 ready、checking、native-updater 和 git-hash 状态。
- 验证：`node --test frontend/src/features/settings/settingsBuildMetadata.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`npm --prefix frontend run build-storybook`、`npm --prefix frontend run test:unit` 均通过。测试期间曾短暂暴露无关 Codex 账号顺序断言失败，随后并行工作区改动补齐后复跑通过；本轮未接管该 Codex 改动。

## Codex account order edge actions

- 决策：在 Codex 账号顺序卡片的拖动控制区补充纵向 `置顶` / `置底` 操作，作为拖拽排序的直接快捷入口；同一排序 section 被 Claude Code 复用，因此 Claude Code 页面同步接入首尾移动链路以保持类型与行为一致。
- 实现：`CodexAccountOrderRow` 新增首尾移动按钮，`CodexAccountOrderSection` 传递是否可移动到首尾，Codex / Claude Code feature 点击后更新本地顺序、标记未保存并触发既有自动保存流程。
- 回归：新增 `moveCodexAccountRowToEdge` 与 `moveClaudeCodeAccountRowToEdge` 模型断言，并用 Codex source test 锁定 row 暴露 `onMoveToTop` / `onMoveToBottom` 与本地化 key。
- 验证：`node --test src/features/codex/codexAccountList.test.mjs src/features/claude-code/claudeCodeAccountList.test.mjs`、`npm run typecheck`、`npm run build` 均通过（工作目录：`frontend/`）。
