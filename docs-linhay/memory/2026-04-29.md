# 2026-04-29

## 网络代理参考实现整理

- 已新建研究空间 `docs-linhay/spaces/20260429-network-proxy/`，用于收敛参考项目 `CLIProxyAPI` 的网络代理实现，不再和“agent 代理”混用概念。
- 当前结论：
  - 参考项目把网络代理抽象成统一的 `proxyutil` 层，支持 `inherit / direct / proxy / invalid` 四种语义。
  - 全局 `proxy-url` 与账号级 `proxy-url` 并存，真实生效时优先走账号覆盖，再退回全局。
  - HTTP、uTLS/HTTP2、Codex WebSocket、管理 API 诊断请求都各自接入了同一套代理语义，而不是只改普通 HTTP client。
  - `direct/none` 被当作显式直连语义，区别于空字符串“继承默认行为”。
- 对 GetTokens 的边界启发已写入该 space README：后续如果做代理设置，必须同时打通 UI 配置、sidecar 配置模型、管理/诊断请求和特殊执行器，而不是只补一个前端输入框。

## Codex API Key 配置保存与稳定 ID

- `ApiKeyDetailModal` 为 `api-key` 账号补齐了配置保存入口，支持直接保存 `apiKey`、`baseUrl`、`prefix`，不再只有验证按钮。
- `codex-api-key` 账号新增持久化 `LocalID`，前端和后端后续都以稳定 `id` 作为主键，避免编辑配置后因为派生标识变化导致详情态、禁用态或排序引用失效。
- `UpdateCodexAPIKeyConfig` 以稳定 `id` 更新配置，同时保持去重规则仍基于 `apiKey + baseUrl + prefix` 的归一化身份判断。
- 已把“稳定 `codex api key` 身份 + disabled 账号保留轮动位置 + 轮动卡片复用账号池卡片语言”沉淀到 `gettokens-domain-engineering`，并补充研发文档 `20260429-account-rotation-and-api-key-identity-boundary.md`。
- 回归验证已覆盖 `go test ./internal/accounts ./internal/wailsapp`、`frontend npm run test:unit`、`frontend npm run typecheck`。

## Nolon 会话管理研究空间与 debate

- 新建了研究空间 `docs-linhay/spaces/20260429-nolon-session-management/`，用于承载本轮对参考项目 `nolon` 会话管理实现的研究。
- 已产出 debate 纪要：`docs-linhay/spaces/20260429-nolon-session-management/debate/20260429/nolon-session-management/20260429-nolon-session-management-v01.md`。
- 当前阶段性结论：
  - `nolon` 的会话对外口径主要是 `CodexSessionStore` 聚合出的 `CodexSessionRecord` / snapshot，而不是单一 JSONL 或单一 SQLite。
  - 列表首屏采用 `projection cache -> skeleton -> snapshot stream -> reconcile` 的分层加载链路。
  - `rewrite` 是 preview、rollout 文件改写、state db 改写、一致性校验组成的多介质链路，不是事务性原子操作。
  - CLI 与 UI 共用 rewrite 引擎，但 grouping 口径已出现漂移：CLI 仍保留 `time-project`，当前 SwiftUI ViewModel 只剩 `project/provider`。
- 继续深挖后补充确认：
  - `summary` 在当前 `CodexSessionStore` 首屏链路里被明确置空，测试也约束“不从 rollout 正文提炼摘要”。
  - 搜索仅覆盖 `title / displayID / provider / summary / cwd` 这些展示字段，不直接依赖 usage index 或全文索引。
  - `usage index` 的真实职责是 usage 聚合、usage 排序预热与 timeline 复用；列表主快照仍由 projection cache 承担。
  - `time-project` 并非纯文档残留：2026-04-11 设计曾要求 UI/CLI 对齐该模式，但 2026-04-15 的 `project-first` 重构已把 SwiftUI UI 收缩为 `project/provider`，CLI 没同步收口。
  - `rewriteProviders` 的实际执行顺序是 `preview -> live rollout -> archived rollout -> state db -> invalidate cache -> verify`；rollout 侧只改首个 `session_meta` 行中的 `model_provider`，state db 侧只改 `threads.model_provider`。
  - `rewrite` 不是事务性原子操作；测试覆盖了“rollout 已改成功但 SQLite 更新失败”的部分成功场景，系统通过 `failures + verify` 暴露不一致，而不是自动回滚已写入的 rollout。
  - 首屏时序已进一步确认：`cached snapshot` 命中且 `clean + fresh` 时可直接短路 reconcile；否则先展示缓存，再走稳定 snapshot；只有缺缓存时才退回 `skeleton -> stream -> fallback snapshot`。
  - 前台 autoresearch 已将 README 收口到“完整分析完成”：
    - `snapshotStream` 的 delta 语义与 `removedSessionIDs` 的最终批次删除逻辑已经补齐。
    - `logicalUsageKey` 与 forked rollout post-fork usage dedupe 的双层去重语义已经补齐。
    - 已新增一张分层边界矩阵，明确 rollout / state sqlite / projection cache / usage index / ViewModel / CLI 各自职责。
    - 随后又补了两层收口：一张“结论 -> 代码/测试证据”矩阵，以及一条 2026-04-11 到 2026-04-21 的实现演进时间线，方便后续回看时快速判断哪些是当前语义、哪些是历史沉积。
  - 继续下钻后，又补了一段“迁移/复用指南”：把 `nolon` 的 session 管理模式和 GetTokens 当前 `usage_local.go` 的本地 usage index 实现对齐，明确哪些模式可借，哪些产品边界不能直接照搬。
  - 随后按用户要求把相关研究材料进一步收拢到单个 space：repo 根的 `research-results.tsv`、`autoresearch-state.json` 已移动到 `docs-linhay/spaces/20260429-nolon-session-management/plans/`，并补了一份 `autoresearch-summary-v01.md` 作为该 space 的运行归档入口。
  - 这轮 autoresearch 的 repo 级 `check-docs` 守护暴露了一个基线外问题：另一个 space 下预存的 `.gitkeep` 会让全局文档校验失败，因此本轮实际守护收窄为当前研究 space 的局部自洽检查，没有去顺手改无关 space。

## Usage Desk 本地投影总量差异归因

- 已确认 `Usage Desk -> 本地投影用量 -> 全部` 与 `nolon` 数值不一致，根因不是前端汇总公式，也不是 GetTokens 的 SQLite 索引丢数。
- 实测 GetTokens 旧实现只扫描 `~/.codex/sessions/**/*.jsonl`，因此本机原始全扫与索引库都稳定落在约 `185.67 亿`。
- `nolon` 本机索引库额外纳入了 `~/.codex/archived_sessions/**/*.jsonl`；其 `usage_entries` 明显更多，导致总量跃升到约 `364 亿` 量级。
- 已在 `APP 层` 修复扫描边界：`GetCodexLocalUsage` 现在同时扫描 `sessions/` 与 `archived_sessions/`，并新增回归测试覆盖 archived rollout 被纳入统计的场景。
- 本轮回归已通过 `go test ./internal/wailsapp/...` 与 `frontend npm run typecheck`。

## Usage Desk 本地投影加载进度

- `APP 层` 为本地投影索引扫描补充了 `usage-local:progress` 事件，当前会向 `H5` 发出 `phase / currentFile / processedFiles / totalFiles / source`。
- `H5` 的 `UsageDeskWorkspace` 已在本地投影加载态接入该事件，`StatePanel` 现在会显示当前处理文件、已处理数量和当前来源（缓存命中 / 增量追加 / 全量重建）。
- 已补充回归测试保护进度文件路径展示为相对 `codexHome` 路径，不向 UI 直接暴露绝对路径。

## Usage Desk 前端交接边界

- 本轮已把 `Usage Desk` 的数据链、扫描边界、进度反馈、图表/表格联动和核心交互闭环收稳。
- 后续前端视觉精修默认交由 Gemini 处理；Codex 继续负责领域边界、接口契约、状态流转、测试门禁与最终联调验收。
- 已为此补建 handoff space：`docs-linhay/spaces/20260429-usage-desk-controls-redesign/`，当前包含 `README.md`、标准子目录和 `design-preview.html` 预览稿。

## Usage Desk APP 层缓存与后台增量刷新

- 已确认旧行为存在产品偏差：`H5` 每次刷新页面都会重新调用 `GetCodexLocalUsage` 并触发 `APP 层` 文件清单校验，这虽然复用了 SQLite 索引，但用户仍会看到“重新扫描样本”。
- 当前裁定已经改成 `APP 层` 持有 `LocalProjectedUsage` 进程内快照缓存：
  - `H5` 正常页面刷新默认直接读取 `APP` 当前缓存
  - 只有点击 `刷新索引 / 重建索引` 才显式触发新的增量刷新 / 重建
- `APP 层` 新增了后台分钟级增量刷新编排：
  - 定时器由 `APP` 自己驱动，不下放到 `H5`
  - 定时刷新继续复用本地 SQLite 增量语义，不改成前端轮询
  - 刷新完成后通过 `usage-local:updated` 事件把最新快照推回 `H5`
- `Settings` 已新增本地投影后台刷新间隔设置，当前允许：
  - `5 分钟`
  - `15 分钟`
  - `30 分钟`
  - `1 小时`
- 当前回归验证已覆盖：
  - `go test ./...`
  - `frontend npm run test:unit`
  - `frontend npm run typecheck`
  - `frontend npm run build`

## Usage Desk 图表点位命中偏移

- 已确认 `frontend/src/features/accounts/components/UsageDeskWorkspace.tsx` 的图表点位存在点击热区偏到右下的问题，根因是 `ChartPoint` 外层绝对定位盒子没有以 `(x, y)` 做居中平移，视觉圆点和实际点击框不重合。
- 当前已把点位样式收敛到 `buildUsageDeskChartPointStyle`，统一强制 `translate(-50%, -50%)`，并补了回归测试保护“点位坐标即命中中心”的契约。
- 本轮回归已通过 `frontend npm run test:unit` 与 `frontend npm run typecheck`。

## Usage Desk 短范围天级趋势点线错位

- 已确认 `7D / 14D` 的天级趋势点位不在线上，根因不是贝塞尔平滑本身，而是 `ChartSurface` 在短序列下会被 `min-w-full` 拉到容器全宽；`svg` 折线按放大后的实际宽度渲染，HTML 点位仍按原始 `chartWidth` 定位，导致两层坐标系不一致。
- 当前已新增 `resolveUsageDeskChartRenderWidth`，并让折线、面积、点位统一使用同一个 `renderWidth`；其中最后一个遗漏点是 `ChartPoint` 的 `x` 计算此前仍引用旧的 `chartWidth`，补齐后 `7D / 14D` 的真实页面截图已确认点线重新对齐。
- 本轮回归已通过 `frontend npm run test:unit` 与 `frontend npm run typecheck`。

## Usage Desk 时间窗口宽度与曲线动画

- 产品裁定更新：`7D / 14D` 不需要为了填满卡片而被强行拉伸到整块画布宽度，短窗口应该按点数使用更自然的内容宽度。
- 时间窗口切换时需要的是“曲线本身”的动画，而不是整块图表容器的窗口淡入淡出；当前已把动画下沉到折线、面积和点位层，切换 `7D / 14D / 30D / 全部` 时直接表现为曲线 sweep + 面积淡入 + 点位上浮。
- 真实页面已重新回归：`7D` 与 `14D` 的最终态截图分别记录为 `...7d-after-v03.png` 与 `...14d-after-v03.png`，用于确认收窄宽度后点线仍保持对齐。

## 本轮整理沉淀

- 已把“`svg` 曲线 + HTML 点位` 混合图层必须共用同一坐标系，并且要用真实页面截图验收”的规则沉淀到项目级 `skill`：`.agents/skills/gettokens-domain-engineering/SKILL.md`。
- 已补充研发文档 `docs-linhay/dev/20260429-usage-desk-chart-layer-alignment.md`，记录短窗口自然宽度、曲线层动画和截图验收步骤。
- 这次沉淀属于项目级前端/图表调试模式，不升级为 repo-wide `AGENTS.md` 规则。
