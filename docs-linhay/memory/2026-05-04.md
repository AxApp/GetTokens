# 2026-05-04

## 请求编排画布交互回归

- 验证对象：`docs-linhay/spaces/20260502-request-orchestration-menu/request-orchestration-design-v01.html`
- 交互结论：请求编排设计稿已确认支持桌面画布空白区平移、主节点头部拖动、入口到账号组拖线、账号到代理拖线、代理到结束节点拖线。
- 关键修正：无效拖拽松手时必须回滚到拖拽前上下文，不再污染 `focus / currentAccountId / lastAction / previewRouteTarget`；成功落到合法目标后才提交状态。
- 实现收敛：画布平移命中区从内联长表达式改为显式 `canPanStage` 判定，减少空白区拖动与节点/卡片交互冲突。
- 浏览器验收：使用 `agent-browser` 对设计稿执行真实鼠标拖拽回归；已验证成功链路和取消拖拽回滚，并归档截图 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-canvas-after-v01.png`。

## 请求编排固定流程收敛

- 关键决策：当前需求不需要自由画布、节点拖拽或连线编辑；请求编排页收敛为固定四阶段流程 `请求入口 -> 账号组 -> 账号出口 -> 结果与测试`。
- 设计变更：同一份设计稿 `request-orchestration-design-v01.html` 已从画布式节点原型切为固定流程配置页，保留底部生效链路摘要和点击复制 locator。
- 交互收敛：入口切换、账号组切换、当前账号选择、代理出口选择都改为显式点击流，不再维护拖拽状态机。
- 浏览器验收：使用 `agent-browser` 真实执行 `claude-code -> Claude 灰度组 -> claude-prod-01 -> us-static-02 -> 测试`，结果链路、结果区日志与底部 active path 均已同步；截图已归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-fixed-flow-after-v02.png`。

## 请求编排按项目设计规范重排

- 设计决策：请求编排设计稿按 GetTokens 既有 Swiss-industrial 规范重排，采用浅色默认、系统深色适配、2px 硬边框、硬投影、monospace 标签和 0 letter spacing。
- 信息架构：页面从四列演示稿改为 `全局侧栏 + 流程 rail + 中央配置矩阵 + 右侧结果栏 + 顶部路径快照`，更接近真实桌面工作台。
- 交互修正：账号行不再使用嵌套 button 结构，改为数据行 + 显式 `选择 / 禁用 / 移出` 操作，避免可访问树里出现按钮嵌套按钮。
- 浏览器验收：重新执行 `claude-code -> Claude 灰度组 -> claude-prod-01 -> us-static-02 -> 测试`，确认无横向溢出、无控制台错误；截图归档到 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-swiss-spec-after-v06.png`。

## 请求编排支持多流程组切换

- 关键决策：请求编排需要支持多组固定流程；每组都是完整的 `01 请求入口 -> 02 账号组与代理 -> 03 账号出口 -> 04 结果与测试`，不等同于第二阶段的账号组。
- 状态边界：每个流程组独立保存 `cli / activeGroupId / currentAccountId / routes / lastAction / testStatus / testLog / lastTest`，账号出口覆盖关系不跨流程组污染。
- 交互验收：已验证从 `生产编排` 切到 `Claude 备用` 后将 `claude-prod-01` 出口改为 `us-static-02`，再切回 `生产编排` 仍保持 `codex / Codex 生产组 / codex-prod-01 / direct`；再次切回 `Claude 备用` 时出口仍恢复为 `us-static-02`。
- 测试链路：已验证 `claude-code -> Claude 灰度组 -> claude-prod-01 -> us-static-02 -> 测试`，结果区和底部路径快照同步正确。
- 验收截图：`docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-multi-group-after-v07.png`。

## 请求编排入口与账号级代理池收敛

- 关键决策：请求入口固定为 `codex` 与 `claude code` 两个可点击选项，不再在入口段展示额外 CLI 列表或复杂上下文。
- 结构调整：第二阶段从 `账号组与账号` 改为 `账号组与代理`，保留账号组切换，但主要操作面改为账号列表。
- 账号规则：单个账号新增代理池启用状态；关闭代理池时该账号最终出口固定为 `direct`，之前 profile route 中选过的代理保留，重新启用代理池后恢复。
- 浏览器验收：已验证 `codex-prod-01` 默认关闭代理池时非 direct 代理 tile 为不可选；点击 `启用代理池` 后可选择 `us-static-02`；再次关闭代理池回到 `direct`；重新启用后恢复 `us-static-02`。
- 验收截图：`docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-entry-proxy-after-v08.png`。

## 请求编排支持自定义流程组

- 关键决策：多流程组不应限制为内置三组；用户需要能自定义编排，每个自定义组仍是完整的 `01 -> 02 -> 03 -> 04`。
- 交互范围：设计稿已加入 `+ 自定义组`、`复制当前`、流程组名称输入和 `删除当前`；删除在只剩一组时不可用。
- 状态规则：新增组默认创建一条完整空白流程；复制组保留入口、账号组、当前账号和出口覆盖，但测试状态回到未测试。
- 浏览器验收：已验证新建 `自定义流程 01`、重命名为 `自定义高优先级`、复制当前组、删除当前副本，流程组列表和 01-04 摘要同步正确；截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-custom-flow-after-v09.png`。

## 请求编排整页逻辑与 UI 重设计

- 关键决策：原设计中顶部流程组、中央配置矩阵和右侧结果并列时层级难理解，整页改为 `左侧流程组管理 / 中央 01-04 四步编辑 / 右侧最终路径验证`。
- 信息架构：左侧只负责多流程组新增、复制、重命名、删除和切换；中央只编辑当前组的 `01 请求入口 / 02 账号组与代理 / 03 账号出口 / 04 结果与测试`；右侧只展示当前组的最终路径、指标和测试日志。
- 浏览器验收：已验证新建自定义组、改名为 `自定义路由 A`、切入口到 `claude-code`、切账号组到 `Claude 灰度组`、选择 `bridge-01`、启用代理池、选择 `us-static-02` 并测试；切回 `生产编排` 后状态未串组。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-redesign-after-v10.png`。

## 请求编排 subagent 体验评审修正

- 评审结论：subagent 判断新版三栏信息架构成立，不建议继续推翻；主要问题集中在布局宽度、`04` 与出口语义混用、应用状态不清。
- 修正动作：账号表改为内部横向滚动，避免中间区域挤压右侧结果栏；右侧最终路径把 `出口` 与 `04 测试` 分开展示；指标区新增 `应用状态`，编辑后显示 `未应用更改`，点击应用后显示 `已应用当前组`。
- 回归验收：浏览器验证路径语义为 `出口 直连 / 04 待测试`，账号表 `overflow-x: auto`，应用状态可从 dirty 切到 applied；截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-subagent-fix-after-v11.png`。

## 请求编排大胆推翻三栏版

- 关键决策：用户明确不需要三栏，设计稿再次推翻为 `顶部流程组货架 / 中央纵向 01-04 编排序列 / 底部固定结果坞`，从上到下完成配置，底部始终看结果。
- 信息架构：顶部货架承载多流程组与自定义编排操作；中央四个大段落按 `01 请求入口 -> 02 账号组与代理 -> 03 账号出口 -> 04 结果与测试` 纵向排列；底部结果坞展示当前路径、应用状态、出口和测试日志。
- 浏览器验收：已验证新建 `大胆编排 A`、切入口到 `claude-code`、切账号组到 `Claude 灰度组`、选择 `bridge-01`、启用代理池、选择 `us-static-02`、应用、测试并切回 `生产编排`；状态未串组。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-bold-board-after-v12.png`。

## 请求编排改为四卡横向流程

- 关键决策：用户明确设想为“从左往右 4 个卡片，卡片从上到下是列表和配置”；设计稿改为横向四卡：`01 请求入口 / 02 账号组与代理 / 03 账号出口 / 04 结果与测试`。
- 信息架构：顶部仍保留流程组切换与自定义编排；主工作区四张卡从左到右表达流程，每张卡上方是可选列表，下方是当前配置；底部结果坞保留最终路径、应用状态和测试日志。
- 浏览器验收：已验证四列卡片结构存在，每张卡都有列表和配置；新建 `四卡编排 A`、切入口到 `claude-code`、切账号组到 `Claude 灰度组`、选择 `bridge-01`、启用代理池、选择 `us-static-02`、应用、测试并切回 `生产编排`；状态未串组。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-four-cards-after-v13.png`。

## 请求编排流程组收敛为默认组与自定义组

- 关键决策：不再内置 `生产编排 / Claude 备用 / 实验编排` 场景组；流程组货架只保留 `默认组` 与 `自定义组 01`，后续新增都使用 `自定义组 NN` 命名。
- 行为约束：`默认组` 不允许删除；自定义组可新增、复制、重命名、删除。
- 设计稿状态：`默认组` 使用 `codex / Codex 生产组 / codex-prod-01 / direct`；`自定义组 01` 使用 `codex / Codex 生产组 / codex-lab-02 / hk-resi-03` 作为可编辑起点。
- 浏览器验收：初始流程组只显示 `默认组` 与 `自定义组 01`；点击 `+ 自定义组` 后新增 `自定义组 02`；切回 `默认组` 时删除按钮禁用。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-default-custom-after-v14.png`。

## 请求编排移除底部结果视图

- 关键决策：用户明确不要底部展示的流程路径、模型、账号、出口、应用状态和 console 文本；这些底部视图会重复第 04 卡片信息并增加理解负担。
- 设计变更：移除 HTML 中的底部 `dock / path-preview / metrics / console` 结构与对应渲染逻辑；结果与测试只保留在第 `04 结果与测试` 卡片内。
- 浏览器验收：已验证 `.dock`、`#path-preview`、`#console` 均不存在；第 `04` 卡测试后只显示卡片内状态与最近测试时间。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-no-bottom-after-v16.png`。

## 请求编排移除应用级主导航

- 关键决策：用户明确不要设计稿顶部的 `01 状态 / 02 账号池 / 03 请求编排 / 04 代理池 / 05 设置` 应用主导航；请求编排设计稿应聚焦流程组与 01-04 四卡配置。
- 设计变更：移除 HTML 中的 `.nav` 样式与主导航 DOM，顶栏仅保留品牌标识、恢复默认和应用当前组操作。
- 浏览器验收：已验证 `.nav` 不存在，页面文本不再包含五个应用级主导航项；截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-no-top-nav-after-v17.png`。

## 请求编排改为多组折叠列表

- 关键决策：用户指出 `/html/body/div[1]/main` 当前表达的是“一组完整编排”；多组时应该在主工作区展示多组块，而不是只用顶部流程组卡片切换。
- 设计变更：主工作区改为流程组折叠列表；默认所有组折叠只显示组头摘要，点击某个组头后只展开该组并渲染 01-04 四卡。
- 浏览器验收：初始状态验证为 2 个流程组、0 个展开组、0 个 board、0 张流程卡；点击 `默认组` 后验证为 1 个展开组、1 个 board、4 张流程卡；再切换到 `自定义组 01` 后只有该组保留 board。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-collapsible-groups-after-v18.png`。

## 请求编排移除独立流程组 section

- 关键决策：用户明确不要 `/html/body/div[1]/section`；设计稿外层结构应进一步收敛为 `header + main`。
- 设计变更：删除独立 `.flowbar` section，把流程组名称、新建、复制、删除控件并入顶栏；`.app` 网格从三行改为两行。
- 浏览器验收：已验证 `.app` 直接子节点为 `header / main`，XPath `/html/body/div[1]/section` 不存在；点击组头后仍能展开为 1 个 board 与 4 张流程卡。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-no-section-after-v19.png`。

## 请求编排补全组内展开体设计

- 关键决策：用户指出 `/html/body/div[1]/main/div/section[2]/div` 内部需要完整设计；展开体不能只是裸四卡容器。
- 设计变更：流程组展开体改为 `.group-body`，内部先展示组内摘要，再展示 01-04 四卡；同时修复 `computeAccount` 作为 `Array.map` 回调时误接收 index 的问题，确保 02-04 卡内容完整渲染。
- 浏览器验收：已验证第二组 XPath 命中 `.group-body`，包含 4 个摘要单元、1 个 board、4 张流程卡、4 个出口项、2 个结果项；截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-group-body-after-v20.png`。

## 请求入口移除模型展示

- 关键决策：用户质疑入口卡配置区为何需要显示模型；结论是 `01 请求入口` 只负责 CLI 选择，不显示模型字段。
- 设计变更：移除入口配置中的 `模型` 行，并把入口卡说明改为只展示当前入口和应用状态；模型仍保留为内部兼容性与账号映射判断。
- 浏览器验收：已验证入口配置区仅剩状态标题与入口值，`模型` 标签不存在；截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-entry-no-model-after-v21.png`。

## 请求编排第二阶段受入口驱动

- 关键决策：用户指出 `/html/body/div[1]/main/div/section[1]/div/div[2]/section[2]` 应根据前一段 `01 请求入口` 的内容变化；第二阶段不能是静态账号列表。
- 设计变更：`02 账号组与代理` 顶部显示当前入口筛选；账号组 ready 数、入口匹配状态、账号可参与/不可参与和灰态原因均按当前入口重算。切换入口时自动选择当前入口下的第一个可参与账号，当前组不可用时再切可参与组。
- 浏览器验收：已验证从 `codex` 切到 `claude-code` 后，第二阶段文本和当前账号发生变化，账号组 ready 数与账号可参与状态同步更新；截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-section2-depends-entry-after-v22.png`。

## 请求编排结果链路改为垂直时间轴

- 关键决策：用户要求 `/html/body/div[1]/main/div/section[1]/div/div[2]/section[4]/div[1]` 链路使用垂直时间轴样式；第 `04` 卡上方不再作为候选账号列表。
- 设计变更：`04 结果与测试` 上方改为 `.timeline`，按 `01 请求入口 -> 02 账号组 -> 03 账号出口 -> 04 测试` 展示最终生效路径；测试后第 `04` 节点同步变为测试通过。
- 浏览器验收：已验证指定 XPath 下存在 `.timeline`，包含 `01/02/03/04` 四个节点；点击测试后时间轴末端显示 `测试通过` 与最近测试时间。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-timeline-after-v23.png`。

## 请求编排设计稿与 space 代码审查

- 触发 `$debate` 合作型审查；Gemini 因首轮超时无输出淘汰，Claude 因默认模型不可用淘汰，Planck 返回旧三栏意见且无代码行号引用，未纳入有效裁定。
- 审查结论：四卡横向流程和 `header + main` space 约束成立，但 README 第 `04` 卡描述、ready 计数与不可参与账号测试存在口径漂移。
- 修复动作：README 改为第 `04` 卡上方使用垂直时间轴；`compatibleAccountsFor` 改为复用 `account.active`；不可参与账号测试显示 `测试未通过` 并展示原因，不允许被应用为有效链路；禁用代理不可被写入出口。
- 验收：HTML 脚本语法校验通过；浏览器复现 `claude-code -> Codex 生产组 -> codex-prod-01` 后测试显示 `测试未通过 / CLI 不兼容`。截图归档 `docs-linhay/spaces/20260502-request-orchestration-menu/screenshots/20260504/request-orchestration/20260504-request-orchestration-debate-fix-after-v24.png`，报告归档 `docs-linhay/spaces/20260502-request-orchestration-menu/debate/20260504/request-orchestration/20260504-request-orchestration-v01.md`。
