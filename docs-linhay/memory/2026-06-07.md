# 2026-06-07

- Codex API Key / OpenAI-compatible 账号详情 stale-cache 修复已收尾：根因是保存成功后只更新 `selectedAccount` 或只依赖 `ListAccounts`，当 reload 因 sidecar / SQLite 短暂失败被吞掉时，下一次从旧卡片对象打开详情会回显旧 API key / URL / prefix。已按稳定 `account.id` 增加 `patchAccountLocally`，覆盖 Codex API key 配置保存、改名、priority、openai-compatible 配置保存；回归测试先红后绿。沉淀规则已写入 `gettokens-domain-engineering`：详情 mutation 成功后必须先 patch 本地账号列表缓存，再把 `ListAccounts` 作为最终刷新。
- `20260606-project-account-candidate-pool-rule` 已冻结 Project Match Rule v01：项目规则命中只做 `rule.enabled && rule.channel == routeContext.channel && rule.projectKey in routeContext.projectIdentity.matchKeys` 的精确匹配；`projectName` 仅用于展示、审计和发现，不进入运行时匹配。首版 Codex 项目 key 来源为单 workspace 的 `workspace:<sha256(filepath.Clean(abs_workspace_path))>`，来源标记 `codex-turn-workspace`、置信度 `strong`；无稳定 key 或多 workspace 默认 `not-evaluated` 且不 fail closed；重复 enabled rule 命中同一 `channel + projectKey` 时才 fail closed 为 `project-candidate-pool:conflict`。管理保存 enabled rule 时需校验 channel、source-prefixed projectKey、非空 allowAccountIDs、同一 `channel + projectKey` 唯一；运行时仍保留 conflict fail closed 兜底。`projectBindings` 继续作为 legacy 删除目标，不允许作为新规则承载。
- `20260606-project-account-candidate-pool-rule` 后端桥接首段已可用：sidecar 已补 Codex workspace project identity、`PolicyStagePoolScope` 的 `project-candidate-pool` strict allow policy、项目候选池规则管理 API；主仓已补 `internal/cliproxyapi` client、`internal/wailsapp` bridge、root `main.App` DTO/mapper，并生成 `frontend/wailsjs` 绑定。聚焦测试覆盖 sidecar policy / management routes / Wails bridge / root mapper。后续补充已完成运行态规则快照与 pool epoch：路由热路径首次加载后不再每次请求读 JSON，管理 API create/update/delete 成功后刷新快照并 bump `AuthManager.BumpSessionAffinityPoolEpoch()`；下一阶段继续补 route explain/audit 与前端入口。
- `20260606-project-account-candidate-pool-rule` 已进入 `frontend-entry-tested`：route explain / audit 前端诊断能展示项目候选池 `matched / not-matched / no-project-key / ambiguous-project / no-routeable-account / conflict`；Codex 与 Claude Channel Routing 账号列表均接入项目候选池规则面板，真实模式走 Wails `List/Create/Update/DeleteProjectCandidatePoolRule`，浏览器预览模式用本地示例规则并支持规则预演。已通过 channel-routing Node 测试、frontend typecheck、root/Wails bridge Go 测试与 CLIProxyAPI sidecar 聚焦测试；后续只剩真实 dev Wails 桌面冒烟和 `projectBindings` 残留清单。
- `20260606-project-account-candidate-pool-rule` 收尾审计进入 `automated-tested`：重新跑通 root mapper、Wails bridge、cliproxyapi client、sidecar policy / management routes、Codex workspace project identity、前端 channel-routing 模型与入口源码断言、TypeScript typecheck、docs-linhay 结构检查。`projectBindings` 残留已分类为 legacy 丢弃测试、历史研发文档和 memory，不存在新的可写入口、Wails DTO、前端表单或 sidecar 管理 API；新规则独立存储在 `project-candidate-pool-rules/config.json`。真实 dev Wails 桌面创建规则、规则预演、下一请求 sticky 重评估仍保留为后续 smoke。
- `20260606-project-account-candidate-pool-rule` 已补 dev Wails / dev sidecar runtime smoke：通过 `./scripts/wails-cli.sh dev` 启动仓库构建产物，确认 dev sidecar 使用 `/Users/linhey/.config/gettokens-dev/config.yaml`，`/healthz` OK，正式版 `/Applications/GetTokens.app` 未被停止或修改。对 `project-candidate-pool-rules` 管理 API 创建唯一临时 enabled rule 后可查询命中，删除后目标规则无残留且数量恢复；本轮证明 dev 运行态的规则 CRUD 闭环可用，真实上游请求命中、trace 观察和 sticky 下一请求重评估仍作为更高阶端到端验收。
- `20260606-project-account-candidate-pool-rule` 又补 Wails explain bridge 证据：新增 `TestExplainChannelRoutingLoadsProjectCandidatePoolRulesFromManagementAPI`，验证 `ExplainChannelRouting` 会经 sidecar management API 同时读取 `/accounts` 与 `/gettokens/project-candidate-pool-rules?channel=codex`，项目上下文命中 `workspace:abc` 后只保留 allow set 内账号，输出 `project-candidate-pool:matched`、ruleID、候选池 before/after 与过滤原因。该测试补齐 CRUD 与纯函数 strict allow 之间的桥接链路；真实上游请求仍留到端到端验收。
- `20260606-project-account-candidate-pool-rule` 前端入口调整为 hash-routed modal：Codex / Claude 账号列表的 `请求模式` header 新增 `项目配置` 按钮，规则编辑器不再作为主页面内嵌区块，而是通过 `modal=project-config` 恢复同一全应用视口 modal。已通过相关 Node tests、frontend typecheck、docs check；in-app browser 验证 hash 直达可打开项目配置 modal，当前浏览器自动化 click 对新增按钮和既有路由探测按钮均未触发，未作为新按钮单点失败判定。
- `20260606-project-account-candidate-pool-rule` 项目配置 modal 收敛为历史项目选择：用户指出 `项目 key / 显示名` 不应手填，本轮确认 `projectKey` 是内部稳定路由键、`projectName` 只用于展示和审计。前端从已配置规则与最近 `ListChannelRouteEvents` 观测项目身份生成候选，下拉选择项目后自动带出内部标识；没有历史项目时显示空态并禁止创建规则。已通过 channel-routing / Codex / Claude Node tests、frontend typecheck、Wails route event ledger Go test、diff check、docs check；in-app browser 验证 direct modal 中无 `项目 key` / `显示名` 输入。
- `20260606-project-account-candidate-pool-rule` 项目候选下拉来源已校准为会话系统主导：用户确认项目列表应从会话历史和运行会话获取并定期同步，不由 route event 主导。Codex / Claude session management 已把 `cwd` 派生为稳定 `workspace:<sha256(filepath.Clean(cwd))>` 并投影到 project/session/detail DTO；Codex 项目配置 modal 打开时每 15 秒同步 live sessions + session history，live-only 项目必须能经历史会话补到稳定 key 才进入候选；Claude 项目配置 modal 每 15 秒同步 session history。前端排序为已配置、运行会话、会话历史、route event 兜底，保存仍只写稳定 key 与 allow account set。
- Menu Bar Popover 进入 `design-v09 / desktop-verified-v01`：运行态首期采用原生 AppKit `NSStatusItem + NSPopover`，dev 构建用 `GetTokens Dev` / `DEV` 区分正式版并在隔离 HOME 下验证 `18317/healthz` OK；正式版未被停止或修改。popover 设计偏好已收窄：用户只关心额度和余额，风险账号行应避免重复 `quota / percentage / window / state` 标签，优先展示账号名、剩余额度、贴边进度条、余额值和处理状态。
- Menu Bar Popover 样式偏差已修复到 `native-popover-style-verified-v02`：用户截图指出首期原生 popover 仍是默认 AppKit 表单，与设计稿差距过大。本轮将 AppKit `NSPopover` 改为额度余额小票结构：纸张底、mono 字体、像素钥匙、摘要格、quota snapshot 行、余额小票和 `打开账号池 / 刷新额度 / 退出 GetTokens` 操作区；真实 quota/balance 快照未接入前只显示占位态，不伪造额度数据。隔离 dev 验证 PID `25371/25379`，`18317/healthz` OK，截图归档到 `20260607-menubar-popover-after-v06.png`；正式版未被停止或修改。
- Menu Bar Popover 已完成 `quota-balance-snapshot-verified-v03`：Go 端只读 sidecar `/gettokens/quota-status` 与账号列表投影 menu bar snapshot，Objective-C 只解析 JSON 渲染小票；`刷新额度` 按钮只重新读取本地 snapshot，不触发上游 quota refresh。新增测试覆盖排序、余额汇总、空态、只读请求路径和 native JSON bridge。最终 clean dev 验证 PID `21447/21498`，`18317/healthz` OK，注入 runtime 快照后 popover 显示最低额度 `06%`、风险账号 `2`、总余额 `$122.25`，截图归档到 `20260607-menubar-popover-after-v14.png`；正式版未被停止或修改。
- Menu Bar Popover SwiftUI 路线已核对：Apple `NSHostingController/NSHostingView` 可以承载 SwiftUI 到 AppKit popover，长期维护小票式 UI 更合适；但当前 GetTokens 仓库没有 `.swift`、SwiftPM、Xcode project 或 Swift build step，native bridge 仍是 Go+cgo+Objective-C。决策为本期保留已通过测试/build/dev 实机验收的 AppKit 版本，后续若迁移先做最小 SwiftUI spike，证明 Wails build、Go bridge、签名打包和 dev 状态栏生命周期都稳定后再替换渲染层。
- Menu Bar Popover 布局修复已进入 `layout-structure-tested-v04`：用户确认小票风格方向正确但布局有问题。本轮在 AppKit bridge 中统一 `kPopoverContentWidth` 主列宽，新增结构化余额表头和专用空态额度行，避免继续靠空格字符串或真实账号行固定列宽凑布局；聚焦测试与 Wails build 已通过。可见截图验收中曾误点正式版状态栏入口，用户指出后已删除错误截图；后续截图必须确认点击顶部 `DEV` 且画面出现 `BALANCE :: / Quota receipt` 才能作为有效验收证据。
- Menu Bar Popover 已推进到 `swiftui-popover-build-tested-v05`：用户明确要求 Codex 负责迁到 SwiftUI。本轮新增 `internal/menubar/swiftui/GetTokensMenuBarPopover.swift`，用 SwiftUI 小票视图渲染 quota/balance；Objective-C bridge 仍负责 `NSStatusItem/NSPopover` 生命周期，打开时优先 `dlopen` `libGetTokensMenuBarSwiftUI.dylib` 并创建 `NSHostingController`，失败时才 fallback 到 AppKit 小票。新增 SwiftUI dylib build/install 脚本并接入 Wails wrapper、本地 package 和 release workflow；popover 操作只保留 `打开账号池 / 刷新额度`，不显示退出 App。已通过 arm64/x86_64 SwiftUI dylib 编译、`go test ./internal/menubar ./internal/wailsapp`、`./scripts/wails-cli.sh build`、app bundle deep codesign 校验、SwiftUI factory `dlopen` 检查、SwiftUI 离线渲染截图 `20260607-menubar-swiftui-render-after-v15.png` 和 docs check；当前用户 dev 设置为 `showMenuBarIcon=false` 且菜单栏入口被系统/隐藏工具收起，未继续盲点点击状态栏。
- Menu Bar Popover SwiftUI 收尾到 `swiftui-popover-build-tested-v06`，覆盖早前“本期保留 AppKit”的历史判断：当前实现为 SwiftUI 优先、AppKit fallback。重新跑 `./scripts/wails-cli.sh build` 后，`codesign --verify --deep --strict --verbose=2 build/bin/GetTokens.app` 通过且 sealed resources 包含 `libGetTokensMenuBarSwiftUI.dylib`；dylib 为 arm64 Mach-O，install name 为 `@executable_path/../Frameworks/libGetTokensMenuBarSwiftUI.dylib`，`nm` 可见 `GetTokensMenuBarCreateSwiftUIViewController`。同时补强 wrapper：仅在 Wails build 成功后安装 dylib 与重签，避免失败构建留下签名漂移；SwiftUI build 脚本默认使用仓库内 module cache，避免依赖用户 `~/.cache/clang/ModuleCache`。`go test ./internal/menubar ./internal/wailsapp`、SwiftUI 离线渲染 `20260607-menubar-swiftui-render-after-v17.png`、docs check 均通过；截图确认小票样式、贴边分割线、`打开账号池 / 刷新额度` 可见且无退出 App。
- Menu Bar Popover `打开账号池` 已修复到 `account-pool-navigation-tested-v07`：根因是 native `OpenWindow` callback 只执行 `Show/WindowShow`，没有发前端导航意图，所以窗口会停留在上次页面。本轮改为显示窗口后 `EventsEmit("menubar:navigate", {"page":"accounts"})`，前端 AppShell 监听后切 `setActivePage('accounts')` 并写入 `#frame=accounts`。已通过 `go test ./internal/wailsapp ./internal/menubar`、新增 `node --test frontend/src/tests/menuBarNavigation.test.mjs`、frontend typecheck/build、`./scripts/wails-cli.sh build`、app codesign 和 docs check；完整 frontend unit 中仍有既有 design-system manifest 对 ProjectCandidatePoolRules 组件登记缺口，非本次修复引入。
- Menu Bar Popover `刷新额度` 已修复到 `active-quota-refresh-tested-v08`：排查确认 dev `/v0/management/accounts` 有 8 个账号，但 `/v0/management/gettokens/quota-status` 返回空数组；此前 popover refresh 只重新读取本地 snapshot，不会执行 sidecar quota refresh，所以已配置 quota curl / billing curl 的账号仍显示空态。现在启动、sidecar ready 和打开 popover 仍只读 snapshot；用户点击 `刷新额度` 时才挑选启用且配置 quota/billing curl 的 Codex API key / OpenAI-compatible 账号调用 `RefreshQuota`，最后重读 snapshot。已通过 `go test ./internal/wailsapp -run 'TestRefreshMenuBarQuotaSnapshot|TestMenuBarOpenWindow'`、`go test ./internal/menubar`、`go test ./internal/wailsapp ./internal/menubar`。
- Menu Bar Popover 空态文案收敛到 `copy-polished-v09`：用户要求显示 `等待账号额度快照`。SwiftUI 主渲染与 AppKit fallback 的空态主提示、资源行 fallback 已统一为该文案，接入态改为 `账号额度快照已接入。`，不再在小票主文案暴露 `sidecar quota / balance` 工程词。已通过 `go test ./internal/menubar` 与 `./scripts/build-menubar-swiftui.sh arm64`。
- Menu Bar Popover 空账号行文案继续修正到 `empty-account-copy-v10`：用户澄清不是只改顶部提示，而是账号列表无真实账号时不要显示 `quota snapshot` 假账号名，应显示 `等待账号额度快照`。SwiftUI `Resource()` 默认 name、JSON fallback 与 AppKit `empty_resource_row()` 已同步；详情副文案为 `点击刷新额度后更新`。新增源码断言禁止 SwiftUI / AppKit fallback 再把 `quota snapshot` 作为空账号名，并已安装新 SwiftUI dylib 到 dev app 后重签验证。
- `20260606-relay-vendor-support` 已完成系统接线整理，状态调整为 `system-audited / runtime-gap-identified`：`sub2api` / `new-api` 仍按 relay / gateway preset 接入，不新增账号主类型；但后续实现不能只加前端预设或详情页字段。必须先补账号能力事实源、sidecar runtime projection、executor 按下游协议选择 `openai_chat / openai_responses / anthropic` endpoint、Channel Routing / route explain 精确过滤，再落厂商预设和创建流。对应可复用规则已沉淀到 `gettokens-domain-engineering`，暂不上升到 `AGENTS.md`。
