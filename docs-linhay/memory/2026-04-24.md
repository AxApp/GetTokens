# 2026-04-24

- 基于 Gemini 会话 `05ae7144-6948-432a-8845-8ae91ef183b5`，为 GetTokens 沉淀了 4 个项目级 skill：
  - `.agents/skills/gettokens-accounts-domain`
  - `.agents/skills/gettokens-wails-dev-loop`
  - `.agents/skills/gettokens-frontend-debug`
  - `.agents/skills/gettokens-ui-system`
- 继续把“会话提炼 skill 的流程”与“AGENTS / docs-linhay / memory / qmd 同步流程”也抽成 skill：
  - `.agents/skills/gettokens-session-skill-distill`
  - `.agents/skills/gettokens-agents-governance-sync`
- 决策：项目级 skills 采用仓库内 `.agents/skills/` 约定，保持与其他 agent 工具兼容，不绑定单一 CLI。
- 这些 skills 覆盖当前项目最稳定的四类长期知识：账号池业务闭环、Wails 开发回路、前端调试定位、视觉与本地化约束。
- 补充决策：凡是“可重复执行的项目动作”，优先沉淀为 `.agents/skills`；只有真正 repo-wide 且长期稳定的规则才升级进 `AGENTS.md`。
- 已将该规则同步进当前 `AGENTS.md`：后续遇到“有用且重复出现”的行为模式，要先提炼并新增/更新项目级 `skills`，只有上升为长期稳定约束时才更新 `AGENTS.md`。
- 为 GetTokens 增加了 VS Code 运行配置：`.vscode/launch.json`、`.vscode/tasks.json` 与 `scripts/wails-cli.sh`。运行入口统一为 `wails dev`，并支持在未安装全局 `wails` 时回退到 `go run github.com/wailsapp/wails/v2/cmd/wails@v2.12.0`。
- 新增 `docs-linhay/dev/20260424-react-migration-guide.md`，给 Copilot 使用的 Svelte -> React 迁移完整指南已落文档，强调分阶段迁移、Wails 稳定性优先、第一轮不同时升级 TypeScript 或状态管理。
- GetTokens 前端已实际从 Svelte 迁移到 React。实现路径采用 React 18 + Vite 原生 esbuild JSX，而非 `@vitejs/plugin-react`，原因是当前环境下 npm registry 不可达，但本机缓存足以离线安装 `react/react-dom`。
- 已完成的验证：
  - `frontend/npm run build` 通过
  - `npm_config_offline=true ./scripts/wails-cli.sh build -skipbindings` 通过
  - 成功打包 `build/bin/GetTokens.app`
- 残余风险：本轮未做完整人工 GUI 逐页点击回归，后续需要在真实桌面窗口下验证 `status/accounts/settings`、detail modal、delete、theme、locale 等交互。
- 修正了 `scripts/wails-cli.sh` 的探测顺序：在 `PATH` 之外，还会主动查找 `~/go/bin/wails` 等常见本地安装路径，避免 VS Code 终端因 `PATH` 不完整而错误走到联网 fallback。
- 决策：`docs-linhay/` 文档结构调整为以 `docs-linhay/spaces/<space-key>/` 为中心的聚合模式；单个需求空间的 spec、plans、screenshots、debate 都归档到同一 `space` 下。
- 已重写 `AGENTS.md`，移除旧的 `features/plans/screenshots/debate` 平铺描述，统一改为 `spaces` 结构，并新增 `README.md` 作为每个 `space` 的固定入口文档。
- 新增 `docs-linhay/dev/20260424-spaces-structure-governance.md`，说明 `spaces` 结构的设计目标、命名规则与文档落位规则。
- 决策：`AGENTS.md` 不再保留“旧结构 -> 新结构”的迁移表与旧路径说明，只保留当前生效的 `spaces` 规则；配套治理文档和 `gettokens-agents-governance-sync` skill 也同步移除了旧结构表述。
- 决策：将 `space` 创建与文档写回两类高频流程下沉为项目级 skills：`gettokens-space-governance` 与 `gettokens-doc-writeback`；`AGENTS.md` 继续只保留 repo-wide 硬约束。
- 已更新 `docs-linhay/dev/20260424-project-skills-overview.md` 与 `gettokens-agents-governance-sync`，把职责边界切分为：AGENTS 级治理、space 治理、文档写回三层。
- 决策：正式目录名从 `spacs` 更正为 `spaces`，并新增 `docs-linhay/README.md`、`docs-linhay/scripts/create-space.sh`、`docs-linhay/scripts/check-docs.sh` 作为日常入口与自动化工具。
- 已验证文档自动化链路：`check-docs.sh` 能识别缺失 `README.md` 的失败场景，`create-space.sh` 生成的标准 `space` 结构可通过 `check-docs.sh` 校验。
- 真实桌面 GUI 回归发现：React 迁移后的 `AccountsPage.jsx` 使用 `window.confirm` 做删除确认时，在 Wails 桌面窗口里多次点击 `DELETE` 均不会发出 `DELETE /v0/management/auth-files` 请求；详情弹窗、models 请求和 raw content 下载正常，问题集中在删除确认链路。
- 修复决策：删除确认改为页面内二次确认，不再依赖 WebView 原生 confirm/alert。这样既规避 Wails confirm 兼容性问题，也让删除状态对用户可见、对自动化可观测。
- 已完成的真实桌面回归：
  - 通过向 `~/.config/gettokens` 注入最小 `openai` auth JSON 样本，验证非空列表渲染
  - 详情弹窗可打开，raw content 可展示，`GetAuthFileModels` / `DownloadAuthFile` 请求正常
  - 搜索命中 `openai`，搜索 miss 时显示 `NO ACCOUNTS MATCHED`
  - 删除二次确认改造后，在真实桌面 app 中成功删除样本；界面从 `1 TOTAL` 刷新到 `0 TOTAL`，磁盘样本文件已移除，sidecar 日志记录了 `DELETE /v0/management/auth-files -> 200`
