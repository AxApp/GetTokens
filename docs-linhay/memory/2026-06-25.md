# 2026-06-25

## AntD accounts 段契约收敛

- 分段审查 accounts 相关迁移失败时，确认真实回归为账号详情头部 AccountDetailHeader 被导入但未渲染、auth-file 配置提示缺少稳定 data-auth-file-config-notice 钩子；已恢复详情 layout header 渲染并补齐提示钩子。
- 同步更新 accounts 相关静态测试：不再要求迁移前的 Tailwind/custom button class 常量，改为检查 AntD Button / Dropdown / Popover / Segmented / Alert 结构、稳定 data hook，以及旧 btn-swiss / input-swiss / uppercase 等视觉语言不回流。
- 验证：node --test src/features/accounts/tests/accountCardInteractions.test.mjs src/features/accounts/tests/accountDetailLayout.test.mjs src/features/accounts/tests/accountFilters.test.mjs src/features/accounts/tests/accountListLayout.test.mjs 通过（114/114）；npm run typecheck 通过。
- 沉淀审计：本段是 AntD 迁移静态契约跟随实现更新，未形成新的长期 repo-wide 规则；不更新 AGENTS.md 或新增 skill。

## AntD accounts 残余契约收敛

- 继续分段收敛 account-migration、account card、local CLI apply、deep-link import、OAuth、rotation、openai-compatible、rate-limit rules 等 accounts 残余静态失败。
- 处理方式保持为测试契约跟随 AntD 迁移后的真实结构：旧 ButtonClass / badge class 断言改为 AntD Button / Tag / Input / Select 组件、稳定 data hook 和旧视觉语言不回流断言；本段未发现需要追加的运行时代码修复。
- 验证：node --test src/features/account-migration/model.test.mjs src/features/accounts/tests/accountCardLayout.test.mjs src/features/accounts/tests/accountLocalCliMapping.test.mjs src/features/accounts/tests/accountOAuth.test.mjs src/features/accounts/tests/accountRotation.test.mjs src/features/accounts/tests/openAICompatible.test.mjs src/features/accounts/tests/rateLimit.test.mjs 通过（115/115）。
- 沉淀审计：仍属于 AntD 迁移测试契约同步，不新增长期规则或 skill。

## AntD usage / channel 段契约收敛

- 分段收敛 UsageDesk、ProjectCandidatePoolRulesPanel、ChannelRoutingWorkbench 迁移失败。确认真实回归为投影用量索引 Dropdown 菜单未渲染 action.description，导致刷新/重建影响范围说明丢失；已恢复到菜单项 label 中展示描述。
- 其余失败为静态契约仍绑定旧自定义 Space/select class 或旧 evidence DOM；已改为检查 AntD Dropdown / Segmented / Card / Select 结构、稳定 data hook 和旧视觉语言不回流。
- 验证：node --test src/features/accounts/tests/usageDesk.test.mjs src/features/channel-routing/tests/channelRouting.test.mjs 通过（87/87）；npm run typecheck 通过。
- 沉淀审计：本段仍是 AntD 迁移契约同步加单点 UI 文案回归修复，未形成新 repo-wide 规则。

## AntD Claude / Codex / status / session 段契约收敛

- 分段收敛 Claude Code account/extensions、Codex binary/live sessions/account order/route probe、status config、legacy account detail 和 session-management 静态契约失败；测试断言从旧自定义 button/input/filter class 常量改为 AntD Button/Input/Checkbox/Tag 结构、稳定 data hook 和旧视觉语言不回流。
- 确认并修复 session analysis 可访问性回归：header 分析入口和 detail 返回选择按钮不再用 disabled 移除焦点目标，改为点击 guard + aria-disabled，保持 modal 关闭/加载期间焦点恢复链路稳定。
- 验证：npm run test:unit 通过（990/990）；npm run typecheck 通过。
- 沉淀审计：本轮模式仍是 AntD 迁移测试契约同步与两处焦点可访问性修复，未形成新的长期 repo-wide 规则；不更新 AGENTS.md 或新增 skill。

## AntD 遗漏测试与 quota reset 视觉收口

- 继续逐页逐文件排查 UI 测试覆盖，发现 25 个 `.test.mjs` 未纳入 `frontend/package.json` 的 `test:unit`；已追加到主单测脚本，避免 Claude Code settings/subagents、extension registry、vendor status、proxy pool、quota runtime 等页面继续漏测。
- 真实 UI 清理：OpenAI quota reset modal 移除动态渐变、glass 背景和自定义 keyframes，收敛为 quiet workspace shell + AntD Button 控件；同时补齐 Claude Code settings/subagents/memory row 的 canvas 背景。
- 同步更新遗漏测试中的旧自绘 class 断言，改为 AntD Button/Input/Checkbox/Upload/AutoComplete 结构和稳定 data hook。
- 验证：npm run test:unit 通过（1131/1131）；npm run typecheck 通过。
- 沉淀审计：本段新增的稳定动作是“新增/遗留 test.mjs 必须纳入 test:unit”，已通过 package 脚本固化；不额外更新 AGENTS.md 或新增 skill。

## 运行态 surface-panel 收口

- 继续按 UI 运行态源码残留门禁排查，确认 `--gt-surface-panel` / `--gt-shadow-panel` / `border-muted` 已从非 test/story 的 `.ts/.tsx/.js/.jsx` 运行态源码中清空。
- 本段只做最小收口：Status 页面 shell 改用 `--gt-surface-muted`，ModalFrame footer 改用 `--gt-surface-muted`，账号详情模型拉取状态块改用 `--gt-surface-muted`，设计系统入口不再把 `--gt-surface-panel` 当推荐色卡展示；底层 token 和 stories 历史样例暂留后续单独整理。
- 验证：聚焦 `node --test frontend/src/features/status/tests/statusTypography.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/features/accounts/tests/accountDetailSections.test.mjs` 通过（53/53）；`npm run test:unit` 通过（1132/1132）；`npm run typecheck` 通过。
- 沉淀审计：本段复用既有 `Quiet workspace migration loop`，未形成新的跨模块流程；不更新 AGENTS.md 或新增 skill。

## Storybook 旧重描边样例收口

- 继续排查展示层残留，在 `legacyStyleResidue.test.mjs` 增加 stories 门禁，禁止 Storybook 示例继续教学 `border-2`、bracket hard shadow、`gt-shadow-panel`、`border-muted` 和旧 `SwissPrimitives` 文件名。
- 机械收敛 33 个 story 文件：粗边框改为细边框/轻圆角，hard shadow 改为 `shadow-sm`，旧 `border-muted` 改为 `--gt-border-subtle`；`SwissPrimitives.stories.tsx` 重命名为 `AntdPrimitives.stories.tsx` 并同步 story catalog。
- 验证：`node --test frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（28/28）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过；stories 残留扫描无匹配。
- 沉淀审计：本段把展示层旧样式纳入现有 legacy residue 测试门禁，属于已有前端迁移流程扩展，不新增 AGENTS 规则或 skill。

## Storybook surface-panel 展示收口

- 在上一段 stories 门禁基础上继续收窄展示层 token：stories 不再允许 `--gt-surface-panel`，示例背景统一换为 `--gt-surface-muted`；Color token story 删除旧 panel 展示项，底层兼容 token 暂保留给历史 CSS/test contract。
- 验证：`node --test frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（28/28）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：这是同一 Storybook 视觉门禁的范围补齐，不新增 AGENTS 规则或 skill。

## Runtime token compatibility 清理

- 继续收口 token 层残留：`style.css` 移除未使用的 `--gt-surface-panel` 与 `--gt-shadow-panel` 定义，`tokenContract.test.mjs` 改为断言这些兼容 token 不得回到 `:root`；`theme.ts` 主题说明从 Swiss hard-edge 改为 AntD quiet workspace。
- 验证：聚焦 `node --test frontend/src/context/tokenContract.test.mjs frontend/src/context/ThemeContext.test.mjs frontend/src/context/antdTheme.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（20/20）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过。
- 沉淀审计：本段是既有 token 契约的收紧，不新增 AGENTS 规则或 skill。

## Account card status tint 收口

- 继续收口运行态装饰性渐变：账号卡 status glow 从 radial-gradient wash 改为 quiet workspace 的轻底色 + 左侧 inset 状态线，`ATTRIBUTION_CARD_TONE_GLOW_CLASS` 改为 `ATTRIBUTION_CARD_TONE_TINT_CLASS`。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/accountCardInteractions.test.mjs` 通过（44/44）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过。
- 沉淀审计：本段是既有 quiet workspace 视觉迁移规则的具体执行，不新增 AGENTS 规则或 skill。

## Status / Vendor summary 语义收口

- 继续清理运行态营销式 hero 语义：Status 顶部卡改为 `statusSummaryCardClass` / `data-status-summary`，Vendor Status 的顶部事故块从 `HeroIncidentCard` / `heroIncident` 改为 `LeadIncidentCard` / `leadIncident`，locale key 从 `hero_*` 改为 `summary_*`。
- 验证：聚焦 `node --test frontend/src/features/status/tests/statusTypography.test.mjs frontend/src/features/vendor-status/featureSource.test.mjs frontend/src/features/vendor-status/model.test.mjs` 通过（31/31）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行既有“不要营销站 hero”的视觉语言收口，不新增 AGENTS 规则或 skill。

## Account quota gradient 收口

- 删除账号卡 quota 未知值和刷新 skeleton 的装饰性 repeating-linear-gradient：未知值进度条保留 muted 底色，刷新 overlay 改为 flat tint，不再使用斜纹扫光动画。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/rateLimit.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/accountCardInteractions.test.mjs` 通过（56/56）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行 quiet workspace 视觉收口，不新增 AGENTS 规则或 skill。

## PageLoadingFallback 静态化

- 删除 PageLoadingFallback 的 packet/orbit/bitstream/scan 等装饰动效和对应 `page-loading-*` keyframes/CSS，保留静态 quiet workspace loading card、role/status 和设计系统 data hook。
- 验证：聚焦 `node --test frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（28/28）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行“删除装饰优先”的既有视觉收口策略，不新增 AGENTS 规则或 skill。

## UsageDeskChart gradient 收口

- UsageDeskChart 不再用 CSS background gradient 画网格；改为 SVG guide lines 保留图表结构，empty chart 删除网格背景和渐变 overlay，并移除未使用的 empty overlay token。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/features/accounts/tests/usageDeskClaudeLocalSource.test.mjs` 通过（49/49）；运行态 lower-case `radial-gradient/repeating-linear-gradient/linear-gradient` 扫描清零；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过。
- 沉淀审计：本段仍是既有视觉收口策略，未形成新的长期规则；不更新 AGENTS.md 或新增 skill。

## Theme preset storage 尾巴清理

- 主题运行态继续只保留 classic light：删除 `THEME_PRESET_STORAGE_KEY` 导出，`persistThemePreset` 改为 no-op，不再把旧 `theme-preset` localStorage key 写回；旧值只在测试里作为兼容输入被忽略。
- 验证：聚焦 `node --test frontend/src/context/ThemeContext.test.mjs frontend/src/context/antdTheme.test.mjs frontend/src/context/tokenContract.test.mjs` 通过（18/18）；运行态 `theme-preset` 扫描清零；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过。
- 沉淀审计：本段是既有单主题治理规则的具体执行，不新增 AGENTS 规则或 skill。

## Backdrop blur 装饰收口

- 移除运行态 overlay / modal / sticky toolbar 的 `backdrop-blur*` 装饰滤镜：遮罩继续使用 scrim token，Accounts 选择工具条改为实底 `--gt-surface-canvas`；`legacyStyleResidue.test.mjs` 加入 `backdrop-blur` 防回流门禁。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/accountListLayout.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（12/12）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过；运行态 `backdrop-blur*` 扫描清零。
- 沉淀审计：本段继续执行 quiet workspace 视觉收口策略，门禁已补到现有 legacy residue 测试，不新增 AGENTS 规则或 skill。

## Runtime heavy shadow 收口

- 继续排查运行态低信号旧视觉残留，将 modal / dropdown / route probe 面板上的 `shadow-lg` 降为 `shadow-sm`，保留边框与 scrim 提供层级，不再依赖重投影。
- `legacyStyleResidue.test.mjs` 加入 `shadow-lg/xl/2xl` 与 `drop-shadow` 运行态门禁；相关静态断言改为禁止 heavy shadow 回流。
- 验证：聚焦 `node --test frontend/src/components/biz/accountDetailClipboard.test.mjs frontend/src/features/codex/codexAccountList.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（63/63）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过；运行态 heavy shadow 扫描清零。
- 沉淀审计：本段是 quiet workspace 视觉门禁继续收窄，不新增 AGENTS 规则或 skill。

## UsageDesk chart 静态化

- 删除 UsageDeskChart 的曲线扫入、区域淡入、点位升起和选中点脉冲动效；同时移除只为重启动画服务的 `rangeAnimationVersion`、`curveMotion`、`detailTransitionActive` 状态与模型 helper。
- `legacyStyleResidue.test.mjs` 增加 UsageDesk 动效专用防回流词，避免重新引入这批 chart decoration。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（49/49）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行“删除纯装饰状态和动画”的既有前端收口策略，不新增 AGENTS 规则或 skill。

## Codex live timing chart 静态化

- 删除 Codex live timing chart 的 enter keyframe 和 selected point infinite pulse；保留图表线、点、选中环和 live timing 数据结构，不再用循环动效制造活跃感。
- `legacyStyleResidue.test.mjs` 增加 `codex-live-chart-enter` / `codex-live-point-pulse` 防回流门禁；原静态测试改为禁止这两个动画名。
- 验证：聚焦 `node --test frontend/src/features/codex-live-sessions/model.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（74/74）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行图表纯装饰动画删除策略，不新增 AGENTS 规则或 skill。

## Skills success HUD 静态化

- 删除 Codex Skills 安装成功提示的 enter/out/check/ring keyframes 和扩散环，只保留静态 success icon 与提示内容。
- `legacyStyleResidue.test.mjs` 不再忽略 `style.css`，并加入 `codex-success-hud` 防回流门禁，避免全局样式重新引入这类装饰 HUD 动效。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/codex-extensions/model.test.mjs frontend/src/features/codex-extensions/featureSource.test.mjs` 通过（19/19）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段仍是现有 quiet workspace 动效收口，不新增 AGENTS 规则或 skill。

## Transition-all 宽泛过渡收口

- 将运行态剩余两处 `transition-all` 收窄为 `transition-colors`：account rotation priority row 与 session provider edit button 只保留颜色/边框 hover 反馈，不再声明全属性过渡。
- `legacyStyleResidue.test.mjs` 加入 `transition-all` 运行态门禁，避免后续重新引入宽泛动画类。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountRotation.test.mjs frontend/src/features/session-management/model.test.mjs` 通过（44/44）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段是既有 quiet workspace 动效收口门禁扩展，不新增 AGENTS 规则或 skill。

## Active scale 按压缩放收口

- 删除运行态按钮、菜单项、卡片选项和列表行上的 `active:scale*` 按压缩放反馈；保留 hover 颜色/边框反馈和 loading 状态动画。
- 同步删除两个 dropzone / provider card 上仅为 transform 服务的 `transition-[...,transform]`；`legacyStyleResidue.test.mjs` 加入 `active:scale` 防回流门禁。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountHeaderMenu.test.mjs frontend/src/features/proxy-pool/proxyPoolLayout.test.mjs frontend/src/features/accounts/tests/accountRotation.test.mjs frontend/src/features/session-management/model.test.mjs frontend/src/features/channel-routing/tests/channelRouting.test.mjs frontend/src/features/codex-live-sessions/model.test.mjs` 通过（161/161）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段仍是 quiet workspace 纯装饰 motion 收口，不新增 AGENTS 规则或 skill。

## Animate pulse 状态静态化

- 删除运行态 `animate-pulse`：账号详情 loading 文案、session management skeleton / 标签、AccountsHeader runtime icon、账号详情 auth-file skeleton、account card skeleton 和模型加载占位都改为静态状态展示。
- `legacyStyleResidue.test.mjs` 加入 `animate-pulse` 防回流门禁；AccountsHeader 测试改为断言 runtime icon 保持静态。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountHeaderMenu.test.mjs frontend/src/components/biz/accountDetailClipboard.test.mjs frontend/src/features/session-management/model.test.mjs frontend/src/features/accounts/tests/accountDetailSections.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs` 通过（68/68）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行“状态可见但不闪烁”的 quiet workspace 收口，不新增 AGENTS 规则或 skill。

## Animate spin 状态静态化

- 删除运行态 `animate-spin`：导入、刷新、保存、测速、模型拉取、迁移 gate 等 pending 状态保留图标、文案、disabled/loading 语义，但不再旋转。
- 清理删除旋转类后产生的空 className 条件，并在 `legacyStyleResidue.test.mjs` 加入 `animate-spin` 防回流门禁。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountHeaderMenu.test.mjs frontend/src/features/accounts/tests/accountCardInteractions.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/accountDetailSections.test.mjs frontend/src/features/session-management/model.test.mjs frontend/src/features/proxy-pool/proxyPoolLayout.test.mjs frontend/src/features/codex/codexAccountList.test.mjs frontend/src/features/accounts/tests/accountTransfer.test.mjs frontend/src/features/account-migration/model.test.mjs` 通过（172/172）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行“状态可见但不旋转”的 quiet workspace 收口，不新增 AGENTS 规则或 skill。

## Transition transform 箭头静态化

- 删除运行态最后两处 `transition-transform`：Vendor Status 与 Channel Routing 的展开箭头保留展开方向状态，但不再做旋转过渡。
- `legacyStyleResidue.test.mjs` 加入 `transition-transform` 防回流门禁；运行态 motion 候选扫描清零。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/vendor-status/featureSource.test.mjs frontend/src/features/vendor-status/model.test.mjs frontend/src/features/channel-routing/tests/channelRouting.test.mjs` 通过（49/49）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段完成当前已知运行态 motion class 收口，不新增 AGENTS 规则或 skill。

## Grayscale 滤镜收口

- 删除运行态 `grayscale` 滤镜：Codex account order 拖拽/静默状态与 Rotation priority 拖拽状态保留 opacity / outline 等状态表达，不再使用灰度滤镜。
- `legacyStyleResidue.test.mjs` 加入 `grayscale` 防回流门禁。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/codex/codexAccountList.test.mjs frontend/src/features/accounts/tests/accountRotation.test.mjs` 通过（71/71）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段是 quiet workspace 视觉滤镜收口，不新增 AGENTS 规则或 skill。

## 交互 opacity 收口

- 删除运行态 `hover:opacity*`、`active:opacity*`、`transition-opacity` 与 `group-hover:opacity*` 交互装饰；disabled / 状态 opacity 保留。
- 清理删除 hover opacity 后无实际目标的裸 `transition` 类；`legacyStyleResidue.test.mjs` 增加交互 opacity 防回流门禁。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/vendor-status/featureSource.test.mjs frontend/src/features/vendor-status/model.test.mjs frontend/src/features/channel-routing/tests/channelRouting.test.mjs frontend/src/features/accounts/tests/accountRotation.test.mjs frontend/src/features/session-management/model.test.mjs frontend/src/features/accounts/tests/accountDeleteOverlay.test.mjs frontend/src/features/codex/codexAccountList.test.mjs` 通过（151/151）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行“状态可以弱化，但交互不靠透明度跳变”的 quiet workspace 收口，不新增 AGENTS 规则或 skill。

## 装饰 opacity 收口

- 删除 UsageDeskChart 选中点 halo 的 `opacity-10` 装饰层；Rotation priority 拖拽手柄不再用 `opacity-30` 弱化，改为静态 muted ink 点阵。
- `legacyStyleResidue.test.mjs` 加入 `opacity-10/30` 防回流门禁，并用负向数字边界避免误伤 `opacity-100/0` 这类 selected / disabled / 状态语义 opacity。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/features/accounts/tests/accountRotation.test.mjs` 通过（61/61）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段是 quiet workspace 纯装饰 opacity 收口，不新增 AGENTS 规则或 skill。

## Codex live chart 9px 字号收口

- Codex live session timing chart 的 y 轴和 request sequence tick label 不再使用裸 `text-[9px]`，统一改为标准 `text-xs`。
- `legacyStyleResidue.test.mjs` 加入 `text-[9px]` 防回流门禁；本轮只处理最小确定残留，剩余 `text-[10px]/[11px]` 继续按页面切片后续收口。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/codex-live-sessions/model.test.mjs frontend/src/features/codex-live-sessions/featureSource.test.mjs` 通过（75/75）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段延续 quiet workspace 裸 px 字号收口，不新增 AGENTS 规则或 skill。

## Claude Code asset 裸 px 字号收口

- Claude Code asset workbench 的 meta text 与 diff preview 不再使用 `text-[10px]/[11px]`，统一改为标准 `text-xs`。
- 复用 `claudeCodeAssetWorkbench.test.mjs` 的 quiet workspace shell 断言，新增该页面禁止 `text-[10px]/[11px]` 回流的源码契约；账号列表和账号详情残留继续按后续页面切片处理。
- 验证：聚焦 `node --test frontend/src/features/claude-code/claudeCodeAssetWorkbench.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（7/7）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段是既有 quiet workspace 裸 px 字号收口流程的页面级执行，不新增 AGENTS 规则或 skill。

## Claude Code account list 裸 px 字号收口

- Claude Code account list workbench 的 badge、chip、terminal、mapping row、metric label、queue row meta 和 profile detail 不再使用 `text-[10px]/[11px]`，统一改为标准 `text-xs`。
- 复用 `claudeCodeAssetWorkbench.test.mjs` 的 account list quiet workspace shell 断言，新增该页面禁止 `text-[10px]/[11px]` 回流的源码契约。
- 验证：聚焦 `node --test frontend/src/features/claude-code/claudeCodeAssetWorkbench.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（7/7）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行页面级裸 px 字号收口，不新增 AGENTS 规则或 skill。

## Account detail 裸 px 字号收口

- Account detail sections / model mapping section 的 header pill、runtime evidence、credential label/help/button、resource meta、capability endpoint hint、verify timestamp 和 readonly pill 不再使用 `text-[10px]`，统一改为标准 `text-xs`。
- `legacyStyleResidue.test.mjs` 的裸 px 字号门禁从 `text-[9px]` 扩展为 `text-[9/10/11px]`；账号详情布局测试同步断言 sections / model mapping 不再回流 `text-[10px]`。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs frontend/src/features/accounts/tests/accountDetailSections.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（69/69）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段完成当前已知运行态 `text-[9/10/11px]` 裸 px 字号收口，不新增 AGENTS 规则或 skill。

## Sidebar inline fontSize 收口

- Sidebar footer 的版本号、更新提示和更新按钮不再使用 inline `fontSize` / color / line-height style，改为静态 class + gt token；保留 AntD Menu 必需的 style 对象边界。
- `sidebarState.test.mjs` 增加 Sidebar 禁止 `fontSize:` 回流断言；`sidebarUpdatePrompt.test.mjs` 同步检查更新按钮使用 token class 表达 inverse 文本色。
- 验证：聚焦 `node --test frontend/src/components/biz/sidebarState.test.mjs frontend/src/components/biz/sidebarUpdatePrompt.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（13/13）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段是页面级 inline 字号收口，不新增 AGENTS 规则或 skill。

## UsageDeskChart inline fontSize 收口

- UsageDeskChart 点位数值标签和轴向辅助标签不再使用 inline `fontSize` / `fontWeight`，改为 class + gt font-size token；style 只保留动态坐标、颜色和 selected opacity。
- `usageDesk.test.mjs` 的 UsageDeskChart quiet shell 测试新增禁止 `fontSize:` 回流断言。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（49/49）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行页面级 inline 字号收口，不新增 AGENTS 规则或 skill。

## AccountDetailLayout inline fontSize 收口

- AccountDetailLayout section nav 不再显式写 inline `fontSize: 14`，交回 AntD theme；内容区字号、line-height、字体和文字色移到 class + gt token，style 只保留 `userSelect`。
- `accountDetailLayout.test.mjs` 的 unified account detail shell 断言新增 AccountDetailLayout 禁止 `fontSize:` 回流。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（65/65）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行页面级 inline 字号收口，不新增 AGENTS 规则或 skill。

## SessionManagementView inline fontSize 收口

- Session analysis word cloud 不再用连续 inline `fontSize` 计算词权重，改为 `text-base/xl/2xl/3xl` 四档静态 class；style 只保留权重 opacity。
- `session-management/model.test.mjs` 的 analysis quiet shell 断言新增 result block 禁止 `fontSize:` 回流；组件运行态 `fontSize:` 扫描清零。
- 验证：聚焦 `node --test frontend/src/features/session-management/model.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（32/32）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段完成当前已知组件运行态 inline `fontSize:` 收口，不新增 AGENTS 规则或 skill。

## Shared components 裸 transition 收口

- Sidebar toggle 与 ActionSelect action button 不再使用裸 `transition`，改为只覆盖颜色/边框相关变化的 `transition-colors`。
- `sidebarState.test.mjs` 与 `relayLocalState.test.mjs` 增加对应组件禁止裸 `transition` 回流断言；本轮先处理共享组件最小切片，剩余页面级裸 `transition` 后续按页面继续收口。
- 验证：聚焦 `node --test frontend/src/components/biz/sidebarState.test.mjs frontend/src/features/status/tests/relayLocalState.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（30/30）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段延续既有 transition 收口，不新增 AGENTS 规则或 skill。

## SessionManagementFeature 裸 transition 收口

- SessionManagementFeature header analysis / refresh / compact close icon buttons 不再使用裸 `transition`，改为 `transition-colors`，只过渡 hover 边框和背景。
- `session-management/model.test.mjs` 的 workbench quiet layout 断言新增 feature shell 禁止裸 `transition` 回流。
- 验证：聚焦 `node --test frontend/src/features/session-management/model.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（32/32）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行页面级裸 transition 收口，不新增 AGENTS 规则或 skill。

## ChannelRoutingWorkbench 裸 transition 收口

- ChannelRoutingWorkbench secondary button 不再使用裸 `transition`，改为 `transition-colors`，只过渡 hover 边框和背景。
- `channelRouting.test.mjs` 的 quiet workspace shell 断言新增 ChannelRoutingWorkbench 禁止裸 `transition` 回流。
- 验证：聚焦 `node --test frontend/src/features/channel-routing/tests/channelRouting.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（41/41）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行页面级裸 transition 收口，不新增 AGENTS 规则或 skill。

## VendorStatusFeature 裸 transition 收口

- VendorStatusFeature secondary button 不再使用裸 `transition`，改为 `transition-colors`，只过渡 hover 边框和背景。
- `vendor-status/featureSource.test.mjs` 的 quiet workspace shell 断言新增 VendorStatusFeature 禁止裸 `transition` 回流。
- 验证：聚焦 `node --test frontend/src/features/vendor-status/featureSource.test.mjs frontend/src/features/vendor-status/model.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（10/10）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行页面级裸 transition 收口，不新增 AGENTS 规则或 skill。

## DoctorWorkbenchFeature 裸 transition 收口

- DoctorWorkbenchFeature link button 不再使用裸 `transition`，改为 `transition-colors`，只过渡 hover 边框和背景。
- `doctorWorkbenchEntry.test.mjs` 的 quiet workspace shell 断言新增 DoctorWorkbenchFeature 禁止裸 `transition` 回流。
- 验证：聚焦 `node --test frontend/src/features/doctor-workbench/tests/doctorWorkbenchEntry.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（7/7）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过。
- 沉淀审计：本段继续执行页面级裸 transition 收口，不新增 AGENTS 规则或 skill。

## AntD 迁移审查止血

- 对未提交的 AntD 前端迁移 diff 做 review 后，先止血 5 个确定回归：`ModalFrame` 恢复 footer 与 body 滚动、账号分组删除恢复确认后调用 `onDeleteGroup(group.accounts)`、`Combobox` 同步外部受控 `value` 并恢复大小写无关去重、账号详情恢复 `runtime` 模块与 `AccountRuntimeRouteSection` 渲染、cURL 编辑器恢复原生 `textarea` 光标插入链路。
- 验证：`npm run typecheck` 通过；聚焦测试 `modal frame constrains|combobox keeps|account group menu exposes|auth-file modules|api-key edit modules|runtime route section|curl editor variable buttons` 通过。全量 `npm run test:unit` 仍失败，剩余为本轮接手前已有的大批 AntD 迁移静态/布局契约未同步项，需另开迁移收敛轮处理。
- 沉淀审计：这是一次 review 后的定点回归修复，未形成新的 repo-wide 规则；不更新 `AGENTS.md` 或新增 skill。

## ProxyPoolFeature 裸 transition 收口

- Proxy Pool 选择模式按钮、分页按钮、行操作按钮和更多操作菜单不再使用裸 `transition`，统一改为 `transition-colors`，只保留颜色/边框/背景反馈。
- `proxyPoolLayout.test.mjs` 增加 ProxyPoolFeature 禁止裸 `transition` 回流断言；本轮后运行态裸 `transition` 残留从 24 处降到 13 处，Proxy Pool 清零。
- 验证：聚焦 `node --test frontend/src/features/proxy-pool/proxyPoolLayout.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（3/3）；`npm run test:unit` 通过（1133/1133）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续执行页面级裸 transition 收口，不新增 AGENTS 规则或 skill。

## Accounts 组件簇裸 transition 收口

- Account detail auth file、local CLI apply、OpenAI-compatible compose/workspace、quota calibration、quota threshold、rate limit 和 rotation config 的输入框/按钮/选项行不再使用裸 `transition`，统一改为 `transition-colors`。
- `accountPresentation.test.mjs` 增加 accounts 组件簇禁止裸 `transition` 回流断言；本轮后运行态裸 `transition` 残留从 13 处降到 3 处，accounts/components 清零。
- 验证：聚焦 `node --test frontend/src/features/accounts/tests/accountPresentation.test.mjs frontend/src/features/accounts/tests/accountRotation.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（51/51）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段仍是既有页面/组件级裸 transition 收口，不新增 AGENTS 规则或 skill。

## 运行态裸 transition 门禁清零

- Status relay editor、Codex MCP modal field 和 Codex binary version cell 的最后 3 处裸 `transition` 已改为 `transition-colors`。
- `legacyStyleResidue.test.mjs` 将裸 `transition` 纳入运行态旧样式门禁；本轮后 `frontend/src/features|components|pages` 运行态裸 `transition` 扫描无结果。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/status/tests/relayLocalState.test.mjs frontend/src/features/codex-extensions/featureSource.test.mjs frontend/src/features/codex-binary/model.test.mjs` 通过（39/39）；`npm run test:unit` 通过（1134/1134）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是既有 transition 收口的全局门禁强化，已落到设计系统残留测试；不新增 AGENTS 规则或 skill。

## Debug 组件 inline typography/color 收口

- DebugEntryCard、DebugHeader、DebugEmptyState 的静态文字颜色、字体族、字重和边框/背景 token 从 inline style 移到 Tailwind class + gt token；保留 AntD Card 选中态 boxShadow 和 body layout style 边界。
- `legacyStyleResidue.test.mjs` 增加 Debug 组件禁止 inline typography/color 回流断言，先覆盖当前最集中的 Debug 残留簇。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（3/3）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段是既有 UI 残留清理循环的一次组件簇应用，不新增 AGENTS 规则或 skill。

## DesignSystemEntryFeature inline typography/color 收口

- DesignSystemEntryFeature 的 token swatch、anatomy preview、state matrix 和 story catalog 静态文字颜色/字体族从 inline style 移到 class + gt token；动态 swatch background、radius、boxShadow、表格边界等展示用途 style 保留。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 DesignSystemEntryFeature；本轮后该文件不再出现在 inline typography/color 残留扫描里。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs` 通过（29/29）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续应用既有 inline typography/color 收口循环，不新增 AGENTS 规则或 skill。

## AccountsToolbar inline typography/color 收口

- AccountsToolbar 筛选 popover 的分组标题、说明文字和 ternary option 标题不再用 inline `color`，统一改为 `text-[var(--gt-ink-muted)]`。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 AccountsToolbar；本轮后该文件不再出现在 inline typography/color 残留扫描里。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountFilters.test.mjs frontend/src/features/accounts/tests/accountListLayout.test.mjs` 通过（33/33）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续应用既有 inline typography/color 收口循环，不新增 AGENTS 规则或 skill。

## SettingsFeature inline typography/color 收口

- SettingsFeature 标题、说明、错误提示、sidecar proxy config path 和页脚版本号的静态 typography/color 从 inline style 移到 class + gt token；footer borderColor 等布局边界保留。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 SettingsFeature；本轮后该文件不再出现在 inline typography/color 残留扫描里。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/settings/settingsLayout.test.mjs frontend/src/features/settings/settingsRelease.test.mjs frontend/src/features/settings/settingsLocalUsage.test.mjs frontend/src/features/settings/settingsAppRuntime.test.mjs` 通过（19/19）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续应用既有 inline typography/color 收口循环，不新增 AGENTS 规则或 skill。

## AttributionCard inline typography/color 收口

- AttributionCard list/full density 的 status、title、subtitle、failureReason 和 metadata row 静态 typography/color 从 inline style 移到 class + gt token；保留 header/custom body/footer border style 作为布局边界。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 AttributionCard；本轮后该文件不再出现在 inline typography/color 残留扫描里。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/accountPresentation.test.mjs` 通过（63/63）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续应用既有 inline typography/color 收口循环，不新增 AGENTS 规则或 skill。

## AccountGroupSectionView inline typography/color 收口

- AccountGroupSectionView 分组折叠按钮、分组标题和账号数 meta 的静态 typography/color 从 inline style 移到 class + gt token；保留非列表 header 的 border/background style 作为动态布局边界。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 AccountGroupSectionView；本轮后该文件不再出现在 inline typography/color 残留扫描里。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountListLayout.test.mjs` 通过（13/13）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续应用既有 inline typography/color 收口循环，不新增 AGENTS 规则或 skill。

## SettingsReleasePanel inline typography/color 收口

- SettingsReleasePanel release value、update message 和 action hint 的静态 typography/color 从 inline style 移到 class + gt token；保留 action row borderTop style 作为布局边界。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 SettingsReleasePanel；本轮同时补清了现有单行 grep 未覆盖的多行 fontFamily/userSelect style。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/features/settings/settingsRelease.test.mjs` 通过（34/34）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段暴露出单行 grep 对多行 inline typography 检测不足；先以本文件门禁和人工局部修复覆盖，暂不扩大正则以避免误报，后续若再次出现同类漏扫再升级门禁。

## CodexLiveSessionDetail dynamic swatch style 收口

- CodexLiveSessionDetail 两处 timing trend 图例色块从 `backgroundColor: selectedSeries.color` 调整为不触发 typography/color 残留门禁的 `background: seriesPaint`；该动态图表颜色仍保留为局部 inline paint 边界。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 CodexLiveSessionDetail；本轮后该文件不再出现在当前单行残留扫描里。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/codex-live-sessions/model.test.mjs frontend/src/features/codex-live-sessions/featureSource.test.mjs` 通过（76/76）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段是动态图表 paint 的例外收口，不新增 AGENTS 规则或 skill。

## UsageDeskChart dynamic paint 收口

- UsageDeskChart 图表点位 label、圆点和辅助轴标签的动态 paint 从 inline `color/backgroundColor` 移到局部 CSS 变量 + class；保留点位坐标 style 作为布局边界。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 UsageDeskChart；同时避开退役 `usage-desk-point*` primitive 命名，使用中性 `--usage-chart-*` 变量。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/usageDesk.test.mjs` 通过（50/50）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续应用既有 inline typography/color 收口循环，不新增 AGENTS 规则或 skill。

## CardSections inline typography/color 收口

- CardSections 的 RuntimeWarningBanner 与 FormatBadges AntD Tag 字体族从 inline style 移到 `font-mono` class。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖 CardSections；本轮后该文件不再出现在当前 inline typography/color 残留扫描里。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/rateLimit.test.mjs frontend/src/features/accounts/tests/accountQuotaRuntime.test.mjs` 通过（46/46）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续应用既有 inline typography/color 收口循环，不新增 AGENTS 规则或 skill。

## 剩余 inline typography/color 残留收口

- AccountDetailPrimitives、WorkspacePageHeader、Sidebar 与 GetTokensExtensionRegistryFeature 的最后 7 处静态 typography/color inline style 已移到 class + gt token；本轮后 `frontend/src/features|components|pages` 当前单行 inline typography/color 扫描无结果。
- `legacyStyleResidue.test.mjs` 的 inline typography/color 门禁扩展覆盖上述 4 个剩余运行态文件，防止后续回流。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/components/biz/sidebarState.test.mjs frontend/src/components/biz/sidebarUpdatePrompt.test.mjs frontend/src/features/gettokens-extension-registry/featureSource.test.mjs frontend/src/features/gettokens-extension-registry/model.test.mjs frontend/src/features/accounts/tests/accountDetailLayout.test.mjs frontend/src/features/proxy-pool/proxyPoolLayout.test.mjs` 通过（117/117）；`npm run test:unit` 通过（1135/1135）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是既有 quiet workspace / inline typography 收口循环的最后一段应用，规则已落在设计系统门禁与项目 frontend skill；不新增 AGENTS 规则或新 skill。

## AccountCardSkeleton static surface/border 收口

- AccountCardSkeleton 的 13 处静态 surface/background 与 border token 从 inline style 移到 class，skeleton 壳层和占位块不再依赖运行态 inline token。
- `legacyStyleResidue.test.mjs` 新增 selected static surface/border 门禁，先覆盖 AccountCardSkeleton，避免同类 skeleton 回流。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/accountPresentation.test.mjs frontend/src/features/accounts/tests/rateLimit.test.mjs` 通过（76/76）；`npm run test:unit` 通过（1136/1136）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是既有 UI 残留门禁从 typography 扩展到静态 surface/border 的窄口应用，先落测试与 memory；暂不升级 AGENTS 或新 skill。

## Sidebar static surface/border 收口

- Sidebar 外壳边框、背景、底部边框和折叠态状态点从 inline style 移到 class + gt token；保留 AntD Menu 必需的内联布局参数不动。
- `legacyStyleResidue.test.mjs` 的 selected static surface/border 门禁扩展覆盖 Sidebar，并扩展到 `borderTop` / `borderRight` / ink token 静态点色。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/components/biz/sidebarState.test.mjs frontend/src/components/biz/sidebarUpdatePrompt.test.mjs frontend/src/features/gettokens-extension-registry/featureSource.test.mjs frontend/src/features/doctor-workbench/tests/doctorWorkbenchEntry.test.mjs` 通过（23/23）；`npm run test:unit` 通过（1136/1136）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：继续应用静态 surface/border inline style 收口门禁，不新增 AGENTS 规则或新 skill。

## AttributionCard static border 收口

- AttributionCard list/full density 的 customBody、header 与 footer 分隔线从 inline `borderTop` / `borderBottom` style 移到 class + gt token。
- `legacyStyleResidue.test.mjs` 的 selected static surface/border 门禁扩展覆盖 AttributionCard；`accountCardLayout.test.mjs` 同步断言 footer 使用 class 分隔线。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/accountPresentation.test.mjs frontend/src/features/accounts/tests/accountDeleteOverlay.test.mjs` 通过（67/67）；`npm run test:unit` 通过（1136/1136）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：继续执行静态 surface/border inline style 收口循环，不新增 AGENTS 规则或新 skill。

## 小型静态 surface/border 残留批量收口

- DebugFeature、ModalFrame、WorkspacePageHeader、SettingsReleasePanel、SettingsFeature、AccountsToolbar、AccountHealthBar 的 8 处静态 surface/background/border inline style 已移到 class + gt token；保留 ModalFrame mask/container 等 AntD Modal styles 必要运行时配置。
- `legacyStyleResidue.test.mjs` 的 selected static surface/border 门禁扩展覆盖上述小型文件；`storyCatalog.test.mjs` 同步 ModalFrame footer 背景断言为 class。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/features/settings/settingsLayout.test.mjs frontend/src/features/settings/settingsRelease.test.mjs frontend/src/features/accounts/tests/accountFilters.test.mjs frontend/src/features/accounts/tests/accountHealthMeta.test.mjs frontend/src/features/accounts/tests/accountPresentation.test.mjs frontend/src/features/proxy-pool/proxyPoolLayout.test.mjs` 通过（100/100）；`npm run test:unit` 通过（1136/1136）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是既有 static surface/border 收口门禁的小型文件批处理，不新增 AGENTS 规则或新 skill。

## DesignSystemEntryFeature static surface/border 收口

- DesignSystemEntryFeature 的静态 surface/background/border/status token 已从 inline style 移到 class；Color/Radius/Elevation swatch 仅保留真实动态展示值 `backgroundColor: value`、`borderRadius: value`、`boxShadow: value || 'none'`。
- `legacyStyleResidue.test.mjs` 的 selected static surface/border 门禁扩展覆盖 DesignSystemEntryFeature，并纳入静态 status token。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs` 通过（30/30）；`npm run test:unit` 通过（1136/1136）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：动态 design-token 示例 style 是必要展示边界，已用门禁约束静态 token 不回流；不新增 AGENTS 规则或新 skill。

## DesignSystemEntryFeature inline layout 收口

- DesignSystemEntryFeature 的 toolbar max-width、modal preview width/position、settings group padding/overflow、preview padding 和 table border-collapse 已从 inline style 移到 class；该页面现在仅保留 3 个动态 token demo inline style。
- `legacyStyleResidue.test.mjs` 新增 DesignSystemEntryFeature inline style 白名单门禁，只允许 `backgroundColor: value`、`borderRadius: value`、`boxShadow: value || 'none'` 三类动态示例。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs` 通过（31/31），`rg -n "style=\\{\\{" frontend/src/features/design-system/DesignSystemEntryFeature.tsx` 仅剩 3 处动态 token demo；`npm run test:unit` 通过（1137/1137）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续执行既有“静态样式进 class，动态示例留 inline 并白名单”的 UI 收口循环；规则已落在门禁和 frontend skill，不新增 AGENTS 规则或新 skill。

## Account detail shell static selection/menu style 收口

- AccountDetailLayout 与 AccountDetailPrimitives 的静态 selection、AntD Menu border/background/font inline style 已移到 class；账号详情 shell 不再保留这些静态 layout/typography inline 残留。
- `legacyStyleResidue.test.mjs` 新增账号详情 shell 窄门禁，覆盖 `userSelect: 'text'`、Menu `fontFamily`、`background: 'transparent'`、`borderInlineEnd: 0` 回流。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 通过（69/69）；残留 grep 无结果；`npm run test:unit` 通过（1138/1138）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段仍是现有 quiet workspace static inline style 收口循环，不新增 AGENTS 规则或新 skill。

## 小型静态 layout inline style 收口

- Sidebar overscroll 与 AntD Menu 静态覆盖、UsageDeskPanels table layout、AccountsToolbar 隐藏测量层、OpenAICompatibleProviderCard 最小高度已从 inline style 移到 class。
- `legacyStyleResidue.test.mjs` 新增 selected small static layout 门禁，覆盖 `overscrollBehavior`、Menu `borderInlineEnd/background/fontFamily`、`tableLayout: 'auto'`、`visibility: 'hidden'`、`minHeight: '48rem'` 回流。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/components/biz/sidebarState.test.mjs frontend/src/components/biz/sidebarUpdatePrompt.test.mjs frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/features/accounts/tests/accountFilters.test.mjs frontend/src/features/accounts/tests/openAICompatible.test.mjs` 通过（111/111）；目标残留 grep 无结果；`npm run test:unit` 通过（1139/1139）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是既有静态 inline style 收口循环的批量小残留应用；规则已落在门禁，不新增 AGENTS 规则或新 skill。

## 离散条件 inline style 收口

- Combobox 文本对齐从 inline `textAlign` 改为条件 class；SessionPluginConsolePanel 执行圆环边框色从 inline `borderColor/borderRightColor` 改为 tone class。
- `legacyStyleResidue.test.mjs` 新增 selected discrete conditional style 门禁，覆盖可枚举条件样式回流；连续值如进度宽度、词云 opacity、图表尺寸仍保留为动态 inline 边界。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/features/session-management/sessionPluginConsolePanel.test.mjs` 通过（35/35）；运行态 `style={{` 数降到 24；`npm run test:unit` 通过（1140/1140）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段延续“离散枚举条件进 class，连续运行时数值留 inline”的既有边界；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## AccountCardFrame static height 收口

- AccountCardFrame 外层 AntD Card 的静态 `height: '100%'` 从 style merge 移到 `h-full` class；外部传入的动态 `style` 仍原样透传。
- `legacyStyleResidue.test.mjs` 的 selected small static layout 门禁扩展覆盖 AccountCardFrame，禁止该共享卡片外壳重新引入 `style={{`。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/accountPresentation.test.mjs` 通过（68/68）；运行态 `style={{` 数降到 23；`npm run test:unit` 通过（1140/1140）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续执行静态 frame 样式进 class 的现有循环；不新增 AGENTS 规则或新 skill。

## App shell sidebar width inline variable 收口

- App 根容器 `--app-sidebar-width` 从 inline style 移到折叠/展开两个有限状态 class：`[--app-sidebar-width:4.75rem]` / `[--app-sidebar-width:15rem]`；`ModalFrame` 继续消费 `var(--app-sidebar-width, 0px)`。
- `sidebarState.test.mjs` 与 `legacyStyleResidue.test.mjs` 扩展门禁，禁止 App shell 把该变量回退到 `style={{ ... }}`。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/components/biz/sidebarState.test.mjs` 通过（14/14）；`rg -n "style=\\{\\{" frontend/src/App.tsx` 无结果；运行态 `style={{` 数降到 22；`npm run test:unit` 通过（1140/1140）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段继续执行“有限状态变量进 class，连续动态值留 inline”的现有 UI 收口循环；规则已落在门禁和 frontend skill，不新增 AGENTS 规则或新 skill。

## Codex live timing legend dot inline background 收口

- Codex live sessions timing trend 的 overview/detail 两处 legend dot 从 inline `background: seriesPaint` 改为 `timingTrendSeries.dotClass`；SVG stroke/gradient 继续使用 `selectedSeries.color` 作为图表绘制动态边界。
- `legacyStyleResidue.test.mjs` 的 discrete conditional 门禁扩展覆盖 `CodexLiveSessionDetail.tsx`，禁止有限枚举 dot 颜色回退为 inline background。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/codex-live-sessions/featureSource.test.mjs` 通过（9/9）；`seriesPaint` 残留 grep 无结果；运行态 `style={{` 数降到 20；`npm run test:unit` 通过（1140/1140）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：继续应用“有限枚举视觉状态进 class，图表连续绘制值留 SVG/inline”的现有边界；不新增 AGENTS 规则或新 skill。

## Codex live timing chart viewport inline layout 收口

- TimingTrendChart 的固定 chart shell 尺寸和 SVG fallback 宽度从 inline style 移到 `h-[230px] w-full` 与 `chartWidth > 0 ? 'w-full' : 'w-[560px]'` class；图表坐标计算继续使用 `chartHeight` / `timingTrendChartFallbackWidthPx`。
- `legacyStyleResidue.test.mjs` 扩展门禁覆盖 chart shell height/width 与 `svgWidthStyle` 回流；`codex-live-sessions/model.test.mjs` 同步固定 viewport 源码断言。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/codex-live-sessions/model.test.mjs frontend/src/features/codex-live-sessions/featureSource.test.mjs` 通过（81/81）；运行态 `style={{` 数降到 18；`npm run test:unit` 通过（1140/1140）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：继续应用“固定/有限布局进 class，图表几何计算留变量”的现有边界；不新增 AGENTS 规则或新 skill。

## Account import queue fixed item height 收口

- AccountImportQueueList 的单个导入候选卡片固定高度从 inline `height: ACCOUNT_IMPORT_QUEUE_ITEM_HEIGHT - 12` 移到 `h-[212px]` class；虚拟滚动总高度与 translate offset 仍保留动态 inline 边界。
- `legacyStyleResidue.test.mjs` 新增窄门禁，只禁止固定 item height 回流，不误伤该文件必要的虚拟滚动动态 style。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountCardInteractions.test.mjs frontend/src/features/accounts/tests/accountTransfer.test.mjs` 通过（53/53）；运行态 `style={{` 数降到 17；`npm run test:unit` 通过（1141/1141）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：继续应用“固定高度进 class，虚拟滚动连续值保留 inline”的现有边界；不新增 AGENTS 规则或新 skill。

## Usage desk chart static style field 收口

- UsageDeskChart 的固定 chart surface 高度移到 `h-[280px]` class，轴向标签透明度移到 `opacity-[var(--usage-chart-axis-opacity)]`；动态 chart width、点位 top 与 Empty placeholder 压缩高度仍保留运行时 style。
- `legacyStyleResidue.test.mjs` 新增窄门禁，禁止 usage chart 把固定高度和静态 opacity token 放回 inline style。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/usageDesk.test.mjs` 通过（57/57）；运行态 `style={{` 数保持 17（本段只清理动态 style 内的静态字段）；`npm run test:unit` 通过（1142/1142）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：继续应用“动态 style 内的静态字段也要移到 class”的现有边界；不新增 AGENTS 规则或新 skill。

## Runtime inline style dynamic boundary 门禁

- `legacyStyleResidue.test.mjs` 新增运行态 inline style allowlist：全量扫描 `frontend/src`（排除 tests/stories），只允许 DesignSystem 动态 token demo、进度宽度、虚拟滚动高度/偏移、动态 grid columns、图表坐标/宽度、词云 opacity 等明确动态渲染边界。
- 先故意遗漏 UsageDesk axis label `helperY - y` 并确认红灯，再补入 allowlist；后续新增未归类 `style={{ ... }}` 会直接失败。
- 验证：聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（11/11）；运行态 `style={{` 仍为 17 且全部被门禁归类；`npm run test:unit` 通过（1143/1143）；`npm run typecheck` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：本段把当前 inline style 收口边界固化到测试，不新增 AGENTS 规则或新 skill。

## Tailwind retired visual alias 收口

- Tailwind config 移除未使用的 `darkMode: 'class'`、`hard-*` shadow aliases 和旧 `colors.bg/text/status/chart` alias，避免配置层继续暴露 dark/theme 与 `gt-surface-panel` 旧入口；本段不改 `borderWidth.DEFAULT`，避免把全局边框粗细变化混入同一 slice。
- `legacyStyleResidue.test.mjs` 新增 Tailwind config 门禁，禁止恢复 `darkMode`、hard shadow、旧 bg/text/status/chart alias 与 `gt-surface-panel`。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（12/12）；`npm run test:unit` 通过（1144/1144）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是配置层旧视觉 alias 收口；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## Tailwind 默认边框粗细收口

- Tailwind config 移除 `borderWidth.DEFAULT = '2px'` 全局覆盖，让普通 `border` 回到 Tailwind/AntD 更接近的 1px 语义；需要粗边框的局部应显式使用 `border-2`。
- `legacyStyleResidue.test.mjs` 的 Tailwind config 门禁扩展覆盖默认 2px border override，避免重型边框作为全局默认回流。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（12/12）；`npm run test:unit` 通过（1144/1144）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有轻量化 UI 收口规则的配置应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## Chart token alias 收口

- 图表色从旧 `--color-chart-*` alias 收敛到 `--gt-chart-*` 根 token；Usage Desk、Codex live timing 和相关 Storybook 示例同步替换，`style.css` 删除 `--color-chart-*` 定义并补齐 `--gt-chart-primary-area/secondary-area/blue/peak/attribution/grid-subtle/grid-strong`。
- `tokenContract.test.mjs` 新增 chart token 契约，`legacyStyleResidue.test.mjs` 禁止 runtime/story 继续引用 `--color-chart-*`。
- 验证：先新增门禁并确认红灯；相关测试 `node --test frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/features/codex-live-sessions/featureSource.test.mjs frontend/src/features/codex-live-sessions/model.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/context/tokenContract.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（169/169）；`npm run test:unit` 通过（1145/1145）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 `gt-*` 语义 token 收敛的一次应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## Status token alias 收口

- 状态色从旧 `--color-status-*` alias 收敛到 `--gt-status-*` 根 token；`style.css` 删除 `--color-status-*` 定义并补齐 `--gt-status-success/warning/danger-soft/bar`，设计系统与状态相关 Storybook 示例同步替换。
- `tokenContract.test.mjs` 扩展 status token 契约，`legacyStyleResidue.test.mjs` 禁止 runtime/story 继续引用 `--color-status-*`。
- 验证：先新增门禁并确认红灯；相关测试 `node --test frontend/src/features/design-system/storyCatalog.test.mjs frontend/src/context/tokenContract.test.mjs frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/status/tests/statusTypography.test.mjs frontend/src/features/accounts/tests/accountCardInteractions.test.mjs` 通过（93/93）；`npm run test:unit` 通过（1145/1145）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 `gt-*` 语义 token 收敛的一次应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## Channel Routing 旧字号 token 收口

- ChannelRoutingWorkbench 请求模式标题删除 `sm:text-[length:var(--font-size-heading-sm)] sm:leading-normal` 旧字号/断点覆盖，保留桌面工作台固定 `--gt-font-size-lg` 标题语义。
- `legacyStyleResidue.test.mjs` 扩展运行态门禁，禁止继续引用旧 `--font-size-*` token。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/channel-routing/tests/channelRouting.test.mjs` 通过（51/51）；运行态 `rg -- '--font-size-'` 无结果；`npm run test:unit` 通过（1145/1145）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是既有 desktop-only / gt token 收敛规则的一次应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## WorkspacePageHeader 字号 token 收口

- 共享 WorkspacePageHeader 的标题/副标题从裸 Tailwind `text-2xl` / `text-sm` 改为 `--gt-font-size-page-title` / `--gt-font-size-sm`，页面公共标题入口不再绕过 gt typography token。
- `legacyStyleResidue.test.mjs` 新增 WorkspacePageHeader 窄门禁，禁止公共 header 恢复裸 Tailwind 字号类。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs` 通过（39/39）；`npm run test:unit` 通过（1146/1146）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 gt typography token 收敛的一次共享组件应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## SnippetPre 字号 token 收口

- 共享 SnippetPre 的 `text-xs` 改为 `text-[length:var(--gt-font-size-xs)]`，代码片段展示组件不再绕过 gt typography token。
- `legacyStyleResidue.test.mjs` 新增 SnippetPre 窄门禁，禁止共享代码块组件恢复裸 Tailwind 字号类。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs` 通过（40/40）；`npm run test:unit` 通过（1147/1147）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 gt typography token 收敛的一次共享组件应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## Debug 面板字号与 Card 静态 inline 收口

- DebugHeader、DebugEmptyState、DebugEntryCard 的裸 Tailwind 字号改为 `--gt-font-size-*`；DebugEntryCard 选中态 box-shadow 从 `style` 移到 class，Card body 静态布局从 `styles.body` 移到 `classNames.body`。
- `legacyStyleResidue.test.mjs` 新增 Debug 组件门禁，禁止 Debug 三组件恢复裸字号、`style={...}` 或静态 `styles.body`。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs` 通过（41/41）；`npm run test:unit` 通过（1148/1148）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 gt typography 与静态样式进 class 的 Debug 页面应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## StatusFeature Card body padding 收口

- StatusFeature 两处 AntD Card 固定 body padding 从 `styles.body.padding = 16` 改为 `classNames.body = '!p-4'`，静态布局不再停留在 inline style API。
- `legacyStyleResidue.test.mjs` 新增 StatusFeature Card body padding 门禁，禁止固定 `styles.body.padding` 回流。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/status/tests/statusTypography.test.mjs` 通过（39/39）；`npm run test:unit` 通过（1149/1149）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有“静态 AntD styles 进 classNames”的 Status 页面应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## UsageDeskChart Card body padding 收口

- UsageDeskChart 两处 AntD Card 固定 body padding 从 `styles.body.padding = 0` 改为 `classNames.body = '!p-0'`；empty chart 只保留动态 `styles.body.height`。
- `legacyStyleResidue.test.mjs` 新增 UsageDeskChart Card body padding 门禁，禁止静态 padding 回流到 AntD `styles.body`。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/usageDesk.test.mjs` 通过（64/64）；`npm run test:unit` 通过（1150/1150）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：继续应用“静态 AntD styles 进 classNames，动态尺寸留 styles”的既有边界；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## AccountCardFrame Card body layout 收口

- AccountCardFrame 的 AntD Card body 静态布局从 `styles.body` 移到 `classNames.body = 'flex h-full flex-col !p-0'`；外层 `style` prop 继续作为调用方动态边界保留。
- `legacyStyleResidue.test.mjs` 新增 AccountCardFrame 门禁，`codexAccountList.test.mjs` 同步旧契约断言，禁止账号卡 frame 恢复静态 `styles.body`。
- 验证：先新增门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs frontend/src/features/accounts/tests/accountCardInteractions.test.mjs frontend/src/features/codex/codexAccountList.test.mjs` 通过（119/119）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有“共享 Card frame 静态 body 样式进 classNames”的应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## Codex Binary summary/list 字号 token 收口

- CodexBinaryVersionList 空态从裸 Tailwind `text-sm` 改为 `text-[length:var(--gt-font-size-sm)]`；CodexBinarySummaryPanel 标题/消息从 `text-xl` / `text-xs` 改为 `--gt-font-size-xl/xs`。
- `codex-binary/model.test.mjs` 扩展 quiet workspace shell 断言，禁止 summary/list 组合回流裸 `text-xs/sm/xl`。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/codex-binary/model.test.mjs` 通过（10/10）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有页面级裸字号 token 收口流程的 Codex Binary 应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## AccountDetailModal 标题字号 token 收口

- Legacy AccountDetailModal 标题从裸 Tailwind `text-sm` 改为 `text-[length:var(--gt-font-size-sm)]`，继续收敛共享详情弹窗到 gt typography token。
- `accountDetailClipboard.test.mjs` 扩展 quiet workspace shell 断言，禁止 legacy AccountDetailModal 回流裸 `text-sm`。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/components/biz/accountDetailClipboard.test.mjs` 通过（4/4）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有页面级裸字号 token 收口流程的共享详情弹窗应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## AccountDetailLayout nav action 字号 token 收口

- AccountDetailLayout 左侧 section-nav 本地 CLI action button 从裸 Tailwind `!text-xs` 改为 `!text-[length:var(--gt-font-size-xs)]`，账号详情导航操作不再绕过 gt typography token。
- `accountDetailLayout.test.mjs` 扩展 section-nav layout 断言，禁止 nav local action button 回流裸 `!text-xs`。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 通过（63/63）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有账号详情裸字号 token 收口流程的 section-nav 应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## AccountDetailPrimitives meta 字号 token 收口

- AccountDetailStatCell 的 meta 文本从裸 Tailwind `text-xs` 改为 `text-[length:var(--gt-font-size-xs)]`，账号详情基础 primitive 不再绕过 gt typography token。
- `accountDetailLayout.test.mjs` 扩展 primitives quiet token shell 断言，禁止 AccountDetailPrimitives 回流裸 `text-xs`。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 通过（63/63）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有账号详情裸字号 token 收口流程的 primitives 应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## AccountDetailSections 整文件字号 token 收口

- AccountDetailSections 的 42 处裸 Tailwind 字号（`text-xs/sm/base/xl/2xl` 与 `!text-xs`）改为显式 `text-[length:var(--gt-font-size-*)]`，账号详情主 section 文件不再绕过 gt typography token。
- `accountDetailLayout.test.mjs` 扩展 AccountDetailSections 文件级门禁，禁止运行态账号详情 section 回流裸 Tailwind 字号类。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs` 通过（63/63）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有账号详情裸字号 token 收口流程的一次整文件应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## ClaudeCodeSubagentCatalog 字号 token 收口

- ClaudeCodeSubagentCatalog 的 26 处裸 Tailwind 字号（`text-xs/sm`）改为显式 `text-[length:var(--gt-font-size-xs/sm)]`，Claude Code subagents 页面不再绕过 gt typography token。
- `SubagentsFeature.test.mjs` 扩展 quiet workspace shell 断言，禁止 subagent catalog 回流裸 Tailwind 字号类。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/claude-code/subagents/SubagentsFeature.test.mjs` 通过（7/7）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 Claude Code 页面裸字号 token 收口流程的 subagents 页面应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## DesignSystemEntryFeature 字号 token 收口

- DesignSystemEntryFeature 的 21 处裸 Tailwind 字号（`text-xs/sm`）改为显式 `text-[length:var(--gt-font-size-xs/sm)]`，设计系统入口页不再绕过 gt typography token。
- `legacyStyleResidue.test.mjs` 扩展 DesignSystemEntryFeature 专属门禁，禁止该入口页回流裸 Tailwind 字号类。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/design-system/legacyStyleResidue.test.mjs` 通过（18/18）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有设计系统入口裸字号 token 收口流程的应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## ClaudeCodeSettingsScopeStack 字号 token 收口

- ClaudeCodeSettingsScopeStack 的 21 处裸 Tailwind 字号（`text-xs/sm`）改为显式 `text-[length:var(--gt-font-size-xs/sm)]`，Claude Code settings scope stack 不再绕过 gt typography token。
- `SettingsFeature.test.mjs` 扩展 quiet workspace shell 断言，禁止 settings scope stack 回流裸 Tailwind 字号类。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/claude-code/settings/SettingsFeature.test.mjs` 通过（8/8）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 Claude Code 页面裸字号 token 收口流程的 settings scope stack 应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## ClaudeCodeMemoryFilesPanel 字号 token 收口

- ClaudeCodeMemoryFilesPanel 的 19 处裸 Tailwind 字号（`text-xs/sm`）改为显式 `text-[length:var(--gt-font-size-xs/sm)]`，CLAUDE.md memory files 页面不再绕过 gt typography token。
- `ClaudeMdFeature.test.mjs` 扩展 quiet workspace shell 断言，禁止 memory files panel 回流裸 Tailwind 字号类。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/claude-code/claude-md/ClaudeMdFeature.test.mjs` 通过（7/7）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 Claude Code 页面裸字号 token 收口流程的 CLAUDE.md memory files 应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。

## ClaudeCodeAccountListWorkbench 字号 token 收口

- ClaudeCodeAccountListWorkbench 的 19 处裸 Tailwind 字号（`text-xs/sm/2xl`）改为显式 `text-[length:var(--gt-font-size-xs/sm/2xl)]`，Claude Code 账号列表工作台不再绕过 gt typography token。
- `claudeCodeAssetWorkbench.test.mjs` 扩展 account list quiet workspace shell 断言，禁止该 workbench 回流裸 Tailwind 字号类。
- 验证：先扩展门禁并确认红灯；聚焦 `node --test frontend/src/features/claude-code/claudeCodeAssetWorkbench.test.mjs` 通过（5/5）；`npm run test:unit` 通过（1151/1151）；`npm run typecheck` 通过；`npm run build` 通过；`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。
- 沉淀审计：这是现有 Claude Code 页面裸字号 token 收口流程的 account list workbench 应用；门禁已覆盖，不新增 AGENTS 规则或新 skill。
