# 2026-04-25

- 决策：新增独立 Debug 页签，前端通过 `DebugContext.trackRequest()` 统一记录 Wails 接口与 HTTP 请求的发送/响应数据；后续新增接口时，默认优先接入这条埋点链路。
- 决策：账号池卡片默认展示 `provider`，对 Codex 凭证额外直出额度摘要（优先显示 5H / 7D 剩余额度），实现链路为 Wails `GetCodexQuota(name)` 读取 auth file 后直连 `chatgpt.com/backend-api/wham/usage`，不依赖 sidecar `/api-call` 暴露。
- 决策：账号池页面标题统一使用“账号池”，顶部工具栏采用“添加账号 + 刷新”操作组，搜索框不再额外套一层外框，避免出现框中框。
- 里程碑：已创建账号池工作空间 `docs-linhay/spaces/account-pool/`，后续账号池相关 README、计划、截图与 debate 资料统一归档到该 space。
- 决策：Svelte -> React 的第一阶段稳定后，第二阶段再单独做 `jsx -> tsx` 收敛；不把“框架迁移”和“类型系统迁移”绑在同一轮提交里。
- 已完成 React 前端的 `tsx` 迁移，覆盖入口、context、pages、业务组件与基础 UI 组件，并新增 `frontend/tsconfig.json`、`frontend/src/types.ts`、`frontend/src/utils/error.ts`。
- 已正式接入 `typescript`、`@types/react@18`、`@types/react-dom@18`，不再依赖临时 React type shim。
- 已完成验证：
  - `frontend/npm run typecheck` 通过
  - `frontend/npm run build` 通过
  - `npm_config_offline=true ./scripts/wails-cli.sh build -skipbindings` 通过
- 持续约束：TypeScript 收敛阶段仍不扩大范围到 Go 契约、sidecar API、状态管理库或视觉系统重做，避免把“类型化”演变成新一轮架构迁移。
- 前端调试工具决策更新：弃用 `@locator/runtime`，改为 `@linhey/react-debug-inspector` + `@vitejs/plugin-react`。原因是当前需求不是 IDE 跳转，而是开发态选中节点并复制组件/文件/行号给 AI 做定位。
- 已完成验证：
  - `frontend/npm run typecheck` 通过
  - `frontend/npm run build` 通过
- 项目级 skill `gettokens-frontend-debug` 已同步更新到 React/TSX 现状，后续前端定位问题优先沿这条调试链路执行。
- 手动验收：通过 `./scripts/wails-cli.sh dev` 启动真实 Wails 桌面环境并在浏览器 DevServer 完成交互验证，`调试面板` 已能展示 `GetVersion`、`GetSidecarStatus`、`ListAuthFiles` 以及 `fetch /healthz` 的发送参数、响应数据、状态与耗时。
- 修复：账号池对 `chatgpt`/`nolon chatgptAccount` auth 文件增加 provider/type 推断，`auth.json` 这类 sidecar 列表返回 `unknown` 的凭证现在会被识别为 `codex`。
- 修复：Codex 额度解析补上 `tokens.access_token`、`tokens.id_token` 和 JWT 内嵌 `https://api.openai.com/auth` claims 的读取；当在线 `wham/usage` 返回 `401` 时，回退使用 auth 文件中的 `nolon.usage_cache` 展示缓存额度。
- 调整：账号池卡片的额度区改为“上方额度栅格 + 下方窗口/用量/重置时间明细”，保留 Swiss-industrial 风格，但把原本单行摘要换成可扫读的二维额度视图。
- 扩展：账号池列表在 Wails `ListAuthFiles` 阶段补充解析 auth file 的 `email / planType`，前端卡片头部现已改为“账号名下方显示邮箱和套餐”，不再只依赖额度接口回填套餐。
- 设计评审：账号池卡片按“头部身份信息 -> 中部额度遥测 -> 底部状态/操作”三段重排，减少重复标签与纵向噪音；provider 退回右上角单标签，套餐下沉到账号名副信息行，额度行强化剩余百分比和重置时间。
- 治理约束：后续用户只说“整理”且语境明确指向当前工作会话时，默认直接触发一次会话沉淀流程，不再额外追问；先更新 `.agents/skills/`，再按 repo-wide 稳定性决定是否同步更新 `AGENTS.md`、`docs-linhay/dev/` 与 `docs-linhay/memory/`。
- 决策：`internal/wailsapp/app.go` 开始按职责拆分为 `runtime / update / sidecar_client / auth_files / quota / types` 多文件结构，`App` 保持 Wails 导出签名不变，只收缩为宿主骨架，避免后续接入 `codex-api-key` 时继续把 sidecar 路由、DTO 和账号领域逻辑堆进单文件。
- 已完成验证：
  - `go test ./...` 通过
  - `frontend/npm run typecheck` 通过
  - `frontend/npm run build` 通过
- 决策：账号池正式接入 `codex api key` 资产，采用“同页统一账号模型”而不是新开页面；`provider` 与 `credentialSource` 分离展示，`api-key` 首版不接额度，只展示脱敏指纹、`baseUrl`、`prefix` 摘要。
- 偏好更新：账号池中的 `codex api key` 允许明文查看，但仅限详情面板；卡片摘要不展示完整 `apiKey`，只保留名称与摘要字段。
- 调试链路扩展：`GetCodexQuota` 内部发起的 `GET https://chatgpt.com/backend-api/wham/usage` 现在会通过 Wails `debug:entry` 事件写入前端调试面板，展示请求头（`Authorization` 已脱敏）和响应体，便于排查额度口径问题。
- 架构：新增 `internal/cliproxyapi` 负责 sidecar `codex-api-key` 管理接口，新增 `internal/accounts` 统一账号记录与资产唯一键；唯一性按凭证资产判定，`auth-file:<name>` 与 `codex-api-key:<fingerprint>@<normalized-base-url>#<prefix>` 不做跨来源合并。
- 里程碑：Wails 新增 `ListAccounts / CreateCodexAPIKey / DeleteCodexAPIKey`，前端账号池已从本地会话态 API key 草稿切换到真实后端持久化链路。
- 决策：`CLIProxyAPI` 不再只作为参考目录临时打补丁，已正式 fork 到 `linhay/CLIProxyAPI`，本地维护关系固定为 `origin=git@github.com:linhay/CLIProxyAPI.git`、`upstream=https://github.com/router-for-me/CLIProxyAPI`，后续以 `upstream` 同步、`origin` 承载定制修复。
- 里程碑：本地 `docs-linhay/references/CLIProxyAPI` 已同步到上游 `main@2c626efc (v6.9.38)`，定制修复提交在分支 `gettokens/wham-token-fix`（commit `90fc9688`）。
- 修复：`CLIProxyAPI` management `/api-call` 的 token 解析已补齐 `metadata.tokens.access_token / accessToken`，避免 Codex auth file 通过 `auth_index` 代发 `wham/usage` 时误报 `auth token not found`。
- 沉淀：新增项目级 skill `gettokens-cliproxyapi-fork-maintenance`，把 `CLIProxyAPI` 行为修复时的标准动作固化为“fork/upstream 同步 -> 基于最新上游建分支 -> 跑 CLIProxyAPI 测试 -> 必要时替换 app bundle sidecar -> 用运行态日志核验”。
