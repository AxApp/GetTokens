# 2026-05-25

- Account Routing Engine WebSocket request-boundary 特例收口完成：CLIProxyAPI fork 将 pinned auth guarded 检查抽成 `responsesWebsocketReleasePinnedAuthAtRequestBoundary()`，统一使用 `AccountRouteGuardStore` 判断 guarded auth；命中时释放 pin、关闭旧 execution session、强制下一条请求 transcript replay，未命中时保持 pinned auth 不变。验证：`go test ./sdk/api/handlers/openai -run 'TestResponsesWebsocketRequestBoundaryReleaseUsesRouteGuard|TestResponsesWebsocketReleasesPinnedAuthAfterRouteGuardBlock|TestResponsesWebSocket.*Guard|TestResponsesWebSocket.*Failover|Test.*previous_response_id'`、`go test ./internal/gettokenshooks ./internal/cmd ./sdk/cliproxy/auth ./sdk/api/handlers/openai -run 'TestRateLimit|TestAccountRouteGuard|TestInstallGetTokensHooksInstallsRoutePolicy|Test.*RoutePolicy|Test.*Scheduler|TestSessionAffinitySelector|TestLegacy.*SessionAffinityRoutePolicy|TestManager_InitializesSchedulerForBuiltInSelector|TestResponsesWebsocketRequestBoundaryReleaseUsesRouteGuard|TestResponsesWebsocketReleasesPinnedAuthAfterRouteGuardBlock|TestResponsesWebSocket.*Guard|TestResponsesWebSocket.*Failover|Test.*previous_response_id'`、`go test ./internal/gettokenshooks ./internal/cmd ./sdk/cliproxy/auth ./sdk/api/handlers/openai` 通过。剩余兼容边界缩减为公共 `RoutePolicy` API。
- Account Routing Engine sticky wrapper 主线收敛完成：CLIProxyAPI fork 中 `SessionAffinitySelector` 继续保留 cache/TTL/fallback selector 语义，但 scheduler 会识别它、提取 fallback strategy，并把 selector 自身作为 manager-local `PolicyStageSticky` 附加到 route engine。cache hit / fallback cache hit 只在当前候选池内排序绑定账号，cache miss 由 built-in selector 选中后通过 `BindRouteResult` 写回 session cache；manager 不再因为 session affinity wrapper 退出 scheduler fast path。验证：`go test ./sdk/cliproxy/auth -run 'TestSchedulerSessionAffinity|TestLegacy.*SessionAffinityRoutePolicy|TestSchedulerRoutePolicy|TestSessionAffinitySelector|TestManager_InitializesSchedulerForBuiltInSelector'`、`go test ./internal/gettokenshooks ./internal/cmd ./sdk/cliproxy/auth -run 'TestRateLimit|TestAccountRouteGuard|TestInstallGetTokensHooksInstallsRoutePolicy|Test.*RoutePolicy|Test.*Scheduler|TestSessionAffinitySelector|TestLegacy.*SessionAffinityRoutePolicy|TestManager_InitializesSchedulerForBuiltInSelector'`、`go test ./internal/gettokenshooks ./internal/cmd ./sdk/cliproxy/auth` 通过。当时剩余 selector shim 缩减为公共 `RoutePolicy` 兼容 API 与 WebSocket request-boundary 特例；其中 WebSocket request-boundary 已在后续批次收口为 helper。
- Account Routing Engine session affinity legacy path 收敛一层：CLIProxyAPI fork 的 `pickNextLegacy` / `pickNextMixedLegacy` 在调用 sticky selector 前复用 staged `RoutePolicy` / `gettokensrouting.Engine` rewrite，新增 `rewriteAuthCandidates` 让 legacy `[]*Auth` 候选复用 scheduler policy pipeline。新增单 provider 与 mixed provider 测试证明 hard guard deny 不能被 session affinity cache 或 fallback 绕过。验证：`go test ./sdk/cliproxy/auth -run 'TestLegacy.*SessionAffinityRoutePolicy|TestSchedulerRoutePolicy|TestSessionAffinitySelector'`、`go test ./internal/gettokenshooks ./internal/cmd ./sdk/cliproxy/auth -run 'TestRateLimit|TestAccountRouteGuard|TestInstallGetTokensHooksInstallsRoutePolicy|Test.*RoutePolicy|Test.*Scheduler|TestSessionAffinitySelector|TestLegacy.*SessionAffinityRoutePolicy'`、`go test ./internal/gettokenshooks ./internal/cmd ./sdk/cliproxy/auth` 通过。当时剩余 shim 为公共 `RoutePolicy` 兼容 API、WebSocket request-boundary 特例，以及后续可选 engine-native sticky stage 重构；其中 sticky stage 已在后续批次完成主线收敛。
- Account Routing Engine rate-limit 双路径收敛完成：CLIProxyAPI fork 删除 `rateLimitPolicy` 兼容 RoutePolicy 注册与 `defaultRateLimitCleanup`，`InstallRateLimitHook()` 只负责启动 evaluator；rate-limit blocked state 统一通过 `AccountRouteGuardSourceRateLimit -> accountRouteGuardPolicy` 在热路径 deny。验证：CLIProxyAPI fork `go test ./internal/gettokenshooks -run 'TestRateLimit|TestAccountRouteGuard'`、`go test ./internal/gettokenshooks ./internal/cmd ./sdk/cliproxy/auth -run 'TestRateLimit|TestAccountRouteGuard|TestInstallGetTokensHooksInstallsRoutePolicy|Test.*RoutePolicy|Test.*Scheduler'` 通过。当时 selector shim 剩余项缩减为普通 session affinity wrapper、公共 `RoutePolicy` 兼容 API 和 WebSocket request-boundary 特例；其中 session affinity legacy path 已在后续收敛中接入 route policy seam。
- Account Routing Engine route ledger 入口补齐：`ChannelRoutingWorkbench` 新增 `Route Ledger` 区块，Codex / Claude 桌面模式读取 `ListChannelRouteEvents(channel, limit=5)`，浏览器 preview 在 Explain 后合成 redacted preview event；新增 `buildChannelRouteAuditEventSummary` 和 `buildPreviewChannelRouteAuditEvent` 纯模型测试。验证：`npm --prefix frontend run typecheck`、前端 unit `456` passed；浏览器预览 Codex EXPLAIN 后显示 `preview -> openai-compatible:deepseek`、`5 candidates / 2 filtered`，Claude EXPLAIN 后显示 `preview -> codex-api-key:deepseek-claude`、`3 candidates / 1 filtered`，控制台仅有既有 `favicon.ico` 404。同步更新 `legacy-routing-cleanup-v01.md`；当时剩余 selector shim 包含 `rateLimitPolicy` 兼容注册、普通 session affinity wrapper、公共 `RoutePolicy` 兼容 API 和 WebSocket request-boundary 特例，其中 `rateLimitPolicy` 已在后续收敛中删除。
- Account Routing Engine 继续收敛尾项：CLIProxyAPI fork 已接入 `AccountRouteGuardResultHook` 到默认 `coreauth.Manager`，真实执行器 `MarkResult` 的 401/429/408/5xx/timeout 会写入 `auth-error`、`upstream-rate-limit`、`upstream-error` transient guard source，成功结果只清 transient source，不清 `manual-disabled`。Codex / Claude 账号列表移除旧 allow/deny/fallback 主 UI，探测只带当前渠道顺序；旧字段保留为空作为 request policy 兼容层。dev sidecar 真实 upstream 冒烟通过：`/v1/models status=200 models=8`，`/v1/responses model=gpt-5.4 max_output_tokens=1 status=200 object=response id_present=true`，未输出任何 key/token/payload 明文。验证：前端 typecheck、前端 unit `454` passed、主仓 channel routing Go 测试、CLIProxyAPI 路由/guard/auth/openai 相关测试均通过。
- Account Routing Engine 启停与失败冷却实现落地：`internal/wailsapp/channel_routing.go` 新增 `StickyAccountID` explain 输入与 `MarkChannelRouteAccountResult` runtime state 持久化；禁用账号或运行态 block 会让 sticky 立即失效并 fallback，激活账号只进入下一轮候选池、不抢占当前 sticky；root `main.App` 与 `frontend/wailsjs` 已同步。验证：新增 sticky invalidation / activation non-preempt / cooldown persistence 单测通过，`go test . ./internal/wailsapp -run 'Test.*ChannelRouting|TestChannelRouteEvent|TestChannelRouteAccountResult'`、`go test ./internal/wailsapp`、`npm --prefix frontend run typecheck`、前端 unit `454` passed、CLIProxyAPI fork guard/failover 相关测试均通过。
- Account Routing Engine 启停语义确认：账号或有效组禁用立即生效，高于 session sticky、失败降级、retry 和 selector；sticky / pinned auth 必须失效，已有流式连接在最近可控边界断开。账号或有效组激活只重新进入可路由账号池，等待下一轮 route / retry 自然选择，不抢占当前 stream / sticky。失败冷却状态需要持久化，401/429/5xx/model-unavailable 等由 `ResultRecorder / MarkResult` 写入运行态或 guard source，后续真实 route 与 explain 读取同一状态，自动恢复只清对应 source，不清 `manual-disabled`。
- Codex 隔离 HOME 冒烟完成：按用户要求使用 `CODEX_HOME=/tmp/gettokens-codex-home-smoke.zaXxDu` 跑 `codex --version`、`codex --help`、`codex features list` 与 `codex features enable apply_patch_streaming_events`，确认 CLI 运行态和 `config.toml` 写入均落在隔离目录；真实 `/Users/linhey/.codex`、`auth.json`、`config.toml` 测试前后 `mtime/size` 一致。补跑 `go test ./internal/wailsapp -run 'TestResolveCodexHomePathUsesCODEXHOMEOverride|TestApplyRelayServiceConfigToLocal|TestGetLocalCodexAuthState'` 通过，覆盖 GetTokens local apply 的 `CODEX_HOME` override 与 Codex 本地 auth/config 写入语义。本轮未发送真实生成请求，未读取或输出真实 token。
- Account Routing Engine 最终验收已完成：重新核对 `go test . ./internal/wailsapp -run 'Test.*ChannelRouting|TestChannelRouteEvent'`、CLIProxyAPI fork 的 `go test ./internal/gettokensrouting ./sdk/cliproxy/auth ./internal/cmd ./internal/gettokenshooks` 与 `go test ./...`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit -- src/features/channel-routing/tests/channelRouting.test.mjs src/features/accounts/tests/accountInventoryBoundary.test.mjs`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`，都保持绿色；shadow/event ledger、Wails 绑定、前端工作台、启动 hook、engine seam 与核心回归已收口，本轮剩余的深层 legacy cleanup 与外部真实 upstream smoke 归为后续兼容项。
- Account Routing Engine 冒烟测试完成：复用现有 Wails dev / Vite 进程，自动化门禁保持绿色；Codex 路由工作台浏览器 EXPLAIN 命中 `5 candidates / 2 filtered`、`selected: openai-compatible:deepseek`、`snapshot: preview / policy: channel-routing-v1`；Claude 路由工作台浏览器 EXPLAIN 命中 `3 candidates / 1 filtered`、`selected: codex-api-key:deepseek-claude`、`snapshot: preview / policy: channel-routing-v1`。dev sidecar `http://127.0.0.1:18317/healthz` 返回 200，客户端 API key `/v1/models` 探针返回 `status=200 models=8`。本轮未发送真实生成请求，避免消耗外部 upstream 额度。截图：`docs-linhay/spaces/20260524-account-routing-engine/screenshots/20260525/codex/20260525-codex-account-routing-smoke-after-v01.png`、`docs-linhay/spaces/20260524-account-routing-engine/screenshots/20260525/claude/20260525-claude-account-routing-smoke-after-v01.png`。
- Account Routing Engine 生产启动 hook 已接入 `internal/cmd/run.go`：服务启动时通过 `buildGetTokensStartupHooks(configPath)` 统一安装 GetTokens route policy，`UsageStatisticsEnabled` 打开时再安装 usage attribution / rate-limit ledger；新增 `internal/cmd/run_test.go` 用真实 `Manager + Executor + RouteMetadata` 验证 route policy 已进入启动链路，并验证启用 usage 时会创建 attribution ledger。验证：`go test ./internal/cmd ./internal/gettokensrouting ./sdk/cliproxy/auth`、`go build ./...` 通过。
- Account Routing Engine sidecar 骨架已补到 `docs-linhay/references/CLIProxyAPI/internal/gettokensrouting/`：新增 `RouteContext / Policy / PolicyDecision / DecisionStep / RouteResult` 的最小引擎闭环，policy 执行顺序按 stage 固定，hard-filter 先于 request/sticky；`go test ./internal/gettokensrouting ./sdk/cliproxy/auth` 已通过。
- Account Routing Engine 三层模型确认：采用 `Account Inventory / Channel Routing / Routing Engine`。总账号池属于 Account Inventory，只管账号、账号组和资产状态；Codex / Claude 账号列表属于 Channel Routing，分别管理各自渠道请求顺序、`sequential/balanced/project`、项目绑定、fallback 和路由说明；sidecar Routing Engine 只读取渠道配置和账号池快照执行决策，不反写产品配置。组级启停分为 `inventoryGroup.enabled`（全局组状态，影响所有渠道）和 `channelGroup.enabled`（渠道组状态，只影响当前渠道）。项目模式只限定目标账号组或账号，命中组后仍按 `sequential` 或 `balanced` 选择账号。
- Account Routing Engine 前端范围确认：可以整页重做 `CodexAccountListFeature` 与 `ClaudeCodeAccountListFeature`，将其升级为渠道路由工作台；`AccountsFeature` 不整页推翻，但必须移除 `AccountRotationModal` / `useAccountRotation` 轮动编排入口。新建共享 `features/channel-routing/` 承载纯模型、校验、preview 数据和共享组件；Codex / Claude 只复用组件，不共用配置。旧 `UpdateAccountPriority` 不能继续表达渠道顺序，旧 `allow/deny/order/fallback` 仅保留为请求级兼容 policy。
- Account Routing Engine space 已整理到 implementation-ready：新增 `plans/implementation-readiness-v01.md` 作为实施入口，首轮执行红灯测试与模型骨架，顺序为 sidecar seam、旧逻辑清理、三模式路由、Wails API、前端重做、shadow/观测。同步更新 Codex / Claude account-list skills，明确渠道顺序不再写全局 `UpdateAccountPriority`，三模式仅 `sequential/balanced/project`，上游兼容模式只进入兼容边界。
- Account Routing Engine 批次 1 实施完成：新增 `frontend/src/features/channel-routing/model/channelRouting.ts` 和对应单测，锁定 `sequential/balanced/project` 三模式、上游兼容模式过滤、项目模式组内 fallback 限制和 Codex/Claude 配置隔离；移除 `AccountsFeature` 的 `AccountRotationModal` 主入口和 `useAccountsPageState` 的 rotation modal state，并新增 `accountInventoryBoundary.test.mjs`。验证：`npm --prefix frontend run test:unit -- src/features/channel-routing/tests/channelRouting.test.mjs src/features/accounts/tests/accountInventoryBoundary.test.mjs` 通过（实际 450 tests passed），`npm --prefix frontend run typecheck` 通过，`npm --prefix frontend run build` 通过；浏览器冒烟确认账号池页面无“轮动/rotation”入口，截图落位 `docs-linhay/spaces/20260524-account-routing-engine/screenshots/20260525/accounts/20260525-accounts-inventory-no-rotation-after-v01.png`。
- Account Routing Engine 批次 2 实施完成：CLIProxyAPI fork 中 `sdk/cliproxy/auth/route_policy.go` 的 route policy snapshot 改为按注册 ID 稳定排序，新增注册顺序测试和“deny 不能被后续 allow/order 放回”测试，作为 hard guard 优先级基线。验证：`go test ./sdk/cliproxy/auth -run 'TestRoutePolicySnapshotPreservesRegistrationOrder|TestSchedulerRoutePolicyDenyCannotBeBypassedByLaterAllow|TestSchedulerRoutePolicyOrdersReadyCandidates|TestSchedulerRoutePolicyCannotBypassCooldown|TestSchedulerRoutePolicyStrictAllow'` 通过，`go test ./internal/gettokenshooks -run 'TestAccountRouteGuard|TestRoute|TestRateLimitPolicy|TestRateLimitEvaluator'` 通过，`go test ./sdk/cliproxy/auth ./internal/gettokenshooks` 通过。
- Settings Release Panel 重设计：`frontend/src/features/settings/components/SettingsReleasePanel.tsx` 改为信息矩阵 + 右侧操作区，并为当前版本、最新 release、GetTokens Git Hash、CLIProxyAPI Git Hash 增加 GitHub 外链入口。URL 构造逻辑收敛到 `settingsRelease.ts`，占位 hash（`DEV`、`—`）不显示外链；浏览器预览下外链走 `window.open`，Wails 桌面下走 `BrowserOpenURL`。验证：`npm run test:unit`、`npm run typecheck`、`npm run build` 通过；Playwright 预览截图落位 `docs-linhay/screenshots/20260525/settings/20260525-settings-release-panel-after-v02.png`。375px 预览暴露的是既有固定侧边栏桌面壳宽度问题，不作为本次 release panel 阻塞。
- Codex `Feature 配置` 页面完成配置项语义分组与控件收敛：root 按启动默认、模型输出、权限沙箱、工作区文档、工具集成、高级兼容分组；features 按推荐稳定、实验性、高级、兼容旧项分组；notice 按安全提示、迁移提示、结构化 notice 分组；model providers 按 provider id 分组。分组只影响展示层，不改变 draft、preview、save 的字段路径和保留式 TOML patch 语义。固定 enum options 已统一渲染为 `SegmentedControl`，bool / boolean 继续使用 `ToggleSwitch`，并按本地 Codex 源码 `7d47056ea4` 的 `codex-rs/core/config.schema.json` 校准 `approvals_reviewer`、`personality`、`model_auto_compact_token_limit_scope`、`model_providers.*.wire_api` 等选项；布尔型缺省假值不会被误显示成 `false` 枚举项。验证：`go test ./internal/wailsapp -run 'TestGetCodexFeatureConfigReturnsTypedRootDefinitionsAndValues|TestGetCodexFeatureConfigReturnsAllModelProviderSchemaFields'`、`node --test frontend/src/features/status/tests/codexFeatureConfig.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过；浏览器确认 `http://127.0.0.1:34115/#frame=codex` 已出现实际非空分组标题，且 DOM 命中 `SegmentedControl: 16`、`ToggleSwitch: 79`、`selectCount: 0`。
- 2026-05-25 的 Account Routing Engine 补充实现已落地 shadow/event ledger：`internal/wailsapp/channel_routing.go` 新增 `shadowEnabled` / `shadowRouteMode`、`ListChannelRouteEvents`、`snapshotVersion`、`policyVersion` 和安全 route event ledger；前端 `ChannelRoutingWorkbench` 已显示 shadow 开关、shadow 选择和 snapshot/policy 摘要；Codex / Claude 浏览器预览截图已落位 `docs-linhay/spaces/20260524-account-routing-engine/screenshots/20260525/codex/20260525-codex-account-routing-shadow-after-v01.png` 与 `docs-linhay/spaces/20260524-account-routing-engine/screenshots/20260525/claude/20260525-claude-account-routing-shadow-after-v01.png`。验证：`go test . ./internal/wailsapp -run 'Test.*ChannelRouting|TestChannelRouteEvent'`、`go test ./...`、`go test ./...`（CLIProxyAPI fork）、`npm --prefix frontend run test:unit -- src/features/channel-routing/tests/channelRouting.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过。当前仍未完成真实 upstream 请求冒烟和最后的 legacy path 收敛。
- 新开 `docs-linhay/spaces/20260525-account-cloud-sync/`，规划账号资产上云端数据库同步、更新、共享与冲突合并。当前决策：同步范围限定在 Account Inventory，不同步 Channel Routing、usage、quota 结果、rate-limit ledger 或 live sessions；默认技术方向已按用户补充调整为 `CloudAccountSyncAdapter` 抽象 + `PsqlAccountSyncAdapter`，云端 PostgreSQL 只保存端到端加密 payload、最小非敏感索引、cursor、ACL 和共享 envelope。冲突策略为字段级自动合并低风险字段，secret、删除/恢复、共享权限冲突必须用户确认。
- 账号云端同步场景模拟补齐：新增 `docs-linhay/spaces/20260525-account-cloud-sync/plans/scenario-simulation-v01.md`，明确账号 `expired` 是本机健康诊断，不应单独作为云端资产 patch；只有重新登录、替换 API Key 或刷新 credential 才生成 secret revision。设备 A 过期而设备 B 已刷新时，A 应 pull 云端较新 secret 并清理过期诊断；并发刷新 secret 必须进入冲突队列。shared-in 账号在接收方过期时不能写回污染所有者资产，`use-only` 只能等待所有者更新，`copy` 后才可生成自己的独立账号。
- 账号云端同步场景模拟扩展到 28 个场景，新增 Keychain/device key 丢失、schema 新旧客户端兼容、重复 API Key 双端新增、auth-file 重命名、模型映射与 base URL 并发修改、禁用意图与 secret 更新合并、共享 copy 脱钩、权限升级、撤销后重授予、PostgreSQL 部分失败、云端损坏、设备时间漂移、冲突另存副本、多个共享来源、账号组与账号乱序到达、关闭同步后重新开启等边界。新增结论：上传必须使用 base revision / logical clock / per-row result；`updatedAt` 只能辅助展示，不能作为唯一冲突裁决依据。
- 用户明确账号云同步后端是可本地直接配置连接的 PostgreSQL/psql 数据库，并且同步模式可选单向或双向。已将 `20260525-account-cloud-sync` 默认方案从 CloudKit 调整为 `PsqlAccountSyncAdapter`：本地配置 host/port/database/schema/user/password/SSL，连接密码或 URI 存 Keychain；支持 `upload-only`、`download-only`、`bidirectional`。新增边界：直连 PostgreSQL 下 `use-only` 只能由 ACL、客户端策略和审计约束，若要密码学强不可导出，需要另设请求代理服务端。
- 账号云端同步继续补齐边界测试矩阵：新增 `docs-linhay/spaces/20260525-account-cloud-sync/plans/boundary-test-matrix-v01.md`，按纯模型、加密/Keychain、PostgreSQL adapter、同步模式、共享 ACL、前端/Wails 和安全红线分层。首轮红灯建议覆盖 expired 不生成 patch、secret 并发冲突、secret 不入云端明文、PostgreSQL base revision CAS、upload-only remote-newer 阻止覆盖、download-only 不 push、bidirectional pull-merge-push、read-only 无 envelope、连接 profile 脱敏。
- 账号云端同步依赖规划补齐：首选 `github.com/jackc/pgx/v5` 做 PostgreSQL driver/pool，migration 候选 `github.com/jackc/tern/v2/migrate`，adapter mock 候选 `github.com/pashagolub/pgxmock/v4`，可选集成测试用 `github.com/testcontainers/testcontainers-go`。首期不引入 ORM，前端不直连 PostgreSQL；Keychain 优先 macOS Security framework/本地封装，连接密码或 URI 必须进 Keychain 而不是普通配置。
- Codex live sessions 请求信息补齐完成：新建 `docs-linhay/spaces/20260525-codex-live-session-request-info/`；按用户确认不兼容旧 sidecar，`projectName` 主来源改到 CLIProxyAPI fork 的 live tracker。sidecar 在生成 snapshot 时扫描 `CODEX_HOME || ~/.codex` 下 `sessions/` 与 `archived_sessions/` 的 Codex JSONL，从 `session_meta.id`、conversation/window/session key、`turn_context.cwd`、`session_meta.cwd` 或 git repo 反查项目名并返回 `projectName`，lookup 加 10 秒 TTL；GetTokens Wails 侧只透传 sidecar 字段，已移除本地兼容反查层。详情页 `请求时间线` 从事件行改为请求监控信息，展示 request/client/upstream id、账号、provider、transport、耗时、速率、token、错误和关键事件摘要。验证：CLIProxyAPI fork `go test ./internal/gettokenshooks -run LiveSessions`、GetTokens `go test ./internal/wailsapp -run CodexLive`、`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run typecheck` 通过；浏览器预览截图已归档到 `docs-linhay/spaces/20260525-codex-live-session-request-info/screenshots/20260525/codex-live-sessions/20260525-codex-live-sessions-request-info-after-v01.png`。
- 账号异常详情 fallback 修复：根因是 sidecar / Wails 内部账号记录已有 `StatusMessage`，但 root `main.AccountRecord` DTO 和 `mapAccountRecord` 未透传，前端账号详情只能显示“当前账号状态异常，但 sidecar 未返回具体原因”。已补 `app_types.go`、`app_mappers.go`、`frontend/wailsjs/go/models.ts` 及回归测试；验证：先用新增测试确认红灯，再跑 `go test ./...`、`npm --prefix frontend run typecheck`、账号相关前端 unit（456 passed）通过。
- Codex `#frame=codex` 功能配置页补齐页面级搜索过滤：顶部新增统一 SearchInput，同一个 query 会过滤 Root Settings、Model Providers、Features 与 Notices；Features 区保留 stage filter 但移除重复局部搜索框。底层 `selectCodexFeatureRows` 已用单测覆盖跨 section query；额外为 Codex 账号列表请求顺序区补了非持久化搜索过滤，覆盖 label/id/base URL/model mapping 与现有筛选 AND 组合。验证：`node --test src/features/status/tests/codexFeatureConfig.test.mjs`、`node --test src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过；Playwright CLI 验证 `http://localhost:5173/#frame=codex` 顶部搜索框可输入并显示清空按钮。
- Codex `#frame=codex` 浏览器预览补齐配置数据：`frontend/src/features/status/api/codexFeatures.ts` 在 HTTP/browser 环境缺少 Wails Codex feature 绑定时返回 browser preview snapshot，覆盖 root settings、model providers、features 与 notices，并让 preview/save 在内存态生效，不读取或写入真实 `~/.codex/config.toml`。验证：`node --test src/features/status/tests/codexFeatureConfig.test.mjs`、`npm --prefix frontend run typecheck` 通过；Playwright CLI 刷新 `http://localhost:5173/#frame=codex` 后确认 `Codex Root Settings 5 可见`、`Codex Model Providers 4 可见`、`Codex Features 4/4 可见`、`Codex Notices 2 可见`，搜索 `relay` 后只保留匹配的 provider 行。
