# 2026-04-27

## Sparkle Bundle Version Sync
- 已定位 macOS 原生更新弹框显示 `GetTokens 1.0.0 is currently the newest version available.` 的根因：设置页读取的是 `main.Version`，但 Sparkle 读取的是 app bundle `Info.plist` 里的 `CFBundleShortVersionString` / `CFBundleVersion`，而 Wails 默认产物未随 release tag 同步，仍可能保留默认 `1.0.0`。
- 已新增 `scripts/sync-macos-bundle-version.sh`，并接入 `.github/workflows/release.yml` 的 macOS 构建链，在 `wails build` 后、签名公证前把 bundle 版本同步为真实 release tag 的语义版本，如 `v0.1.10 -> 0.1.10`。
- 设置页当前版本与最新版本显示已统一走 `formatAppVersion()`，去掉语义版本前缀 `v`，避免与 Sparkle 原生弹框继续肉眼不一致。
- 本轮验证通过：`go test ./...`、`node --test frontend/src/utils/version.test.mjs frontend/src/features/settings/settingsRelease.test.mjs`。

## Skills Context Budget Governance
- 已处理 CLI `Exceeded skills context budget` 告警的项目侧成因：`AGENTS.md` 里存在 3 个不存在的项目级 skill 引用，会放大治理入口噪音并误导后续 skill 选择。
- 当前决策是继续维持“少量核心 skill”策略，不新增 skill，而是把 `space` / 文档写回 / AGENTS 同步统一收敛到 `gettokens-ops-governance`，把“整理”入口收敛到 `gettokens-session-skill-distill`。
- `.agents/skills/*.md` 的 frontmatter `description` 已统一压缩为短描述；后续新增或修改项目级 skill 时，`description` 只保留触发场景与能力边界，细节写正文。

## Account Metrics Split
- 已新建 `docs-linhay/spaces/20260427-account-metrics-split/`，用于沉淀账号卡与详情页的指标分层方案。
- 当前决策已收紧：`AccountCard` 只回答“能不能用 / 值不值得选 / 要不要处理”，保留状态、失败原因、套餐、额度与单一稳定性摘要；`successRate` 和趋势条二选一，不同时堆叠。
- `AccountDetailModal` 承接 `success / failure` 原始计数、`latency`、请求/响应、状态码、时间戳和原始调试信息；`latency` 不作为首批卡片指标。

## Settings Release Page Open Fix
- `frontend/src/features/settings/SettingsFeature.tsx` 的手动更新流程已修复：`CheckUpdate` 返回的新版本信息不再丢失 `releaseUrl`，点击“打开发布页”会把完整 release 页面 URL 传给 `BrowserOpenURL`。
- 回归根因是前端在 `trackRequest(... mapSuccess ...)` 里把 `ReleaseInfo` 裁剪成只剩 `version` 和 `assetName`，导致手动更新场景实际拿到的是缺失 `releaseUrl` 的对象。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`。

## Account Rotation Routing Defaults
- `frontend/src/features/accounts/components/AccountRotationModal.tsx` 现已在轮动配置弹窗中显式展示默认值，避免用户误判缺省行为。
- 当前展示规则收敛到 `frontend/src/features/accounts/model/accountRotation.ts`：`strategy` 默认 `round-robin`，`sessionAffinityTTL` 默认 `1h`，三个重试数值默认 `0`，四个布尔开关默认 `关闭`。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`。

## Account Rotation Modal
- 账号轮动设置已从 `StatusFeature` 主编辑区迁到账号池入口：`AccountsHeader` 新增“轮动设置”操作，打开 `AccountRotationModal` 后统一处理 relay routing config 编辑与账号顺序调整。
- 边界更新：账号主列表 `frontend/src/features/accounts/AccountsFeature.tsx:97` 的现有展示逻辑不参与拖动排序；统一排序只在 modal 中处理。
- OAuth `auth-file` 账号已纳入统一轮动：GetTokens 现在会读取并写回 auth file 顶层 `priority`，并通过统一的 `UpdateAccountPriority` 同时覆盖 `auth-file` 与 `api-key` 资产。
- 重新登录回填链路已保留旧 `priority`，避免 OAuth 账号 reauth 后掉出既有轮动顺序。
- 状态页中的轮动区已退回只读展示，避免继续把 `StatusFeature` 扩成配置中心。
- 新增后端 priority 读写测试与前端拖动纯函数测试；本轮验证通过：`go test ./internal/accounts ./internal/wailsapp`、`npm run test:unit`、`npm run typecheck`、`npm run build`。

## VS Code Sidecar Ensure
- `scripts/wails-cli.sh` 现已在 `wails dev` / `wails build` 之前自动执行 `scripts/ensure-sidecar.sh`，开发时点击 `.vscode` 运行会先检查 `cli-proxy-api` 是否需要重编。
- 当前 sidecar 指纹使用 `<short-commit>:<dirty|clean>:<goos>:<goarch>`，元数据写入 `build/bin/cli-proxy-api.meta.json`；二进制或 meta 缺失，或者指纹不一致时，都会重新执行 `scripts/build-sidecar.sh`。
- 本轮手工验证通过：首次执行 `./scripts/ensure-sidecar.sh darwin arm64` 会重编 sidecar，再次执行会命中 `up to date` 并跳过重编。

## Account Rotation Wails Binding Fix
- 账号轮动 modal 的 `UpdateAccountPriority` 前端绑定缺失不是后端实现问题，而是 `main.App` 没有暴露同名 wrapper，导致 Wails 没有生成对应的 `wailsjs` 导出。
- 已补 `app.go` 的 `UpdateAccountPriority` wrapper，并重新生成 `frontend/wailsjs`；`npm run build` 已通过。

## Session Distillation Skill
- `AGENTS.md` 里“用户说整理 => 触发会话沉淀”这条规则已补上对应的项目级 skill：`.agents/skills/gettokens-session-skill-distill`。
- 这次把“整理”明确成固定流程：先抽可复用模式，再判断是否补 skill、写 docs/memory，最后执行 `qmd update` 与 `qmd embed`。
- 项目级 skills 总览文档也已同步，避免规则只写在 AGENTS 里、实际入口却缺失。

## Accounts Toolbar Filters
- 账号池工具栏已从三段式来源按钮改为单一“显示”菜单，当前筛选状态拆分为 `source`、`hasLongestQuota`、`errorsOnly` 三个维度，并持久化到本地存储。
- “仅显示有最长窗口额度”的语义已固定：只对 `auth-file + codex` 生效；单窗口直接使用该窗口，多窗口优先取 `weekly / *-weekly`，否则回退到最后一个展示窗口；只有 `remainingPercent > 0` 才保留。
- “仅显示异常账号”的语义已固定：命中 `disabled === true`、`rawAuthFile.unavailable === true`，以及 `status` 不属于 `ACTIVE / CONFIGURED / LOCAL` 的账号。
- 本轮文档已补到 `docs-linhay/spaces/account-pool/plans/20260427-accounts-toolbar-filters-plan-v01.md`。
- 本轮验证通过：`cd frontend && npm run test:unit`、`./docs-linhay/scripts/check-docs.sh`。
- 当前仍存在与本次改动无关的前端全量类型检查阻塞：`frontend/src/features/status/StatusFeature.tsx` 缺少 `addRelayServiceAPIKey` 与 `renameRelayServiceAPIKey` 两个符号，导致 `npm run typecheck` 失败。

## Session Distillation Result
- 本次“整理”没有新增 repo-wide 长期规则，因此未更新 `AGENTS.md`。
- 已把可复用模式沉淀到项目级领域 skill：`.agents/skills/gettokens-domain-engineering/SKILL.md`，重点补充了账号池筛选状态拆分与最长窗口额度筛选语义。
- 已把同一套边界补写到 `docs-linhay/dev/20260425-account-pool-codex-api-key-architecture.md`，作为后续账号池筛选与 quota 展示的技术事实源。
- 本次未纳入沉淀的内容：单次提交号、菜单具体视觉细节、临时 typecheck 阻塞本身。

## DeepSeek Provider Support
- 已新建 `docs-linhay/spaces/20260427-deepseek-provider-support/`，用于沉淀“DeepSeek 是否可接入”与“其他厂商接入范围”的需求边界。
- 当前决策已明确分层：`GetTokens` 主产品层现在不支持直接新增 `DeepSeek` 账号；产品里唯一闭环的 API Key 录入能力仍是 `Codex API Key`，OAuth 增强链路也主要绑定 `codex`。
- 参考 `CLIProxyAPI` 的底层能力明显更宽：执行器目录已出现 `codex / claude / gemini / gemini-cli / vertex / kimi / aistudio / antigravity / openai-compatible` 等家族；`DeepSeek` 当前更接近通过 `openai-compatible` 槽位承载，而不是现成的产品级账号类型。

## OpenAI-Compatible Debate
- 已与 Gemini 视角子 agent 完成一轮合作型讨论，辩论文档落在 `docs-linhay/spaces/20260427-deepseek-provider-support/debate/20260427/accounts/20260427-openai-compatible-provider-support-v01.md`。
- 当前共识：`openai-compatible` 不应直接复用“单 API Key 账号”心智；更合理的产品与协议边界是“provider 容器 + key entries + models”。
- 推荐落地顺序也已收敛：先补 `internal/cliproxyapi` 与 `internal/wailsapp` 的 openai-compatible bridge，再做 provider 级最小表单和展示；不要先做 `CreateDeepSeekAPIKey` 这种厂商特例。

## Account Pool Full IA
- 账号池需求已补到完全体：左侧点击 `账号池` 后，需要在父级下展开两个正式子菜单 `codex / openai-compatible`。
- `codex` 子菜单继续承接 ChatGPT OAuth auth-file、Codex API Key、quota、reauth、rotation 等既有闭环；`openai-compatible` 子菜单明确改成 provider 级工作区，不再复用单条 `Codex API Key` 交互心智。
- 相关需求已同步写入 `docs-linhay/spaces/account-pool/README.md` 与 `docs-linhay/spaces/20260427-deepseek-provider-support/README.md`。

## OpenAI-Compatible Consensus
- 与 Gemini 视角达成一致：如果现在要进入实现，需求文档还必须先补 4 条，优先级依次为：
  1. 子菜单导航状态恢复规则
  2. `openai-compatible provider` 的稳定标识与唯一性
  3. `openai-compatible` 是否独立列表模型
  4. `openai-compatible` 的空状态与默认 CTA
- 子路由形态、迁移策略、测试矩阵、共性/差异矩阵被一致判断为“重要但可后置”的事项。

## Provider Verify Boundary
- 新增一条稳定边界：GetTokens 当前缺的不是“API Key 卡片上的验证按钮”，而是“provider 配置验证链路”。
- 当前 `codex api key` 只有本地持久化与同步 sidecar 的资产管理，没有 verify/test 接口；现有 `quota` 外呼桥接依赖 `auth_index + wham/usage`，不适合作为通用 API key/provider 验证链路。
- 需求归属已收敛到 `account-pool -> openai-compatible` 子菜单：正式验证能力应以 provider 为对象，最小入参至少是 `baseUrl / apiKey / headers? / model`，前端至少落 `idle/loading/success/error + 错误消息`。
- 第一阶段实现已经落地最小闭环：侧边栏 `账号池` 下新增 `codex / openai-compatible` 子菜单；`openai-compatible` 工作区已具备 provider 列表、新增、删除和 `/chat/completions` 验证能力。
- 这轮实现把 `verify` 的 `model` 从“建议支持”收敛成“显式必填”，避免默认模型不稳定导致验证结果失真；对应后端桥接新增 `List/Create/Delete/VerifyOpenAICompatibleProvider`，前端保持独立 provider 列表模型，不回落到 `AccountRecord`。
- 如果先在 `codex api key` 上补最小验证，只能视为过渡方案，不能代表最终信息架构。

## Status Relay Config Session
- `StatusFeature` 这轮已固定一条边界：服务真实状态与本地 UI 偏好必须分层。relay key / endpoint / sidecar uptime 属于后端事实源；relay key 别名、局域网地址显示开关、model 名称列表与当前选中 model 只属于当前桌面端本地偏好。
- sidecar 真实 uptime 不再允许由前端用 `Date.now()` 本地起表推导；已改为由 `internal/sidecar/manager.go` 在进程成功启动后写入 `sidecar.Status.startedAtUnix`，前端刷新页面后不会重置运行时长。
- 状态页当前的“局域网已开启 / 已关闭”只是 endpoint 暴露层开关，不是 sidecar 监听地址或 remote-management 的真实配置入口；这类状态允许本地持久化，但交付时必须明确其边界。
- `model` 名称列表当前被定义为 snippet 工作台偏好，而不是 sidecar 模型注册表；允许在状态页本地新增、删除、选择并持久化，但不应伪装成后端正式配置。
- 本次沉淀写回到 `docs-linhay/dev/20260426-relay-service-config-boundary.md`；没有形成新的 repo-wide 长期治理规则，因此未升级 `AGENTS.md`，也未新增项目级 skill。
- 发布命名调整：对外 release 资产从工程化的 `GetTokens_darwin_arm64.*` / `GetTokens_darwin_amd64.*` 改为用户可读的 `GetTokens_macOS_AppleSilicon.*` / `GetTokens_macOS_Intel.*`；内部构建参数与更新逻辑继续使用 `arm64/amd64`。
- Sparkle 接入推进：已建立 `20260427-macos-sparkle-updater` space，补齐架构文档，并新增 Sparkle plist 注入、官方 xcframework 下载、app bundle framework 嵌入脚本；release workflow 现可通过 `SPARKLE_ENABLE=1` 启用实验链路，但运行时 bridge 仍未真正接管更新入口。
- Sparkle 接入推进（二）：`internal/sparkle` 已改为动态加载 `Sparkle.framework` 的 darwin bridge，`wailsapp` 与设置页已能识别原生更新 UI 模式；当前 macOS 更新入口可切到 Sparkle 实验路径，但还缺 appcast 与真实升级回归。
- Sparkle 接入推进（三）：新增 `scripts/generate-sparkle-appcast.sh` 与对应测试，release workflow 现在可在 `SPARKLE_ENABLE=1` 时用 Sparkle 官方 `generate_appcast` 基于 notarized `.dmg` 生成签名后的 `appcast.xml`，并推送到 `sparkle-appcast` 分支供 `raw.githubusercontent.com` 托管稳定 feed。
- Sparkle 正式 release 首次回归（`v0.1.7`）失败点已定位：仅 `macOS amd64` 在 `Embed Sparkle framework` 阶段拿到损坏的 Sparkle zip（约 55 KB HTML/错误页），不是 framework 集成本身失败；已把 `scripts/prepare-sparkle-framework.sh` 改成 `curl --fail --retry --retry-all-errors` 并在下载后执行 `unzip -tq` 校验，不合法则重试。
- Sparkle 正式 release 第二次回归（`v0.1.8`）失败点已定位：`Publish Sparkle appcast` 不能把 `arm64` 与 `amd64` 两个同版本 DMG 放进同一个 appcast，Sparkle 会判定为 duplicate updates；发布链已改成分架构 feed，分别生成 `appcast-arm64.xml` 与 `appcast-amd64.xml`，构建时按目标架构写入对应 `SUFeedURL`。
- Sparkle 正式 release 第三次回归（`v0.1.9`）失败点已定位：`generate_appcast` 实际把输出写到了 `-o` 指定路径，但脚本仍按旧假设去临时目录里 `cp` staged appcast，导致 `Publish Sparkle appcast` 因 `No such file or directory` 失败；已在 `scripts/generate-sparkle-appcast.sh` 中把输出改成显式写入 staged 绝对路径，并补了测试。
- Sparkle 正式 release 第四次回归（`v0.1.10`，run `24979794016`）已全绿完成：两条 macOS 构建、分架构 appcast 发布和 GitHub Release 均成功，正式资产为 `GetTokens_macOS_AppleSilicon.*`、`GetTokens_macOS_Intel.*` 与 `checksums.txt`，`sparkle-appcast` 分支已落地 `appcast-arm64.xml` / `appcast-amd64.xml`。

## Wails Window Drag Region
- 当前窗口拖不动的根因已确认：`main.go` 启用了 `mac.TitleBarHiddenInset()`，但前端此前没有任何 `--wails-draggable` 区域，导致 macOS 将整个壳层视为普通 Web 内容。
- 本轮修复在 `frontend/src/App.tsx` 顶部新增独立拖拽条 `WINDOW_DRAG_REGION`，避免把整个页面标成 drag 区后与按钮、输入框、滚动容器争抢事件。
- Wails 拖拽样式已收口到 `frontend/src/utils/windowDrag.ts`，并补了 `frontend/src/utils/windowDrag.test.mjs` 作为回归测试；技术边界文档写入 `docs-linhay/dev/20260427-wails-window-drag-region-boundary.md`。
- 本轮验证通过：`cd frontend && npm run test:unit`、`cd frontend && npm run typecheck`。

## Native Titlebar Rollback
- 后续继续尝试自绘状态栏后，结论改为回退这条路线：当前视觉语言下，自绘顶栏很容易变成“把 Sidebar 碎片搬上去”的拼接感，收益不值得维护成本。
- 现阶段改回 macOS 原生标题栏更稳：`main.go` 移除 `mac.TitleBarHiddenInset()`，前端恢复到原来的 Sidebar 品牌块 + 导航 + 版本结构，不再维护 `--wails-draggable` 工具层。
- 本轮已删除 `frontend/src/utils/windowDrag.ts` 与对应测试，验证通过：`cd frontend && npm run typecheck`、`cd frontend && npm run test:unit`、`go test ./...`。

## Session Distillation Porting Doc
- 已新增 `docs-linhay/dev/20260427-session-distillation-trigger-and-porting-guide.md`，明确说明“用户说整理”在 GetTokens 中会触发的标准动作，不再把它理解为普通聊天总结。
- 当前固定流程已文档化为：先抽可复用模式，再按 `skill -> docs/memory -> AGENTS` 的顺序判断沉淀层级，最后执行 `qmd update` 与 `qmd embed`。
- 该文档额外补了跨项目移植最小要求，可直接复用到其他仓库的治理规则设计。

## CLIProxyAPI Fork Sync
- 本地 `docs-linhay/references/CLIProxyAPI` remote 已调整为 `origin=AxApp fork`、`linhay=legacy fork backup`、`upstream=router-for-me/CLIProxyAPI`；当前 `AxApp/CLIProxyAPI` 已成为正式主维护 fork。
- 问题跟进已完成：通过 `gh` / SSH / HTTPS 三路核验，确认最初的 `Repository not found` 不是凭证故障，而是 `AxApp` 组织下当时确实不存在 `CLIProxyAPI` 仓库；随后已直接将原个人 fork 迁入 `AxApp/CLIProxyAPI`，并验证 fork 关系建立成功。
- 上游同步已完成：`gettokens/wham-token-fix` 从 `upstream/main@2c626efc (v6.9.38)` 合并到 `736ae61e (v6.9.39)`，本地 merge commit 为 `a12fbeae`；同时已将 `origin/main` 推进到 `736ae61e`，`origin/gettokens/wham-token-fix` 推进到 `a12fbeae`。
- 本轮最小回归通过：`go test ./internal/api/handlers/management ./internal/runtime/executor ./sdk/cliproxy/...`。

## Account Detail Provider Scope
- `ApiKeyDetailModal` 的最终口径已收敛为“统一账号池里的 detail shell + 单条 API key 资产详情语义”：它可以复用壳层，但不是通用 provider setting。
- 当前 detail modal 必须显式展示 `provider` 归属；本轮已把标题、`provider config` 区块、`configuration workspace` 标题和明文风险提示都收口成带 `{provider}` 的文案。
- 正式验证能力继续归属 `openai-compatible provider workspace / provider detail`，不下沉到 `ApiKeyDetailModal`。

## OpenAI-Compatible Provider Detail Flow
- `openai-compatible` 第一阶段工作流已从“provider 卡片 + 临时验证输入”升级为“provider card 摘要 + detail modal + 保存/验证”。
- 当前 detail modal 已支持修改 `name`、`baseUrl`、首个 `apiKey entry`，并通过新的 `UpdateOpenAICompatibleProvider` Wails/Go 链路回写 sidecar 配置。
- provider 验证仍然是 provider-scoped：在 detail modal 中输入 `model` 后发起验证，验证对象是当前 draft 配置，而不是 `Codex API Key` 资产详情。
- 应用级回归已补一层：`./scripts/wails-cli.sh build` 成功生成并签名 `build/bin/GetTokens.app`；随后直接启动 `build/bin/GetTokens.app/Contents/MacOS/GetTokens`，进程在 smoke 窗口内稳定驻留，`Ctrl+C` 可正常触发 `Shutting down...`，未出现启动即崩溃。

## OpenAI-Compatible Provider Advanced Edit
- 第二阶段已补齐 `headers` 与多 `apiKey entries` 编辑能力：detail modal 现在按 provider 草稿整体编辑，不再只覆盖首个 key。
- `UpdateOpenAICompatibleProvider` 现支持整包写回 `apiKeys[] + headers`；保存时会去空、去重，并尽量保留既有 entry 的 `proxy-url` 对位信息。
- provider 验证仍取当前 draft 的首个可用 key，并带上当前 draft 的 `headers` 发起请求，保持“验证对象是 provider 配置”这个边界不变。
- 当前 detail modal 也已补齐 `models / alias` 编辑区，并把已配置模型回流成验证模型快捷选择；`verifyModel` 默认优先取最近一次验证模型，否则回退到首个已配置模型。

## Codex API Key Transitional Verify
- `ApiKeyDetailModal` 已按用户确认补了一个 `codex-only` 的过渡验证区，不再坚持“这里只能纯展示不验证”的旧边界。
- 当前实现复用现有 `VerifyOpenAICompatibleProvider` bridge，但输入面严格收敛在单条 `Codex API Key` 资产的 `apiKey + baseUrl + model`，不伪装成通用 provider setting。
- 页面会显示最近一次验证状态与时间；当测试模型为空时，直接在 modal 内提示用户补齐测试模型。

## OpenAI-Compatible Provider Presets
- `OpenAICompatibleComposeModal` 新增了基于 Cherry Studio API 厂商配置抽取的 preset 选择器，当前先覆盖 `deepseek / siliconflow / zhipu / moonshot / dashscope / openrouter / groq / together / doubao / openai`。
- 这轮没有直接照搬 Cherry Studio 的 `apiHost` 原值，而是按 GetTokens 当前 provider 验证链路会自动拼接 `/chat/completions` 的事实，先把预设 `baseUrl` 适配成可直接验证的路径形态，例如 `.../v1`、`.../openai/v1`、`.../api/paas/v4`。
- preset 目前只负责填充创建弹窗里的 `name/baseUrl` 和 `api key placeholder`，不改后端 provider 数据结构，也不引入新的 repo-wide 治理规则。
- `OpenAICompatibleDetailModal` 的验证区也已接入同一份 preset：现在不仅能回填 `name/baseUrl/api key placeholder`，还会覆盖当前 draft 的推荐 `models[]`，并把 `verifyModel` 默认切到该厂商的首个推荐模型。
- 后续已按用户纠正收口：detail modal 不再提供 provider 选择器；验证区只基于当前 provider 自动推断推荐模型，并把 `verifyModel` 默认显示为该厂商的首个推荐模型。
