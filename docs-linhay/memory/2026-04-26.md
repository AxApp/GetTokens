# 2026-04-26

## Codex OAuth Bridge & Reauth
- 账号池已补齐 `nolon/chatgpt(codex)` 最小 OAuth bridge：Wails 新增 `StartCodexOAuth`、`GetOAuthStatus`、`FinalizeCodexOAuth`，前端可从 Header 发起 `ChatGPT 登录`，也可从失败态 `codex` 卡片触发 `重新登录`。
- 本轮明确了一条稳定边界：GetTokens 不引入 Web 管理后台式全局登录态；`codex` 账号过期只在账号池内局部修复，不做全局 `401 -> logout -> route redirect`。
- 重新登录成功后的默认行为不是保留一新一旧两个账号，而是把新的 `codex` auth 内容回填到原文件名，再删除临时生成的新 auth file，避免账号池里残留重复资产。
- 本轮验证通过：`go test ./internal/wailsapp ./internal/cliproxyapi`、`npm run typecheck`、`npm run test:unit`。

## Sidebar Version Label
- 新增 `ReleaseLabel` 构建注入字段，保留 `Version` 给自动更新比较使用，避免界面发布日期格式影响更新逻辑。
- `frontend/src/utils/version.ts` 将 Sidebar 版本展示统一格式化为 `YYYY.MM.DD.HH`。
- `Sidebar` 底部文案改为 `VERSION`，并展示格式化后的发布日期标签。
- Release 工作流在构建阶段按 `Asia/Shanghai` 时区注入 `ReleaseLabel`。

## Release Prep & Auto Update
- 首发版本建议采用 `v0.1.0`：当前仓库无历史 tag，且前端版本号已是 `0.1.0`。
- 自动升级继续沿用 `go-selfupdate`，不重写更新框架；本轮补的是 release 资产分层、universal 选择配置和设置页入口。
- GitHub Release workflow 现在同时产出下载安装包与 updater 原始资产：安装包给用户下载，`.tar.gz` 原始二进制给自更新框架消费。
- Windows 安装包重命名为 `GetTokens_windows_amd64_installer.exe`，避免被 `go-selfupdate` 误选。
- Settings 页面新增检查更新入口；Windows / Linux 保持 `ApplyUpdate`，macOS 改为打开 release 页面安装，避免对已签名 `.app` 做 bundle 内原地替换。
- `internal/updater` 增加显式版本比较 helper，修复“同版本 release 也会被误报为可更新”的假阳性问题。
- macOS release workflow 调整为“先签名并公证 `.app`，再生成并公证 DMG”；不能先打 DMG 再签 app，否则 DMG 内仍然是未签名 app。

## Release v0.1.0 Published
- GitHub 仓库已落位到 `AxApp/GetTokens`，首个正式 release 已于 `2026-04-26` 发布：`https://github.com/AxApp/GetTokens/releases/tag/v0.1.0`。
- `v0.1.0` 发布资产已齐备：`GetTokens_darwin_universal.dmg`、`GetTokens_darwin_universal.tar.gz`、`GetTokens_linux_amd64.AppImage`、`GetTokens_linux_amd64.tar.gz`、`GetTokens_windows_amd64_installer.exe`、`GetTokens_windows_amd64.tar.gz`、`checksums.txt`。
- 自动升级链路现已打通：Windows / Linux 使用 `go-selfupdate` 消费 `.tar.gz` 原始资产，macOS 保持“检查更新后打开 release 页面下载已签名公证的 DMG”。
- 为稳定 release workflow，额外补了三处 CI 修复：Windows runner 安装 `NSIS`、sidecar 拉取增加重试与 GitHub token 认证、`latest` tag 解析改为 JSON 解析。
- 当前 release workflow 仍有两个非阻塞告警待后续治理：GitHub Actions Node 20 deprecation，以及 `docs-linhay/references/CLIProxyAPI` 相关的 `.gitmodules` 清理警告。
- 自动升级按钮回归验证时额外发现两个事实：`app.go` 中的 `GitHubRepo` 曾错误指向旧仓库 `linhay/GetTokens`，已修正为 `AxApp/GetTokens`；但 `AxApp/GetTokens` 当前是 private repo，匿名访问 `https://api.github.com/repos/AxApp/GetTokens/releases/latest` 返回 `404`，因此终端用户环境下 `go-selfupdate` 仍然会把它判成“无可用更新”。
- 后续已将 `AxApp/GetTokens` 切为 public，并完成真实 updater 检查验证：`CheckUpdate` 能命中 `v0.1.0` 与当前平台资产。

## Session Distill: Release Source & Card Click
- 会话整理结论：自动升级存在一个稳定边界，`GitHubRepo` 必须与真实发布仓库一致，且终端用户默认 update source 不能使用 private GitHub repo。
- 账号卡片存在一个稳定交互边界：整张卡应该支持进入详情，但按钮、复选框等嵌套交互控件不能触发卡片级打开详情。
- 上述两条规则已同步沉淀到项目 skill `gettokens-domain-engineering`，并新增研发文档 `docs-linhay/dev/20260426-updater-release-source-and-card-click-boundary.md`。
- 本轮不更新 `AGENTS.md`，因为这些规则属于 GetTokens 项目级实现边界，不是 repo-wide 治理约束。

## Accounts Domain Boundary Refresh
- `AccountsPage` 已正式拆成页面装配层，重型 UI 与状态逻辑下沉到 `frontend/src/features/accounts/` 和 `useAccountsPageState.ts`；后续账号池迭代不要再回到单文件大页。
- 统一账号池仍以 `AccountRecord` 为唯一前端模型；`provider` 和 `credentialSource` 继续分离表达。
- `codex api key` 的事实源固定为本地目录 `~/.config/gettokens-data/codex-api-keys/`，而不是 `auth-dir` 或 sidecar `config.yaml`。

## Codex Quota Reset Time Rule
- 相对倒计时必须使用原始 unix 秒字段 `resetAtUnix` 计算，`resetLabel` 只做展示。
- 直接反解析 `resetLabel` 会因为秒级精度丢失导致临近重置时误显示 `0s`。
- 这条规则已经在 Go 额度窗口 DTO 和前端 quota helper 上收敛，并补了测试。
- 会话整理结论：该边界已上升为项目级可复用模式，已同步写入 `gettokens-domain-engineering` 和 `docs-linhay/dev/20260426-release-label-version-boundary.md`；不需要更新 `AGENTS.md`。

## Provider Config Setup
- 新建 `docs-linhay/spaces/20260426-provider-config-setup/README.md`，将“API Key 复制 / 供应商配置片段 / 一键复制配置 / 缺失值补填”收敛为独立需求空间。
- `ApiKeyDetailModal` 新增配置工作台：支持分别复制 `API KEY`、`BASE URL`、`PREFIX`，并根据当前 provider 生成配置片段，一键复制整段配置。
- 缺失字段不阻塞面板使用，允许用户先在详情面板补填后再复制配置；当前实现为前端工作台，不扩展为完整 provider 管理后台。
- `StatusPage` 补充“后端中转服务配置”卡片，直接给出指向本地 sidecar 端口的最小 `auth.json` 示例，仅保留 `auth_mode`、`OPENAI_API_KEY`、`base_url` 三个有用字段。
- 聚合服务自己的客户端 API KEY 与上游 `codex-api-key` 资产分离：状态页读取的是 sidecar 顶层 `api-keys`，并支持通过管理接口热更新保存，不再错误复用账号池中的上游供应商 API KEY。
- 聚合服务客户端 API KEY 现支持多值维护；状态页以“每行一个 key”的形式编辑并保存，同时可选择任意 key 生成配置预览。
- sidecar 默认绑定全接口而不是 `127.0.0.1`，状态页会同步展示 `localhost`、主机名和局域网 IP 形式的访问地址，方便局域网设备接入。
- 状态页新增“账号轮动策略”只读区，直接展示 sidecar 当前实际生效的 `routing / request-retry / quota-exceeded` 配置，避免把“系统支持哪些模式”和“当前正在使用哪种模式”混淆。
- 本轮验证完成：`npm run typecheck`、`npm run build`、`docs-linhay/scripts/check-docs.sh`；未运行额外自动化 UI/E2E 测试，因为前端仓库当前未接入对应测试基建。
- 会话整理结论：`relay service key` 与上游 provider key 的边界已上升为项目级可复用模式，已同步写入 `gettokens-domain-engineering` 和 `docs-linhay/dev/20260426-relay-service-config-boundary.md`；本轮不需要更新 `AGENTS.md`。

## Componentization Split Planning
- 基于大文件扫描结果新建 `docs-linhay/spaces/20260426-componentization-split/`，用于承载“组件化与业务拆分”计划。
- 当前第一优先级明确为前端 `accounts` 域，主要热点是 `useAccountsPageState.ts` 与 `helpers.ts`；第二优先级是 Go 侧 `codex_quota.go`、`sidecar manager.go` 与 `wailsapp` bridge 收口。
- 已新增计划文档 `docs-linhay/spaces/20260426-componentization-split/plans/20260426-componentization-split-plan-v01.md`，按 `accounts -> quota -> sidecar/wailsapp -> status/settings` 的顺序推进，避免前后端同时大改。
- 本轮仅完成结构规划和文档落位，未开始代码重构；自动化验证仅执行文档结构校验，未运行代码测试。

## Componentization Split Implementation Batch 1
- `accounts` 域第一批已实施：把 `helpers.ts` 中的纯逻辑拆成 `accountConfig.ts`、`accountPresentation.ts`、`accountQuota.ts`、`accountSelectors.ts` 四个模块。
- `useAccountsPageState.ts` 已改为通过 `buildAccountsView(...)` 统一生成 `accounts / filteredAccounts / groupedAccounts / selection` 派生视图，减少超级 hook 内联计算。
- 当前仍遵守 `AccountRecord` 作为统一前端模型，未改 Wails 接口契约，也未把页面路由层迁出 `pages/`。
- 前端新增轻量单测 `accountConfig.test.mjs` 与 `accountSelectors.test.mjs`，并在 `frontend/package.json` 中新增 `npm run test:unit`。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`、`npm run build`。

## Componentization Split Implementation Batch 2
- `accounts` 域第二批已实施：新增 `accountSelection.ts`、`useAccountsQuotaState.ts`、`accountTransfer.ts`，继续从 `useAccountsPageState.ts` 中剥离 selection、quota state 和导入导出辅助逻辑。
- 当前 `useAccountsPageState.ts` 仍是编排入口，但已不再直接承载“选择集切换逻辑 / quota 请求状态机 / 文件上传解析 / 粘贴导入命名规则 / 导出文件名拼装”等细节。
- 前端新增轻量单测 `accountSelection.test.mjs` 与 `accountTransfer.test.mjs`，`test:unit` 已扩展到 23 个用例。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`、`npm run build`。

## Componentization Split Implementation Batch 3
- `accounts` 域第三批已实施：新增 `useAccountsActions.ts`，将 delete / upload / create api key / paste import / export / rename 等 mutation 编排从 `useAccountsPageState.ts` 中外移。
- `types.ts` 新增共享 `TrackRequest` 类型，`useAccountsQuotaState.ts` 与 `useAccountsActions.ts` 统一复用，不再各自重复定义。
- `useAccountsPageState.ts` 已从上一批的 `507` 行下降到 `237` 行，当前主要负责状态装配、派生视图和子 hook 组合。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`、`npm run build`。

## Componentization Split Implementation Batch 4
- `accounts` 域第四批已实施：创建 `frontend/src/features/accounts/AccountsFeature.tsx`，将原 `pages/AccountsPage.tsx` 的装配逻辑迁入 feature 层。
- `frontend/src/pages/AccountsPage.tsx` 现在只剩 `10` 行路由包装代码，`pages/` 开始真正回归页面入口职责。
- `SettingsPage` 中 `PAGE_ACCOUNTS` 的调试源码映射已同步到 `src/features/accounts/AccountsFeature.tsx`。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`、`npm run build`。

## Componentization Split Implementation Batch 5
- `accounts` 域第五批已实施：将原 `frontend/src/pages/accounts/` 下的组件、hooks、纯逻辑模块和测试文件整体迁入 `frontend/src/features/accounts/`。
- 空目录 `frontend/src/pages/accounts/` 已删除，账号域前端目录级收口已完成。
- `StatusPage` 对账号域配置片段工具的导入已同步到 `../features/accounts/model/accountConfig`，`test:unit` 里的账号域测试路径也已全部切换。
- 当前 `features/accounts` 下共有 `25` 个文件；下一阶段若继续整理，应优先做二级目录分层，而不是继续沿用平铺结构。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`、`npm run build`。

## Componentization Split Implementation Batch 6
- `accounts` 域第六批已实施：`features/accounts` 内部已按 `components / hooks / model / tests` 完成二级分层，旧平铺结构已清理。
- `features/accounts/helpers.ts` 兼容层已删除，账号域代码不再保留过时导出入口。
- `Phase 1` 前端拆分已完成：路由包装层、feature 装配层、hooks、model、tests 均已归位。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`、`npm run build`。

## Account Detail Raw Copy
- `frontend/src/components/biz/AccountDetailModal.tsx` 的 `RAW_SOURCE_DATA` 展示区已支持点击复制与键盘触发复制，同时在标题区补了显式“复制原始内容”按钮和成功/失败反馈。
- 复制边界收敛到独立 helper：空白内容或加载中不触发复制，保留原始格式全文写入剪贴板，避免排障时丢换行或缩进。
- 前端补充单测 `src/components/biz/accountDetailClipboard.test.mjs`，验证“可复制时写入全文”“空内容不写入”。
- 本轮验证完成：`npm run test:unit`、`npm run typecheck`；后续如补组件级测试基建，可再把该交互升级为真实 DOM 事件回归。

## Auth File Minimal Sanitize
- `internal/accounts/auth_file_normalize.go` 现在对 `codex` legacy auth file 做“最小可用字段”清洗，而不是保留原始 payload 再补字段；落盘结果只保留 `type / access_token / id_token / refresh_token / account_id / email / plan_type`。
- 账号详情弹层 `frontend/src/components/biz/AccountDetailModal.tsx` 新增“清洗字段”按钮，支持在 `RAW_SOURCE_DATA` 与 `SANITIZED_SOURCE_DATA` 之间切换，并复制清洗后的结果。
- Wails 新增 `NormalizeAuthFileContent`，前端不自己拼清洗逻辑，直接复用后端统一归一化入口，保证“导入链路”和“详情页展示”看到的是同一套结果。
- 本轮验证通过：`go test ./internal/accounts ./internal/wailsapp ./internal/sidecar`、`npm run test:unit`、`npm run typecheck`、`npm run build`。

## Account Card Failure Reason
- `frontend/src/features/accounts/AccountCard.tsx` 现在会在失败态账号卡片标题下直接显示 `statusMessage`，避免用户必须进详情页才能知道为什么失效。
- `statusMessage` 已从后端 `internal/accounts/account_records.go` 一路透传到前端 `AccountRecord`，`auth-file` 卡片和列表态数据不再丢这个字段。
- 本轮验证通过：`go test ./internal/accounts ./internal/wailsapp ./internal/sidecar`、`npm run test:unit`、`npm run typecheck`、`npm run build`。
- 会话整理结论：`codex auth file 最小清洗` 与 `statusMessage 列表态直显` 已上升为项目级可复用边界，已同步写入 `gettokens-domain-engineering` 与 `docs-linhay/dev/20260426-auth-file-sanitize-and-status-boundary.md`；本轮不需要更新 `AGENTS.md`。

## Componentization Split Phase 2
- Go 侧 `internal/accounts/codex_quota.go` 已完成职责拆分，现已拆为 `quota_types.go`、`quota_auth_parser.go`、`quota_client.go`、`quota_builder.go`、`quota_debug.go` 五个文件。
- 本轮保持 `GetCodexQuota / ResolveCodexQuotaRequestInfo / BuildCodexQuotaResponse` 的外部调用面不变，只做内部职责收口，避免同时改动 Wails bridge 契约。
- 当前 `quota` 结构已经把“请求发起 / auth file 与 JWT 解析 / quota window 构建 / debug 脱敏”分离，后续新增额度规则时优先在 builder 层局部修改。
- 本轮验证通过：`go test ./internal/accounts`、`go test ./internal/accounts ./internal/wailsapp ./internal/sidecar`。

## Componentization Split Phase 3
- `internal/sidecar/manager.go` 已完成第一轮职责收口，端口选择、YAML 配置写入、二进制与健康检查辅助逻辑已分别下沉到 `port.go`、`config.go`、`process_support.go`。
- `internal/wailsapp/quota.go` 已收敛到 bridge 主流程，debug 事件发送与 auth file 查询辅助逻辑已拆到 `quota_debug.go` 和 `quota_support.go`。
- 本轮保持 `sidecar.Manager` 与 `wailsapp` 导出接口不变，先做结构性拆分，不同时改绑定契约。
- 本轮验证通过：`go test ./internal/sidecar ./internal/wailsapp ./internal/accounts`。

## Componentization Split Phase 4
- `StatusPage` 与 `SettingsPage` 已回归页面入口职责，主实现分别迁入 `frontend/src/features/status/StatusFeature.tsx` 与 `frontend/src/features/settings/SettingsFeature.tsx`。
- 当前 `pages/` 层已形成一致边界：`Accounts / Status / Settings` 三页都只负责路由包装，重型装配逻辑放在 `features/`。
- 本轮没有继续细拆 `StatusFeature` 内部卡片组件，先优先完成“页面装配层迁移”这一稳定边界。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`、`npm run build`。

## Session Distill: Componentization Split Workflow
- 本次“整理会话”已把组件化拆分流程沉淀到 `docs-linhay/dev/20260426-componentization-split-workflow.md`，覆盖前端 page->feature 迁移顺序和 Go 大文件拆分模板。
- `gettokens-domain-engineering` 已同步更新：
  - `AccountsPage` 边界从旧的 `pages/accounts` 规则切到 `features/accounts`
  - 新增 `Feature / Page Boundary`
  - 新增 `Go Large-File Split Heuristics`
- 本轮判断这些规则已稳定到项目级 skill / dev 文档，但还不需要上升为 `AGENTS.md` 的 repo-wide 硬约束。
- 发布流程纠偏：GetTokens 打包前的 sidecar 不能再通过 `scripts/fetch-sidecar.sh` 拉上游 release 二进制；必须先从 `docs-linhay/references/CLIProxyAPI` 的 fork 源码构建。
- 发布修复：新增 `scripts/build-sidecar.sh`，统一从本地 fork 源码构建 sidecar；`darwin/universal` 场景会先分别构建 `arm64/x86_64`，再用 `lipo` 合成为单一 universal `build/bin/cli-proxy-api`。
- 发布治理：`.github/workflows/release.yml` 已切换为源码构建 sidecar，不再依赖上游 `router-for-me/CLIProxyAPI` release 资产。
- 额外发现：Wails `darwin/universal` 打包后，`GetTokens.app/Contents/MacOS/cli-proxy-api` 会退回单架构副本；因此 workflow 需要在 `wails build` 之后显式把 `build/bin/cli-proxy-api` 覆盖回 app bundle，再进入签名与 DMG。
