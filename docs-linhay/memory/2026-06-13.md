# 2026-06-13

## v1.2.7 在线更新不可见归因
- 用户询问为什么在线更新更不到 `1.2.7`、CI 发布是否没过 Sparkle。核查结论：`v1.2.7` GitHub Release 不存在，`gh release view v1.2.7` 返回 `release not found`；远端 `sparkle-appcast` 分支的 `appcast-arm64.xml` 与 `appcast-amd64.xml` 最新条目仍是 `1.2.6`，因此 Sparkle 客户端不会看到 `1.2.7`。
- `v1.2.7` Release run `27399638655` 结论为 `failure`。`Build H5` 成功，但两个 macOS build job 在 `Ensure CLIProxyAPI sidecar from source` 失败，导致 `Publish Sparkle appcast` 与 `Publish Release` 均 skipped；失败发生在打包、签名、公证、Sparkle appcast 生成之前。
- 具体日志：arm64 在拉取 `AxApp/CLIProxyAPI.git#fef495dd3f5cd212a0e37f6ef37180492a5056ec` 时返回 `fatal: remote error: upload-pack: not our ref`；amd64 同 step 同 ref 后续遇到 `Could not resolve host: github.com`，且前置 Wails 安装阶段也出现 `proxy.golang.org` 依赖下载超时。
- 时间线：`v1.2.7` tag 指向 `43a1b418`，时间 `2026-06-12 14:47:18 +0800`；Release CI 优化提交 `90b4edab ci: parallelize release sidecar build` 在 `2026-06-12 16:18:14 +0800` 才进入 master，所以旧 tag 不包含 sidecar 独立构建和 release/Sparkle 并行化修复。后续发版需使用下一个 patch tag，不复用已消费失败的 `v1.2.7`。

## v1.2.9 发版完成
- 用户要求直接发版。本轮先按 release governance 将前端版本三件套从 `1.2.7` 递增到 `1.2.8`，本地门禁 `go test ./...`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 均通过；`v1.2.8` tag 指向 `674deacc chore: bump version to 1.2.8`。
- `v1.2.8` Release run `27431265119` 失败，根因仍是 `docs-linhay/references/CLIProxyAPI` gitlink 指向的 `fef495dd3f5cd212a0e37f6ef37180492a5056ec` 只存在本地 `gettokens/sidecar`，尚未推到 `AxApp/CLIProxyAPI.git` 远端，CI 拉取时报 `fatal: remote error: upload-pack: not our ref`。该 tag 已被失败 run 消费，未复用。
- 已在 reference repo 补推 sidecar：`git -C docs-linhay/references/CLIProxyAPI push origin gettokens/sidecar`，远端 `refs/heads/gettokens/sidecar` 更新到 `fef495dd3f5cd212a0e37f6ef37180492a5056ec`；随后用 `git ls-remote https://github.com/AxApp/CLIProxyAPI.git refs/heads/gettokens/sidecar` 验证远端可达。
- 正式改发 `v1.2.9`。版本三件套为 `frontend/package.json`、`frontend/package-lock.json`、`frontend/package.json.md5`，其中 `frontend/package.json.md5=0615a108f18c196d2fd5bdeda393042a`。本地门禁再次通过：`go test ./...`、`npm --prefix frontend run test:unit`（829 tests）、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`（仅 Vite chunk size warning）、`docs-linhay/scripts/check-docs.sh`、`git diff --check`。
- release commit/tag：`305ce465 chore: bump version to 1.2.9` / `v1.2.9`。Release workflow `27431447245` 全绿，包含 `Build H5`、`Build CLIProxyAPI sidecar (darwin, arm64)`、`Build CLIProxyAPI sidecar (darwin, amd64)`、`Build macOS arm64`、`Build macOS amd64`、`Publish Release`、`Publish Sparkle appcast`。workflow URL：`https://github.com/AxApp/GetTokens/actions/runs/27431447245`。
- GitHub Release 已发布：`https://github.com/AxApp/GetTokens/releases/tag/v1.2.9`。七个资产齐全：`GetTokens_macOS_AppleSilicon.dmg`、`GetTokens_macOS_AppleSilicon.tar.gz`、`GetTokens_darwin_arm64.tar.gz`、`GetTokens_macOS_Intel.dmg`、`GetTokens_macOS_Intel.tar.gz`、`GetTokens_darwin_amd64.tar.gz`、`checksums.txt`。
- Sparkle appcast 已发布到 `origin/sparkle-appcast`：`appcast-arm64.xml` 顶部版本为 `1.2.9`，指向 `GetTokens_macOS_AppleSilicon.dmg`；`appcast-amd64.xml` 顶部版本为 `1.2.9`，指向 `GetTokens_macOS_Intel.dmg`。因此在线更新检测目标已从 `1.2.6` 前进到 `1.2.9`。
- 官方 Release 资产下载验收完成：`shasum -a 256 -c checksums.txt` 全部 OK；两个 DMG `spctl -a -t open --context context:primary-signature -v` accepted；两个 DMG `xcrun stapler validate` 成功；挂载后的两个 `.app` 均通过 `spctl -a -vvv -t execute`，来源为 `Notarized Developer ID`，签名链为 `Developer ID Application: HAN LIN (3L8RM3MDLS)`。
- 官方包内元数据验收：Apple Silicon 可执行文件为 `Mach-O 64-bit executable arm64`，Intel 可执行文件为 `Mach-O 64-bit executable x86_64`；两包 `CFBundleShortVersionString` 与 `CFBundleVersion` 均为 `1.2.9`；`SUFeedURL` 分别为 `https://raw.githubusercontent.com/AxApp/GetTokens/sparkle-appcast/appcast-arm64.xml` 与 `https://raw.githubusercontent.com/AxApp/GetTokens/sparkle-appcast/appcast-amd64.xml`；两包 `cli-proxy-api.meta.json` 的 `commit` 均为 `fef495dd3f5cd212a0e37f6ef37180492a5056ec`。
- 沉淀审计：本轮发现“本地 reference repo 存在 sidecar commit，但远端 gettokens/sidecar 不可达，会导致 Release CI `upload-pack: not our ref`”是后续发版会重复遇到的单领域门禁，已补入 `.agents/skills/gettokens-release-governance/SKILL.md` 的 tag 前确认步骤；不升级 `AGENTS.md`，因为这是 release 领域专用流程，不是 repo-wide 通用硬约束。
