# 2026-06-07

- Codex API Key / OpenAI-compatible 账号详情保存后重新进入显示旧值的根因定位为前端账号列表缓存未同步：后端保存成功后只更新详情态或只依赖 `ListAccounts()`，当 reload 延迟或失败时，重新从旧账号卡片对象打开详情会回显旧 `apiKey / baseUrl / prefix`。修复规则：保存成功后先按稳定 `account.id` patch `derivedAuthFileRecords`、`apiKeyRecords` 与 `selectedAccount`，再执行 `ListAccounts()` 作为最终刷新。
- `acct_*` 是 account-store/SQLite 的稳定账号 key，不由 `apiKey / baseUrl / prefix` 合成；编辑这些字段不得重建或替换 `acct_*`。本轮已把该治理规则沉淀到 `.agents/skills/gettokens-domain-engineering/SKILL.md`，范围限定为账号详情 mutation 的本地缓存一致性，不升级 `AGENTS.md`。
- 本轮先补红灯测试再实现：新增 `patchAccountDetailByID` 纯函数回归，以及 API Key rename / priority / config、OpenAI-compatible config 保存路径必须先 `patchAccountLocally` 再 reload 的源结构测试。
