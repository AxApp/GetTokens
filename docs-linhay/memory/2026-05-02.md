# 2026-05-02 内存记录

## 核心进展
### 1. 前端组件化治理与架构优化
完成了一轮大规模的前端重构，重点解决了代码膨胀和职责耦合问题，严格遵循 `20260426-componentization-split-workflow.md` 规范。

- **DebugPage 迁移**：将 `DebugPage.tsx` 逻辑迁移至 `features/debug/`，拆分为 `DebugHeader`、`DebugEntryCard`、`DebugEmptyState` 组件，并提取了 `useDebugFeature` Hook。
- **UsageDeskWorkspace 彻底拆分**：
  - 将 1400+ 行的 `UsageDeskWorkspace.tsx` 重命名为 `UsageDeskFeature.tsx`。
  - 提取 `useUsageDeskFeature` Hook，收拢了数据加载、事件订阅和派生状态逻辑。
  - 拆分出 `UsageDeskChart`、`UsageDetailTable`、`UsageDeskPanels` 等子组件。
- **App Shell 瘦身**：
  - 提取 `useAppBootstrap` Hook，处理版本获取、Sidecar 状态监控等初始化逻辑。
  - 提取 `useAppNavigation` Hook，接管 Hash 路由同步与页面状态持久化。
  - `App.tsx` 现已收缩为纯粹的布局与 Provider 容器。
- **AccountRotationModal 优化**：提取了 `useAccountRotation` Hook，并拆分出 `RotationPriorityItem` 和 `RotationConfigSection` 子组件。

## 技术决策
- **Hook 优先**：优先提取重型业务 Hook 以实现逻辑与视图的分离，使 `*Feature.tsx` 文件保持在 300 行以内的可读规模。
- **子组件目录化**：对于重型 Feature，在 `components/` 下建立二级目录（如 `usage-desk/`）来存放仅在该 Feature 内部使用的组件。

## 验收结果
- **类型检查**：`npm run typecheck` 通过。
- **单元测试**：187 项单元测试全部通过。
- **性能/加载**：通过 `lazy` 加载保持了较好的首屏性能。

### 2. Codex API key 卡片重复与删除修复
修复账号池 Codex API key 模式中“添加后出现两张卡片”和“最后重复卡片无法删除”的问题。

- 新建 space：`docs-linhay/spaces/20260502-codex-api-key-card-delete/README.md`。
- 决策：Codex API key 的前端展示/选择继续使用稳定 `local-id`，但合并、查重、删除、重命名、优先级和禁用操作都要同时接受稳定 ID 与配置派生 ID。
- 修复：`mergeCodexAPIKeyInputs` 同时按记录 ID 与 `apiKey + baseUrl + prefix` 配置身份去重，避免 sidecar 无 `local-id` 的镜像记录被展示为第二张卡。
- 修复：`DeleteCodexAPIKey` 等变更入口通过配置派生 ID 兜底匹配本地稳定记录，确保点到重复派生卡时也能删除真实本地记录并同步 sidecar。
- 验收：新增 `TestMergeCodexAPIKeyInputsDeduplicatesStoredStableIDAndSidecarMirror` 与 `TestDeleteCodexAPIKeyAcceptsDerivedConfigIDForStableLocalRecord`，并通过 `go test ./internal/wailsapp ./internal/accounts`、`go test ./...`。

### 3. 模型列表按大小排序
调整模型下拉与 relay model catalog 的排序规则：不同模型族保持字母序，同一模型族按数字版本从大到小排列，标准版优先于 `mini / lite / nano` 等小型号。

- 示例：`gpt-5.5`、`gpt-5.4`、`gpt-5.4-mini`、`gpt-5.3-codex`、`gpt-5.2`、`gpt-4.1`。
- 后端：`ListRelaySupportedModels` 聚合后的模型名称使用语义排序，保证 Status 页和其他 Wails 调用方拿到稳定顺序。
- 前端：`resolveAPIKeyModelMenuNames` 对账号详情模型下拉做同样排序兜底，避免传入列表顺序抖动。
- 验收：新增 `TestListRelaySupportedModelsSortsModelFamilyFromLargeToSmall`，更新 `apiKeyModelCatalog.test.mjs`；`go test ./...`、`npm run test:unit -- src/features/accounts/tests/apiKeyModelCatalog.test.mjs`、`npm run typecheck` 通过。

### 4. 模型下拉与 rotation 拖拽 review 修复
针对 review 提出的 3 个回归点完成修复：

- API key 详情弹窗的模型菜单区分 browse/filter 两种模式：点击下拉或获焦展示完整 relay model catalog，用户输入后才按查询过滤。
- `AccountsFeature` 的 relay model catalog 会在 OpenAI-compatible provider catalog 变化和打开 API key 详情时重新拉取，不再只依赖首次 `ready` 切换。
- `RotationPriorityItem` 补回 `onDragEnd` 清理拖拽状态，`handleDrop` drop 到自身时也会清空 `draggedAccountID`。
- 新增 `apiKeyModelCatalog.test.mjs` 锁定模型菜单浏览/过滤和 provider catalog 签名变化。
- 验收：`npm run test:unit` 190 项通过，`npm run typecheck` 通过，`go test ./...` 通过；`npm run build` 已完成产物输出，但 Vite 进程未自然退出，已手动结束残留 build 进程。

### 5. 账号卡右上角操作菜单
将账号池 `AccountCard` 的危险删除动作从底部按钮区移到右上角垂直三点菜单，并补齐复制能力。

- 新建 space：`docs-linhay/spaces/20260502-account-card-actions-menu/README.md`。
- UI：卡片右上角新增三点菜单，菜单项为 `复制 / 复制内容 / 删除`；菜单按钮和菜单项不会触发整卡详情跳转。
- 行为：`复制` 写入账号主标识，`复制内容` 写入 `gettokens.account-card.v1` 结构化账号 JSON，`删除` 复用现有 `pendingDeleteID` 二次确认流程。
- 粘贴导入：`gettokens.account-card.v1` 支持重新创建账号卡；Codex API key payload 走 `CreateCodexAPIKey`，auth-file payload 在携带原始内容时走 `UploadAuthFiles`。粘贴弹窗文案已从 Auth-only 调整为账号内容导入，并继续兼容裸 Auth File JSON。
- 调整：非删除确认态的卡片底部操作区移除删除按钮，只保留详情、刷新额度、重新授权等常规动作。
- 验收：新增 `accountCardActions` 纯函数与测试，`npm run test:unit -- src/features/accounts/tests/accountCardInteractions.test.mjs`、`npm run typecheck`、`git diff --check` 通过。

### 6. Codex 本地 config.toml 快捷配置方案
新建 space：`docs-linhay/spaces/20260502-codex-config-toml-settings/README.md`，用于推进 Codex 本地 `config.toml` 快捷配置能力。

- 方案边界：该能力定位为“本地 Codex 配置工作台”，事实源是 `CODEX_HOME/config.toml`，默认 `~/.codex/config.toml`。
- 决策：前端只维护结构化 draft，不直接拼 TOML；后端提供读取、预览 diff、保存，并复用现有保留式 patch 语义。
- 安全约束：保存时只修改受控字段，保留未知字段、注释、换行风格、MCP section、agents section 与 provider 未知键；无法安全 patch 时拒绝写入。
- 产物：`docs-linhay/spaces/20260502-codex-config-toml-settings/plans/20260502-codex-config-toml-settings-plan-v01.md`。

补充：一期范围已按用户要求收窄为只支持 `[features]` 下的 bool feature 开关；官方 Codex 参考源码已归档到 `docs-linhay/references/codex/`，当前为 `openai/codex` main HEAD `35aaa5d9fcb606fb6f27dd5747ecab3f4ba0c07e`。新增 v02 方案：`docs-linhay/spaces/20260502-codex-config-toml-settings/plans/20260502-codex-config-toml-settings-plan-v02.md`。

补充设计稿：新增并修订 `docs-linhay/spaces/20260502-codex-config-toml-settings/design-preview.html`。初稿偏离项目视觉规范后，已收回到 GetTokens 现有 Swiss-industrial 体系：白底/浅灰 surface、2px 黑边、`card-swiss`/`btn-swiss` 式投影、黑白主色与红色风险强调，围绕 `[features]` bool 开关、stage 分组、legacy alias 提示和保存前 diff/TOML preview 展示一期交互方案。

补充调整：按反馈移除设计稿标题下方 4 个概览卡片，让页面直接进入 feature switchboard 与 patch preview 主工作区。

补充调整：按反馈移除右侧 preview 列，不再展示 diff/TOML 预览；页面收敛为单列 feature switchboard，底部仅保留最小写入边界说明与 Reset/Save 操作。

补充调整：按反馈重新设计 feature 开关样式，改为 Swiss-industrial 矩形拨档；ON/OFF 文案内置于控件，黑底 ON 与浅底 OFF 区分状态，并保留固定尺寸、硬投影和键盘焦点态。随后继续调整为默认值直接反映在开关上：toggle 底部显示 `DEF TRUE/FALSE`，当前值偏离默认值时默认基线用红色强调，左侧 metadata 不再重复展示 `default true/false`。

补充调整：按反馈将 feature 配置项改为项目内表格样式；每个配置项对应单独一条表格记录，桌面端使用表头 + 2px 分隔线 + 独立行，移动端按单条记录纵向展开字段，保留开关内的当前值与默认值表达。

补充调整：按反馈参考 UsageDesk/额度页的更简洁控制面板风格，重新收敛设计稿信息密度：配置路径、筛选和搜索合并为紧凑 control dock；推荐项与 legacy 项合并为一个薄表格，行分隔改为浅线，减少重复表头和重边框。

补充调整：按反馈将设计稿从居中 HTML 预览画板改为全屏应用工作台布局；移除 workspace 最大宽度限制，页面标题左对齐，主面板铺满视口宽度并撑满可用高度，保存条贴近底部。

补充实施：Codex 本地 `[features]` bool 快捷配置一期已落地。后端新增 `GetCodexFeatureConfig / PreviewCodexFeatureConfig / SaveCodexFeatureConfig`，根层 `main.App` 已补 wrapper 并生成 Wails bindings；保存只 patch `CODEX_HOME/config.toml` 的 `[features]` bool key，保留注释、未知字段、复合 `[features.*]` table 与换行风格。前端新增全宽 `Codex Features` 表格工作区，移除顶部 4 个概览卡片，每个 feature 独立一行，switch 直接呈现当前 ON/OFF 与默认 `true/false`。验证通过：`go test ./...`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`（203 项）、`docs-linhay/scripts/check-docs.sh`、`./scripts/wails-cli.sh build`；并完成构建产物桌面启动 smoke 与实现截图 `docs-linhay/spaces/20260502-codex-config-toml-settings/screenshots/20260502/codex-config/20260502-codex-config-status-table-after-v01.png`。

补充导航调整：按用户要求新增一级菜单 `Codex`，并把本期 `config.toml` bool feature 配置作为二级菜单 `Feature 配置`；前端新增 `CodexWorkspace = feature-config`、`#frame=codex` hash/storage 持久化与 `CodexPage`，原状态页不再直接承载该配置面板。新增导航持久化回归后，`npm --prefix frontend run test:unit` 为 208 项通过；新导航截图已归档到 `docs-linhay/spaces/20260502-codex-config-toml-settings/screenshots/20260502/codex-config/20260502-codex-config-codex-menu-after-v01.png`。

补充修复：Codex Feature 配置表格全部显示“暂无描述”的根因是后端 `CodexFeatureDefinition` 未暴露 `description`，且前端 `definitions[]` 归一化路径也没有透传 description。已补齐后端 description 字段、root mapper、Wails bindings 和前端 normalization；experimental 项优先使用上游 `/experimental` menu description，其余项参考上游源码注释，legacy alias 自动生成 canonical key 提示。验证通过：`go test ./internal/wailsapp -run CodexFeatureConfig`、`node --test frontend/src/features/status/tests/codexFeatureConfig.test.mjs`、`go test ./...`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`（211 项）、`./scripts/wails-cli.sh build`。

补充导航调整：会话管理从一级菜单移入 `Codex` 一级菜单下，作为 `Feature 配置` 的并列二级项；新入口为 `#frame=codex&workspace=session-management`，旧 `#frame=session-management` 会兼容迁移到同一 Codex workspace。相关改动覆盖 `Sidebar`、`CodexPage`、`useAppNavigation` 与 `pagePersistence`，验证通过：`node --test frontend/src/utils/pagePersistence.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`（211 项）、`npm --prefix frontend run build`、`git diff --check`。

补充导航调整：用量工作台一级菜单移除，`Codex 用量` 移入 `Codex` 二级菜单，入口为 `#frame=codex&workspace=usage-codex`；Gemini 用量入口暂不暴露，旧 `#frame=usage-desk` 与 `#frame=usage-desk&workspace=gemini` 都兼容迁移到 `Codex 用量`。验证通过：`node --test frontend/src/utils/pagePersistence.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`（212 项）、`npm --prefix frontend run build`、`git diff --check`。

### 7. CLIProxyAPI 上游同步
已将 `docs-linhay/references/CLIProxyAPI` 的维护分支 `gettokens/wham-token-fix` 从 `upstream/main@79579c34` 合并到本地，并提交 GetTokens 兼容修复 `b83dfe56`。

- 上游跨度：合并后包含 `v6.10.0`，上游删除原 `internal/usage` logging/TUI usage 功能，并新增 Antigravity、image、API key usage 等能力。
- 兼容修复：恢复 GetTokens 自定义的 `internal/usage` 内存聚合器，供 `internal/gettokenshooks` 的 SQLite usage snapshot 持久化继续工作，不回滚上游删除的 TUI/management usage endpoint。
- 测试修复：Antigravity credits 测试改为跟随上游默认 `daily-cloudcode-pa.googleapis.com`；Codex registry 测试改为接受最新上游数据中 `gpt-5.5` 出现在 free/team/plus/pro tiers。
- 验收：`go test ./...` 在 CLIProxyAPI 仓库通过；已重建 `darwin/arm64` sidecar，`build/bin/cli-proxy-api` 与 `build/bin/GetTokens.app/Contents/MacOS/cli-proxy-api` SHA256 一致。
- sidecar 版本：`v6.10.0-6-gb83dfe56`，fingerprint 为 `b83dfe56:clean:darwin:arm64`。

### 8. Claude Code API key 模式一键配置研究
新建 space：`docs-linhay/spaces/20260502-claude-code-api-key-mode/README.md`，用于研究 Claude Code API key 模式的一键配置能力。

- 方案边界：目标文件默认是 `~/.claude/settings.json`，只写 `env` 下受控 API key 模式字段，不修改 `~/.claude.json`、MCP、permissions、hooks、statusLine 或其他未知配置。
- 前端目标：放在现有状态页本地 CLI 配置区域，并用横向选项卡区分 `Codex` / `Claude Code`；切换 tab 不应丢失各自未保存草稿。
- 组件约束：实现时必须复用项目通用组件与样式，包括 `SegmentedControl`、`btn-swiss`、`input-swiss`、`select-swiss`、Status 页已有 section/snippet 模式；设计稿不代表新增平行 UI 体系。
- 决策：默认使用 `env.ANTHROPIC_API_KEY` + `env.ANTHROPIC_BASE_URL`；检测到既有 `env.ANTHROPIC_AUTH_TOKEN` 时不静默删除，必须提示冲突或依赖 GetTokens 上次写入元数据确认 ownership。
- 安全约束：不能用整文件 JSON marshal/format 作为保存策略；后续实现应提供后端最小 JSON patch、preview diff、非法 JSON 拒绝写入和原子写入。
- 设计稿：新增 `docs-linhay/spaces/20260502-claude-code-api-key-mode/design-preview.html`，按 GetTokens 项目级 Swiss-industrial 视觉体系收敛；同一个 HTML 内包含 `Codex` / `Claude Code` 两个 tab，支持交互切换、输入更新和右侧文件 diff 实时刷新。
- Codex tab 交互补充：Relay API key 做成下拉选择并提供 `+` 弹框添加；Model / Provider 都使用下拉；Wire API 固定为 `responses`，不允许编辑。
- 产物：`docs-linhay/spaces/20260502-claude-code-api-key-mode/plans/20260502-claude-code-api-key-mode-research-v01.md`。

补充实施：Claude Code API key 模式一键配置已完成最小闭环。后端新增 `ApplyClaudeCodeAPIKeyConfigToLocal`，只写 `CLAUDE_CONFIG_DIR/settings.json` 或默认 `~/.claude/settings.json` 的 `env` 受控字段，保留 `permissions`、`hooks`、`statusLine`、未知顶层字段和非受控 env 字段；检测到 `ANTHROPIC_AUTH_TOKEN` 时保留并返回 warning/conflict。状态页本地 CLI 配置区已用 `SegmentedControl` 区分 `Codex / Claude Code`，右侧直接显示当前 tab 的 diff；Codex tab 的 Relay API key、Model、Provider 均为下拉，Wire API 固定 `responses`。补充修正：Claude Code tab 已接真实 Wails apply，不再是 placeholder；默认 model draft 为 `claude-sonnet-4-5`，diff 包含 `ANTHROPIC_MODEL`。验证通过：`go test ./...`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`（211 项）、`./scripts/wails-cli.sh build`。截图归档到 `docs-linhay/spaces/20260502-claude-code-api-key-mode/screenshots/20260502/status/`。

补充实现：状态页本地 CLI diff preview 不引入外部 diff UI 依赖，改为在 `relayLocalState` 提供轻量 unified diff 行级 tone classifier，并由通用 `StatusSnippetPanel` 渲染红绿增删行。成熟库调研结论：`react-diff-view` / `diff2html` 更适合完整 unified diff 解析与 GitHub-like 视图，`react-diff-viewer` 更偏 old/new 文本对比；当前场景只是短 patch preview，直接分类 `+` / `-` 行可减少依赖和视觉体系漂移。验证通过：`node --test frontend/src/features/status/tests/relayLocalState.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`（212 项）、`git diff --check`。截图：`docs-linhay/spaces/20260502-claude-code-api-key-mode/screenshots/20260502/status/20260502-status-local-cli-diff-colors-after-v01.png`。
