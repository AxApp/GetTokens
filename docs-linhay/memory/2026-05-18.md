# 2026-05-18

## Unified Account Cards 桌面验收闭环
- 根因 1：昨日 `wails dev` 无法完成桌面验收，直接原因是残留 `GetTokens.app` 进程继续监听 `127.0.0.1:34115`；清理旧 `wails dev`、桌面壳和孤儿 sidecar 后，端口冲突消失。
- 根因 2：更深层问题在 `internal/sidecar/profile.go`。现有逻辑只按可执行名识别 dev/profile，而 `wails dev` 产物名就是普通 `GetTokens`，被误判为 prod，导致 sidecar 只在 `.app/Contents/MacOS` / `Resources` 查找，找不到实际存在的 `build/bin/cli-proxy-api`。
- 修复：新增基于完整可执行路径的 dev 判定，命中 `.../build/bin/GetTokens.app/Contents/MacOS/GetTokens` 时强制走 dev profile；补充 `internal/sidecar` 回归测试。
- 验收：重启 `~/go/bin/wails dev` 后，`window.go.main.App.GetSidecarStatus()` 返回 `code=ready, port=18317`，真实 Wails 窗口中的账号池页面退出 skeleton，截图已归档到 `docs-linhay/spaces/20260517-unified-account-cards/screenshots/20260518/accounts/20260518-accounts-wails-window-after-v05.png`。

## Unified Account Cards 余额展示补齐
- 根因：`internal/wailsapp/quota.go` 已返回 `billing`，但 root Wails bridge `app.go` 的 `GetCodexQuota` / `TestCodexAPIKeyQuotaCurl` 没有透传 `billing`，前端详情页也没有余额区和 `billingCurl/billingEnabled` 编辑入口。
- 修复：新增 root mapper `mapCodexQuotaResponse` / `mapCodexQuotaBillingInfo`，补 `app_test.go` 回归测试；统一账号详情弹窗增加 `Balance` 区块、`billingCurl/billingEnabled` 编辑与 `Test Billing`，并把配置草稿状态收口到 modal 顶层，避免 quota/billing 编辑后无法保存。
- 验收：重启 `./scripts/wails-cli.sh dev` 后，`http://127.0.0.1:34115/#frame=accounts&detail=codex-api-key%3A05020af65051` 的列表卡片已显示 `BALANCE Total 121.58 CNY / Granted 0.00 CNY`，详情弹窗也能显示实时余额和 billing 编辑区；截图已归档到 `docs-linhay/spaces/20260517-unified-account-cards/screenshots/20260518/accounts/20260518-accounts-detail-billing-after-v02.png`。

## Codex Preserve ChatGPT Provider Mode 收口
- 这条线原先停在 `docs-linhay/spaces/20260517-codex-preserve-chatgpt-provider-mode/README.md` 的 `designed` 状态，但代码实际上已经落到 `internal/wailsapp/relay_local_apply.go`、`app.go`、`frontend/src/features/status/*`，只是没有做最终验收和文档收尾。
- 根改动：root Wails bridge 新增 `GetLocalCodexAuthState` 与 `ApplyRelayServiceConfigToLocalV2`；后端 preserve 模式会保留 `auth.json`，只更新 `config.toml`，写入 `experimental_bearer_token` 并删除冲突 `env_key`；前端状态页新增 `AUTH STRATEGY`、本地 auth 状态展示、阻断提示和 preserve-mode diff。
- 自动化验证通过：`go test ./internal/wailsapp`、`cd frontend && npm run typecheck`、`cd frontend && npm run test:unit -- --runInBand src/features/status/tests/relayLocalState.test.mjs`、`cd frontend && npm run build`。
- 真实运行页复验通过：重启 `./scripts/wails-cli.sh dev` 后，在 `http://127.0.0.1:34115/#frame=status` 上确认：
  - `GetLocalCodexAuthState()` 返回 `authMode=chatgpt`、`canPreserveChatGPTAuth=true`
  - provider=`openai` + preserve 模式会阻断并禁用“应用到 CODEX”
  - provider=`gettokens` + preserve 模式会放行，diff 中出现 `+experimental_bearer_token` 与 `-env_key = "OPENAI_API_KEY"`
- 截图已归档到 `docs-linhay/spaces/20260517-codex-preserve-chatgpt-provider-mode/screenshots/20260518/status/20260518-status-codex-preserve-chatgpt-auth-after-v01.png`。

## GetTokens v1.0.9 可分发 DMG 发布
- 发布链路：推送 `feat/20260515-rate-limit-middleware` 与 tag `v1.0.9` 后，GitHub Actions run `26009770548` 完成成功，release `v1.0.9` 已公开发布。
- 产物：release 包含 `GetTokens_macOS_AppleSilicon.dmg`、`GetTokens_macOS_Intel.dmg`、对应 `tar.gz` 与 `checksums.txt`。
- 校验：本地下载两个 DMG 后，sha256 与 `checksums.txt` 一致；`spctl` 均返回 `accepted / Notarized Developer ID`；`xcrun stapler validate` 均通过。
- 签名抽查：挂载两个 DMG 后确认内部 `GetTokens.app` 为 `CFBundleShortVersionString=1.0.9`、`CFBundleVersion=1.0.9`，签名链为 `Developer ID Application: HAN LIN (3L8RM3MDLS) -> Developer ID Certification Authority -> Apple Root CA`，并带 stapled notarization ticket。

## Release 文档收口
- 为避免后续重复踩坑，`docs-linhay/dev/20260426-release-prep-guide.md` 已从旧的首发准备说明升级为 macOS 可分发 DMG 发布手册，明确 tag/version、CI 契约、sidecar 构建、公证签名、Sparkle 分架构、发布后验收和失败处理边界。
- `README.md`、`docs-linhay/README.md` 和 `.agents/skills/gettokens-ops-governance/SKILL.md` 已同步入口与可分发验收口径：GitHub Release 资产存在不等于完成，必须下载正式资产并通过 checksum、`spctl`、`stapler validate`、`codesign`、架构、bundle version 与 `SUFeedURL` 检查后才能宣称“可分发 DMG 已发布”。
