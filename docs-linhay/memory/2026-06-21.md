# 2026-06-21

## 账号详情凭据布局修复

- 用户在浏览器评论中指出 `accounts` 详情页的“凭据与验证”区域布局混乱，复现 URL 为 `/#frame=accounts&detail=codex-api-key%3Astable-001`。
- 根因定位：`AccountCredentialVerifySection` 在 wide modal 内仍使用左右等分 `lg:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]`，实际单列约 300px；同时 `CredentialInputField` 使用 `max-w-sm`，连通验证模型输入和发送按钮在窄列中容易被挤压。
- 修复：左/右改为 `minmax(20rem,7fr) / minmax(16rem,5fr)`，字段控件改为全宽，连通验证行改为 `minmax(0,1fr)_auto` grid，并恢复左侧“账号凭据”标题与右侧“协议端点 / 留空使用默认基础 URL。”短说明。
- 验证：`node --test --test-name-pattern "api key credential module|credential endpoint|credential module edits|account detail verify copy" src/features/accounts/tests/accountDetailLayout.test.mjs` 通过；浏览器同 viewport 指标显示 section 608px、left 336px、right 256px、verify 320px，控件 overflow 为空；截图归档到 `docs-linhay/spaces/20260606-relay-vendor-support/screenshots/20260621/accounts/20260621-account-credential-layout-after-v01.png`。
- `npm run typecheck` 仍失败在既有 `AccountsFeature.tsx` 回调类型不一致（`onVerify`、OAuth probe、quota/billing test handler、script route 类型），不属于本轮布局改动。

## Session Skill Distillation

- 候选模式：账号详情凭据区在窄 modal 中应避免两等分和固定 `max-width` 控件；该模式已由 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Account credential endpoint boundary` 和实现测试覆盖。
- 决策：本轮不新增 skill、不更新 `AGENTS.md`；只记录到 memory 和对应 space，因为属于单次页面布局修复，没有形成新的 repo-wide 长期约束。

## 单运行态样式收敛

- 用户反馈桌面 App 和 Web 颜色不一致，并明确要求“只留一套样式”。根因链路是旧主题体系同时受 `system/dark/light`、`theme-preset`、各 runtime 独立 `localStorage`、Settings 主题控件和 Wails 透明/半透明窗口合成影响。
- 修复：运行态固定为 `light / classic`；`ThemeContext` 读取和写回旧 key 时归一到单一值；移除 Settings 的 theme mode/preset 控件与 `ThemePresetPicker`；AntD adapter 全局固定 neutral palette；`style.css` 删除 `.dark` 和 parchment token override；颜色 token story 改为只展示一套 token；Wails `main.go` 改为白底、不透明、非半透明窗口。
- 验证：`node --test src/context/ThemeContext.test.mjs src/context/antdTheme.test.mjs src/context/tokenContract.test.mjs src/features/settings/settingsLayout.test.mjs` 通过；`npm run typecheck` 通过；`npm run build` 通过；`go test -run '^$' .` 通过；`go test .` 通过；`./scripts/wails-cli.sh build` 生成 `build/bin/GetTokens.app/Contents/MacOS/GetTokens`。本地 `codesign --verify` 失败于未签名 build 产物中的 `cli-proxy-api.meta.json`，不作为本轮 dev build 门禁。
- 沉淀判断：这是前端运行态长期边界，已写入 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的固定边界；不升级 `AGENTS.md`。同时写回 `docs-linhay/spaces/20260519-theme-skinning/README.md` 和本 memory。

## 单运行态样式收敛阻塞处理

- 用户要求继续“处理阻塞”。已处理两个会卡住本轮交付的工程门禁：`npm run typecheck` 失败来自 `UnifiedAccountDetailModal` props 与 `AccountsFeature` 传参类型漂移，已补齐 callback 类型和 script hash route 的 null 归一；`go test .` 失败来自 `build/darwin/Info.plist` 缺失且 `Info.dev.plist` 仍依赖 Wails protocol 模板占位，已补齐 prod plist 并让 prod/dev 都静态注册 `gt`，禁止 `gt-dev`。
- 验证：`npm run typecheck` 通过；`go test .` 通过；`npm run build` 通过。
- 剩余红灯：`npm run test:unit` 仍失败在既有 Sidebar / Account Detail / Design System 源码契约漂移，集中表现为账号详情 v09 primitives、business component manifest 和若干 sidebar 源码正则断言不匹配；该失败簇不是单运行态样式收敛或上述阻塞修复引入，范围已超出本轮颜色一致性修复，应作为独立账号详情/设计系统契约收敛项处理。

## 提交后 test:unit 阻塞收敛

- 用户要求“提交，提交后继续处理”。已先提交单运行态样式收敛，提交为 `f117c1f2 fix: converge runtime theme style`；提交后继续处理此前记录的 `npm run test:unit` 红灯。
- 根因归类：Sidebar、Account Detail 和 Design System 的失败不是运行时颜色逻辑问题，而是源码契约测试仍断言旧账号详情 band/card 布局、旧 sidebar brand typography、旧 manifest 状态；另外 AccountDetail primitives 残留 `uppercase tracking-wider`，会触发 legacy style residue 门禁。
- 修复：将 Sidebar 字体显式接入 `--gt-font-family-sans`；账号详情布局测试改为当前 section-nav / split section 文件边界；Design System manifest 补齐 `AccountDetailLayout`、`AccountDetailAuthFileSection`、`AccountDetailModelsSection`；移除账号详情 stat/evidence label 的旧大写字距；rate limit 注入测试接受当前 `props.rateLimitRulesAPI` 透传形式。
- 验证：`node --test src/components/biz/sidebarState.test.mjs`、`node --test src/features/accounts/tests/accountDetailLayout.test.mjs`、`node --test src/features/design-system/storyCatalog.test.mjs`、`node --test src/features/accounts/tests/rateLimit.test.mjs src/features/design-system/legacyStyleResidue.test.mjs` 均通过；完整 `npm run test:unit` 通过（983 pass）。
- 沉淀判断：本轮属于既有前端设计契约门禁的测试同步和残留清理，已被 `gettokens-frontend-design-quality` 的 quiet workspace / style residue 边界覆盖；不新增 skill、不更新 `AGENTS.md`。
