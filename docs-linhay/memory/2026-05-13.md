# 2026-05-13

## Codex Skills / MCP

- 用户要求 Git Skill 安装 modal 中“相关的全部值都要显示”；已将原先的一行摘要改为字段列表，展示 Source、Provider、Host、Repo、Ref、Path，保持单层 modal + 分隔线结构，不引入卡片嵌套。
- 用户进一步要求 MCP server 编辑 modal 中“相关的全部值都要显示”；已在右侧增加当前值字段区，展示 Server ID、Label、Enabled、Status、Transport、Source Path、Command、Args、URL、Bearer Token Env、Env，并把 change preview 外层重边框改成分隔线结构。
- 用户指出 MCP server 编辑 modal 不应是 `main` 单独滚动，而应整个 modal 页面滚动；已移除 `McpServerEditorModal` 内 body/main/dialog 的独立滚动限制，保留最外层 overlay 负责滚动。
- 用户要求继续核对 Codex 源码后修正 MCP editor；已按 Codex `mcp_types.rs` / `mcp_edit.rs` 补齐 stdio、streamable_http、shared 字段，确认 `[mcp_servers.<id>.tools.<tool>]` 归属父 server 而不是独立 server，并将 MCP modal 改为 transport 分区 + 右侧当前值栏。浏览器验证 overlay `overflowY=auto`、`main` 不独立滚动，1280 宽度下右侧栏与编辑区并排。
- 用户指出 `CodexMcpServersWorkspace` 列表区当前设计不可接受并要求 `$debate` 与 Gemini 讨论；结论是保留“只要列表”的单 section，不做右侧 dashboard、不加行内 toggle，移除独立 metrics 卡片，将 total/enabled/changed、config path、visible count 合入列表 section header。已记录 debate 报告并截图验证 MCP 页面只剩一个主列表 section。

## Codex Account List

- `CodexAccountDetailModal` 的 OAuth/auth-file 详情已补齐 Web/OAuth 模型展示：打开 `detail=auth-file:auth.json` 时调用 `GetAuthFileModels`，将返回模型按同名透传展示为 `model -> model` 只读行；openai-compatible 仍保留可编辑模型映射。浏览器 preview 数据同步补充 OAuth 模型样本。已验证 `npm run typecheck`、`npm run test:unit -- src/features/codex/codexAccountList.test.mjs`，并用 Chrome DevTools 打开 `http://localhost:34115/#frame=codex&workspace=account-list&detail=auth-file%3Aauth.json` 确认显示 8 个兼容模型。
- `CodexAccountDetailModal` 的 openai-compatible 真实模型输入已改为自定义 combobox，复用账号池现有 `FetchOpenAICompatibleProviderModels` 拉取逻辑；Codex 模型/alias 仍允许自由输入。当前 `openai-compatible:MI` 远端模型接口因账号配置返回 401，页面显示 `模型拉取失败`，但控件已渲染为统一 combobox。
- `CodexAccountDetailModal` 的 Codex 模型/alias 输入也已改为可下拉、可自定义输入的 combobox，候选来自现有 `ListRelaySupportedModels` 模型目录。Chrome DevTools 验证 `openai-compatible:MI` 详情中 Codex 模型列有 8 个候选，真实模型列因 provider 拉取 401 仍为空，控制台无 error。
- `CodexAccountListFeature` 的拖拽排序语义已定为“请求顺序”：保存时仍写入 sidecar 识别的唯一递减 `priority`，但 UI 表达为从上到下的 failover 顺序；运行时目标是每次选择第一个当前可用账号，禁用、冷却或不可用账号会被跳过。文案已明确“保存后运行时会从上到下选择第一个可用账号”，避免误解为同级轮询。
- `CodexAccountListFeature` 新增路由探测交互：顶部提供测试模型输入、`测试一次`、`连续测试 3 次`，存在未保存顺序时禁用测试并提示先保存；后端新增 `ProbeCodexAccountRouting`，通过真实 relay `/v1/chat/completions` 最小请求与 sidecar recent request 差量识别命中的 auth-file / codex-api-key / openai-compatible 账号。已验证 `go test ./...`、`npm run typecheck`、`npm run test:unit -- src/features/codex/codexAccountList.test.mjs`，并归档截图 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-routing-probe-after-v01.png`。
- `CodexAccountDetailModal` 的 OAuth/auth-file 模型区已从只读兼容模型改为可编辑映射。实现上加载 `GetAuthFileModels` 与 `ListOAuthModelAliases(provider)` 后合并展示，保存时通过 `UpdateOAuthModelAliases` 写入 sidecar `oauth-model-alias[provider]`，语义为真实上游模型 `name` -> Codex 对外模型 `alias`，可用于把高等级模型名路由到低等级真实模型。该配置是 provider/channel 级共享，不是单个 auth.json 私有配置。已验证 Go 相关测试、`go test ./...`、`npm run typecheck`、`npm run test:unit -- src/features/codex/codexAccountList.test.mjs`。
- 用户指出模型下拉选择样式不对后，已将 `CodexAccountDetailModal` 模型映射和顶部测试模型输入统一切到自定义 combobox，移除浏览器原生 `datalist`；菜单候选改为 monospace + normal-case，避免 `gpt-5.4-mini` 等模型 ID 被 CSS 强制大写；弹层允许宽于表格单元格但不撑布局，保留项目 Swiss-industrial 硬边框和投影。已用 Chrome DevTools 验证可访问树中的 option 文案保留原始大小写，并归档截图 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-model-combobox-after-v02.png`。
- Codex 账号列表已从单纯测试按钮升级为路由策略调试区：页面可配置允许账号、排除账号、策略顺序和是否允许 fallback；`ProbeCodexAccountRouting` 新增策略字段并负责将页面 row id 翻译为 sidecar auth id，通过 `X-GetTokens-Route-*` loopback header 控制本次测试请求。这个设计保留 UI 友好的账号 id，不向页面暴露 sidecar hash 细节，也为后续“高延迟账号短期跳过” overlay 复用同一路由策略输入。已验证 Go/前端测试、typecheck，并归档截图 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-route-policy-after-v03.png`。
- 用户要求 Codex 账号列表路由策略 UI 由 Gemini 参与方案、Codex 执行。已按 Gemini 方案把三列重复账号重构为控制台 summary + 默认折叠的单一账号策略清单，测试结果合并到 summary，账号列表行展示 `路由 NN / 跳过` 与策略模式；保留 Swiss-industrial 视觉。Gemini 二轮评审认为可以交付，额外建议的 metric 风格、策略行过渡、combobox `aria-label` 已补齐；截图归档 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-route-policy-redesign-after-v03.png`。
- 用户反馈 openai-compatible 账号 MI 的模型映射编辑后保存不下来。根因是 Codex 详情页、账号池 openai-compatible 编辑模型和 Wails 保存链路都按真实模型 `name` 去重，导致同一真实模型 `mimo-v2.5` 同时映射 `gpt-5.5`、`gpt-5.4` 时后者被丢弃。已统一改为按 `name + alias` 去重，并补前端/Go 回归测试。真实页面验证：在 `http://localhost:34115/#frame=codex&workspace=account-list&detail=openai-compatible%3AMI` 保存两条映射后刷新仍保留，用 `gpt-5.4` 执行 `测试一次` 命中 `MI · HTTP 200 · recent requests +1`；截图归档 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-model-mapping-save-after-v01.png`。
- 用户要求 `CodexAccountListFeature:section:714` 改成独立卡片，并像终端流一样动态展示正在测试哪些账号。已将路由探测从请求顺序列表内拆出为独立卡片，新增 terminal 风格 `测试流`：显示命令、候选账号顺序和每次 attempt 的结果；`连续测试 3 次` 改为逐次调用后端并逐条追加日志。已用 Chrome DevTools 验证 `测试一次` 后显示 `01 MI`、`02 公司` 与 `#01 MI · HTTP 200 · recent requests +1`，控制台无 error；375px 窄屏按既有桌面工作台最小画布横向滚动。截图归档 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-probe-terminal-card-after-v01.png`。
- 用户继续指出路由探测卡片左侧控件太乱，右侧测试流仍有卡中卡。已将左侧整理为单列流程：标题、测试模型、主操作、次级操作、策略编辑；右侧测试流移除外层重边框和独立背景，直接作为同一卡片内终端区。Chrome DevTools 新页面验证无 console error；截图归档 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-probe-controls-cleanup-after-v01.png`。
- 用户指出 `RoutePolicyEditor` 的账号策略列表与 `请求顺序` 列表视觉重复。已取消独立策略账号清单，把默认/允许/排除与策略上移/下移控件内联到既有 `AccountOrderRow`；顶部路由探测卡只保留测试模型、测试按钮、策略开关、回退开关和测试流。已验证点击内联策略按钮不会打开账号详情，候选顺序实时刷新；截图归档 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-policy-inline-editor-after-v01.png`。
- 用户追问 `RouteProbeCard` 内 `允许回退` 是什么。该控件是 `allowFallback`，用于“设置允许账号后，是否继续尝试其他未排除账号”。为降低误解，已改文案为 `候选范围 / 备用账号` 并补充生效条件；截图归档 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-policy-fallback-label-after-v01.png`。
- 用户明确 OAuth 模型映射语义：默认原样穿透模型名，不展示或保存同名映射；一旦配置显式映射，则关闭未映射模型穿透。已将 Codex OAuth 详情改为默认 0 条映射，Web 模型只作为下拉候选；sidecar fork 的 OAuth alias 模型列表也改为有 alias 配置时只暴露映射 alias，未映射原始模型除 `fork: true` 外不再穿透。已验证前端类型/单测、GetTokens Wails OAuth alias 测试、sidecar `go test ./...`；截图归档 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-oauth-passthrough-mapping-after-v01.png`。
- 用户指出 `RouteProbeCard` 的 `编辑策略` 按钮只控制备用账号区块显隐不值得。已将 `候选范围 / 备用账号` 改为常驻展示，`编辑策略` 只控制账号行内策略控件展开/收起；截图归档 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260513/codex/20260513-codex-account-list-fallback-always-visible-after-v01.png`。
- 用户反馈调整请求顺序后测试仍执行之前账号。根因是列表排序更新 `orderedRows`，但路由探测使用的 `routePolicyOrderIDs` 保留旧策略顺序。已新增同步规则：可请求账号基础顺序变更时，测试策略顺序同步到新的从上到下顺序；基础顺序不变时保留用户在策略编辑中的自定义上/下移。拖拽事件内同步更新 `routePolicyOrderIDs`，避免刚拖完立即测试读到旧顺序；前端类型检查与 Codex 账号列表单测通过。
- 用户要求 `AccountOrderRow` 的编辑模式内容固定显示，并让 Gemini 重新设计账号行。已按 Gemini 四段式方案将账号行重构为 `拖拽/顺位 | 账号身份与请求出口 | 路由策略 | 启停开关`，默认/允许/排除与策略上/下移控件常驻；同时移除 `routePolicyEditing` 与路由探测卡片中失效的 `编辑策略` 开关。已验证前端类型检查、Codex 账号列表单测、Chrome DevTools 交互与截图归档。

## Codex Binary Management

- 用户反馈 Web 上“正式版”筛选拉不到数据。根因确认在后端 release fallback：GitHub REST API 失败后只读 `https://github.com/openai/codex/releases.atom`，而 Atom 近期窗口几乎全是 `rust-v0.131.0-alpha.*`，导致正式版没有进入 `RemoteVersions`。
- 已修复 `internal/codexbinary`：Atom fallback 若没有 stable release，会继续读取 GitHub releases HTML 分页，解析 `/openai/codex/releases/tag/rust-v*`，过滤 `rust-v` + semver，并最多扫描 5 页，拿到正式版即停止。真实 GitHub `releases?page=2` 已确认包含 `rust-v0.129.0` 等正式版链接。
- 同步修正 Atom prerelease 判断：不能用 tag 是否包含 `-` 判断，因为正式版 tag `rust-v0.129.0` 本身包含 `rust-v` 的连字符；现在统一按 version/tag 中的 alpha/beta/rc/pre/preview 标记判断。
- 页面实测发现刷新后只显示正式版、Alpha 被后端裁掉；根因是 `RefreshAvailable` 仍按 `manifest.IncludePrerelease=false` 构建远端列表。已改为远端刷新始终缓存正式版与 prerelease 完整列表，由前端 `全部 / 正式版 / Alpha` 筛选器决定显示范围。
- 新增 Go 回归测试覆盖 REST 被限流、Atom 只有 alpha、HTML 第 2 页补到 stable、Atom 正式版不应被误标 prerelease、刷新缓存同时保留 stable 与 alpha 的场景，并保留 HTML tag 解析过滤测试。
- 用户要求正式版不能只拿最新一个，需要历史版本分页拉取并可由本地缓存承载。已将 GitHub REST releases 改为 `per_page=100`、最多 5 页分页拉取；REST 不可用时 HTML fallback 也最多扫 5 页，不再拿到第一个 stable 就停止。刷新后仍写入本地 release catalog，页面进入时继续先读缓存。
- 用户要求 Codex Binary `VersionCell` 内部布局与 Gemini 讨论后重做。已采用三段式布局：左侧身份与变更记录入口，中间状态/发布时间/安装时间，右侧固定主操作；下载进度独立跨列，移除未实现的灰色定位/取消按钮。默认筛选同时改为“正式版”。已归档桌面与窄屏截图。
- 用户继续指出 `VersionCell:article:273` UI 奇怪；真实截图确认三段式版本 cell 像表格货架，中间留白和右侧灰色操作井噪音过高。经 Gemini 二次评审后改为紧凑工业铭牌行：去掉内部竖向边框、固定高度和灰色操作井，版本/状态/变更记录在左，主操作在右；窄屏取消按钮固定最小宽度，避免截断。
- 用户要求继续简化 Codex Binary `VersionCell`：移除 `VersionBadge`、次级元信息行和独立“变更记录”按钮；现在点击版本 cell 非操作区域直接展开/收起 release notes，右侧下载/激活/回退按钮阻止冒泡，避免误触发展开。
- 用户要求下载显示进度，且“下载”和“激活”是两个按钮但不同时显示。已将 `DownloadCodexBinary` 默认改为只下载/导入不激活；下载期间 `DownloadTaskView` 写入 snapshot，前端轮询刷新并在 cell 内只显示百分比和阶段，完成后同一行变为“激活”。已实测真实下载：0.130.0/0.129.0 进度显示正常，完成后只显示激活按钮。
- 真实下载暴露上游归档结构差异：`codex-aarch64-apple-darwin.tar.gz` 内只有 `codex-aarch64-apple-darwin` 可执行文件，不是根目录 `codex`。已扩展解包逻辑支持 `codex` 与 `codex-*` 可执行候选，并用 Go 测试覆盖平台命名归档。
- 用户要求 `VersionCell` 右侧支持“在 Finder 中打开”和“删除”，并放入菜单按钮。已为已安装版本新增右侧更多菜单，主按钮继续只负责下载/激活/回退；后端新增按 `versionID` 定位托管二进制与删除非当前版本能力，删除当前启用版本前端禁用且后端拒绝。页面验证菜单不触发行展开，外部点击和 Escape 会关闭菜单；截图归档到 `docs-linhay/spaces/20260511-codex-binary-management/screenshots/20260513/codex-binary/20260513-codex-binary-version-menu-after-v01.png`。
- 用户继续要求未下载版本也有菜单按钮，并在菜单内添加“在浏览器中打开”。已让 `VersionRowView` 透传 release `htmlURL`，远端未下载版本右侧也显示更多菜单；未下载菜单只打开浏览器，已下载远端版本菜单同时显示浏览器、Finder 和删除。截图归档到 `docs-linhay/spaces/20260511-codex-binary-management/screenshots/20260513/codex-binary/20260513-codex-binary-version-menu-browser-after-v01.png`。
- 用户要求 `VersionCell` 下载区显示文件大小。已将 release `assetSize` 透传到 `VersionRowView`，版本行在版本号下显示文件大小；旧缓存或 HTML fallback 没有 asset 元数据时显示“未知”，下载中优先展示任务 `bytesDone / bytesTotal`。截图归档到 `docs-linhay/spaces/20260511-codex-binary-management/screenshots/20260513/codex-binary/20260513-codex-binary-version-file-size-after-v01.png`。
- 用户要求把旧版本按钮“激活回退”改为“激活”。已仅调整展示文案，内部仍保留 `rollback` action 语义和成功提示，避免破坏旧版本激活判断与测试。
- 用户要求整理 Codex Binary 代码并拆小复杂结构。已将 `CodexBinaryFeature.tsx` 收敛为 controller，把顶部摘要、版本列表、版本 cell 拆到 `frontend/src/features/codex-binary/components/`，并把 release URL/下载大小展示工具移到 `presentation.ts`；行为保持不变，已通过前端类型检查、Codex Binary 单测和生产构建。

- Codex Skills 新增移除能力：详情 modal 内二次确认后删除 user 全局 root 下的 skill 目录，并清理对应 `[[skills.config]]` 禁用 override；系统 Skill 通过最具体 root 匹配识别为 `.system` / `/etc`，后端拒删。已用 Go 测试覆盖可删与系统拒删，浏览器预览验证移除后列表与 header 统计同步更新。
- 用户追问“移除 skill 本地文件没删？”后确认边界：移除 Skill 必须删除本地目录，否则 Codex 仍会扫描到。浏览器预览没有本地文件权限，因此不再允许从列表假删除，只提示到桌面 App 操作；真实桌面 Wails 路径必须调用后端删除目录。已在后端 `RemoveCodexSkill` 的 `os.RemoveAll` 后追加目录不存在校验，避免提示成功但目录仍在。
- 用户要求删除的二次确认需要用 alert 形式；已将 Skill 详情 footer 内的二次点击确认改为独立 `role="alertdialog"` 确认层，展示删除标题、Skill 路径和取消/删除动作，确认后才调用真实删除。
- 用户要求删除成功后需要成功动画 HUD；已在真实删除成功后关闭详情 modal，并展示顶部短暂成功 HUD，包含勾选缩放、扩散环和自动退出动画。当前未引入第三方库，使用现有 CSS 动画与 lucide 图标实现，避免为单点反馈增加依赖。
- 用户要求 Skill 详情文件列表支持点击预览；已将后端 `CodexSkillFile` 扩展为 `previewable`，snapshot 只带文件路径/类型/可预览标记，不再一次性返回所有文件内容。前端文件列表点击后调用 `GetCodexSkillFilePreview` 懒加载单文件内容并缓存，Markdown 文件使用安全 Markdown 渲染，脚本/其他文本使用等宽只读预览，不可预览文件显示提示。
- 用户指出 Skill preview modal 左右区域需要独立滚动且一次性请求全部文件内容太卡；已将 modal body 改为固定高度内的左右独立滚动区，后端新增按需单文件预览接口并校验 skill root 与相对路径边界，避免越界读取和打开详情时批量读取文件。
- 用户要求检查滚动条显示/隐藏是否影响宽度变化；确认 `overflow-y-auto/overflow-auto` 容器在非 overlay scrollbar 平台会因滚动条出现挤压内容宽度。已新增通用 `.scrollbar-stable` / `.scrollbar-stable-both` 样式，并给 Codex Extensions 的页面、列表、modal、Skill preview 左右滚动区和 textarea 滚动容器加稳定 gutter，避免宽度抖动。
- 用户指出 MCP Servers 列表区域不应定最小高度，而应跟随内容高度变化。已移除 MCP 列表 section 的 `min-h-[34rem]` 和列表容器的 `flex-1/overflow-auto`，让列表按 server 行数自然撑开，由页面外层滚动承载。

- Codex Skills 详情新增 `在 Finder 中打开`：macOS 桌面环境通过 `open -R <SKILL.md>` 定位文件，其他平台退化为系统文件管理器打开目录；后端复用 skill root 校验，只允许打开 Codex 已配置 skill roots 内的 `SKILL.md`。浏览器预览显示不可用提示。
- 已核对 Codex 真实源码确认 `[[skills.config]] enabled=false` 是官方支持能力，selector 支持 `path` 或 `name` 二选一；`enabled=true` 的持久化语义通常是删除 override。GetTokens 已同步修正：读取时按 canonical path/name 和规则顺序计算 Skill 启用状态，UI 启用时同时传入 Skill name 以清理同名禁用 override，避免手写 `name = "..."; enabled = false` 后列表开关状态与 Codex 实际读取不一致。
- 用户要求 Skills 页面不显示 project / git 两类，只显示全局 Skills。已按 Codex `core-skills/src/loader.rs` 确认全局 roots 来自 User/System layer：`$CODEX_HOME/skills`、`$HOME/.agents/skills`、`$CODEX_HOME/skills/.system`、`/etc/codex/skills`；Repo scope 的 project `.codex/.agents` roots 和 plugin/Git 追加 roots 不进入当前列表。后端 `resolveCodexSkillRoots` 已移除 project roots，前端移除 PROJECT/GIT filter 并只保留 system/user 全局 Skills。

## CLIProxyAPI Sidecar

- 已将 `docs-linhay/references/CLIProxyAPI` 维护分支 `gettokens/wham-token-fix` 同步到上游 `upstream/main@6bfcb0ce`，生成 merge commit `8906296c`。冲突处理保留上游 v7 module path，同时保留 GetTokens `internal/gettokenshooks` usage persistence hook，额外修正残留 v6 import。
- 验证结论：`go test ./...` 在 sidecar 仓库通过。上游 v7 已有 auth/model 冷却、`disable-cooling`、`request-retry`、`max-retry-credentials`、`max-retry-interval`，并在 Redis usage queue 中输出 `latency_ms`；但调度层仍只按失败状态、quota、429 retry-after 等信号短期跳过账号，没有“成功请求延迟超过阈值后自动冷却/跳过账号”的原生配置。
- 已按“客户端可控候选账号与顺序”的方向在 sidecar fork 增加通用 RoutePolicy 口子并提交 `be5b0624`：`sdk/cliproxy/auth` 负责对 ready candidates 做 allow/deny/order/fallback 重写，GetTokens 侧 `internal/gettokenshooks/route_policy.go` 负责读取受信任 metadata 与本机 header。策略运行在 disabled/cooldown/model availability 之后、selector 之前，不能绕过保护状态。已验证 `go test ./...`。
