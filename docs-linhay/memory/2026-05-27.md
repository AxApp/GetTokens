# 2026-05-27

## Codex live sessions 图表布局修复 space
- 新建 `docs-linhay/spaces/20260527-codex-live-session-chart-layout/`，用于跟进“请求耗时趋势”图表变形修复。已把根因初判沉淀到 space README：直接问题是 `TimingTrendChart` 的 `overflow-hidden` 外框、左右/底部 padding 不足、x 轴标签密度过高；CLIProxyAPI 50 request 裁剪重编号只是放大因素。计划文件 `plans/chart-layout-fix-plan-v01.md` 已列出 BDD 场景、TDD 红绿重构步骤、浏览器 DOM/截图验收和 Wails 验收边界。

## 账号统一新增弹窗重做
- 决策：`#frame=accounts` 的统一新增弹窗不再沿用“厂商列表 + 旧式表单”拼接感，配置步骤改成和账号详情页一致的模块栈布局，按 `端点 / 凭据 / 高级 / 余额` 分区表达，减少首层视觉噪音。
- 实现：`UnifiedComposeModal` 复用了 `AccountDetailModalFrame`、`AccountDetailBody`、`AccountDetailSection`、`AccountDetailModuleStack` 和 `AccountDetailPill`；选择态卡片继续保留本地厂商图标与短名称，但内部层级改成“分类锚点 + 品牌名 + 格式条”的三段式。
- 本地化：新增 `accounts.unified_compose_credentials`，并把统一新增弹窗的配置分区、标题和格式标签继续收口到现有 `accounts.unified_compose_*` 文案。
- 补充：头部 actions menu 的添加账号项去掉硬编码 `ADD ACCOUNT` fallback，改用 `accounts.add_account`；统一新增弹窗的 Provider / Endpoint / Credential / Automation / Billing 小眉标也改为 locale copy。
- 沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的账号详情 surface 边界，后续账号创建/配置弹窗也按 detail primitives、命名分区和 locale copy 收口；未升级 `AGENTS.md`，因为这是账号领域规则。
- 验证：`node --test frontend/src/features/accounts/tests/accountHeaderMenu.test.mjs frontend/src/features/accounts/tests/accountPresentation.test.mjs`、`npm --prefix frontend run typecheck`、`docs-linhay/scripts/check-docs.sh` 通过；浏览器已在桌面视口完成 `添加账号 -> 选择 DeepSeek -> 配置账号` 流程复核，最新本地化截图保存为 `docs-linhay/spaces/20260526-account-provider-picker-icons/screenshots/20260527/accounts/20260527-accounts-unified-compose-config-localized-after-v01.png`。

## Sidebar 升级提示时机确认
- 结论：Sidebar 底部版本区固定展示版本标签，升级提示只会在后端已经检测到新版本并写入 `availableRelease` 后出现；普通浏览器预览和无 Wails runtime 场景不自动出现。
- 预览边界：`?preview=sidebar-update` 仅用于稳定复现升级提示态，真实 App 仍以启动后的异步更新检查和设置页手动检查结果为准。
- 文档：已把该时机边界补写到 `docs-linhay/spaces/20260426-sidebar-version/README.md`，最近更新日期同步到 `2026-05-27`。
- 追加实现：非原生更新器场景改为启动立即检查一次，之后每 6 小时定时检查一次；检测到新版本时继续通过 `updater:available` 驱动 Sidebar 升级提示。
- 验证：先新增 `runtime_update_test.go` 红灯覆盖 `6*time.Hour` 间隔、启动即时检测、tick 后再次检测、无可用更新/错误不 emit；实现后通过 focused Go 测试与 `go test ./...`。

## Codex live-session runtime optimization

- 新建 space：`docs-linhay/spaces/20260527-codex-live-session-runtime-optimization/`。
- 背景：排查 `cli-proxy-api` RSS 与 `sidecar.log` 增长时，确认 `/v0/management/gettokens/live-sessions` 当前返回 realtime 全量快照，列表轮询会反复序列化 session/request/timeline；缺失 `projectName` 时还可能触发 `.codex` JSONL 扫描。
- 决策：按“列表轻量化、详情分页化、projectName 补全移出 snapshot 热路径、前端轮询降频/隐藏暂停、access log 降噪、realtime 内存与 disk history 分离”的方向推进。
- 计划入口：`docs-linhay/spaces/20260527-codex-live-session-runtime-optimization/plans/runtime-optimization-plan-v01.md`。
- 实施结果：sidecar 列表已切成 row feed，request/timeline 详情改由 history endpoint + Wails/root binding 懒加载；`projectName` 补全改为后台缓存刷新，列表成功轮询默认降噪。
- 验收结果：已通过 `go test ./...`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`，并完成 browser preview 冒烟，截图为 `output/playwright/20260527-codex-live-sessions-runtime-optimization-smoke.png`。
- 未完成项：真实 Wails 桌面窗口 smoke 尚未跑通，因此当前只可判定为 browser-smoked，不宣称 desktop runtime 验收完成。
- 沉淀判断：本轮未新增 project skill 或 AGENTS 规则；稳定模式仍落在现有 `gettokens-domain-engineering` 的 `Codex Live Sessions` 边界内，因此只同步 space / memory / qmd。

## Auth-file metadata cache

- 修复方向：桌面客户端普通展示场景不再反复下载同一 auth-file 原文；`ListAuthFiles` 首次 fresh 推断后按 canonical `name` 归一缓存展示元数据，`size + modified` 只作为 freshness fingerprint。
- 归一化边界：auth-file 身份仍是 `auth-file:<name>`；`size` 不是账号身份、不能参与前端选择态/禁用态/排序/modal detail 的 ID 归一，只用于判断缓存是否过期。
- 验证：构造 10,000 次同名 auth-file fingerprint churn 的 benchmark 中，新实现约 `16.9 KB/op / 164 allocs/op`，旧 `name|size|modified` composite-key 基线约 `4.68 MB/op / 30070 allocs/op`。

## Codex live sessions 单指标音频波形图
- 决策：`#frame=codex&workspace=live-sessions` 的请求耗时趋势图不再把多个 timing 维度同时画进同一张图里，而是改成“一个指标一组音频波形条”；最终图表类型定为 forward-moving audio waveform chart，一柱一请求，x 轴是请求序号而不是真实时间间隔。
- 实现：`SessionDetail` 维护当前 `selectedTimingMetric`，`TimingMetrics` 把可绘图项变成可点击按钮并用 `aria-pressed` 标示选中态，`TimingTrendChart` 只按当前 metric 计算 y 轴和单组垂直振幅条；图表去掉横向滚动，按容器宽度决定可见请求数，宽屏显示更多最近请求、窄屏显示更少，最新 request 靠右密集推进。动画改为“指标切换时短淡入 + live 点光圈呼吸”，不再对整条波形做 sweep 重扫。
- 补充：`请求时间线` 是扫描区而非完整历史列表，页面内只展示排序后的最近 15 条 request，标题行数按实际可见行数展示。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过；早前 Playwright 复核确认点击 `TTFT 562ms` 后图表切到蓝色单线，点击 `流式` 后切到绿色单线。本轮最终浏览器复核被 Browser URL policy 拦截，未绕过执行。
- 失效边界：上传、删除、启停状态修改成功后按文件名清理缓存；文件大小或修改时间变化会自然触发重新下载与缓存刷新。
- 关键决策：auth-file 仍以文件作为事实源，不迁移到数据库；进程缓存只保存 provider/type/email/plan/priority 等展示元数据，不保存完整凭证内容。
- 验证：新增 sidecar mock 测试覆盖重复列表、元数据仍不完整、fingerprint 变化三类场景，并通过 `go test ./internal/wailsapp`。

## Codex live-session controls 评估

- 新建 space：`docs-linhay/spaces/20260527-codex-live-session-controls/`，用于拆解 `live-sessions` 页的账号指定切换与活跃连接终止能力。
- 重新评估修正：用户截图中选中的 `ws_sess_7a91` 来自 `localhost:5173` browser preview/cache，页面显示 `VERSION BROWSER` 与 `来源 CACHE`，且 `hasWails=false`；dev sidecar `18317` 的真实 live-session snapshot 为 `activeSessions=0`。该行不能作为真实 kill 对象。
- 现状判断：当前 `live-sessions` management API 的 clear tracker 不等于断开连接；按 `session_id` 定点终止活跃请求或 websocket 连接仍需要 sidecar runtime 控制能力。
- 设计结论：`账号指定切换` 更适合落在路由/账号偏好层，只影响后续请求；`kill` 则需要新增 sidecar management API 或 runtime executor 的会话级 cancel/close hook，不能只靠前端补按钮。
- 操作边界：正式 App / `8317` sidecar 不属于本需求的止血对象；后续不得对正式环境做 stop、kill、restart 或清理类动作，除非用户明确授权。
- 后续安排：用户确认本轮只开 space 记录，`20260527-codex-live-session-controls` 状态调整为 backlog，暂不进入实现。

## Account detail close 与 OpenAI-compatible 布局修复

- 修复：`#frame=accounts&detail=...` 账号详情关闭时同步清空本地 `accountDetailIDFromHash`，避免旧 hash detail 通过 hydration effect 重新选中账号，导致 `关闭面板` 需要点两次。
- 布局：OpenAI-compatible provider detail 改为单列配置工作表，runtime/evidence split 延后到 `2xl`，模型行在中等宽度下让删除按钮独立横向换行，避免 `MODEL CATALOG` 产生右侧空白或按钮竖排。
- 沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的账号详情边界；未升级 `AGENTS.md`，因为这是账号详情领域规则，不是 repo-wide 流程约束。
- 验证：`node --test src/features/accounts/tests/accountDetailLayout.test.mjs`、`node --test src/features/accounts/tests/openAICompatible.test.mjs`、`npm run typecheck`、`npm run test:unit` 均通过。

## 当前 rollout 稳定化计划

- 新建 space：`docs-linhay/spaces/20260527-stabilize-current-rollout/`，用于收敛当前工作区跨账号 UI、账号 disabled 同步、auth-file metadata cache、Codex live-session 图表和文档写回的混合改动。
- 执行决策：采用“先冻结边界，再按交付组验证，最后治理产物与拆提交”的最稳方案；不继续扩大功能面，不实现 live-session kill / 指定会话切号，不 stage / commit，直到分组测试和未跟踪产物归属清楚。
- 计划入口：`docs-linhay/spaces/20260527-stabilize-current-rollout/plans/execution-plan-v01.md`。
- 执行进展：已把 `accountDisabledSync.test.mjs` 纳入 `frontend/package.json` 的 `test:unit`，并新增 `output/` 忽略规则；A/B/C/D focused tests、`go test ./...`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`./scripts/wails-cli.sh build` 均通过。
- 提交边界：当前 `master` 已有 5 个本地提交覆盖 live-session runtime optimization、unified compose、account detail layout、disabled sync、update polling；后续继续按 auth-file cache、live-session 单指标 ECG、route mode header、测试入口/治理文档拆提交。
- 账号启停同步：`#frame=accounts` 与 `#frame=codex&workspace=account-list` 已统一走 canonical account id 的 `SetAccountDisabled(id, disabled)` 写回与前端同步事件，浏览器 preview 额外把禁用覆盖写入同源 localStorage，避免切换 frame 后状态回跳；同步事件只接受 `auth-file:` / `codex-api-key:` / `openai-compatible:` 三类新 ID，不再保留 bare name 兼容。

## Channel Routing route mode 校准

- 决策：当前 Codex / Claude Channel Routing 主路径只保留 `sequential / balanced` 两种 route mode；`project` 已从可配置 route mode 下线。
- 边界：历史 `projectBindings` 可继续作为项目名到账号/账号组的范围约束或兼容数据保留，命中后仍交给 `sequential` 或 `balanced` 在目标池内选择账号；`projectModeFallbackRouteMode` 仅属于旧兼容路径，不能作为新自定义路由模式的扩展点。
- 文档：已同步 `.agents/skills/gettokens-codex-account-list/SKILL.md`、`.agents/skills/gettokens-claude-code-account-list/SKILL.md`、`.agents/skills/gettokens-domain-engineering/SKILL.md`、`docs-linhay/dev/20260524-account-routing-engine.md` 以及 `docs-linhay/spaces/20260524-account-routing-engine/` 下 README 和计划文档。
- 后续：新增自定义路由模式时应从两模式主线扩展，并同时更新前端 `CHANNEL_ROUTE_MODES`、归一化测试、Wails DTO、后端 normalize / explain / runtime policy；不要复用旧 `project` 语义作为新模式入口。

## Accounts bulk action sticky toolbar

- 修复：`#frame=accounts` 进入批量选择后，吸顶范围收敛为独立的选中操作条，仅包含全选、清空、导出、已选数和 `批量刷新 / 批量激活 / 批量禁用 / 移除`；搜索筛选工具栏和账号卡片继续按列表内容滚动。吸顶外层使用 `-top-12 -mx-12 px-12 py-3` 抵消页面滚动容器的 `p-12`，让操作条从视口 `y=0` 开始并用 `bg-surface` 覆盖整条顶部背景，避免账号卡从上方间距透出。
- 验证：新增 `accountListLayout.test.mjs` 结构回归；已通过 focused accounts layout/filter 测试、`npm run typecheck`，并在 in-app browser 对 `http://localhost:5173/#frame=accounts` 执行刷新、点击 `批量选择`、滚动页面的实测验收。复测结果：`stickyRect.top=0`、`stickyPaddingTop=0px`、顶部采样 `hasTopCardLeak=false`；after 截图为 `docs-linhay/screenshots/20260527/accounts/20260527-accounts-bulk-sticky-after-v01.png`。
- 追加设计：批量操作条改成单条 48px workbench toolbar，去掉 `border-t border-dashed` 虚线；左侧展示已选数、全选/取消全选、清空选择，右侧保留高频 `批量刷新`，把导出、批量激活、批量禁用、移除收进 `MoreVertical` 菜单，移除确认也在菜单内完成。Playwright 复测：`actions.height=48`、`dashedCount=0`、`menuOpen=true`、`topLeak=false`；截图为 `docs-linhay/screenshots/20260527/accounts/20260527-accounts-bulk-toolbar-menu-after-v01.png`。
- 追加修正：吸顶操作条取消内部 `border/shadow` 卡片包装，直接作为无框 sticky band 渲染；`py-3` 对称上下边距让 40px 按钮在 72px band 内居中。Compact 模式 Playwright 复测：`sticky.height=72`、`actions.top=12`、`actions.height=48`、`paddingTop=12px`、`paddingBottom=12px`、`actionsAlignItems=center`、`hasCardBorder=false`、`shadowWrapperCount=0`、`topLeak=false`；截图为 `docs-linhay/screenshots/20260527/accounts/20260527-accounts-bulk-toolbar-flat-after-v01.png`。
- 间距收紧：用户反馈操作栏上下空间仍偏大后，sticky band 从 `py-3` 调为 `py-1.5`，并用 `!mt-4` 覆盖父级 `space-y-8` 的 32px 间距；账号列表容器在选中模式下同步加 `!mt-4`，把操作栏与列表之间的流间距收敛到 16px。Compact 模式 Playwright 复测：`sticky.height=60`、`actions.height=48`、`paddingTop=6px`、`paddingBottom=6px`、`marginTop=16px`、下方列表 `marginTop=16px`、`topLeak=false`；截图为 `docs-linhay/screenshots/20260527/accounts/20260527-accounts-bulk-toolbar-compact-spacing-after-v01.png`。
- 自适应动作：用户指出横向空间足够时不应把批量动作固定收进菜单；`AccountsSelectionActions` 新增 `ResizeObserver` + `shouldUseAccountsSelectionActionMenu`，按完整展开宽度判断右侧动作是否 fallback。1203px 视口滚动验收：可见按钮为 `全选/清空选择/批量刷新/导出选中/批量激活/批量禁用/移除`，`hasMoreMenuButton=false`、`stickyRect.top=0`、`sticky.height=60`、`topLeak=false`；720px 窄视口验收：可见按钮为 `取消全选/清空选择/批量刷新/更多操作`，`hasMoreMenuButton=true`、`topLeak=false`。截图为 `docs-linhay/screenshots/20260527/accounts/20260527-accounts-bulk-toolbar-adaptive-inline-after-v01.png` 与 `docs-linhay/screenshots/20260527/accounts/20260527-accounts-bulk-toolbar-adaptive-menu-after-v01.png`。
- 会话沉淀：将账号池批量选择操作栏的复用规则写入 `.agents/skills/gettokens-domain-engineering/SKILL.md` 与 `docs-linhay/dev/20260527-accounts-bulk-toolbar-session-distillation.md`：sticky band 覆盖顶部、单条工具栏、宽屏 inline、窄屏菜单 fallback、滚动后 `topLeak=false` 浏览器验收。不升级 `AGENTS.md`，因为这是账号池领域 UI 规则。

## Accounts single-card group blank lane

- 修复：账号池 full/compact 卡片网格从 `auto-fill` 改为 `auto-fit`，单卡分组不再保留可见空轨道，避免 FREE 等只有 1 个资产的分组在右侧/下个分组上方留下大片空白。
- 验证：更新 `accountCardLayout.test.mjs` 锁定 `auto-fit` 并禁止账号卡网格回退到 `auto-fill`；通过 focused layout/filter tests 与 `npm run typecheck`，浏览器计算样式确认单卡分组为 `804px 0px`，空轨道已折叠。

## Codex account order toolbar adaptive actions

- 修复：`#frame=codex&workspace=account-list` 的请求顺序工具栏不再只按固定右侧小容器判断是否收起；改为按 header 宽度、标题自然宽度和工具栏宽度选择 `inline / wrapped / menu`。
- 行为：宽屏同排直接展开；中等宽度换到标题下方展开；窄屏才收进 `更多操作`。展开态搜索框左对齐并 `flex` 拉伸补齐剩余空间，刷新、筛选、密度按钮按内容宽度显示。
- 验证：新增 `chooseCodexOrderSectionActionLayout` 回归测试，通过 `node --test frontend/src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck`，并用 in-app browser 验证 1600px / 1280px / 720px 三档行为；1148px 视口下搜索框从请求顺序区域左边缘开始，宽度拉伸到刷新按钮前。截图为 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-toolbar-adaptive-after-v02.png`。

## Codex account order footer flatten

- 修复：`#frame=codex&workspace=account-list` 的 Codex 请求顺序卡片 footer 去掉 `RegionHead` 小卡片矩阵；请求出口、模型映射、运行态、路由状态改成扁平 meta 信息，避免卡中卡。
- 追加修正：footer 内部不再绘制 `border-t/b border-dashed`，模型映射行和路由状态只靠间距排版，避免与账号归因区上一段底部分割线叠成双线。
- 边界：该方案最终被后续“直接复用账号池 `AccountCard`”收口取代；Codex 请求顺序卡不再渲染自定义 footer，避免卡中卡和分割线叠加，账号池普通卡片不受影响。
- 验证：`node --test frontend/src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过；in-app browser 1148px 视口确认 deepseek footer 内部 dashed 分割线计数为 0，仅保留共享账号卡自身结构。截图为 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-footer-flat-after-v01.png`、`docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-footer-lines-after-v01.png`。

## Shared refresh action button

- 决策：页面级刷新入口统一使用共享 `RefreshActionButton`，由组件内封装 `RefreshCw`、loading 旋转、Swiss button 尺寸和禁用态，避免各页面重复手写刷新图标/文案切换。
- UX 边界：Codex 请求顺序工具栏展开态的强制刷新只显示图标，保留 `aria-label/title`；更多菜单等纵向扫描场景仍可显示文字。
- 覆盖：本轮已迁移 Codex 请求顺序工具栏、Channel Routing 最近路由、Codex live sessions、Codex binary、Codex extensions、Vendor Status。
- 验证：`node --test frontend/src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过；in-app browser 1148px 视口确认 Codex `强制刷新` 可见文字为空、带 svg 图标、按钮宽度收敛为约 42px。截图为 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-refresh-button-component-after-v01.png`、`docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-refresh-icon-only-after-v01.png`。

## Codex account order card shared header

- 修复：Codex 请求顺序卡虽然复用 `AttributionCard`，但原先把 `拖动 / 置顶 / 置底` 放在 `leadingAction`，会改变账号池共享卡 header 主布局。
- 决策：Codex 排序动作改用 `topActions` 右上角 icon-only 操作组；拖动、置顶、置底仍保留，但不再产生可见文字，不挤占账号名称、endpoint 和 badge 区域。
- 追加修正：用户反馈右上角竖排排序图标组仍然太丑，甚至不如最初版本；Codex 卡头操作进一步收敛为账号池同款单个 `更多操作` / `账号操作` 图标，默认态不显示 `拖动 / 置顶 / 置底`，置顶/置底进入下拉菜单，拖动从同一图标发起。
- 最终收口：用户要求不要做“账号池相似组件”，而是直接复用账号池 `AccountCard`；非列表模式的 Codex 请求顺序卡已改为直接渲染 `AccountCard`，启用/禁用回到账号菜单，Codex 页只补 `ORDER` / 来源 badge，关闭删除和 footer 动作，排序通过拖动整张卡完成。
- 验证：`node --test frontend/src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过；in-app browser 1106px 视口确认 deepseek 卡只有账号池菜单按钮、无 Codex footer 标签、无独立 toggle 文案，外层整卡可拖。截图为 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-shared-card-actions-after-v01.png`、`docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-card-actions-menu-after-v01.png`、`docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-account-card-reuse-after-v01.png`。

## Codex participant filter relocation

- 决策：Codex 请求模式区不再展示独立 `参与账号` 折叠列表；该信息与下方请求顺序账号列表重复，应统一作为账号列表展示筛选维度。
- 修复：移除 `ChannelRoutingWorkbench` 中的参与账号 details，将 `参与账号` / `跳过账号` 放入请求顺序工具栏 `显示` 菜单的 `路由` 分组，筛选语义保持 AND-style，只影响展示，不改真实排序或运行时请求顺序。
- 验证：`node --test frontend/src/features/codex/codexAccountList.test.mjs`、`node --test frontend/src/features/channel-routing/tests/channelRouting.test.mjs`、`npm --prefix frontend run typecheck` 通过；Playwright 验证 `http://localhost:5173/#frame=codex&workspace=account-list` 上方无 `参与账号 5 个账号`，下方 `显示` 菜单包含 `路由 / 参与账号 / 跳过账号`。截图为 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-participant-filter-after-v01.png`。

## Codex browser mock visual parity

- 偏好：`localhost:5173` browser mock 虽然使用 mock 数据，但 Codex 账号列表界面不应默认暴露 `预览` 标识，应尽量与真实桌面环境显示一致。
- 修复：移除 `ChannelRoutingWorkbench` 的 preview chip 和 `preview` prop；Codex browser mock 下请求顺序 hint、加载/保存/启停 message 复用真实环境文案，保留 mock 数据加载、本地排序和启停覆盖行为。
- 验证：`node --test frontend/src/features/channel-routing/tests/channelRouting.test.mjs frontend/src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过；in-app browser / Playwright 验证 `hasPreviewChip=false`、`hasBrowserHint=false`，截图为 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-preview-chrome-hidden-after-v01.png`。

## Codex account card equal height

- 偏好：Codex 请求顺序卡片既然直接复用账号池 `AccountCard`，同一网格行内也应保持账号池式等高卡片；短卡片不能露出一大片行背景。
- 修复：撤回 Codex 请求顺序卡的 `fillHeight={false}` 特例，移除共享卡片链路里的 `fillHeight` 分叉，恢复 `AccountCardFrame` 固定 `h-full`。Codex 页仍只通过 `extraBadges`、`showDeleteAction=false`、`showFooterActions=false` 表达必要差异。
- 验证：`node --test frontend/src/features/codex/codexAccountList.test.mjs` 通过；in-app Browser 验证第一行 `deepseek / ops-pro / Stable 001` 高度均为 `371px`，第二行 `team-routing / Gray Canary / openrouter` 高度均为 `371px`。截图为 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-card-equal-height-after-v01.png`。

## Codex account order status prefix

- 偏好：Codex 请求顺序页需要在账号卡状态前显示当前顺位，例如 `#1 可用`；该序号只属于请求顺序页，不进入账号池普通卡片。
- 修复：共享 `AccountCard` / `AttributionCard` 增加默认空的 `eyebrowPrefix`，Codex 请求顺序卡传 `#${index + 1}`；序号与状态同一行显示，视觉上用 flex gap 分隔。
- 验证：`node --test frontend/src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过；in-app Browser 验证前 6 张卡显示 `#1` 到 `#6` 状态前缀。截图为 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260527/codex/20260527-codex-account-list-order-prefix-after-v01.png`。
- 会话沉淀：已更新 `.agents/skills/gettokens-codex-account-list/SKILL.md` 与 `docs-linhay/dev/20260527-codex-account-order-ui-distillation.md`，固定“请求顺序卡直接复用账号池 `AccountCard`，Codex 差异只走 opt-in props”的规则；不升级 `AGENTS.md`。

## Codex live-session chart layout

- 根因：`TimingTrendChart` 外层 `overflow-hidden`，但原 `padding.left=30` 不足以容纳秒级 y 轴标签，`padding.right=18` 小于 live ring 最大半径 `28`；同时 x 轴标签直接按 `sequence % 5` 显示，在 50 request 长会话下容易密集堆叠。
- 修复：新增 `resolveTimingTrendChartPadding`，左侧 y 轴标签安全区提升到 56px，右侧 live ring 安全区提升到 38px，底部刻度区提升到 54px；新增 `resolveTimingTrendAxisLabelIndexes`，按实际 x 坐标与 36px 最小间距稀疏显示 `#sequence` 标签。
- 样本：按用户提示补充伪造数据，`model/mockData.ts` 的 browser preview 首个 live session 现在生成 50 条近 5 分钟 request，最新请求仍为 `gt-req-8912`，序号为 `#50`，可稳定复现长会话图表密度。
- 验证：先补红灯测试，`node --test frontend/src/features/codex-live-sessions/model.test.mjs` 首次按预期失败 2 条；实现和 50 request 样本调整后 46/46 通过，并通过 `npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`git diff --check`。
- 浏览器验收：`http://localhost:5173/#frame=codex&workspace=live-sessions` 在 50 request preview 下 DOM 检查结果为 `labelOverflow=[]`、`xLabelOverlap=[]`、`liveRingOverflow=false`；可见 x 轴标签为 `#20/#25/#30/#35/#40/#45/#50`。after 截图归档到 `docs-linhay/spaces/20260527-codex-live-session-chart-layout/screenshots/20260527/codex-live-sessions/20260527-codex-live-sessions-chart-layout-after-v02.png`。
- 会话沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 Codex Live Sessions 规则，明确布局/密度类图表问题必须伪造触发问题的边界数量样本，并用 DOM 断言检查 label overflow、x 轴重叠和 live ring 裁切；不升级 AGENTS，因为这是 live-session 领域内规则。
- 剩余风险：真实 Wails runtime 当前没有 active 50 request 样本；本轮已用 browser preview 伪造 50 request 完成长会话验收，后续有真实样本时补 Wails 截图。

## Codex live-session detail contained scroll

- 根因：`CodexLiveSessionsWorkbench` 的右侧详情列只有 `xl:sticky xl:top-5`，没有高度上限和内部滚动；长详情内容会撑大外层 workbench，导致用户在请求耗时趋势或请求时间线附近滚动时，把整个页面页头、搜索栏和左侧列表一起卷走。
- 修复：右侧详情列在宽屏下增加 `xl:max-h-[calc(100vh-2.5rem)]`、`xl:overflow-y-auto`、`xl:overscroll-contain` 与稳定滚动条，详情列成为独立滚动容器，图表本身仍保持固定非横向滚动视口。
- 验证：先补红灯测试 `codex live session detail scroll is contained inside the detail column`，确认旧结构失败；实现后 `node --test frontend/src/features/codex-live-sessions/model.test.mjs` 47/47 通过，`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过。in-app browser 宽屏 DOM 验收确认 detail 为 `position: sticky`、`overflow-y: auto`、`overscroll-behavior-y: contain`，高度 924px、内容高度 1937px。
- 沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 与 `docs-linhay/dev/20260523-session-distillation-codex-live-sessions-ui.md`，不升级 `AGENTS.md`。

## CLIProxyAPI upstream merge planning

- 新建 space：`docs-linhay/spaces/20260527-cliproxyapi-upstream-merge/`，用于处理 `docs-linhay/references/CLIProxyAPI#gettokens/sidecar` 合并 `upstream/main@4b681031`。
- 现状：fork 工作区干净，当前 HEAD 为 `c3907174`，相对 `origin/gettokens/sidecar@1c0f0031` ahead 1；canonical upstream 已前进到 `4b681031`，分叉计数为 fork 侧 37、upstream 侧 7。
- 预演：`git merge-tree --write-tree HEAD upstream/main` 可生成 tree `5e85c21886c14c6a2a41be2ffcf350f851881c68`，暂未发现文本级硬冲突；风险主要是语义层面。
- 决策：先按 space 计划执行逐项审核，再 merge；重点审核 Gemini CLI schema cleanup、GPT Image 2 base model/SSE、Responses websocket tool call repair、OpenAI/Claude Responses reasoning signature。若触及 GetTokens route guard、channel routing、live sessions、usage ledger 或 Codex WebSocket failover 退化，暂停整包 merge，改选择性 cherry-pick 或重新设计合并方案。
- 计划入口：`docs-linhay/spaces/20260527-cliproxyapi-upstream-merge/plans/upstream-merge-plan-v01.md`。

## CLIProxyAPI upstream merge completed

- 监督模式：本轮按用户要求开 subagent 审核，4 路只读审核结论一致：`upstream/main@4b681031` 可整包合入，`de280d99` 和 `e399edd3` 作为高优先验证点。
- 合并结果：`docs-linhay/references/CLIProxyAPI#gettokens/sidecar` 已合并 upstream，生成 merge commit `57ab8229`，并追加测试补强 commit `ef93d8c0 test: cover configurable codex image base model`；`origin/gettokens/sidecar` 已推送到 `ef93d8c0`。
- 测试补强：新增 `gpt-image-2-base-model` config diff 测试，以及 Codex OpenAI image body 使用配置 base model / invalid fallback 的 executor 测试，覆盖 subagent 指出的 image base model 语义风险。
- 验证：合并前后 focused tests、WebSocket failover/route guard 正则测试、image/config/payload 正则测试、`go test ./...`、`git diff --check` 均通过。
- sidecar：已执行 `./scripts/ensure-sidecar.sh darwin arm64`，本地 meta 为 `commit=ef93d8c0`、`dirty=clean`、`goos=darwin`、`goarch=arm64`。
- 注意：并发重复 push 中只有一条实际推进远端，另两条因远端已更新到同一 commit 被 remote lock 拒绝；最终远端与本地 HEAD 均为 `ef93d8c0`。

## CLIProxyAPI upstream merge session distillation

- 沉淀：CLIProxyAPI fork 偏离 upstream 较远时，先用 subagent 按总体 commit、高风险运行时、兼容面拆分只读审核；主控只在审核一致后执行 merge，并把 subagent 指出的未锁行为补成 fork 内窄测试。
- 已更新：`.agents/skills/gettokens-domain-engineering/SKILL.md` 的 CLIProxyAPI Fork Maintenance 段落，新增 Subagent Upstream Audit 与 Audit-to-Test Rule。
- 已更新：`docs-linhay/dev/20260519-cliproxyapi-upstream-sync-session-distillation.md`，补充 2026-05-27 subagent 审核同步记录。
- 不升级 `AGENTS.md`：该模式仍属于 CLIProxyAPI fork 领域维护流程，不是 repo-wide 通用治理规则。

## Codex HTTP 入站 + GetTokens 上游 WebSocket backlog

- 决策：已在 `docs-linhay/spaces/20260524-account-routing-engine/README.md` 记录 `Codex -> HTTP/SSE -> GetTokens/CLIProxyAPI -> OpenAI Responses WebSocket -> SSE 回传 Codex` 作为未来备选方案。
- 边界：这是代理层桥接，不是 Codex 客户端同一 session 内透明切回 WebSocket；近期不实现，不改 Codex 参考源码，不改 CLIProxyAPI，不新增测试。
- 后续：若未来启动该方案，需要先设计 SSE <-> WebSocket event 映射、请求边界重建 WS / 换账号 / replay 规则，以及上游 `426 Upgrade Required` 的内部处理策略。

## Taste Skill project install

- 安装：按用户要求将 `https://github.com/Leonxlnx/taste-skill.git` 的全部 `skills/` 子目录安装到项目级 `.agents/skills/`，覆盖前端设计、重设计、image-to-code、web/mobile imagegen、brandkit、Stitch、输出完整性等 13 个外部 skills。
- 固定哈希：远端 `main` 当前 commit 为 `3c7017d636c3a4aad378433ea6d0cfa6c921da4a`，已写入 `.agents/skills/taste-skill.lock.json`，并在 `docs-linhay/dev/20260527-taste-skill-project-install.md` 记录后续更新流程。
- 更新机制：新增 `docs-linhay/scripts/update-taste-skill.sh`，默认 `--check` 只比较 lock 与远端哈希；显式 `--update` 才按 lock 的安装清单替换本地 skill 目录并更新 commit。
- 会话沉淀：外部 Git skill 包安装流程已沉淀到 `.agents/skills/gettokens-codex-extensions-management/SKILL.md`，固定要求记录 source/ref/commit/source-path/local-path/skill-name，更新脚本默认只检查，显式 `--update` 才替换本地目录。
- 风险：Taste Skill 包的 frontmatter 描述较长，可能增加 Codex skill discovery 上下文预算压力；若后续出现预算告警，优先收敛为高频子集。

## cc-switch reference update

- 更新：`docs-linhay/references/cc-switch/` 已从 `farion1231/cc-switch` 的 `origin/main` 浅克隆同步到 HEAD `3c3d417457a4c3420139488c19718b7415641584`，上游版本从本地快照 `3.14.1` 更新到 `3.15.0`。
- 方式：保留参考项目目录为源码快照，不在目录内保留 `.git/`；同步后更新 `docs-linhay/references/README.md` 的本地参考项目索引。
- 边界：本轮只刷新外部参考项目源码和索引，不改 GetTokens 业务代码；该目录属于历史遗留已跟踪源码，diff 体量较大。

## Codex live-session diff refresh

- 根因：`#frame=codex&workspace=live-sessions` 的 browser preview/cache 每秒刷新会改变 `generatedAt`、preview 时间戳、active duration、stream duration 等时钟字段；前端原先整包 `setSnapshot(next)`，导致列表、详情和图表引用反复替换，页面表现为持续闪烁。
- 修复：新增 `mergeCodexLiveSessionsSnapshot`，对时钟型变化做结构归一比较；clock-only refresh 直接复用旧 snapshot，局部结构变化时只替换发生变化的 session/request。`CodexLiveSessionsFeature` 的 preview/cache、sidecar unavailable 和真实 Wails snapshot 都统一走差分合并。
- 追加边界：browser preview/cache 下的 detail polling 停止每秒重写 detail state；图表的实时增长由已有 `nowMs` 投影承担。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs` 50/50 通过，`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过，scoped `git diff --check` 通过。in-app browser 对 live-sessions CACHE 页面连续采样 4 次、约 3.6 秒，`来源 CACHE`、会话列表片段、`最新样本#50` 和 `scrollY` 均稳定。
- 沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 与 `docs-linhay/dev/20260523-session-distillation-codex-live-sessions-ui.md`，不升级 `AGENTS.md`，因为这是 Codex live sessions 领域内刷新策略。

## Codex live-session rolling count window

- 用户反馈：请求耗时趋势图的标签仍希望随请求继续向后推进，但窗口数据量应保持恒定上限。
- 修复：`buildCodexLiveRequestTimingTrend` 从固定时间窗过滤改为固定数量上限，默认只保留最新 50 个 request points；browser preview 每 6 秒合成一个新的 latest request，仍保留 50 条请求，因此 `最新样本` 和右侧轴标签会从 `#50` 继续推进到 `#51/#52/...`。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs` 51/51 通过，`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、scoped `git diff --check` 通过。in-app browser 验证首次为 `请求数50 / 最新样本#50`，约 7.2 秒后为 `请求数50 / 最新样本#51`，x 轴右侧标签从 `#50` 推进到 `#51`。
- 沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 与 `docs-linhay/dev/20260523-session-distillation-codex-live-sessions-ui.md`，不升级 `AGENTS.md`。

## Codex live-session sidecar request sequence

- 用户追问：需要看 sidecar 侧的数据发生是否正确，不能只依赖 browser preview/mock 的滚动窗口。
- 真实检查：只读查询正式 `8317` management endpoint 后，多个长会话 `requestCount=50 / requestsLen=50`，但 retained window 的 `firstSeq=1 / lastSeq=50`，说明运行中的 sidecar 会在裁剪后把 retained requests 重新编号。
- 根因：CLIProxyAPI fork 的 live tracker 用 `len(session.requests)+1` 生成新请求序号，且 `pruneLiveSessionRequestsLocked` 在裁剪后按 retained slice 重写 `Sequence=index+1`；history 里最新请求也会被写成错误序号。
- 修复：`docs-linhay/references/CLIProxyAPI/internal/gettokenshooks/live_sessions.go` 新增会话级 `nextRequestSequence`，新请求按会话生命周期单调递增；裁剪只删除旧请求，不重编号；合并 transient session 时保留已有 sequence 并推进 counter。
- 验证：先补红灯测试复现 `req-5` 被重写成 `#1`、`req-54` history 被写成 `#51`；修复后 `go test ./internal/gettokenshooks -run 'TestLiveSessionsPrunesRequestsWithinLongSession|TestLiveSessionsHistoryPersistsTrimmedRequests' -count=1`、`go test ./internal/gettokenshooks -run 'LiveSessions' -count=1`、`go test ./internal/gettokenshooks -count=1` 均通过。再次只读查询 8317 仍是旧二进制行为，未对正式 sidecar 做重启或替换。
- 沉淀：已更新 `gettokens-domain-engineering` 的 Codex Live Sessions 规则，明确 request `sequence` 是 sidecar-owned lifecycle counter，内存保留上限不能重编号。

## Status local CLI casing fix

- 用户反馈：运行状态页多个值被 UI 强制大写，应遵从原始文本大小写。
- 修复：移除共享 `btn-swiss`、`select-swiss` 和 `FieldLabel` 的 `text-transform: uppercase`；状态页本地 CLI 应用区域的 endpoint、动态提示、toggle 文案和 runtime 状态徽标不再套 `uppercase`；中文 locale 中 Provider / Reasoning Effort / Auth Strategy / Model 等文案改回显式原始大小写。
- 测试：新增 `frontend/src/features/status/tests/statusTypography.test.mjs` 并纳入 `npm run test:unit`，覆盖下拉/按钮/字段标签不强制大写、状态页动态值不套 uppercase、中文 copy 保持大小写。
- 验证：`node --test src/features/status/tests/statusTypography.test.mjs`、`npm run typecheck`、`npm run test:unit` 通过；in-app Browser 在 `http://127.0.0.1:5173/#frame=status` 验证 `selectTransform/buttonTransform/fieldLabelTransform/endpointTransform` 均为 `none`，示例文本为 `OpenAI / openai`、`high`、`http://127.0.0.1:18317/v1`。
- 沉淀判断：这是一次具体 UI casing 修复，暂不新增项目 skill，也不升级 `AGENTS.md`。

## Status Codex provider selection source

- 用户反馈：运行状态页 Provider 下拉不应默认 OpenAI；需要遵守本地 `config.toml` 中显式激活的 `model_provider`，只有没有显式激活值时才默认 GetTokens。
- 修复：后端 `LocalCodexModelProviderState` 增加 `hasExplicitCurrentProvider`，区分 Codex 自身缺省 OpenAI 与用户在 `config.toml` 显式配置的 provider；状态页改用 `GetLocalCodexModelProviderStateView`，显式激活值优先于旧 UI localStorage，没有显式值时回落到 `gettokens`。
- 测试：新增 `resolveInitialRelayProviderSelection` 分支测试，覆盖显式 provider 优先、无显式值默认 GetTokens、显式缺失 provider id 仍保留；补充 Go provider state 测试覆盖 `hasExplicitCurrentProvider`。
- 验证：`node --test src/features/status/tests/relayLocalState.test.mjs`、`go test ./internal/wailsapp -run 'LocalCodexModelProvider'`、`npm run typecheck`、`npm run test:unit` 通过；浏览器 `http://localhost:5173/#frame=status` 验证 Provider 当前值为 `GetTokens / gettokens`。
- 沉淀判断：这是状态页本地 Codex 配置同步的具体修复，暂不新增项目 skill，也不升级 `AGENTS.md`。

## Status Codex model selection source

- 用户反馈：运行状态页 Model 名称下拉也应遵守本地 `config.toml` 的显式 `model`，只有没有显式 model 时才默认 GetTokens 的 `GT`。
- 修复：后端 `LocalCodexModelProviderState` 增加 `currentModel` 与 `hasExplicitCurrentModel`；状态页加载本地 Codex state 时同步解析模型，显式 model 优先于旧 UI localStorage，并自动补进下拉选项；无显式 model 时回落到 `GT`。
- 测试：新增 `resolveInitialRelayModelSelection` 分支测试，覆盖显式 model 优先、无显式值默认 `GT`、显式缺失 model 名仍保留；补充 Go provider state 测试覆盖显式/缺省 model。
- 验证：`node --test src/features/status/tests/relayLocalState.test.mjs`、`go test ./internal/wailsapp -run 'LocalCodexModelProvider'`、`npm run typecheck`、`npm run test:unit` 通过；浏览器 `http://localhost:5173/#frame=status` 验证 Model 当前值为 `GT`，选项保留 `GT` 与 `gpt-5.4` 原始大小写。
- 沉淀判断：这是状态页本地 Codex 配置同步的具体修复，暂不新增项目 skill，也不升级 `AGENTS.md`。

## Status Provider label clarity

- 用户反馈：运行状态页 Provider 下拉显示 `OpenAI / openai`、`GetTokens / gettokens` 时，裸 `/` 不清楚前后含义。
- 解释：前半是给人看的 provider 显示名，后半是写入 `config.toml` 的 `model_provider` ID。
- 最终修复：用户进一步要求只显示 `model_provider` 的值；Provider 下拉改为只显示 provider id，例如 `openai`、`gettokens`，不再混入显示名或 `ID:` 辅助文本。
- 验证：新增 `status provider picker labels show only the model_provider id` 测试；`node --test src/features/status/tests/statusTypography.test.mjs`、`npm run typecheck`、`npm run test:unit` 通过；浏览器 `http://localhost:5173/#frame=status` 验证当前值为 `gettokens`，选项为 `openai` 与 `gettokens`。
- 沉淀判断：这是状态页局部展示修复，暂不新增项目 skill，也不升级 `AGENTS.md`。

## Status Provider creation single field

- 用户反馈：运行状态页新增 Provider 弹窗不应继续区分 `Provider ID` 与 `显示名称`；既然下拉只显示 `model_provider` 值，新增配置也应直接让用户输入一个值。
- 修复：`RelayProviderEditorModal` 移除显示名称输入，只保留一个 `model_provider` 输入框；提交时 `providerID` 与内部 `providerName` 都使用同一个输入值，避免重新引入显示名/ID 双轨语义。
- 验证：新增 `status provider creation only asks for model_provider value` 测试；`node --test src/features/status/tests/statusTypography.test.mjs`、`npm run typecheck`、`npm run test:unit` 通过；Playwright 验证弹窗 `inputCount=1`、label 为 `model_provider`、placeholder 为 `例如：gettokens`、无 `显示名称`，label `textTransform=none`。
- 沉淀判断：这是状态页局部展示修复，沿用现有状态页测试与 memory 记录，不新增项目 skill，也不升级 `AGENTS.md`。

## Account card copy/import clarification

- 用户反馈：账号卡更多菜单中“复制 / 复制内容”语义不清，并且复制出的账号内容粘贴回现有账号池时，auth-file 会沿用原文件名，无法直接得到一份新的账号资产。
- 修复：账号卡菜单只保留“复制账号配置”，移除无实际用途的“复制名称”；复制 auth-file 账号配置后再粘贴导入时，会根据现有 auth-file 名自动生成不冲突文件名，避免覆盖原账号。
- 决策：所有类型账号都应允许复制成新账号资产；Codex API Key 即使 `apiKey + baseUrl + prefix` 完全相同，也应作为带独立 `local-id` 的新记录持久化，方便用户导入后再修改配置。
- 验证：`node --test src/features/accounts/tests/accountTransfer.test.mjs src/features/accounts/tests/accountCardInteractions.test.mjs`、`go test ./internal/wailsapp -run 'TestCreateCodexAPIKeyAllowsDuplicateConfigAsSeparateAccounts|TestPersistCodexAPIKeySetKeepsDuplicateConfigWithDistinctLocalIDs|TestMergeCodexAPIKeyInputs|TestUpdateCodexAPIKeyConfigPreservesStableID|TestDeleteCodexAPIKeyAcceptsDerivedConfigIDForStableLocalRecord' -count=1`、`go test ./internal/wailsapp -count=1` 与 `npm --prefix frontend run typecheck` 通过。

## Codex live-session timing averages

- 用户反馈：`#frame=codex&workspace=live-sessions` 中请求耗时趋势下方的“耗时”指标块看起来像最新单次请求值，但该区域应显示趋势窗口平均值。
- 修复：新增 `buildCodexLiveRequestTimingMetricAverages`，基于固定数量趋势窗口计算平均耗时/间隔/速率；active request 的 `totalDurationMs` 仍按 `nowMs` 投影参与平均，历史 stale streaming request 不实时增长。`TimingMetrics` 改为显示该平均结果，最新单请求值仍留在 timeline 和 chart footer。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm run typecheck`、`npm run build` 通过；in-app browser 刷新 `http://localhost:5173/#frame=codex&workspace=live-sessions` 后，最新 timeline 为 `#50 总 8.0s / TTFT 562ms / 首 810ms`，耗时指标块为 `总耗时 7.1s / TTFT 740ms / 首 token 1.2s`。
- 截图：`docs-linhay/spaces/20260527-codex-live-session-controls/screenshots/20260527/codex-live-sessions/20260527-codex-live-sessions-timing-average-after-v01.png`。

## Codex live-session timing summary space

- 决策：后续将 live sessions 的耗时均值从前端临时聚合前移到 sidecar，由 CLIProxyAPI 返回 session 级 `timingSummary`，前端只负责展示并保留旧 sidecar fallback。
- 文档：新建 `docs-linhay/spaces/20260527-codex-live-session-timing-summary/README.md`，明确背景、目标、范围、非目标和 BDD 验收；新增 `plans/implementation-plan-v01.md`，拆分 sidecar、Wails/root DTO、前端模型、UI、浏览器/桌面验收与文档收尾。
- 边界：本期 summary 只覆盖 retained requests 平均值与 active request total 投影，不做 full history / p95 / 任意时间窗口，不展示 payload 或敏感 headers。
- 追加修复：`#frame=accounts&detail=openai-compatible%3Adeepseek` 中“复制失败”的直接原因是浏览器预览环境会拒绝系统剪贴板写入，原实现只依赖 `navigator.clipboard.writeText`；修复为 DOM copy / Web Clipboard / Wails runtime / app-local storage 多级 fallback。系统剪贴板不可用时，账号页会把复制 payload 存到本地 fallback，并在打开“粘贴账号内容”时自动预填，保证复制后可导入为新结构。
- openai-compatible：账号卡复制 payload 增加 `openAICompatibleProvider` 结构，粘贴导入时以 `deepseek #2`、`deepseek #3` 等新 provider 名创建副本，允许完全相同的 `apiKey + baseUrl + prefix` 先复制成功再由用户修改。
- 追加验证：`node --test src/features/accounts/tests/accountClipboard.test.mjs src/features/accounts/tests/accountTransfer.test.mjs src/features/accounts/tests/accountCardInteractions.test.mjs` 24/24 通过，`npm --prefix frontend run typecheck` 通过；in-app Browser 验证 DeepSeek 卡点击“复制账号配置”显示“复制完成”，再打开“粘贴账号内容”时 textarea 自动包含 `gettokens.account-card.v1` openai-compatible payload。
- 用户偏好更新：复制导入遇到同名账号时，不使用 `-copy` 后缀；直接在标题后追加 ` #2`、` #3`。该规则适用于 Codex API Key label、openai-compatible provider name，以及保留 `.json` 后缀的 auth-file 文件名（例如 `codex-auth #2.json`）。
- 追加实现：新增 `resolveNumberedDuplicateTitle`，复制导入时统一从基础标题计算后缀；如果用户再次复制 `deepseek #2` 且已有 `deepseek`、`deepseek #2`，下一个仍为 `deepseek #3`，避免后缀嵌套。
- 追加验证：`node --test src/features/accounts/tests/accountTransfer.test.mjs src/features/accounts/tests/accountCardInteractions.test.mjs src/features/accounts/tests/accountClipboard.test.mjs` 26/26 通过，`npm --prefix frontend run typecheck` 通过。

## Status Codex model legacy GT removal

- 用户追问：运行状态页 Model 下拉中的 `GT` 来源不清楚，不能继续显示历史 UI 兜底值。
- 结论：`GT` 来自前端旧默认 `defaultRelayModelOptions` 和若干 `|| 'GT'` fallback，并非必须来自 `config.toml`；若 `config.toml` 明确配置 model，仍按原始文本显示。
- 修复：默认模型改为 `RELAY_CODEX_DEFAULT_MODEL`（当前 `gpt-5.4`）；旧 localStorage model options 中的 `GT` 会被过滤迁移，过滤后为空则回落到 `gpt-5.4`。
- 验证：`node --test src/features/status/tests/statusTypography.test.mjs src/features/status/tests/relayLocalState.test.mjs`、`npm run typecheck`、`npm run test:unit` 均通过；in-app Browser 验证 `http://localhost:5173/#frame=status` 中 Model 下拉当前值与唯一选项均为 `gpt-5.4`，Provider 下拉只显示 `openai` 与 `gettokens`。
- 截图：`docs-linhay/screenshots/20260527/status/20260527-status-model-fallback-after-v01.png`。

## Status local CLI config session distillation

- 会话整理：本轮把状态页 Provider/Model 下拉的语义边界沉淀为项目级规则：显示和保存必须贴近 `config.toml` 的真实 key/value，UI 不再制造显示名/ID 双轨，也不再把历史 `GT` 兜底当作有效默认模型。
- 写回：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 `Status Local CLI Config`，并补充 `docs-linhay/dev/20260426-relay-service-config-boundary.md` 第 8 节。
- 不升级：该模式属于 Status 本地 CLI 配置域内规则，暂不升级 `AGENTS.md`。
