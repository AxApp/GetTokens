# 2026-06-25

## AntD accounts 段契约收敛

- 分段审查 accounts 相关迁移失败时，确认真实回归为账号详情头部 AccountDetailHeader 被导入但未渲染、auth-file 配置提示缺少稳定 data-auth-file-config-notice 钩子；已恢复详情 layout header 渲染并补齐提示钩子。
- 同步更新 accounts 相关静态测试：不再要求迁移前的 Tailwind/custom button class 常量，改为检查 AntD Button / Dropdown / Popover / Segmented / Alert 结构、稳定 data hook，以及旧 btn-swiss / input-swiss / uppercase 等视觉语言不回流。
- 验证：node --test src/features/accounts/tests/accountCardInteractions.test.mjs src/features/accounts/tests/accountDetailLayout.test.mjs src/features/accounts/tests/accountFilters.test.mjs src/features/accounts/tests/accountListLayout.test.mjs 通过（114/114）；npm run typecheck 通过。
- 沉淀审计：本段是 AntD 迁移静态契约跟随实现更新，未形成新的长期 repo-wide 规则；不更新 AGENTS.md 或新增 skill。

## AntD accounts 残余契约收敛

- 继续分段收敛 account-migration、account card、local CLI apply、deep-link import、OAuth、rotation、openai-compatible、rate-limit rules 等 accounts 残余静态失败。
- 处理方式保持为测试契约跟随 AntD 迁移后的真实结构：旧 ButtonClass / badge class 断言改为 AntD Button / Tag / Input / Select 组件、稳定 data hook 和旧视觉语言不回流断言；本段未发现需要追加的运行时代码修复。
- 验证：node --test src/features/account-migration/model.test.mjs src/features/accounts/tests/accountCardLayout.test.mjs src/features/accounts/tests/accountLocalCliMapping.test.mjs src/features/accounts/tests/accountOAuth.test.mjs src/features/accounts/tests/accountRotation.test.mjs src/features/accounts/tests/openAICompatible.test.mjs src/features/accounts/tests/rateLimit.test.mjs 通过（115/115）。
- 沉淀审计：仍属于 AntD 迁移测试契约同步，不新增长期规则或 skill。

## AntD usage / channel 段契约收敛

- 分段收敛 UsageDesk、ProjectCandidatePoolRulesPanel、ChannelRoutingWorkbench 迁移失败。确认真实回归为投影用量索引 Dropdown 菜单未渲染 action.description，导致刷新/重建影响范围说明丢失；已恢复到菜单项 label 中展示描述。
- 其余失败为静态契约仍绑定旧自定义 Space/select class 或旧 evidence DOM；已改为检查 AntD Dropdown / Segmented / Card / Select 结构、稳定 data hook 和旧视觉语言不回流。
- 验证：node --test src/features/accounts/tests/usageDesk.test.mjs src/features/channel-routing/tests/channelRouting.test.mjs 通过（87/87）；npm run typecheck 通过。
- 沉淀审计：本段仍是 AntD 迁移契约同步加单点 UI 文案回归修复，未形成新 repo-wide 规则。

## AntD Claude / Codex / status / session 段契约收敛

- 分段收敛 Claude Code account/extensions、Codex binary/live sessions/account order/route probe、status config、legacy account detail 和 session-management 静态契约失败；测试断言从旧自定义 button/input/filter class 常量改为 AntD Button/Input/Checkbox/Tag 结构、稳定 data hook 和旧视觉语言不回流。
- 确认并修复 session analysis 可访问性回归：header 分析入口和 detail 返回选择按钮不再用 disabled 移除焦点目标，改为点击 guard + aria-disabled，保持 modal 关闭/加载期间焦点恢复链路稳定。
- 验证：npm run test:unit 通过（990/990）；npm run typecheck 通过。
- 沉淀审计：本轮模式仍是 AntD 迁移测试契约同步与两处焦点可访问性修复，未形成新的长期 repo-wide 规则；不更新 AGENTS.md 或新增 skill。

## AntD 遗漏测试与 quota reset 视觉收口

- 继续逐页逐文件排查 UI 测试覆盖，发现 25 个 `.test.mjs` 未纳入 `frontend/package.json` 的 `test:unit`；已追加到主单测脚本，避免 Claude Code settings/subagents、extension registry、vendor status、proxy pool、quota runtime 等页面继续漏测。
- 真实 UI 清理：OpenAI quota reset modal 移除动态渐变、glass 背景和自定义 keyframes，收敛为 quiet workspace shell + AntD Button 控件；同时补齐 Claude Code settings/subagents/memory row 的 canvas 背景。
- 同步更新遗漏测试中的旧自绘 class 断言，改为 AntD Button/Input/Checkbox/Upload/AutoComplete 结构和稳定 data hook。
- 验证：npm run test:unit 通过（1131/1131）；npm run typecheck 通过。
- 沉淀审计：本段新增的稳定动作是“新增/遗留 test.mjs 必须纳入 test:unit”，已通过 package 脚本固化；不额外更新 AGENTS.md 或新增 skill。

## AntD 迁移审查止血

- 对未提交的 AntD 前端迁移 diff 做 review 后，先止血 5 个确定回归：`ModalFrame` 恢复 footer 与 body 滚动、账号分组删除恢复确认后调用 `onDeleteGroup(group.accounts)`、`Combobox` 同步外部受控 `value` 并恢复大小写无关去重、账号详情恢复 `runtime` 模块与 `AccountRuntimeRouteSection` 渲染、cURL 编辑器恢复原生 `textarea` 光标插入链路。
- 验证：`npm run typecheck` 通过；聚焦测试 `modal frame constrains|combobox keeps|account group menu exposes|auth-file modules|api-key edit modules|runtime route section|curl editor variable buttons` 通过。全量 `npm run test:unit` 仍失败，剩余为本轮接手前已有的大批 AntD 迁移静态/布局契约未同步项，需另开迁移收敛轮处理。
- 沉淀审计：这是一次 review 后的定点回归修复，未形成新的 repo-wide 规则；不更新 `AGENTS.md` 或新增 skill。
