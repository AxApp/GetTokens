# 2026-05-22

## 统一账号详情系统
- 决策：账号详情统一先走 `AccountDetailPrimitives` + 共享壳层，不合并 Accounts / Codex / Claude Code 的 controller。
- 进展：`UnifiedAccountDetailModal`、`OpenAICompatibleDetailModal` / `OpenAICompatibleDetailPanel`、`CodexAccountDetailModal` 已统一到同一详情视觉语言；Codex 详情被 Claude Code 复用，因此 Claude Code route row 详情同步受益。
- 补充：卡片模式已有的近期请求、token、cached token、平均延迟、quota 窗口与 balance 已进入详情页 `AccountRuntimeSnapshotSection`；Codex 排序卡片与 Codex 详情共用 `buildCodexQuotaSummaryAccount`，避免 quota 映射分叉。
- 设计系统：账号详情模块布局固定为 `AccountDetailBody -> AccountRuntimeSnapshotSection -> AccountDetailModuleGrid/Stack -> AccountDetailSection`，并用 `AccountDetailSectionDensity` 管理 standard/dense/hero 密度；OpenAI-compatible 与 Codex 详情已纳入该布局。
- 交互：Codex 模型映射保存移到弹窗 footer，限流规则和模型映射均不再在模块内自带独立保存按钮。
- 沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 Account Detail Surfaces 规则；该约束属于账号域，不上升到 repo-wide `AGENTS.md`。
- 边界：capability registry 只适合作为下一阶段模块选择层，不能接管 Wails 调用、保存编排或 hash 同步。
- 验证：`node --test frontend/src/features/design-system/storyCatalog.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`（390 passed）、`git diff --check` 已通过。

## Codex 运行会话正式版 mock 回退修复
- 根因：`CodexLiveSessionsFeature` 在 Wails 桌面环境也用 preview snapshot 初始化；真实 `GetCodexLiveSessionsSnapshot` 请求失败时 catch 分支按 `current.sessions.length` 把 preview rows 保留为 `cache`，导致正式版旧 sidecar / 404 场景显示 mock 数据。
- 修复：新增 `codex-live-sessions/model/snapshotState.ts`，明确浏览器 preview 与桌面真实链路状态边界；桌面初始和失败状态使用空的 `unavailable` snapshot，只有此前成功返回过 `source: live` 的真实 rows 才允许降级成 `cache`。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run typecheck`、`go test ./internal/wailsapp -run CodexLive` 已通过。

## 账号卡片 Quota 重置时间展示
- 修复：`QuotaBars` 在额度条下方显示 `accounts.quota_reset` 与格式化后的重置时间，数据来源为 quota window 的 `resetLabel/resetAtUnix`，并复用 `formatQuotaResetDisplayWithUnix` 避免反解析展示文案。
- 验证：先新增 `accountCardLayout.test.mjs` 回归断言确认红灯，再实现；`node --test frontend/src/features/accounts/tests/accountCardLayout.test.mjs`、`npm --prefix frontend run typecheck` 已通过。

## 路由守卫规则详情收敛
- 决策：`RateLimitRulesSection` 不再自带详情分区边距、行内保存按钮、标签输入框或卡中卡布局；规则编辑跟随账号详情页面 footer 保存。
- 实现：限流规则改为单层横向行，支持自然日窗口 `calendar-day`，UI 展示 `00:00-23:59`；本地 CLIProxyAPI 参考源按本地自然日 00:00 计算该窗口。
- 验证：`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`（385 passed）、`go test ./internal/gettokenshooks`、`git diff --check`、`git -C docs-linhay/references/CLIProxyAPI diff --check` 已通过。

## Codex 运行会话按 conversation_id 收拢
- 决策：Codex live sessions 的主聚合 key 改为真实 `conversation_id` / `ThreadId`，从请求中的 `session_id`、`prompt_cache_key`、`x-codex-window-id`、`x-client-request-id` 抽取；`passthroughSessionID` 仅作为 sidecar 临时 downstream / execution 诊断字段。
- 实现：CLIProxyAPI fork 的 `gettokenshooks` 新增 identity 抽取与临时 session 迁移合并逻辑；downstream WebSocket handler、Codex WebSocket executor、Codex HTTP executor 均把 headers/body identity 写入 tracker。
- 验证：`go test ./internal/gettokenshooks ./internal/runtime/executor ./sdk/api/handlers/openai` 已在 `docs-linhay/references/CLIProxyAPI` 通过。

## Codex 运行会话页面整理
- 决策：`CodexLiveSessionsWorkbench` 只作为页面编排层；会话列表、右侧诊断详情、顶部统计和格式化工具拆为独立组件/工具文件，继续沿用 Swiss-industrial 工作台视觉。
- 交互：页面按 conversation 会话列表 + 固定详情面板收拢，不再在列表行内展开详情；选中逻辑使用 `getSelectedCodexLiveSession`，显式 `conversation_id` 失效或未选择时回落到当前过滤结果第一条。
- 边界：正式版真实数据链路不变，mock 仅用于 preview / Storybook；本轮没有改 sidecar 聚合协议。
- 验证：`npm --prefix frontend run typecheck`、`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run build`、`git diff --check` 已通过；Playwright / Chrome DevTools 页面验收因审批超时未完成。

## Codex 请求顺序卡片复用账号卡样式
- 决策：`#frame=codex&workspace=account-list` 非列表密度下不再维护独立大块账号卡布局，直接复用账号页共享 `AttributionCard` 的头部、指标、quota 与 tone 体系；Codex 专属拖拽、启停、路由、模型映射摘要和策略控件只放入底部 footer 操作栏。
- 实现：移除 `AccountOrderRow` 的 `customBody` 工作台区域，新增 `CodexAccountSpecialActionBar`；补源码结构测试，确保后续仍使用 `AttributionCard` + `footer`，不回退到自定义卡片主体。
- 验证：`npm --prefix frontend run test:unit -- src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过；Playwright CLI 已生成桌面与移动截图到 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260522/codex/`。

## Codex 请求顺序卡片共享账号信息
- 根因：Codex 页复用账号卡时只复用了视觉组件，`CodexAccountRow -> AccountRecord` 的转换丢失了 `quotaCurl`、`quotaEnabled`、`billingCurl`、`billingEnabled` 等字段，导致账号页额度/账单模块判断为 unsupported。
- 决策：`#frame=codex&workspace=account-list` 与 `#frame=accounts` 的账号卡不仅共享样式，也共享具体账号信息；Codex 特殊操作继续放在底部操作栏或右上角菜单，不进入账号卡主体。
- 实现：Codex row 保留账号原始元数据，`buildCodexQuotaSummaryAccount` 回填完整 `AccountRecord`，Codex controller 和 row 统一使用该转换，并将 billing 传入共享 `AttributionCard`。
- 验证：`node --test frontend/src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck` 通过；Playwright CLI 等待 `BALANCE` 出现后截图归档到 `docs-linhay/spaces/20260511-codex-account-list-tab/screenshots/20260522/codex/20260522-codex-account-list-shared-quota-after-v01.png`。

## 账号卡片应用到 Codex API Key 模式修复
- 根因：账号卡片“应用到 Codex”确认弹窗和实际写入在 `replace_auth_with_apikey` 模式仍使用 GetTokens relay key 与 relay endpoint，导致预览和写入内容不是当前 Codex API key 账号自身内容。
- 决策：Codex API key 模式固定写入当前账号资产的 `apiKey` 与格式化后的上游 `baseUrl`；OAuth/auth-file 模式继续写 ChatGPT OAuth auth.json；Claude Code 仍使用 GetTokens relay key。
- 实现：`AccountCliApplyDraft.codex` 增加 `apiKey`，确认弹窗 diff 与 `ApplyRelayServiceConfigToLocalV2` 调用都改用当前账号 key；后端 DTO 增加 `skipRelayKeyMetadata`，避免把账号池 API key 误记为 relay key last-used。
- 验证：`npm --prefix frontend run test:unit -- src/features/accounts/tests/accountLocalCliMapping.test.mjs`、`npm --prefix frontend run typecheck`、`go test ./internal/wailsapp -run 'TestApplyRelayServiceConfigToLocalV2|TestApplyRelayServiceConfigToLocal'`、`./scripts/wails-cli.sh generate module` 已通过。
