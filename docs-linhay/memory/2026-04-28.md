# 2026-04-28

- 账号轮动修复：`auth-file` 调整 priority 的实现原先是“下载内容 -> 改 priority -> 删除旧文件 -> 重新上传”，会把 sidecar metadata 里的 `disabled` 状态冲掉。本轮已在 `updateAuthFilePriority` 中先读取原始 `disabled`，重传后自动补一次 `SetAuthFileStatus(true)`，保证“禁用账号保留在轮动位置，但不参与实际轮动”。
- 已新增 Go 回归测试 `TestUpdateAuthFilePriorityPreservesDisabledStatus`，防止后续再次因为 auth-file 重传链路丢失禁用态。
- 账号轮动弹窗现已把这条语义显式展示到 UI：禁用资产会显示 `已禁用 / Disabled` badge，副文案改为“已禁用，保留当前位置但不参与轮动”；对应前端模型已新增 `buildRotationParticipationSummary` 和单测，避免文案与后端行为再次分叉。
- `AccountRotationModal` 当前已补启用/禁用控件，但边界刻意收紧为 `auth-file`：右侧按钮直接调用 `SetAuthFileStatus(name, disabled)`；`codex api key` 和 `openai-compatible provider` 暂不显示切换按钮，因为现有 Go/Wails/sidecar 只支持 auth-file 的 disabled 状态写回。
- 这条边界随后已被打通并上升为统一账号动作：后端新增 `SetAccountDisabled(id, disabled)`，按 `auth-file / codex-api-key / openai-compatible` 分发到各自存储或 management 配置；`CodexAPIKeyInput`、`OpenAICompatibleProvider` 与统一 `AccountRecord` 现都带 `disabled`，轮动弹窗右侧可直接切换三类账号状态。
- 聚合轮动弹窗的 reload 链路也已补齐：保存排序或切换状态后，会同时刷新 `accounts` 与 `openai-compatible providers`，避免 provider 在 modal 内状态更新后、关闭重开又回退成旧值。
- `openai-compatible` 独立工作区随后也已与这条统一账号动作对齐：provider 卡片直接显示 `已禁用 / Disabled` badge 和“保留当前位置但不参与轮动”提示，底部操作区新增启用/禁用按钮，调用同一条 `SetAccountDisabled("openai-compatible:<name>", disabled)` 链路，不再只允许在轮动弹窗里切状态。
- 账号轮动弹窗补充额度摘要展示：`AccountRotationModal` 现在直接消费 `codexQuotaByName`，在拖拽按钮内展示最长 quota window 的剩余百分比和重置倒计时；非 quota 资产明确显示“不跟踪额度”。
- 为额度摘要新增前端单测，覆盖 longest window 优先级、loading 态和 unsupported 态，避免轮动视图与账户卡片的额度语义再次分叉。
- Codex API Key 名称已从前端 `localStorage` 别名切回后端本地存储：创建时直接写入 store，详情页重命名走新的 `UpdateCodexAPIKeyLabel` Wails 接口，列表 `displayName` 以后台返回值为准。
- 为避免已有用户丢失自定义名称，`accounts` 页面加载时会尝试把旧版 `gettokens.apiKeyLabels` 迁移到后端 store；迁移成功后清理旧前端缓存，后续不再依赖本地覆盖。

## OpenAI-Compatible Single-Key Provider Flow
- `openai-compatible` 当前产品形态已进一步收口为“1 张 provider 卡片 = 1 个 provider + 1 个 apiKey”，不再在前端暴露多 `apiKey entries` 编辑；detail modal 只保留 `name / baseUrl / apiKey / headers 文本 / models-alias / verify`。
- `headers` 编辑已从动态行收口为单个文本框，规则固定为“一行一个 `Key: Value`”；示例文案已直接写进 UI，减少用户对 header 结构的猜测。
- provider 名称编辑已移到 detail modal header 区，与验证摘要并列；详情页不再分 tab，单页即可完成编辑、拉取模型和验证。
- detail 打开后会自动尝试拉取远端 `/models`；拉取结果按 provider 配置签名缓存，配置不变时按天复用，手动刷新可强制更新。
- `workspace` 卡片上的 `MODELS` 数和验证状态现在只消费“与当前已保存 provider 配置签名一致”的缓存，未保存草稿触发的拉取或验证结果不会再污染列表卡片。

## Reference Usage Study
- 已新建研究空间 `docs-linhay/spaces/20260428-nolon-codex-usage/`，用于沉淀“参考项目里的 Codex 用量统计是如何实现的”。
- 已校正研究对象：真实 `nolon` 仓库位于 `/Users/linhey/Desktop/FlowUp-Libs/nolon`，且其自身 `docs-linhay/` 已沉淀 Codex usage 设计文档；此前基于 `cc-switch` 的链路判断不再作为本次结论。
- 当前真实链路：`rollout/session JSONL -> CodexSessionEventParser.reduceUsageLine -> CodexSessionUsageIndex(SQLite, usage-index-v1.sqlite) -> CodexSessionStore projected minute usage -> CodexTokenTrendService / CodexIntradayUsageService -> Provider Usage / Codex Sessions`。
- 关键边界：`Codex Usage` 是全局口径，会主动移除 `CODEX_HOME`；首屏缓存使用 full snapshot，而不是不完整的 day cache；手动刷新当前真实实现按 `affectedDayKeys` 做受影响日期回填。
- 通过本轮多方补证，已确认 `usage-index-v1.sqlite` 至少包含 `usage_entries`（文件级索引）和 `session_usage_minutes`（分钟级投影）两张关键表；`requestCount` 的口径是“1 个产生非零 token delta 的 `token_count` 事件 = 1 次 request”。
- 已发现一处明确文档偏差：设计文档把手动刷新描述成“只回填最近两天”，但当前代码实际按 `affectedDayKeys` 回填所有受影响日期。
- 已新增协作文档：`docs-linhay/spaces/20260428-nolon-codex-usage/debate/20260428/codex-usage-collab-detailing/20260428-codex-usage-collab-detailing-v01.md`。
- 已完成文档校验：`docs-linhay/scripts/check-docs.sh`。

## GetTokens Usage Dual Source Debate
- 已新建需求空间 `docs-linhay/spaces/20260428-gettokens-usage-dual-source/`，专门讨论 GetTokens 的双轨用量统计设计。
- debate 已达成共识：GetTokens 后续应拆成三条并列信息，而不是一个“用量大桶”：
  - `ObservedRequestUsage / 真实请求用量统计`
  - `LocalProjectedUsage / 本地用量统计`
  - `QuotaSnapshot / 配额快照`
- 当前 `GetUsageStatistics` 继续只代表 sidecar `/usage` 观测链路；它应被重新命名和展示为“请求观测/稳定性”，而不是 provider 全量消耗。
- `quota` 继续独立域；即便底层有时吃 cached usage snapshot，最终语义仍只是额度窗口，不属于 usage fact。
- 已按本机 `~/.codex` 样本复核：`session_meta / turn_context` 里只有 `cwd / git / model_provider / source / model` 等字段，没有可稳定映射到 GetTokens `auth-file` 的 `authIndex / account_id / email / token` 类标识，因此 `Codex local usage` 当前无法归因到 `auth-file`；首版只能做 provider/global 级视图。
- 已进一步收紧本地 usage 边界：`LocalProjectedUsage` 必须先按 `provider` 分域统计，不能把 `Codex / Claude / Gemini` 这类本地事件混成一个总桶；即使首版只落地 `Codex`，数据模型、Wails API 和前端状态也必须保留 `provider` 维度。
- 前端 IA 已进一步裁定：v1 入口仍放在 `Accounts`，但要新增独立的 `Usage Desk` 子工作区或等价页面级分析区；账号卡片继续只展示资产状态、`recent health` 与 quota，不承载本地 usage 明细或趋势图。
- 已在当前 space 根目录落一份 `HTML` 设计稿：`usage-dashboard-design-v01.html`，用于承载 `provider` 过滤、`sourceKind` 分段切换和双轨用量的独立分析面。
- 已归档 debate：`docs-linhay/spaces/20260428-gettokens-usage-dual-source/debate/20260428/gettokens-usage-dual-source-debate/20260428-gettokens-usage-dual-source-debate-v01.md`。
- 已新增前端页面规划 debate：`docs-linhay/spaces/20260428-gettokens-usage-dual-source/debate/20260428/gettokens-usage-dual-source-debate/20260428-gettokens-usage-dual-source-debate-v02.md`。

## Accounts Aggregate Workspace
- 账号池侧边栏主入口现在显式落到新的 `all` workspace，而不是默认跳进 `codex` 子页；这个聚合页负责承接“父级入口 = 子 workspace 汇总视图”的语义。
- `all` workspace 当前聚合两块内容：`codex` 凭证资产列表与 `openai-compatible providers` 列表；即使 `codex` 过滤结果为空，也继续展示 provider 区块，避免父级页被误解为“只有 codex”。
- 账号轮动入口已从 `codex` 子页头部移除，只保留在 `all` 聚合页；轮动逻辑仍只作用于现有可轮动的 `codex` 账号资产，不把 provider 配置错误地混入轮动集合。

## OpenAI-Compatible Rotation Support
- 已确认 CLIProxyAPI 原生支持 `openai-compatibility[].priority`，并且会在配置合成阶段写入统一 auth scheduler 的 `priority` 属性，因此 provider 级 priority 可以真正参与后端路由选择。
- GetTokens 现已补上这一层桥接：`UpdateAccountPriority` 新增支持 `openai-compatible:<name>` 资产前缀，并通过新的 `UpdateOpenAICompatibleProviderPriority` 写回 management `openai-compatibility` 配置。
- 前端轮动弹窗现在会把 `openai-compatible provider` 映射成可排序轮动资产，与现有 `auth-file / codex-api-key` 一起展示和保存；这里接入的是 provider 级 priority，而不是 provider 内部多 key entry 的独立排序。

## Session Handoff
- 本轮会话后半段已确认：当前主需求空间应收敛到 `docs-linhay/spaces/20260428-gettokens-usage-dual-source/` 与 `docs-linhay/spaces/20260428-nolon-codex-usage/`，后续新会话不应再把 `account-pool` 视为本轮主 space。
- 已发生过一次方向偏航：把账号池/轮动文案问题误当成主任务；随后已纠正，当前应以 `20260428-gettokens-usage-dual-source/usage-dashboard-design-v01.html` 作为直接修改对象继续推进设计稿。
- `usage-dashboard-design-v01.html` 已完成一轮“中文设计稿口径”收口，主要是把明显的英文 UI 标题和控件文案改成中文，同时保留 `ObservedRequestUsage / LocalProjectedUsage / QuotaSnapshot / provider` 这类概念锚点，便于后续继续讨论信息架构与实现映射。
- 下轮会话建议入口：先对齐 `20260428-gettokens-usage-dual-source/README.md`、`usage-dashboard-design-v01.html` 与 `20260428-nolon-codex-usage/README.md` 三者口径，再决定是继续打磨设计稿，还是开始把 `Usage Desk` 落成代码。

## Usage Desk Workspace Implementation
- `Usage Desk` 已作为新的 `accounts` 子 workspace 真正接入前端应用壳子：`AccountWorkspace`、sidebar 子入口、localStorage 持久化与 `AccountsFeature` 分支均已支持 `usage-desk`。
- 现阶段策略是“保留现有全局 sidebar，主内容做成新页面”：新增 `frontend/src/features/accounts/components/UsageDeskWorkspace.tsx`，不复用旧账号卡片栅格，而是落成新的主舞台布局。
- 当前实现沿用了现有 Swiss/硬边工具风格：左侧是页面内的账号池子 rail，右侧是新的 `用量分析` 页面，包含来源切换、多日统计、单日分钟级下钻、带顶点数值和面积阴影的 SVG 图表，以及联动明细表。
- 当前 `Provider` 不做单独切换，统计归属跟随页面内账号池子 rail；时间范围切换放在图表模块内部，与设计稿最终裁定一致。
- 本轮已补充自动化验证：`pagePersistence` 单测新增 `usage-desk` workspace 覆盖；`frontend` 单测全绿，`npm run typecheck` 通过。
- `Usage Desk` 现已从静态 mock 推进到“真实请求量真数据驱动”：新增 `frontend/src/features/accounts/model/usageDesk.ts`，直接消费 `GetUsageStatistics -> usage.apis.*.models.*.details`，聚合出多日统计与单日分钟级下钻所需的 `dailyPoints / minutePoints / minuteRows`。
- 当前 `UsageDeskWorkspace` 已接上真实 `ObservedRequestUsage`：进入页面后会实际拉取 sidecar `/usage`，显示加载态、错误态、无数据态，并用真实样本渲染多日趋势图、单日分钟图和联动明细表。
- `LocalProjectedUsage` 本轮没有伪装成已接入；页面明确展示“未接入独立 DTO”的占位态，后续必须新增 provider 级本地 usage API，而不是继续复用 sidecar request health 链路。
- `LocalProjectedUsage` 随后已完成首版真实接入：新增 Wails 接口 `GetCodexLocalUsage`，后端直接扫描 `~/.codex/sessions/**/*.jsonl`，按 `event_msg.payload.type == token_count` 解析 `total_token_usage / last_token_usage`，规约成 token delta，并回传 provider/global 级明细。
- 当前 `GetCodexLocalUsage` 明确边界：`provider = codex`、`sourceKind = local_projected`、不做 `auth-file` 归因、不落 SQLite 索引；前端 `Usage Desk` 已接上这条真实链路，并用同一套多日趋势 / 单日分钟级 / 明细表 UI 展示本地投影用量。
- 本轮已补验证：`go test ./internal/wailsapp/...`、`npm run test:unit`、`npm run typecheck` 全部通过。
- 参考 `nolon` 的优化方向后，`GetCodexLocalUsage` 已正式切到可丢弃 SQLite 索引：当前落在 `~/.config/gettokens-data/codex-local-usage/usage-index-v1.sqlite`，并已跑通 `fileMissing / cacheHit / deltaAppend / fullRebuild` 四个出口，避免每次都全量重扫全部 rollout。
- 当前 SQLite 真源又进一步收口为 `usage_entries + session_usage_minutes` 两张表；`H5` 继续消费 `GetCodexLocalUsage.details`，但这些 `details` 已经是分钟投影回放结果，不再是原始事件级明细。
- 本轮新增 Go 单测已覆盖：首次 `fullRebuild`、重复读取 `cacheHit`、文件 append 后 `deltaAppend`；并且仓库 Go 语言基线已放宽到 `1.25.0`，用于承接 `modernc.org/sqlite`。
- `Usage Desk` 的 `H5` 现已补上本地投影侧的显式操作入口：`刷新索引` 直接重新拉取 `GetCodexLocalUsage`，`重建索引` 走新的 `RebuildCodexLocalUsage`；页面同时展示 `cacheHit / deltaAppend / fullRebuild / fileMissing` 统计，便于观察 APP 层本地索引行为。
- 本轮已补验证：`go test ./internal/wailsapp/...`、`npm run test:unit`、`npm run typecheck` 全部通过。
- Wails 绑定层补了一条硬约束：`internal/wailsapp.App` 新增方法后，若仓库根 `app.go` 不同步添加 wrapper，Wails 只会继续绑定旧的 `main.App` 导出，`H5` 在 `vite build` 阶段会直接报 `"... is not exported by wailsjs/go/main/App.js"`。本轮已为 `GetCodexLocalUsage / RebuildCodexLocalUsage` 补齐根层 DTO 与转发方法，并验证 `./scripts/wails-cli.sh build` 成功。
- `Usage Desk` 的页面层级已重新裁定：它与 `账号池` 是主导航同级，不再作为 `accounts` 子 workspace。原因有两层：
  - 产品信息架构上，`Usage Desk` 是独立分析面，不属于账号池子子项
  - 技术实现上，之前把 `usage-desk` 放进 `AccountsFeature` 的条件提前返回分支，切页时会改变 hooks 调用数量，触发 React 运行时错误并导致黑屏
- 本轮已完成修正：`AppPage` 新增 `usage-desk`，`AccountWorkspace` 移除 `usage-desk`，Sidebar 改为主导航入口，`App.tsx` 直接渲染 `UsageDeskWorkspace`，`AccountsFeature` 不再承载该页面。前端回归通过：`npm run test:unit`、`npm run typecheck`、`npm run build`。
- 侧边栏导航现已和 URL hash 绑定，统一采用 `#frame=...`：
  - 顶级页：`#frame=status|accounts|usage-desk|settings|debug`
  - 账号池子子页：`#frame=accounts&workspace=codex|openai-compatible`
  - 默认账号池子：`#frame=accounts`
- `App.tsx` 初始化会优先从 hash 恢复页面状态，状态切换会回写 hash，并监听 `hashchange` 支持浏览器前进/后退；localStorage 继续只作为无 hash 时的回退。
- `LocalProjectedUsage -> auth-file` 归因边界已从“当前未做”提升为“按现有本地事实源不可实现”：
  - 当前 `~/.codex/sessions` rollout 样本没有可稳定 join 到 GetTokens 账号资产的标识
  - 因此这不是一个普通 backlog，而是明确约束
  - space README 已补充“注意事项”，要求产品、接口和 H5 文案都不得暗示账号级精确归因
- 已重新核对 `CLIProxyAPI` 上游到 `2026-04-28 / v6.9.40`：`/v0/management/usage` 仍然只是内存快照，核心仓库没有把 `ObservedRequestUsage` 接入 PostgreSQL 或其他自动持久化层；现有内建能力仍是 `usage export/import`。
- 上游 README 也把“使用量持久化 + SQLite + dashboard”描述为独立外围项目能力，而不是 `CLIProxyAPI` 核心内建能力；因此 GetTokens 若要多日稳定 `ObservedRequestUsage`，不能假设升级上游后会自动获得这条能力。
- `CLIProxyAPI` fork 的 observed usage 持久化边界已正式收敛为“核心仓库一个薄接入点 + `internal/gettokenshooks/` 独立实现目录”：
  - 不把大改动散落到 `internal/usage/`、management handler、config/store 体系
  - `cmd/server/main.go` 只在 `UsageStatisticsEnabled` 打开时调用一次 `gettokenshooks.InstallUsagePersistenceHook(...)`
  - 实际 SQLite 入库与启动回灌集中实现于 `docs-linhay/references/CLIProxyAPI/internal/gettokenshooks/usage_persistence.go`
- 当前 hook 首版采用 `usage_snapshots` 单表 + snapshot JSON blob 持久化：
  - 启动时从 SQLite 读最近 snapshot，并 `MergeSnapshot` 回灌到内存
  - 运行中通过 usage plugin 订阅请求记录，并 debounce 回写当前内存 snapshot
  - 同时增加低频周期 flush，覆盖 `/usage/import` 这类只改内存、不产生新 request record 的场景
  - 目标是先解决 `ObservedRequestUsage` 跨重启/多日恢复，同时把 merge-upstream 冲突面控制在最小范围
- `ObservedRequestUsage` 的分层责任已进一步固定：
  - `CLIProxyAPI` 保持时间级 request history 与 snapshot 持久化
  - `APP 层` 负责多日统计、单日分钟级聚合和日内下钻
  - 不把分钟桶回写到 `CLIProxyAPI`
- 本轮已验证 `CLIProxyAPI` fork：
  - `go test ./cmd/server/... ./internal/gettokenshooks/... ./internal/usage/...`
  - `./scripts/build-sidecar.sh darwin arm64 build/bin`
  均已通过
- 已完成运行态验证：
  - 用真实 `build/bin/cli-proxy-api` 启动临时 sidecar
  - `POST /v0/management/usage/import` 导入一条 `2026-04-27T06:20:00Z` usage 样本
  - 等待 hook flush 后确认 SQLite 文件生成
  - 停服务重启后，启动日志已出现 `restored usage snapshot`
  - 再次读取 `/v0/management/usage`，历史样本仍然存在
- sidecar 运行态又补了一条隔离约束：`dev` 与正式 app 不能再共用 `8317` 和 `~/.config/gettokens`
  - 新增 `internal/sidecar/profile.go`
  - `GETTOKENS_APP_PROFILE=dev` 或 dev 可执行名会让 `sidecar.Manager` 走 `18317`
  - dev 配置目录改为 `~/.config/gettokens-dev`
  - `scripts/wails-cli.sh dev` 会自动注入 `GETTOKENS_APP_PROFILE=dev`
  - `go test ./internal/sidecar/...` 已覆盖 `NewManager()` 在 dev profile 下的端口初始化
- dev sidecar 二进制解析又补了一层：dev 可执行不再优先吃 app bundle 内陈旧的 `cli-proxy-api`，而是优先指向 `build/bin/cli-proxy-api`
  - 位置：`internal/sidecar/process_support.go`
  - 原因：`wails dev` 打出来的 `build/bin/GetTokens.app/Contents/MacOS/cli-proxy-api` 可能落后于当前 fork 源码
  - 结果：新起的 dev 进程已确认使用 `build/bin/cli-proxy-api -config ~/.config/gettokens-dev/config.yaml`
- 已完成 dev 会话联调：
  - dev `GetSidecarStatus.port == 18317`
  - 向 `18317` 导入一条 `2026-04-27T15:22:00+08:00` observed usage 样本
  - `34115/#frame=usage-desk` 现已正确显示 `04-27`、`1 success`、`15:22` 分钟级明细
  - 调试面板已确认 `GetUsageStatistics` 的 Wails 响应体与 `18317 /usage` 一致
- 当前遗留真实问题：`GetCodexLocalUsage` 在 dev 调试面板中仍报 `bufio.Scanner: token too long`；这已从“联调环境问题”收敛为单独的本地 projected usage 解析问题。
- `GetCodexLocalUsage` 的超长 JSONL 行问题已修复：`APP 层` 的本地 rollout 解析从 `bufio.Scanner` 切到 `bufio.Reader.ReadBytes('\n')`，不再受 4MB token 限制；已新增超长 `token_count` 行回归测试，并在 dev `#frame=usage-desk` 联调确认 `本地投影用量` 正常显示多日与分钟级明细，不再出现“本地投影用量暂时不可用”。
- `Usage Desk` 的侧边栏子页状态现已补齐 URL 与持久化联通：`codex / gemini` 现在走 `#frame=usage-desk&workspace=<codex|gemini>`，并新增本地存储恢复；这条联通已经过前端单测、`vite build` 与真实浏览器点击验证，点击 `用量分析 > GEMINI` 后地址会更新为 `#frame=usage-desk&workspace=gemini`。
- `Usage Desk` 图表组件已补齐两条展示约束：点位数值必须带单位，且长时间轴图表必须支持横向滑动。当前 `真实请求量` 图表统一显示 `次`，`本地投影用量` 图表统一显示 `tokens`；`ChartSurface` 已改成可横向滚动容器，并通过 `pointCount * 78px` 动态扩展宽度，避免“全部 / 30D”视图被硬压缩。
- `Usage Desk` 的图表交互已进一步收口为“一张图两种状态”：默认不再自动选中最新一天，而是直接显示多日按日统计；只有用户点击某个具体日期后，图表区域才原位切换成该日分钟级，并展开分钟明细表。`多日统计` 按钮现在也承担“返回日视图”的职责，避免同一来源下并排出现两张重复图卡。

## ApiKeyDetailModal Relay Boundary
- `ApiKeyDetailModal` 本轮已明确收口为“上游 API key 资产编辑 + 验证”面板，不再提供单账号 `auth.json / config.toml` 配置片段导出；原因是 GetTokens 当前对外语义是 relay service，而不是让客户端直接消费单个上游账号。
- `recent health` 已从 `MANAGEMENT` 右侧卡片上移到 modal header 下方，形成头部状态带；`recent failure / average latency` 收到该状态带右下角，正文区只保留 `apiKey / baseUrl` 编辑与验证区。
- 本轮已做截图验收：`docs-linhay/screenshots/20260428/accounts/20260428-accounts-api-key-detail-modal-after-v03.png`。
- 会话蒸馏判断：这次沉淀的是单组件产品边界与布局收口，不是跨任务流程，因此仅写入 `docs-linhay/dev/20260428-api-key-detail-modal-relay-boundary.md` 与 memory，暂不新增 skill，也不升级 `AGENTS.md`。
