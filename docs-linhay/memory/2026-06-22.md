# 2026-06-22

## Status 本地 CLI 面板 AntD 规范复核

- 用户点名 `src/features/status/components/StatusPanels.tsx:StatusApplyLocalSection:section:608`，询问页面是否符合设计规范。
- 结论：原实现不完全符合。虽然页面外壳和目标切换已接入 AntD，但本地 CLI 写入面板内部仍大量使用旧自定义控件和原生 button/select/input，且窄 rail 内强行双列导致 Select 挤压，不符合 AntD Natural / Certain / Meaningful 的可读可操作原则。
- 修复：将该 section 内可见表单和动作控件替换为 AntD Button / Select / Input / Switch / Space.Compact；StatusSnippetPanel 复制按钮也改为 AntD Button；本地 CLI 配置 rail 字段改为单列，保障窄列可读性。
- 验证：status 局部测试、`npm --prefix frontend run typecheck`、AntD 色彩/字重静态契约、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 均通过；浏览器度量确认 `data-status-local-cli-panel` 内 native button/select 为 0，AntD Button 10、Select 5、Switch 2、Segmented 2，warnings 为 0。
- 沉淀判断：这是既有 AntD-only 设计语言边界的页面级修复，已写入 status redesign plan 和本 memory；不新增 skill、不更新 AGENTS.md。

## StatusApplyLocalSection 本地 CLI 拆卡片

- 用户继续点名 `src/features/status/components/StatusPanels.tsx:StatusApplyLocalSection:div:789`，指出该块太长，要求按卡片拆掉。
- 修复：新增 `StatusLocalCliCard` 统一 AntD Card 外壳；Codex 分支拆为连接与凭证、Provider 与 Model、能力开关、运行前检查、Codex 最小 Diff；Claude Code 分支拆为连接与凭证、Model 映射、运行参数、运行前检查、settings Diff。`StatusSnippetPanel` 也改为 AntD Card，避免右侧 diff 仍保留自定义卡片外壳。
- 验证：status 局部测试、`npm --prefix frontend run typecheck`、AntD 色彩/字重静态契约、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 均通过；浏览器度量确认 Codex 分支渲染 5 张 AntD Card，其中 4 张带 `data-status-local-cli-card` 业务分区，Claude Code 分支渲染 5 张 AntD Card，并显示本地 CLI 通用预检文案。
- 沉淀判断：仍属于 Status 页面 AntD-only 规范落地，补写 space plan 和 memory，不新增 skill、不更新 AGENTS.md。

## Status Account Store Diagnostics 布局复核

- 用户点名 `src/features/status/StatusFeature.tsx:AccountStoreDiagnosticsPanel:Card:126`，质疑为什么该辅助诊断卡单独占第二列。
- 结论：不应该单独占第二列。Account store diagnostics 是辅助诊断状态，不是与本地 CLI 写入并列的主任务。
- 修复：将 `statusWorkbenchGridClass` 从桌面双列改为单列 `grid items-start gap-4`，保留 diagnostics rail 语义但让它跟随主内容下方同宽呈现。
- 验证：statusTypography 局部测试和 `npm --prefix frontend run typecheck` 通过；浏览器度量确认 `gridTemplateColumns` 为单列，diagnostics rail 在 primary rail 下方且同宽。
- 沉淀判断：这是 Status 页面布局规则的局部修正，已写入 space plan 和 memory，不新增 skill、不更新 AGENTS.md。

## Account local CLI preview 逻辑整合

- 用户基于账号页 `应用模板到 Codex` 确认弹窗截图指出“将逻辑整合到一起维护”。
- 结论：组件不应该继续维护 `PreviewFile`、Codex diff 拆分、Claude settings diff 和 model catalog patch 拼接；这些属于账号本地 CLI draft 的模型层输出。
- 修复：新增 `buildAccountLocalCliPreviewFiles()` 到 `frontend/src/features/accounts/model/accountLocalCliMapping.ts`，由 model 层统一生成 Codex / Claude Code preview files；`AccountLocalCliApplyConfirm.tsx` 只通过该函数消费结果并渲染。
- 验证：新增 accounts model 测试覆盖 Codex auth/config 文件拆分、model catalog patch 和 Claude settings preview；组件测试锁定不再直接导入 `buildClaudeCodeSettingsDiff` / `buildCodexLocalApplyDiff`，局部测试和 `npm --prefix frontend run typecheck` 已通过。
- 沉淀判断：这是 20260520 account-template-cli-mapping space 的实现边界收敛，写回 space detail 和 memory；不新增 skill、不更新 AGENTS.md。

## ModalFrame AntD 化

- 用户追问 `ModalFrame.tsx` 第 87 行自绘根层遮罩 div 是否还需要。
- 结论：在“只保留 AntD / 不保留过渡”的设计系统方向下，根层遮罩、dialog 语义、mask close、ESC 和 portal/container 不应继续由 GetTokens 自绘；但 `ModalFrame` 作为项目适配层仍有价值，用于统一尺寸、detail fullscreen、sidebar 覆盖策略和业务 slot。
- 修复：将 `frontend/src/components/ui/ModalFrame.tsx` 底层切换为 AntD `Modal`，删除自绘 overlay/panel root；保留原调用方 API、`header/body/footer/error` slot、`size="detail"` 与 `portal/coverViewport/zIndexClassName` 语义。
- 验证：相关 ModalFrame / account detail / OAuth / local CLI tests、`npm --prefix frontend run typecheck`、全量 unit、frontend build、docs check 与浏览器实测均已通过。
- 沉淀：更新 `docs-linhay/dev/20260520-modal-frame-design-system-standard.md`，明确 ModalFrame 是 AntD Modal adapter，而不是自绘 modal 实现。

## Account detail local CLI apply 入口补齐

- 用户要求账号详情页显示“应用到 xxx”。
- 结论：账号详情页不应该重新实现模板判断；应复用账号卡已有 `resolveLocalCliActionsForAccount`，让详情页和卡片菜单共享同一批 mapping、disabled 状态和原因。
- 修复：`UnifiedAccountDetailModal` 新增 `localCliActions`，`AccountDetailFooter` 在右侧主操作区展示 `应用到 Codex` / `应用到 Claude Code`；`AccountsFeature` 将当前选中账号的现有 local CLI actions 注入详情页。
- 验证：新增 account detail source test 锁定详情页接入 mapping；局部测试、typecheck 和浏览器当前账号详情页验证已通过，当前 disabled preview 账号显示 `应用到 Codex` disabled。
- 沉淀：更新 `20260520-account-template-cli-mapping` space README 与 detail plan，明确详情页 footer 也是正式入口。

## Account detail 关闭入口移到内容区右上角

- 用户继续点名 `AccountDetailFooter` 的关闭按钮，要求放到 `AccountDetailLayout` 内容区右上角。
- 修复：将详情页关闭入口从 footer 移到 `AccountDetailLayout` 右上角 AntD `Button` icon-only 样式，保留 `aria-label/title`；footer 只保留本地 CLI 应用和保存类动作。
- 验证：更新 account detail source test 锁定 layout 右上角 close 结构；浏览器 DOM 验收确认 close 为 AntD `ant-btn-icon-only`，距离内容 panel 右侧和顶部均为 16px，且 footer 不再包含关闭按钮。
- 沉淀判断：这是同一账号详情 local CLI 入口补齐后的安全布局修正，写回 space 与 memory；不新增 skill、不更新 AGENTS.md。
