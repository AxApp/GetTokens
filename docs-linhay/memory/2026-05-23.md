# 2026-05-23

## Codex config panel expands to notices

- 决策：`#frame=codex` 下的 `Feature 配置` 不只承接 `[features]` bool 开关，还纳入 `[notice]` 下的 bool 提示开关；其中 `hide_rate_limit_model_nudge` 作为明确需求已进入页面，`notice.model_migrations`、`notice.external_config_migration_prompts`、`mcp_servers`、`skills.config`、`projects.<path>.trust_level` 仍保留在各自专用配置路径。
- 实现：前端把 `Codex Features` 与 `Codex Notices` 拆成两个 section，preview / save / dirty 统计按 section scope 计算，避免一个面板的 diff 反馈污染另一个面板；notice 面板新增 story，便于设计系统回归。
- 参考源码：当时 GitHub commits 页面复核到 `openai/codex` main 可见 HEAD `932f72c225889102257493f57460251016cbfdc2`（2026-05-22，`fix: reject legacy profile selectors (#24059)`），且本地 `git fetch` 因网络超时失败；后续已在本日 typed schema coverage 记录中同步到更新的 `7d47056ea42636271ac020b86347fbbef49490aa`。
- 验证：前端变更已补测试和类型约束，后续还需跑 `npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`go test ./...`、`docs-linhay/scripts/check-docs.sh` 以及桌面验收，当前尚未完成。

## Claude Usage Desk attribution filter fix

- 问题：`#frame=claude&workspace=usage` 在真实 sidecar attribution 有数据时仍可能显示空，根因是 `UsageDesk` 的 Claude workspace 只按 `provider/model/accountKey/attributionKey` 文本是否包含 `anthropic/claude/sonnet/opus/haiku` 判断，漏掉了 `openai-compatible:deepseek`、`bailian`、`mimo` 等 Anthropic-compatible 供应商。
- 修复：`frontend/src/features/accounts/model/usageDesk.ts` 新增 Claude provider/accountKey 判定，保留显式 Claude/Anthropic 关键词规则，同时让已知 Anthropic-compatible provider 和 `openai-compatible:<provider>` / `auth-file:<provider>` accountKey 进入 Claude observed usage；`github/openai_chat` 类非 Claude provider 仍被过滤。
- 回归：`frontend/src/features/accounts/tests/usageDesk.test.mjs` 新增 DeepSeek 正例，并把非 Claude 负例改为 GitHub，覆盖 “account key 映射到 Anthropic-compatible 格式时 Claude Usage Desk 不应为空”。
- 验证：`node --test frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/utils/pagePersistence.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 均通过。`npm --prefix frontend run test:unit` 暴露两个无关既有问题：`codexAccountList.test.mjs` 引用不存在的 `moveCodexAccountRowToEdge`，以及 `settingsBuildMetadata.ts` 在 Node test 环境读取缺失的 `import.meta.env.VITE_GIT_HASH`。

## Claude Usage Desk local file projection

- 决策：Claude Usage Desk 不再只依赖 relay attribution；按 `cc-switch` 同类能力补齐 Claude Code 本地 session 文件投影，读取 `CLAUDE_CONFIG_DIR || ~/.claude` 下的 `projects/**/*.jsonl`，但只做只读 token 投影，不写、不删、不压缩 Claude 原生 session。
- 实现：新增 `internal/wailsapp/usage_local_claude.go` 与 Wails root bindings，按 assistant `message.usage` 解析 token；按 `message.id` 去重并保留最终 `stop_reason` 条目；跳过 `subagents/agent-*`、`output_tokens=0` 和未完成流式记录；Claude / Codex 本地投影分 provider 缓存与进度事件。
- 前端：`UsageDeskFeature` 的 Claude workspace 支持“本地文件投影”，按 workspace 调用 `GetClaudeLocalUsage` / `RefreshClaudeLocalUsage`，并过滤 `usage-local:*` 事件的 `provider`，避免 Claude 页面误读 Codex rollout 投影。
- 回归：新增 `usage_local_claude_test.go` 和 `usageDeskClaudeLocalSource.test.mjs`，覆盖本地 JSONL 解析、缓存刷新、空 projects、Claude preview projected rows 和绑定分流。
- 验证：`go test ./internal/wailsapp -run TestGetClaudeLocalUsage`、`go test ./internal/wailsapp`、`go test ./...`、`node --test frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/features/accounts/tests/previewData.test.mjs frontend/src/features/accounts/tests/usageDeskClaudeLocalSource.test.mjs` 均通过；`npm --prefix frontend run typecheck` 被无关 `StatusCodexFeaturesSection.stories.tsx` 缺 `section` 字段阻塞。
- 复核修正：前端 `UsageDeskFeature` 之前把 `workspace === 'claude'` 硬编码进 observed 分支，导致 Claude 页面即使切到“本地文件投影”也仍显示真实请求量区块；已改为按 `source === 'observed'` / `source === 'projected'` 真正分流，并新增 source branch 断言测试。

## 会话整理：Claude Usage 本地投影

- 沉淀：Usage Desk 新增 provider-local projection 时，不能只补 parser；必须同时覆盖 provider 独立缓存、`usage-local:*` provider 事件、root Wails binding、生成绑定、preview projected rows、前端 `source` 分支和真实本机文件 sanity check。
- Skill：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 `Usage Desk Local Projection`，记录 Claude Code JSONL 只读边界、streaming usage 去重规则、token 映射和前端 source 分支陷阱。
- Dev 文档：新增 `docs-linhay/dev/20260523-session-distillation-claude-usage-local-projection.md`，记录可复用执行入口、不纳入范围和验证命令。
- 不升级：本轮规则属于 Usage Desk / Claude Code 领域交付边界，不是 repo-wide 通用治理规则，因此不更新 `AGENTS.md`。

## Settings release panel design-system admission

- 决策：将 `SettingsFeature` 更新区块抽为 `frontend/src/features/settings/components/SettingsReleasePanel.tsx`，纳入设计系统业务组件，运行时根节点暴露 `data-design-system-component-name="SettingsReleasePanel"`。
- 实现：设置页更新面板新增当前项目构建 `Git Hash` 字段；前端通过 `VITE_GIT_HASH` 注入，开发态由 Vite 自动读取 `git rev-parse --short=12 HEAD`，release workflow 使用 `${{ github.sha }}` 注入并由前端截断为 12 位。
- 回归：新增 `SettingsReleasePanel.stories.tsx`、`settingsBuildMetadata.test.mjs`，并更新 `componentManifest.ts` / `storyCatalog.ts` / `storyCatalog.test.mjs`，覆盖 ready、checking、native-updater 和 git-hash 状态。
- 验证：`node --test frontend/src/features/settings/settingsBuildMetadata.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`npm --prefix frontend run build-storybook`、`npm --prefix frontend run test:unit` 均通过。测试期间曾短暂暴露无关 Codex 账号顺序断言失败，随后并行工作区改动补齐后复跑通过；本轮未接管该 Codex 改动。

## Codex account order edge actions

- 决策：在 Codex 账号顺序卡片的拖动控制区补充纵向 `置顶` / `置底` 操作，作为拖拽排序的直接快捷入口；同一排序 section 被 Claude Code 复用，因此 Claude Code 页面同步接入首尾移动链路以保持类型与行为一致。
- 实现：`CodexAccountOrderRow` 新增首尾移动按钮，`CodexAccountOrderSection` 传递是否可移动到首尾，Codex / Claude Code feature 点击后更新本地顺序、标记未保存并触发既有自动保存流程。
- 回归：新增 `moveCodexAccountRowToEdge` 与 `moveClaudeCodeAccountRowToEdge` 模型断言，并用 Codex source test 锁定 row 暴露 `onMoveToTop` / `onMoveToBottom` 与本地化 key。
- 验证：`node --test src/features/codex/codexAccountList.test.mjs src/features/claude-code/claudeCodeAccountList.test.mjs`、`npm run typecheck`、`npm run build` 均通过（工作目录：`frontend/`）。

## 会话整理
- 沉淀：`UsageDesk` 的 Claude 观测过滤需要识别 Anthropic-compatible provider / accountKey 映射，不再只看字面 `claude`；Codex / Claude Code 顺序页共享 `move*RowToEdge` 这类纯模型 helper，页面层只保留按钮状态与自动保存；Settings 更新面板已抽成独立设计系统组件，并用 `VITE_GIT_HASH` + Node-test-safe helper 展示构建信息。
- 落位：新整理文档写入 `docs-linhay/dev/20260523-session-distillation-usage-desk-release-panel.md`；`gettokens-ops-governance` 已补充“Feature Panel Extraction & Design-System Admission”。
- 不升级：这轮没有形成新的 repo-wide 硬规则，因此不更新 `AGENTS.md`。

## Codex thread goal read failure
- 结论：`TUI` 里的 `Failed to read thread goal` 是 `tui/src/app/thread_goal_actions.rs` 对 `app_server_session.thread_goal_get()` 失败的展示；真正的 RPC 路径在 `app-server/src/codex_message_processor/thread_goal_handlers.rs`。
- 失败条件：`thread/goal/get` 会在 goals feature 未开启、线程是 ephemeral、线程不存在、sqlite state db 不可用，或 `state_db.get_thread_goal()` 本身返回 SQLite/查询错误时失败。
- 风险：如果本地 state db 旧、迁移不完整或损坏，界面上就只会看到这类泛化报错，根因需要继续看 app-server 返回的 JSON-RPC error message。
- 这次实查结果：`~/.codex/state_5.sqlite` 的 `_sqlx_migrations` 已到 34，且日志里出现 `no such table: thread_goals`；这说明当前目标库路径上的状态库已经没有 `thread_goals` 表，TUI 报错不是空 goal，而是后端查询目标表失败。
- 关联文件：`~/.codex/state_5.sqlite`、`~/.codex/goals_1.sqlite`、`/Users/linhey/.codex/logs_2.sqlite`。其中 `goals_1.sqlite` 虽然有 `thread_goals` schema，但当前这条 `thread/goal/get` 路径实际命中的还是会报缺表的那套状态链。

## Settings release panel CLIProxyAPI hash exposure

- 决策：settings 的发布信息区块继续收敛在 `SettingsReleasePanel`，新增一列 `CLIProxyAPI Git Hash`，不再把后端构建信息散落回 `SettingsFeature` 大块 JSX。
- 实现：`internal/sidecar` 读取 `build/bin/cli-proxy-api.meta.json` 的 `commit` 字段并挂到 `sidecar.Status.gitHash`；前端 `useAppBootstrap` / `StatusFeature` 补齐新字段默认值，`SettingsFeature` 把 app hash 和 CLIProxyAPI hash 一并传给设计系统面板。
- 回归：`SettingsReleasePanel.stories.tsx`、`storyCatalog.test.mjs`、`internal/sidecar/manager_test.go` 同步补了新 hash 字段、4 列布局与 meta 读取断言。
- 验证：`go test ./internal/sidecar ./internal/wailsapp`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build-storybook` 均通过。

## Release metadata write path hardening

- 决策：发布流程不再直接调用 `build-sidecar.sh`，改为先跑 `scripts/ensure-sidecar.sh`，让 `cli-proxy-api.meta.json` 和二进制由同一条路径生成，再把 meta 一起复制进 `GetTokens.app/Contents/MacOS/`。
- 实现：`.github/workflows/release.yml` 新增 CLIProxyAPI commit 解析和 metadata 校验步骤，`scripts/ensure-sidecar.sh` 优先尊重显式 `CLI_PROXY_COMMIT`，macOS 打包前同时复制 `cli-proxy-api` 与 `cli-proxy-api.meta.json`，并在签名前校验 bundled metadata 的 `commit` 与源码仓库一致。
- 回归：`scripts/ensure-sidecar.test.sh` 现在显式断言 meta.json 里记录了 source commit，并覆盖显式 `CLI_PROXY_COMMIT` 写入，避免只检查 fingerprint 而漏掉发布写入字段。
- 验证：`bash scripts/ensure-sidecar.test.sh` 通过。

## CPA 上传空间初始化

- 决策：新建 `docs-linhay/spaces/20260523-cpa-auto-detect-upload/`，作为“多格式自动检测转 CPA 上传”的需求入口，先固化背景、目标、范围、非目标和验收标准，再进入实现设计。
- 参考：将 `https://github.com/yynxxxxx/GPTSession2CPAandSub2API` 列为 GitHub 调研候选，并同步落一条 `docs-linhay/references/20260523-gptsession2cpasub2api.md` 作为参考索引。
- 风险：当前仅完成需求与参考登记，还没有实际分析仓库源码，后续若要推进实现，需要先补研究结论，再写技术方案和测试门禁。

## CPA 上传自动转换实现

- 决策：CPA 自动转换落在 `internal/accounts.NormalizeAuthFileForSidecar` 域层，而不是前端上传组件；这样文件上传、粘贴后上传、详情 normalize 都复用同一套转换规则。
- 实现：新增 ChatGPT Web session 与 9router Codex OAuth 检测，统一转换为 `type: "codex"` 的 CPA auth JSON；缺少真实 `id_token` 时生成带 `https://api.openai.com/auth` claims 的 synthetic id token；既有 CPA / legacy Codex auth-file 继续走原规范化逻辑。
- 回归：新增 `internal/accounts/auth_file_normalize_test.go` 的 ChatGPT session、9router OAuth、未知 JSON 用例，并新增 `internal/wailsapp/auth_files_test.go` 上传入口断言，确保上传给 sidecar 前已转 CPA。
- 验证：`go test ./internal/accounts ./internal/wailsapp`、`go test ./...`、`git diff --check` 均通过。

## 会话整理：CPA auth-file 自动转换

- 沉淀：多格式凭证导入应统一落在 `internal/accounts.NormalizeAuthFileForSidecar`，前端不要复制 token/session/id-token 转换；session-like OAuth JSON 识别必须同时具备 access token 与账号身份信号，未知 JSON 不强转。
- Skill：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 `Auth File Normalize & Status Surface Boundary`，新增 CPA Auto-Convert 规则、最小字段和测试门禁。
- Dev 文档：新增 `docs-linhay/dev/20260523-session-distillation-cpa-auth-file-conversion.md`，记录本轮可复用模式、不纳入范围、沉淀入口和验证命令。
- 不升级：本轮规则属于账号凭证域内工程边界，不是 repo-wide 长期治理规则，因此不更新 `AGENTS.md`。

## AccountsToolbar 筛选顺序调整

- 决策：账号工具栏筛选弹层改为状态优先，其次资源信号，最后账号来源；状态项顺序为全部、可请求、异常、禁用，资源项顺序为最长窗口额度、余额。
- 实现：`frontend/src/features/accounts/components/AccountsToolbar.tsx` 同步调整弹层分组顺序与筛选按钮摘要顺序，避免弹层和摘要展示不一致。
- 回归：`frontend/src/features/accounts/tests/accountFilters.test.mjs` 新增顺序断言，锁住状态 / 资源 / 来源分组和摘要串顺序。
- 验证：`node --test src/features/accounts/tests/accountFilters.test.mjs`、`npm run typecheck` 均通过（工作目录：`frontend/`）。

## AccountsToolbar 菜单项样式统一

- 决策：`AccountsToolbar` 的筛选菜单项统一改为同一种勾选框行视觉，不再混用按钮格子和 checkbox 行。
- 实现：来源、资源、状态三组筛选项共用同一套 checkbox 行样式、边框和 active/hover 规则；后续状态筛选已进一步迁移为 AND 叠加语义。
- 回归：`frontend/src/features/accounts/tests/accountFilters.test.mjs` 补了结构断言，确保 `FilterMenuButton` / radio 样式不再回归。

## AccountsToolbar AND 筛选迁移

- 决策：账号池主列表筛选全面改为 AND 条件叠加，不再使用旧单选枚举，也不再兼容旧字段。
- 实现：`AccountsFilterState` 改为 `requiresRequestable` / `requiresDisabled` / `requiresError` 加资源与来源字段；`AccountsToolbar` 状态项从单选切换为可叠加勾选；本地存储读取只接受新字段，旧 only 字段会被忽略。
- 回归：`frontend/src/features/accounts/tests/accountFilters.test.mjs` 和 `frontend/src/features/accounts/tests/accountSelectors.test.mjs` 覆盖存储读取、菜单结构、AND 过滤和旧字段不再出现在 toolbar 源码。
- 验收：`agent-browser` 无头打开 `http://127.0.0.1:5174/#frame=accounts`，确认筛选菜单展示可请求、异常、已禁用、最长窗口额度、有余额五个勾选项；截图归档到 `docs-linhay/spaces/account-pool/screenshots/20260523/accounts/20260523-accounts-filter-and-after-v01.png`。

## Codex 账号编排筛选补齐

- 决策：Codex 账号编排筛选是 AND 条件叠加，不再用 `*Only` 作为主字段和 UI 口径；阻塞条件用于要求 `requestable=false`，不同于禁用和异常。
- 实现：`CodexAccountOrderFilter` 主字段改为 `requiresRequestable` / `requiresBlocked` / `requiresDisabled` / `requiresError`，并移除旧 `*Only` / `'requestable'` normalize 兼容入口；筛选菜单改成状态、资源、来源三组，并统一为勾选框行样式；筛选摘要顺序同步为状态、资源、来源。
- 回归：`frontend/src/features/codex/codexAccountList.test.mjs` 覆盖默认 filter、AND 过滤、菜单包含阻塞项，并断言组件不再出现旧字段。
- 沉淀：同步更新 `.agents/skills/gettokens-domain-engineering/SKILL.md`，避免后续账号编排筛选再按 `*Only` 兼容口径实现。
- 验证：`node --test src/features/codex/codexAccountList.test.mjs`、`npm run typecheck` 均通过（工作目录：`frontend/`）。

## Codex live session row display narrowing

- 决策：`CodexLiveSessionFeed` 的会话行只展示“会话 ID / 项目名”和“关联账号 / http|ws”，把模型、状态标签和持续时长继续留在详情区，避免列表噪声。
- 实现：新增 Wails / 前端 `projectName` 字段和 `buildSessionRowSummary` 展示 helper；预览数据补 `GetTokens` 项目名，真实数据兼容 sidecar 未来传入 `projectName`，缺失时显示未知项目。
- 回归：`frontend/src/features/codex-live-sessions/model.test.mjs` 新增行摘要断言，锁定 ws/http 简写和不回显模型、状态、时长。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run typecheck` 均通过；`go test ./internal/wailsapp -run CodexLive` 被当前工作区无关编译错误阻塞：`internal/accounts/auth_file_normalize.go` 引用缺失的 `convertSessionLikePayloadToCPA`。

## Codex live session account card reuse

- 决策：`CodexLiveSessionDetail` 的额度和余额区直接复用账户页现成的 `QuotaBars` / `BillingBalance` 组件，不再保留 live sessions 私有的 quota / balance 复制实现。
- 实现：新增 `buildLiveSessionQuotaDisplay` / `buildLiveSessionBillingDisplay` 适配层，把 live request 的 quota / billing 数据转成账户组件接受的 display 结构；`AccountCard` 现在只负责布局和数据挂接。
- 回归：`frontend/src/features/codex-live-sessions/model.test.mjs` 新增映射断言，锁定 quota progress、reset 文案和 balance 字符串化格式。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run typecheck` 均通过。

## 会话整理：Codex live sessions UI 边界

- 沉淀：live sessions feed 只展示 `sessionID / projectName` 与 `account / http|ws`，诊断指标留在详情区；详情页账号资源展示复用 accounts 域 `QuotaBars` / `BillingBalance`，通过纯 adapter 连接 live request 数据。
- Skill：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 Codex Live Sessions 小节，补充低噪声 feed、可选 `projectName` DTO 链和账号资源组件复用规则。
- Dev 文档：新增 `docs-linhay/dev/20260523-session-distillation-codex-live-sessions-ui.md` 记录模式、非目标和验证命令。
- 不升级：本轮是 Codex live sessions 领域内 UI 边界，不是 repo-wide 长期治理规则，因此不更新 `AGENTS.md`。

## Codex config notices and latest source sync

- 决策：`#frame=codex` 的 Feature 配置页扩展为 root bool + `[features]` + `[notice]` 三段 bool 配置面，新增 `hide_rate_limit_model_nudge`、`fast_default_opt_out` 以及 `hide_agent_reasoning`、`show_raw_agent_reasoning`、`allow_login_shell` 等根级 bool。
- 实现：后端 `GetCodexFeatureConfig` / `PreviewCodexFeatureConfig` / `SaveCodexFeatureConfig` 按 root / section patch，支持 TOML quoted key；前端新增 `Codex Root Settings` 与 `Codex Notices` 区块并纳入设计系统 story。
- 参考源码：`docs-linhay/references/codex` 已从 `35aaa5d9fc` 继续更新到 2026-05-23 `origin/main` HEAD `7d47056ea42636271ac020b86347fbbef49490aa`。
- 同步：简单 bool feature registry 补齐 `plugin_sharing`、`mentions_v2`、`auth_elicitation`、`responses_websocket_response_processed`、`remote_compaction_v2`，并修正上游已 removed 的兼容 key 状态。
- 边界：`experimental_use_unified_exec_tool` 因 root 与 `[features]` legacy alias 同名暂不进入；`notice.model_migrations`、`notice.external_config_migration_prompts`、`multi_agent_v2`、`apps_mcp_path_override`、`network_proxy`、`mcp_servers`、`skills.config`、`projects."<path>".trust_level` 仍不进入本页简单 bool 保存面。

## Codex config typed schema coverage

- 决策：`#frame=codex` 的 Feature 配置页不再限定 bool；root、`[features]`、`[notice]`、`[model_providers.<id>]` 均按 `valueType` 纳入。boolean 用开关，enum 用下拉，integer/number 用数字框，string 用文本框，string_array/text 用多行文本。
- 实现：后端新增 typed `changes[]` 保存输入，root/main DTO 与 Wails generated models 透传 `section/id/path/valueType/options/defaultValue/readOnly/unsupported/typedValues/rawValues`；复合 TOML table 已从只读展示升级为 path-scoped raw TOML textarea，保存时校验 section header 不得越过当前配置 path。
- 覆盖：以本地最新 `docs-linhay/references/codex/codex-rs/core/config.schema.json` 做差集审查，root、features、notice、model_provider missing 均为 `(none)`；features 仅额外保留 `experimental_use_freeform_apply_patch`、`include_apply_patch_tool` 两个 legacy alias；provider 17 个字段全部纳入。
- 边界：本地参考仓库已同步到 `7d47056ea42636271ac020b86347fbbef49490aa`；因 upstream main 曾 force-update，普通 `merge --ff-only` 被拒绝为 unrelated histories，本轮在确认参考仓库无本地修改后重置到 `origin/main`。
- 验证：`go test ./internal/wailsapp -run CodexFeatureConfig`、`go test ./...`、`node --test frontend/src/features/status/tests/codexFeatureConfig.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs`、`npm --prefix frontend run typecheck`、`git diff --check` 通过；浏览器 MCP 导航被自动审批超时阻塞后，改用 headless Chrome + CDP 验证 `#frame=codex`，页面 `loading=false` 且命中 root/provider/features/notice 四段与关键新增字段。
- 整理：本轮可复用模式沉淀到 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 Raw + Structured Config Editors：schema-backed 复杂 TOML table 不应永久只读，可用 path-scoped raw TOML textarea，但保存必须校验 section header 前缀并只替换目标 path 子树；该规则属于 Codex 配置域，不升级 `AGENTS.md`。

## 账号详情错误信息展示

- 决策：账号详情页应和账号卡片一样消费 `AccountRecord.statusMessage`；失败态详情直接展示 sidecar 报错原因，没有具体原因时显示兜底诊断，不再只让用户在列表卡片上看到原因。
- 实现：新增 `buildAccountDetailStatusMessage`，统一过滤 `ACTIVE / CONFIGURED / DISABLED / LOCAL`，`UnifiedAccountDetailModal` 使用 `AccountDetailNotice` 渲染错误条，并补齐中英文文案。
- 回归：`frontend/src/features/accounts/tests/accountPresentation.test.mjs` 覆盖 statusMessage、无原因兜底、健康账号隐藏旧消息。
- 验证：`node --test src/features/accounts/tests/accountPresentation.test.mjs`、`npm --prefix frontend run typecheck` 均通过。

## 账号卡 QuotaBars token 进度切换

- 决策：`QuotaBars` 默认继续展示剩余额度百分比；当 quota window 提供 `usedTokens / limitTokens` 时，点击右侧数值可在百分比与 token 进度之间切换，且点击会阻止账号卡片详情联动。
- 实现：quota window DTO 增加 `usedTokens / limitTokens / remainingTokens`，Wails/root/frontend 映射同步透传；小米 MiMo token plan usage 解析保留 `used / limit`，前端 `buildQuotaDisplay` 规范化 token 计数。
- 回归：新增 `internal/accounts/quota_curl_test.go` token 计数断言和 `frontend/src/features/accounts/tests/accountCardLayout.test.mjs` QuotaBars 切换结构断言。
- 验证：`go test . ./internal/accounts ./internal/wailsapp`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`git diff --check` 均通过。

## 会话整理：QuotaBars token 进度链路

- 沉淀：quota token 进度必须作为真实遥测字段全链路透传，入口是 accounts domain parser -> Wails/root DTO -> generated bindings -> `QuotaDisplay` -> `QuotaBars`；不能只在前端根据百分比伪造 token 总量。
- Skill：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 `Quota Rules`，新增 `Token Progress Display Boundary`。
- Dev 文档：新增 `docs-linhay/dev/20260523-session-distillation-quota-token-progress.md`，记录复用边界、执行入口、不纳入范围和验证命令。
- 不升级：本轮规则属于账号 quota 领域内工程边界，不是 repo-wide 长期治理规则，因此不更新 `AGENTS.md`。

## Account filter AND semantics hardening

- 决策：账号池与 Codex 账号编排的筛选状态继续保持 AND-style requirement 字段，摘要由 model 层输出可组合 summary parts，组件层只负责拼接 label，不再自行维护顺序和旧字段兼容。
- 实现：`accountFilters.ts` 新增 `normalize/apply/summarize` 纯函数边界；`codexAccountOrderSectionLayout.ts` 新增同类 `apply/summarize`，并对 Codex filter source 做白名单规范化；`AccountsToolbar` 与 `CodexAccountOrderSection` 改为消费这些纯函数。
- 回归：`accountFilters.test.mjs` 与 `codexAccountList.test.mjs` 覆盖 normalize、apply、summary parts 顺序；新增 `frontend/src/tests/legacyFilterGuard.test.mjs` 并纳入 `npm run test:unit`，禁止活跃代码重新引入 `requestableOnly`、`blockedOnly`、`disabledOnly`、`errorsOnly`、`AccountsAvailabilityFilter` 和 `filters.availability`。
- 验证：`node --test frontend/src/features/accounts/tests/accountFilters.test.mjs frontend/src/features/codex/codexAccountList.test.mjs frontend/src/tests/legacyFilterGuard.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 均通过。
- 沉淀：既有 `.agents/skills/gettokens-domain-engineering/SKILL.md` 已包含账号 filter 必须使用 requirement 字段、不兼容 `availability` enum / `*Only` 字段的规则，本轮仅加代码守卫，不再升级 `AGENTS.md`。
