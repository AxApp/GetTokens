# 2026-05-29

## 账号卡身份模型迁移

- 决策：GetTokens 本地产品模型不引入用户实体，只有账号卡；`account_key/account_id` 是账号卡唯一业务身份。
- 决策：账号登录、新增、导入、复制创建新账号卡；重新登录和编辑当前凭证更新原账号卡并保留原 ID。
- 决策：`auth-id/auth-index/provider/attribution_key/email/API key hash` 都是 runtime evidence，不允许作为 rate-limit 策略匹配键。
- 进展：sidecar fork 已让 Codex API key、auth-file、OpenAI-compatible provider 的 runtime auth 携带 `AccountKey`。
- 进展：sidecar standalone Codex API key 缺失 `local-id` 时会生成 `codex-api-key:legacy-*` 并写回配置。
- 进展：sidecar rate-limit schema/API/evaluator 破坏性删除 `match_key`，usage 查询只按 `account_key`。
- 进展：GetTokens Wails / frontend rate-limit DTO 删除 `matchKey`，`RateLimitRulesSection` 改为单行摘要 + 配置态，无横向滚动表格。
- 进展：账号详情 rate-limit CRUD 改为由页面 shell 注入，browser preview 不再直接触发真实 Wails binding。
- 验证：Sidecar 聚焦 Go 测试、GetTokens Go 测试、frontend `npm run typecheck`、`npm run build`、`npm run test:unit` 均通过。
- 验证：已用 `playwright-cli` 验收账号详情限流区 summary / config 态，确认无 Wails binding 崩溃并归档截图。
- 验证：已用当前 GetTokens worktree 构建产物加载当前 sidecar worktree，dev sidecar `/healthz` 200；rate-limit management API 的 strategies/create/status/events/delete/list 链路均 200，临时规则已清理。

## Sidecar 自治状态治理

- 决策：sidecar 是 GetTokens 运行态自治层；账号选择、rate-limit、route guard、live sessions、usage attribution、system proxy、Codex WebSocket 等热路径状态必须在 `CLIProxyAPI#gettokens/sidecar` 内闭环。
- 决策：合并 CLIProxyAPI 上游提交时，一旦上游实现对 sidecar 自治状态存在风险，不整包照搬；在 `gettokens/sidecar` 内重新实现合理逻辑、补窄测试、重建 sidecar，再合回 GetTokens 主分支（当前为 `master`）。
- 进展：已将该规则写入 `AGENTS.md`、`gettokens-domain-engineering` skill、CLIProxyAPI 上游同步文档和账号卡身份模型文档。

## 账号凭证 SQLite 存储调研

- 进展：已创建 space `docs-linhay/spaces/20260529-account-credential-sqlite-store/`，用于承接账号/凭证 SQLite 统一存储需求调研。
- 进展：已恢复并更新 `docs-linhay/dev/account-credential-sqlite-store-design.md`，将账号卡、Codex OAuth/auth-file、Codex API key、OpenAI-compatible provider 的事实源收敛到 sidecar 持有的 `~/.config/gettokens/accounts-v1.sqlite`。
- 决策：GetTokens 不再管理账号事实源；账号相关改动全部放到 sidecar，GetTokens 只通过 sidecar management API 展示和发起账号操作。
- 决策：SQLite 迁移后的账号卡主 ID 应由 sidecar account store 分配为新的 `acct_*`，旧 `auth-file:*`、`codex-api-key:*`、`openai-compatible:*` 只进入 `account_runtime_identities` 做迁移、诊断和历史归因辅助。
- 决策：`account_key` 使用 UUID 分配，格式为 `acct_<uuid>`，每个账号卡唯一。
- 决策：敏感 entry 不写入 `secret_json` / secret store，明文存在 SQLite 对应类型表中，依靠 DB 文件权限、父目录权限和日志/debug export 脱敏保护。
- 决策：最终 SQLite schema 增加 `account_cards.revision`、`account_runtime_apply_state` 和 `account_migration_sources`，分别解决凭证更新驱动 sidecar runtime apply、当前进程使用旧凭证风险、旧源删除审计问题。
- 决策：迁移后直接修改 sidecar 侧账号事实源。OAuth finalize/relogin、token refresh、Codex API key 更新、OpenAI-compatible provider 更新都由 sidecar 写 SQLite 并刷新 runtime。
- 决策：从账号凭证 SQLite 存储版本开始，GetTokens sidecar 断开与 CLIProxyAPI 上游的合并式同步；上游功能只作为参考输入，需要的功能在 sidecar 侧重新设计、实现和测试。
- 决策：账号 management API 可以按 GetTokens 统一账号模型破坏性调整；旧 `/auth-files`、`/codex-api-key`、`/openai-compatibility` 不作为兼容层保留，新版本直接调用 `/v0/management/accounts` 与 `/v0/management/account-migration`。
- 决策：账号凭证/配置数据必须保迁；rate-limit、usage attribution、route guard、渠道路由和前端详情 hash 可以直接重做，不作为本次 SQLite 迁移的数据保迁目标。
- 决策：当前已经在执行 worktree/分支 `账号与凭证统一存储方案` 上推进，不再新建 feature worktree。
- 决策：迁移完成后必须删除旧账号事实源；只停止读取旧源不满足完成定义。
- 风险：迁移后账号更新是高风险路径；Codex API key 编辑、OAuth 重新登录、token refresh、OpenAI-compatible provider 更新必须保留原 `account_key`，同时更新 SQLite type-specific rows 并刷新 sidecar runtime 快照，避免 sidecar 当前进程继续使用旧凭证。
- 风险：后续实现仍需保证旧账号凭证和配置可 dry-run 校验、可 round-trip，并在删除旧文件前提供迁移校验报告；首阶段建议只做 sidecar account store、schema 和 dry-run importer。
- 进展：已整理 space 并新增最终执行入口 `docs-linhay/spaces/20260529-account-credential-sqlite-store/plans/20260529-final-sidecar-account-store-plan-v01.md`；README 状态更新为 `finalized`。
- 进展：sidecar 已实现 `internal/gettokens/accountstore`、统一账号 management API、SQLite dry-run/commit/delete legacy、Codex OAuth finalize 写 SQLite、Codex/OpenAI-compatible/auth-file 合成器读取 SQLite，并在账号 CRUD 后触发 watcher runtime refresh，成功时标记 `account_runtime_apply_state=applied`。
- 进展：GetTokens 父仓新增 `/v0/management/accounts` typed client、`UnifiedAccount -> AccountRecord` 映射，`acct_*` Codex API key 创建/更新/删除/status/priority 走 sidecar 统一账号 API；旧 ID 分支只作为迁移前兼容路径。
- 进展：GetTokens 父仓 OpenAI-compatible provider 管理已收敛到统一账号 API；列表、创建、编辑、删除、disabled、priority 都走 `/v0/management/accounts`，前端 DTO 增加 `accountKey` 并优先传 `acct_*`，provider name 仅作为迁移前旧卡片兜底。
- 进展：完成 `AccountRecord.accountKind` 透传与前端判定收敛，修正统一 OpenAI-compatible 账号进入通用账号列表后因 `acct_*` 无旧前缀而被误判成 Codex API key 的删除/详情编辑风险。
- 修正：sidecar `PATCH /v0/management/accounts/{account_key}` 现在和 create/status/priority/delete 一样触发 runtime apply，并在测试中覆盖“更新 API key 后保留原 `account_key`、递增 revision、runtime_apply_status=applied”，避免 DB 已保存但当前进程继续使用旧凭证。
- 验证：父仓 `go test ./internal/...` 通过；sidecar `go test ./internal/...` 通过。
- 验证：OpenAI-compatible 与 `accountKind` 收敛后新增/更新聚焦测试，`go test ./internal/wailsapp`、frontend `npm run typecheck`、`node --test src/features/accounts/tests/openAICompatible.test.mjs src/features/accounts/tests/accountDelete.test.mjs src/features/accounts/tests/accountDisabledSync.test.mjs src/features/codex/codexAccountList.test.mjs`、`npm run test:unit`、`npm run build` 均通过。
- 验证：从 `origin/master` 合并后当前分支仍 up to date；再次跑父仓 `go test ./internal/...`、sidecar `go test ./internal/...`、frontend `npm run typecheck`、`npm run test:unit`、`npm run build` 均通过。`docs-linhay/scripts/check-docs.sh` 仍因历史 space 缺目录失败，当前 `20260529-account-credential-sqlite-store` space 结构齐全。
- 进展：已在 space 内新增执行计划 `plans/20260529-execution-plan-v01.md`，并将阶段 7 自动化测试项更新为已通过；真实桌面验收和真实 profile 迁移删除旧源仍保留为待执行/hold。
- 验证：再次运行 `docs-linhay/scripts/check-docs.sh`，失败仍由历史 spaces 缺 `plans/screenshots/debate` 引起；本次 `20260529-account-credential-sqlite-store` 包含 `plans/`、`screenshots/`、`debate/`，未出现在错误列表。
- 进展：已 push sidecar fork `gettokens/sidecar` 至 `a4896197`，已 push 父仓功能分支 `账号与凭证统一存储方案` 至 `6ef891f`；父仓 gitlink 指向的 sidecar commit `a489619720a2627473c8c8d7d1c9923d00d2562f` 已在远端可获取。
- 进展：已创建父仓 PR `https://github.com/AxApp/GetTokens/pull/4`，用于合并账号凭证统一 SQLite 存储方案。
- 进展：PR #4 已合并回远端 `master`，merge commit 为 `5c5f31a00e0db146e5dbb5d0a2fa49f20cd72182`；本轮整理未新增通用 skill，继续沿用已更新的 `gettokens-domain-engineering` 和 `gettokens-ops-governance` 规则。

## Codex 二进制管理页面接管

- 决策：Codex 负责后续维护 Codex 二进制管理页面，继续按 `gettokens-codex-binary-management` skill 的边界推进。
- 边界：该页面保持独立业务，只处理 Codex CLI 版本源、下载、激活、回退、托管 PATH、release cache、版本说明和紧凑列表 UI，不并入账号池、local apply、用量、会话或路由策略。
- 基线：当前工作区干净，`frontend/src/features/codex-binary` 已按 controller / components / model / presentation 拆分。
- 验证：已运行 `npm --prefix frontend run test:unit -- src/features/codex-binary/model.test.mjs`，实际 frontend unit suite 613 项全通过；`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`go test ./internal/codexbinary ./internal/wailsapp` 均通过。
- 治理：补齐该 space 的 `screenshots/.gitkeep`，并调整 `.gitignore` 为忽略截图内容但允许跟踪截图目录骨架。

## Codex 二进制管理页面回归修复

- 根因：托管 PATH 状态只看当前 App 进程 `PATH`，macOS GUI App 重启不会 source shell profile，导致 profile 已写入 GetTokens block 时仍显示“一键托管”。
- 修复：`managedConfig()` 同时检查进程 `PATH` 和目标 profile 中的 GetTokens managed block；新增重启场景回归测试。
- 根因：`VersionNotes()` 只读本地 `cache/release-notes`，点击版本行不会请求 GitHub release body，所以已缓存 notes 永远显示“来自缓存”。
- 修复：`GetCodexBinaryVersionNotes` 后端路径改为优先拉取对应 GitHub release 内容并刷新缓存，远端失败时才回退缓存；新增远端优先和缓存兜底测试。
- 验证：`go test ./internal/codexbinary ./internal/wailsapp`、`go test ./...`、`npm --prefix frontend run test:unit -- src/features/codex-binary/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 均通过；build 仅有既有 chunk size warning。

## Wails Dev 绑定生成竞态修复

- 风险结论：`wails dev` 会在前端 dev server 已启动后重新生成 `frontend/wailsjs`，Vite 可能在 `models.ts` 被短暂删除时预转换源码，触发 `/wailsjs/go/models.ts` 缺失的 Pre-transform error。
- 处理：在 `frontend/vite.config.js` 增加 dev-only `wails-generated-file-retry` 插件，仅对 `frontend/wailsjs/**/*.js|ts` 的 `ENOENT` 读取做 2 秒短重试，避免扩大到生产 build 或普通源码。
- 验证：新增 `storyCatalog` 静态约束测试；`node --test src/features/design-system/storyCatalog.test.mjs`、`npm run typecheck`、`npm run build` 通过；`./scripts/wails-cli.sh dev` 已越过原失败点，未再出现 `/wailsjs/go/models.ts` Pre-transform error。

## 文档门禁与 Fast Refresh 收尾

- 处理：`docs-linhay/scripts/check-docs.sh` 改为只强制每个 space 有 `README.md`；`plans/screenshots/debate` 若存在则继续校验文件命名，避免历史 space 缺目录阻塞当前门禁。
- 处理：`DebugContext` 与 `AccountsPageStateProvider` 拆出 hook/context 到独立文件，Provider 模块只导出组件，消除 Vite React Fast Refresh 的混合导出 invalidate。
- 验证：`docs-linhay/scripts/check-docs.sh`、`node --test src/features/design-system/storyCatalog.test.mjs`、`npm run typecheck`、`npm run build` 通过；`./scripts/wails-cli.sh dev` 未再出现 Fast Refresh invalidate。

## Wails Dev 单实例启动修复

- 根因：正式版 `/Applications/GetTokens.app` 运行时持有 `SingleInstanceLock` 的 `com.linhay.gettokens`，开发版 `wails dev` 使用同一 lock，会被 Wails 判定为第二实例并正常退出，表现为构建完成后 `Development mode exited`。
- 处理：新增 `appSingleInstanceUniqueID()`，当 `GETTOKENS_APP_PROFILE=dev` 时使用 `com.linhay.gettokens.dev`，保持正式版仍使用 `com.linhay.gettokens`；`scripts/wails-cli.sh dev` 已自动注入 dev profile。
- 验证：`go test ./...`、`npm run test:unit`、`npm run typecheck`、`npm run build`、`docs-linhay/scripts/check-docs.sh` 通过；在正式版仍运行的情况下，`./scripts/wails-cli.sh dev` 已保持 watch/dev 模式运行，进程包含 `build/bin/GetTokens.app/...` 和 `build/bin/cli-proxy-api -config ~/.config/gettokens-dev/config.yaml`，验证后手动中断。

## 账号详情页 UI 改造

- 进展：创建并收尾 `docs-linhay/spaces/20260529-account-detail-ui/`，账号详情 API Key 模块改为凭据、连接验证、代理路由自上而下的列表式工作区。
- 进展：`AccountProxyRouteSection` 仍保留给 OpenAI-compatible 详情复用，但账号池 API Key 详情只嵌入其内部 route editor，不再作为独立详情模块。
- 进展：`AccountQuotaSection` / `AccountBillingSection` 改为摘要卡 + curl 编辑 modal；modal 左侧是文本编辑器，右侧是默认变量与模板列表，关闭后回到详情页由底部保存统一提交。
- 进展：curl 编辑 modal 的默认变量支持按 textarea 光标插入；无活动光标时变量按钮切换为复制语义并走统一 clipboard fallback。
- 进展：额度脚本和余额脚本 modal 接入账号详情 hash 子路由，使用 `script=quota` / `script=billing` 可直达，关闭子弹窗只移除 `script` 并保留账号详情页。
- 进展：底部“保存改动”在账号配置与限流规则保存成功后关闭详情页；保存失败或限流规则返回失败时保持详情页打开。
- 进展：补齐 `#frame=accounts` 浏览器预览详情数据，包括 API Key 验证模型、auth-file 内容和模型目录，并修复 preview auth-file 摘要 loading 不结束的问题。
- 验证：`node --test frontend/src/features/accounts/tests/accountDetailLayout.test.mjs frontend/src/features/accounts/tests/previewData.test.mjs`、`npm --prefix frontend run typecheck`、`docs-linhay/scripts/check-docs.sh` 通过。
- 验证：无头浏览器确认 `#frame=accounts&detail=codex-api-key%3Astable-001` 可打开、curl 编辑 modal 可用、底部保存后 hash 回到 `#frame=accounts`；`#frame=accounts&detail=auth-file%3Acodex-pro.json` 不再停留在 loading，并显示模型目录。

## 本机 macOS 正式环境安装包脚本

- 决策：新增 `scripts/build-local-macos-package.sh` 作为本机正式 profile 安装包入口，默认读取 `frontend/package.json` 版本，按当前 macOS 架构构建 Wails production app、回灌 freshly built sidecar、同步 bundle version、ad-hoc codesign、生成 DMG 并挂载校验。
- 边界：默认产物用于本机安装测试，不等同于 GitHub Release 可分发资产；需要 Developer ID 签名和公证时显式传 `--notarize` 并提供 notary 环境变量。
- 验证：`go test ./...`、`npm --prefix frontend run typecheck`、`docs-linhay/scripts/check-docs.sh` 通过；`scripts/build-local-macos-package.sh --skip-tests` 产出 `dist/local-release/GetTokens_local_macOS_AppleSilicon.dmg` 和 `.sha256`，DMG 挂载校验、arm64 架构、bundle version `1.0.33`、app/sidecar codesign verify 均通过。
