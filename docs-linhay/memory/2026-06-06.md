# 2026-06-06

- `20260606-codex-requestable-alignment` 已落地：Codex 请求顺序把 `configured` 且无 requestability evidence 的账号标为 `waiting-check`，继续显示但不进入 requestable candidates / explain / probe 候选；`active / local / ready / ok / verified / manual / usage / quota / configured-provider` 可作为候选资格证据，openai-compatible provider 不因缺少 usage/quota 被误排除。用户手动确认“我知道这个能用”保存到 `ChannelRoutingConfig.manualRequestableAccountIDs`，不写账号凭证本体；该证据不能绕过 disabled、runtime guard、quota-empty、cooldown、model-unavailable 等硬阻塞。验证通过 `npm --prefix frontend run test:unit -- src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck`、`go test ./internal/wailsapp -run 'Test.*ChannelRouting|Test.*Codex.*'`、`go test ./internal/accounts`、`docs-linhay/scripts/check-docs.sh`。
- 账号卡片 quota empty badge 的时间差异根因：sidecar `blockReason` 里的 `reset at 2026-...Z` 是 UTC/RFC3339 原始原因；quota row 下方“重置时间”来自 window `resetAtUnix/resetLabel` 并按本地时区格式化。前端展示层不得直接把原始 `quota empty: ...; reset at ...Z` 放入可见 badge，需先归一为本地化短文案，例如 `5H 已空 · 06/06 04:10`，原始 reason 继续保留在数据层供诊断。
- 账号卡片无上限流量统计（`value / ∞`）不应伪造 quota 上限，也不应使用突兀的图表主色进度条。当前采用“活动强度条”：保留 `/ ∞` 文案，宽度按当前累计量相对近期 `trafficBuckets` 基线计算，颜色使用 `text-primary` 低透明混合色，表达近期活跃度而非剩余额度。
- 账号卡片的“刷新额度”是用户理解中的卡片运行状态刷新入口；当卡片存在 Route Guard 规则时，应同时刷新 `GetAllRateLimitStatuses` 并在 Route Guard 区域显示同款扫光反馈。否则 quota 有动画而 Route Guard 不动，会被误解为该区域没有刷新。
- 账号卡片显式刷新时，无上限 usage 活动条也必须显示刷新反馈：顶部刷新、分组刷新、单卡刷新都应触发 usage 刷新 busy 状态并复用同款扫光；后台 usage 轮询保持静默，避免页面周期性自发动画干扰扫读。
- v1.1.1 发布完成：版本三件套为 `frontend/package.json`、`frontend/package-lock.json`、`frontend/package.json.md5`，`package.json` md5 为 `a3160eccd61afd948585908e5243cb33`；release commit/tag 为 `a0c7c401ca7d0cbb21e34982f40387d8c478e4f9` / `v1.1.1`。本地门禁通过：`go test ./...`、`npm --prefix frontend run test:unit`（728 项）、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`；Vite chunk-size warning 按 release 规则不阻塞。GitHub Release workflow `27030754594` 成功，URL `https://github.com/AxApp/GetTokens/actions/runs/27030754594`；`Build H5`、`Build macOS arm64`、`Build macOS amd64`、`Publish Sparkle appcast`、`Publish Release` 均 success。Release URL `https://github.com/AxApp/GetTokens/releases/tag/v1.1.1`，资产齐全：`GetTokens_macOS_AppleSilicon.dmg`、`GetTokens_macOS_AppleSilicon.tar.gz`、`GetTokens_macOS_Intel.dmg`、`GetTokens_macOS_Intel.tar.gz`、`checksums.txt`。Sparkle appcast 分支 `648f9a5f19a3ad18184c2a1c72af2c9bc2d2e2ce`，`appcast-arm64.xml` 与 `appcast-amd64.xml` 顶部均包含 `1.1.1` 且指向对应 DMG。官方 Release 资产下载到 `/tmp/gettokens-v1.1.1-verify.5jvN22` 后，`shasum -a 256 -c checksums.txt` 全部 OK；两个 DMG 均 `spctl ... accepted` 且 `source=Notarized Developer ID`，`xcrun stapler validate` 成功。挂载验收显示 `.app` 签名链为 `Developer ID Application: HAN LIN (3L8RM3MDLS)`，包含 timestamp 与 `Notarization Ticket=stapled`；Apple Silicon 可执行文件为 `arm64`，Intel 可执行文件为 `x86_64`；两个包的 `CFBundleShortVersionString` 与 `CFBundleVersion` 均为 `1.1.1`，`SUFeedURL` 分别为 `appcast-arm64.xml` / `appcast-amd64.xml`。最终状态：CI 发布完成，可分发 DMG 验收完成。
- Codex OAuth/auth-file 账号详情页也应展示额度与余额模块；OAuth 的差异只在管理侧内置模板/数据来源，不应让详情页缺少资源模块。实现上已让 OAuth 详情模块计划包含 `quota` / `billing`，并通过共享区块的 `readOnlyScripts` 只读展示实时资源信息，避免暴露无法保存的脚本编辑入口。
- 新建 `docs-linhay/spaces/20260606-account-runtime-sync/` 规划账号级运行态自动同步：前端不做页面级全量 `ListAccounts()` 高频轮询，自动同步只读取 sidecar 已维护的 `quota-status`、`rate-limit-status` 与 usage attribution；用户显式刷新才触发远端 quota/usage/rate-limit 刷新反馈。当前只写需求与执行计划，暂不创建 worktree。
- 账号级运行态自动同步已落地：Wails root 新增 `GetAllQuotaStatuses/GetQuotaStatus` 只读 bridge；前端 `useAccountsQuotaState` 拆出 `syncCodexQuotaStatuses`，`useAccountsPageState` 统一做 30 秒账号级 runtime sync，静默读取 quota/status、usage attribution、rate-limit；用户显式刷新仍保留主动刷新和扫光反馈。后台 usage sync 已收窄为 sidecar attribution 只读路径，不解析 `/accounts`，也不 fallback 旧 `/usage`。门禁通过 `go test ./...`、`npm --prefix frontend run test:unit`（729 项）、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`。dev Wails 桌面冷启动验收通过：正式版未被停止或修改，账号池 13 个账号正常渲染，20:41:35 / 20:42:05 / 20:42:35 连续自动 tick 只出现 `quota-status`、`rate-limit-status`、`rate-limit-strategies`、`usage-attribution`，没有页面级 `/accounts` 高频轮询或旧 `/usage` fallback。
- 新建 `docs-linhay/spaces/20260606-relay-vendor-support/`，先沉淀 relay 厂商接入的需求与执行计划，并拉取本地参考项目 `docs-linhay/references/sub2api/`、`docs-linhay/references/new-api/`。当前判断：`sub2api` 与 `new-api` 都是 AI API gateway / relay 平台，不应在 GetTokens 中新增账号主类型；后续优先按 `vendor preset` 接入厂商模块，是否还要同步进入 `openai-compatible provider preset` 需在实现前单独冻结。本轮未做代码实现，仅完成 README、计划文档、参考摘要与结构校验，`docs-linhay/scripts/check-docs.sh` 已通过。
- relay 厂商支持三类能力时，账号详情页不能只展示主 `baseUrl`；同一账号的 `openai-compatible / codex API / anthropic` endpoint 应落到 `formatBaseUrls` 并在详情页可见可编辑。已补 `ApiKeyConfigDraft.formatBaseUrls`、详情页三端配置面板、Codex API key 与 openai-compatible provider 的 Update 保存链路；未新增账号类型。
- relay 厂商账号的余额 / 额度接口已按管理端能力对齐，不新增第四端运行态类型。`{{baseUrl}}` 解析规则为优先使用 `formatBaseUrls.openai_chat`，缺省回落主 `baseUrl`；quota / billing 模板、变量面板、测试调用、保存前预检共用该规则。Codex API Key 根层 Wails DTO 到 internal DTO 已补齐 `platformCookie / curlVariables` 透传，这些字段仅用于 quota / billing 管理调用，不进入运行态路由凭据。
- `20260606-relay-vendor-support` 已补需求设计 v01：`sub2api`、`new-api` 首期按 `aggregator` 厂商预设接入，不新增账号主类型；两者默认声明 `openai_chat / openai_responses / anthropic` 三端能力。默认 endpoint 采用自部署语义：`sub2api` 的 OpenAI/Codex 为 `http://localhost:8080/v1`、Anthropic 为 `http://localhost:8080/antigravity`；`new-api` 的 OpenAI/Codex 为 `http://localhost:3000/v1`、Anthropic 为 `http://localhost:3000`。设计同时冻结创建流、详情页、本地 CLI apply、`/models`、quota/billing 管理接口的 endpoint 读取规则：Codex direct 用 `openai_responses`，Claude direct 用 `anthropic`，OpenAI-compatible、模型拉取和余额/额度管理默认用 `openai_chat`。
- relay 多端能力应作为账号资产通用模型处理，而不是 `sub2api/new-api` 特例：`baseUrl` 只做默认入口和旧数据回退，`supportedFormats + formatBaseUrls` 才是账号支持哪些端、每端请求哪个 endpoint 的事实源。后续实现需覆盖新建、编辑、复制、导入、恢复、本地 CLI apply、`/models`、quota/billing 等链路，避免只在详情页展示三端但其他账号流丢失配置。
- 多端路由和本地直连配置必须以下游请求协议为优先匹配键，而不是账号主 `apiFormat`：OpenAI-compatible 下游优先走 `openai_chat`，Codex / Responses 下游优先走 `openai_responses`，Claude Code / Anthropic Messages 下游优先走 `anthropic`。如果账号不支持当前下游协议，应在 route explain / apply disabled reason 中说明缺少对应 `supportedFormats`，不能静默切到其他端。
- 新建并整理 `docs-linhay/spaces/20260606-project-account-candidate-pool-rule/`，调研“一个项目只能在固定几个账号中路由”的新规则。当前结论：不恢复 legacy `projectBindings`；推荐把它设计成 sidecar route engine 的项目账号候选池规则，先在 `RouteContext` 补稳定 `projectKey`，再把启用规则编译进 `CompiledRouteSnapshot`，由 `PolicyStagePoolScope` strict allow 收窄 account candidate pool，后续 `P2 RequestPolicy`、sticky 与 `sequential / balanced` selector 都只在收窄池内工作。最脆弱前提是请求上下文里必须存在稳定的 `project key`，后续若实现应先验证这一真源，再做 explain-only，最后接入真实执行。
- `20260606-project-account-candidate-pool-rule` 已补需求设计 v01：规则命名冻结为 Project Account Candidate Pool Rule，只支持 `projectKey + channel + allowAccountIDs + enabled`，命中后默认 fail closed；未命中项目规则则保持原 channel routing。设计明确要求删除 legacy `projectBindings` 可写入口，新规则独立于 `ChannelRoutingConfig` 存储，并把 `projectName` 显示字段与运行时稳定 `projectKey` 拆开。
- `20260606-project-account-candidate-pool-rule` 已补技术证据 v01：sidecar `gettokensrouting` 已有 `hard-filter -> pool-scope -> request -> sticky` stage 排序，`routing_policy.go` 已通过 `Options.Metadata` 构造 `RouteContext`，`engine.Route()` 会输出 `DecisionStep` trace；engine/scheduler 测试证明 hard filter 不能被后续 allow 或 sticky 绕过；前后端 channel routing 测试证明只接受 `sequential / balanced` 且 `projectBindings` 必须丢弃。因此项目账号候选池规则应实现为快照驱动的 `PolicyStagePoolScope` strict allow set，而不是 UI 补偿、request policy 或 legacy `projectBindings`。
- `20260606-project-account-candidate-pool-rule` 已产出 Project Account Candidate Pool Rule 流程图：SVG 源文件位于 `plans/20260606-project-account-candidate-pool-rule-flowchart.svg`，PNG 截图位于 `screenshots/20260606/project-account-candidate-pool-rule/20260606-project-account-candidate-pool-rule-flowchart-baseline-v01.png`。本图使用确定性 SVG 绘制再导出 PNG，覆盖 `RouteContext`、`CompiledRouteSnapshot`、P0 hard filter、P1 strict allow、P2 request policy、P3 sticky、P4 selector、pool epoch、route error/explain/audit 与删除 legacy `projectBindings` 的清理边界。
- 本轮“整理”将项目账号候选池规则边界轻量沉淀到 `.agents/skills/gettokens-domain-engineering/SKILL.md`：这是账号候选池与 sidecar routing 的领域知识，不升级为 AGENTS repo-wide 规则。
- 新建 `docs-linhay/spaces/20260606-menu-bar-popover/` 规划 menu bar 图标与 popover：目标是启动后可默认显示菜单栏图标、设置页可关闭图标、点击图标打开轻量 popover；当前技术判断是不先承诺现有 Wails 主窗口视图能直接放进 `NSPopover`，首期建议原生 AppKit popover，另做 `NSPopover + WKWebView/React` spike 验证资源加载、Wails API、事件同步和焦点生命周期。
- `20260606-menu-bar-popover` 设计稿收窄为额度余额小票 v04：用户只关心额度和余额，因此 popover 删除账号类型说明、模型映射和路由解释；首屏只展示最低额度、风险账号数、可读余额、刷新时间、额度风险列表和余额列表，完整编辑回主窗口。
- `20260606-menu-bar-popover` v05 根据用户反馈收紧额度进度条左右间距：窗口标签和百分比读数改为紧凑列，百分比右对齐，让小票式额度条更密集。
- `20260606-menu-bar-popover` v07 根据浏览器评论重设计风险账号行：移除冗余风险标题后，单行改为“左侧剩余额度票根 + 中间账号/窗口 + 右侧状态戳 + 底部紧凑条形读数”，继续坚持只展示额度和余额，不重新引入账号类型解释。
- `20260606-menu-bar-popover` v08 根据浏览器评论继续收窄风险账号行：取消大百分比票根和右侧状态戳，改为“账号名/重置窗口 + 右侧额度百分比 + 紧凑进度条 + 底部状态/余额摘要”，让 `codex-free-low.json` 这类行更像额度余额读数而不是账号类型说明。
- `20260606-menu-bar-popover` 已开始实现 v01：新增 `showMenuBarIcon` 运行时设置，默认显示菜单栏图标；前台模式可关闭图标，`keep_service_in_menu_bar` 驻留模式强制开启图标，避免后台服务没有恢复/退出入口。`internal/menubar` 已从 `NSStatusItem + NSMenu` 改为点击状态栏图标打开原生 `NSPopover`，首期 popover 显示服务状态、sidecar 端口提示、打开窗口、检查更新和退出。门禁通过 `go test ./...`、`npm --prefix frontend run test:unit`（741 项）、`npm --prefix frontend run typecheck`、`./scripts/wails-cli.sh build`。
- Codex API Key 账号详情页“保存后再进详情仍显示旧 API key / URL / 前缀”的根因不是 `acct_*` 变化或数据库未保存：`acct_*` 是 account-store SQLite 的稳定 `account_key`，保存只更新同一账号记录。实际问题是前端保存成功后只更新当前详情 modal 的 `selectedAccount`，没有同步账号列表缓存；若随后 `ListAccounts` 因 sidecar 短暂 500 / SQLite locked 失败并被前端吞掉，下一次从账号卡片打开详情会重新使用列表中的旧对象。修复口径：账号详情配置保存成功后必须按稳定 id 同步 patch 列表缓存和当前详情对象，`ListAccounts` 只作为最终刷新，不作为唯一回显真源。
- 账号详情 stale-cache blast 已覆盖主账号页同类入口：Codex API key 改名、priority 更新、Codex API key 配置保存、openai-compatible 配置保存均必须在后端保存成功后通过 `patchAccountLocally(accountID, patch)` 同步 `apiKeyRecords / derivedAuthFileRecords / selectedAccount`，再尝试 `ListAccounts` 刷新；delete / disabled 已有本地 remove/patch 保护。Codex / Claude 专属账号列表的模型映射保存仍属于相邻风险：它们详情行来自各自 `orderedRows + reload()`，不会复现主账号页“再进账号详情旧 API key / URL / prefix”的同一状态容器，但若 reload 失败可能出现专属列表行回显不即时更新，后续治理应给这些 workspace 增加本地 row patch 或统一 mutation cache helper。
