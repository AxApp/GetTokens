# 2026-05-03

## Codex 配置导航与开关视觉

- 偏好更新：Codex feature 配置表格里的开关只保留拨档控件本体，不再在开关按钮内重复显示 ON/OFF 或默认值文案；开启态使用绿色，关闭态保持浅色底。
- 细节更新：Codex feature 开关滑块关闭态固定在左侧、开启态固定在右侧，使用 transform 动画过渡；滑块需始终留在线框内，不压出外框。
- 验证：`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`git diff --check` 通过。

## Codex Feature 描述本地化

- 偏好更新：Codex feature 配置表格中的 description 不直接展示后端英文，前端先读取 `status.codex_feature_descriptions.<feature_key>`，未知 key 才回退到后端 description 或“暂无描述”。
- 覆盖范围：stable、experimental、under_development、deprecated、removed 与 legacy alias 已补齐中英文文案。
- 验证：locale JSON 解析、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`git diff --check` 通过。

## Codex 导航继续收敛

- 偏好更新：OpenAI 状态不再作为一级菜单 `厂商状态` 暴露，合并到一级菜单 `Codex` 下的二级项 `OpenAI 状态`。
- 偏好更新：Codex 二级菜单不强制英文大写，中文显示收敛为 `功能开关 / 会话管理 / OpenAI 状态 / 用量统计`，避免 `FEATURE 配置`、`CODEX 用量` 这类中英混排噪音。
- 兼容策略：旧 `#frame=vendor-status` 和本地存储的 `vendor-status` 会迁移到 `#frame=codex&workspace=vendor-status`，避免老链接或缓存失效。
- 验证：`node --test frontend/src/utils/pagePersistence.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 通过。

## 请求编排页需求补充

- 关键决策：`docs-linhay/spaces/20260502-request-orchestration-menu/README.md` 的四段泳道保持不变，但账号级编排能力补充为“支持的 CLI + 模型映射”。
- 约束补充：每个账号除了代理模式，还需要声明支持哪些 CLI，以及入口模型如何映射到账号真实模型；术语优先复用仓库现有 `models[].name / models[].alias`。
- 文档落位：已更新对应 space README，并通过 `docs-linhay/scripts/check-docs.sh` 结构校验；本轮未运行自动化测试，因为当前仍处于需求定义与文档收敛阶段。

## 请求编排 V1 分层方案

- 关键决策：`请求编排` V1 先不改 CLIProxyAPI 原生策略；GetTokens 作为 APP 层编排控制器，通过本地 orchestration store + sidecar 现有 routing/disabled 能力实现“唯一 provider / 唯一账号组”。
- 分层边界：`支持的 CLI`、`模型映射`、`代理模式` 先作为 APP-owned overlay；真正 apply 到 sidecar 的只包括 routing 配置和账号/provider 启停状态。
- 文档落位：实施计划已写入 `docs-linhay/spaces/20260502-request-orchestration-menu/plans/20260503-request-orchestration-v1-plan-v01.md`，并回链到对应 space README。

## 请求编排设计稿 v01

- 设计稿产物：`docs-linhay/spaces/20260502-request-orchestration-menu/request-orchestration-design-v01.html`
- 视觉方向：`Swiss-industrial workbench`，保持 GetTokens 现有黑白灰硬边工作台语义，用左侧产品导航、顶部链路态总览、四段泳道和右侧影响面板表达“调度台而不是表单页”。
- 交互重点：入口 CLI / 模型切换、唯一组模式切换、账号代理模式切换都会即时影响出口预览和右侧焦点说明。

## Claude / Codex 本地配置 UI 复用

- 偏好更新：状态页本地 CLI 配置区中，Codex 与 Claude Code 的 Relay API key 选择器、Endpoint / Base URL 区域必须复用同一组局部组件，不再在两个 tab 内平行维护同款 JSX。
- 实现：`StatusApplyLocalSection` 复用 `StatusRelayKeyPicker`、`StatusEndpointPicker` 与 `StatusModelPicker`；Relay API key / Provider / Model 的“下拉 + 框内右侧动作按钮”已抽为项目级 `ActionSelect` 通用组件，Endpoint 模块统一承载 LAN Access、endpoint 选择与 Base URL 复制。
- 验证：`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`git diff --check` 通过。
