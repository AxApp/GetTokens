# 2026-06-17

## DeepSeek 单卡刷新入口图标化修复

- 用户截图反馈 DeepSeek 卡片处于“等待检测”时，右上角显示三点菜单而不是刷新按钮；随后明确底部也应是刷新按钮，而不是纯文本“同步运行状态”。
- 根因：`AccountCard.tsx` 的 `topActions` 固定渲染账号操作菜单；单卡 refresh/runtime sync 动作只出现在 footer 文本按钮。
- 修复：右上角和 footer 刷新动作都改为 `RefreshCw` 图标按钮，复用既有 `buildAccountCardRefreshAction()` 的文案、禁用态和 `onRefreshQuota(account)` 链路；三点菜单继续保留复制、重登、禁用、删除等账号操作。
- 验证：`node --test frontend/src/features/accounts/tests/accountCardInteractions.test.mjs`、`node --test frontend/src/features/accounts/tests/accountCardLayout.test.mjs`、`npm --prefix frontend run typecheck` 通过；无头 preview DOM 断言确认 22 张账号卡 footer 都是刷新图标按钮，且底部文本“同步运行状态/刷新额度”消失，并归档截图到 `docs-linhay/spaces/20260606-account-runtime-sync/screenshots/20260617/accounts/20260617-account-card-refresh-icons-after-v02.png`。
- 沉淀审计：这是账号卡 UI 入口一致性的小修，已写入 `20260606-account-runtime-sync` space 和 memory；暂不升级 skill 或 `AGENTS.md`。

## 账号卡 quota fact 诊断条收起

- 用户指出正常账号卡不需要展示 `FACT AVAILABLE / SOURCE / FRESHNESS / CONFIDENCE / evidenceRefs` 这类调试证据。
- 证据门禁：`frontend/src/features/accounts/components/CardSections.tsx` 的 `QuotaBars` 直接渲染 `QuotaFactEvidenceStrip`，而设计系统 manifest 已声明账号卡 `evidence` 降级到详情页诊断区；截图现象与代码事实一致。
- 修复：正常账号卡移除 `QuotaFactEvidenceStrip`，只保留 quota window 剩余额度、重置时间和 stale/runtime warning；底层 `QuotaDisplay.fact` 与 `resolveQuotaFact()` 保持透传，后续详情/Doctor/usage 诊断面仍可消费。
- 验证：先补红灯 `quota bars keep sidecar quota fact diagnostics out of normal cards`，再修复；`node --test src/features/accounts/tests/accountCardLayout.test.mjs`、`node --test src/features/accounts/tests/accountQuotaRuntime.test.mjs src/features/accounts/tests/accountQuotaFact.test.mjs` 通过。
- 沉淀审计：该边界已由 `gettokens-frontend-design-quality` 和设计系统 manifest 覆盖，本轮只修正回归并更新 quota space / memory，不新增 skill，不升级 `AGENTS.md`。

## OmniRoute 第八轮主控验收

- 第八轮 subagents 已完成三个互不重叠切片：Route Resilience main bridge、Quota + Doctor sidecar diagnostics、Protocol Bridge MCP adapter handler contract。
- Route 主仓新增 `RunRouteResilienceAction` 全链路桥接，501 `not_implemented` 保留 body 与 `httpStatus`，不伪造 bounded reconcile / routeability recheck。
- Doctor / Quota 在 CLIProxyAPI reference 内新增只读 `doctor-diagnostics` endpoint，复用 route dropped reasons 与 `QuotaRuntimeState.fact`，不刷新 quota、不清 guard、不新增 repair mutation。
- Protocol Bridge 新增 MCP adapter handler contract，unknown tool/resource、missing scope、missing idempotency 都在 adapter boundary 拒绝，safe action 只返回 operation ref。
- 主控验收：Route client/Wails/root/WailsJS tests、CLIProxyAPI Doctor diagnostics focused、Protocol focused、frontend typecheck、contract artifact validator、docs check、父仓与 CLIProxyAPI diff check 均通过。
- 剩余项：Route UI 尚未消费 action bridge，Doctor Workbench 尚未消费 sidecar `doctor-diagnostics`，Protocol 尚未接真实 MCP stdio / sidecar execution，Extension UI 下一轮单独排期；本轮复用既有 supervision / sidecar authority / bridge contract 规则，不新增 skill 或 `AGENTS.md`。

## 项目级 skill 读取优先级修正

- 用户指出此前按全局技能目录查找项目级 skill 会漏掉本仓 `.agents/skills/` 下的版本。
- 决策：GetTokens 项目级 skill 的权威读取路径固定为 `.agents/skills/<skill-name>/SKILL.md`；全局技能目录仅作为补充来源，不能用全局缺失否定项目级 skill。
- 写回：已更新 `AGENTS.md`、`.agents/skills/gettokens-ops-governance/SKILL.md`、`docs-linhay/dev/20260616-agent-skill-operating-model.md` 和 `docs-linhay/dev/20260424-project-skills-overview.md`。

## OmniRoute 第九轮主控验收

- 第九轮 subagents 已完成四个互不重叠切片：Route Resilience UI 消费 action bridge、Doctor/Quota 主仓消费 `doctor-diagnostics`、Extension Contract read-only management UI、Protocol Bridge sidecar execution binding contract。
- Route UI 已在 `ChannelRoutingWorkbench` 读取 structured dropped reasons 并触发 `RunRouteResilienceAction`；`clear_transient_lockout` 仅限 sidecar 支持的 transient source，未实现的 reconcile / routeability action 原样展示 `not_implemented`。
- Doctor / Quota 主仓 `GetDoctorSnapshot` 在 sidecar ready 时优先读取只读 `GET /v0/management/gettokens/doctor-diagnostics`，映射 route guard dropped reasons 与 quota facts；旧 sidecar `404/501` 回退既有 `wails-aggregate`。
- Extension Contract 新增独立 read-only registry feature，只展示 snapshot、diagnostics、capability/source/root 信息；仍未接 workspace 入口、enable/disable、capability runner、Codex Skills/MCP 保存链路或 marketplace。
- Protocol Bridge 收紧 MCP adapter executor contract，新增 stub executor 与 audit persister interface；safe action 只返回 accepted operation ref，真实 sidecar runner / audit persistence 仍是后续切片。
- 主控验收：main client、Wails core、root app、Protocol/Extension core、CLIProxyAPI reference focused、frontend focused 55 项、frontend typecheck、contract artifact validator、docs check、父仓与 CLIProxyAPI diff check 均通过。
- 沉淀审计：本轮仍复用既有 supervision / sidecar authority / read-only extension / bridge contract 边界，未形成新的 repo-wide 规则，因此不新增 skill 或 `AGENTS.md`。

## OmniRoute 第十轮主控验收

- 第十轮 subagents 已完成四个切片：Extension Registry workspace 入口、Protocol Bridge sidecar HTTP executor contract、Doctor/Quota usage-status evidence view model、Route Resilience operator surface refinement。
- Extension Registry 已接入 `#frame=codex&workspace=extension-registry` 与 Codex sidebar，只读展示 snapshot/diagnostics/capability/source/root；主控修正了验收产物落位，把文本 snapshot 从 `screenshots/` 移到 `plans/`，并让 preview 脚本在 Chrome headless 失败时 fallback 到归档 Playwright snapshot/PNG。
- Protocol Bridge 新增 `SidecarHTTPExecutor` fake transport contract；read 返回 sidecar-authority envelope，safe action 只返回 accepted operation ref，且只传 idempotency hash，不传明文 key / Authorization / Cookie。
- Quota Intelligence 新增 `coerceQuotaFactDisplay()` 与 `buildQuotaFactEvidenceView()`，Doctor 侧复用同一 evidence helper 对齐 source/summary label，不在前端重推 quota authority。
- Route Resilience UI 从单个 first-hit target 升级为 selectable action targets + per-target action history；`not_implemented` / failure 仍按 sidecar 原始语义展示。
- 主控验收：Extension preview 脚本、frontend focused 123 项、frontend typecheck、Protocol/Extension Go、contract artifact validator、docs check、父仓与 CLIProxyAPI diff check 均通过。
- 剩余项：Extension 仍不做 enable/disable/runner；Protocol 尚未接真实 sidecar HTTP client / MCP stdio；Quota usage/status UI 尚未完整接入；Route target 去重仍可能受 reason 文案变化影响，history 仍未持久化。
- 沉淀审计：本轮仍属于五个 space 内的 feature-local 推进，没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十一轮主控验收

- 第十一轮 subagents 已完成四个质量切片：Route stable target identity、Protocol HTTP failure taxonomy、Quota Usage Desk evidence consumer、Extension preview gate hardening。
- Route Resilience action target id 已从 `account/auth/model/source/scope/reason` 收敛为 `account/auth/model/source/scope`，reason 只做聚合展示和计数，action history 继续按 stable target 绑定。
- Protocol Bridge `SidecarHTTPExecutor` 现在分类 HTTP non-2xx、transport/timeout、malformed JSON、sidecar rejected envelope，并通过 canonical rejected envelope 暴露安全 `sidecar_error_code`，不泄露 Authorization / Cookie / 明文 idempotency key。
- Quota Intelligence 的 Usage Desk 已实际消费 `QuotaFactEvidenceView`，observed/projected 两路只读取显式 `quotaFact/quota_fact/fact`，不从 windows 或 `blockReason` 推导 authority。
- Extension Contract preview gate 已硬化为 live Chrome / archived snapshot 双路径同语义检查，并校验 README 中 hash、脚本、snapshot、screenshot 引用一致性；当前环境 Chrome headless 仍失败，验收使用 archived Playwright fallback。
- 主控验收：Protocol/Extension Go、frontend focused 170 项、frontend typecheck、Extension preview gate、contract artifact validator、docs check、父仓与 CLIProxyAPI diff check 均通过。
- 剩余项：Route history 未持久化；Protocol 还未接真实 sidecar HTTP client / MCP stdio；Usage Desk 依赖运行时 payload 显式携带 quota fact；Extension live Chrome 产物待后续有可用浏览器时刷新。
- 沉淀审计：本轮继续属于五个 space 内 feature-local 推进，没有形成新的跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十二轮主控验收

- 第十二轮 subagents 已完成五个切片：Route account detail evidence reuse、Doctor route evidence normalization、Quota status evidence boundary、Extension enable-state read-only contract、Protocol real HTTP client boundary。
- Route Resilience 在账户详情运行态路由中展示只读 route resilience evidence，复用 `account/auth/model/source/scope` stable target identity 和 reason aggregation，不调用 `RunRouteResilienceAction`，不把 evidence 当 sidecar route truth。
- Doctor Workbench 对 route dropped reason evidence 增加 stable target / reason summary / route blocking label；主控进一步收窄边界，只有完整 account/auth、model、source、scope 时结构化，partial identity fallback。
- Quota Intelligence 新增 `resolveQuotaStatusEvidenceFromPayload()`，Usage Desk 改为复用通用 helper；只信显式 `quotaFact` / `quota_fact` / `fact`，不会从 windows、blockReason、usage totals 推导 authority。
- Extension Contract 增加 enable-state v0 只读解释层与 action availability UI，覆盖 enabled / disabled / blocked / pending / readonly-unsupported；仍不写配置、不执行 capability、不接 marketplace。
- Protocol Bridge 新增真实 HTTP transport 构造边界，校验 loopback base URL、timeout、bearer token、redirect 禁止和 forbidden header；仍不实现 MCP stdio server、不证明 sidecar endpoint 已存在。
- 主控验收：Protocol/Extension Go、frontend focused 251 项、frontend typecheck、contract artifact validator、Extension preview gate、docs check、父仓与 CLIProxyAPI diff check 均通过；主控清理了 `frontend/wailsjs/go/models.ts` 生成物 whitespace 以恢复全仓 diff check。
- 剩余项：Route action history 未持久化；Doctor 依赖上游完整字段；Quota status page 还未接入 helper；Extension enable/disable storage 和 runner 未实现；Protocol retry/proxy/mTLS/profile-aware transport factory 仍待后续。
- 沉淀审计：本轮仍是五个 space 内 feature-local 推进，已落 space plan 和 memory；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十三轮主控验收

- 第十三轮 subagents 已完成五个切片：Route evidence digest helper、Doctor structured route evidence UI、Quota Status page consumer、Extension enable-state artifact gate、Protocol transport factory boundary。
- Route Resilience 新增共享 `buildRouteResilienceEvidenceDigests()`，`buildRouteResilienceActionTargets()` 与账户详情只读 evidence 复用同一 stable digest；reason 只聚合展示，不再参与 stable key。
- Doctor Workbench 在完整 `targetKey` 场景下渲染结构化 route evidence 字段，覆盖 target、account、auth、model、source/scope、blocking label；partial identity 继续 fallback，不编造 route truth。
- Quota Intelligence 的 Status 页开始消费 `GetAllQuotaStatuses()` + `resolveQuotaStatusEvidenceFromPayload(status, 'codex')`，只展示显式 quota fact evidence，不从 windows、blockReason 或 usage totals 推导 authority。
- Extension Contract 新增 enable-state v0 schema/example，并把 `check-omniroute-contract-artifacts.mjs` 扩展到 41 项检查；仍只做 artifact gate，不写配置、不执行 capability、不接 marketplace。
- Protocol Bridge 新增 profile/authority-aware executor factory，创建前做 sidecar authority precheck，token provider 在 authorize 后解析，bearer token 不进入 canonical body、audit 或 error。
- 主控验收：Protocol/Extension Go、frontend focused 263 项、frontend typecheck、contract artifact validator、Extension preview gate 均通过；随后继续执行 docs / diff 收尾门禁。
- 剩余项：Route digest 不持久化 action history；Doctor 依赖完整 identity；Status evidence 依赖运行时 payload 显式带 quota fact；Extension storage/mutation/runner 未实现；Protocol MCP stdio、profile resolver、retry/proxy/mTLS/audit persistence 仍待后续。
- 沉淀审计：本轮仍是五个 space 内 feature-local 推进，复用既有 subagent supervision、quota explicit-fact、read-only extension artifact 和 protocol authority 门禁；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十四轮主控验收

- 第十四轮 subagents 已完成五个切片：Route digest recency metadata、Doctor route digest reuse、Quota Status evidence preview/empty-state、Extension enable-state schema validation gate、Protocol MCP stdio transport preflight。
- Route Resilience 的 digest 在 stable target id 不变的前提下增加 `firstObserved*` / `lastObserved*` metadata；`decisionID` / `recordedAt` 收敛为最新证据，reason 文案变化仍不会分裂 target。
- Doctor Workbench route evidence 改为通过窄 adapter 复用 Route Resilience digest helper，Doctor / route / account detail 的 stable identity 语义对齐；partial identity 继续 fallback。
- Quota Intelligence 新增 Status evidence section state helper，显式 quota fact 才展示 authoritative cards；只有 windows、blockReason、usage totals 时展示 `NON-AUTHORITATIVE` 空态，不做前端 authority 推导。
- Extension Contract 的 enable-state artifact gate 增加本地轻量 schema validation，valid example 必须通过，invalid enum / actionAvailability / missing required fixtures 必须被拒绝；artifact validator 扩展到 49 项检查。
- Protocol Bridge 新增 MCP stdio preflight contract，tool 在 authorize 后、executor 前拒绝 mapping 外或 credential-bearing input，resource 只允许 fixture 内 URI，response/audit 不回显 token/header/cookie。
- 主控验收：Protocol/Extension Go、frontend focused 267 项、frontend typecheck、contract artifact validator、Extension preview gate、docs check、父仓与 CLIProxyAPI diff check 均通过；主控清理 `frontend/wailsjs/go/models.ts` 生成物 whitespace 以恢复全仓 diff check。
- 剩余项：Route recency 依赖可比较 `recordedAt`；Doctor 仍从文本解析 route fields；Quota mixed payload 不逐条提示无 fact 账号；Extension schema runner 不是完整 Draft 2020-12；Protocol preflight 不等于真实 stdio server 或 sidecar endpoint。
- 沉淀审计：本轮仍是五个 space 内 feature-local 推进，已落 space plan / README 和 memory；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十五轮主控验收

- 第十五轮 subagents 已完成五个切片：Route account detail recency surface、Doctor structured route evidence DOM gate、Quota mixed payload non-authoritative hints、Extension manifest schema validation gate、Protocol stdio query schema allowlist。
- Route Resilience 在账户详情运行态路由 evidence 中展示 `Latest Evidence`、`First Seen`、`Last Seen`，字段来自共享 digest recency metadata；同 stable target 跨多个 decision / reason 文案变化仍不分裂。
- Doctor Workbench preview gate 现在校验 structured route block 的 target/account/auth/model/scope/blocking markers、partial identity fallback、read-only mode 和无 mutation/repair handler，并归档 round15 snapshot / screenshot。
- Quota Intelligence 在 Status mixed payload 中保留 authoritative fact cards，同时列出缺显式 quota fact 的账号；无 fact payload 继续只显示 non-authoritative，不从 windows、blockReason、usage totals 推导 authority。
- Extension Contract 将 manifest v0 valid/invalid examples 纳入 schema-level artifact gate，新增 forbidden permission / missing required invalid fixtures；artifact validator 扩展到 54 项检查。
- Protocol Bridge 的 MCP stdio preflight 基于 mapping `query_schema_ref` 做 canonical top-level query key allowlist，拒绝 schema 外 key，并保持 authorize-before-executor。
- 主控验收：Protocol/Extension Go、frontend focused 270 项、frontend typecheck、contract artifact validator、Extension preview gate、Doctor preview gate、docs check、父仓与 CLIProxyAPI diff check 均通过；主控清理 `frontend/wailsjs/go/models.ts` 生成物 whitespace 以恢复全仓 diff check。
- 剩余项：Route recency 信息在多个 decision 卡片下可能重复；Doctor/Extension preview live Chrome 分支本机不可用，走 archived fallback；Quota 缺 accountKey 时无法精确点名；Extension schema runner 仍是轻量实现；Protocol allowlist 只校验 top-level key，不校验完整 JSON Schema 类型/required。
- 沉淀审计：本轮仍是五个 space 内 feature-local 推进，已落 space plan / README 和 memory；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十六轮主控验收

- 第十六轮 subagents 已完成五个切片：Route account detail digest-level dedupe、Doctor typed route evidence adapter、Quota unknown-account missing fact count、Extension manifest negative schema coverage、Protocol stdio query type/required preflight。
- Route 原 agent `019ed3e2-7450-7d42-a7b1-a29e911dd0b3` 因 502 中断；replacement `019ed409-10fa-7c02-8d0c-b53668a2f6fc` 复核其半成品后收敛实现。账户详情现在对同一 route resilience stable digest 做“首次完整展示 + 后续 reference”，并通过 `matchedRouteBlocking`、`Digest Coverage` 保留当前 decision 的 blocking/observe 可解释性。
- Doctor Workbench 优先消费 typed route fields / nested `routeEvidence` payload，继续保留文本 fallback 和 partial identity fallback；不新增 repair action。
- Quota Intelligence 在 Status mixed payload 中新增无 `accountKey` payload 的 missing explicit fact count；显式 fact 仍是唯一 authority 来源。
- Extension Contract 为 manifest schema gate 增加 unknown top-level field、invalid `source.type`、capability missing required `source` 三类负例，artifact validator 扩展到 57 项检查。
- Protocol Bridge 为 MCP stdio preflight 增加当前 canonical query schema 的最小 `required` 与 `boolean/string/array[string]` 类型校验；缺 `protocol/model` 或类型错误会在 executor 前拒绝。
- 主控验收：Protocol/Extension Go、frontend focused 276 项、frontend typecheck、contract artifact validator、Extension preview gate、Doctor preview gate、docs check、父仓与 CLIProxyAPI diff check 均通过；主控再次清理 `frontend/wailsjs/go/models.ts` 生成物 whitespace 以恢复全仓 diff check。
- 剩余项：Route 尚未细化到同一 decision 内每条 dropped reason 的 blocking/observe 明细；Doctor runtime typed payload 仍需后端 DTO/mapper；Quota 无 accountKey 只能计数；Extension schema runner 仍非完整 Draft 2020-12；Protocol preflight 仍不是完整 JSON Schema 或真实 MCP stdio server。
- 沉淀审计：本轮继续是五个 OmniRoute space 内 feature-local 推进，复用既有 supervision / explicit-fact / read-only artifact / protocol preflight 边界；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## 账号卡片 quota-empty 误报排查

- 用户反馈 PLUS 账号明明有额度但卡片显示“额度已空”。只读检查正式 sidecar `/v0/management/gettokens/quota-status` 后确认：PLUS auth-file `acct_751d74a2...` 的 quota fact 为 available，5H 99%、7D 75%，但账号处于 `manual-disabled`；PRO API key `acct_dd2172ea...` 5H 94%、7D 52%，仍在实际请求中被选中且 `/v1/responses` 返回 200。
- 根因定位到前端 `AccountCard` / `buildQuotaBlockBadgeLabel`：旧逻辑把任意 quota runtime `blocked=true` 都 fallback 成“额度已空”，没有区分 `manual-disabled`、`rate-limit` 与真正的 `quota-empty`。
- 修复：新增 `hasQuotaEmptyBlock()`，只有 `sources[].source == quota-empty` 或 `blockReason` 明确包含 quota empty/exhausted 时才渲染空额度 badge，并同步卡片 critical tone 判定；手动禁用只显示禁用状态，不再覆盖新鲜正额度窗口。
- 验证：`node --test-name-pattern 'quota display preserves sidecar route guard explain fields|quota blocked badge' frontend/src/features/accounts/tests/accountSelectors.test.mjs` 与 `npm --prefix frontend run typecheck` 通过。完整 `accountSelectors.test.mjs` 仍有一条既有 loading/fact 断言失败，非本轮引入。
- 沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md`，记录“通用 blocked 不等于 quota-empty”的账号池领域规则；不升级 `AGENTS.md`。

## OmniRoute Round 17 Doctor Wails Typed Route Evidence

- Doctor Workbench 子任务补齐 Wails/root typed route evidence passthrough：`DoctorEvidenceRef` 现在透传 `accountKey/accountID/authId/model/scope/reason/routeBlocking/routeEvidence`，aggregate `doctorRouteDecisionCheck` 从 route dropped reasons 填只读 typed payload。
- root `mapDoctorSnapshot` 保留 typed route evidence，`frontend/wailsjs/go/models.ts` 同步最小 binding 字段；前端测试继续证明 Doctor 只读消费 typed fields，未新增 repair/mutation handler。
- 验证：`go test -count=1 ./internal/wailsapp`、root Doctor mapper focused tests、`npm --prefix frontend run test:doctor-workbench`、`npm --prefix frontend run typecheck`、`docs-linhay/scripts/check-docs.sh`、scoped `git diff --check` 均通过。
- 沉淀审计：本轮属于 Doctor space 内 feature-local DTO 补链路，已落 space plan / README / memory；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十七轮主控验收

- 第十七轮 subagents 已完成五个切片：Route per-reason blocking detail、Doctor Wails typed route DTO passthrough、Quota unscoped payload trace labels、Extension conditional source schema gate、Protocol stdio enum preflight。
- Route Resilience 在 account detail digest dedupe 基础上新增 `matchedReasonDetails`，同一 decision 内每条 dropped reason 都保留 `BLOCKING/OBSERVE`；stable digest id 仍不包含 reason 文案，不改变 sidecar truth。
- Doctor Workbench 将 typed route evidence 从前端 adapter 推进到 Wails/root DTO 和 aggregate mapper，只读透传 `routeEvidence`，未新增 repair/mutation。
- Quota Intelligence 对无 `accountKey` 且缺显式 fact 的 payload 增加最多 5 条 non-authoritative trace sample，只用 `payload #n/source/status/updatedAt/provider`，不推导账号或 quota authority。
- Extension Contract 增加 declared-endpoint missing endpoint、static-json missing path 两个 invalid fixtures，并补本地 schema runner 对 `if/then` object keywords 的最小支持；artifact validator 扩展到 59 项检查。
- Protocol Bridge stdio preflight 增加当前 canonical query 的手写 enum gate，覆盖 `protocol/detail_level/probe_mode/kinds[]`，仍不做完整 JSON Schema 或真实 MCP stdio server。
- 主控验收：Go aggregate、frontend focused 279 项、frontend typecheck、contract artifact validator 59 checks、Extension/Doctor preview archived fallback、docs check、父仓与 CLIProxyAPI diff check 均通过；主控对 Go 文件执行 gofmt 并清理 `frontend/wailsjs/go/models.ts` 生成物 whitespace。
- 验收说明：本机 Chrome preview 命令本轮无输出卡住，主控中断后用 `CHROME_EXECUTABLE_PATH=/nonexistent/chrome` 强制走脚本内 archived snapshot/screenshot fallback；该证据不替代真实 Wails 桌面壳层验收。
- 剩余项：Route per-reason detail 尚未复用到 Doctor；Doctor 真实 sidecar diagnostics typed payload 仍取决于后续 sidecar 输出；Quota unscoped trace 不能定位账号；Extension runner 仍非完整 Draft 2020-12；Protocol enum gate 仍需随 schema 手工同步。
- 沉淀审计：本轮仍是五个 OmniRoute space 内 feature-local 推进，复用既有 supervision / explicit-fact / read-only artifact / protocol preflight 边界；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute Round 18 Doctor Sidecar Diagnostics Chain

- Doctor Workbench 子任务把真实 sidecar `/v0/management/gettokens/doctor-diagnostics` typed payload 接进 main side client / Wails / root DTO 链路；404/501 仍回退既有 `wails-aggregate`。
- Route evidence 继续只读透传 typed route fields / `droppedReason`；quota evidence 新增 `DoctorEvidenceRef.quotaFact`，只消费 sidecar typed `quotaFact` 或同一 evidence typed quota fields，不从文本推导 quota authority。
- 验证：`go test ./internal/cliproxyapi -run 'TestDoctorDiagnosticsClient'`、`go test ./internal/wailsapp -run 'TestDoctor'`、`go test . -run 'TestMapDoctorSnapshot|TestGetDoctorSnapshot'` 均通过；本轮未启动 dev App、未触碰正式版。
- 沉淀审计：本轮是 Doctor space 内 DTO/client 接线切片，已落 space plan 和 memory；未形成新的跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute Round 18 Quota Sidecar Fact Output

- Quota Intelligence sidecar 子任务在 CLIProxyAPI 参考实现中补齐 runtime/doctor 显式 fact 输出：`QuotaRuntimeState` JSON 在 legacy `fact` 外增加 `quotaFact`，doctor quota evidence 继续拷贝 sidecar `Fact`、`evidenceRefs` 和脱敏 explanation。
- 读取路径收窄：`withGuardState` 只 normalize 已存在的 fact，不再对 raw no-fact runtime state 从 `windows/blockReason/usage totals` 旁路推导 quota authority；缺 fact 的 doctor quota check 保持 `not_ready`。
- 验证：`go test ./internal/gettokenshooks -run 'TestQuotaRuntimeRoutesPutAndGetStatus|TestDoctorDiagnosticsIncludesQuotaFactEvidence|TestDoctorDiagnosticsDoesNotInferQuotaFactWhenMissing'`、`go test ./internal/gettokenshooks`、`go test ./internal/api/handlers/management -run 'Quota|Billing'`、`docs-linhay/scripts/check-docs.sh`、scoped `git diff --check` 均通过。
- 沉淀审计：本轮复用既有 quota explicit-fact / sidecar authority 边界，仅落 space plan 与 memory；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute Round 18 Route Recheck Routeability Tracer

- Route Resilience 子任务将 CLIProxyAPI reference `recheck_routeability` 从 `not_implemented` 推进为 sidecar-owned 只读 tracer action：要求 `accountKey` 或 `authId`，重新采样 in-memory guard 与 persisted channel runtime blocks，返回 `before/after`、`droppedReasons`、`authority=sidecar`。
- dry-run 返回 `status=dry_run` 且不生成 audit；非 dry-run 返回 `status=applied`、`auditId=route-audit-*`、`tracerOnly=true`、`reconcileRuns=0`，证明没有进入 bounded reconcile 或无限 reconcile。
- `rerun_bounded_reconcile` 继续保持 `501 not_implemented`，本轮未触碰真实外部服务、正式 App、前端、Doctor、Quota、Protocol 或 Extension。
- 验证：`go test ./internal/gettokenshooks -run 'TestRouteResilienceAction' -count=1`、`go test ./internal/gettokenshooks -count=1`、`go test ./... -count=1`、`docs-linhay/scripts/check-docs.sh`、scoped `git diff --check` 均通过。
- 沉淀审计：本轮是 Route Resilience space 内 sidecar action tracer 切片，已落 space plan / README / memory；未形成新的跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十八轮主控验收

- 第十八轮 subagents 已完成五个真实上下游 tracer-bullet：Route `recheck_routeability` sidecar tracer、Doctor sidecar diagnostics client/Wails/root 链路、Quota sidecar 显式 `quotaFact` 输出、Extension enable-state core storage、Protocol in-process MCP stdio JSON-RPC handler。
- 主控验收确认五个切片未越界：未触碰正式版 App，未读写用户 Codex config，未执行 extension capability，未启动外部 stdio 进程，未把 windows/blockReason/usage totals 重新当作 quota authority。
- 验证已通过：主仓 Go aggregate、CLIProxyAPI reference gettokenshooks/management、frontend focused 279 项、frontend typecheck、OmniRoute contract artifact 59 checks、Extension/Doctor preview archived fallback、docs check、父仓与 CLIProxyAPI `git diff --check`。
- 主控收尾修正：清理 `frontend/wailsjs/go/models.ts` 生成物 whitespace；修正第十八轮 dispatch 文档顺序，把主控验收段落追加到第十七轮之后。
- 发布准备判断：第十八轮自动化门禁已通过，但正式发布还需要完整 release preflight、目标版本号确认、提交/tag、CI Release workflow、GitHub Release 七资产、Sparkle feed 和官方 DMG 下载验收；当前不能只凭本地测试宣称可分发。
- 沉淀审计：本轮是五个 OmniRoute space 内真实链路收敛，复用既有 subagent supervision / sidecar authority / explicit-fact / extension artifact / protocol preflight 边界；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute Round 19 Extension Local Enable State Mutation

- Extension Contract 子任务把 Round 18 core enable-state storage 接入 Wails/root/frontend：新增 `SetGetTokensExtensionEnabled`，默认写入 GetTokens app-local `~/.config/gettokens(-dev)/extension-enable-state.json`，snapshot 默认读取同一 state file 并 merge 到 registry view。
- 前端 registry view 现在提供 local-only enable/disable action；按钮与 notice 明确不写 Codex config、不执行 capability、不接 marketplace，成功后刷新 snapshot。
- 验证：`go test ./internal/gettokensextensions ./internal/wailsapp -run 'Extension|GetTokensExtension'`、`go test . -run 'GetTokensExtension'`、`npm run test:unit -- ...gettokens-extension-registry...`、`npm run typecheck` 均通过。
- 沉淀审计：本轮是 Extension Contract space 内本地状态 mutation 接线，复用既有 app-local state / no Codex config / no runner 边界；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第十九轮主控验收

- 第十九轮按“一次性写完再整体测试”执行：五个 subagents 并行完成 Route action history / bounded reconcile boundary、Doctor diagnostics typed evidence completeness、Quota main-side quotaFact casing decode、Extension local enable-state mutation、Protocol stdio lifecycle/audit skeleton；主控等全部返回后统一审查和聚合测试。
- Route Resilience 新增 sidecar-owned action history 查询/记录，`clear_transient_lockout`、`recheck_routeability`、`rerun_bounded_reconcile` 都写 history；`rerun_bounded_reconcile` 从纯 501 推进为 bounded tracer boundary，`reconcileRuns=1`，不循环、不调用外部服务。
- Doctor Workbench 的 CLIProxyAPI diagnostics 输出补齐 typed route dropped reason 字段和 typed quota fact 深拷贝/脱敏；缺 explicit quota fact 仍 `not_ready`，不从 windows/blockReason 推导。
- Quota Intelligence main side decode 同时支持 `quotaFact`、`quota_fact`、legacy `fact`，并兼容 `observedAt/evidenceRefs` 与 snake case；下游仍坚持 explicit fact-only authority。
- Extension Contract 的 registry UI 已允许 local-only enable/disable，Wails/root 写 GetTokens app-local `extension-enable-state.json`；主控同步修正 preview gate，不再把 local mutation 误判为违规，但仍禁止 Codex config save、capability runner、marketplace。
- Protocol Bridge 新增 stdio lifecycle wrapper 和 audit persistence skeleton，覆盖 Serve/Shutdown/context cancel 与 tool/resource/preflight audit write；不启动真实外部 sidecar，不持久化 token/header/cookie/raw query/raw URI。
- 聚合验证已通过：GetTokens Go aggregate、CLIProxyAPI `gettokenshooks` + management、frontend unit 903 项、frontend typecheck、contract artifact 59 checks、Extension/Doctor preview fallback、extension focused check、docs check、父仓与 CLIProxyAPI `git diff --check`。
- 剩余项：Route 仍缺真实外部 repair service 和 durable ledger；Doctor 真实 dev App 桌面壳层未本轮启动；Extension 仍未做 Codex config save/runner/marketplace；Protocol 仍未做真实外部 stdio process / sidecar runner / durable audit persistence。
- 沉淀审计：本轮未产生新的 repo-wide 规则；新增内容属于五个 OmniRoute space 内产品化切片，已落 dispatch、space plan/README 和 memory，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第二十轮主控验收

- 第二十轮继续按“一次性写完再整体测试”执行：Route durable action ledger、Doctor typed diagnostics consumer、Quota explicit-fact anti-regression、Extension Codex config dry-run preview、Protocol external stdio / durable audit 五个切片由 subagents 并行实现；主控统一做聚合验证和文档写回。
- Route Resilience 在 CLIProxyAPI reference 中把 action history 推进为 sidecar-owned JSONL/file ledger；`clear_transient_lockout`、`recheck_routeability`、`rerun_bounded_reconcile` 都 append ledger，history endpoint 优先从 ledger newest-first 读回。bounded reconcile 仍是受限 tracer，不调外部 repair service、不循环。
- Doctor Workbench 现在从 Wails/root/frontend DTO 透传 nested `droppedReason`；route target 只从 nested typed payload 或 legacy typed `routeEvidence` 生成，顶层文本字段不再升级为 authority；`quotaFact` 继续只消费 typed payload。
- Quota Intelligence account quota consumer 收紧为 explicit-fact-only：只有 `quotaFact` / `quota_fact` / legacy `fact` 才返回 authority fact；缺 fact 时不再从 exhausted windows、`blockReason`、`degradedReason` 或 usage totals 推导额度结论。
- Extension Contract 新增 Codex config dry-run preview / validation boundary，基于 enabled extensions 生成 summary、blocked sections、validation errors 与可扩展 operations；本轮不写真实 `~/.codex/config.toml`、不执行 capability、不接 marketplace。主控派回修复 `previewData.ts` generated model `convertValues` 类型缺口。
- Protocol Bridge 新增受控 fake external stdio process wrapper 和 JSONL audit sink；测试覆盖 start、JSON-RPC stdin/stdout 往返、context shutdown、stderr/exit error 脱敏，audit 写入前脱敏 `TargetRefs` 和 `Authority.SourceNotes`。
- 聚合验证已通过：GetTokens Go aggregate、CLIProxyAPI `gettokenshooks` + management、frontend unit 907 项、frontend typecheck、contract artifact 59 checks、Extension/Doctor preview fallback、Extension focused correction、docs check、父仓与 CLIProxyAPI `git diff --check`。
- 剩余项：Route ledger 还没有 rotation/compaction/profile-aware path；Doctor 未做真实 dev App 桌面壳层验收；Extension dry-run 目前因 v0 schema 未声明 Codex Skills/MCP 写入而 `operationCount=0`；Protocol 仍缺 MCP initialize/capabilities、并发 request、audit rotation/query API 和真实 sidecar runner。
- 沉淀审计：本轮复用既有 subagent supervision、sidecar authority、explicit-fact、dry-run config preview、protocol audit redaction 边界；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第二十一轮主控验收

- 第二十一轮继续按“一次性写完再整体测试”执行：Route ledger bounds/query filters、Doctor typed binding regression gate、Quota Status/Usage explicit-fact gate、Extension dry-run operation projection、Protocol MCP initialize/audit query 五个切片由 subagents 并行实现；主控统一做聚合验证和文档写回。
- Route Resilience action ledger 增加 `maxEntries` 默认 200，append 后同步截断 JSONL，只保留最新 N 条；history endpoint 支持 `action/status/target/account/auth/model/limit` filter，仍无 scheduler、无外部 repair service。
- Doctor Workbench 新增 root DTO JSON contract、Wails DTO JSON roundtrip、frontend generated binding gate，覆盖 `GetDoctorSnapshot` binding 和 generated `DoctorEvidenceRef` constructor，防止 `droppedReason` typed payload 在 Wails/root/frontend 同步时丢失。
- Quota Intelligence 的 Status/Usage 消费面补 explicit-fact-only 门禁：缺 `quotaFact` / `quota_fact` / legacy `fact` 时只显示 missing / non-authoritative，不从 windows、blockReason、usage totals 或伪 authority 字段推导 quota truth。
- Extension Contract dry-run preview 现在能从 v0 manifest capabilities 投影候选 operations：`provider-metadata` -> `skills.config`，`model-catalog-source` -> `mcp_servers`，`action=preview`；成功投影只产生 `codex-config-projection-only` warning，不写真实 Codex config、不执行 capability、不接 marketplace。
- Protocol Bridge 新增 minimal `initialize` 响应和 `JSONLAuditReader` 查询 API；initialize 返回 `protocolVersion/serverInfo/tools/resources` capabilities 且不调用 executor，audit query 支持 `limit/kind/status` 并继续脱敏 malformed/secret-bearing 内容。
- 聚合验证已通过：GetTokens Go aggregate、CLIProxyAPI `gettokenshooks` + management、frontend unit 909 项、frontend typecheck、contract artifact 59 checks、Extension/Doctor preview fallback、focused binding gates、docs check、父仓与 CLIProxyAPI `git diff --check`。
- 剩余项：Route ledger 还没有 profile-aware runtime path / rotation metadata；Doctor 未运行 Wails binding generator 或 dev App；Extension projection 还不是真实 TOML 局部 patch 计划；Protocol initialize/audit query 仍缺 tools/list、resources/list、并发 request、progress/cancel、audit rotation/compaction/fsync。
- 沉淀审计：本轮仍是五个 OmniRoute space 内产品化切片，复用既有 subagent supervision、sidecar authority、explicit-fact、dry-run config preview、protocol audit redaction / local JSONL query 边界；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第二十二轮主控验收

- 第二十二轮继续按“一次性写完再整体测试”执行：Route profile-aware ledger path / append error surface、Doctor dev-bridge snapshot fixture gate、Quota explicit-fact shared helper、Extension TOML patch-plan dry-run、Protocol tools/resources list + audit pagination 五个切片由 subagents 并行实现；主控统一做聚合验证和文档写回。
- Route Resilience 新增 `ledgerError` response surface，append/truncate failure 不再静默吞掉，同时保留内存 history fallback；ledger path 支持从 `<profile>/config.yaml` 推导到 `<profile>/route-resilience/actions.jsonl`，测试覆盖 append failure、truncate failure、profile-aware path 写入。
- Doctor Workbench browser-preview fixture 改为 nested `droppedReason`，并保留冲突 `label/summary/refID/source`；新增 fixture gate 证明 preview/dev-bridge path 也保留 typed route authority，不靠文本补 truth。
- Quota Intelligence 将 Account/Status/Usage 的 explicit-fact-only 判定收敛到 `resolveExplicitQuotaFactDisplay(payload)` 共享 helper；统一矩阵覆盖 `quotaFact`、`quota_fact`、legacy `fact`，并验证 bait payload 不升级 authority。
- Extension Contract dry-run operation 增加 `patchPlan`，输出 `targetSection/operation/beforeSnippet/afterSnippet/validation`；生成 Skills `[[skills.config]]` 和 MCP `[mcp_servers.<id>]` 父 table 预览，但不投影 nested oauth 为 server、不输出 bearer token、不写真实 Codex config。
- Protocol Bridge MCP stdio handler 增加 minimal `tools/list` / `resources/list`，从本地 mapping fixture 投影 manifest 且不调用 executor；`JSONLAuditQuery` 增加 `Offset/Cursor`，结果返回 `NextCursor/HasMore`，保持 latest-first 语义。
- 聚合验证已通过：GetTokens Go aggregate、CLIProxyAPI `gettokenshooks` + management、frontend unit 911 项、frontend typecheck、contract artifact 59 checks、Extension/Doctor preview fallback、focused gates、docs check、父仓与 CLIProxyAPI `git diff --check`。
- 剩余项：Route 启动 hook 是否接 profile-aware ledger path 仍待后续聚合；Doctor 未做真实 Wails runtime/dev App；Extension patch plan 还不是 TOML 保存器；Protocol list/pagination 仍是 minimal manifest/numeric cursor；未来 Quota 新消费面仍需共享 helper 门禁。
- 沉淀审计：本轮仍是五个 OmniRoute space 内产品化切片，复用既有 sidecar authority、explicit-fact shared helper、dry-run TOML patch preview、protocol local JSONL pagination 边界；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第二十三轮主控验收

- 第二十三轮继续按“一次性写完再整体测试”执行：Route startup ledger path wiring、Doctor preview gate typed route check、Quota no-direct-fact-parser gate、Extension read-only TOML patch planner、Protocol list pagination/stable audit cursor 五个切片由 subagents 并行实现；主控统一做聚合验证和文档写回。
- Route Resilience 将 profile-aware ledger path 接到 CLIProxyAPI reference startup hook：`InstallRoutingPoliciesWithConfigPath(configPath)` 会驱动 ledger 写到 `<profile>/route-resilience/actions.jsonl`，测试 override 仍可覆盖到 `t.TempDir()`。
- Doctor Workbench preview gate 现在读取 preview fixture source，并检查 nested `droppedReason.accountKey/authId/model/source/scope/reason/routeBlocking`；同时确认冲突 `label/summary/source` 仍存在，且 fixture 不使用 legacy `routeEvidence` 作为 authority。
- Quota Intelligence 新增 no-direct-fact-parser 静态门禁，扫描 `frontend/src/features`；唯一 direct parser 入口限定为 `frontend/src/features/accounts/model/accountQuota.ts`，未授权新 feature 直接读 `payload.quotaFact` / `payload['quota_fact']` / `payload.fact` 会失败。
- Extension Contract read-only TOML planner 只解析调用方传入的 `configText`，不读取/写入真实 Codex config；可提取 `[[skills.config]]` 和精确 `[mcp_servers.<id>]` 父 table，nested `tools/oauth` 不作为 server，`bearer_token` literal 在 snippets 中 redacted。
- Protocol Bridge `tools/list` / `resources/list` 增加 `limit` + stable cursor 分页，cursor 形如 `pb-list-v1:<tools|resources>:<offset>`；audit cursor 从裸数字收敛为 `pb-audit-v1:<offset>`，malformed cursor 与 wrong-kind cursor 会拒绝。
- 聚合验证已通过：GetTokens Go aggregate、CLIProxyAPI `gettokenshooks` + management、frontend unit 911 项、frontend typecheck、contract artifact 59 checks、Extension/Doctor preview fallback、Quota static gate、focused gates、docs check、父仓与 CLIProxyAPI `git diff --check`。
- 主控修正：Extension `GetTokensExtensionRegistryFeature.tsx` 首轮 typecheck 因 generated class `convertValues` 缺失失败；已派回 Extension agent 用 generated `createFrom(...)` 构造 input，同时保持 `configText` preview-only 边界。
- 剩余项：Route 未重建 sidecar 或 dev App 验证 runtime path；Doctor preview gate 不替代真实 Wails runtime；Quota static scan 不是完整 AST parser；Extension 仍非真实 TOML AST writer；Protocol stable cursor 仍映射 offset，未解决 rotation/compaction 后跨文件稳定语义。
- 沉淀审计：本轮继续复用既有 sidecar startup path、preview fixture gate、quota shared-helper/static gate、read-only TOML planner、protocol cursor boundary；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。

## OmniRoute 第二十四轮主控验收

- 第二十四轮继续按“一次性写完再整体测试”执行：Wails binding generated surface consistency、CLIProxyAPI sidecar rebuild smoke evidence、Quota static gate docs-check integration、Extension sensitive-field redaction gate、Protocol package-level no-network verifier 五个切片由 subagents 并行实现；主控统一做聚合验证和文档写回。
- Wails binding 一致性补强：新增/强化 Go DTO/mapping tests 与 frontend generated source tests，证明 Doctor `droppedReason`、Extension dry-run `configText` / typed `patchPlan` 在 root/Wails/frontend generated surface 中不丢；本轮未运行 generator，只做最小手动同步并用测试覆盖。
- CLIProxyAPI reference 新增 bounded sidecar build smoke：`scripts/gettokens-sidecar-build-smoke.sh` focused 测试 `gettokenshooks`，构建 `./cmd/server` 到 `/private/tmp/gettokens-cliproxyapi-sidecar-smoke/cli-proxy-api-round24-smoke`，并运行 binary `-h` smoke；sha256 为 `7e76f226786fd96f7707bcd8817943fa28c12095e15ee8604a80b86a0e604e16`。
- Quota no-direct-fact-parser gate 已纳入 `docs-linhay/scripts/check-docs.sh`；当前扫描 `335` 个 frontend feature 文件、识别 `49` 个 fixture 文件、`findings: []`，后续 docs check 会自动防止新消费面绕过 shared helper。
- Extension read-only TOML planner 增强敏感字段 redaction：`token/api_token/headers/Authorization/cookie/secret` 类 key 的 RHS 会被隐藏，`bearer_token_env_var` 保持可见，前端 dry-run view model 展示前二次 redaction。
- Protocol Bridge 新增 no-network verifier：`docs-linhay/scripts/check-protocolbridge-no-network.mjs` 调用 `go test -tags protocolbridge_no_network`，覆盖 list/audit cursor 关键路径，规避当前沙箱对既有 `httptest.NewServer` 的监听限制。
- 聚合验证已通过：GetTokens Go aggregate、CLIProxyAPI `gettokenshooks` + management、frontend unit 911 项、frontend typecheck、docs check + quota static gate、sidecar build smoke、protocol no-network verifier、contract artifact 59 checks、Extension/Doctor preview fallback、focused gates、父仓与 CLIProxyAPI `git diff --check`。
- 剩余项：Wails generator 未运行；sidecar smoke binary 只能作为 dirty reference 测试侧证据，不能作为 release artifact；Quota static gate 仍非 AST parser；Extension redaction 还不是 TOML AST writer；Protocol no-network verifier 不替代非受限环境完整包测试。
- 沉淀审计：本轮复用既有 Wails binding hygiene、CLIProxyAPI reference build smoke、quota static gate、extension redaction、protocol no-network verifier 边界；没有新增跨领域 workflow 或 repo-wide 硬约束，因此不更新 skill 或 `AGENTS.md`。
