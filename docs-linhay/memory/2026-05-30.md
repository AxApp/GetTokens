# 2026-05-30

## 账号凭证 SQLite 迁移启动页排障

- 正式 App 迁移页报错根因定位为 sidecar `accountstore.Open` 未给 modernc SQLite 连接配置 `busy_timeout`。启动期 management API 与 watcher/synthesizer 并发访问 `accounts-v1.sqlite` 时，`EnsureSchema` 写入 metadata 遇到短暂写锁会直接返回 `SQLITE_BUSY`，表现为 `ensure account store metadata: database is locked (5)`。
- 已在 `docs-linhay/references/CLIProxyAPI` 的 `gettokens/sidecar` 分支提交修复：`e6655916 fix: wait for account store sqlite locks`。修复内容是使用 `file:` DSN 为 account store 每个连接设置 `_pragma=busy_timeout(5000)` 与 `_pragma=foreign_keys(1)`，并新增并发写锁回归测试。
- 验证通过：`go test ./internal/gettokens/accountstore -run TestStoreEnsureSchemaWaitsForConcurrentSQLiteWriter -count=1`、`go test ./internal/gettokens/accountstore`、`go test ./internal/api/handlers/management ./internal/watcher/synthesizer ./internal/gettokens/accountstore`、`go test ./...`。
- 已重建父仓 `build/bin/cli-proxy-api`，meta 指向 `e6655916:clean`，版本输出为 `v7.1.16-92-ge6655916`。
- 按用户要求不停止正式 App/sidecar。正式环境只读在线复制到 `~/.config/gettokens-dev` 与 `~/.config/gettokens-dev-data`，dev 配置隔离为端口 `18317`、`auth-dir: ~/.config/gettokens-dev`、`account-store-db: ~/.config/gettokens-dev/accounts-v1.sqlite`。
- dev 正式数据副本迁移闭环通过：迁移前 SQLite 账号 1 个；dry-run 候选 11 个，其中 auth-file 3 个、codex-api-key 8 个；commit imported 11、skipped 0、errors 0；delete legacy deleted 11；迁移后账号 12 个，其中 auth-file 4 个、codex-api-key 8 个。dev 日志未出现 `SQLITE_BUSY`、`database is locked` 或 `account store metadata` 错误。
- 边界：正式 `/Applications/GetTokens.app` 当前仍在运行旧 sidecar，不能直接停；需要后续安装包含 sidecar `e6655916` 的新版本或在安全窗口替换 sidecar 后，正式迁移页才会具备该修复。
- 风险：本次只修复账号 store 的启动/迁移锁等待。usage、rate-limit、live-session 等其他 SQLite store 仍有独立锁等待治理空间，但不属于本次账号凭证迁移阻塞路径。

## 账号凭证 SQLite token refresh 写回

- 用户确认迁移后 `auth_file_accounts.auth_json` 必须承载完整 auth-file JSON，不能再依赖旧 `codex-*.json`。凭证 refresh 会更新，因此 sidecar 需要在 refresh 成功后把新 token payload 写回 SQLite。
- 根因：迁移后 runtime auth 从 SQLite 合成，但 Codex 自动 refresh 成功后走 `coreauth.Manager.Update()` 的通用持久化入口；该入口原本仍是文件 store 语义，导致迁移后的 auth-file 账号没有稳定写回 `auth_file_accounts.auth_json`。
- 修复边界：sidecar 新增 account-store token store 包装层。`Auth.AccountKey=acct_*` 且账号为 `auth-file` 时，`Manager.Update()` 持久化会更新原账号的 `auth_file_accounts.auth_json`，保留 `account_key`，不新建账号卡，不写旧 auth-file；account store 内的非 `auth-file` 账号也不会 fallback 到旧文件 store。
- 运行时 token refresh 是当前 runtime 生成的新 credential snapshot，只更新类型表和 `updated_at_unix_ms`，不递增 `account_cards.revision`，避免重新排队 runtime apply；management API 发起的凭证/配置编辑仍必须 bump revision 并触发 apply。
- 已补测试覆盖 `UpdateAuthFileCredential` 不改 revision、`coreauth.Manager.Update()` 真实入口更新 SQLite、非 account-store auth 才 fallback、account-store 非 auth-file 不 fallback、refresh payload 缺套餐时保留已有 `plan_type`。

## 账号池导入账号写入 SQLite

- 用户发现账号池“导入账号”仍会创建 `codex-*.json` 文件。排查确认前端 `submitAccountImport` 对 auth-file payload 仍调用 Wails `UploadAuthFiles`，而该方法 POST 旧 sidecar `/v0/management/auth-files` multipart 上传端点；sidecar handler 会 `os.WriteFile` 到 `auth-dir`，所以这条链路没有进入 SQLite。
- 修复：公开 `UploadAuthFiles` 改为解码/规范化 auth JSON 后调用 `/v0/management/accounts` 创建 `kind=auth-file` 账号，写入 `auth_file_accounts.auth_json`。旧 `/auth-files` 上传逻辑拆成私有 `uploadLegacyAuthFiles`，仅供迁移前文件替换和 OAuth 文件回填兼容流程使用。
- 验证：`go test ./internal/wailsapp -run 'TestUploadAuthFilesConvertsChatGPTSessionToCPA|TestUpdateAuthFilePriorityPreservesDisabledStatus|TestUniqueAuthFileUploadName|TestApplyDeepLinkImportAuthFile|TestFinalizeCodexOAuth' -count=1`、`go test ./internal/wailsapp -count=1`、`go test ./internal/accounts -count=1`。

## 旧 auth-files management 端点废弃

- 用户要求旧 auth-files 端点直接废除报错，用于排查仍未切到 SQLite 的旧实现。根因确认：`UploadAuthFiles` 已切到 `/v0/management/accounts` 后，Wails 侧 `ListAuthFiles`、`DownloadAuthFile`、`SetAuthFileStatus`、`DeleteAuthFiles`、`GetAuthFileModels`、`FinalizeCodexOAuth`、quota fallback、usage attribution join 和 routing probe 仍有旧 `/v0/management/auth-files*` 调用；sidecar 自带 TUI client 也仍调用旧 auth-files 管理面。
- 修复：sidecar route 层把 `/v0/management/auth-files`、`/download`、`/models`、`/status`、`/fields` 的 GET/POST/PATCH/DELETE 入口统一改为 `410 Gone`，响应中指向 `/v0/management/accounts`；新增 `/v0/management/accounts/{account_key}/models` 替代旧模型查询。
- 修复：GetTokens/Wails auth-file 读写、下载、禁用、删除、优先级更新、OAuth 回填、quota fallback、usage attribution join、Codex routing probe 全部改走 account store；sidecar TUI 的 auth tab 也改走 `/accounts`。
- 验证：`go test ./internal/wailsapp -count=1`、`go test ./internal/cliproxyapi ./internal/accounts -count=1`、sidecar `go test ./internal/tui ./internal/api/handlers/management -count=1`、`go test ./internal/api/handlers/management ./internal/gettokens/accountstore ./sdk/cliproxy -count=1`。

## 导入和 OAuth 登录链路复查

- 复查入口：前端 `submitAccountImport`、Wails `UploadAuthFiles`、deep link `ApplyDeepLinkImportAuthFile`、Codex OAuth `StartCodexOAuth/GetOAuthStatus/FinalizeCodexOAuth`、sidecar `saveTokenRecordToAccountStore`、runtime token refresh `accountStoreTokenStore.Save`。
- 结论：auth-file 导入和 deep link 导入均经 `/v0/management/accounts` 创建 `kind=auth-file` 账号并写入 `auth_file_accounts.auth_json`；生产代码扫描未发现 GetTokens/Wails/frontend 对旧 `/v0/management/auth-files*` 的调用。sidecar 仍会在 auth-dir 写 OAuth callback 临时 `.oauth-*` 文件，但这不是账号凭证事实源。
- 修复：Codex OAuth 二次登录命中已有 SQLite 账号时，sidecar 更新凭证前保留原 `account_key`、`title`、`source_file_name`、`priority`、`disabled`，只替换 `auth_json` 和派生 metadata，避免用户禁用态/优先级/卡片名称被新登录结果重置。
- 修复：Wails `FinalizeCodexOAuth` 兼容 sidecar 原地更新成功；如果没有 replacement auth-file，不再误报“未找到新的 codex 登录结果”，同时仍禁止调用废弃 `/auth-files` 端点。
- 验证：`go test ./internal/wailsapp -run 'TestUploadAuthFilesConvertsChatGPTSessionToCPA|TestApplyDeepLinkImportAuthFile|TestFinalizeCodexOAuth|TestStartCodexOAuth|TestUpdateAuthFilePriorityPreservesDisabledStatus' -count=1`、`go test ./internal/wailsapp ./internal/cliproxyapi ./internal/accounts -count=1`、sidecar `go test ./internal/api ./internal/api/handlers/management ./sdk/api/handlers ./sdk/cliproxy -count=1`、`rg -n "auth-files" internal frontend/src docs-linhay/references/CLIProxyAPI/internal docs-linhay/references/CLIProxyAPI/sdk -g'*.go' -g'*.ts' -g'*.tsx' -g'!*_test.go'` 无生产调用。

## 迁移后账号 pending 与 API key 额度刷新

- 用户反馈“公司 1”显示异常且额度不显示。只读检查正式 sidecar `/v0/management/accounts` 确认该账号是 `codex-api-key`：`account_key=acct_dd2172ea-9dd9-458a-88bd-590cc55a468c`，未禁用，`quota_enabled=true`，quota curl 存在，但 `runtime_apply_status=pending`。
- 根因一：sidecar 迁移提交和删除旧源只触发 runtime reload，没有把本轮已重建的 `account_runtime_apply_state` 从 `pending` 批量标记为 `applied`。旧正式数据因此重启后仍在管理面显示 pending。
- 根因二：Wails `GetCodexQuota` 对 API key 额度仍只支持旧 `codex-api-key:*` 本地 store ID；迁移后的账号卡使用 `acct_*` 作为 `quotaKey`，导致无法从 SQLite/management API 读取 `api_key/base_url/quota_curl`。
- 修复：sidecar 新增 pending runtime apply 批量标记，迁移 commit、delete legacy sources 和 `/accounts` 列表自愈在 runtime apply hook 成功后把当前 revision 标成 `applied`，失败则标成 `failed`。这既覆盖新迁移，也覆盖已存在 pending 的旧 DB。
- 修复：Wails `GetCodexQuota` 对 `acct_*` 先查 `/v0/management/accounts/{account_key}`，按账号类型分别读取 SQLite 中的 auth-file JSON 或 codex API key credential；auth-file 查找也支持按 `AuthIndex/account_key` 匹配。
- 验证：父仓 `go test ./...` 通过；sidecar `go test ./...` 通过；针对性测试覆盖 `acct_*` API key 额度刷新、`acct_*` auth-file quota fallback、迁移 commit 后 pending -> applied。
