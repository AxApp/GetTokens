# 2026-06-01

- Codex DeepSeek/openai-compatible 第一阶段已实施：DeepSeek 默认模型使用真实模型名 `deepseek-v4-flash`、`deepseek-v4-pro`，不自动 alias 为 `codex-deepseek-*`。
- Codex-facing 模型选项语义固定为 `alias || name`：无 alias 显示真实模型名；用户主动配置 alias 时只显示 alias，真实上游模型名仅保留给 route engine / executor 上游请求。
- GetTokens 前端 route probe 模型候选已合并 relay catalog，避免 DeepSeek/openai-compatible 自动下发模型无法在 Codex 页面选择。
- CLIProxyAPI fork 增加 Codex channel 静态 DeepSeek v4 模型、DeepSeek openai-compatible provider 默认模型注册、`alias || name` 匹配、Responses 请求接到 OpenAI-compatible `/chat/completions` executor 的非流式测试、DeepSeek reasoning 专门映射，以及 Chat SSE -> Responses SSE executor 热路径测试。
- DeepSeek reasoning 决策：仅在 provider/baseURL 可识别为 DeepSeek 时启用 DeepSeek applier；`xhigh|max` 映射为 `reasoning_effort=max`，其他启用档映射为 `high`，显式 `none` 转为 `thinking.type=disabled` 且不发送 `reasoning_effort`。
- Browser preview/mock DeepSeek provider 也同步为 `deepseek-v4-flash`、`deepseek-v4-pro` 无 alias，避免预览误导；CLIProxyAPI 新增 Codex `/v1/models?client_version` catalog 测试证明已注册 DeepSeek openai-compatible 模型会下发到 Codex model options。
- 已验证：主仓 Wails 聚焦测试、前端 Codex/OpenAI-compatible/previewData 单测、前端 typecheck、CLIProxyAPI registry/util/executor/cliproxy/openai handler 聚焦测试、CLIProxyAPI `internal/thinking/...` 与 existing responses translator 测试。剩余风险是尚未用真实 DeepSeek API 做实机请求。
- 追加上下游 mock smoke：CLIProxyAPI handler 级测试从下游 `/v1/responses` 进入，经 auth manager 选中 `openai-compatibility` DeepSeek auth，上游 mock 断言收到 `/v1/chat/completions`；非流式与 SSE 两条链路均通过 `go test ./sdk/api/handlers/openai -run 'TestCodexDeepSeekOpenAICompatibleResponses(HTTPDownstreamChatUpstream|StreamDownstreamChatSSEUpstream)Smoke'`。
- 追加真实 DeepSeek dev 冒烟：用临时 dev config 和隔离 `CODEX_HOME`，从正式 SQLite DeepSeek 账号复制 API key 到临时 `openai-compatibility` provider；`GET /v1/models?client_version=0.133.0` 返回 `deepseek-v4-flash`，非流式 `POST /v1/responses` 返回 `status=200 object=response usage total_tokens=10`，SSE 返回 `response.output_text.delta` 与 `response.completed`。临时 sidecar 已停止，含 key 的 `/tmp/gettokens-deepseek-real-smoke.*` 已删除。
- 追加真实 Codex CLI 进程冒烟：使用 `codex-cli 0.133.0`、隔离 `CODEX_HOME` 和临时 CLIProxyAPI dev sidecar，真实运行 `codex -a never exec --model deepseek-v4-flash "Reply with exactly: ok"`；Codex CLI exit code 为 `0`，最后消息为 `ok`，sidecar 收到 `/responses` 请求。含 key 的 `/tmp/gettokens-codex-cli-deepseek-smoke.*` 临时目录已删除。
- 会话整理沉淀：真实 Codex CLI 冒烟流程已写入项目级 `gettokens-codex-account-list` skill，适用于 Codex model catalog、openai-compatible alias、Responses <-> Chat/SSE 桥接和 DeepSeek 等上游协议适配；本轮不升级 `AGENTS.md`，因为规则属于 Codex 账号/路由验证边界，不是 repo-wide 治理。
