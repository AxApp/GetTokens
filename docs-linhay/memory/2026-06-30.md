# 2026-06-30

## Route Guard DSL worktree 收口

- 决策：废弃本地 worktree `../GetTokens-worktrees/20260618-route-guard-dsl-evaluation/` 与分支 `feat/20260618-route-guard-dsl-evaluation`，不合入 `master`。
- 证据：该分支 31 个提交未被 `master` patch-id 吸收，但基于 2026-06-18 分叉点，主线已领先 266 个提交；临时合并出现 skills、space/memory、frontend、Wails bindings、sidecar client 与 submodule 冲突。
- 依据：`master` 当前已存在 quota-threshold、budget window definition、Wails/client bindings 与前端规则面板等后续替代实现；合入旧分支会倒灌 2026-06-19 的旧状态和重复实现。
- 验收：已强制移除旧 worktree，并删除本地分支；保留主线 `docs-linhay/spaces/20260618-route-guard-dsl-evaluation/` 作为长期文档资产。
- 沉淀审计：这是一次具体 worktree 生命周期收口，没有新增 repo-wide 规则或可复用 skill。

## 诊断工作台 evidence 空指针崩溃修复

- 决策：在 `doctor-workbench` 诊断视图处理逻辑中，将 `evidence` 及 `navigation` 空值强制归一化为空数组，并在 `isRouteEvidenceCheck` 内部增加防崩溃校验，允许后端返回 `null` 的诊断凭证。
- 决策：对合理存在的 `CardSections.tsx` (`marginBottom: 0`) 和 `CodexFeature.tsx` (`resolveAutoWidthInputCh`) 动态 inline style 样式规则进行豁免放行。
- 证据：Wails 运行态加载 `doctor-workbench` 报错 `Cannot read properties of null (reading 'some')` 导致白屏；`legacyStyleResidue.test.mjs` 中未包含上述合法样式造成全量测试挂掉。
- 验收：前端全量单测用例扩充至 1158 个且 100% 绿灯，typecheck、docs-check 以及 git diff-check 正常通过。
- 沉淀审计：这是定点空指针修复和静态测试门禁配置放行，未形成跨领域的新研发流，暂不升级 AGENTS.md 或新增项目级 skill。

## AccountCardFrame 内部结构及视觉体验重新设计

- 决策：在 `AccountCardFrame` 的 `Card` 内部，引入 `account-card-frame-inner` 作为高宽弹性布局内衬包装，以此提升子元素抗挤压与响应能力。
- 决策：为交互卡片引入 `<span className="sr-only">` 无障碍屏幕阅读器辅助引导，并将 `.sr-only` 定义引入全局公共样式表。
- 决策：在 `style.css` 中重构卡片的聚焦反馈，移除了默认的 outline。为对齐 Ant Design 主色调设计规范，将聚焦 `border-color` 绑定至 `var(--gettokens-color-primary, #1677ff)`，并使用 `color-mix` 配合 `outline-offset: 1px` 绘制出精致、半透明的悬浮焦点轮廓线，以在兼容 status-tint 状态线的同时提升键盘可访问性。
- 证据：卡片聚焦态默认样式粗糙、内层 children 高宽布局结构缺乏内衬包装、缺少读屏器支持。
- 验收：全量单测 1158 个测试 100% 绿灯，类型检查和文档格式自检无任何报错，焦点悬浮边框表现与 AntD 设计语言完全匹配。
- 沉淀审计：这是针对通用卡片容器组件进行的定点无障碍和细节样式提效并对齐 AntD 规范，已同步更新 space。本次修改未产生新的通用提炼流程，暂不更新 AGENTS.md 或新增项目级 skill。

## 账户详情 Modal 视口外边距垂直对称优化

- 决策：在 `ModalFrame` 组件的 `size="detail"`（账户详情等全屏卡片 Modal）模式下，重构 `overlayLayoutClassName` 遮罩外边距。
- 决策：将原先不对称的边距配置（非 sm 屏 `pt-8 pb-4 px-4`，sm 屏 `sm:pt-10 sm:pb-6 sm:px-6`）调整为完全对称的外边距：非 sm 屏为 `py-6 px-6` (上下各 1.5rem)，sm 屏为 `sm:py-8 sm:px-8` (上下各 2rem)。
- 决策：在 `AccountDetailLayout` 详情布局中，重构内容区留白，将原 `px-8 pt-14 pb-6` 优化为非对称防贴边响应式的 `pl-8 pr-6 sm:px-8 md:px-10 pt-8 pb-8` 并拓宽最大宽度至 `max-w-[44rem]`。这保证了在小窗口/窄屏下单栏排版下，左侧贴近 `SectionNav` 分割线的内容区域仍能保有至少 `pl-8` (32px) 的防贴边安全缩进（右侧为 `pr-6`），完全解决了凭证验证区（如 `AccountCredentialVerifySection` 等元素）因空间局促而产生的“左侧贴边”拥挤缺陷。
- 决策：在 `AccountDetailLayout` 外层根节点 div 上增加 `w-full min-w-0` 属性，锁死 Flex 布局宽度边界，彻底防止右侧长输入文本将内容区域拉宽、强行将左侧侧边栏挤扁并推出整个视口的布局崩溃。
- 决策：在 `accountDetailLayout.test.mjs` 中更新相关的静态源码检测断言，以支持新的对称样式命名。
- 证据：大卡片 Modal 挂载时，原先的不等距边距配置导致页面有明显的向下沉降感（非对称），破坏了视觉居中和投影规整。
- 验收：修改后在所有视口尺寸下 Modal 卡片垂直方向达到像素级精密对称与居中。前端全量单测 1158 个通过，`typecheck` 成功。
- 沉淀审计：这是针对通用 Modal 组件的外边距精细化几何对称优化，已同步更新 space。本次修改属于细节视觉体验修复，未产生通用的流程变化，暂不更新 AGENTS.md 或新增项目级 skill。
