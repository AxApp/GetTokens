# 2026-05-21

## Codex Binary 设计系统圈定

- `CodexBinarySummaryPanel` 与 `CodexBinaryVersionCell` 已在运行时根节点暴露 `data-design-system-component="true"` 和对应 `data-design-system-component-name`，设计系统项目页高亮可直接圈定 Codex Binary 摘要面板与版本行。
- `frontend/src/features/design-system/storyCatalog.test.mjs` 已把这两个业务组件加入 runtime marker 门禁，防止后续重构丢失该设计系统标记。

## Codex Account Order 设计系统复用

- `CodexAccountOrderSection` 的 `InlineActionControls` 已将显示模式与账号过滤两组手写分段按钮替换为通用 `SegmentedControl`，删除局部 `DensityButton` 实现。
- 当前验证通过 `node --test frontend/src/features/codex/codexAccountList.test.mjs` 与 `npm --prefix frontend run build`；后续全量 `npm --prefix frontend run typecheck` 已恢复通过。

## SearchInput 业务搜索框统一

- `AccountsToolbar`、`ProxyPoolFeature`、`StatusCodexFeaturesSection`、`SessionManagementSearchBar`、`UnifiedComposeModal` 的业务搜索框已统一替换为设计系统 `SearchInput`；代理池测速网址输入不属于搜索框，保持普通输入。
- 已复扫确认剩余手写输入为文件选择、表单字段、测速 URL 或 Story 示例，不属于业务搜索框。
- 已验证相关 accounts/proxy/status/design-system 单测、`npm --prefix frontend run typecheck` 与 `npm --prefix frontend run build` 通过。

## Codex OAuth 本地应用 tokens 结构修正

- 根因：GetTokens / sidecar 会把 Codex auth-file 归一化成扁平结构（`type=codex`、`access_token`、`id_token`、`refresh_token`、`account_id`），并移除 nested `tokens`；账号卡 OAuth 应用到 Codex 的后端写入逻辑只识别原生 Codex nested `tokens`，导致真实写入时报“Codex OAuth auth.json 缺少 tokens”。
- 修正：`buildRelayCodexOAuthAuthJSON` 现在同时接受原生 nested tokens 与 sidecar flat auth-file，写入 `CODEX_HOME/auth.json` 前统一转回 Codex 原生结构：`auth_mode=chatgpt`、`tokens.id_token/access_token/refresh_token/account_id`，并移除根部 API key / flat token 字段。
- 验证：新增 sidecar flat auth-file 回归测试；`go test ./internal/wailsapp` 通过。

## Codex API Key 本地应用 auth.json 最小化

- Codex 官方源码 `codex-rs/login/src/auth/manager.rs` 的 `login_with_api_key` 会写出只含 `auth_mode=ApiKey`、`OPENAI_API_KEY` 的 auth payload；`tokens`、`last_refresh`、`agent_identity` 不属于 API key 模式。
- GetTokens 账号模板应用到 Codex 的 API Key 模式已改为重建最小 `CODEX_HOME/auth.json`，清理旧 OAuth tokens、refresh 时间、agent identity 和 user metadata，避免一个账号配置同时残留 API key 与 OAuth 语义。
- 确认页 diff 与账号配置 snippet 已同步显示最小 auth payload；验证通过 `go test ./internal/wailsapp -run 'TestApplyRelayServiceConfigToLocal|TestGetLocalCodexAuthState'`、相关前端 node tests 与 `npm --prefix frontend run typecheck`。

## Codex auth.json 读取优先级校准

- Codex 源码 `AuthDotJson.resolved_mode()` 先使用显式 `auth_mode`，缺失时才根据 `OPENAI_API_KEY` fallback 到 API Key，否则默认 ChatGPT/OAuth。
- GetTokens `GetLocalCodexAuthState` 已按该优先级修正：`auth_mode=chatgpt` 且残留 `OPENAI_API_KEY` 时仍识别为 ChatGPT/OAuth，可保留 tokens，但给出残留字段 warning；`auth_mode` 缺失且存在 `OPENAI_API_KEY` 才识别为 API Key。
- 回归测试新增 `auth_mode=chatgpt + OPENAI_API_KEY + tokens` 场景，防止后续把 OAuth 登录态误判成 API Key。

## v1.0.14 发布完成

- 已提交账号模板应用到本机 CLI 的实现与版本号更新：`08a8a39 feat: apply account templates to local cli`、`025484c chore: bump version to 1.0.14`。
- 已推送 `master` 与 tag `v1.0.14`，GitHub Release 地址：https://github.com/AxApp/GetTokens/releases/tag/v1.0.14。
- Release workflow `26178243596` 已成功完成，产物包含 Apple Silicon / Intel 的 DMG、tar.gz 与 `checksums.txt`。
- 分发校验已通过：`shasum -a 256 -c checksums.txt`、两个 DMG 的 Gatekeeper 与 stapler、包内 app `codesign --verify --deep --strict`、架构、`CFBundleShortVersionString=1.0.14`、`CFBundleVersion=1.0.14`、arm64/amd64 Sparkle `SUFeedURL` 与远端 appcast `1.0.14` 条目。

## 账号模板本机 CLI 应用会话沉淀

- 本轮“整理会话”未新增第 9 个项目 skill，避免继续扩大 skills 列表；稳定规则并入 `gettokens-domain-engineering` 与 `gettokens-ops-governance`。
- `gettokens-domain-engineering` 新增账号模板本机 CLI 应用规则：官方/已验证模板才展示目标按钮、DeepSeek 当前只展示 Claude Code、账号应用模式由来源固定、Codex API key/OAuth `auth.json` 写入语义必须对齐源码、Claude Code 只 patch 受控 `env`、账号列表先快速渲染已有信息再增量 enrichment。
- `gettokens-ops-governance` 补充发布后规则：tag 指向实际发版代码，发布后的 docs/memory commit 不移动 tag；DMG 分发验收还需确认远端 per-arch Sparkle appcast 已包含当前版本。
- 沉淀文档已写入 `docs-linhay/dev/20260521-account-template-cli-apply-session-distillation.md`，本次无需更新 `AGENTS.md`。

## Claude Code 与 Codex 能力对齐 space

- 新建 `docs-linhay/spaces/20260521-claude-code-codex-alignment/`，用于承接 Claude Code 后续完善方向，不与已完成的 `20260519-claude-code-account-list` 混合。
- 当前判断：Claude Code 已在账号列表 / 请求顺序 / 路由探测 / 模型映射上与 Codex 基本对齐；下一步优先补 `Skills / commands` 与 `MCP servers` 扩展资产工作台，复用 Codex Extensions 的 UI 与纯模型逻辑，但后端按 Claude JSON / directory adapter 实现。
- Claude Code settings、hooks、permissions、subagents、output styles 属于 Claude-only enhancement 或中等语义对齐，不应强行套用 Codex feature config；后续应基于 preservative JSON / Markdown patcher 分期实现。
- 用户确认这套后续能力是 Claude Code 专属体系，每一块进入实现前都要先做技术调研；已新增 `plans/20260521-technical-research-index.md`，要求账号路由、Skills/Commands、MCP、Settings/Hooks/Permissions/Output Styles、CLAUDE.md/Memory、Subagents、Session/Usage、Binary/Runtime 分别产出 research 文档。
- 本地参考项目治理更新：`docs-linhay/references/*/` 源码目录默认不进 git，只提交根部 Markdown 索引和调研结论；既有已跟踪参考目录暂作为历史遗留，不在本轮批量 untrack。
- 已按“补充技术细节，需要验证过可行”补齐 8 个 Claude Code 能力块的 research 文档：账号路由、Skills/Commands、MCP、Settings/Hooks/Permissions/Output Styles、CLAUDE.md/Memory、Subagents、Session/Usage、Binary/Runtime。当前可进入下一步 BDD/TDD 的 P0 仅限 Skills/Commands 与 MCP；Session/Usage 和 Binary/Runtime 明确只做只读摘要或 PATH doctor，不做写入/安装管理。
- 用户补充“相关业务先进行设计系统打磨”；已新增 `plans/20260521-design-system-business-polish-plan.md`，把 Claude Code P0 资产工作台的 Skills/MCP 组件、Storybook 状态矩阵、candidate/deferred 边界和验证门禁前置到产品开发之前。
- Claude Code 资产工作台设计系统第一刀已落地：新增 `ClaudeCodeAssetWorkbench` 纯展示组件与 Storybook mock 矩阵，覆盖 Skills、legacy commands、MCP scope precedence、secret redaction、empty、parse error 和 saving diff；已登记到 `storyCatalog.ts` 与 `componentManifest.ts`，并归档 desktop/mobile Storybook 截图。该阶段不接 Wails、sidecar、fetch 或真实文件写入。
- 用户指出需要参考 Codex 设计；已将 `ClaudeCodeAssetWorkbench` 从独立 dashboard 收敛到 `CodexExtensionsFeature` 的设计骨架：`WorkspacePageHeader`、投影单面板、顶部 filter/search、`divide-y` 列表和右侧 preview rail。Claude 仍保留自身语义差异：不复用 Codex skill enabled toggle，展示 legacy command、scope precedence、shadowed、secret redaction 和 preservative diff。
- 按“开始实施，记得复用组件”完成第一刀前端接入：Claude workspace 新增 `extensions`，侧边栏显示“扩展资产 / Extensions”，`#frame=claude&workspace=extensions` 可路由到 `ClaudeCodeAssetWorkbenchFeature`；该页面复用设计系统 `ClaudeCodeAssetWorkbench` 和抽出的 `assetPreviewData`，只做 preview/mock、tab 与搜索过滤，不接真实 Wails/sidecar 文件扫描。
- 复用层级已从“视觉参考 Codex Extensions”提升到“共享组件复用”：新增 `AssetWorkbenchShell` 通用组件，Codex Extensions 与 Claude Code Asset Workbench 都引用同一 shell；`storyCatalog.test.mjs` 增加门禁防止回退到两套壳。已通过 `node --test frontend/src/features/design-system/storyCatalog.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build-storybook`，并用浏览器验证 Claude extensions、Codex skills、Codex MCP 三条入口。
- Claude Code 资产工作台已从 preview/mock 推进到 Wails app 内真实只读扫描：新增 `GetClaudeCodeExtensionsSnapshot`，扫描 `$CLAUDE_CONFIG_DIR/skills`、项目 `.claude/skills`、项目 `.claude/commands/*.md`、`~/.claude.json` user/local MCP 与项目 `.mcp.json` MCP；root Wails binding、前端 feature 与 `frontend/wailsjs` 已接通。当前只读展示，不声明支持 MCP 保存/patch。
- 本轮真实接入仍复用 Codex 设计壳：`ClaudeCodeAssetWorkbenchFeature` 只做 Wails snapshot、preview fallback 和 DTO 映射，`ClaudeCodeAssetWorkbench` 与 `CodexExtensionsFeature` 继续共用 `AssetWorkbenchShell`；设计系统测试保留双端复用门禁。验证通过 `go test ./internal/wailsapp -run 'ClaudeCode.*Extensions|ApplyClaudeCode'`、`go test .`、`go test ./...`、`node --test frontend/src/features/claude-code/claudeCodeAssetWorkbench.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build` 与 `./scripts/wails-cli.sh build -skipbindings`。
- Claude Code MCP 保存第一刀已完成：新增 `SaveClaudeCodeMcpServer`，支持 project `.mcp.json`、user `~/.claude.json.mcpServers`、local `~/.claude.json.projects[projectPath].mcpServers` 的单 server preservative patch；只更新 `type` 与 endpoint 对应字段，保留其它 server、project section、top-level 和 server 内未知字段。前端 MCP 行内 `Edit` 可编辑 transport / endpoint，桌面 app 真实保存，browser preview 只标记 mock dirty。
- MCP 保存验证通过：`go test ./internal/wailsapp -run 'ClaudeCode.*Extensions|SaveClaudeCodeMcpServer'`、`go test .`、`go test ./...`、`node --test frontend/src/features/claude-code/claudeCodeAssetWorkbench.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build`、`./scripts/wails-cli.sh build -skipbindings`；无头浏览器打开 `http://127.0.0.1:5173/#frame=claude&workspace=extensions`，确认 MCP tab 与 Edit endpoint 输入、Save/Cancel 控件可渲染。
- Claude Code 会话管理第一刀已完成：Claude workspace 新增 `session-management`，后端只读扫描 `~/.claude/projects/**/*.jsonl`，跳过 `subagents/` 与 `agent-*`，复用 Codex `SessionManagementSnapshot` / `SessionManagementFeature` 展示项目、会话、脱敏详情和 `claude --resume <sessionId>` 摘要语义。
- 该功能保持 Claude 原生 session 不写入、不删除、不归档，不做 Codex provider merge；前端 provider mapping 编辑入口仅在 Codex workspace 展示，session snapshot cache 已按 `codex` / `claude` 分 key，避免跨工作区污染。
- 会话管理验证通过：`go test ./internal/wailsapp -run 'ClaudeCodeSession|CodexSessionManagement'`、`go test .`、`go test ./...`、`node --test frontend/src/features/session-management/cache.test.mjs frontend/src/features/session-management/model.test.mjs frontend/src/utils/pagePersistence.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build`、`./scripts/wails-cli.sh build -skipbindings`；无头浏览器打开 `http://127.0.0.1:5173/#frame=claude&workspace=session-management` 实测扫到 8 个项目、38 条主会话，无 console error，截图已归档到对应 space。
- Claude Code 用量 tab 第一刀已完成：Claude workspace 新增 `usage`，复用 Usage Desk observed 图表和明细表，只展示 Sidecar Attribution 中可识别为 Claude / Anthropic 的请求用量；不读取、不估算 Claude 原生 session token，不展示 Codex-only 本地投影用量、刷新索引和重建索引操作。
- 同步移除 Usage Desk 里此前预留但未接真源的 `gemini` workspace 适配：`UsageDeskWorkspace` 只保留 `codex` / `claude`，`gemini` 存储值回退到 `codex`；旧 `usage-gemini` 不再是有效 workspace。
- 用量 tab 验证通过：`node --test frontend/src/features/accounts/tests/usageDesk.test.mjs frontend/src/utils/pagePersistence.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run build`；无头浏览器打开 `http://127.0.0.1:5173/#frame=claude&workspace=usage` 确认有 `Claude Usage Desk` / `Claude Attribution`，没有“本地投影用量”和 Gemini placeholder，截图已归档到对应 space。
- Claude Code Skills / MCP 已从合并的 `extensions` 入口拆成二级菜单和两个独立页面：`#frame=claude&workspace=skills` 渲染 `ClaudeCodeSkillsWorkspace`，`#frame=claude&workspace=mcp-servers` 渲染 `ClaudeCodeMcpServersWorkspace`；旧 `extensions` hash 与本地存储值迁移到 `skills`。页面内不再用 Skills/MCP segmented tab 做导航，和 Codex `skills` / `mcp-servers` 菜单保持一致。

## Codex 实时运行会话详情需求

- 用户希望基于每个 Codex 会话/请求已有标识，实时查看正在运行的会话详情，尤其关注 WebSocket 上下游连接、账号命中、request id、上游断开、重连与 HTTP fallback 推断。
- 已新建 `docs-linhay/spaces/20260521-codex-live-session-detail/`，定位为 Codex 一级工作区下的新 `live-sessions` tab，中文建议“运行会话”；它不同于现有 `session-management`，后者偏本地历史 session 文件，本需求偏 sidecar 运行时观测。
- 第一期开工边界：只读展示 sidecar runtime event 聚合、Wails snapshot/detail API、request id 搜索、脱敏诊断摘要、WebSocket -> HTTP fallback 推断提示；不做请求重放、取消、强制恢复 WebSocket、完整 payload 查看或长期事件库。
- 已补充详细技术调研和 UI / 设计系统方案：运行事件聚合建议落在 CLIProxyAPI `gettokenshooks` 内存 ring store，Wails 只做 management API 透传和 DTO 映射；前端按 Swiss-industrial 运维控制台设计，新增 `codex-live-sessions` feature，并要求业务组件通过 Storybook mock、`componentManifest.ts` 与 `storyCatalog.ts` 收编进设计系统。
- 关键风险结论：Codex upstream fallback 是 session-scoped sticky 状态，GetTokens 只能观测和提示，不能在 Codex 已降级 HTTP 后透明恢复 WebSocket。
- Codex 运行会话第一版前端 mock 已补入请求级速率/耗时测量：queue wait、auth select、upstream connect、TTFT、first token、stream duration、total duration、average/max event gap、output tokens/s、total tokens/s、reconnect count；工作台详情和脱敏诊断摘要使用同一 timing formatter。
- 用户要求 `CodexLiveSessionsWorkbench` 按单列表设计收敛：已移除左右两栏详情布局，改为 session 单列表行内展示 transport / model / auth / timing 摘要，选中行在同一列表流内展开完整详情。
- 用户继续要求“设计简洁些”：行内摘要进一步压缩为状态、session id、模型、账号、transport、TTFT、first token、output rate；展开详情里的 timing 从多卡片改为轻量键值表，transport lane 降低边框权重。
- 用户指出 `SessionFeed` 仍“看不清楚、布局很乱”：已重做为固定列列表，桌面列为 Status、Session/request、Route、Latency、Open；移动端每行按 Status/Session/Route/Latency 分块显示，减少横向混排和无标签指标。
- Codex 运行会话组件已接入现有 `useI18n` 本地化：`CodexLiveSessionsWorkbench` 可见文案、筛选项、SessionFeed 表头、状态、空态、Sidecar 未就绪提示、详情分区标题与未知值占位已写入 `codex_live_sessions` 翻译命名空间，并同步 `zh.json` / `en.json`。
- 用户确认继续实现真实后端数据链路；已完成 CLIProxyAPI fork 内存 live session tracker、管理端点 `/v0/management/gettokens/live-sessions`、Responses WebSocket handler / Codex WebSocket executor / Codex usage record 接入，以及 GetTokens Wails `GetCodexLiveSessionsSnapshot` 转发和前端 2 秒轮询真实 snapshot。
- 当前 `Codex -> 运行会话` 默认列表只展示用户关心的状态、模型/账号、连接方式、输出速率、首 token、运行时长；内部 request/session/execution id 与脱敏诊断保留在展开详情。行点击支持再次收起。
- 实现边界保持只读观测，不做请求取消、重放、强制恢复 WebSocket 或完整 payload 展示。验证通过 CLIProxyAPI `go test ./...`、GetTokens `go test ./...`、前端 typecheck/model+design-system tests/build、`./scripts/wails-cli.sh generate module` 与 `./scripts/wails-cli.sh build -skipbindings`。

## Codex 运行会话整理沉淀

- 本轮“整理会话”将 Codex live sessions 的可复用模式并入 `gettokens-domain-engineering`，不新增独立 skill，也不升级 `AGENTS.md`。
- 稳定规则：运行时观测类 Codex tab 要从 CLIProxyAPI fork 的内存 tracker / management API 起步，Wails 只做透传 DTO，前端桌面轮询真实 snapshot、浏览器使用 preview mock。
- 稳定边界：默认 UI 只展示用户关心的状态、模型/账号、连接方式、速率和关键耗时；内部 ids 与脱敏诊断放在展开详情；不展示 raw payload、凭据、cookies 或完整错误体。
- 关键排障规则：WebSocket request id 必须贯穿 handler、executor 和 usage hook；usage hook 命中已知 request id 时更新已有 WebSocket request，不新建重复 HTTP session。
- 风险结论保持：GetTokens 只能观测和提示 Codex upstream HTTP fallback，不能承诺在同一已降级会话中透明恢复 WebSocket。
- 沉淀文档已写入 `docs-linhay/dev/20260521-codex-live-sessions-session-distillation.md`。

## Claude Code Workspace 拆分整理沉淀

- 本轮“整理会话”将 Claude Code 对齐 Codex 菜单的规则并入 `gettokens-domain-engineering`，不新增独立 skill，也不升级 `AGENTS.md`。
- 稳定规则：Claude Code 功能若对应 Codex 已有独立 workspace，应优先保持同样的 workspace 粒度；例如 Codex `skills` / `mcp-servers` 是两个页面，Claude 也应是 `ClaudeCodeSkillsWorkspace` / `ClaudeCodeMcpServersWorkspace` 两个页面，而不是一个 `extensions` 页面内 tab。
- 允许复用 `AssetWorkbenchShell`、preview data、DTO mapper 和列表视觉模式，但共享 UI 不能把不同导航表面重新折叠成 tab。
- 旧 hash / localStorage 兼容必须显式迁移；本轮 `extensions` 迁移到 `skills`。
- 沉淀文档已写入 `docs-linhay/dev/20260521-claude-code-workspace-split-session-distillation.md`。

## v1.0.15 发布完成

- 已提交并推送本轮功能与版本号更新：`c08c679 feat: align claude code workspaces`、`d247038 chore: bump version to 1.0.15`。
- 已推送 `master` 与 tag `v1.0.15`，GitHub Release 地址：https://github.com/AxApp/GetTokens/releases/tag/v1.0.15。
- GitHub Actions Release run `26222815136` 已成功，产物包括 `GetTokens_macOS_AppleSilicon.dmg`、`GetTokens_macOS_AppleSilicon.tar.gz`、`GetTokens_macOS_Intel.dmg`、`GetTokens_macOS_Intel.tar.gz`、`checksums.txt`。
- 分发校验已通过：`shasum -a 256 -c checksums.txt`、两个 DMG 的 Gatekeeper `accepted` 与 `stapler validate`、包内 app `codesign --verify --deep --strict`、签名链 `Developer ID Application: HAN LIN (3L8RM3MDLS)`、`Notarization Ticket=stapled`、arm64/x86_64 架构、`CFBundleShortVersionString=1.0.15`、`CFBundleVersion=1.0.15`。
- Sparkle 远端 appcast 已包含当前版本：arm64 指向 `GetTokens_macOS_AppleSilicon.dmg`，amd64 指向 `GetTokens_macOS_Intel.dmg`，两者均为 `1.0.15` 条目。
