# 2026-07-08

## 账号页 usage 资源复用

- 背景：继续处理 4000+ 账号下批量刷新和卡片数据同步开销。确认账号页卡片 usage 只是运行时补充态，旧默认会让 Wails/sidecar 重新解析账号池来补 attribution 归属。
- 决策：账号页 `loadAccountUsage()` 默认改为 `resolveAccountKeys=false`，同时请求 `includeUnresolved=true`，前端复用当前 `AccountRecord[]`，先为 sidecar attribution 建一次性索引，再按 `id / quotaKey / authIndex` 本地 join。完整 Usage Desk / 诊断页仍可显式 opt-in 后端账号解析。
- 实现：`frontend/src/features/accounts/hooks/useAccountsUsageState.ts`、`frontend/src/features/accounts/model/accountUsage.ts`。
- 验证：`node --test src/features/accounts/tests/accountUsage.test.mjs`、`node --test src/features/accounts/tests/accountRuntimeSync.test.mjs`、`npm --prefix frontend run typecheck` 通过。
- 沉淀：更新 `.agents/skills/gettokens-performance-governance/SKILL.md`，记录账号卡片 usage refresh 默认复用当前账号快照，避免为卡片展示重复扫描账号池。

## Usage Attribution 安装包观测打点

- 背景：用户准备安装新包到本机，需要在正式运行日志中确认账号页 usage 资源复用是否生效。
- 实现：`internal/wailsapp/usage_attribution.go` 增加 Wails bridge 打点，输出 `usage attribution bridge complete`，包含 `resolve_account_keys`、`include_unresolved`、items/unresolved 数量、`sidecar_ms`、`resolve_ms`、`total_ms`；只有后端解析开启时输出 `usage attribution account resolution complete` 和索引构建耗时。
- 安全边界：日志只输出计数、布尔值和耗时，不输出账号 key 列表、凭证、请求/响应正文。
- 验收：账号页卡片 usage 刷新应看到 `resolve_account_keys=false`；高频 `resolve_account_keys=true` 或 `usage attribution account resolution complete` 表示账号页又误触发后端账号池解析。
- 验证：`go test ./internal/wailsapp -run 'TestGetSidecarUsageAttribution|TestCodexAttributionIdentityStore'` 通过。

## 账号卡片 usage-limit 语义

- 背景：用户在新安装包账号池中看到 `usage_limit_reached` raw JSON 被当成“异常”展示，并出现“重新登录”入口。
- 决策：`usage_limit_reached` 是上游用量已达上限/等待重置的运行态 quota/cooldown 信号，不是安装错误，也不是 OAuth 重新登录可修复的 auth failure。
- 实现：`frontend/src/features/accounts/model/accountPresentation.ts` 识别该 payload，卡片 operational state 显示“用量已达上限”，failure reason 摘要为用户可读的 quota/reset 文案，并让 `isCodexReauthEligible()` 对该状态返回 `false`；`AccountCard` 传入 `t` 以使用中英文 locales。
- 沉淀：更新 `.agents/skills/gettokens-domain-engineering/SKILL.md`，记录 usage-limit 与 reauth/auth failure 的边界。
- 验证：`node --test src/features/accounts/tests/accountPresentation.test.mjs`、`npm --prefix frontend run typecheck` 通过。

## 账号组刷新反馈

- 背景：用户反馈账号池组头“刷新”按钮点击后看起来没有反应。
- 根因：组头按钮确实触发 `refreshGroupQuota()`，但父层用 `void` 并行发起 quota / usage / rate-limit 刷新，组头自身没有 `loading` / `aria-busy` / disabled 反馈；同时按钮可用性只看 quota 目标，runtime-only 组即使可同步 usage/rate-limit 也可能被禁用。
- 实现：`AccountGroupSection` 从 quota / usage / rate-limit refreshing 状态派生组级 `isGroupRefreshing`，传给 `AccountGroupSectionView`；组头按钮刷新期间显示 loading、禁用重复点击并暴露 `data-account-group-refreshing`。`resolveAccountGroupActionAvailability()` 改为只要组内存在可运行态刷新的账号 id 即允许组刷新，批量 quota 目标筛选仍保持 quota-only。
- 验证：`node --test src/features/accounts/tests/accountSelection.test.mjs src/features/accounts/tests/accountListLayout.test.mjs src/features/accounts/tests/accountRuntimeSync.test.mjs src/features/accounts/tests/rateLimit.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过。
- Dev 实测：通过 `./scripts/wails-cli.sh dev` 启动 `build/bin/GetTokens.app`，dev sidecar 使用 `/Users/linhey/.config/gettokens-dev/config.yaml`、端口 `18317`；账号页加载 883 个 dev 账号。Playwright 点击 `Free` 与 `API Key / 兼容服务` 组头刷新后，按钮立即进入 `aria-busy=true` / `data-account-group-refreshing=true` / disabled，完成后恢复可点；dev sidecar 日志出现 `POST /v0/management/gettokens/quota-refresh-batch/jobs` 202 以及 rate-limit、quota-status、usage-attribution 刷新。正式版 `/Applications/GetTokens.app` 未触碰。

## 账号池 WebContent 内存回升

- 证据：用户反馈 Activity Monitor 中 `wails://wails` 达到约 1GB；只读排查确认 pid `38725` 是 `/Applications/GetTokens.app` 的 WebKit WebContent。`vmmap -summary 38725` 显示 Physical footprint `1.0G`、peak `1.8G`，主要在 `WebKit Malloc`，不是 sidecar Go heap。生产 sidecar pid `38727` RSS 约 188MB。
- 根因链路：账号页打开 1179 个账号时，生产日志显示每 30 秒自动请求 `rate-limit-strategies`、`rate-limit-status`、6 批 `quota-status?account_keys=[redacted:200]` + 1 批 `[redacted:179]`、以及 `usage-attribution?bucket=1h&include_unresolved=true&window=24h`；页面加载后的 supplemental sync 与 runtime sync effect 还会重复立即跑一次。LocalStorage key 本体约数 MB，但 `com.wails.GetTokens` LocalStorage WAL 出现 144MB，说明 quota/list cache 整包写入被轮询放大。
- 决策：大账号池自动运行态同步降频，`>500` 账号 120s 一轮，`>1000` 账号 300s 一轮；`>500` 账号跳过加载后的重复即时 runtime sync，保留手动“同步运行状态”、组头刷新和卡片刷新。账号列表 cache 与 quota cache 改为内容未变不写，避免仅因 `updatedAt` 变化扩大 WebKit LocalStorage WAL。
- 沉淀：更新 `.agents/skills/gettokens-performance-governance/SKILL.md`，记录大账号池自动 runtime sync 自适应频率、避免加载后重复 whole-pool sync、以及 timestamp-only cache 写入会放大 WebKit WAL 的性能边界。
- 验证：`node --test frontend/src/features/accounts/tests/accountRuntimeSync.test.mjs frontend/src/features/accounts/tests/accountQuotaCache.test.mjs frontend/src/features/accounts/tests/accountListCache.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过。

## 账号池 WebContent 性能治理 Space

- 背景：用户要求“开space治理这些问题”，将 2026-07-08 生产 WebContent 1GB、整池 runtime sync、LocalStorage WAL 放大、usage/rate-limit 全量链路后续优化收敛到独立需求空间。
- 落位：新增 `docs-linhay/spaces/20260708-account-pool-webcontent-performance/README.md` 与 `plans/performance-governance-plan-v01.md`。
- 范围：Wails WebContent/React、WebKit storage、Wails bridge payload、sidecar runtime snapshot、usage attribution summary、rate-limit batch/filter、账号 summary/detail 分层。
- 决策：该 space 是 7 月 WebContent 内存治理入口；6 月 `20260608-account-pool-scale-optimization` 和 `20260608-account-runtime-bulk-sync` 作为前置历史引用，不再混写新一轮生产证据。

## Wise Council：账号池 WebContent 优化顺序

- 背景：用户点名 `$wise-council-dialogue`，要求和智者讨论优化方案。
- 顾问：Antigravity CLI `agy` 与 GitHub Copilot CLI `copilot --model auto` 均给出实际外部回复；Copilot 第一次默认模型调用失败，第二次用 `--model auto` 成功。
- 一致裁决：第一刀不做 LocalStorage/WAL 清理，而做候选 B：自动 runtime sync 从整池改为可见/展开/脏账号/增量候选集合。排序为 `B > E > D > A > C`。
- 采纳：手动 header refresh 保留整池语义，组头/card refresh 保留用户意图；离屏账号状态中心化保存；自动 sync 打点输出 `visible / expanded / dirty / manual` 原因；第一阶段预算为 1179 账号静置 30 分钟 WebContent Physical Footprint P95 `<500MB`、自动 sync payload P95 `<150KB`、CPU spike P95 `<20%`，更严格的 `<350MB / <100KB / <15%` 作为第二阶段目标。
- 推迟：A 作为第二刀或并行低风险项；C/D/E 暂不进入第一刀，避免 sidecar 新契约、usage summary、rate-limit API 和 summary/detail 重构扩大首轮范围。

## 账号池 WebContent Phase 1 优化执行

- 实现：账号页自动 runtime sync 从整池 `runtimeSyncAccounts` 改为使用展开组虚拟窗口上报的 visible account targets；大账号池无 targets 时跳过自动 sync，小账号池保留整池兼容；手动“同步运行状态”、组头刷新、卡片刷新仍保留原语义。
- 额外止血：`DebugProvider.trackRequest()` 原来会把 Wails 请求/响应完整保存在 React state 且无限增长，现改为保存有界摘要：数组只记长度、敏感字段脱敏、长字符串截断、最多保留最近 80 条，避免调试日志复制 4000+ 账号 payload。
- 文档：更新 `docs-linhay/spaces/20260708-account-pool-webcontent-performance/README.md` 与 `plans/performance-governance-plan-v01.md`，记录 Phase 1 执行、验证和剩余风险。
- 验证：`node --test frontend/src/features/accounts/tests/accountRuntimeSync.test.mjs frontend/src/features/accounts/tests/accountListLayout.test.mjs frontend/src/context/debugPayload.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`docs-linhay/scripts/check-docs.sh`、本轮触达文件 scoped `git diff --check`、`./scripts/wails-cli.sh build` 通过；全局 `git diff --check` 仍失败在既有 `frontend/wailsjs/go/models.ts` trailing whitespace。

## OAuth usage token_invalidated 重登与路由剔除

- 用户要求：ChatGPT `/backend-api/wham/usage` 对 OAuth 账号返回 `401 token_invalidated` 时显示“重新登录”，并把该账号移出路由候选直到恢复正常。
- 实现：CLIProxyAPI sidecar `QuotaRuntimeStore` 将 quota runtime denied reason `token_invalidated / invalid_grant` 映射为 account-scoped `auth-error` route guard，fresh success 后清理；账号卡 `isCodexReauthEligible(account, quotaDisplay)` 对 ACTIVE auth-file 账号识别 token invalidated，显示可见“重新登录”按钮；OAuth 启动流程用 quota runtime 证据回填原账号文件名。
- 边界：`402 deactivated_workspace` 继续作为异常展示，不自动显示重登 CTA；`usage_limit_reached` 仍是 quota/cooldown，不作为重登。
- 验证：CLIProxyAPI `go test ./internal/gettokenshooks -run 'TestQuotaRuntimeStoreTokenInvalidatedFeedsAuthErrorGuardUntilRecovery|TestQuotaRuntimeStoreRecoveryClearsAuthScopedQuotaEmptyByAccountKey' -count=1`，主仓 Wails 401 focused test、账号展示 node tests、`npm --prefix frontend run typecheck`、`docs-linhay/scripts/check-docs.sh` 通过。主仓 `git diff --check` 因既有脏生成物 `frontend/wailsjs/go/models.ts` trailing whitespace 失败，本轮未修改该生成物。
- 沉淀：已更新 `docs-linhay/spaces/20260609-account-card-401-status/README.md` 与 `.agents/skills/gettokens-domain-engineering/SKILL.md`；这是账号/OAuth/route guard 领域规则，不升级 `AGENTS.md`。
- 补充修正：用户指出仍显示“等待检测”。根因是账号卡状态只消费 `stale + degradedReason`，没有消费 sidecar 已经返回的 `blocked=true + sources[].source=auth-error`；已让 auth-file operational state 优先识别 `auth-error` route guard 为“异常”，并让重登资格读取 `blockReason/sources`。补充验证：`node --test frontend/src/features/accounts/tests/accountPresentation.test.mjs`、`npm --prefix frontend run typecheck` 通过。
- 补充扩展：用户提供 `https://auth.openai.com/oauth/token` 返回 `400 invalid_refresh_token / Could not validate your refresh token. Please try signing in again.`，并要求同样处理。按 `wise-council-dialogue` 咨询 `agy` 后，将其归入“终态 OAuth 凭证失效 / 需要重登”统一治理类：纳入 `invalid_refresh_token / token_invalidated / invalid_grant / refresh_token_reused / app_session_terminated / please sign/log in again`，排除 workspace、quota、rate-limit、网络、5xx 和泛化 `refresh_failed`。
- 实现扩展：CLIProxyAPI `internal/auth/codex/openai_auth.go` 对 `invalid_refresh_token` 不再重试 3 次；`sdk/cliproxy/auth/conductor.go` 将其视为 unauthorized terminal refresh failure，写入 `Unavailable=true` 并停止 auto-refresh 调度；`QuotaRuntimeStore` 将同类 evidence 写入 account-scoped `auth-error` route guard；账号卡重登资格识别 `invalid_refresh_token` 与明确重登文案。
- 扩展验证：新增红灯确认后修复，`go test ./internal/auth/codex ./internal/gettokenshooks ./sdk/cliproxy/auth -run 'TestRefreshTokensWithRetry_InvalidRefreshTokenOnlyAttemptsOnce|TestQuotaRuntimeStoreInvalidRefreshTokenFeedsAuthErrorGuardUntilRecovery|TestManager_RefreshAuthInvalidRefreshTokenStopsAutoRefreshRetry' -count=1`、`go test ./internal/auth/codex ./internal/gettokenshooks ./sdk/cliproxy/auth -count=1`、`node --test frontend/src/features/accounts/tests/accountPresentation.test.mjs`、`npm --prefix frontend run typecheck` 通过。
- 再次补充：用户标注同一张卡同时显示“可用”和【重新登录】。根因是 `resolveAccountOperationalState()` 的 `runtimeStatus=registered_routeable` 分支早于 quota/runtime `auth-error`，导致仍注册过的 OAuth 账号即使已有 `401 token_expired` 也被顶部状态判成可用。已将 auth-file quota refresh failure / `auth-error` 判断提前，并把 `token_expired / authentication token is expired` 纳入重登证据；DOM 验收目标卡显示“异常 ... unauthorized ... 重新登录”，不再包含“可用”。
- 视觉补充：用户继续标注重启后异常卡片仍是绿色。根因是 `AccountCard.tsx` 的 tint 派生在 `operationalState.tone=danger` 时回退到 `resolveAccountStatusTone(account)`，仍注册过的账号会得到 positive tint。已改为 `danger -> critical` 直接映射，并补 `accountCardLayout.test.mjs` 回归测试；DOM 验收 `jchrb770@...` 与 `AbebSuell275@outlook.com` 均为 `account-card-status-tint-critical`。
- 筛选补充：用户标注【可请求】筛选仍显示异常卡。按 `wise-council-dialogue` 咨询 `agy` 后采纳第一刀语义修复：不改 UI 文案/布局，直接让状态筛选消费同一套 operational auth-error 证据。根因是 `isAccountRequestable()` 只看静态 `AccountRecord`，没有看 quota runtime `blocked/auth-error`；已抽出 `hasAccountOperationalFailure()` 并让 `filterAccounts()`、状态分组、状态排序消费 `CodexQuotaState`。DOM 验收【可请求】不再出现 `AbebSuell275@outlook.com` 异常卡，【异常】筛选会出现重登卡。

## 账号页 App/Web 卡片差异诊断

- 背景：用户在 `./scripts/wails-cli.sh dev` 下打开 `http://127.0.0.1:34115/#frame=accounts`，反馈 app 内卡片与 web 卡片显示不一致，并要求咨询 `$wise-council-dialogue` 后执行。
- 证据：本机同时运行 dev 与正式版 GetTokens；`127.0.0.1:34115` 属于 dev app，存在 Wails 绑定，读取 dev sidecar `18317` 与 `~/.config/gettokens-dev/accounts-v1.sqlite`，账号数 883。账号页大池自动 runtime sync 已按 visible targets 收窄，因此不同窗口的筛选、展开组、滚动位置、localStorage cache 会造成阶段性显示差异。
- Wise Council 裁决：不要统一 WebView/Chrome localStorage，也不要先改卡片业务语义；先按 backend truth、window view-state、cache 三层做 DEV 诊断，确认同一账号 id 的 React runtime 与 quota-cache 是否一致。
- 实现：新增 DEV-only `Accounts Dev Diagnostics` 面板，显示 origin、Wails bindings、sidecar、账号数、runtime/cache 数量、visible runtime sync ids，并支持输入账号 id 对比 React runtime 与 localStorage quota-cache。
- 验收：Playwright headless Chrome 访问当前 dev URL，确认诊断面板存在、Wails 绑定存在、账号数 883、quota cache 882、visible runtime sync ids 可见；`node --test frontend/src/features/accounts/tests/accountDiagnostics.test.mjs frontend/src/features/accounts/tests/accountRuntimeSync.test.mjs` 与 `npm --prefix frontend run typecheck` 通过。
- 沉淀：新增 `docs-linhay/spaces/20260708-accounts-dev-diagnostics/README.md`，并更新 `.agents/skills/gettokens-domain-engineering/SKILL.md`，将“App/Web 卡片差异先分层诊断，不默认统一缓存或改业务语义”固化为账号领域规则。

## 账号池筛选状态语义重构 Phase 1

- 背景：用户标注【可请求】筛选仍显示异常账号，并要求重新规划全部筛选项；`wise-council-dialogue` 裁决第一期只治理主视图语义，不扩后端契约或完整 issue taxonomy。
- 决策：快捷筛选收敛为 `全部 / 可请求 / 需处理 / 已禁用`。旧字段兼容保留：`status.requestable -> requestable`、`status.error -> needs_attention`、`status.disabled -> disabled`；无运行证据账号作为 `pending/not_observed`，只在全部视图里出现。
- 实现：`accountSelectors` 新增 `resolveAccountOperationalStatus()`，让筛选、分组、排序消费同一套 quota/runtime `auth-error` 证据；`attention` 预设不再混入 disabled 或自动 HTTP code；新增 `disabled` 预设；active tag 显示 `状态: 可请求/需处理/已禁用`。
- 验收：`node --test frontend/src/features/accounts/tests/accountFilters.test.mjs frontend/src/features/accounts/tests/accountSelectors.test.mjs frontend/src/features/accounts/tests/accountPresentation.test.mjs frontend/src/features/accounts/tests/accountCardLayout.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过。Playwright headless Chrome 验证当前 dev 页面：`可请求` 仅可用卡，`需处理` 4 张异常重登卡，`已禁用` 3 张禁用卡，筛选弹层主入口只剩四项。
- 沉淀：新增 `docs-linhay/spaces/20260708-account-filter-taxonomy/README.md`，并更新 `.agents/skills/gettokens-domain-engineering/SKILL.md`；这是账号领域筛选规则，不升级 `AGENTS.md`。

## 账号 auth-error 重启水合修复

- 背景：用户反馈异常账号在重启 dev app 后被重置为“可用”。证据显示 `~/.config/gettokens-dev/channel-routing/config.json.runtimeStates` 仍有 Abeb/Accola 的 `auth-error token_invalidated`，但重启后的 `/v0/management/gettokens/quota-status?account_keys=...` 返回 `blocked=false sources=[]`。
- 根因：真实请求路由会懒加载 persisted runtimeStates，但账号页 management quota-status 只消费内存 `AccountRouteGuardStore`；sidecar 启动后没有把 persisted runtimeStates 水合回 guard，导致请求路由和卡片展示分裂。
- 实现：CLIProxyAPI sidecar 新增 `hydrateAccountRouteGuardStoreFromPersistedRuntimeStates()`，并在 `SetChannelRoutingPolicyConfigPathFromConfig()` 设置 profile-local channel-routing 路径后水合有效 route-blocking sources。水合不重写配置文件，恢复清理仍走既有 fresh success / `ClearAuth()`。
- 验证：`go test ./internal/gettokenshooks -run 'TestSetChannelRoutingPolicyConfigPathHydratesPersistedRuntimeStatesForQuotaStatus|TestAccountRouteGuardPolicyDeniesCandidatesFromPersistedRuntimeStates|TestAccountRouteGuardStorePersistsRuntimeStateToChannelRoutingConfig' -count=1`、`go test ./internal/gettokenshooks -count=1`、`./scripts/ensure-sidecar.sh darwin arm64` 通过并重建 `build/bin/cli-proxy-api`。当前已运行的 dev sidecar 进程仍需重启 dev app 后加载新二进制。
- 沉淀：更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 与 `docs-linhay/spaces/20260609-account-card-401-status/README.md`，记录 persisted runtimeStates 必须同时驱动 real request hard-filter 与 management quota-status；这是账号/sidecar 领域规则，不升级 `AGENTS.md`。

## 账号 auth-error auto-refresh 持久化修复

- 背景：用户重启 dev app 后反馈 K12 组只有 2 个账号异常，预期整组异常。证据：K12 组 875 个账号，`runtimeStates` 只命中 2 个 K12 `auth-error`，`quota-status` 对 875 个 K12 keys 也只返回 2 个 `blocked=true`；sidecar 启动日志同时出现批量 `invalid_refresh_token / refresh_token_reused / refresh_token_invalidated` auto-refresh 失败。
- 根因：`Manager.refreshAuth()` 在 refresh 失败时只更新 auth manager 内存状态，没有触发 `OnAuthUpdated` hook；GetTokens `AccountRouteGuardResultHook.OnAuthUpdated()` 也没有把 `auth.LastError=unauthorized` 的终态 OAuth refresh failure 转成 account-scoped `auth-error`。所以启动期发现的 K12 凭证失效没有进入 route guard / quota-status，只剩历史已持久化的 2 个可见异常。
- 实现：CLIProxyAPI `sdk/cliproxy/auth.Manager.refreshAuth()` 在失败状态落盘到内存后通知 hook；`internal/gettokenshooks.AccountRouteGuardResultHook.OnAuthUpdated()` 将 401/unauthorized auth state 写入 `auth-error` route guard。真实请求失败、quota runtime denied、auto-refresh 终态失败现在进入同一条 persisted runtime state 链路。
- 验证：新增红灯测试后修复，`go test ./sdk/cliproxy/auth -run 'TestManager_RefreshAuthInvalidRefreshTokenNotifiesAuthUpdatedHook|TestManager_RefreshAuthInvalidRefreshTokenStopsAutoRefreshRetry' -count=1`、`go test ./internal/gettokenshooks -run 'TestAccountRouteGuardResultHookBlocksTerminalOAuthRefreshFailureOnAuthUpdated|TestSetChannelRoutingPolicyConfigPathHydratesPersistedRuntimeStatesForQuotaStatus' -count=1`、`go test ./sdk/cliproxy/auth -count=1`、`go test ./internal/gettokenshooks -count=1`、`./scripts/ensure-sidecar.sh darwin arm64` 通过。当前运行中的 dev sidecar 仍需重启后加载新逻辑并重新执行 auto-refresh 才能把整组失败写入 `runtimeStates`。
- 沉淀：更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 与 `docs-linhay/spaces/20260609-account-card-401-status/README.md`；这是账号/OAuth/sidecar route guard 领域规则，不升级 `AGENTS.md`。

## 账号 auth-error provider identity 传播修复

- 背景：继续验收发现 K12 组重启后仍只有 2 个账号异常；进一步查明 K12 875 个账号中有 675 个共享同一个 OpenAI `auth_json.account_id=7bf3a2ce-3298-40b0-ac9b-3c922a5a91a6`。截图中的绿色 sibling 卡和红色 Abeb/Accola 都属于这个 provider account identity。
- 根因：route guard 只按单个 `acct_*` / auth id / auth-file lookup，历史 `token_invalidated` 只持久化在两张卡的 `acct_*` 上。sibling access token 尚未触发 auto-refresh，因此不会自己写入失败，`quota-status` 也无法通过 provider identity 命中已有终态失败。
- 实现：CLIProxyAPI sidecar 新增 account-store identity resolver，从 auth-file `auth_json.account_id / chatgpt_account_id` 派生 `openai-account-id:<id>` / `provider-account-id:openai:<id>` lookup key；写入、水合、候选过滤和 management `quota-status` 查询都按该 identity 扩展；`quota-status` sources 去重，避免同一 reason 重复展示。
- 验证：`go test ./internal/gettokenshooks -count=1`、`go test ./sdk/cliproxy/auth -count=1`、`./scripts/ensure-sidecar.sh darwin arm64` 通过；重启 `./scripts/wails-cli.sh dev` 后 dev sidecar `BuiltAt: 2026-07-08T07:54:52Z`，真实 API 验收 K12 `total=875`、`blocked=675`、`authError=675`、共享 OpenAI account identity `675/675` blocked，`maxSourcesPerBlocked=1`。
- 沉淀：更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 与 `docs-linhay/spaces/20260609-account-card-401-status/README.md`；规则属于账号/OAuth/sidecar route guard 领域，不升级 `AGENTS.md`。
