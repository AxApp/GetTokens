# 2026-06-08

- 会话整理：本轮收敛了 `20260606-menu-bar-popover` 的 SwiftUI menu bar popover、账号池导航、额度/余额快照和空态文案边界。最终规则是：menu bar popover 展示 sidecar quota runtime snapshot，不展示原始账号清单；启动、sidecar ready 和打开 popover 只读 `/gettokens/quota-status`，只有用户点击 `刷新额度` 才对启用且配置 quota/billing curl 的账号调用 sidecar `quota-refresh`，然后重读 snapshot。
- 用户偏好已明确：账号列表无真实额度快照时，不要显示 `quota snapshot` 这类假账号名；空账号行应显示 `等待账号额度快照`，副文案为 `点击刷新额度后更新`。SwiftUI 主渲染与 AppKit fallback 已同步，并用源码断言防止回退。
- 排查结论：`公司 1` 账号的 menu bar 主动刷新已触发 sidecar，但其 quota curl 指向 `http://cpa.host.dxy/v0/management/public/custom/monitor/quota?...`；当前本机 DNS 直接解析 `cpa.host.dxy` 失败，走系统代理 `127.0.0.1:7897` 返回 HTTP 502。sidecar 报 `codex api key quota request failed with status 502` 属于自定义额度接口/代理链路不可达，不是 popover 数据接入或渲染失败。
- 本次可复用模式已沉淀到 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 `Menu Bar Quota Snapshot Boundary`：不要把 `/accounts` 有账号误判为额度快照可用；诊断 menu bar 额度空态时先查 `/gettokens/quota-status`、`quota-refresh`、curl endpoint、DNS/VPN/proxy 和 `use-system-proxy`。
- 提交流程边界：当前父仓包含多组会话工作，且 `docs-linhay/references/CLIProxyAPI` 子仓有 project-candidate-pool 相关 sidecar runtime 改动。整理提交应先提交子仓，再提交父仓 gitlink、Wails/frontend/docs/skills 变更；不触碰 `/Applications/GetTokens.app` 正式版。
- 账号详情“修改基础 URL 后点保存没反应”已在 `http://localhost:34115/#frame=accounts&detail=acct_116260d8-1996-4079-9e02-1c0d28b11a33` 复现：点击保存实际触发了 Codex API Key 配置保存，但启用的 quota curl 会先预检，当前自定义额度接口经代理返回 502，`onSaveConfig` 抛错后 `UnifiedAccountDetailModal.saveConfig` 没有在弹窗内展示错误，导致用户看到静默不动。修复为保存失败时在详情弹窗顶部显示 `保存失败` 和原始错误摘要，配置草稿再次变化时清除旧错误；回归覆盖在 `accountDetailLayout.test.mjs`。
- 账号详情保存语义调整：修改 API key / 基础 URL / prefix / quotaCurl / billingCurl / 模型映射是数据库保存行为，不应被 quota/billing curl 的网络可用性阻塞；quota 502 只应出现在显式 `测试` 或 `刷新额度` 操作中。`updateSelectedApiKeyConfig` 已移除保存前 `TestCodexAPIKeyQuotaCurl/TestCodexAPIKeyBillingCurl` 预检，账号领域 skill 同步更新该边界。
- 为避免旧计划把 bug 重新设计回来，已同步更新 `.agents/skills/gettokens-domain-engineering/SKILL.md`、`docs-linhay/dev/20260519-quota-curl-template-boundary.md`、`20260505-codex-api-key-custom-quota-curl` 与 `20260606-relay-vendor-support` space 文档：保存动作不触网，quota/billing 网络请求只属于显式测试或刷新额度。
