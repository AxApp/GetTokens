# 2026-07-03

## CLIProxyAPI 上游 v7.2.49 reference-port

- 用户要求“同步 cpa 上游功能”。已按 `gettokens-cliproxyapi-reference-port` 做 intake：canonical upstream `router-for-me/CLIProxyAPI.git` 最新 tag 为 `v7.2.49@cde9336b`，本轮窗口为 `v7.2.16..v7.2.49`，约 110 commits / 303 files / `+22287/-9316`，禁止整包 merge。
- 本轮只接受一个窄 slice：Codex Responses 下游 WebSocket 经 sidecar 转发到 CPA-mediated upstream 时，后续 turn 必须合并完整 transcript，不再向上游发送 `previous_response_id`。
- CLIProxyAPI fork commit：`7c6c1077 fix(gettokens): replay transcript for codex ws sse`。红灯为新增 `TestResponsesWebsocketMergesTranscriptForNonPassthroughUpstream` 捕获 `previous_response_id` 泄漏；绿灯后 `go test ./sdk/api/handlers/openai -count=1` 与 `go test ./... -count=1` 均通过。
- 已运行 `./scripts/ensure-sidecar.sh darwin arm64` 重建本地 sidecar，meta fingerprint 为 `7c6c107792d4faa14ed1aa53a3117110b05c7768:clean:8217992a8fe06b4265aa6d7f9248a8d1978b35531abb24fa6faea4a39262a3d1:darwin:arm64`。
- 上游 pluginhost/pluginstore/home plugin、build/release/Docker/README/partners、删除 Gemini CLI translator/executor 等大块变化本轮只分类，不移植。正式版 `/Applications/GetTokens.app`、正式 sidecar、正式配置和正式进程未触碰。

## 账号详情重构整理提交

- 用户要求“整理，提交”。已对工作区中既有账号详情/Modal/Doctor Workbench 前端改动做混合 worktree ship hygiene：补回 `CodexAccountDetailModal` 薄 adapter，保留旧调用方和 Storybook 的导入契约；把 Codex 详情实际渲染收敛到 `UnifiedAccountDetailModal`。
- 修复提交前门禁问题：Codex adapter 显式透传 `rateLimitRulesAPI`，`UnifiedAccountDetailModal` 修正 `undefined` 类型点，Codex detail 内部样式降级到设计系统允许的 `font-semibold` / token 字号，恢复 account detail 静态契约要求的 capability endpoint card 与 compact action stack。
- 验收：`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 均通过。
- 沉淀审计：本轮没有新 workflow。失败模式仍落在既有 `gettokens-ops-governance` 的 mixed worktree ship hygiene 和设计系统门禁范围内，不新增 skill、不更新 `AGENTS.md`。
