# 2026-05-26

## 状态页 local apply 栅格归一化
- 决策：`#frame=status` 的本地配置表单统一使用 `grid gap-3 md:grid-cols-2`，两字段同行时一律平分宽度；Codex / Claude 两侧都沿用这套规则，避免 provider / reasoning / model / wire API 之类字段出现 12rem 窄列。
- 决策：Relay key picker 继续保留复制动作，并把 `selectedRelayKey` / `selectedClaudeRelayKey` 分别透传到 `StatusRelayKeyPicker`，让两个 local apply 面板都能直接复制当前 key。
- 验证：`node --test src/features/status/tests/relayLocalState.test.mjs src/features/design-system/storyCatalog.test.mjs`、`npm run typecheck` 通过；浏览器预览桌面视图下三组两列字段宽度均为 376px / 376px，横向溢出为 0。
- 边界：这次只收敛状态页 local apply 的密度和复制入口，不上升为 repo-wide 的表单布局规则。

## Codex Channel Routing 兼容遮罩收口
- 决策：Codex 账号列表里的 `session-affinity`、`websocket-pin`、`route-order-header` 不再回写新 `ChannelRoutingConfig`；前端只展示 `Legacy compatibility mask` 的总数与说明，不展开三条明细，并在 explain 中记录 `legacy:compatibility-mask=blocked`，以便后续和上游合并时保持最小改动面。
- 验证：`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit -- src/features/channel-routing/tests/channelRouting.test.mjs`、`npm --prefix frontend run test:unit -- src/features/codex/codexAccountList.test.mjs` 通过；浏览器 dev 预览在 `http://127.0.0.1:5173/#frame=codex&workspace=account-list` 只显示兼容遮罩总数与说明。
- 边界：这次只收口 Codex Channel Routing 的前端语意和 preview/explain 文案，不改上游核心路由实现，也不扩大到 AGENTS 级别规则。

## Codex / Claude 路由模式收成两项
- 决策：当前前端 `Channel Routing` 只保留 `sequential / balanced` 两种可见路由模式；旧 `project` 输入会被归一化为 `sequential`，项目名绑定继续作为兼容数据保留，但不再作为主模式入口。
- 验证：`npm --prefix frontend run typecheck` 与 `npm --prefix frontend run test:unit -- src/features/channel-routing/tests/channelRouting.test.mjs` 通过。
- 边界：这次仍然只收前端可见模式，不改 Go 后端 `project` 兼容实现，方便后续和上游合并时保持最小 diff。

## Codex 账号列表外层壳去卡中卡
- 决策：`#frame=codex&workspace=account-list` 的请求顺序 section 外壳从重卡阴影改成 `bg-surface` 承载，标题与消息条继续用 `bg-main`，内层账号行保留原有卡片体系，整体从“卡中卡”转成更轻的工作区段落。
- 验证：`npm --prefix frontend run test:unit -- src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过；浏览器在 `http://127.0.0.1:5173/#frame=codex&workspace=account-list` 桌面视图确认 outer section 去掉大阴影，账号卡层级仍保持清晰。
- 边界：这次只收 Codex 账号列表请求顺序区的视觉层级，不改账号排序、路由探测或 Wails 绑定语义。

## Codex live sessions 时间线首 token 补齐
- 决策：`#frame=codex&workspace=live-sessions` 的请求时间线行补出 `firstTokenLabel`，和 `总 / TTFT` 并排显示；次级的 `stream` 指标继续保留但会在更窄断点隐藏，避免紧凑行过满。
- 补充：线上版也需要稳定显示 `总 / TTFT / 首` 三个指标，因此前三个指标不再受 `sm` 断点隐藏影响；只有第 4 个 `stream` 仍作为宽屏次级指标。
- 修复：请求耗时趋势图中，只有当前 active request 才允许用 `now - startedAt` 投影总耗时；历史请求即便因为缓存/sidecar 状态仍标为 `streaming`，也不再一起增长。
- 结论：这次属于 Codex live sessions 的 UI 排障优先级调整，不上升为 repo-wide 规则，也不新增独立 skill。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过；浏览器按 1192x964 视口复验 5 条请求行均可见 `总 / TTFT / 首`，并观察 2.2 秒确认历史总耗时点不再一起增长。

## 账号池单点刷新与 OAuth 回填
- 决策：账号池里的删除、禁用、重命名、优先级调整、API Key 配置保存和 OAuth 回填，都优先做局部 patch；只有新增、导入或显式强制刷新才重拉整组 quota / usage / rate-limit，避免单点操作把整页附加数据洗一遍。
- 决策：OAuth 回填在新旧文件名不稳定时优先按邮箱匹配当前卡片，只要邮箱一致就更新原卡片，不把重新登录误判成新增账号。
- 决策：账号卡片的可用状态不能被 usage/quota 的“成功”覆盖；如果详情或账号状态已经是 error，列表卡片也应继续显示 error。
- 验证：前端单测补了 `resolveAccountOperationalState` 的 error 优先级回归；Go 单测补了 `resolveReplacementCodexAuthFileName` 的邮箱回填回归；`go test ./internal/wailsapp/...`、`node --test src/features/accounts/tests/accountPresentation.test.mjs`、`npm run typecheck` 通过。

## 账号详情运行快照 / Evidence 等分布局
- 决策：账号详情顶部 `AccountDetailOverviewGrid` 的运行快照与 Evidence 区域改为桌面 `lg` 起左右 50/50 等分，窄屏继续上下堆叠。
- 实现：`frontend/src/features/accounts/components/AccountDetailPrimitives.tsx` 增加 `data-account-detail-overview-layout="split-50-50"`，列模板从 `1fr + 24rem` 改为 `minmax(0,1fr) + minmax(0,1fr)`。
- 验证：新增 `frontend/src/features/accounts/tests/accountDetailLayout.test.mjs`，并纳入 `frontend/package.json` 的 `test:unit`；浏览器预览实测桌面宽度下 runtime/evidence 都为 372.5px，宽度差 0。
- 风险：375px 预览时 app shell 仍保留左侧 sidebar，导致详情弹窗可用宽度约 51px；这是既有响应式壳层问题，和本次 50/50 布局变更无直接关系。

## 账号详情 Auth File / Proxy Route 等分布局
- 状态：已被后续“Auth File 简化与 Deep Link 恢复”取代，auth-file 详情不再展示只读代理出口模块。
- 决策：auth-file 账号详情中，文件摘要与出口/代理模块同样进入 card grid 的左右 50/50 布局，路由守卫下移到下一行。
- 实现：`buildAccountDetailModulePlan(auth-file)` 调整为 `auth-file-actions -> proxy-route -> models -> rate-limit`，并移除 `AuthFileSummarySection` 的 `span="wide"`，保留 auth-file 代理出口只读提示与模型目录。
- 验证：`accountDetailLayout.test.mjs` 覆盖 auth-file 模块顺序与文件摘要不跨列；浏览器预览实测文件摘要 / 代理出口同为 372.5px，宽度差 0，且位于同一行。

## 账号详情 Auth File 简化与 Deep Link 恢复
- 决策：auth-file 详情默认保留完整业务闭环但降低噪音，模块顺序收敛为 `文件摘要 -> 模型目录 -> 路由守卫`；原始 auth JSON 不默认展开，只提供按需脱敏与复制；只读代理出口提示移出 auth-file 详情。
- 修复：`#frame=accounts&detail=auth-file:<name>` 现在会从 hash 自动恢复并打开对应账号详情，不再停留在账号列表。
- 验证：`accountDetailLayout.test.mjs` 覆盖 auth-file 模块计划、模型目录保留、原始内容隐藏和 detail id 解析；已运行 focused test、`npm run typecheck`、`npm run build`，并在浏览器复验 `codex-pro.json` 详情可直达且不再显示代理出口占位。

## 账号详情 Runtime Quota 进度条重设计
- 问题：运行快照中的 quota 进度条原来放在窄 `Remaining` 列内，5H / 7D 只显示很小的绿色块，浏览器标注场景下几乎不可读。
- 决策：quota 行改成 `Window + 整行进度轨道`，百分比和重置时间叠在 32px 高的轨道上，填充宽度按剩余额度比例渲染，并复用 quota 状态色。
- 验证：`accountDetailLayout.test.mjs` 覆盖 `data-account-quota-progress-track/fill` 与 `min-h-7`；已运行 focused test、`npm run typecheck`、`npm run build`，浏览器复验 5H / 7D 轨道宽 273px、高 32px，填充比例可见。

## 账号详情 Runtime Quota 样式复用卡片
- 决策：运行快照里的 quota 展示不再维护独立表格样式，直接复用账号卡片的 `QuotaBars`，让列表卡片与详情页的额度表达保持一致。
- 验证：`AccountDetailSections.tsx` 只保留 `QuotaBars quotaDisplay={quotaDisplay} t={t}`，移除了自定义 progress track/fill；focused test、`typecheck`、`build` 已通过。

## 账号详情 Runtime Stats 紧凑化
- 问题：运行快照顶部 6 项统计原本使用 `AccountDetailStatGrid`，在当前详情宽度下形成 2 列 3 行的大表格，占用过多垂直空间。
- 决策：改为 `RuntimeStatStrip` 紧凑指标条，保持 6 项信息不丢失，但使用 3 列两行、单行 label/value、较小 padding，让 quota bars 成为运行快照的主视觉。
- 验证：`accountDetailLayout.test.mjs` 覆盖 `data-account-runtime-stat-strip` 与不再使用 `<AccountDetailStatGrid columns={6}>`；focused test、`npm run typecheck`、`npm run build` 已通过。

## 账号详情 API Key 凭据区块上下化
- 问题：API 密钥和基础 URL 原先左右并排，视觉上太横，且标签和输入框分离，和后续的前缀字段不统一。
- 决策：凭据区块改成上下堆叠的三段式输入，字段名嵌在输入框上沿，复制按钮嵌在右侧；API 密钥与基础 URL 保留复制动作，前缀使用同一表单风格但不加复制按钮。
- 验证：`accountDetailLayout.test.mjs` 覆盖 `data-account-credential-fields="stacked"` 与嵌入式 label；focused test、`npm run typecheck`、`npm run build` 已通过。

## 账号详情 Runtime 摘要去重
- 问题：运行快照顶部的摘要和下方的 quota / balance 区块重复展示了额度与余额，导致同一页出现两套相似信息。
- 决策：`buildAccountRuntimeStats` 只保留运行态本身的 4 项摘要：近期请求、累计 Token、Cached、平均耗时；额度和余额只在下方专门区块展示。
- 验证：`accountPresentation.test.mjs` 已更新为 4 项摘要断言；`node --test src/features/accounts/tests/accountPresentation.test.mjs src/features/accounts/tests/accountDetailLayout.test.mjs`、`npm run typecheck`、`npm run build` 均通过。

## 会话整理结论
- 本次会话沉淀出一条可复用模式：账号详情页先做重复信息去重，再做密度压缩；顶部只保留运行态信号，quota / balance / raw auth / route control 统一下沉到专门区块。
- 已更新 `gettokens-domain-engineering` skill，新增账号详情降噪和布局压缩规则。
- 已新增 `docs-linhay/dev/20260526-account-detail-density-session-distillation.md`，后续可作为同类 detail 页面改版的参考入口。

## macOS 菜单与状态栏原生入口沉淀
- 决策：用户找的是左上角 `GetTokens` App 菜单，不是 Wails 自定义 `Help` 菜单；后续 App 菜单自定义项必须按原生 macOS 语义验收。
- 实现边界：`internal/appmenu` 负责 AppKit `NSApp.mainMenu` 插入 `检查更新...`；`internal/menubar` 的右侧状态栏按钮从 `GT` 文字改为嵌入式 template PNG，Objective-C 侧先复制 Go embed 图片字节到 `NSData` 再主线程安装。
- 沉淀：更新 `gettokens-ops-governance` 的 macOS runtime residency 规则，并补充 `docs-linhay/dev/20260519-macos-runtime-residency-session-distillation.md`，不升级 `AGENTS.md`，因为这是 macOS/Wails 交付细则而非 repo-wide 通用治理。
- 验证：`go test ./internal/menubar`、`go test . -run 'TestBuildApplicationMenuKeepsCheckForUpdatesOutOfHelpMenu|TestConsumeLoginItemArgRemovesCustomArgBeforeWailsParsesFlags'`、`./scripts/wails-cli.sh build`、`docs-linhay/scripts/check-docs.sh` 通过，`qmd update` / `qmd embed` 已执行。

## Codex 运行会话请求时间线压缩
- 决策：`#frame=codex&workspace=live-sessions` 的请求时间线行改为单行扫描式展示，request id 压缩为 `REQ-xxxx`，ISO 时间压缩为 `HH:mm:ss-HH:mm:ss`，指标只保留总耗时、TTFT、首 token、流式耗时；下方“脱敏诊断”块不再渲染。
- 实现：限定在 live session detail 与 timeline summary 相关代码内调整布局与格式化，并保留点击行打开详情的交互。
- 验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run typecheck` 通过；浏览器预览实测时间线行高 42px、页面横向溢出 0、诊断块不存在。截图：`docs-linhay/screenshots/20260526/codex-live-sessions/20260526-codex-live-sessions-timeline-row-after-v01.png`。

## Codex 运行会话筛选与详情容器去卡片化
- 决策：运行会话页面的筛选条和详情根容器去掉外层卡片壳，避免“卡中卡”视觉噪音；详情内部继续保留必要的分区边界。
- 验证：桌面与 390px 两种视口都保持无横向溢出，时间线 row 仍为 42px，诊断块不再出现。截图补充：`docs-linhay/screenshots/20260526/codex-live-sessions/20260526-codex-live-sessions-layout-after-v02.png`、`docs-linhay/screenshots/20260526/codex-live-sessions/20260526-codex-live-sessions-layout-mobile-after-v01.png`。

## 本次整理结论
- 沉淀：`gettokens-domain-engineering` 的 Codex live sessions 小节新增了时间线单行化、核心指标前置和去“卡中卡”边界；`docs-linhay/dev/20260523-session-distillation-codex-live-sessions-ui.md` 也补了对应段落。
- 不纳入：这仍然是 runtime observability 的 UI 密度规则，不升级为 repo-wide 的 `AGENTS.md` 规范。

## Codex live sessions 未知项目根因
- 结论：线上最新 App 仍显示“未知项目”的直接原因是 live snapshot 里没有 `projectName`；前端和 Wails 只是透传 sidecar 字段并在缺失时显示 fallback。
- 根因：本机 8317 端口上残留了 2026-05-24 启动的 `/Applications/GetTokens.app/Contents/MacOS/cli-proxy-api` 孤儿进程。新 App 的端口检测只尝试 `127.0.0.1:<port>`，未覆盖旧进程的 wildcard IPv6 监听，导致误判 8317 可用并把健康检查/管理接口连到旧 sidecar。
- 修复：`internal/sidecar/isPortFree` 改为按 sidecar 实际 `host: ""` 绑定语义探测 `:<port>`；App 启动时清理同 profile `config.yaml` 的孤儿 `cli-proxy-api`，退出时 interrupt 后等待，超时 force kill。
- 沉淀：更新 `gettokens-ops-governance` 的 `Sidecar Process Binding` 规则，并补充 `docs-linhay/dev/20260519-macos-runtime-residency-session-distillation.md`；不升级 `AGENTS.md`，因为这是 GetTokens Wails/macOS 生命周期细则。
- 验证：`go test ./internal/sidecar ./internal/wailsapp -run 'TestPortAvailabilityMatchesWildcardSidecarBind|TestFindOrphanedSidecarPIDsMatchesSameConfigOnly|TestStopProcessKillsWhenInterruptIsIgnored|TestGetCodexLiveSessionsSnapshot' -count=1` 通过；重新拉起当前 App 后，8317 由新 App 子进程持有，live snapshot 已返回 `projectName`，样本包含 `GetTokens` 与 `design`。

## CLIProxyAPI 上游同步
- 完成：`docs-linhay/references/CLIProxyAPI#gettokens/sidecar` 已合并 `upstream/main@167edfec`，生成并推送 merge commit `b72ac277` 到 `AxApp/CLIProxyAPI#gettokens/sidecar`。
- 冲突：只在 `sdk/api/handlers/openai/openai_responses_websocket.go` 发生冲突；处理时保留上游 file-backed websocket timeline，同时保留 GetTokens live-session hook、pinned auth failover、wrapped status code 提取和 `coreauth.AuthAllowsWebsockets(auth)` 默认能力边界。
- 验证：fork 内 `go test ./sdk/api/handlers/openai`、受影响包测试、`go test ./...`、`git diff --check` 均通过；父仓库 `./scripts/ensure-sidecar.sh darwin arm64` 已重建本地 sidecar，meta 记录 `commit=b72ac277`、`dirty=clean`。
- 归档：同步记录追加到 `docs-linhay/dev/20260519-cliproxyapi-upstream-sync-session-distillation.md`；本次仍属于既有 CLIProxyAPI fork 同步流程，不升级 `AGENTS.md`。

## CLIProxyAPI 内存剪裁
- 决策：live sessions tracker 只保留最近 50 条 request，保留 30 分钟窗口但增加 `DELETE /v0/management/gettokens/live-sessions` 清理接口，避免单个长会话无限堆 request。
- 决策：live sessions 的历史 request 另外落到 `live-sessions-v1.sqlite`，通过 `GET /v0/management/gettokens/live-sessions/history` 分页追溯；清理实时 snapshot 不删除磁盘历史。
- 决策：legacy `internal/usage/logger_plugin.go` 不再把 request detail 长驻内存，改为聚合常驻、明细落盘分页读取；`usage-observed` 迁移到 `usage-observed-v2.sqlite`，首次启动若只剩 v1 会自动回写 v2。
- 新增：usage attribution 增加 `GET /v0/management/gettokens/usage-attribution/details`，支持 `window / limit / offset / account_key / attribution_key`，给后续前端或调试页按页取明细。
- 纠正：这轮调整的边界只是在内存里裁剪 live sessions / requestMap 和 usage detail 常驻量，磁盘上的历史仍要保留完整可追溯数据，不能把落盘内容也裁短。
- 沉淀：已更新 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 Codex Live Sessions 小节，新增“内存裁剪必须先定义磁盘历史归属、snapshot 清理不删除历史、历史接口从磁盘分页读取”的规则；该规则属于 CLIProxyAPI fork / Codex runtime observability 领域边界，不升级 `AGENTS.md`。
- 验证：`go test ./internal/gettokenshooks ./internal/usage ./internal/cmd`、`go test ./...`、`git diff --check` 通过；这轮剪裁是为了降低 sidecar RSS，不是前端展示优化，因此没有改前端 dev server 的职责边界。

## 账号池批量操作空间规范化
- 决策：这轮账号池批量操作使用正式 space slug `20260526-accounts-bulk-actions`，不再使用临时 `space1`；space README、实施计划、截图与后续讨论都归档到该目录。
- 决策：批量动作复用现有单账号删除、刷新与启停接口，前端先用模型层纯函数筛出可执行对象，再串行执行并统一输出 notice；危险移除保留二次确认。
- 验证：`node --test frontend/src/features/accounts/tests/accountSelection.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过；`npm --prefix frontend run test:unit` 经过全量回归时撞到仓库里既有的 `frontend/src/features/channel-routing/tests/channelRouting.test.mjs` 导出失败，和这次改动无关。
- 验证：浏览器预览在 `http://localhost:5173/#frame=accounts` 里进入批量选择后，能看到 `批量刷新 / 批量激活 / 批量禁用 / 移除`，执行一次批量禁用后会出现成功 / 跳过 notice，再点移除会出现确认移除护栏；截图已归档到 `docs-linhay/spaces/20260526-accounts-bulk-actions/screenshots/`。
- 补充：分组标题右侧已增加刷新和更多菜单；顶部批量选择开启后才显示本组选择 / 取消选择；分组更多菜单提供激活本组、禁用本组并复用批量启停路径。按反馈同步放大选择控制行字号，并把账号卡 `禁用账户` 菜单项改为红色危险态。
- 整理：新增 `docs-linhay/dev/20260526-accounts-bulk-actions-session-distillation.md`，沉淀“选择目标筛选在 model、动作编排在 hook、工具条和分组标题只发意图”的账号域批量操作模式；暂不升级 `AGENTS.md`。
- 风险：当前 `npm run test:unit` 的 channel-routing 失败不是本次 feature 引入，但会影响“全量 unit 100% 绿”的表述，后续需要单独清理。

## Codex live sessions 请求趋势整理
- 沉淀：请求耗时趋势图只允许当前 active request 使用 `nowMs - startedAt` 投影；历史请求保留记录值或完成时间差，避免 stale `streaming/reconnecting` 样本一起增长。
- 补充：请求耗时趋势图只展示固定最近 5 分钟窗口内的请求点；窗口外旧请求不参与曲线和 y 轴最大值，x 轴域固定为 `latestStartedAt - windowMs` 到 `latestStartedAt`。
- 已更新：`.agents/skills/gettokens-domain-engineering/SKILL.md` 的 Codex Live Sessions 小节、`docs-linhay/dev/20260523-session-distillation-codex-live-sessions-ui.md`。
- 不升级：该规则属于 Codex runtime observability 领域建模边界，不是 repo-wide `AGENTS.md` 约束。
