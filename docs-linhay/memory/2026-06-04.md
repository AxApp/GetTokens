
## 2026-06-04

### Bug Fix: curl_variables_json 列缺少迁移导致已存在 SQLite 数据库 500

**问题**：写入 credential 时 sidecar 返回 500 错误：
`SQL logic error: no such column: curl variables_json (1)`

**根因**：`curl_variables_json` 列虽然在 `CREATE TABLE` 和 `SELECT/INSERT` 中已定义，但 `EnsureSchema` 函数缺少对应的 `ensureTextColumn` 迁移调用。新建数据库不受影响，但已有数据库缺少该列导致查询失败。

**修复**（sidecar repo `docs-linhay/references/CLIProxyAPI`）：
- `internal/gettokens/accountstore/store.go`: 在 `EnsureSchema` 中添加 `curl_variables_json` 列的 `ensureTextColumn` 调用
- `internal/gettokens/accountstore/store_test.go`: 添加 `TestStoreEnsureSchemaAddsCodexAPIKeyCurlVariablesJSONColumn` 测试，验证 DROP COLUMN 后 EnsureSchema 能恢复列

**验证**：
- 迁移测试通过：模拟已有数据库缺少列，确认 EnsureSchema 自动添加
- Go 全量测试通过（accountstore 模块）
- Sidecar 二进制已重新构建（commit: df370a6f，在 `gettokens/sidecar` 分支）

**教训 & AGENTS.md 更新**：
- 新增规则 24：正式版数据可拷贝到 dev 环境复现问题，但必须先备份 dev 数据，验证后恢复。
- 规则 23 重申：严禁擅自修改 `/Applications/GetTokens.app` 正式版二进制、sidecar 或配置。

### Review Fix: model_fetch 列同样缺少旧库迁移

**复审发现**：`model_fetch_api_key` / `model_fetch_base_url` 与 `curl_variables_json` 属于同类 SQLite schema 演进风险。虽然新库 DDL 已包含列，已有数据库仍可能缺列并在 `SELECT openai_compatible_accounts ... model_fetch_api_key...` 时 500。

**修复**（sidecar repo `docs-linhay/references/CLIProxyAPI`）：
- `internal/gettokens/accountstore/store.go`: 在 `EnsureSchema` 中添加 `openai_compatible_accounts.model_fetch_api_key` 与 `model_fetch_base_url` 补列迁移。
- `internal/gettokens/accountstore/store_test.go`: 新增 `TestStoreEnsureSchemaAddsOpenAICompatibleModelFetchColumns`，模拟旧库 DROP COLUMN 后确认 EnsureSchema 恢复两列。

**验证**：
- `go test ./internal/gettokens/accountstore ./internal/api/handlers/management -count=1`
- `go test ./internal/accounts ./internal/cliproxyapi ./internal/wailsapp -count=1`
- `node --test frontend/src/features/accounts/tests/accountConfig.test.mjs frontend/src/features/accounts/tests/accountDetailLayout.test.mjs frontend/src/features/accounts/tests/openAICompatible.test.mjs`
- `cd frontend && npx tsc --noEmit`
- `docs-linhay/scripts/check-docs.sh`

### Review Fix: relay 模型目录刷新与状态变更错误语义

**问题 1**：OpenAI-compatible / Xiaomi MiMo Token Plan 账号保存了 `modelFetchApiKey` / `modelFetchBaseUrl` 后，账号创建后的 scheduled Codex model catalog refresh 仍用运行时 `tp-*` 凭证和 runtime base URL 拉 `/models`，导致 catalog cache 为空或陈旧。

**修复 1**：`internal/wailsapp/relay_model_catalog.go` 增加 relay 拉模凭证选择逻辑：`ModelFetchBaseURL` / `ModelFetchAPIKey` 非空时优先用于 `/models` 拉取，缺省时分别回退到 runtime `BaseURL` / `APIKey`。新增测试覆盖专用凭证路径和 runtime fallback 路径。

**问题 2**：`SetCodexAPIKeyStatus` 在 sidecar `PatchAccountStatus` 已成功后，如果本地 relay model account cache prune 因 cache 损坏等原因失败，会把 disable 操作返回为失败，并跳过 catalog refresh。

**修复 2**：`internal/wailsapp/accounts.go` 将该路径的 prune 错误改为 log-only，保持状态变更返回成功，并继续调度 Codex model catalog refresh；新增损坏 cache 场景测试验证不返回 prune error 且仍触发 refresh。

**验证**：
- `go test ./internal/wailsapp -run 'TestListRelaySupportedModelsUsesDedicatedModelFetchCredentials|TestListRelaySupportedModelsFallsBackToRuntimeCredentialsWhenModelFetchCredentialsMissing|TestSetCodexAPIKeyStatusIgnoresPruneErrorAndSchedulesRefresh'`
- `go test ./internal/wailsapp`
- `go test ./internal/...`

### Session Distillation: modelFetch 与 mutation 后 cache prune 边界

**沉淀内容**：将本次 review fix 中可复用的两个账号/模型目录边界写入 `.agents/skills/gettokens-domain-engineering/SKILL.md`：
- `modelFetchApiKey/modelFetchBaseUrl` 是管理面 `/models` 拉取凭证，catalog refresh 必须优先使用，不能用 token-plan runtime 凭证替代。
- sidecar account mutation 成功后，本地 relay model cache prune 错误只能 log-only，不能把已生效的状态变更变成 UI/API 失败；仍需继续调度 catalog refresh。

**不升级 AGENTS**：这两个规则属于账号/模型目录领域实现约束，不是 repo-wide 流程治理规则，暂不写入 `AGENTS.md`。

### Session Distillation: account-store schema evolution guard

本轮反复踩到 SQLite 新列只更新 DDL/SQL、漏掉旧库 `EnsureSchema` 迁移的风险（`curl_variables_json` 与 `model_fetch_*`）。已沉淀到 `.agents/skills/gettokens-domain-engineering/SKILL.md`：任何 account-store 新字段必须同步 DTO、CREATE TABLE、EnsureSchema 补列迁移、SELECT/INSERT/patch，并新增“模拟旧库缺列后 EnsureSchema 恢复”的回归测试。

本次不升级 AGENTS.md：该模式属于 sidecar/account-store 领域工程规则，适合先放入项目级 domain skill，而不是 repo-wide 行为规范。

### Design System: 5173 Web 快捷入口

**需求**：设计系统页面需要一个明确打开 `http://127.0.0.1:5173/#frame=design-system` 的按钮，避免从 Wails dev origin 或 Storybook 入口绕路。

**实现**：
- `frontend/src/features/design-system/storyCatalog.ts` 新增 `DESIGN_SYSTEM_VITE_PORT/URL` 与 `resolveDesignSystemViteOpenURL()`，固定指向 5173 应用内设计系统 frame。
- `DesignSystemEntryFeature.tsx` 在 dev actions 中新增高亮按钮 `打开 5173 Web` / `Open 5173 Web`，沿用 Wails `BrowserOpenURL` 与浏览器 `window.open` 外链逻辑。
- 同步补齐中英文 locale 与 focused URL 回归测试。

**验证**：
- `node --test frontend/src/features/design-system/storyCatalog.test.mjs`
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run dev -- --host 127.0.0.1` 后用 `curl` 检查 Vite 转译模块中存在 `open_5173_web` 按钮和固定 5173 URL resolver；验证后已停止 dev server。

### Fix: OpenAI-compatible provider 承载 DeepSeek quota/billing

**纠正**：DeepSeek / 第三方厂商通过 Unified Compose 创建时必须保持 `openai-compatible` provider 语义，不能改建成 `codex-api-key`。此前“带 billing preset 创建为 codex-api-key”的方案已通过反向提交撤回。

**决策**：最佳方案是在 `openai-compatible` credential 上承载 quota/billing 配置：`quotaCurl` / `quotaEnabled` / `billingCurl` / `billingEnabled` / `platformCookie` / `curlVariables`。新建 DeepSeek provider 时继续走 `CreateOpenAICompatibleProvider`，同时保存 preset 的 DeepSeek `/user/balance` cURL；`ListAccounts` 投影为 `openai-compatible` `AccountRecord` 时带 `quotaKey=acct_*` 与 billing 配置；`GetCodexQuota` 对 openai-compatible 账号走 sidecar `quota-refresh`。

**sidecar**：CLIProxyAPI fork commit `f985fcda` 增加 openai-compatible account-store 字段、旧库补列、读写映射，以及 `/gettokens/quota-refresh/:account_key` 对 openai-compatible 的 quota/billing refresh 支持。已执行 `./scripts/ensure-sidecar.sh darwin arm64` 重建 dev sidecar。

**验证**：
- `go test ./internal/gettokens/accountstore ./internal/api/handlers/management -count=1`（sidecar）
- `go test ./internal/... -count=1`（sidecar）
- `go test ./internal/wailsapp ./internal/accounts ./internal/cliproxyapi`
- `cd frontend && node --test src/features/accounts/tests/accountDetailLayout.test.mjs src/features/accounts/tests/openAICompatible.test.mjs`
- `cd frontend && npm run test:unit`
- `cd frontend && npm run typecheck`

### Codex live sessions deleted account row recurrence

用户反馈正式环境最新代码仍在运行会话列表显示账号池不存在的 `78cline.murals+gzu@icloud.com`。排查既有 `20260603-codex-live-session-account-identity` space 后确认，sidecar 默认 snapshot 已按 `RuntimeAccountProjection` 过滤 detached/disabled/coarse-unavailable 行，但前端 `mergeCodexLiveSessionsSnapshot()` 会在同为 `source=live` 的后续轮询中把被 sidecar 省略的旧 session 重新 append 回浏览器状态，导致 sidecar 过滤被 UI 本地 merge 抵消。

修复：`frontend/src/features/codex-live-sessions/model/snapshotMerge.ts` 将 live poll 视为权威快照，`source=live` 下不再保留下一轮缺失的 session；cache/failure 的旧行保留仍由 `buildCodexLiveSessionsLoadFailureSnapshot()` 显式进入 `source=cache` 后负责。更新回归测试覆盖 `78cline.murals+gzu@icloud.com` detached 行被后续 live poll 删除。

验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs` 通过。`cd frontend && npm run typecheck` 仍因当前工作区既有 OpenAI-compatible billing 类型缺口失败（`OpenAICompatibleProvider` 缺少 quota/billing/cookie/curlVariables 字段），本次 live sessions 改动未引入该失败。

### Deep Fix: live sessions account deletion root cause

在用户要求“彻底修改根本原因”后，将运行会话旧账号问题从前端展示修复下沉到 sidecar 运行态：新增 `gettokenshooks.PruneCodexLiveSessionsForAccount(authID, accountKey, reason)`，账号删除/runtime auth removal 与账号禁用/status change 时主动清理 live-session tracker 中匹配 `authID` 或 `accountKey` 的当前会话，同时清理 requestMap 和 active auth counts。这样即使不经过前端，sidecar 当前快照也不会继续持有已删除/禁用账号的 running session 行。前端 live poll 权威快照修复继续保留，防止 UI merge 抵消 sidecar 过滤。

修改点：`docs-linhay/references/CLIProxyAPI/internal/gettokenshooks/live_sessions.go`、`live_sessions_test.go`、`sdk/cliproxy/service.go`，以及前端 `snapshotMerge.ts` / `model.test.mjs`。验证通过：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`cd docs-linhay/references/CLIProxyAPI && go test ./internal/gettokenshooks ./internal/gettokensrouting ./sdk/cliproxy`、`bash docs-linhay/scripts/check-docs.sh`。

补充验证：已执行 `./scripts/ensure-sidecar.sh darwin arm64`，dev sidecar 二进制已按当前 CLIProxyAPI dirty 源重建到 `build/bin/cli-proxy-api`。未触碰 `/Applications/GetTokens.app` 正式版。

### Hunt: Codex WebSocket interrupted 日志佐证

用户反馈 Codex 会话 `019e90ad-2afe-7b50-9c27-35d44246338d` 使用 `GetTokens - http://cpa.host.dxy/v1`、模型 `gpt-5.5` 时大量出现 `Conversation interrupted`，而直配本地凭证不复现。正式 sidecar 日志 `/Users/linhey/.config/gettokens/sidecar.log` 佐证为 GetTokens Codex WebSocket relay 路径问题：`2026-06-04 11:02:22` 对 `gpt-5.5` 选择 `codex:apikey:3df2001c2d1b` 且 `websockets=true`，连到 `ws://cpa.host.dxy/v1/responses`；`11:22:50` 同 session `635931a8-9660-436b-8976-e97c90d95498` 上游 `session_closed`，随后 `pinned auth released`，原因是 `408 stream closed before response.completed`，之后同模型请求持续返回 `503 auth_unavailable: no auth available (providers=codex, model=gpt-5.5)`。

源码链路：Codex `responses_websocket.rs` 在 websocket `Ok(None)` 且未收到 `response.completed` 时返回 `stream closed before response.completed`；CLIProxyAPI `openai_responses_websocket.go` 在 data channel close 且未 completed 时写 408 error，并在 pinned auth 释放后进入后续 503；Codex TUI `input_restore.rs` / `turn_runtime.rs` 对 `TurnAborted` 渲染固定 `Conversation interrupted` 文案。当前结论：根因不在模型本身，直配本地凭证绕过 GetTokens WS relay / pinned auth / route guard，因此不复现。低风险规避方向是让本地 Codex relay provider 默认 `supports_websockets=false`，必要时用户手动启用 WS。

### Repro: Codex WS upstream 408 -> route guard 503

已在 CLIProxyAPI fork 新增可执行模拟测试 `TestResponsesWebsocketMockUpstream408ThenRouteGuardUnavailable`：fake upstream WebSocket 接收 `/responses` 后返回 `type=error,status=408,stream closed before response.completed`，fake downstream Codex client 经 GetTokens relay 第一次触发 upstream transient route guard，第二次重连同模型 `gpt-5.5` 命中 `503 auth_unavailable: no auth available (providers=codex, model=gpt-5.5)`。测试日志与正式 sidecar 事故日志形态一致，包含 `route auth selected ... websockets=true`、`codex websockets: upstream connected`、`downstream_out ... status":408`、后续 `downstream_out ... status":503`。

验证命令：`cd docs-linhay/references/CLIProxyAPI && go test ./sdk/api/handlers/openai -run 'TestResponsesWebsocket(MockUpstream408ThenRouteGuardUnavailable|ClosesOnCodexUpstreamDisconnect|ReleasesPinnedAuthAfterRouteGuardBlock|RequestBoundaryReleaseUsesRouteGuard)' -count=1`。

### Space: Codex WebSocket interrupted 根治方案

已建立 `docs-linhay/spaces/20260604-codex-websocket-interruption-fix/`，方案入口为 `plans/root-fix-plan.md`。推荐方案定为三层修复：GetTokens relay provider 默认 `supports_websockets=false` 走 HTTP Responses；sidecar 将 WS 传输型 `408 stream closed before response.completed` / session close / abnormal close / timeout 归类为 WebSocket transport failure，不打成 auth 全局不可用；Status 页检测存量 `supports_websockets=true` 的 GetTokens provider 并提供显式修复入口。最小可交付是默认降级与存量检测，完整根治需要再加 sidecar WS circuit breaker。

### Fix: Codex WebSocket interrupted 根治落地

已在 dev/source 范围完成 Codex WebSocket interrupted 根治的 Phase 1 + Phase 2。前端 Status 和账号卡 Codex local apply 默认 `supports_websockets=false`；Wails `GetLocalCodexModelProviderStateView` 解析当前 provider 的 `base_url` 与 `supports_websockets`，Status 页可检测当前本地 GetTokens relay provider 已显式开启 WebSocket 的存量风险，并提示保持开关关闭后应用会改写为 HTTP Responses。

CLIProxyAPI fork 新增 `TransportFailureKindWebsocket`、WebSocket transport failure error wrapper、auth 内存级 `websocket_circuit_open_until` 熔断。Codex upstream WS 返回 `408 stream closed before response.completed`、`session_closed`、abnormal close 或 timeout 时，只熔断该 auth 的 WebSocket 能力，不再创建 upstream transient route guard，不再把同一 auth/model 放大成后续 `503 auth_unavailable`。Codex auth 显式 `websockets=false` 或 circuit 命中时，downstream WS 会关闭并提示 retry over HTTP；openai-compatible 仍走 HTTP fallback。

验证通过：
- `cd docs-linhay/references/CLIProxyAPI && go test ./sdk/cliproxy/auth ./sdk/api/handlers/openai -count=1`
- `go test ./internal/wailsapp -run 'TestParseLocalCodexModelProviderStateReadsCurrentProvider|TestGetLocalCodexModelProviderStateReadsConfigTomlFromCodexHome|TestApplyRelayServiceConfigToLocalV2.*SupportsWebsockets|TestApplyRelayServiceConfigToLocalWritesCustomProviderFacingFiles' -count=1`
- `npm --prefix frontend run test:unit -- src/features/accounts/tests/accountLocalCliMapping.test.mjs src/features/status/tests/relayLocalState.test.mjs`（实际执行全量 689 个前端 unit）
- `npm --prefix frontend run typecheck`

边界：未触碰 `/Applications/GetTokens.app` 正式版、正式版 sidecar 或正式配置；真实 Codex CLI + 隔离 `CODEX_HOME` 冒烟后续已补验，结果见下方“真实冒烟完成”记录。

沉淀：已更新 `.agents/skills/gettokens-codex-account-list/SKILL.md`，新增 Codex WebSocket relay failure boundary。不上升 AGENTS.md，因为这是 Codex relay/domain 规则，不是 repo-wide 流程治理规则。

补充纠偏：按用户提醒复核 Codex 源码，`codex-rs/codex-api/src/provider.rs::Provider.websocket_url_for_path()` 支持 `http -> ws`、`https -> wss`，并对 `ws/wss` 原样保留。已修正 CLIProxyAPI fork 的 `buildCodexResponsesWebsocketURL()`，允许 `ws://` / `wss://` 作为显式 WebSocket upstream base URL，不再只接受 `http/https`；验证 `go test ./internal/runtime/executor -run 'TestBuildCodexResponsesWebsocketURLRequiresHTTPURL|TestCodexWebsockets' -count=1` 通过。默认 `supports_websockets=false` 是 GetTokens local apply 策略，不代表删除 WSS 支持。

最终复核：已重跑 `bash docs-linhay/scripts/check-docs.sh`、相关主仓库 diff `git diff --check`、CLIProxyAPI 相关 diff `git diff --check`，均通过；已重跑 `cd docs-linhay/references/CLIProxyAPI && go test ./sdk/cliproxy/auth ./sdk/api/handlers/openai -count=1`、`go test ./internal/wailsapp -run 'TestApplyRelayServiceConfigToLocal|TestApplyRelayServiceConfigToLocalV2|TestGetLocalCodexModelProviderState|TestParseLocalCodexModelProviderState' -count=1`、前端全量 unit 689 个与 typecheck，均通过。真实 Codex CLI + 隔离 `CODEX_HOME` 冒烟后续已补验，未触碰正式版应用、正式 sidecar 或正式配置。

补充回归定位：用户指出“路由改造前这个功能是好的”。复核 CLIProxyAPI 历史后确认，WebSocket relay 本身不是根因；`c9a91feb feat: converge gettokens account route guard pipeline` 之后，执行结果 `408` 被统一接入 `AccountRouteGuardResultHook`，才把 WS 单次断流放大成 auth/model 全局 route guard。已补齐 executor handshake `408/5xx`、dial、send/retry-send 失败的 `TransportFailureKindWebsocket` 包装，并让 auth manager 的直接 `ExecuteStream` 错误分支也识别该标记。新增验证：`go test ./internal/runtime/executor -run 'TestCodexWebsocketsExecuteStreamHandshake408IsTransportFailure|TestBuildCodexResponsesWebsocketURLRequiresHTTPURL|TestParseCodexWebsocketError' -count=1`、`go test ./sdk/cliproxy/auth -run 'TestManagerExecuteStreamWebsocketTransportFailureOnlyOpensWebsocketCircuit|TestManagerExecuteStreamWebsocketHTTPFallbackStripsGenerateOnlyForHTTPAuth|TestAuthAllowsWebsocketsHonorsTemporaryCircuit|TestWebsocketsAllowedForRequestRequiresDownstreamWebsocket' -count=1` 通过；随后相关 executor/auth/openai WebSocket 聚焦测试通过。

真实冒烟完成：按用户要求跑了一遍 dev source 真实 Codex CLI 链路。执行 `./scripts/ensure-sidecar.sh darwin arm64` 重建当前 dirty sidecar；复制 `~/.config/gettokens-dev` 到 `/tmp/gettokens-codex-smoke.*`，改端口 `28317`，用临时 `auth-dir` / `accounts-v1.sqlite` 启动 sidecar；隔离 `CODEX_HOME` 写入 GetTokens provider `supports_websockets=false` 和本地 relay key。`GET /v1/models?client_version=0.136.0` 返回 `200` 且包含 `gpt-5.5`；`codex -a never exec --skip-git-repo-check --ephemeral --sandbox read-only --model gpt-5.5 --output-last-message ... 'Reply with ok'` exit code `0`，最后消息 `ok`。sidecar 日志显示真实请求为 `POST "/v1/responses"`，未出现 WebSocket upgrade / upstream connected。验证后停止临时 sidecar 并删除临时含密目录；未触碰 `/Applications/GetTokens.app`、正式 sidecar 或正式配置。

### Fix: Codex 运行会话表头请求视图误切换

用户澄清 `Codex -> 运行会话 -> 会话列表` 表头不应该切换到请求列表；当期需求是表头汇总当前列表内全部请求，右侧作为未选中时的总览。根因是 `SessionFeed` 后续被实现成 `feedMode=sessions|requests`，并把表头做成切换按钮，导致点击表头进入请求列表视图。

修复：移除左侧请求列表视图和表头点击切换，仅保留会话列表；表头显示 `会话数 · 请求数`；`CodexLiveSessionsWorkbench` 未选中会话时不再自动回落到第一条，而是把右侧渲染为请求总览，点击左侧会话后再进入单会话详情。

验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs` 通过；`npm --prefix frontend run typecheck` 仍因当前工作区既有 OpenAI-compatible billing 类型缺口失败，错误在 `frontend/src/features/accounts/model/openAICompatible.ts`，与本次 live sessions 改动无关。

补充调整：按用户要求，右侧总览进一步收敛为“汇总行 + 请求耗时趋势图 + 请求列表”，不保留统计卡片。无头浏览器验收 `#frame=codex&workspace=live-sessions` 通过：右侧标题为 `请求总览 / 请求耗时趋势 / 请求列表`，图表存在，请求行存在，统计卡数量为 0，汇总行为 `4 个会话 · 53 个请求 · 1 运行 · 1 降级 · 1 失败`；截图归档到 `docs-linhay/spaces/20260521-codex-live-session-detail/screenshots/20260604/live-sessions/20260604-live-sessions-chart-after-v02.png`。

再次修正：清空按钮按用户要求移到页面顶部操作区，左侧会话列表表头不再放清空按钮。总览图表空白的根因是未选中会话时只使用 row snapshot，而真实 live snapshot 可能没有 embedded request timing；已增加全局 history 加载路径，调用 `GetCodexLiveSessionHistory({ sessionID: '', window: 'all', limit: 80 })`，并将 history requests 转为 overview request rows。验证：`node --test frontend/src/features/codex-live-sessions/model.test.mjs`、`npm --prefix frontend run typecheck` 通过；无头浏览器确认顶部有清空、左侧表头无按钮、右侧图表 line path 存在且点数 31；截图归档到 `docs-linhay/spaces/20260521-codex-live-session-detail/screenshots/20260604/live-sessions/20260604-live-sessions-nav-after-v03.png`。

补充：用户要求 `SessionOverview` 第 165 行汇总块拆成卡片，并用 Open Design 参与设计。Open Design 起初不可达，用户启动后 `od status --json` 正常，版本 `0.8.0`；本轮选取 `ant` 作为数据密集桌面工作台参考，采用其清晰层级、一致网格和 4/8/12/16 间距原则，不引入新的品牌色。设计修正为 4 个同级卡片：身份说明、请求总量、运行态、风险态；汇总图表改为复用会话详情的 `TimingTrendChart` 与 footer 指标样式；汇总请求列表改为复用 `Timeline` 请求时间线样式。验证：focused 单测、typecheck、无头浏览器 DOM 通过；before/after 截图归档到 live-session space。

继续压缩：用户指出 `SessionOverview` 汇总卡片高度过高，Open Design 复核截图发现 620px 右侧容器强行四列时 identity 卡说明文案被压成多行，是高度噪声来源。已将 identity/metric 卡片从 `min-h-[9rem] p-4` 调整为 `min-h-[5.75rem] p-2.5`，移除 identity 说明长句，主数字降到 `font-size-ui-2xl`。无头浏览器实测 4 张汇总卡片高度均为 `92px`；图表与请求时间线仍复用会话详情样式，页面文本中有 `请求耗时趋势` 和 `请求时间线 15 行`，新版截图为 `docs-linhay/spaces/20260521-codex-live-session-detail/screenshots/20260604/live-sessions/20260604-live-sessions-overview-after-v06.png`。

交互补充：用户要求 `SessionFeed` 表头点击显示汇总。实现为表头按钮 `data-codex-session-feed-overview-trigger="true"`，点击时 `setSelectedSessionID(undefined)`；这只是回到右侧汇总，不恢复之前误做的请求列表视图切换。无头浏览器验证：点击第一条会话后 `selectedRows=1`、`aria-pressed=false`；点击表头后 `selectedRows=0`、`aria-pressed=true`，右侧显示 `请求总览` 与 `请求耗时趋势`。截图为 `docs-linhay/spaces/20260521-codex-live-session-detail/screenshots/20260604/live-sessions/20260604-live-sessions-overview-after-v07.png`。

样式补齐：用户指出 `SessionFeed` 表头总览选中态不能和其他列表选中样式不一致。已把表头改为与 `SessionRow` 同类的 `div role="button"`，并为表头与会话行共用 `codex-live-session-list-item-selected` / `codex-live-session-list-item-idle`。选中态使用稳定 token `--bg-muted`，无头浏览器等待 transition 后验证表头总览态与会话行选中态 computed background 均为 `rgb(240, 240, 240)`。

样式加重：用户继续反馈选中样式太淡。保持同一 shared selected 类不变，把 `codex-live-session-list-item-selected` 加重为 `color-mix(in srgb, var(--text-primary) 12%, var(--bg-main))` 背景，并加 `inset 3px 0 0 var(--text-primary)` 左侧 rail。无头浏览器验证表头总览态与会话行选中态 background 均为 `color(srgb 0.88 0.88 0.88)`，box-shadow 均为 `rgb(0, 0, 0) 3px 0px 0px 0px inset`。截图为 `docs-linhay/spaces/20260521-codex-live-session-detail/screenshots/20260604/live-sessions/20260604-live-sessions-overview-after-v08.png`。

### Fix: OpenAI-compatible billing Wails 类型缺口

`scripts/build-local-macos-package.sh` 的前置 `npm --prefix frontend run typecheck` 失败，根因是 root Wails DTO `main.OpenAICompatibleProvider` 未同步 `internal/wailsapp.OpenAICompatibleProvider` 已存在的 quota/billing/cookie/curlVariables 字段，而前端 draft 已按这些字段读取。已补齐 `app_types.go`、root `app.go` 映射和 `frontend/wailsjs/go/models.ts` 生成类型，保持 root Wails 契约、core DTO 与前端类型一致。

验证：`go test . -run 'TestOpenAICompatibleRootDTOsPreserveBillingFields' -count=1`、`go test ./internal/wailsapp -run 'Test(Create|Update|List).*OpenAICompatible|TestGetCodexQuotaLoadsUnifiedOpenAICompatibleCredential' -count=1`、`go test ./...`、`npm --prefix frontend run typecheck`、`bash docs-linhay/scripts/check-docs.sh` 通过；`npm --prefix frontend run test:unit -- src/features/accounts/tests/openAICompatible.test.mjs` 实际跑完全量前端 unit，689 个通过。

### References: frontend design project mirrors

按用户要求将 3 个前端设计/系统设计参考项目整理到 GetTokens 文档体系：本地克隆 `Leonxlnx/taste-skill`、`pbakaus/impeccable`、`devkodeio/frontend-system-design` 到 `docs-linhay/references/<project>/`，源码目录继续受 `.gitignore` 忽略，不进入主仓库提交。新增摘要 `docs-linhay/references/20260604-frontend-design-reference-projects.md`，更新 `docs-linhay/references/README.md` 索引。结论：`taste-skill` 用作 AI 反模板化前端 skills 与视觉生成参考；`impeccable` 用作命令化设计流程、anti-pattern 检测和 live iteration 参考；`frontend-system-design` 用作中大型前端 feature 的系统设计 checklist。已新增项目级 skill `.agents/skills/gettokens-frontend-design-quality/`；暂不新增 AGENTS 规则或生产依赖。

### Skill: GetTokens frontend design quality

根据用户纠正，3 个参考项目不能只停留在 references 摘要，已进一步沉淀为项目级 skill `.agents/skills/gettokens-frontend-design-quality/`。该 skill 融合 `taste-skill` 的反模板化视觉 pre-flight、`impeccable` 的 shape/critique/audit/polish/harden 命令化 loop、`frontend-system-design` 的系统设计 checklist，并加上 GetTokens 桌面/Wails、sidecar 数据边界、无头浏览器截图和桌面验收门禁。使用 `uv run --with pyyaml python .../quick_validate.py .agents/skills/gettokens-frontend-design-quality` 验证通过；`docs-linhay/scripts/check-docs.sh` 通过。

### Skills: frontend design references split into executable layers

继续沉淀 3 个前端参考项目：在总控 skill `.agents/skills/gettokens-frontend-design-quality/` 基础上新增 3 个细分项目级 skills：`gettokens-frontend-shape-handoff`（前端需求成形与 Gemini/实现交接 brief）、`gettokens-frontend-system-design`（中大型 Wails/desktop 前端 feature 的状态/DTO/routing/性能/测试 checklist）、`gettokens-frontend-audit-polish`（现有页面审计、反 AI 模板化、polish 与 harden）。总控 skill 已补 skill 分层路由。4 个 frontend-design skills 均通过 `quick_validate.py`；docs check 通过。

### Skill merge: frontend design quality consolidation

按用户提醒“相似 skills 可以合并”，已将刚拆出的 `gettokens-frontend-shape-handoff`、`gettokens-frontend-system-design`、`gettokens-frontend-audit-polish` 三个相似前端 skills 合并回统一入口 `.agents/skills/gettokens-frontend-design-quality/`，删除细分目录，避免项目级 skills 触发语义重叠和维护碎片化。统一 skill 内保留 4 个模式：`Shape/Handoff`、`System Design`、`Audit/Polish/Harden`、`Implementation Gate`。合并后 `quick_validate.py` 通过，`check-docs.sh` 通过。

## 账号详情页全量模块状态稿

- 在 `docs-linhay/spaces/20260529-account-detail-ui/account-detail-full-module-state-board-v01.html` 修正 Open Design 账号详情页全量模块状态板。
- 用户指出上一版不是账号详情页，后续明确要求以「前端现有页面」而不是线上截图为准；已改为按 `OpenAICompatibleDetailPanel`、`UnifiedAccountDetailModal`、`AccountProxyRouteSection`、`RateLimitRulesSection` 等现有组件与业务流梳理。
- 设计组织规则：垂直按现有模块，横向按业务状态；覆盖空闲/已保存、编辑 dirty、拉取/验证中、成功/已应用、错误/受阻。

### Space: Codex live sessions project aggregation

按用户要求为 `Codex -> 运行会话 -> 会话列表/项目列表` 创建需求 space：`docs-linhay/spaces/20260604-codex-session-project-aggregation/`。需求边界定位为运行态 live sessions 的项目维度聚合，不替代历史 `session-management`；首版目标是 `会话维度 + 项目维度 + 项目汇总详情`，项目聚合字段必须由 sidecar/Wails DTO 稳定返回或显式派生，前端不得通过 prompt、本地绝对路径等敏感/不稳定数据猜测项目。同步补充实施计划 `plans/20260604-implementation-plan.md`，明确 BDD/TDD、DTO、selector、无头浏览器与 Wails dev 验收门禁。

## 账号详情页重新设计 v02

- 按用户要求使用 Open Design、`gettokens-frontend-design-quality` 与工业线框设计方向，产出 `docs-linhay/spaces/20260529-account-detail-ui/account-detail-redesign-v02.html`。
- 交互形态从现有长表单重新组织为三栏 Provider Control Workbench：Identity Rail / Configuration Stack / Run Inspector + 底部 Command Bar。
- 同步新增设计 brief：`docs-linhay/spaces/20260529-account-detail-ui/plans/20260604-account-detail-redesign-brief.md`，明确数据权威、状态矩阵、保存门禁和后续实现证据。

### Space revision: live sessions project aggregation compact layout

用户指出运行会话页面没有足够空间做左中右三栏。已修正 `20260604-codex-session-project-aggregation`：废弃常驻项目栏 + 会话栏 + 详情栏并列方案，改为“顶部总览 + 项目 chips/更多项目面板 + 单一会话列表 + 详情复用/覆盖层”。项目维度作为紧凑过滤控制层，不额外占用一列；溢出项目进入 `更多…` 搜索面板。

## 账号详情页不同账号类型模拟稿 v03

- 新增 `docs-linhay/spaces/20260529-account-detail-ui/account-detail-account-types-v03.html`，在 v02 三栏工作台基础上完整模拟不同账号类型。
- 覆盖 OpenAI-compatible、Codex API Key、Codex Auth-file/OAuth、Claude Code、Token Plan 五类账号，并支持 ready/dirty/loading/error 状态组合切换。
- 关键设计判断：统一账号详情骨架，按类型替换合法模块；避免前端用同一表单强行承载 OAuth/auth-file、Claude、本地 CLI、split credential 等不同业务语义。

## 账号详情页低嵌套模拟稿 v04

- 用户反馈 v03 卡中卡过多、三栏宽度不足导致压缩且文字过多；已新增 `docs-linhay/spaces/20260529-account-detail-ui/account-detail-account-types-v04.html`。
- v04 将详情主体改为顶部摘要条 + 全宽 band row 分段 + 底部 Command Bar，取消常驻右侧 inspector，减少说明文字和嵌套容器。
- 仍支持五类账号类型与 ready/dirty/loading/error 状态组合切换，用更宽的表单行和表格承载主要交互。

### Space revision: live sessions navigation dimension switch

用户进一步明确“在导航区支持切换维度就好了”。已再次修正 `20260604-codex-session-project-aggregation` 并作为最终信息架构：项目维度不再放在主体上方 chips / rail，而是放入 Codex 运行会话导航区的 `会话` / `项目` 维度切换。主体始终保持单一列表：会话维度展示 session rows，项目维度展示 project aggregation rows，点击项目行后右侧切换为该项目汇总。

## 账号详情页对齐修正版 v05

- 用户指出 v04 左侧索引不一致、固定头部/验证与限制/身份凭据/请求配置需要重设计，并追问 Claude Code 账号类型与余额额度配置。
- 已新增 `docs-linhay/spaces/20260529-account-detail-ui/account-detail-account-types-v05.html`：统一 `--index` 左侧宽度，重做固定头部，重新设计第 01/02/04 模块，新增第 05“余额与额度”。
- 设计判断：Claude Code 不作为本轮账号详情主类型，避免把 Claude channel/local CLI/OAuth 域混入 OpenAI-compatible/Codex 详情主线；后续可单独设计 Claude channel 详情。

## 账号详情页头部不截断修正版 v06

- 用户继续指出 v05 中账号类型长标题（Token Plan / Split Credential、Codex Auth-file / OAuth 等）在头部固定区下方被截断。
- 已新增 `docs-linhay/spaces/20260529-account-detail-ui/account-detail-account-types-v06.html`：头部分为 `head-bar` 系统命令条与 `head-context` 上下文条，类型名称允许换行，指标区域只放短值。

### Implementation: live sessions navigation dimension switch

已开始并完成首版 `20260604-codex-session-project-aggregation` 实施：在 Codex 运行会话页面 Header 区域增加 `会话` / `项目` 维度切换，hash 支持 `view=project`。主体区域保持单一列表；`会话` 维度展示 session rows，`项目` 维度展示基于现有 `CodexLiveSession.projectName` 显式派生的项目聚合 rows，点击项目后不进入下级列表，右侧显示项目汇总。补充 `buildCodexLiveProjectSummaries` 与 page persistence 测试；未改 sidecar/Wails DTO，未触碰正式版 GetTokens。

### Verification: live sessions project dimension browser smoke

完成 `20260604-codex-session-project-aggregation` 无头浏览器冒烟：Vite dev 自动使用 `127.0.0.1:34116`，Playwright CLI 打开 `#frame=codex&workspace=live-sessions&view=project`，DOM 确认运行会话 Header 内 `会话` / `项目` 维度存在且 `项目` pressed，主体展示 `项目运行态`，点击项目行后左侧仍保持项目列表，右侧显示项目汇总。截图归档到 `docs-linhay/spaces/20260604-codex-session-project-aggregation/screenshots/20260604/live-sessions/20260604-live-sessions-project-after-v01.png`。

## 账号详情页请求配置对齐修正版 v07

- 用户指出 v06 的请求配置模块仍未和其他模块统一；已新增 `docs-linhay/spaces/20260529-account-detail-ui/account-detail-account-types-v07.html`。
- v07 将请求配置改为同一套字段网格：Header Mode、Proxy Mode、Request Route、Headers、Proxy URL；操作按钮统一放到底部动作行，避免 Proxy 字段内部混排导致视觉不一致。

## 账号详情页请求配置左右结构修正版 v08

- 用户指出 v07 的 `02 请求配置` 是上下结构，而其他模块是左右结构；已新增 `docs-linhay/spaces/20260529-account-detail-ui/account-detail-account-types-v08.html`。
- v08 将请求配置改为左右 split：左侧请求头与 route 元信息，右侧 proxy 与动作，保持低嵌套且与其他模块阅读方向一致。

## 账号详情页配色修正版 v09

- 用户在 in-app browser 打开 `account-detail-account-types-v08.html` 后指出配色不对；已直接更新该文件，并另存 `account-detail-account-types-v09.html`。
- 配色从米黄/工业纸底改为 GetTokens 当前浅色工作台 token：白主面、浅灰 surface、黑边框、灰弱文本，状态色只用于状态表达。
- Open Design 归档 artifact：`gettokens/account-detail-account-types-v09-palette-fixed.html`。

## 账号详情页 Split Credential 命名修正版 v10

- 用户在浏览器批注中询问“token plan 是啥？”。已确认 `Token Plan` 不应作为通用账号类型名。
- 已将当前打开的 `account-detail-account-types-v08.html` 与归档稿中的可见标签改为 `Split Credential`，表达“对话 key 与模型拉取 key 分离”的业务形态。
- 新归档稿：`docs-linhay/spaces/20260529-account-detail-ui/account-detail-account-types-v10.html`。

## 账号详情页代理池选择逻辑修正版 v11

- 用户在浏览器批注指出请求配置右侧应只能从代理池选择。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v11.html`。
- 代理区域现在只显示代理池节点选择、只读 resolved URL、只读 pool status；删除 Inherit/Direct/Use Pool/Test Route 和自定义 proxy URL 语义。

## 账号详情页验证区移除限流摘要修正版 v12

- 用户在浏览器批注指出第 04 段中的 RPM/TPM/Dirty/Route “没这东西”。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v12.html`。
- 第 04 段从“验证与限制”收窄为“验证”，只保留测试模型、OAuth reauth/刷新模型和执行验证动作。

## 账号详情页余额额度测试结果修正版 v13

- 用户在浏览器批注指出余额和额度支持测试，测完应在此模块显示结果。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v13.html`。
- 第 05 段新增测试 Quota / 测试 Billing 操作，并在模块内展示 Last Quota Test / Last Billing Test 的成功、加载、错误结果。

## 账号详情页模型两列列表修正版 v14

- 用户在浏览器批注指出模型支持两列，否则模型多时过高。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v14.html`。
- 第 03 段从单列表格改为两列 `model-grid`，每个模型为紧凑条目，保留拉取远端模型、使用远端模型、新增模型动作。

## 账号详情页顶部关闭按钮移除修正版 v15

- 用户在浏览器批注指出顶部 `Close` 与底部关闭/放弃操作重复。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v15.html`。
- 头部 actions 区改为只提示 `Footer controls`，关闭语义收敛到底部命令栏。

## 账号详情页请求配置字段收窄修正版 v16

- 用户在浏览器批注询问 Header Mode/Header Count/Request Route/Headers 是否都支持。已确认只有 headersText 与代理池选择是当前应展示的配置语义。
- 已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v16.html`：左侧只保留 Custom Headers，右侧保留代理池节点选择。

## 账号详情页余额额度左右分栏修正版 v17

- 用户在浏览器批注建议余额额度模块“直接中间加上分割线，重新设计这块”。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v17.html`。
- 第 05 段现在左右分栏：Quota / 额度 与 Billing / 余额，中间用边线分割；每侧独立显示配置、测试、测试结果与最近测试日志。

## 账号详情页头部紧凑重设计修正版 v18

- 用户在浏览器批注指出头部高度太高且垃圾文本太多。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v18.html`。
- 头部现在为 74px 紧凑业务摘要，移除 detail/frame/actions/close 等实现文本，只保留账号名、类型、状态和关键业务指标。

## 账号详情页余额额度卡片模式修正版 v19

- 用户通过 Open Design/browser 批注指出余额/额度模块的 Source/Test Result 字段化设计不如卡片模式。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v19.html`。
- 第 05 段现在为两张业务卡片：Quota / 额度 与 Billing / 余额，各自显示关键指标、配置/测试动作和最近测试日志。

## 账号详情页代理功能按现有支持收窄 v21

- 用户在浏览器批注质疑代理池区域功能是否都支持。已查看 `AccountProxyRouteSection`：详情页可选择已保存 proxy pool 节点并保存 resolved URL，但不支持在详情页刷新/测速/管理节点。
- 已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v21.html`：删除刷新代理池、测试所选节点和假 available 状态，只保留节点选择、只读 resolved URL 与说明。

## 账号详情页 Custom Headers 高度自适应修正版 v22

- 用户在浏览器批注指出 Custom Headers textarea 高度应自适应，否则区域太空。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v22.html`。
- Headers 内容从转义 `\n` 改为真实换行，textarea 增加 autosize 脚本，默认按两行显示，输入更多内容时自动撑高。

## 账号详情页账号类型与模型映射整理 v23

- 用户指出头部类型区还会支持其他类型，且模型区应体现 A -> B 的映射关系。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v23.html`。
- 头部改为通用“账号类型”表达；模型区改为 `Source Model → Alias / Route` 映射卡，保留两列布局。

## 账号详情页模型映射错位修正版 v24

- 用户在浏览器批注指出模型映射区错位。根因是类型切换 class 的 `display:block` 覆盖了 mapping card 的 grid。
- 已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v24.html`：映射卡强制为 Source Model → Alias / Route 三段式 grid，窄视口下单列显示。

## 账号详情页头部类型摘要整理 v25

- 用户建议头部中区改为“上方显示标签组，下方文字描述并分割”。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v25.html`。
- 头部中区现在上方为类型/凭据/验证/路由/余额标签组，下方为账号类型能力描述，避免指标表格挤压和信息混杂。

### UI correction: live sessions dimension switch moved to header

根据浏览器评论，`会话` / `项目` 维度切换不应在左侧 sidebar 的 `运行会话` 子菜单中，而应在运行会话页面顶部 `WorkspacePageHeader` 区域。已移除 sidebar 内子维度按钮，改到 Header actions；Playwright DOM 确认 sidebar 只保留 `运行会话`，Header 内存在 `运行会话维度`，且 `项目` pressed。截图：`docs-linhay/spaces/20260604-codex-session-project-aggregation/screenshots/20260604/live-sessions/20260604-live-sessions-project-after-v02.png`。

## 账号详情页头部真实标签样式修正版 v26

- 用户指出 READY 那种才是标签。已修正当前打开的 `account-detail-account-types-v08.html`，并新增归档稿 `account-detail-account-types-v26.html`。
- 头部摘要中的类型、凭据、验证、路由、余额/额度现在使用真实 pill/tag 样式，下方保留能力描述。

### UI correction: project row selects summary instead of drilling into sessions

根据浏览器评论，`项目` 维度下点击项目聚合行不应进入下级列表。已改为左侧保持项目聚合列表并高亮选中项目，右侧直接显示该项目汇总、耗时趋势和请求列表；移除未使用的 `返回项目` 按钮契约和相关文案。截图：`docs-linhay/spaces/20260604-codex-session-project-aggregation/screenshots/20260604/live-sessions/20260604-live-sessions-project-after-v03.png`。

### UI correction: selected session timing trend matches session card style

根据浏览器评论，选中会话后的右侧 `请求耗时趋势` 模块样式需与会话列表卡片一致。已将 `RequestTimingTrend` 改为 session-style 卡片壳：同类边框、背景、轻 shadow、header 分隔与内容区内边距。截图：`docs-linhay/spaces/20260604-codex-session-project-aggregation/screenshots/20260604/live-sessions/20260604-live-sessions-session-trend-after-v01.png`。

### UI correction: overview and selected-session timing trends share one style

根据浏览器评论，运行总览与选中会话的 `请求耗时趋势` 不应存在两种样式。已统一 `OverviewTimingTrend` 和 `RequestTimingTrend` 的外壳：同类边框、背景、轻 shadow、header 分隔和内容内边距，并移除 overview 外层旧 wrapper。截图：`docs-linhay/spaces/20260604-codex-session-project-aggregation/screenshots/20260604/live-sessions/20260604-live-sessions-overview-trend-after-v01.png`。

### UI correction: request timeline/list style unified

根据浏览器评论，请求时间线 / 请求列表也存在多套样式。已将 `Timeline` 统一为 session-style 卡片壳，移除 overview 外层旧 wrapper，并去掉固定 `min-h-[320px]`，单请求会话不再撑出大块空白。截图：`docs-linhay/spaces/20260604-codex-session-project-aggregation/screenshots/20260604/live-sessions/20260604-live-sessions-timeline-style-after-v01.png`。

### Session distillation: live/detail surface consistency

本轮“整理会话”沉淀了一个可复用前端模式：同一页面的 overview、selected detail、project/session view 若展示同类趋势图、请求时间线或列表，必须共用同一套外壳和行级样式，不能只修用户指出的单个实例。已写入 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Live/detail surface consistency rule`。本次不升级 AGENTS：这是前端质量 skill 内的可复用审计/实现规则，不是 repo-wide 行为治理。
