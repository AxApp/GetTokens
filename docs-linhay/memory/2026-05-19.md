# 2026-05-19

## Codex WebSocket 路由策略

- 用户指出下游已经发起 WebSocket 时，GetTokens/CLIProxyAPI 不应因为 `codex-api-key` 没有显式 `websockets=true` 就跳过高优先级代理账号；正确语义应是按下游协议尝试当前候选账号，只有显式 `websockets=false` 才禁用。
- 排查确认当前真实请求链路为 `Codex CLI -> 127.0.0.1:8317 -> auth-2.json OAuth -> wss://chatgpt.com/backend-api/codex/responses`，原因是 `AuthDataAllowsWebsockets` 只让 Codex auth-file 默认支持 WebSocket，而 `codex-api-key` 默认被判为不支持，导致 `preferCodexWebsocketAuths` 在下游 WebSocket 场景过滤掉最高优先级 `cpa.host.dxy` 代理账号。
- 已在 `docs-linhay/references/CLIProxyAPI` 修正策略：Codex provider 下账号默认跟随下游 WebSocket，显式 `websockets=false` 才禁用；更新选择器与 scheduler 测试，确认 `codex-api-key` 默认可参与 WebSocket 且保留优先级顺序。
- 验证通过：`go test ./sdk/cliproxy/auth`、`go test ./sdk/api/handlers/openai ./internal/runtime/executor`。注意当前运行中的 `/Applications/GetTokens.app` sidecar 仍是旧二进制，需重新构建/替换后实机路由才会生效。

## Theme Skinning Space

- 换肤 / 主题能力进入排期，新建 `docs-linhay/spaces/20260519-theme-skinning/`。
- 本期边界先定为把现有 `system / light / dark` 明暗模式升级为可扩展主题体系，保持 `theme-mode` 兼容，同时补齐主题 token、设置入口、核心页面接入和截图回归验证。
- 初始计划记录在 `docs-linhay/spaces/20260519-theme-skinning/plans/20260519-theme-skinning-plan-v01.md`，当前状态为 `scheduled`。
- UI 框架研判结论：不建议接入 MUI / Mantine / Ant Design / Chakra 这类大而全预设样式框架；建议保留 Tailwind + CSS variables + 自有组件体系，按需引入 headless / unstyled primitives（优先 Radix Primitives，备选 React Aria Components）解决 Dialog、Popover、Dropdown、Select、Tooltip 等复杂交互的可访问性和焦点管理。

## Design System Workbench Space

- 为设计系统工作台新建 `docs-linhay/spaces/20260519-design-system-workbench/`，目标是在应用内新增独立前端路由 `design-system`，集中预览基础组件、主题 token、文字缩放与各类交互状态。
- 本 space 与 `20260519-theme-skinning` 互相关联：换肤负责主题体系，设计系统工作台负责可运行的组件预览、截图回归和后续 headless primitive spike 场地。
- 初始计划记录在 `docs-linhay/spaces/20260519-design-system-workbench/plans/20260519-design-system-workbench-plan-v01.md`，优先从 `AppPage` / hash 持久化 / Sidebar 入口 / `components/ui` catalog 开始。
- 技术方案补齐：一期采用应用内 `design-system` hash frame 路由，不引入 Storybook、React Router 或新 UI 框架依赖；新增 `features/design-system` 的 catalog / tokenCatalog / sampleData / model 分层，测试从 `pagePersistence` 红灯和 catalog model 完整性开始。
- 用户确认可接受 Storybook 这类认知边界外的专业工具后，设计系统方案升级为 v02：Storybook 作为主工作台，应用内 `design-system` 路由只做入口、说明和覆盖矩阵；v01 标记 superseded。
- 会话沉淀：后续遇到成熟专业工具直接命中问题核心时，应主动建议并解释边界、风险和回滚，而不是默认自研较弱版本；已写入 `gettokens-domain-engineering` skill 和 `docs-linhay/dev/20260519-design-system-storybook-session-distillation.md`。
- 设计系统工作台首期已落地：接入 Storybook 10 + `@storybook/react-vite`，新增 `#frame=design-system` 应用内入口、Sidebar 文案、story catalog 和 9 个首批 stories（tokens / primitives / components）。
- 验证通过：`node --test frontend/src/utils/pagePersistence.test.mjs frontend/src/features/design-system/storyCatalog.test.mjs`、`npm --prefix frontend run test:unit`（307 passed）、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`npm --prefix frontend run build-storybook`。
- 浏览器验收通过：Storybook `http://127.0.0.1:6006` 可打开并渲染 CSS token，应用内 `http://127.0.0.1:5173/#frame=design-system` 显示 3 个分组 / 9 个 stories。截图归档到 `docs-linhay/spaces/20260519-design-system-workbench/screenshots/20260519/design-system/`。
- Storybook 本地化补齐：新增 `GetTokens preview language` toolbar，默认中文，支持切换 `中文 / English`，并同步 iframe `lang`；首批 stories 示例文案接入 `I18nProvider`。边界为 GetTokens preview 内容本地化，Storybook 管理界面本身不做非官方汉化。
- 设计系统 story 形态补充：复杂组件不能只拆成多个单状态 story，还需要默认提供 `Overview` 状态矩阵，一页展示关键状态用于主题/字号/密度回归。`ActionSelect` 已新增同屏展示“仅创建 / 创建和删除 / 长内容 / 禁用”的 Overview story，单状态 stories 保留用于隔离调试。
- `Overview` 状态矩阵规则已推广到首批全部 component stories（ActionSelect、Combobox、PageLoadingFallback、SegmentedControl、ToggleSwitch、WorkspacePageHeader），并在 `storyCatalog.test.mjs` 加测试门禁；同时写入 `gettokens-domain-engineering` skill，后续新增组件 story 默认遵循 Overview + 单状态 stories 双层结构。
- 设计系统准入边框规则落地：新增 `DesignSystemStoryFrame`，所有已纳入 design system 的 component story 示例用红色虚线边框、`DS` 角标和 `data-design-system-component="true"` 标记，帮助区分正式组件与未纳入系统的业务拼装；测试门禁已要求 component stories 使用该 frame。

## 账号池密度模式

- 账号池聚合页已参考 Codex 账号列表补齐 `完整 / 缩略 / 列表` 三种密度模式，默认保持原有完整卡片，缩略模式隐藏重信息区，列表模式改为单列横向扫描行。
- 密度偏好持久化为 `gettokens.accounts.display-mode`，并同步到 hash 的 `density=compact|list`；完整模式会移除默认 density 参数。
- 验证通过：`node --test src/features/accounts/tests/accountListLayout.test.mjs`、`npm run typecheck`、`npm run test:unit`。Playwright 预览确认桌面缩略/列表布局可切换，截图归档到 `docs-linhay/screenshots/20260519/accounts/`。

## Codex 请求顺序卡片网格

- 用户反馈 Codex 请求顺序 `AccountOrderRow` 在中等屏幕仍只显示单张卡；确认根因是 `CodexAccountOrderSection` 非列表模式使用 `xl:grid-cols-3`，导致 `xl` 以下无论容器实际宽度都保持单列。
- 已改为 Codex 专用 `auto-fit + minmax` 自适应网格：缩略卡最小 288px，完整卡最小 320px；列表模式继续固定单列。
- 验证通过：`npm --prefix frontend run test:unit -- src/features/codex/codexAccountList.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`；浏览器预览 1100px 视口下主内容宽度约 782px，缩略和完整模式均为两列 `367px 367px`。

## CLIProxyAPI 上游同步

- 已将 `docs-linhay/references/CLIProxyAPI` 的维护分支 `gettokens/wham-token-fix` 合并到上游 `upstream/main` 最新 `bbe30f53`（v7.1.15），本地合并提交为 `497ec7c9`，随后提交 Codex WebSocket 路由修复 `c640f8cd`。
- 冲突集中在系统代理相关实现，解析策略为保留 GetTokens 的 `use-system-proxy` / `BuildSystemDialer` 覆盖，同时吸收上游 HTTP CONNECT dialer、代理凭据脱敏和 xAI/model catalog 变更。
- 合并后补齐 `internal/api/server_test.go` 缺失的 `strings` import，并验证 `go test ./sdk/proxyutil ./internal/auth/claude`、`go test ./sdk/cliproxy/auth`、`go test ./...` 均通过；WebSocket 修复提交后再次验证 `go test ./sdk/cliproxy/auth ./sdk/proxyutil ./internal/auth/claude`。
- 已执行 `./scripts/ensure-sidecar.sh darwin arm64` 重建本地 `build/bin/cli-proxy-api`。本地 `server` 可执行文件判断为构建产物，不纳入提交，并已通过 CLIProxyAPI `.gitignore` 忽略。

## Codex MCP 本地启动超时

- 用户偏好确认：`xcode`、`xcode-tools`、`figma-desktop` 这类本地 MCP 即使当前服务未启动，也不应被自动改为 `enabled = false`；未启动只代表当次不可连接，不代表长期禁用。
- 本地 MCP 启动超时应快速失败，`~/.codex/config.toml` 中相关 server 保持启用，并将 `startup_timeout_sec` 调整为 5 秒。
- `xcode-tools` 移除陈旧的 `MCP_XCODE_PID` / `MCP_XCODE_SESSION_ID` 固定绑定，避免 Xcode 重启后继续指向不存在的进程。

## CLIProxyAPI 上游同步流程沉淀

- 本轮“整理”提炼出 CLIProxyAPI fork 上游同步闭环：fork 内先完成上游 merge、冲突修复、本地补丁提交；每次 fork HEAD 变化后重跑 `./scripts/ensure-sidecar.sh darwin arm64`；最后在父仓库只提交 gitlink、docs/memory 和必要构建产物。
- 对未跟踪文件的判断规则已固化：先用 `file`、`ls -lh`、`git ls-files`、`git log --all -- <path>`、`git check-ignore -v` 判断来源。类似 `server` 的大型 Mach-O 可执行文件视为本地构建产物，不提交本体；需要时在 fork `.gitignore` 增加窄规则。
- 已同步更新 `gettokens-domain-engineering` skill 的 CLIProxyAPI Fork Maintenance 段落，并新增 `docs-linhay/dev/20260519-cliproxyapi-upstream-sync-session-distillation.md`。本模式属于 CLIProxyAPI 领域维护流程，不升级到 `AGENTS.md`。

## Mac 状态栏更新入口 Space

- 新建 `docs-linhay/spaces/20260519-mac-menubar-app-update/`，用于推进“在 macOS 状态栏菜单中添加更新 App 入口”。
- 本期边界：复用既有 Sparkle 原生更新链路，在状态栏菜单新增“检查更新...”入口；不重新实现 appcast、签名、公证或自定义更新 UI。
- 初始计划记录在 `docs-linhay/spaces/20260519-mac-menubar-app-update/plans/20260519-mac-menubar-app-update-plan-v01.md`，当前状态为 `scheduled`；实现前需先确认状态栏菜单实现位置与 Wails/macOS 原生桥接边界。
- 已完成实现：macOS 顶部菜单栏新增 `Help -> Check for Updates...`，点击复用 root `App.CheckUpdate()`；设置页新增“启动与驻留”，支持开机启动、关闭窗口即退出或隐藏窗口继续运行服务；隐藏驻留时启动原生 macOS status item，菜单包含状态、打开窗口、检查更新和退出。
- 后端补齐 `AppRuntimeSettings` 存储、用户级 LaunchAgent 写入/删除、`OnBeforeClose` 生命周期处理、`internal/menubar` Objective-C bridge 与 Wails root binding；前端 Wails bindings 已由 `wails build` 生成。
- 验证通过：菜单模型红绿测试、应用生命周期 Go 测试、`go test ./...`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`./scripts/wails-cli.sh build`；System Events 确认本地构建进程菜单栏包含 `Help`，且 `Help` 菜单项包含 `Check for Updates...`。截图归档到 `docs-linhay/spaces/20260519-mac-menubar-app-update/screenshots/20260519/menubar/20260519-menubar-check-update-after-v01.png`。
- 补充核对：本需求不需要重新做真实 Sparkle 点击回归；设置页原有更新入口已能正常运行。代码链路已确认一致：设置页、顶部菜单栏和 status item 都调用 root `App.CheckUpdate()`，最终落到 `internal/wailsapp.CheckUpdate()`，由 `usesNativeUpdaterUI()` 决定 Sparkle 或 GitHub Release 检查分支。
- 本轮补充设置页验收：浏览器预览 `#frame=settings` 已确认“启动与驻留”块可见，关闭行为可切换预览状态；截图归档到 `docs-linhay/spaces/20260519-mac-menubar-app-update/screenshots/20260519/settings/`。移动端 375px 下需先收起现有 Sidebar 后阅读设置内容，这是当前 app shell 的既有布局约束。

## macOS 生命周期设置整理沉淀

- 本轮“整理”提炼出 macOS Runtime Residency 闭环：设置页涉及开机启动、关闭窗口是否退出、后台驻留或状态栏入口时，必须把前端开关、Wails root binding、`internal/wailsapp` runtime 行为、macOS native status item 和真实桌面验收一起处理。
- 关键规则：如果支持关闭主窗口但保留服务运行，必须同时提供 status item 或等价 native 恢复/退出入口；菜单至少覆盖服务状态、打开窗口、检查更新和退出，不能让服务无可见控制地留在后台。
- Wails 绑定边界已固化到 skill：`internal/wailsapp` 新增运行时 API 后，要同步 root `app.go` / `app_types.go` 并重新生成 `frontend/wailsjs`；浏览器预览只负责设置页布局和截图，不能替代 LaunchAgent、close behavior、status item 或 native updater 验收。
- 已新增 `docs-linhay/dev/20260519-macos-runtime-residency-session-distillation.md`，并更新 `.agents/skills/gettokens-ops-governance/SKILL.md`；该模式暂不升级 `AGENTS.md`，因为它属于 GetTokens Wails/macOS 桌面交付流程，而非全仓长期硬规则。
