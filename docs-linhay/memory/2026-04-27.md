# 2026-04-27

## Settings Release Page Open Fix
- `frontend/src/features/settings/SettingsFeature.tsx` 的手动更新流程已修复：`CheckUpdate` 返回的新版本信息不再丢失 `releaseUrl`，点击“打开发布页”会把完整 release 页面 URL 传给 `BrowserOpenURL`。
- 回归根因是前端在 `trackRequest(... mapSuccess ...)` 里把 `ReleaseInfo` 裁剪成只剩 `version` 和 `assetName`，导致手动更新场景实际拿到的是缺失 `releaseUrl` 的对象。
- 本轮验证通过：`npm run test:unit`、`npm run typecheck`。

## Account Rotation Modal
- 账号轮动设置已从 `StatusFeature` 主编辑区迁到账号池入口：`AccountsHeader` 新增“轮动设置”操作，打开 `AccountRotationModal` 后统一处理 relay routing config 编辑与账号顺序调整。
- 边界更新：账号主列表 `frontend/src/features/accounts/AccountsFeature.tsx:97` 的现有展示逻辑不参与拖动排序；统一排序只在 modal 中处理。
- OAuth `auth-file` 账号已纳入统一轮动：GetTokens 现在会读取并写回 auth file 顶层 `priority`，并通过统一的 `UpdateAccountPriority` 同时覆盖 `auth-file` 与 `api-key` 资产。
- 重新登录回填链路已保留旧 `priority`，避免 OAuth 账号 reauth 后掉出既有轮动顺序。
- 状态页中的轮动区已退回只读展示，避免继续把 `StatusFeature` 扩成配置中心。
- 新增后端 priority 读写测试与前端拖动纯函数测试；本轮验证通过：`go test ./internal/accounts ./internal/wailsapp`、`npm run test:unit`、`npm run typecheck`、`npm run build`。
