# 2026-06-02

## Codex model_catalog_json 投影方案

- 参考 `docs-linhay/references/cc-switch` 后形成 GetTokens 方案：不把 `model_catalog_json` 作为账号或路由真源，只作为本地 Codex `/model` 展示的 GetTokens-owned projection。
- 推荐生成 `~/.codex/gettokens-model-catalog.json`，并在本地 Codex apply 流程中写入顶层 `model_catalog_json`；生成内容必须是完整 relay catalog，避免静态 catalog 替换后隐藏 GPT 内置模型。
- 方案文档已落位：`docs-linhay/dev/20260602-codex-model-catalog-projection-plan.md`。

## Codex /model 投影实施

- 已实现 GetTokens-owned Codex model catalog projection：`ApplyRelayServiceConfigToLocalV2` 可在 Status 页本地 Codex apply 时写 `CODEX_HOME/gettokens-model-catalog.json` 并设置顶层 `model_catalog_json`。
- 默认落点是 Status 页 Codex 本地应用区域，提供单一同步开关；打开开关会立即写入 GetTokens-owned `model_catalog_json` pointer，关闭开关会立即移除该 pointer。退出 app 不自动取消关联，避免隐式改写用户 Codex 配置。
- 外部 `model_catalog_json` 默认不覆盖；后端返回 `existingExternalModelCatalogPath`，前端展示保留外部指针。
- 账号卡 direct upstream / OAuth apply 保持既有语义，本轮不默认投影 GetTokens catalog。
- 已通过 `go test ./... -count=1`、`npm --prefix frontend run typecheck`、前端全量 `test:unit`。真实 Codex CLI `/model -> All models` 冒烟仍待使用隔离 `CODEX_HOME` 补跑。

## Codex config 分层展示与控件修正

- Codex 功能开关页 root / model provider 配置行改为“一级选项 + 多级子选项”展示，避免把 `model_providers.gettokens.env_key_instructions` 这类完整 path 平铺成单独大标题。
- 分层展示规则细化为：一级 path 只在 provider/group 头或 root 子分组头出现一次，配置行内只显示缩进后的子路径，避免 `openai/base_url/name/...` 每行重复 provider 标题。
- `marketplaces` 与 `plugins` root TOML table 现在在前端展开为 leaf 行，例如 `plugins -> browser@openai-bundled -> enabled`；后端允许这两个 root table 的 leaf path 保存，并按 TOML dotted key 正确读写 `[plugins."browser@openai-bundled"]`。
- Model provider 字段描述已补齐 i18n，`base_url/name/wire_api/auth/aws/env_http_headers/http_headers/query_params/env_key_instructions` 等字段不再回退显示后端英文 `Model provider ... field ...`。
- 右侧编辑控件使用响应式宽列并右对齐；`true/false` 字符串或 bool-only enum 使用 toggle，普通枚举使用 segmented control，页面根支持文本选择复制。
- 已通过 focused node tests、`npm run typecheck`、`go test ./internal/wailsapp -run 'Test.*CodexFeatureConfig'`，并用无头 Playwright 验证 `#frame=codex` 文本可选择、控件右对齐和 model provider 层级展示。

## 账号 quota 耗尽后未切换排查

- 2026-06-02 11:03 正式版 `/v1/responses` 最新错误是 `auth_unavailable: no auth available (providers=codex, model=gpt-5.5)`，不是某个上游账号返回 quota 错误后继续失败。
- 正式版 quota runtime 中 `acct_fd804b01-4695-476f-8c7c-849951533303` 已写入 `quota-empty`，说明 sidecar route guard 对 fresh quota empty 能生效；但该账号同时已禁用，不能作为下一个候选。
- 当前正式版和 dev sidecar config 都把 `request-retry`、`max-retry-credentials`、`max-retry-interval` 写成 0；这会禁止同一次请求失败后的凭证重试，解释“当前请求不会马上换下一个账号”。
- 代码风险仍存在：`quotaEmptyRouteGuardBlockForResult` 只有在上游错误携带正数 `RetryAfter` 时才写 `quota-empty`；WebSocket pinned auth 释放只依赖 route guard，因此没有 reset hint 的 quota 错误可能只进入普通 cooldown，不一定触发 pinned auth 切换。
- 进一步排查 balanced 不生效：正式版 profile 目录 `~/.config/gettokens/channel-routing/` 为空，balanced 配置还停留在旧路径 `~/.config/gettokens-data/channel-routing/config.json`；当前 sidecar 按 profile config dir 查找 routing 文件，因此运行时未加载 balanced，回退到 `routing.strategy: fill-first`，所有 live sessions 都命中同一个 `公司 1` runtime auth。
- 已补启动迁移：prod profile 缺少 `channel-routing/config.json` 时从旧 `gettokens-data/channel-routing/config.json` 迁移；dev profile 不迁移旧 prod 配置，避免 dev/prod 账号 ID 混用。当前机器已手动复制到 `~/.config/gettokens/channel-routing/config.json`。
- 已继续修复 retry 默认值：sidecar 新配置默认写 `request-retry=3`、`max-retry-credentials=0`、`max-retry-interval=30`；旧配置三项全 0 且未迁移过时会一次性修复为 `3/0/30`，并写 profile marker，之后用户显式改回 0 不会被启动反复覆盖。
- 当前正式版与 dev sidecar 配置已通过 management API 更新为 `request-retry=3`、`max-retry-interval=30`；账号轮动 UI 默认值文案同步改为 `requestRetry=3`、`maxRetryInterval=30`。

## 文档治理规则调整

- 用户明确要求移除讨论归档目录与旧索引命令相关内容；当前治理规则已改为：新建 `space` 只生成 `README.md`、`plans/`、`screenshots/`，文档校验不再检查讨论归档目录，memory 写回不再要求执行旧索引同步。
- 已同步 `AGENTS.md`、`docs-linhay/README.md`、`docs-linhay/scripts/create-space.sh`、`docs-linhay/scripts/check-docs.sh`、相关项目 skills、`.codex/agents` 职责描述和 subagent 治理文档。
