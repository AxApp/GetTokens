# 2026-04-30

## 代理池页面边界收口
- `20260429-network-proxy` space 下的“代理池管理”设计稿已明确收口为纯客户端维护语义。
- 本期页面中的代理节点列表、添加、删除、分组、排序、最近检测结果都按本地状态表达，不假设后端已存在代理池实体。
- 当前后端只具备全局 `proxy-url`、账号/凭证级 `proxy-url` 与通用 `api-call` 探测能力，不具备代理池节点 CRUD、节点列表查询、节点级健康状态存储与延时采样接口。

## 调试面板卡顿修复
- `frontend/src/pages/DebugPage.tsx` 已改为默认折叠调试记录，首次进入页面时不再同步格式化每条 request/response 正文。
- 新增 `frontend/src/pages/debugPageModel.ts` 与 `debugPageModel.test.mjs`，把 payload 序列化下沉到“展开单条记录”时触发，用回归测试锁定该行为。
- 当前 `cd frontend && npm run test:unit`、`cd frontend && npm run typecheck` 已通过，未发现本次调试页改动新增的类型错误。
- 为了做纯浏览器回归，`frontend/src/context/DebugContext.tsx` 新增了仅开发环境生效的 `debug:inject-entries` 自定义事件入口，可在浏览器里注入 mock 调试记录，不影响生产逻辑。
- 浏览器自动化脚本 `docs-linhay/scripts/debug-page-browser-check.mjs` 已验证：从 `proxy-pool` 页点击进入调试面板，在注入 300 条大 payload 记录后，进入调试页并看到折叠态列表耗时约 `203ms`，随后可正常展开首条详情；截图产物为 `output/playwright/debug-page-browser-check.png`。

## 代理池前端落地
- `frontend/src/features/proxy-pool/` 已落地真实页面实现，`代理池` 作为与 `账号池` 同级的一级导航页接入应用。
- 当前实现严格收口为纯客户端维护：节点列表、筛选、搜索、分组切换、删除、复测与批量复测都只作用于本地状态，并写入 `localStorage`。
- `frontend/src/utils/pagePersistence.ts` 与 `Sidebar.tsx` 已补齐 `proxy-pool` 页面持久化和导航接入，保留现有 `账号池 / 用量分析` 结构不扩展到其他未完成页面。
- 验证结果：`npm --prefix frontend run typecheck` 通过，`npm --prefix frontend run test:unit` 通过，包含新增的 `src/features/proxy-pool/model.test.mjs`。

## 代理池交互补全
- `frontend/src/features/proxy-pool/ProxyPoolFeature.tsx` 已把 `新增代理 / 编辑 / 导入列表 / 导出` 从占位按钮补成真实交互，仍保持纯客户端维护。
- 新增和编辑通过本地弹层表单完成；导入支持粘贴 JSON 和读取本地 `.json` 文件；导出会下载当前代理池 JSON。
- `frontend/src/features/proxy-pool/model.ts` 补充了草稿到节点的校验构建、导入解析、导出文件名生成、导入合并覆盖等本地数据能力。
- 新增单测覆盖：新增节点、编辑节点、导入解析、导入合并、导出文件名生成。回归结果：`node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit` 全部通过。

## 代理池批量维护补强
- 代理池表格已支持多选、全选当前筛选、清空选择、导出选中、批量删除，页面从单条操作提升为可批量维护。
- 导入格式已从仅支持 JSON 扩展为同时支持逐行 `scheme://host:port` 或 `host:port` 的纯文本输入，默认按本地节点导入并建议后续复测。
- `frontend/src/features/proxy-pool/model.test.mjs` 已新增文本导入解析测试；本轮回归仍通过 `node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit`。

## 代理池工作台化增强
- 代理池页面已新增分组筛选和排序切换，当前支持按延时、可用率、最近检测、分组、名称排序。
- 选中态批量动作已继续补强，支持 `复测选中` 和 `选中移到观察组`，配合前一轮的导出选中、批量删除形成完整的选中项工作流。
- 数据层 `frontend/src/features/proxy-pool/model.ts` 已新增分组过滤、节点排序、显式设组等能力；`model.test.mjs` 已新增对应测试。
- 回归结果：`node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit` 全部通过。

## 代理池表格交互收尾
- 代理池表格已从工具条排序切换为列头点击排序，`节点 / 分组 / 延时 / 可用率 / 最近检测` 支持升降序切换，更贴近现有工作台表格交互。
- 选中项移组已从固定“移到观察组”改成“选择目标分组后批量移动”，避免批量维护时反复进入单行操作。
- `frontend/src/features/proxy-pool/model.ts` 新增默认排序方向和排序切换逻辑，`frontend/src/features/proxy-pool/model.test.mjs` 已补对应单测。
- 回归结果：`node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit` 全部通过。

## 代理池分页补强
- 代理池主表格已新增分页与每页条数切换，当前支持 `10 / 20 / 50` 条每页，并在底部展示页码与筛选后总数。
- 为了和分页行为一致，表格批量选择新增“全选当前页”，保留“全选当前筛选”作为跨页操作入口。
- `frontend/src/features/proxy-pool/model.ts` 已新增分页切片与页码钳制逻辑，`frontend/src/features/proxy-pool/model.test.mjs` 已补对应测试。
- 回归结果：`node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit` 全部通过。

## 代理池接入环境代理与真实检测
- 代理池页曾短暂接过“导入环境代理”入口；后续产品范围收口后，这条链路已整体移除，页面不再从当前进程里的 `https_proxy / http_proxy / all_proxy` 注入本地节点。
- 行内 `复测`、`复测选中`、`批量复测` 与筛选结果复测已切换为真实代理探测，请求通过节点自身代理访问 `https://example.com`，回填真实延时、检测时间与成功/失败状态。
- `internal/wailsapp/proxy_pool.go` 新增了环境代理读取与代理探测接口；其中 `http/https/socks5/socks5h` 都可用于真实探测。
- 回归结果：`go test ./internal/wailsapp/...`、`node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit` 全部通过。

## 代理池默认假数据移除
- 代理池页面已不再以内置种子节点作为默认首屏数据；无真实来源时直接显示空表格和空态反馈。
- `frontend/src/features/proxy-pool/model.ts` 的本地读取逻辑已改为默认返回空列表，并会自动识别旧版本写入的那批演示节点 `localStorage` 数据后丢弃，避免页面继续显示历史假数据。
- 回归结果：`node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run test:unit` 全部通过。

## 代理池按标准 Wails 绑定收尾
- 代理池的真实测速桥接仍通过根层 `package main` 的 `App` 暴露 `ProbeProxyNode`；环境变量导入对应的 `ListEnvironmentProxyEntries` 后续已随产品收口一并移除。
- 当前前端已经恢复为直接使用 `frontend/wailsjs/go/main/App` 的生成绑定调用，不再保留动态 `window.go.main.App` 兜底逻辑。
- 回归结果：`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`go test ./...` 全部通过，`./scripts/wails-cli.sh dev` 也能正常完成 bindings 生成和前端构建。

## 代理池协议维度去重修复
- 代理池节点 ID 生成逻辑已把 `protocol` 纳入身份键，修复了 `http://127.0.0.1:7897` 与 `socks5://127.0.0.1:7897` 被视为同一条并发生覆盖的问题。
- `frontend/src/features/proxy-pool/model.test.mjs` 已新增同 host/port 不同协议保留两条的测试，同时用浏览器在页面里通过“导入列表”实测确认两条都能出现在表格中。
- 回归结果：`node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 全部通过。
- Codex 通过 `openai_base_url` 接 GetTokens relay 时，当前 relay 旧版 `/v1/models` 只返回 OpenAI 标准 `data[]` 结构，不返回 Codex 需要的 `models[] + rich metadata` 结构，因此 Codex 无法用 relay 正常刷新模型目录。
- 已在 `docs-linhay/references/CLIProxyAPI/sdk/api/handlers/openai/openai_handlers.go` 增加 Codex 兼容分支：当 `/v1/models` 请求带 `client_version`（或 Codex UA）时，返回 Codex 可解析的 `models[]` 目录；普通 OpenAI 客户端仍保持原始 `data[]` 响应。
- 验证结果：`go test ./sdk/api/handlers/openai -run 'TestOpenAIModels|TestOpenAIResponsesCompact'` 通过；本机另外起了修复版 sidecar `http://127.0.0.1:18417/v1`，并已把 `~/.codex/config.toml` 指向该端口，重开 Codex 后即可走兼容的 `/models`。
- GetTokens 的“一键配置到本地 Codex”最终没有固定切成 `gettokens` provider，而是改成“用户可选 provider，默认保留 `openai` 连续性”：选 `openai` 时继续写 `openai_base_url = ...`，选自定义 provider 时才写 `model_provider = "<id>"` + `[model_providers.<id>]`，避免把既有 Codex 会话搜索和 resume picker 从 `openai` 静默切裂到 `gettokens`。
- 同一轮修复还补齐了状态页本地工作台配置的三点一致性：前端选中的 relay `model` 会真实透传给 `ApplyRelayServiceConfigToLocal` 并写入 `~/.codex/config.toml`，不再固定为 `gpt-5.4`；`model_reasoning_effort` 也已加入同一条本地落盘链路，并限制为 Codex 支持的 `none/minimal/low/medium/high/xhigh` 枚举；provider 则和 model 一样支持本地新增/删除，用于决定本次落盘是“保持 `openai` 连续性”还是“显式创建 custom provider”。

## 代理池订阅导入与来源标签
- 代理池已补上最小“订阅管理”能力：后端新增 `FetchProxySubscription`，只负责拉取 `http/https` 纯文本订阅；前端新增“导入订阅”弹层，导入后会把 `sourceLabel / sourceURL` 写入每条代理节点，并在主表新增“来源”列展示。
- 订阅内容目前按逐行 `scheme://host:port` 或 `host:port` 解析；像 `https://raw.githubusercontent.com/hookzof/socks5_list/master/proxy.txt` 这类源会默认推导来源标签为 `hookzof/socks5_list`，也允许用户手工覆盖。
- 模型层已补测试覆盖：订阅导入后保留来源标签、来源链接，以及同 host/port 不同协议仍保留为两条。
- 回归结果：
  - 通过：`node --test frontend/src/features/proxy-pool/model.test.mjs`
  - 通过：`go test ./...`
  - 通过：`npm --prefix frontend run typecheck`
  - 通过：Wails 绑定生成后，`frontend/wailsjs/go/main/App.js` 与 `App.d.ts` 已包含 `FetchProxySubscription`
  - 受既有基线阻塞：`npm --prefix frontend run test:unit` 仍有 `src/utils/pagePersistence.test.mjs` 针对 `vendor-status` 的 4 条失败，不是本轮代理池改动引入

## 代理池订阅源管理补全
- 订阅不再只是“一次性导入动作”，前端现在会把订阅源本地保存为独立记录，字段包含 `url / label / lastSyncedAt / lastImportCount / lastError`。
- 页面新增“订阅源”管理弹层，支持单源刷新、刷新全部、只删订阅源、删订阅源并清理其对应节点。
- 节点清理规则按 `sourceLabel / sourceURL` 双条件匹配，避免误删手工维护或来自其他订阅的节点。
- 本轮验证结果：
  - 通过：`node --test frontend/src/features/proxy-pool/model.test.mjs`
  - 通过：`npm --prefix frontend run typecheck`
  - 通过：`npm --prefix frontend run build`
  - 通过：`go test ./...`
  - 通过：`./scripts/wails-cli.sh dev` 已完成 bindings、前端构建和应用编译；当前仅因 `127.0.0.1:34115` 已被占用而无法重复监听，不是构建失败
  - 仍有既有基线失败：`npm --prefix frontend run test:unit` 里 `src/utils/pagePersistence.test.mjs` 的 4 条 `vendor-status` 相关测试

## 代理池分层口径澄清
- 已明确修正表述：代理池与订阅管理不是“做服务端”或“后端化”，而是继续完善 GetTokens 的 APP 层能力。
- 当前正确分层是：
  - APP 层负责代理池节点、订阅源、本地持久化、刷新策略、批量操作和检测结果回填。
  - Wails / Go bridge 只负责前端不方便直接做的能力，例如远程订阅拉取、真实代理探测、系统环境访问。
  - sidecar / 上游只负责在真实请求阶段消费某个代理 URL，不承载代理池产品语义。

## 代理池工具条减噪
- 用户指出 `ProxyPoolFeature` 顶部按钮过多后，已把常态工具条重构为“新增代理 + 更多操作”两级结构。
- `导入列表 / 导入订阅 / 订阅源 / 导出全部 / 复测筛选结果` 已收进 `更多操作` 菜单；后续“导入环境代理”入口已被删除，不再保留在菜单中。
- 批量动作改成仅在有选中节点时显示，未选中时只保留简短提示，避免页面常态下同时暴露十多个按钮。
- 验证结果：`npm --prefix frontend run typecheck`、`npm --prefix frontend run build` 通过。

## Session Management 设计稿重做
- `20260429-nolon-session-management/design-preview.html` 已从偏暖色文档册式稿件重做为完整 app shell 截图态，只保留一张真实页面，不再展示模块比较或概念分镜。
- 新稿明确贴齐 GetTokens 当前语言：白底工作台、黑色硬边框、红色点缀、斜体大标题、左侧真实导航骨架和右侧单主画布。
- 页面主次也重新收口：左侧是 `Session List`，右侧只承接 `Selected Detail + Rewrite Preview`，避免再次滑回“模块册”或“控制台概念稿”。
- 随后又继续收掉密度过高的问题，当前版本进一步减少了次级模块和说明区，只保留 `顶部状态 + 左列表 + 右详情` 三段主结构，明确朝更安静的真实页面态收敛。
- 之后又按用户明确裁定把结构进一步改成：
  - 左栏 `项目列表`
  - 右栏 `项目会话` 列表
  - 点击会话后弹出 `会话文件弹层`
  - 弹层主体是“一条消息一行”的 `消息列表`
- 弹层内容已替换为来自本机真实 rollout 文件的脱敏样本，去掉了路径、id、源码定位和环境细节，不再使用虚构对话占位。
- 当前该 space 已补充交接文档 `plans/20260430-nolon-session-management-design-handoff-v01.md`，状态收口为 `research-complete-design-handoff-ready`，可直接交给后续执行者。
- 本轮没有新增稳定到 repo-wide 的治理规则，也没有发现必须新建项目级 skill 的新模式，因此未更新 `AGENTS.md` 或新增 skill。

## Session Management 第一页开始接入工程
- 用户已明确批准从 design handoff 进入工程实现，并要求以 `gettokens-subagent-supervision` 模式监督到闭环。
- 当前前端已接入 `session-management` 一级导航和第一页静态骨架：左栏项目列表、右栏项目会话列表、列表头 `全部 / 活跃 / 已归档` 筛选，以及会话详情弹层。
- 详情弹层主体保持为“一条消息一行”的消息列表，其余信息压缩到顶部摘要区；当前内容仍使用本地 mock 数据和脱敏样本结构。
- `frontend/src/utils/pagePersistence.ts` 与对应测试已补齐 `session-management` 页面级持久化与 hash 解析，当前不引入额外 workspace 分叉。
- 当前边界仍然明确：不接真实 session 数据、不接真实 rewrite 执行链、不进入 `provider groups` 第二页。

## Session Management 第一页接入真实 rollout 数据
- `session-management` 第一页已从静态骨架推进到真实数据实现：左栏项目列表、右栏项目会话列表、会话详情弹层均通过 Wails bridge 读取本机 `CODEX_HOME/sessions` 与 `archived_sessions`。
- 后端新增 `GetCodexSessionManagementSnapshot`、`RefreshCodexSessionManagementSnapshot`、`GetCodexSessionDetail`，会按项目聚合真实 session 文件，并在详情层返回脱敏后的消息列表。
- 脱敏边界继续保持：绝对路径替换为占位标记，长 system/developer 提示压缩处理，不把完整机器上下文和大段敏感提示直接暴露到前端。
- 前端已从 mock 数据切换为运行时 bridge 读取；项目会话筛选、详情弹层加载/错误/关闭与消息列表渲染均已接通真实返回。
- 本轮整体验证已通过：`go test ./...`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`。
- 当前仍明确未做：真实 rewrite 执行链、`provider groups` 第二页、会话级批量操作。

## Session Management 浏览器预览与截图验收
- `session-management` 已补一个明确的浏览器预览入口：`?preview=session-management#frame=session-management`，在没有 Wails bridge 的环境下会回退到稳定的预览样本，不影响桌面真实数据链路。
- 预览态支持 `detail=session-gettokens-01` 这类 query，能够直接打开会话详情弹层，便于截图和对稿，不需要再做浏览器点击编排。
- 新增脚本 `docs-linhay/scripts/session-management-browser-check.mjs`，当前通过 Chrome headless 产出两张基线截图：
  - `docs-linhay/spaces/20260429-nolon-session-management/screenshots/20260430/session-management/20260430-session-management-project-sessions-page-baseline-v01.png`
  - `docs-linhay/spaces/20260429-nolon-session-management/screenshots/20260430/session-management/20260430-session-management-session-detail-modal-baseline-v01.png`
- 本轮补充验证已通过：`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`，截图脚本也已成功产出文件。

## Session Management localhost 开发态真实数据回退
- 为了让 `http://localhost:<vite-port>/#frame=session-management` 在普通浏览器里也能看到真实 rollout 数据，当前已新增一条 dev-only HTTP 回退链路。
- `frontend/vite.config.js` 现在挂载了 `/__dev/session-management/snapshot` 和 `/__dev/session-management/detail` 两个本地接口，实际扫描本机 `CODEX_HOME/sessions` 与 `archived_sessions`。
- `frontend/src/features/session-management/api.ts` 在“无 Wails runtime、但运行在 localhost/127.0.0.1”时会自动改走这组 HTTP 接口，不再直接报缺少 `window.go.main.App`。
- 当前实测开发态返回的是本机真实数据，不是预览假数据：一次 smoke check 已拿到 `projectCount = 99`、`sessionCount = 2853`，详情接口也能返回真实脱敏消息列表。
- 本轮验证已通过：`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`。

## Relay Key 元数据独立挂钩
- 状态页 relay key 列表已确认上游事实源只有 `/v0/management/api-keys` 的纯 `string[]`，不直接提供 `createdAt / lastUsedAt`。
- 本轮采用独立挂钩方案：保持上游 relay key 协议不变，在本地 `~/.config/gettokens-data/relay-service-api-key-metadata.json` 旁路维护 metadata，并通过 Wails `RelayServiceConfig.apiKeyItems` 回传给前端。
- `createdAt` 在首次观察到 key 或首次本地生成 metadata 时写入；`lastUsedAt` 在执行 `ApplyRelayServiceConfigToLocal` 成功后写入。
- 这样后续即使上游 management API 升级出原生时间字段，也只需要替换 metadata 读写来源，不需要重做状态页的列表展示结构。
- 回归结果：`go test ./internal/wailsapp/...`、`npm --prefix frontend run typecheck` 通过。

## OpenAI 厂商状态页落地
- 已确认 `status` 继续代表本地 relay / sidecar / runtime 状态，外部厂商状态单独落在新的一级菜单 `vendor-status`。
- OpenAI 状态页的数据策略固定为“RSS 管事件、JSON 管状态”：顶部事故卡和历史事件流优先用 `feed.rss`，`System status` 与 uptime 条带用 `proxy/status.openai.com` 和 `component_impacts`。
- 由于 `https://status.openai.com/feed.rss` 不提供浏览器所需的 CORS 头，当前通过 `app.go` 新增 `FetchVendorStatusRSS` 由 Wails 运行时抓取 RSS，再把 XML 交给前端解析。
- 前端真实实现已接入 `vendor-status` 导航、hash/page 持久化、本地化文案以及 `frontend/src/features/vendor-status/` 页面模块。
- 后续又补了一轮产品态收口：`vendor-status` 静态标题和说明文案已接入 i18n，同时在无 Wails bridge 的浏览器预览态下不再直接调用 `window.go.main.App`，而是跳过 RSS、优雅回退到 JSON 侧展示，避免预览态同步报错。
- 为了把截图和浏览器验收固定下来，当前又新增了正式 preview 能力：`?preview=vendor-status` 会走 `frontend/src/features/vendor-status/previewData.ts` 的静态数据，`frontend/src/utils/previewMode.ts`、`App.tsx` 与 `DebugContext.tsx` 会避开无 Wails runtime 的初始化路径。
- 验收截图已通过 `docs-linhay/scripts/vendor-status-browser-check.mjs` 产出，路径为 `docs-linhay/spaces/20260430-openai-status-page/screenshots/20260430/openai-status-page/20260430-openai-status-page-vendor-status-web-baseline-v01.png` 和 `20260430-openai-status-page-vendor-status-mobile-baseline-v01.png`。
- 最新一轮视觉收口里，`System status` 展开态已改成更接近官方状态站的“组件逐行明细”：点击 `12 components` 后按子组件显示状态图标、组件名、右侧 uptime 和独立条带；同时补上 `localStorage` 五分钟缓存来源标识、官方站/历史页跳转，并顺手修复了仓内会阻塞全局前端 `typecheck` 的 `session-management` 与 `proxy-pool` 现有类型断点。
- 后续又继续精修了一轮展开态结构：父级 group 在展开后不再同时保留自己的 uptime 条带，而是直接让位给子组件列表，避免“父级条带 + 子组件条带”双层信息重复；同时下调了组标题和 uptime 的字重，把阅读重心压回到彩色状态条本身。对应桌面/移动基线截图已在原路径覆盖更新。
- 当前回归结果：`node --test frontend/src/utils/pagePersistence.test.mjs`、`node --test frontend/src/features/vendor-status/model.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`go test . ./internal/...`、`./scripts/wails-cli.sh build` 全部通过。

## Usage Desk 本地 usage 按模型统计
- 本地 projected usage 原始 `jsonl` 只有 `turn_context.payload.model` 带模型名，`event_msg.token_count` 本身不带模型；此前落到 sqlite 分钟索引后模型字段丢失，导致 Usage Desk 无法回答“每条本地记录是什么模型”。
- 本轮已把本地 usage 聚合粒度从“按分钟”补到“按分钟 + 模型”：后端 sqlite 改为新分钟表 `session_usage_minutes_v2`，每条 `LocalProjectedUsageDetail` 会带回 `model`，同一分钟不同模型会拆成多条明细。
- 前端 Usage Desk projected 明细表已新增“模型”列，分钟图仍保持按分钟总量汇总，不改变现有趋势图语义。
- 针对这次修复已补验证：`go test ./internal/wailsapp -run 'TestGetCodexLocalUsage(AggregatesTokenCountDeltas|SplitsSameMinuteByModel)$'` 与 `node --test frontend/src/features/accounts/tests/usageDesk.test.mjs` 通过。
- 后续又按交互规则收口展示：无论日级还是分钟级，表格仍保持“一桶一行”；模型列展示该时间桶用量最大的模型，若该桶还有其他模型参与，则显示为 `<dominant-model>,*`。
- 当前前端门禁状态已恢复：`npm --prefix frontend run typecheck` 通过；此前阻塞它的 `session-management` 文案与签名问题已一并清掉。
- 这轮“整理”没有继续新增项目级 skill 或升级 `AGENTS.md`：因为沉淀出来的是 `Usage Desk` 的产品/实现规则，不是跨任务复用的交付流程。对应知识已单独落到 `docs-linhay/dev/20260430-usage-desk-model-rollup-rule.md`。

## Proxy Pool 测速网址配置与历史
- 代理池顶部搜索轨道已补“测速网址”配置输入，支持指定 `http/https` 测速站点，不再固定打到默认站点。
- 单条测速、批量测速和选中测速共用同一份 `probeTargetURL`，测速前会先做 URL 归一化与合法性校验。
- 已配置并使用过的测速网址会写入本地历史，默认只保留最近 5 条；页面同时提供 datalist 和历史快捷按钮，便于重复切换常用测速目标。
- 本轮验证已通过：`node --test frontend/src/features/proxy-pool/model.test.mjs`、`npm --prefix frontend run build`；`typecheck` 也已重新回归，无新增代理池类型错误。

## Skills 整理
- 本轮把真正重复出现且可复用的交付模式沉淀进 `.agents/skills/gettokens-ops-governance/SKILL.md`，没有继续扩 `AGENTS.md`。
- 新增的稳定模式有两条：
  1. `Wails Browser Preview & Screenshot Loop`：针对 `vendor-status` 和 `session-management` 这类桌面页，先补 `?preview=` 或 dev-only HTTP bridge，再用 `docs-linhay/scripts/*-browser-check.mjs` 产稳定截图，不把浏览器验收和真实 Wails 验收混为一谈。
  2. `Cleanup Before Claiming Done`：长会话下如果提交后仍有同轮 tracked diff，主控 agent 需要继续追到只剩参考仓、临时研究文件和本地非版本化产物，不在“主提交已落”时过早停止。
- 这次没有升级到 AGENTS 的原因是：上述动作属于 GetTokens 项目级执行技巧，复用范围明确，但还没有必要上升成所有任务都必须遵守的 repo-wide 硬规则。

## Status 页本地 Codex 模型目录改为账号池聚合来源
- `StatusFeature` 的“应用到本地 Codex”区块不再只依赖本地静态 model 列表；当前已新增 `ListRelaySupportedModels()`，由后端聚合 openai-compatible provider 本地模型、远端 `/models` 返回，以及 codex api key 本地模型。
- `internal/wailsapp/openai_compatible.go` 的 `/models` 解析已补齐 Codex 目录形状兼容：除 OpenAI `data[]` 外，也能识别 `models[] / slug / supported_reasoning_levels / default_reasoning_level`。
- 状态页当前会优先用账号池聚合目录渲染 `model` 选项，并按当前模型的 `supportedReasoningEfforts` 动态收窄 `model_reasoning_effort`；只有模型没有元数据时，才回退到 Codex 通用枚举 `none/minimal/low/medium/high/xhigh`。
- 本地手工新增 model 仍保留，但只作为账号池目录之外的补充；删除最后一个本地自定义 model 也已允许，只要账号池目录本身仍非空即可。
- 本轮验证已通过：`go test ./internal/wailsapp -run 'TestFetchOpenAICompatibleProviderModels|TestParseOpenAICompatibleModelsResponseSupportsCodexModelCatalogShape|TestListRelaySupportedModelsAggregatesRemoteLocalAndCodexKeyModels|TestApplyRelayServiceConfigToLocal'`、`go test .`、`node --test frontend/src/features/status/tests/relayModelCatalog.test.mjs frontend/src/features/accounts/tests/accountConfig.test.mjs`、`npm --prefix frontend run typecheck`。

## 一键应用到本地改成保留式写入
- 已确认旧版 `ApplyRelayServiceConfigToLocal` 会直接覆盖 `~/.codex/auth.json` 和 `~/.codex/config.toml`，会抹掉用户额外配置项，这对真实用户配置不安全。
- 当前实现已改成 merge 写入：
  - `config.toml` 走文本级最小补丁，只更新 `model / model_reasoning_effort / openai_base_url / model_provider / [model_providers.<id>]` 这些 GetTokens 负责的键位，尽量保持原有键顺序、无关 section 和注释不动。
  - `auth.json` 走字段合并，只更新 `auth_mode = apikey` 和 `OPENAI_API_KEY`，保留其他未知字段。
- 若现有 `auth.json` 不是有效 JSON，当前会直接报错停止，避免把坏文件静默覆盖成新文件。
- 新增回归测试已覆盖：
  - 保留现有 root key 顺序和 section
  - 保留 provider section 里的额外字段
  - 保留 auth.json 里的既有字段

## Session Management 浏览器开发态取数修复
- 已确认 `http://localhost:34115/#frame=session-management` 首屏只有壳没有数据，不是列表组件本身坏掉，而是浏览器开发态取数链路不稳定。
- 根因分三层：`34115` 当前 origin 不承接 `__dev/session-management/*`；前端最初只回退到错误端口 `4173`；即使回退端口修正后，跨域和首个无效候选请求的无超时等待仍会让页面长期停在“加载中”。
- 当前已收口的修复包括：`session-management` 浏览器态改为优先尝试真实 Vite dev 端口候选 `5173/4173/5174/4174`；跨域 dev bridge 已补 `Access-Control-Allow-Origin: *`；每个候选请求已加快速超时；前端新增 `localStorage` snapshot 缓存；dev bridge 新增内存缓存和磁盘缓存预热，避免每次都重新顺序等待全量 rollout 扫描。
- 在重启 `wails dev` 后已完成真实页面自测：`34115` 页面正文可见 `99 个项目 / 2860 条会话`，项目列表和项目会话列表均已渲染，不再是空壳等待。
- 本轮验证已覆盖：`node --test frontend/src/features/session-management/model.test.mjs src/features/session-management/cache.test.mjs`、`npm --prefix frontend run test:unit`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`，并通过 `agent-browser` 在 `34115` 页面实测可见真实项目与会话数据。

## Session Management 对齐 Usage 的 App 内缓存语义
- `session-management` 现在不再把“页面刷新”解释成“重新扫描所有 rollout 文件”。语义已经对齐 `Usage Desk`：`GetCodexSessionManagementSnapshot()` 优先返回 app 进程内缓存，只有 `RefreshCodexSessionManagementSnapshot()` 才强制重扫。
- 对应后端已在 `internal/wailsapp` 增加 `sessionMgmt` 运行时缓存状态、并发等待和 snapshot clone 逻辑；前端挂载时也取消了“有本地快照就自动 refresh”的行为，改为始终先走 `Get...Snapshot()`。
- 当前结果是：同一个 Wails dev / app 进程里，页面刷新、重新进入 `session-management`、切换回来都不再重新触发扫描；只有点击页面上的“刷新”按钮才会重扫本机 session 文件。
- 本轮验证已通过：`go test ./internal/wailsapp -run 'TestGetCodexSessionManagementSnapshot(GroupsProjectsAndStatuses|UsesAppMemoryCacheUntilExplicitRefresh)|TestGetCodexSessionDetailMasksSensitiveTextAndKeepsMessageRows'`、`node --test frontend/src/features/session-management/model.test.mjs src/features/session-management/cache.test.mjs`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`。

## Session Management 完整会话行集与 localhost 分流修复
- `session-management` 详情弹层不再只显示 `user / assistant / system` 三类 message。当前已把真实 rollout 中的 `event_msg:user_message / agent_message / agent_reasoning`、`response_item:reasoning`、`function_call / custom_tool_call / web_search_call`、`function_call_output / custom_tool_call_output` 一并并回消息列表。
- 前端角色集合已扩成 `system / user / assistant / reasoning / tool_call / tool_result / event`，顶部“当前消息”和角色汇总也同步改为这套完整标签。
- 浏览器 dev 回退链 `frontend/dev/sessionManagementDevData.js` 现在与 Go 解析保持同语义，不再只抽 `response_item.message`。
- 另外修了一个 `34115` 浏览器开发页特有的分流问题：只要运行在 `localhost/127.0.0.1`，`session-management` 现在优先走带缓存的 dev HTTP bridge，而不是因为存在 `window.go` 就误走 Wails runtime 全量扫描链。
- 验证结果：
  - `go test ./internal/wailsapp -run 'TestGetCodexSessionManagementSnapshot|TestGetCodexSessionDetail'` 通过
  - `node --test frontend/src/features/session-management/model.test.mjs frontend/src/features/session-management/cache.test.mjs` 通过
  - `npm --prefix frontend run typecheck` 通过
  - `npm --prefix frontend run build` 通过
  - 真实 dev bridge 详情抽查已能看到 `system / event / user / reasoning / tool_call / tool_result / assistant` 混合行集
- 仍有一个独立的开发态稳定性问题：`wails dev` 的 `34115` 壳子在本轮自测时出现过端口占用/掉线抖动，但这不影响当前代码路径本身和 dev bridge 取数结果。
