# 2026-06-16

## 公司 1 routeability 正式只读复验

- 用户授权继续后，执行正式环境只读 verifier：`GETTOKENS_VERIFY_OUTPUT_JSON=docs-linhay/spaces/20260615-account-store-runtime-routeability/plans/20260615-prod-routeability-evidence.json docs-linhay/scripts/verify-prod-routeability-readonly.sh`。
- verifier 只读取 `/Applications/GetTokens.app/Contents/MacOS/cli-proxy-api.meta.json` 与正式 `8317` 只读信息；未替换、不重启、不 kill、不修改正式配置。
- 结果：正式 bundle 内 sidecar commit 是 `fef495dd3f5cd212a0e37f6ef37180492a5056ec`，dirty=`clean`，fingerprint=`fef495dd3f5cd212a0e37f6ef37180492a5056ec:clean:c27a21c1c29751797249a4327b5a328aa2e0be25c485f91a0b25e2c9a492eb9f:darwin:arm64`。
- 期望修复 commit 是 `688f29726719e01e1206d23db47017dea8028253`。因此正式环境仍是旧构建，当前正式 `公司 1` 问题不能判定为 routeability 修复回归；它已转化为 release / 分发链路未升级问题。
- 证据已写入 `docs-linhay/spaces/20260615-account-store-runtime-routeability/plans/20260615-prod-routeability-evidence.json`。下一步需要发布/安装包含 `688f2972` sidecar 的正式构建后，再重新执行同一个 verifier。

## Xiaomi MiMo Token Plan 额度 / 余额 parser 修复

- 用户提供 Xiaomi MiMo `tokenPlan/usage` cURL 后，确认真实响应的 `data.usage.percent=0.53` 与 `data.monthUsage.percent=0.7842` 是 0-1 比例语义，不是 0-100 百分数；旧 parser 因此把剩余额度算成约 99%，实际应为 PLAN 47%、MONTH 22%。
- 同轮确认 Xiaomi balance 响应是 `data.balance/currency/giftBalance/cashBalance`，旧 billing parser 只覆盖 DeepSeek/OpenRouter/OpenAI/generic balance，未覆盖该结构。
- 已在主仓 `internal/accounts/quota_curl.go` 和 sidecar fork `docs-linhay/references/CLIProxyAPI/internal/api/handlers/management/quota_refresh.go` 适配 Xiaomi MiMo fractional percent 与 balance shape，并补回归测试。
- sidecar fork 已提交 `2a1696056466b1c8240c0b6f43379b3ebe068684 fix(gettokens): parse xiaomi mimo quota ratios`，本地 `build/bin/cli-proxy-api` 已从干净临时 worktree 重建，meta fingerprint 为 `2a1696056466b1c8240c0b6f43379b3ebe068684:clean:0a20cd74e24f15510ffe20c0e4a96ef1ad19a1b1931e7a061930c6c421c66009:darwin:arm64`。
- 验证：主仓 `go test ./internal/accounts ./internal/wailsapp -run 'Quota|Billing|Xiaomi|OpenAICompatible'` 通过；sidecar fork `go test ./internal/api/handlers/management -run 'TestQuotaRefresh(ParsesXiaomiMiMoFractionalPercent|ParsesNestedBalanceData|ParsesNestedSnakeCaseBalanceData|OpenAICompatibleAccountWritesBillingRuntime|CodexAPIKeyAccountWritesRuntimeGuard)'`、`git diff --check`、`go test ./... -count=1` 通过。补充运行态验收通过：临时 fixture server + 临时 config/account DB + 本地 `build/bin/cli-proxy-api` 创建 Xiaomi 临时账号并执行 `quota-refresh`，返回 PLAN 47%、MONTH 22%、CNY balance；验收后临时进程与目录已清理。
- 正式版未触碰：未修改 `/Applications/GetTokens.app`，未重启/替换/kill 正式 sidecar；正式环境需安装包含 sidecar commit `2a169605` 的构建后才会体现修复。
