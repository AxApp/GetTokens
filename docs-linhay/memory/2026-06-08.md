# 2026-06-08

- 会话整理：本轮收敛了 `20260606-menu-bar-popover` 的 SwiftUI menu bar popover、账号池导航、额度/余额快照和空态文案边界。最终规则是：menu bar popover 展示 sidecar quota runtime snapshot，不展示原始账号清单；启动、sidecar ready 和打开 popover 只读 `/gettokens/quota-status`，只有用户点击 `刷新额度` 才对启用且配置 quota/billing curl 的账号调用 sidecar `quota-refresh`，然后重读 snapshot。
- 用户偏好已明确：账号列表无真实额度快照时，不要显示 `quota snapshot` 这类假账号名；空账号行应显示 `等待账号额度快照`，副文案为 `点击刷新额度后更新`。SwiftUI 主渲染与 AppKit fallback 已同步，并用源码断言防止回退。
- 排查结论：`公司 1` 账号的 menu bar 主动刷新已触发 sidecar，但其 quota curl 指向 `http://cpa.host.dxy/v0/management/public/custom/monitor/quota?...`；当前本机 DNS 直接解析 `cpa.host.dxy` 失败，走系统代理 `127.0.0.1:7897` 返回 HTTP 502。sidecar 报 `codex api key quota request failed with status 502` 属于自定义额度接口/代理链路不可达，不是 popover 数据接入或渲染失败。
- 本次可复用模式已沉淀到 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 `Menu Bar Quota Snapshot Boundary`：不要把 `/accounts` 有账号误判为额度快照可用；诊断 menu bar 额度空态时先查 `/gettokens/quota-status`、`quota-refresh`、curl endpoint、DNS/VPN/proxy 和 `use-system-proxy`。
- 提交流程边界：当前父仓包含多组会话工作，且 `docs-linhay/references/CLIProxyAPI` 子仓有 project-candidate-pool 相关 sidecar runtime 改动。整理提交应先提交子仓，再提交父仓 gitlink、Wails/frontend/docs/skills 变更；不触碰 `/Applications/GetTokens.app` 正式版。
- `20260606-relay-vendor-support` 需求已整理为 runtime-first v02：下一轮实现先补 `formatBaseUrls` 事实源、endpoint resolver、sidecar runtime projection、executor endpoint selection 和 route explain 过滤，再落 `sub2api/new-api` 预设。首期建议从非空 `formatBaseUrls` keys 派生 `supportedFormats`，旧账号无多端配置时才回退 provider 默认能力；暂缓独立 `supported_formats_json`。Codex / Responses 不默认 fallback 到 `openai_chat`，同一 `/v1` 同时支持 Chat 和 Responses 时必须显式配置两个 format。验收新增 endpoint matrix smoke，用不同 mock server 证明 Chat、Responses、Anthropic 请求分别命中正确 endpoint。
- `20260606-relay-vendor-support` 可行性评估完成：主仓 DTO/Wails/前端、CLIProxyAPI account-store 和 local CLI apply 已有多端基础，整体可行且不需要推倒重来；主要缺口是 `supportedFormats` 仍按 provider 推断、synthesizer 未投影 `format_base_urls_json`、executor 仍读单一 `base_url`、Codex Channel Routing 对 openai-compatible 账号过宽。推荐下一步先做 Slice 1：从非空 `formatBaseUrls` keys 派生能力，并让 route explain 输出 `missing_format:<format>`。
- 账号详情“修改基础 URL 后点保存没反应”已在 `http://localhost:34115/#frame=accounts&detail=acct_116260d8-1996-4079-9e02-1c0d28b11a33` 复现：点击保存实际触发了 Codex API Key 配置保存，但启用的 quota curl 会先预检，当前自定义额度接口经代理返回 502，`onSaveConfig` 抛错后 `UnifiedAccountDetailModal.saveConfig` 没有在弹窗内展示错误，导致用户看到静默不动。修复为保存失败时在详情弹窗顶部显示 `保存失败` 和原始错误摘要，配置草稿再次变化时清除旧错误；回归覆盖在 `accountDetailLayout.test.mjs`。
- 账号详情保存语义调整：修改 API key / 基础 URL / prefix / quotaCurl / billingCurl / 模型映射是数据库保存行为，不应被 quota/billing curl 的网络可用性阻塞；quota 502 只应出现在显式 `测试` 或 `刷新额度` 操作中。`updateSelectedApiKeyConfig` 已移除保存前 `TestCodexAPIKeyQuotaCurl/TestCodexAPIKeyBillingCurl` 预检，账号领域 skill 同步更新该边界。
- 为避免旧计划把 bug 重新设计回来，已同步更新 `.agents/skills/gettokens-domain-engineering/SKILL.md`、`docs-linhay/dev/20260519-quota-curl-template-boundary.md`、`20260505-codex-api-key-custom-quota-curl` 与 `20260606-relay-vendor-support` space 文档：保存动作不触网，quota/billing 网络请求只属于显式测试或刷新额度。
- `20260606-project-account-candidate-pool-rule` 允许账号语义已补“启用 + 排序”：用户指出项目配置 modal 的允许账号列表需要支持排序和启用。本轮保持 DTO 简洁，不新增 per-account enabled 字段；`allowAccountIDs` 数组同时表达启用集合与项目内候选顺序，checkbox 移除即停用，上/下箭头调整数组顺序。Wails explain 按 `allowAccountIDs` 重排候选；sidecar `project-candidate-pool` policy 同时输出 `AllowIDs` 和 `OrderIDs`，确保真实 routing engine 在 strict allow 后按项目顺序进入 selector。
- 项目配置 modal “视图没到底”已定位为 flex 继承中断：`ModalFrame` body 已是剩余高度容器，但项目候选池 panel / grid / 左右列未继续 `min-h-0 flex-1`，导致内容按自然高度结束。修复后 modal body、panel、grid、配置列和规则列形成 full-height 链路；浏览器实测 `1324x804` 下 grid / 左侧配置列 / 配置列表 bottom 一致，body 底部只保留 `p-5` 的 20px 内边距。
- 项目配置 modal 左列继续细化：`请选择项目` 校验提示从底部按钮下方移动到项目选择区域下方、允许账号列表上方；允许账号列表从固定 `max-h-56` 改为 `min-h-0 flex-1 overflow-auto`，让列表吃满剩余高度并把底部操作贴住左列底部。浏览器实测 `新建规则` 按钮 bottom 与左列 bottom 一致。
- 项目配置 modal 的 `新建规则` 入口已从左侧配置列底部移动到内容区右上角动作位；页面仅保留一个提交按钮，继续复用 `saveRule` 和原禁用条件，编辑态仍显示 `更新规则`。用户随后确认移除 `项目候选池 / 命中历史项目后... / rules` 二级标题栏，当前 modal 内容区不再展示这组重复标题和规则计数。
- 项目配置 modal 左列中间提示继续压缩：项目下拉与允许账号之间的历史项目说明块、选中项目摘要块和红色 inline draft 校验提示已移除；按钮仍用 `draftIssues` 控制禁用，避免未选项目误提交，但页面不再展示 `暂无可选历史项目...`、`带工作目录身份`、`请选择项目` 等提示。
- 项目配置 modal 底部横向滚动条已移除：modal body 和主体 grid 增加 `overflow-x-hidden`，左右列表从 `overflow-auto` 改为 `overflow-y-auto overflow-x-hidden`；浏览器实测 dialog 内 body / grid / 左右列表 `scrollWidth == clientWidth`，内部无横向溢出。
- 项目配置 modal 的 `新建规则 / 更新规则` 已进入顶部导航栏：modal header 提供 primary action slot，`ProjectCandidatePoolRulesPanel` 通过 portal 挂载按钮，继续由 panel 持有 `saveRule`、draft 和禁用态；浏览器实测按钮在 header 内、不在 body 内，且位于 body 上方。
- 本次项目配置 modal 反复收敛出的 UI 模式已沉淀到 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Detail modal primary action slot`：detail modal 主动作优先进入 header 导航栏动作位，业务组件可通过 portal 挂载，body 不再放二级标题、规则计数或空白动作行；该规则暂不升级到 AGENTS。
- 账号详情 endpoint 文案调整：`基础 URL` 改为 `默认基础 URL` 并说明“三端配置未填写专用端点时回退使用”；三端配置区改为 `按协议 Endpoint 覆盖`，说明分别覆盖 OpenAI-compatible、Codex Responses 和 Anthropic 请求，留空回退默认基础 URL。
- 账号详情凭据模块进一步收敛：左侧只承载账号名称、API 密钥、默认基础 URL、前缀和短消息连通验证，不再展示默认 URL 回退说明或英文 `CREDENTIAL/CONNECTION`；协议端点说明统一放到右侧连接/路由区，位于代理配置上方，文案压缩为 `协议端点 / 留空使用默认基础 URL。`，并保持无顶部线。
- 本次账号详情凭据/协议端点边界已沉淀到 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Account credential endpoint boundary`，后续同类详情页按“左侧凭据动作、右侧连接/路由配置”组织。
- `20260606-relay-vendor-support` 首期实现完成并通过 focused tests：账号能力优先从非空 `formatBaseUrls` keys 派生；Channel Routing 对 Codex / Claude 分别要求 `openai_responses / anthropic` 并输出 `missing_format:<format>`；CLIProxyAPI synthesizer 投影 `format_base_url:<format>`；OpenAI-compatible / Codex / Claude executor 分别读取 `openai_chat / openai_responses / anthropic` endpoint；统一厂商入口新增 `sub2api/new-api`，本地 CLI apply 对两个 relay preset 开放 Codex 与 Claude 动作。真实部署联调、生产 quota/billing 模板和旧入口同源化仍留后续。
- `20260606-relay-vendor-support` 完成度审计后补齐剩余自动化缺口：账号卡片复制、解析、导入恢复保留 `formatBaseUrls`，导入创建/更新 payload 写回多端 endpoint；`/models` 管理调用在详情页和 relay model catalog 中缺省使用 `formatBaseUrls.openai_chat`，专用 `modelFetchBaseUrl/modelFetchApiKey` 仍优先；旧 `openAICompatibleProviderPresets` 对 `sub2api/new-api` 改为从统一 `vendorPresets` 派生薄别名。新增测试覆盖 `accountTransfer.test.mjs`、`accountDetailLayout.test.mjs`、`openAICompatible.test.mjs` 与 `relay_model_catalog_test.go`。
- `20260608-subagent-project-experience` 第 1 轮评估修复完成：三份体验报告共 52 条建议（产品/运营 19、运行态/路由 12、扩展工作台 21），已归并为可立即修复 20、需要产品决策 15、需要较大技术方案 15、暂不处理 2。低风险落地 3 组：MCP conflict/unknown transport 不再降级成 stdio/ready，Git Skill `tk://` source path 拒绝空 path/`..`/绝对路径/Windows drive，菜单栏导航抽出 resolver 并覆盖 Codex live sessions/usage。验证通过 focused node tests 与 `npm run typecheck`；未启动 Wails dev 桌面验收，下一轮优先 raw config TOML 预检、MCP args 数组编辑、账号池筛选空态和 Live Sessions 清空文案。
- `20260608-subagent-project-experience` 第 2 轮评估修复继续落地 2 组低风险前端修复：MCP args 从空白 split 改为逐行数组编辑，避免带空格路径/JSON 参数被结构化保存拆坏；Live Sessions `清空` 改为 `清空实时视图` 并增加确认，文案明确不删除磁盘历史、不取消进行中的请求。验证通过 Codex extensions focused tests、Codex live sessions focused tests、`npm run typecheck` 和 `check-docs.sh`；仍有下一轮候选：raw config TOML 预检备份、账号池筛选空态、raw/结构化 editor dirty arbitration、MCP env/header 行级校验。
- `20260608-subagent-project-experience` 第 3 轮评估修复继续落地 2 组低风险前端修复：账号池空态拆分为真实无账号与筛选无结果，并为筛选空态提供清空搜索/重置筛选；MCP env/header textarea 增加行级校验，缺少 `=` 或非法 TOML key 时提示并禁用保存，避免结构化编辑静默丢字段。验证通过 accounts filters / Codex extensions focused tests、`npm run typecheck` 和 `check-docs.sh`；仍有下一轮候选：raw config TOML 预检备份、MCP raw/结构化 editor dirty arbitration、quoted server id 写回保护、Skills 启停规则来源解释。
- `20260608-subagent-project-experience` 第 4 轮评估修复继续落地 2 组低风险修复：Codex MCP raw `config.toml` editor dirty 时阻止打开/保存结构化 MCP editor，结构化 draft dirty 时阻止打开 raw editor，避免双编辑器互相覆盖；Wails MCP 保存链路支持 quoted server id，如 `[mcp_servers."linear.team"]` 会原地 patch 并保留 `.oauth` / `.tools.*` nested section，不再追加错误的 bare dotted section。验证通过 focused Go test、Codex extensions focused node tests、`npm run typecheck` 和 `check-docs.sh`；仍有下一轮候选：raw config TOML 预检备份、Skills 启停规则来源解释、MCP tool approval 编辑保存、多行 TOML warning/AST patch。
- `20260608-subagent-project-experience` 第 5 轮评估修复继续落地 2 组低风险 Wails 修复：MCP tool approval 结构化保存链路会在显式传入 `Tools` 时重建当前 server 的 `.tools.*` sections，支持 quoted tool key、删除旧 tool section，并保留其他 server；MCP raw 多行 TOML 结构只增加 warning，不做 AST patch，提示 `args = [` / `env = {` 等结构需优先用 raw config 检查。验证通过 focused Go tests 和 `check-docs.sh`；仍有下一轮候选：raw config TOML 预检与后端备份、Skills 启停规则来源解释、tool approval 前端编辑入口强化、多行 TOML AST patch。
- `20260608-subagent-project-experience` 第 6 轮评估修复继续落地 2 组低风险修复：raw `config.toml` 保存前增加后端轻量 TOML 结构预检，已有原文件时真实创建 `config.toml.gettokens-backup-*` 并返回/展示备份路径，无原文件时不提示备份；MCP tool approval 前端结构化 editor 增加 `tool=approval_mode` textarea 和 change preview，接入第 5 轮后端 `.tools.*` 保存链路。验证通过 focused Go tests、Codex extensions node tests、`npm run typecheck` 和 `check-docs.sh`；仍有下一轮候选：Skills 启停规则来源解释、完整 TOML AST 预检/parser 化保存、MCP tool approval 行级校验。
- `20260608-subagent-project-experience` 第 7 轮评估修复继续落地 2 组低风险修复：Skills snapshot 增加 `enabledSource/enabledSourceValue`，详情 modal 展示启停来源为默认启用、path rule 或 name rule；MCP tool approval 前后端增加行级校验，允许空、`auto`、`prompt`、`approve`，空值语义是只保留 tool section 不写 `approval_mode`。验证通过 focused Go tests、Codex extensions node tests、`npm run typecheck` 和 `check-docs.sh`；本轮后暂时无低风险可继续修改候选，剩余完整 TOML AST/parser 化预检、多行 AST patch 和 sidecar/运行态项均需较大技术方案或产品决策。
- `20260608-subagent-project-experience` 收尾验收完成：Trace/Compass/Socket 三个体验 subagent 共输出 52 条中度建议并覆盖业务体验与代码逻辑审核，Frame 连续完成 7 轮评估修复，最终落地 16 组低风险改进。主控复跑 `go test ./...`、`cd frontend && npm run test:unit`（764 项）、`cd frontend && npm run typecheck`、`docs-linhay/scripts/check-docs.sh` 全部通过；本轮只在 dev 环境和仓库内操作，未触碰正式版 App、正式进程或正式数据目录。
- `20260608-subagent-project-experience` 已重开第 8 轮第一修复包方案，范围锁定 `P15` 账号禁用错误归因、`R2` channel routing reason summary、`R3` rate-limit legacy key 只读检测、`P16/P17` 账号用量失败态与 hook 测试、`P13/P14` 菜单栏真实入口与风险摘要。方案要求继续在 dev/本仓环境内执行，先补 BDD/TDD 红灯测试，再分阶段实现；菜单栏相关变更需要真实 macOS dev 桌面验收。
- `20260608-subagent-project-experience` 第 8 轮第一修复包已完成自动化与构建验收：P15 禁用/启用失败归因改走账号动作通知，P16/P17 用量失败态增加 `loadState/errorMessage` 并在卡片显示 `ERR/STALE`，R2 route event 支持 filtered reason summary，R3 rate-limit legacy account key 只读检测提示，P13/P14 菜单栏 payload 指向账号风险入口并补 `riskSummary/moreRiskLabel`。验证通过 focused node tests、`npm run test:unit`（767 项）、`npm run typecheck`、`go test ./...`、`check-docs.sh` 和 `./scripts/wails-cli.sh build`；未主动启动 dev App 做真实菜单栏点击验收，避免打断用户桌面。
- `20260608-subagent-project-experience` 第 9 轮入口诊断修复完成自动化与构建验收：`filter=risk` 已由账号页真实消费并重置为风险巡检宽筛选，账号 header 刷新拆成账号列表刷新与运行态刷新，Usage Desk 索引动作补影响范围说明，Codex Skills 详情 modal 接入 `detail=<skillId>` hash 恢复，Skill 扫描增加 200 文件 / 8 层深度预算 warning 并在详情展示。验证通过 focused tests、`npm run test:unit`（776 项）、`npm run typecheck`、`go test ./...`、`docs-linhay/scripts/check-docs.sh` 和 `./scripts/wails-cli.sh build`；未做真实 macOS 菜单栏点击验收，未触碰正式版 App、正式进程或正式数据目录。
- 新建 `20260608-cliproxyapi-upstream-v7150-intake` space，规划下一轮 CLIProxyAPI upstream intake。只读探测确认 canonical upstream `router-for-me/CLIProxyAPI` 的最新 tag 已到 `v7.1.50@4f55ecca`，`main@c989cdd9` 另有 tag 后 Codex Service Tier normalizer plugin；本地 fork `docs-linhay/references/CLIProxyAPI#gettokens/sidecar@29f4f577` 目前只配置 `origin`，没有 `upstream` remote，且本地 tag 缓存到 `v7.1.37`。本轮沿用 2026-06-01 后的方案：不做整包 merge / 不大块 cherry-pick，上游作为参考输入，按 usage refresh、xAI/Gemini/model compat、Codex replay/reasoning、auth runtime events、logging/safemode、pluginhost research、build workflow 七类拆分候选；接受项必须一个能力一个子 space，先 BDD/TDD，再在 GetTokens sidecar 边界内重实现。
- `20260608-cliproxyapi-upstream-v7150-intake` 继续核对 canonical upstream 新 tag：`router-for-me/CLIProxyAPI` 当前最新确认为 `v7.1.53@d55f215c`，`v7.1.54` 在 canonical GitHub remote 与 fresh clone 中均不存在；tag 后 `main@6f38e848` 仅 README sponsorship。v7.1.51 的 Gemini/Antigravity Claude signature handling 与 Codex Gemini `systemInstruction` fallback 建议作为一个子 space reference-port 到 GetTokens sidecar；v7.1.52 Linux release workflow、v7.1.53 Docker ca-certificates、service tier plugin example 与 README sponsorship 不进入本轮合并。
- `20260608-cliproxyapi-upstream-v7150-intake` 已补实现级 review：第一批推荐落地 `Responses WebSocket dedupe 保留被 output 引用的 tool call`、`xAI 空 tools 删除 tool_choice/parallel_tool_calls`、`Gemini message-level system role 规范化`、`Codex 非流式 response 非空文本累积`、`usage executor_type / refresh marker`、`Cloudflare/Home refresh`。暂不直接移植 Codex reasoning replay cache、auth runtime Remove/error events、file-backed API request/response logging、pluginhost、FreeBSD/release workflow；模型删除类变更必须先走 GetTokens model catalog/requestable alignment。
- `20260608-cliproxyapi-codex-websocket-dedupe-referenced-call` 已按上游单点修复完成 GetTokens fork 重实现：Responses WebSocket input 按 `id` 去重时会优先保留仍被 `function_call_output/custom_tool_call_output` 引用的 tool call，没有引用时保持“保留最后一个”旧语义。本轮只改 `CLIProxyAPI` 子仓 OpenAI handler 与测试，不引入 reasoning replay cache 或 executor 重构；验证通过新增红灯/绿灯、OpenAI handler focused 回归、相关包测试、`git diff --check` 和 `go test ./...`。
- `20260608-cliproxyapi-codex-websocket-dedupe-referenced-call` 已补真实 dev App 验收：通过 `./scripts/wails-cli.sh dev` 启动 dev profile，确认 dev App 进程为 `build/bin/GetTokens.app`，dev sidecar 为 `build/bin/cli-proxy-api -config ~/.config/gettokens-dev/config.yaml`，端口 `18317` 与正式版 `8317` 分离，sidecar 指纹为当前 `29f4f577:dirty:622a...:darwin:arm64`，`/healthz` 返回 200。正式版 `/Applications/GetTokens.app` 与 prod sidecar 未被 kill、重启或替换；截图已裁剪归档到对应 space，避免保存账号邮箱明细。dev 日志里的 `refresh_token_reused` 属于旧 dev 账号数据噪声，不影响本次 WebSocket 去重修复。
- 用户明确要求从本轮开始“每一轮都必须做真实 dev App 手点测试”。已升级为 repo-wide 规则并写入 `AGENTS.md` 与 `.agents/skills/gettokens-ops-governance/SKILL.md`：每轮修复收尾必须使用本仓 dev 构建产物和 `GETTOKENS_APP_PROFILE=dev`，在真实 macOS/Wails 窗口或菜单栏走关键入口，截图归档到对应 space；自动化、浏览器预览、hash 断言和 Wails build 不能替代。
- `20260608-subagent-project-experience` 第 9 轮补做真实 dev App 菜单栏验收时，首次点击 `DEV -> 打开账号池` 暴露本仓 dev App SIGSEGV，正式版 `/Applications/GetTokens.app` 未触碰。根因收敛为 menu bar native 导出回调同步从 AppKit/SwiftUI 事件栈进入 Go/Wails window 操作；修复为 SwiftUI action 先延后到下一轮 main queue，Go 导出回调再通过 goroutine 派发。复测同一路径通过：dev App 保持存活，主窗口进入账号池风险筛选，仅筛出 1 项禁用风险资产；截图归档在 `docs-linhay/spaces/20260608-subagent-project-experience/screenshots/20260608/menubar/20260608-round9-dev-menubar-popover-before-v02.png` 与 `20260608-round9-dev-menubar-open-accounts-after-v02.png`。
- 用户进一步明确：每个候选必须有确凿事实证据才能进入修复，不能凭 backlog 标签或直觉判断“低风险可修”。已升级为 repo-wide 证据门禁并写入 `AGENTS.md` 与 `.agents/skills/gettokens-ops-governance/SKILL.md`：修复前必须在对应 space/plan 记录问题来源、当前代码或 UI 事实位置、可复现现象或缺失证明、验收路径；证据不足的候选只能进入调研或方案，不能进入代码修改。
