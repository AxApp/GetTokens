# 2026-06-19

## Route Guard / Account Budget mock-first 测试沉淀纠偏

- 用户明确指出“你不是应该沉淀下去吗”，确认本轮“mock 上下游测试我们的服务”不能只作为聊天记忆处理。
- 已将 Route Engine、Account Budget Guard、route guard source、quota threshold、usage calibration 等 sidecar 热路径能力的默认测试方式落到项目级 skill：每个实现 slice 开工前必须先明确 mock upstream facts 与 mock downstream/spy outputs；不清楚上下游 seam 时先停在调研/space 文档，不直接接真实服务。
- 现有 workflow 文档入口仍是 `docs-linhay/dev/20260603-upstream-downstream-mock-testing.md`，当前需求证据入口仍是 `docs-linhay/spaces/20260618-route-guard-dsl-evaluation/`。
- 二次纠偏：该模式已从领域 skill / dev workflow 升级到 `AGENTS.md` 测试门禁。后续 sidecar 热路径、路由决策、route guard、quota / usage、live sessions、Codex WebSocket 或 management API 状态回写类功能，必须先构造 mock upstream + mock downstream / spy 服务级测试，真实账号和真实外部服务仅作为后置 smoke。
- 执行进展：已启动 Route Guard DSL / Account Budget Guard 的首个 sidecar tracer-bullet。实现 `quota-threshold` route guard source 与 `QuotaThresholdRouteGuardBlocks`，用 fake quota window + typed threshold rule 验证 remaining-percent <= 20% 时生成 block，并通过现有 account route guard policy deny 对应 auth candidate。验证命令：`go test ./internal/gettokenshooks -run 'TestQuotaThresholdGuard|TestAccountRouteGuardPolicyDeniesQuotaThresholdCandidate' -count=1`、`go test ./internal/gettokenshooks -count=1`。
- 执行进展 2：`QuotaRuntimeStore.Upsert` 已接入 `channel-routing/config.json` 顶层 `quotaThresholdRules`，fresh quota runtime 更新会同步 `quota-threshold` source；fresh recovery 到阈值外只清该 source，不清 `rate-limit`。验证：`go test ./internal/gettokenshooks -run 'TestQuotaRuntimeStore(UpsertFeedsQuotaThresholdGuardFromConfig|FreshRecoveryClearsOnlyQuotaThreshold)' -count=1` 和 `go test ./internal/gettokenshooks -count=1`。
- 执行进展 3：已引入 sidecar runtime 内存 `AccountQuotaUsageCalibration` 与 `ApplyQuotaUsageCalibrations`，支持 `delta` / `set-effective`，过期或 revoked entries 不参与计算，`set-effective` 默认不能低于 observed usage。`QuotaRuntimeStore.Upsert` 可通过 `SetUsageCalibrations` 将校准应用到 effective window，quota-threshold 使用 effective remaining 触发。验证：`go test ./internal/gettokenshooks -run 'TestQuotaRuntimeStoreAppliesManualCalibrationBeforeQuotaThreshold|TestQuotaThresholdGuardUsesManualCalibration|TestQuotaUsageCalibration' -count=1` 和 `go test ./internal/gettokenshooks -count=1`。剩余：持久化 ledger、add/revoke API、Wails DTO、前端入口。
- 执行进展 4：已新增 sidecar quota calibration management routes：`GET/POST /gettokens/quota-calibrations` 与 `POST /gettokens/quota-calibrations/:id/revoke`。add/list/revoke 已能驱动内存 ledger；revoke 后下一次 quota runtime upsert 不再使用该 calibration。验证：`go test ./internal/gettokenshooks -run 'TestQuotaRuntimeCalibrationRoutesAddListAndRevoke' -count=1` 和 `go test ./internal/gettokenshooks -count=1`。剩余：文件/SQLite 持久化、Wails DTO、前端入口和审计历史体验。
- 执行进展 5：已补齐 quota calibration 的 Go client -> internal Wails -> root App -> generated Wails bindings -> accounts frontend hook 链路。新增 `QuotaUsageCalibration` / `QuotaUsageCalibrationInput` DTO、`List/Add/RevokeQuotaCalibration` 方法与 `useQuotaCalibrations` 前端调用入口。验证：`go test ./internal/cliproxyapi -run 'TestQuota(RuntimeClientStatus|UsageCalibrationClientEndpoints)' -count=1`、`go test . -run 'TestMap(CodexQuotaResponsePreservesBilling|QuotaUsageCalibrationPreservesManualCalibrationFields)' -count=1`、`node --test frontend/src/features/accounts/tests/quotaCalibrationBindings.test.mjs`、`go test ./internal/cliproxyapi ./internal/wailsapp . -count=1`、`npm --prefix frontend run typecheck`、sidecar `go test ./internal/gettokenshooks -count=1`。剩余：持久化 ledger、可见编辑 UI、typed quota-threshold rule CRUD / 前端编辑器。
- 执行进展 6：已补齐 typed `quota-threshold` rule 的 sidecar CRUD 与 App/前端调用链路。sidecar 新增 `GET/POST/PUT/DELETE /gettokens/quota-threshold-rules`，持久化到现有 `channel-routing/config.json` 顶层 `quotaThresholdRules`；读取兼容旧 camelCase config。主仓新增 `QuotaThresholdRule` DTO、client/root/Wails bindings 与 `useQuotaThresholdRules`。验证：sidecar focused/full `go test ./internal/gettokenshooks`、主仓 `go test ./internal/cliproxyapi ./internal/wailsapp . -count=1`、frontend binding test、`npm --prefix frontend run typecheck` 均通过。剩余：可见规则编辑器、rule 变更立即重算、calibration 持久化与审计列表。
- 执行进展 7：manual calibration 已从内存 ledger 升级为 profile 文件持久化，路径为 `<profile-dir>/quota-calibrations/config.json`；`InstallRoutingPoliciesWithConfigPath` 会配置默认 store。add/revoke 会写回文件，新 store 使用同一路径能恢复 created/revoked entries。验证：`go test ./internal/gettokenshooks -run 'TestQuotaRuntimeCalibrationLedgerPersistsAcrossStores|TestQuotaRuntimeCalibrationRoutesAddListAndRevoke|TestQuotaRuntimeStoreAppliesManualCalibrationBeforeQuotaThreshold' -count=1` 与 sidecar full `go test ./internal/gettokenshooks -count=1` 通过。剩余：可见 UI 编辑器、rule 变更立即重算、calibration audit trace 展示。
- 执行进展 8：已补齐 rule / calibration 变更后的当前状态即时重算。QuotaRuntimeStore 增加 raw/effective state 分层，manual calibration add/revoke 会基于最后一次真实 quota 观测立即刷新 effective usage 和 route guard；quota-threshold rule create/update/delete 保存成功后立即刷新当前 quota-threshold guard。sidecar 测试新增运行时 HOME 隔离，并修复 route resilience action 测试的 channel-routing explicit path 串扰，避免 mock 服务级测试读写真实 ~/.config/gettokens-data。验证：go test ./internal/gettokenshooks -run 'TestQuotaThresholdRuleManagementRoutesRefreshRuntimeGuard|TestQuotaRuntimeStoreRefreshesCurrentStateWhenCalibrationChanges' -count=1 与 sidecar full go test ./internal/gettokenshooks -count=1 通过。剩余：可见 UI 编辑器、calibration 历史体验、完整 DSL AST。
- 执行进展 9：账号详情 Quota 区已接入最小可见编辑入口。QuotaThresholdRulePanel 支持按 quota window 配置 remaining-percent <= N% 停止路由，并支持启用/停用、删除；QuotaCalibrationPanel 支持 delta / set-effective 校准和撤销。当前 UI 只覆盖 typed quota-threshold 主场景，不暴露完整 DSL AST。验证：node --test frontend/src/features/accounts/tests/quotaCalibrationBindings.test.mjs 与 npm --prefix frontend run typecheck 通过。剩余：calibration 历史 / audit trace、rule 冲突提示、完整 DSL AST 编辑器。
- 沉淀：sidecar 服务级测试隔离已写入 .agents/skills/gettokens-domain-engineering/SKILL.md 与 docs-linhay/dev/20260603-upstream-downstream-mock-testing.md。后续 mock upstream/downstream 测试必须隔离 HOME/profile/config path，不得读写真实 ~/.config/gettokens* 或跨测试复用 persisted runtime state。
- 执行进展 10：已补齐 DSL AST / 表达式引擎、前端规则编辑增强、calibration 历史/audit 和规则冲突体验。sidecar quota-threshold rule 新增 condition AST，支持 all/any/not 与 comparison leaf，metric 支持 remaining-percent、used-percent、remaining、used；management API 对冲突启用规则返回 409 conflicts，对非法 AST 返回 400。前端 QuotaThresholdRulePanel 支持结构化阈值 + 高级 DSL JSON；QuotaCalibrationPanel 增加历史/Audit 区域。验证：sidecar DSL/conflict 聚焦测试、sidecar full、cliproxyapi/root 聚焦测试、主仓 Go、frontend binding test、typecheck、docs check、git diff check 均通过。
- 执行进展 11：按智者裁决和用户确认，暂停扩展规则表达力，先完成 runtime/simulator shared evaluator 硬验收口。sidecar 新增 `SimulationFacts` / `SimulationResult` / `AccountDecisionTrace` / `ReasonTraceStep` contract，quota-threshold runtime guard 改为调用 shared evaluator；新增 `POST /v0/management/route-guard/rules/simulate`，读取持久化 quota-threshold rules 并输出稳定 code+data trace。验证：同一 facts 下 simulator decision 与 runtime block 的 source/reason/expiresAt 一致；stale/missing quota fact 只产生 diagnostic/missing trace，不 hard block。聚焦测试与 sidecar full `go test ./internal/gettokenshooks -count=1` 通过。剩余：主仓 client/Wails DTO/前端“模拟当前规则”入口，以及单日/多日/起止时间 window 扩展。
- 执行进展 12：已完成 quota-threshold simulator visible loop。主仓新增 `SimulateRouteGuardRule` client / Wails / root App / generated bindings，DTO 保留 `reasonTrace[].code` 与结构化 `data`；前端新增 `routeGuardSimulation.ts` 和 `useRouteGuardSimulation`，`QuotaThresholdRulePanel` 支持“模拟当前规则”和单条规则模拟，展示 decision、matched rule、recovery/expiry、account decision、trace data 与 diagnostics。sidecar simulate route 支持 draft rule / rules 输入且不持久化草稿。验证：sidecar focused/full、主仓 cliproxyapi/root/wails Go、frontend binding test 与 typecheck 均通过。发现并修复测试时钟问题：calibration route 测试必须显式控制 created/revoked 与 runtime facts 的时间关系，避免真实系统时间让校准被误判为未来创建或尚未撤销。
- 沉淀：已把“time-bearing mock facts and ledgers must share the same injected clock”补入 `.agents/skills/gettokens-domain-engineering/SKILL.md` 的 Account Budget Guard / Route Engine Mock-First Testing 规则。该规则属于单领域服务测试失败模式，不升级 `AGENTS.md`。
- 执行进展 13：已完成 multi-window budget facts contract。sidecar simulator facts 新增 `quotaWindows[]`，同一账号可携带 daily / multi-day / bounded 多个窗口；shared evaluator 仍通过 `window_key` 选择目标窗口，并用 same-facts 测试证明 simulator decision 与 runtime guard 的 reason/expiresAt 一致。主仓 client/root/frontend DTO 透传 `quotaWindows[]`，旧 `quotaWindow` 保持兼容。同步修复 persisted condition-only AST rule 被 runtime loader 因缺少 legacy `windowKey` 过滤的问题。验证：sidecar 聚焦测试、主仓 client/root 聚焦测试、frontend binding test 与 typecheck 通过。剩余：真实 usage aggregator 还未生成这些窗口；前端未做多窗口选择/创建 UI。
- 智者裁决：Route Guard / Account Budget Guard 下一步必须先做 facts 生产线，依赖顺序固定为 Slice 14 Usage Aggregator -> Budget Window Facts、Slice 15 Aggregator Facts -> Simulator/Runtime Same-Facts Gate、Slice 16 Window Definition Management API、Slice 17 Frontend Multi-window UI、Slice 18 Recurring/Cron 编译层评估。明确不先扩 DSL、不先做 UI、不先接 cron。
- 裁决细节：daily window 必须基于 definition 显式 timezone 的自然日；multi-day 第一版只做 calendar N days，不做 true rolling；bounded window 使用半开区间 `[startsAt, endsAt)`；usage budget recovery 使用 `window.endsAt`，provider quota 可使用 provider resetAt；route hot path 只消费 materialized facts，不解析 cron/timezone/DST/business calendar。
- Slice 14 验收升级：必须用 deterministic mock committed usage events、injected clock、explicit timezone、daily/multi-day/bounded 边界测试、committed-only event status 测试、calibration delta 测试证明 `BuildBudgetWindowFacts`。`not blocked != safe`：missing/stale facts 在 simulator/UI 必须显示 diagnostic/inconclusive，不得翻译成绿色允许。完整计划已写入 `docs-linhay/spaces/20260618-route-guard-dsl-evaluation/plans/20260618-route-guard-dsl-engine-evaluation.md`。
- 执行进展 14：已完成 Slice 14 Usage Aggregator -> Budget Window Facts。sidecar 新增 `BuildBudgetWindowFacts`，从 `usage_attribution_events` 生成 daily / multi-day calendar / bounded `QuotaWindowFacts[]`；使用 explicit timezone、UTC 半开窗口、committed-only `completed_at_unix_ms > 0 && failed = 0`、tokens/request metric、delta calibration、`window-end` recovery source。`QuotaWindowFacts` 已扩展 raw/calibration/calibrated/percent/source/generatedAt 等 trace 字段。`set-effective` 未进入 facts builder，留到语义闭合后再做。验证：`go test ./internal/gettokenshooks -run 'TestBuildBudgetWindowFacts' -count=1`、相关 quota-threshold/calibration 聚焦回归、sidecar `go test ./internal/gettokenshooks -count=1` 与 sidecar full `go test ./...` 均通过。
- 沉淀：发现 quota-threshold / calibration 旧测试固定 `2026-06-19T10:00Z`，但 active guard lookup 使用真实当前时间；当天真实时间超过 resetAt 后会误判 block 过期。已将相关测试时间基准改成当前时间之后的稳定小时。该问题属于本 space 测试修复，不新增 skill，不升级 `AGENTS.md`。
- 执行进展 15：已完成 Slice 15 Aggregator Facts -> Simulator/Runtime Same-Facts Gate。sidecar 新增共享转换 seam `accountQuotaRuntimeStateFromQuotaWindowFacts`，`accountQuotaRuntimeStateFromSimulationFacts` 委托该 seam 解释 `QuotaWindowFacts[]`。新增 aggregator same-facts 测试：temp sqlite usage events -> `BuildBudgetWindowFacts` -> `SimulationFacts.quotaWindows` -> simulator decision，同时用同一份 `QuotaWindowFacts[]` 转 runtime state -> `QuotaThresholdRouteGuardBlocks`；断言 block 场景下 source/reason/expiresAt 一致，未达阈值场景下 simulator allow 且 runtime 无 block。验证：Slice 15 focused、sidecar `go test ./internal/gettokenshooks -count=1` 与 sidecar full `go test ./...` 通过。
- 执行进展 16：已完成 Window Definition Management API。sidecar 新增 budget window definition ledger `<profile-dir>/budget-window-definitions/config.json` 与 `GET/POST/PUT/DELETE /gettokens/budget-window-definitions`、`POST /gettokens/budget-window-definitions/preview`；delete 为 soft-disable，update 禁止改 id，validation 拒绝 invalid timezone、invalid bounded start/end、zero limit、unsupported metric/kind/semantics。preview 只调用 `BuildBudgetWindowFacts` 产出 facts，不写 route guard。主仓 client/Wails/root App/generated binding surface 已接入。
- 执行进展 17：已完成 Frontend Multi-window UI 首版。账号 Quota 规则区新增 budget window 面板，支持创建 daily / multi-day calendar / bounded definition、刷新 sidecar preview facts、选择 preview facts 作为规则窗口并调用 simulator trace。缺失 preview facts 时显示 inconclusive，不显示绿色安全；前端仍不计算 used/remaining/blocked。
- 执行进展 18：已完成 Recurring/Cron 编译层评估。结论：当前单日、多日、起止时间由 daily / multi-day calendar / bounded 覆盖，不引入 cron；若后续需要工作日/节假日/固定时段，cron 只能作为 management recurrence intent，编译为 explicit bounded windows，并配 preview/audit/replay。当前 API 负向测试拒绝 `kind=cron` 与 `multi-day semantics=rolling`，确保 route hot path 只消费 materialized facts。
- 收口沉淀：Route Guard DSL / Account Budget Guard 本轮从 Slice 14 推进到 Slice 18 并完成统一测试。最终可复用门禁为：budget facts 必须由 sidecar usage aggregator 生产；runtime guard 与 simulator 必须消费同一份 facts；前端只能展示 sidecar preview facts 与 simulator trace，不本地计算 used/remaining/blocked；cron/recurring 如后续引入，只能在 management 编译层展开为 explicit bounded windows，不能进入 route hot path。该边界已落到 space、domain skill、dev workflow 和 AGENTS 测试门禁，本次不再新增 skill。

## Theme Skinning Long-Term Planning
- 决策：GetTokens 后续换肤不引入 Ant Design、MUI、Chakra、Mantine 等完整预设 UI 框架，长期采用自有 `ThemeMode + ThemePreset + Semantic Tokens + Component Skin Contracts` 框架。
- 决策：`theme-mode` 保留为明暗策略，新增独立 `theme-preset` 承载视觉风格；首套非默认风格定为 `Parchment Trust Console`。
- 决策：页面迁移时信息默认不改，但允许重排布局和呈现方式；如需删除、合并、重命名、降级或新增派生信息，先写 `Information Change Ledger` 再与用户核对。
- 文档：长期规划落到 `docs-linhay/spaces/20260519-theme-skinning/plans/20260619-theme-skinning-long-term-plan-v02.md`，space README 已更新状态和入口。
- 文档：补充界面迁移顺序 `docs-linhay/spaces/20260519-theme-skinning/plans/20260619-ui-migration-sequence-v01.md`，按基础设施、全局壳层、Settings/Design System、Accounts、Account Detail Modal、Codex/Claude、运维状态、Usage/Session、工具面、Debug/Doctor、收尾清理推进。
- 进度：Wave 0-2 已实现主题 preset 基础设施、`--gt-*` token 兼容层、Settings Appearance 主题风格入口、Design System 主题 preset baseline 与 Storybook ColorTokens Parchment swatch；聚焦测试、typecheck、frontend build 和 Storybook build 已通过。
- 验收补齐：首次收口遗漏了真实页面浏览器验收；已新增 `docs-linhay/scripts/check-theme-skinning-wave02-preview.mjs`，通过 headless Chrome 验证 Settings classic/Parchment 与 Design System Parchment DOM、token、无横向溢出，并归档三张截图与 `plans/20260619-wave-0-2-preview-snapshot-v01.md`。
- 设计迭代：用户明确要求重新设计 Settings 与 Design System 两个界面；已将 Settings 改为 Parchment 审计台布局，将 Design System 改为控制台首屏和主题实验区，并把设计系统组件红色 marker 限制到 inspect mode，普通预览 gate 固定 `Visible design-system markers: 0`。
- 方向纠偏：用户提供新的 Parchment Trust Console 参考图并指出应先定义设计组件和规范；已归档参考图，新增 `plans/20260619-parchment-trust-console-component-spec-v01.md`，并把迁移顺序调整为先做 Wave -1 component anatomy gate，再进入页面迁移。规范明确 Parchment 应为 sans-first、hairline、圆角、柔和阴影、modal/detail/table/card/status 等组件契约，而不是继续页面级 scoped CSS。
- AntD 试点：用户追问 Ant Design 换肤能力后，已将“完整 UI 框架不作为默认全站主路径”调整为“AntD 可作为受控 adapter 先在 Settings 页验证”。Settings 页已安装当前 npm `antd`，新增 `buildGetTokensAntdTheme` 与 `GetTokensAntdThemeProvider`，由 `ThemeMode + ThemePreset` 派生 AntD token；Settings 页面已真实使用 AntD `Card`、`Segmented`、`Switch`、`Button`，但不改变 Wails 设置读写逻辑。
- 执行交接：用户要求“落实成 space，找人来改”；已新增 `plans/20260619-settings-antd-handoff-v01.md`，明确 Settings AntD 执行者任务、信息变更边界、文件入口、验收命令、DoD 和风险，README 已加入入口。
- Settings 功能回归收口：用户要求“直接使用 ant”，已确认 Settings 主题风格入口不再使用旧 `ThemePresetPicker`，改为直接 AntD `Segmented`；同时补上浏览器预览下 local usage refresh 与 system proxy 两条 Wails-only 调用保护，避免 5173 预览误报保存失败。聚焦测试、typecheck、frontend build 与 theme-skinning headless preview gate 通过；完整 `test:unit` 仍有当前工作区 Sidebar 未提交改动导致的两个无关 sidebar 字符串断言失败，本次不混修。

## OmniRoute Workbench signal navigation closure

- 已为 Doctor Workbench 顶部 OmniRoute summary signals 增加显式多入口 `actionLinks`：Route -> account detail / route decisions，Quota -> status / related account detail，Extension -> extension registry，Ledger -> workbench。
- `deriveOmniRouteWorkbenchProductizationView` 继续只消费 Doctor typed evidence 与 Extension dry-run view；新增导航只基于已有 `DoctorNavigationTarget` 和 evidence label/refID，不从文本推导 route/quota authority。
- 验证：`npm --prefix frontend run test:doctor-workbench`、`npm --prefix frontend run typecheck`、`CHROME_EXECUTABLE_PATH=/nonexistent/chrome node docs-linhay/scripts/check-doctor-workbench-preview.mjs`、`node docs-linhay/scripts/check-omniroute-workbench-productization-preview.mjs` 均通过；产品化 preview gate 新增 `signalActionLinks=true`。
- 沉淀判断：这是 OmniRoute Workbench space 内的导航验收收口，复用现有 hash/deep-link 规则；不新增项目 skill，不升级 AGENTS。

## OmniRoute Extension Registry staged temp apply UI

- 已为 `frontend/src/features/gettokens-extension-registry/` 增加 staged temp apply 测试面板：prepare/apply 仅允许 `/tmp/gettokens-extension-codex-config-staged-preview.toml`，展示 confirmation token、diff preview、result、rollback；真实 `~/.codex/config.toml` local apply 仍 blocked。
- Doctor Workbench 的 `extension-staged-apply` 卡片保持 Review/blocked，不直接接入 `PrepareGetTokensExtensionCodexConfigApply` / `ApplyGetTokensExtensionCodexConfigTransaction`，避免诊断页发起配置写入。
- 验证：`node --test frontend/src/features/gettokens-extension-registry/model.test.mjs frontend/src/features/gettokens-extension-registry/featureSource.test.mjs`、`npm --prefix frontend run typecheck`、`GETTOKENS_EXTENSION_REGISTRY_CHROME_TIMEOUT_MS=35000 node docs-linhay/scripts/check-gettokens-extension-registry-preview.mjs`、`GOCACHE=/private/tmp/gettokens-go-cache go test -count=1 ./internal/wailsapp ./internal/gettokensextensions` 均通过。
- Preview gate 增加 `stagedApplyTestSurface`、`stagedApplyActions`、`stagedApplyRuntimeBoundary`；同时加入 Chrome timeout，避免 headless Chrome 卡住时长期挂起。
- 沉淀判断：这是 Extension Contract/OmniRoute 产品化 space 的具体 UI 验收收口，复用既有 explicit temp target 与 no-real-config-write 规则；不新增项目 skill，不升级 AGENTS。

## OmniRoute Doctor Evidence ledger surface

- 已为 Doctor Workbench safe action surface 增加 `Evidence ledger` 区域，固定三类 entry：`diagnostics-snapshot`、`route-action-ledger`、`extension-config-ledger`。
- route recheck 的 pending / success / warning / failed 会同步映射到 `route-action-ledger`；sidecar 返回 `auditId` 时 ledger detail 直接显示 `audit=<id>`。
- 验证：`npm --prefix frontend run test:doctor-workbench`、`npm --prefix frontend run typecheck`、`node docs-linhay/scripts/check-omniroute-workbench-productization-preview.mjs` 均通过；产品化 gate 新增 `evidenceLedgerSurface=true`。
- 沉淀判断：这是 OmniRoute Workbench space 内的验收补齐，复用已有 safe action / preview gate 模式；不新增项目 skill，不升级 AGENTS。

## OmniRoute Doctor check filters

- 已为 Doctor Workbench 增加 All / Actionable / Route / Quota / Critical 过滤入口；过滤只收窄 `view.checks` 展示，不改变 sidecar authority，不从 usage totals 或 summary 本地推导 route/quota 真相。
- 验证：`npm --prefix frontend run test:doctor-workbench`、`npm --prefix frontend run typecheck`、`node docs-linhay/scripts/check-omniroute-workbench-productization-preview.mjs` 均通过；产品化 gate 新增 `checkFilterSurface=true`。
- 沉淀判断：这是 OmniRoute Workbench space 内的信息检索体验补齐，复用现有 typed evidence/filter preview gate 规则；不新增项目 skill，不升级 AGENTS。

## Mixed Worktree Ship Hygiene

- 沉淀来源：OmniRoute 提交时工作区同时存在 Theme Skinning、`.codex/config.toml`、package/App/theme、未跟踪 snapshot 等并行改动；直接整文件暂存会把无关需求混入 commit。
- 已沉淀到 `.agents/skills/gettokens-ops-governance/SKILL.md` 的 `Mixed Worktree Ship Hygiene`，要求提交前分类 in-scope / unrelated / generated evidence / mixed-content，混合 memory/docs 只暂存目标段落。
- 已同步到 `docs-linhay/dev/20260616-agent-skill-operating-model.md`，补充 browser DOM snapshot / screenshot 在脏主题环境下不能自动进入其它 feature slice。
- 不升级 `AGENTS.md`：这是跨功能提交卫生流程，已有 AGENTS 的 dirty worktree 与文档沉淀入口能覆盖；执行细则放在 skill/dev workflow 更合适。

## Wise Council Dialogue context carry-forward

- 用户纠偏：使用 `wise-council-dialogue` 做策略裁决时，不能只输出裸裁决；必须带上项目背景、已完成事实、约束和当前疑问，否则后续读者无法判断裁决依据。
- 已更新 `/Users/linhey/.nolon/skills/wise-council-dialogue/SKILL.md`：新增 `Carry Background Forward` 门禁，并把最终裁决模板扩展为 `Background`，停止条件要求 final answer 包含 background/current truth。
- 本次不升级 `AGENTS.md`：这是外部通用 skill 的输出质量规则，项目侧通过 memory 保持偏好可追溯即可。

## UI migration functional review scope

- 用户纠偏：审查 Parchment token / Debug panel 迁移时，本轮只应判断对应功能有没有被改坏，不审查样式设计质量。
- 已将规则沉淀到 `.agents/skills/gettokens-frontend-design-quality/SKILL.md` 的 `Functional review scope for UI migrations`：当用户限定“只审功能”时，审查范围必须聚焦事件、状态、输入输出、disabled/readOnly、portal/overlay、选择/清空/复制/保存、hash/modal 和 Wails 调用保护；视觉 token、字号、颜色、阴影、圆角等只作为非阻塞备注，除非直接导致功能不可达。
- 验证口径：typecheck/build/相关 unit 或 DOM 断言可证明编译与行为链；完整测试若因脏工作区其它未提交文件失败，必须隔离说明，不能归因给当前 UI 迁移提交。

## BuilderIO/skills intake

- 用户要求评估并吸收 `https://github.com/BuilderIO/skills` 中对 GetTokens 有用的部分；结论是不全量安装，按外部 workflow intake admission gate 翻译为项目原生治理。
- 初始新增 `.agents/skills/gettokens-plan-arbiter/SKILL.md`：用于比较多个 agent、方案文档、PR 策略或设计方向，输出 Adopt / Hybrid / Revise first 决策；本轮后续已并入 `gettokens-ops-governance` 的 `Plan Arbitration`。
- 初始补强 `.agents/skills/gettokens-subagent-supervision/SKILL.md`：吸收 `agent-watchdog` 的 audit loop，要求重建原始请求、检查实际证据，并分类 gap / bug / verification miss / scope drift / no issue；本轮后续已并入 `gettokens-ops-governance` 的 `Subagent Delivery Loop`。
- 已补强 `.agents/skills/check/SKILL.md`：新增 `Large Change Recap Gate`，把 BuilderIO `visual-recap` 翻译成本地 review recap，不依赖 hosted Agent-Native Plan。
- 已更新 `.agents/skills/gettokens-ops-governance/SKILL.md` 与 `docs-linhay/dev/20260424-project-skills-overview.md`，并新增 `docs-linhay/dev/20260619-builderio-skills-intake.md` 记录 admission gate、已吸收和不吸收内容。
- 不吸收项：不采用 `quick-recap` emoji 状态行；不把 hosted Agent-Native Plan 作为默认计划/复盘依赖；`stay-within-limits` 暂无可验证一手 usage 工具，仅保留分 wave 和并行节流思想。

## Project skill discovery slimming

- 用户要求继续推进项目级 skills 合并/瘦身；当前第一轮只处理 Taste 外部包，不动 Waza 通用工程 skill，也不动 GetTokens 领域 skill。
- 已从 `.agents/skills/` 项目级 discovery 面移除 12 个视觉/图像生成类 Taste 子 skill：`taste-skill`、`taste-skill-v1`、`gpt-tasteskill`、`image-to-code-skill`、`imagegen-frontend-web`、`imagegen-frontend-mobile`、`brandkit`、`redesign-skill`、`soft-skill`、`minimalist-skill`、`brutalist-skill`、`stitch-skill`。
- 保留 `.agents/skills/output-skill`，因为它服务完整输出/防截断，和 GetTokens 前端设计统一入口不重叠。
- 后续 GetTokens 桌面/Wails 前端体验、设计审计、视觉 harden、Gemini handoff 默认走 `.agents/skills/gettokens-frontend-design-quality/`；原 Taste 内容只作为 `docs-linhay/references/taste-skill/skills/` 参考副本。
- 已更新 `.agents/skills/taste-skill.lock.json` active / retired discovery paths、`docs-linhay/dev/20260527-taste-skill-project-install.md`、`docs-linhay/dev/20260424-project-skills-overview.md`、`docs-linhay/references/20260604-frontend-design-reference-projects.md` 和 `gettokens-frontend-design-quality`。

## Workflow trigger skill consolidation

- 第二轮继续合并流程触发型 skills：删除 `external-workflow-intake`、`gettokens-session-skill-distill`、`gettokens-subagent-supervision`、`gettokens-plan-arbiter` 的独立 active 入口。
- 对应能力已并入 `.agents/skills/gettokens-ops-governance/SKILL.md`：`External Workflow Intake`、`Session Skill Distillation`、`Subagent Delivery Loop`、`Plan Arbitration`。
- 这次不是删除能力，而是把“如何工作”的治理触发统一收敛到 ops governance，减少 discovery 面和上下文预算压力。
- 更新 `AGENTS.md`、`docs-linhay/dev/20260424-project-skills-overview.md`、`docs-linhay/dev/20260616-agent-skill-operating-model.md`、`docs-linhay/dev/20260619-builderio-skills-intake.md` 以及旧 session distillation dev 文档中的当前入口。
